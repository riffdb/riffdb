spec_version: '1.30'
product: RiffDB
repository: riffdb
status: deployable-application-alpha-architecture-accepted
source_spec: SPEC.md
normative_terms:
- MUST
- MUST NOT
- SHOULD
- SHOULD NOT
- MAY
global_rules:
- Database semantics, protocols, transport trust decisions, and authoritative runtime code are first-party Rust;
  target-language generated application bindings require an accepted interface ADR and equivalent semantic tests.
- All application mutation passes through the shared application service and commit coordinator.
- No work package may weaken a normative guarantee without an accepted ADR.
- Every work package must map requirements to automated tests or an explicitly approved manual artifact.
- Generated sources and fixtures must be reproducible and checked for a clean worktree.
- Every root-level integration test is an explicit [[test]] target in exactly one owning crate manifest, and its
  acceptance command is package-qualified.
requirement_coverage:
  source: SPEC.md
  excluded_ids:
  - id: TXN-004
    reason: Illustrative identifier in the normative-language explanation, not a defined requirement.
governance:
  process_version: 2
  cutover: "2026-09-02"
  tiers_file: governance/tiers.yaml
  brief: ./scripts/brief
  acceptance: ./scripts/acceptance
gates:
- id: P0
  name: Executable semantics
  requires:
  - WP-030
  - WP-040
  - WP-060
  - WP-080
- id: P1
  name: Durable standalone
  requires:
  - WP-050
  - WP-065
  - WP-070
  - WP-090
  - WP-100
  - WP-110
  - WP-120
  - WP-127
  - WP-130
  - WP-137
  - WP-150
- id: P2
  name: POC exit
  requires:
  - WP-140
  - WP-160
  - WP-170
  - WP-180
  - WP-185
  - WP-155
  - WP-190
  - WP-200
- id: P3
  name: Symbolic application platform
  requires:
  - WP-205
  - WP-210
  - WP-220
  - WP-230
  - WP-240
  - WP-250
  - WP-260
  - WP-270
  - WP-275
  - WP-280
  - WP-285
  - WP-290
  - WP-295
  - WP-300
- id: P4
  name: Agent Application Alpha
  requires:
  - WP-305
  - WP-310
  - WP-315
  - WP-320
  - WP-325
  - WP-330
  - WP-335
  - WP-340
  - WP-345
  - WP-350
  - WP-355
  - WP-360
  - WP-362
  - WP-364
  - WP-365
  - WP-367
  - WP-368
  - WP-369
  - WP-371
  - WP-372
  - WP-373
  - WP-374
  - WP-375
  - WP-370
- id: P5
  name: Python application driver
  requires:
  - WP-394
  - WP-395
  - WP-396
  - WP-397
  - WP-398
- id: P6
  name: Public documentation handbook
  requires:
  - WP-401
  - WP-402
  - WP-403
  - WP-404
- id: P7
  name: Robust offline contract migration
  requires:
  - WP-405
  - WP-406
  - WP-407
  - WP-408
  - WP-409
  - WP-410
  - WP-411
  - WP-412
  - WP-413
- id: P8
  name: Reactive applications
  requires:
  - WP-414
  - WP-415
  - WP-416
  - WP-417
  - WP-418
  - WP-419
  - WP-420
  - WP-421
- id: P9
  name: Public marketing website
  requires:
  - WP-495
- id: P10
  name: Deployable Application Alpha
  requires:
  - WP-550
  - WP-551
  - WP-552
  - WP-553
  - WP-554
  - WP-555
  - WP-556
  - WP-557
  - WP-558
  - WP-559
  - WP-560
  - WP-561
  - WP-562
  - WP-563
  - WP-564
  - WP-565
  - WP-566
  - WP-567
  - WP-568
  - WP-569
  - WP-570
  - WP-572
  - WP-573
  - WP-574
  - WP-575
  - WP-576
  - WP-577
  - WP-578
  - WP-579
- id: P11
  name: Repository ownership notices
  requires:
  - WP-571
- id: P12
  name: Consolidation and availability program
  requires:
  - WP-746
  - WP-747
  - WP-748
  - WP-749
  - WP-750
  - WP-751
  - WP-752
  - WP-753
  - WP-754
  - WP-755
  - WP-756
  - WP-757
  - WP-758
  - WP-759
  - WP-760
  - WP-761
  - WP-762
  - WP-763
  - WP-764
  - WP-765
  - WP-766
  - WP-767
  - WP-772
  - WP-773
  - WP-774
- id: P13
  name: Relational operators in the candidate algebra
  requires:
  - WP-768
  - WP-769
  - WP-770
  - WP-771
work_packages:
- id: WP-000
  closure:
    status: complete
    evidence: Cargo.toml
    decision: The standalone pinned Rust workspace, crate graph, policy checks, CI scripts, licenses, and deny-by-default lint/dependency boundaries are established. Current workspace policy, formatting, Clippy, tests, docs, generation, and cargo-deny checks pass.
  name: Repository foundation
  depends_on: []
  objective: Create the reproducible Rust workspace, quality gates, ADR workflow, and generation checks used by
    every later package.
  requirements:
  - SYS-001
  - SYS-002
  - SYS-003
  - REP-001
  - POC-010
  required_adrs: []
  allowed_paths:
  - .gitignore
  - Cargo.toml
  - Cargo.lock
  - rust-toolchain.toml
  - deny.toml
  - .github/**
  - scripts/**
  - adr/**
  - crates/*/Cargo.toml
  - crates/*/src/**
  deliverables:
  - Workspace crate skeletons
  - Pinned Rust toolchain
  - Formatting, Clippy, test, docs, dependency-policy CI
  - ADR template and PR contract
  - Deterministic generated-artifact check
  - Machine-checked normative requirement coverage for work_packages.yaml
  acceptance_commands:
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace
  - cargo deny check
  - ./scripts/check-requirement-coverage
  exit_gate: Clean CI on the skeleton workspace from a fresh checkout.
  human_review_triggers: &id001
  - A normative transaction, durability, authorization, or MCP guarantee would change.
  - A durable format, public protocol, or contract IR change is incompatible.
  - Unsafe Rust, native code, cryptography, or a new critical dependency is required.
  - Implementation needs files outside allowed_paths or an unmet dependency interface.
- id: WP-010
  closure:
    status: complete
    evidence: crates/riffdb-types
    decision: Domain newtypes, exact values, checked identifiers, bounded collections, canonical ordering, error taxonomy, redaction-safe public errors, and compatibility fixtures are implemented and pass the complete workspace suite.
  name: Core types and error taxonomy
  depends_on:
  - WP-000
  objective: Define canonical domain newtypes, values, versions, timestamps, decimals, safe public errors, and internal
    error layering.
  requirements:
  - SYS-003
  - ID-001
  - ID-002
  - ID-003
  - ID-004
  - ID-005
  - VAL-001
  - VAL-002
  - STO-002
  required_adrs:
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0011
  - ADR-0012
  - ADR-0013
  - ADR-0014
  - ADR-0016
  - ADR-0017
  - ADR-0018
  - ADR-0021
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-types/**
  - crates/riffdb-errors/**
  - crates/riffdb-testkit/src/canonical/**
  - proto/riffdb/v1/error.proto
  - crates/riffdb-proto/src/generated/riffdb.v1.rs
  - crates/riffdb-proto/src/command.rs
  - crates/riffdb-proto/src/public_error.rs
  - crates/riffdb-proto/src/value.rs
  - crates/riffdb-proto/src/wire.rs
  - crates/riffdb-proto/examples/generate_proto.rs
  - crates/riffdb-proto/tests/schema.rs
  - crates/riffdb-proto/tests/wire_vectors.rs
  - fixtures/proto/descriptors/riffdb-v1-descriptor-set.bin
  - fixtures/proto/schema-inventory.txt
  - fixtures/proto/wire-vectors.txt
  deliverables:
  - ID and version newtypes
  - Fixed-scale decimal representation
  - Canonical value and hashing rules
  - Typed partition/index keys and command/projection/root hash domains
  - Timestamp and logical-time types
  - Public safe error taxonomy
  - Consumer-owned synchronous IncidentIdSource interface in riffdb-errors, with bounded static source failures and
    no concrete clock, entropy provider, fallback identifier, or runtime dependency
  - Nonzero assigned CommitSequence, AdministrationSequence, EntityVersion, and IndexEpoch types with explicit
    before-first FrontierPosition, fallible EventId decoding, and zero-rejection fixtures
  - One-based nonzero ContractVersion and compiler-assigned EntityTypeId, FieldId, CommandId, OutcomeId, ProjectionId,
    EventTypeId, IndexId, EnumTypeId, EnumVariantId, AggregateTypeId, and InvariantId with fallible construction and
    zero-rejecting decoders
  - Nonzero DigestKeyId plus phase-zero command/value decoder rejection of zero ContractVersion, CommitSequence,
    FieldId, EnumTypeId, and EnumVariantId without adding another schema symbol
  - Shared service-operation, audit-phase, ingress, and bounded admitted-provenance component vocabulary, plus the
    exact ADR-0021 nine-variant lineage-scoped ServiceAuditTargetV1 tags, canonical key/order, duplicate rejection,
    and zero-through-16 checked collection
  - Foundational ActorKind, Audience, ApprovalId, CapabilityTokenDigest, capability-token hash domain, bounds,
    redaction, and canonicalization fixtures
  - AdmittedActorContext, nonzero ExecutionFailureCode, CommandExecutionFailed, fixed execution budgets, and safe
    mapping/redaction fixtures
  - The exact additive ADR-0012 CommandExecutionFailed error.proto symbols, generated artifacts, safe domain mapping,
    preflight validation, and golden fixtures; no other public or durable schema change
  - ProjectionIdentity, ProjectionGeneration, projection group/prefix/frontier/apply-key builders,
    ProjectionApplyHash, and riffdb.projection-apply/v1 golden cross-domain fixtures
  - Pure checked UUIDv7 assembly for every shared UUID identifier, exact ADR-0018 byte golden, timestamp overflow
    rejection, and validation proving riffdb-types has no ambient clock/entropy dependency
  acceptance_commands:
  - cargo test -p riffdb-types -p riffdb-errors -p riffdb-proto
  - cargo clippy -p riffdb-types -p riffdb-errors -p riffdb-proto --all-targets -- -D warnings
  - ./scripts/generate-proto --check
  exit_gate: Canonicalization, ordering, decimal, assigned-type zero rejection, hash-domain separation, and
    error-redaction property tests pass.
  human_review_triggers: *id001
- id: WP-687
  name: Operational access-path registry and binary text membership
  depends_on: [WP-564, WP-565, WP-685, WP-686]
  objective: Implement ADR-0150's single compiler-owned operational component-capability registry,
    audit every admitted non-equality ordinary plan for pre-page physical enforcement, fail unsafe
    legacy plans closed, and prove bounded binary_utf8_v1 membership plus bytewise order through one
    shared index without adding a durable format or framework-specific branch.
  requirements: [OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-007,
    OQ-008, OQ-009, OQ-015, OQ-032, OQ-033, OQ-034, OQ-035, OQ-036, OQ-037,
    OQ-039, OQ-040, OQ-043]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124, ADR-0129, ADR-0130, ADR-0133, ADR-0134, ADR-0145,
    ADR-0150]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0150-compiler-sealed-operational-access-path-algebra.md,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-testkit/**, fixtures/riffql/**, fixtures/query-modules/**,
    tests/operational_queries.rs, tests/**, scripts/**, docs/SUMMARY.md,
    docs/riffql/**, docs/getting-started/OPERATIONAL-QUERIES.md,
    docs/known-limitations.md]
  deliverables:
  - One closed table-driven registry mapping canonical, presence-aware, binary_utf8_v1, and
    unavailable unicode_fold_v1 components to their exact selection/order role combinations, used
    by planner eligibility rather than duplicated encoding-specific conditionals
  - One compiler-sealed transient access shape and pay-once lowering path carrying partition and
    exact prefixes, at most one branch, complete order/tie-breaker, physical profile, range and work
    bounds, and complete authority without changing grammar, query IR, cursor, storage, wire, or
    generated identities
  - Repository-wide inventory of ordinary non-equality predicates proving physical enforcement
    before page and continuation selection; every unproved plan fails source compilation, activation,
    or pre-storage execution through the accepted typed unavailable path with rebuild guidance
  - Bounded in membership on a binary_utf8_v1 component that also supplies the first remaining
    order term, encoding each canonical distinct member once and traversing disjoint exact prefixes
    under one page, plus-one probe, scan/fuel, cursor, and output budget
  - Shared memory/redb forward and reverse range-boundary cursor behavior over length-divergent text,
    plus architecture tests for one registry/lowering owner, no per-row validation, no residual
    application predicate, and no duplicate-index or external-framework workaround
  - Public operational-query and known-limitation documentation describing the executable matrix,
    typed refusal/rebuild path, bytewise membership/order semantics, and explicitly deferred range,
    multi-branch, join, provider-bridge, and unicode-fold cells
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-executor --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test operational_queries --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every executable ordinary predicate is physically enforced before page/cursor
    formation or refused before storage; one binary_utf8_v1 index serves exact equality, bounded
    membership, prefix, and compatible bytewise orders with exact cursor parity and global work
    bounds; old proved artifacts remain exact; and no scan, residual filter, duplicate index,
    framework branch, provider bridge, or new durable identity lands.
  standing_design_tests:
    interface_safety: Applications still invoke finite generated named operations with typed bounded
      values and opaque cursors. They cannot select an index, encoding, role, physical range, merge,
      cost, policy, storage path, or fallback, and every incomplete shape fails closed before work.
    scale: Registry work is bounded by declared plan fields; request lowering is bounded by compiled
      set count and bytes; all ranges share one finite scan/page/fuel/output budget; no population-
      sized materialization, page walk, database-wide memory, or per-row proof is introduced.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006,
      OQ-007, OQ-008, OQ-009, OQ-015, OQ-032, OQ-033, OQ-034, OQ-035, OQ-036,
      OQ-037, OQ-039, OQ-040, OQ-043]
    evidence: Commit 0547d368 introduced the compiler registry, sealed access shape, pay-once
      executor range schedule, binary-text membership/order tests, shared memory/redb cursor
      contract, and public operational-query documentation; those retained proofs pass the package
      acceptance commands on the current tree.
    known_limitations: WP-687 itself deliberately did not activate canonical scalar intervals,
      multiple branching components, relationship composition, provider bridges, or unicode-fold
      ordinary access; later accepted successor work may add independently governed cells without
      changing this retained binary-membership proof.
    follow_ups: [WP-688, WP-689, WP-690]
  human_review_triggers: *id001
- id: WP-688
  name: Typed physical intervals and ordinary access-path consolidation
  tier: guarantee
  depends_on: [WP-687]
  objective: Complete ADR-0150's ordinary access-path matrix by installing physically enforced
    canonical range, inequality, and finite complement intervals where the ordered shape is proved,
    consolidating compiler/executor/memory/redb range semantics without expanding accepted language
    vocabulary, adding general unions, or changing durable identities.
  requirements: [OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-010, OQ-032,
    OQ-033, OQ-034, OQ-036, OQ-037, OQ-038, OQ-039, OQ-043]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124, ADR-0129, ADR-0134, ADR-0145, ADR-0150, ADR-0189]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0150-compiler-sealed-operational-access-path-algebra.md,
    crates/riffdb-catalog/**, crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-testkit/**, fixtures/riffql/**, tests/**, scripts/**,
    docs/SUMMARY.md, docs/riffql/**, docs/getting-started/OPERATIONAL-QUERIES.md,
    docs/known-limitations.md]
  deliverables:
  - Typed inclusive/exclusive canonical interval lowering for accepted range and inequality cells,
    plus finite disjoint complement intervals only when the selected component supplies the first
    remaining order term and every logical predicate is consumed before page formation
  - One shared range schedule and forward/reverse continuation truth table used by memory and redb,
    with global probes, scan fuel, plus-one, cursor, row-policy, hydration, and output accounting
  - Complete canonical, presence-aware, and binary-text capability matrix conformance and deletion
    of duplicated semantic branches/tests that can drift from the registry
  - Negative evidence for multiple branching dimensions, overlapping unions, residual predicates,
    logical/text comparator substitution, skip scans, intersections, adaptive planning, and bitmap
    bridges
  - Updated handbook and known limitations with every active and unavailable matrix cell
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-compiler -p riffdb-query-executor --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every accepted canonical range/inequality/complement plan constrains storage before
    page and cursor formation with exact memory/redb parity and one global cost; every unproved or
    multi-branch shape fails at a source span; and no old artifact, durable key, provider bridge, or
    public unsafe surface changes.
  standing_design_tests:
    interface_safety: Callers provide only values for a compiled operator family and cannot choose
      intervals, branches, encodings, indexes, directions, merge strategies, costs, or fallback.
    scale: At most one branching/interval component forms a compiler-bounded disjoint schedule under
      one scan/page/output budget, with no unbounded union, intersection, or materialization.
  closure:
    status: complete
    completed_at: '2026-09-03'
    requirements_satisfied: [OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-010,
      OQ-032, OQ-033, OQ-034, OQ-036, OQ-037, OQ-038, OQ-039, OQ-043]
    evidence: The three named ADR-0189 obligation proofs cover canonical byte and hash invariance,
      complete descriptor mismatch refusal before storage, and shared unicode_fold_v1 lowering;
      package acceptance passed all 8 stages and 290 tests, and generated artifacts remain current.
    known_limitations: Multiple branching dimensions, overlapping unions, skip scans, intersections,
      adaptive planning, bitmap bridges, caller-selected fallback, and undeclared profiles remain
      unavailable and fail closed.
    follow_ups: [WP-689, WP-690]
  human_review_triggers: *id001
- id: WP-689
  name: Bounded relationship access-shape inventory and conformance
  depends_on: [WP-687]
  objective: Audit existing exact point dependencies and bounded dependent complete-key batches
    against ADR-0150's relationship-composition invariants, centralize generic conformance where
    needed, and document the extension boundary without adding join syntax or a runtime operator.
  requirements: [OQ-033, OQ-039, OQ-040, OQ-041, OQ-042, OQ-043]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0054, ADR-0055,
    ADR-0108, ADR-0111, ADR-0124, ADR-0129, ADR-0150]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0150-compiler-sealed-operational-access-path-algebra.md,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-testkit/**, fixtures/riffql/**, tests/**, scripts/**,
    docs/architecture/**, docs/riffql/**, docs/known-limitations.md]
  deliverables:
  - Inventory mapping every existing relationship read and dependent complete-key batch to declared
    same-partition key mappings, driver and fan-out bounds, intermediate/probe/byte/output costs,
    missing/duplicate/null semantics, authority, explain output, and plan identity
  - Generic conformance for accepted point and dependent-batch behavior across memory/redb, including
    policy denial, duplicates, missing targets, maximum counts/bytes, cancellation, and cursor-free
    bounded completion
  - Architecture negatives proving no semijoin, correlated existence, bounded one-to-many expansion,
    arbitrary/cross-partition join, Cartesian product, recursion, materialize-all, or runtime optimizer
    was introduced
  - Documentation defining the real-consumer amendment and independent bound/evidence required for a
    future relationship access shape
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-compiler -p riffdb-query-executor -p riffdb-testkit --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every existing composed relationship read has complete same-partition, cardinality,
    cost, policy, and result evidence; unsupported composition remains unexpressible; and no new
    syntax, operator, join planner, durable identity, or application-programmable structure lands.
  standing_design_tests:
    interface_safety: Relationship shape remains compiler-declared and callers cannot supply join
      conditions, targets, fan-out, traversal, missing semantics, costs, or policy bypasses.
    scale: Driver rows, per-edge fan-out, total targets/intermediates/probes/bytes/output are finite
      and independently charged; no cross-partition or unbounded traversal exists.
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [OQ-033, OQ-039, OQ-040, OQ-041, OQ-042, OQ-043]
    evidence: Historical commit 25ecbc24 introduced the relationship inventory, dependent-batch
      executor evidence, architecture negatives, and extension guidance. This closure strengthens
      the frozen inventory with same-partition, probe, intermediate, projected-value, result-byte,
      authority, and explain assertions; adds executable position, absence, and policy conformance
      against memory and redb; and carries post-cutover requirement tags. The complete package
      acceptance matrix passes on the current successor tree.
    known_limitations: Dependent batches remain ordered, cursor-free, complete-primary-key reads.
      Later one-to-many expansion is independently governed by accepted ADR-0185 and is not part of
      this package's runtime or identity surface.
    follow_ups: [WP-690]
  human_review_triggers: *id001
- id: WP-690
  name: Operational access-path generic and external acceptance
  depends_on: [WP-688, WP-689]
  objective: Close ADR-0150 with one generic index-rich application corpus, external-consumer reruns,
    compatibility and architecture evidence, and public documentation, while retaining every external
    adapter artifact in its owning repository.
  requirements: [OQ-032, OQ-033, OQ-034, OQ-035, OQ-036, OQ-037, OQ-038,
    OQ-039, OQ-040, OQ-041, OQ-042, OQ-043]
  required_adrs: [ADR-0055, ADR-0108, ADR-0111, ADR-0124, ADR-0129,
    ADR-0130, ADR-0150]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0150-compiler-sealed-operational-access-path-algebra.md,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-testkit/**, fixtures/**,
    examples/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Generic application corpus covering compatible equality, membership, prefix, range, nullable
    order, forward/reverse cursor, policy, exact bounds, and dependent-key shapes over shared indexes
  - Fresh external tuple-consumer acceptance proving bytewise bounded membership removes duplicate-
    index pressure, recorded only as a value-free capability receipt in RiffDB
  - Compatibility evidence for unchanged proved artifacts and typed inventory/redeployment guidance
    for every refused historically unproved ordinary plan
  - Final handbook, architecture, extension-rule, limitation, and requirement-coverage closure with
    no external schema, route, adapter, generated profile, or false framework acceptance claim
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Generic and external evidence proves the complete capability matrix, range/cursor and
    relationship contracts, compatibility repair, policy, and global bounds with no duplicate index,
    scan, client shaping, unsafe join, provider bridge, framework artifact, or unexplained failure.
  standing_design_tests:
    interface_safety: All evidence uses generated named operations and typed values; no application
      surface can select structure, access paths, policy, cost, provider, or fallback.
    scale: Every corpus dimension and external loopback is compiler-bounded and partition-local; no
      full population, cross-provider, cross-partition, or duration-proportional state is retained.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-032, OQ-033, OQ-034, OQ-035, OQ-036, OQ-037,
      OQ-038, OQ-039, OQ-040, OQ-041, OQ-042, OQ-043]
    evidence: Commit 2f3b93c2 closes the generic operational-access corpus, four generated
      languages plus MCP, exact memory/redb semantics, handbook guidance, compatibility receipt,
      and the value-free external tuple-store receipt at
      fixtures/riffql/wp690-external-tuple-capability-v1.json. This closure metadata was restored
      during WP-733's hard-dependency audit; no WP-690 implementation changed.
    known_limitations: The accepted semijoin/candidate extension remained intentionally unavailable
      until ADR-0174 and WP-734.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-691
  name: Atomic unary delete and revalidated preimage outcome
  depends_on: [WP-561, WP-630]
  objective: Implement ADR-0151's one exact-key ordinary no_inbound delete whose requirements,
    explicit events, and success outcome read the same revalidated preimage carried by the atomic
    delete mutation, with exact concurrency, idempotency, secret, and typed diagnostic semantics.
  requirements: [BLK-028, BLK-029, BLK-030, BLK-031, BLK-032, BLK-033,
    BLK-034, BLK-035, TXN-001, TXN-010, TXN-011, TXN-012, TXN-013]
  required_adrs: [ADR-0002, ADR-0003, ADR-0005, ADR-0012, ADR-0023,
    ADR-0031, ADR-0055, ADR-0059, ADR-0107, ADR-0124, ADR-0126, ADR-0129,
    ADR-0151]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0151-atomic-delete-and-return-preimage.md,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-invariant/**,
    crates/riffdb-runtime/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-policy/**, crates/riffdb-testkit/**,
    fixtures/**, tests/**, scripts/**, docs/contracts/**, docs/known-limitations.md]
  deliverables:
  - Checked ordinary command construction accepts exactly one complete-key partition-local delete
    only for no_inbound policy, records every preimage/access/reveal dependency, forbids set and
    duplicate-target aliases, and gives source-spanned diagnostics for every unsupported shape
  - Ordinary deterministic runtime materializes the exact present preimage once, uses it for
    requirements/events/outcome and the delete mutation prior image, and returns the declared
    missing outcome with zero mutation for absence
  - Coordinator validation and deterministic schedules prove an update winner rebuilds from the
    updated preimage, a delete/delete race commits one consumed and one missing result, and no stale
    evaluated outcome becomes durable or releasable
  - Idempotent replay and uncertain recovery return the persisted consumed/missing outcome without
    current-state reads or repeated delete; memory/redb mutation, index removal, history, changelog,
    provenance, event, and commit sequencing remain atomic and exact
  - Secret reveal grant/deny/revoke/redaction and architecture evidence proves delete authority does
    not imply output authority and values never enter diagnostics, logging, metrics, or MCP text
  - Existing valid command/IR/durable bytes remain exact and old runtimes fail closed on the newly
    admitted combination; no new identity is added unless ADR-0124 review proves it necessary
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler --all-features
  - cargo +1.97.0 test -p riffdb-runtime -p riffdb-commit -p riffdb-policy --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One ordinary no_inbound delete atomically returns only the exact transaction-current
    preimage it removes, exactly one concurrent consumer wins, update/delete and replay are exact,
    secret authority remains explicit, unsupported shapes are source diagnostics, and no transaction,
    second read, stale outcome, framework branch, or new unreviewed identity exists.
  standing_design_tests:
    interface_safety: The caller invokes one generated exact-key command and cannot select a target,
      delete policy, transaction, return field, reveal, validation, retry, durability, or fallback.
    scale: The unary path reads/deletes one partition-local entity and returns one schema-bounded
      outcome with no scan, collection growth, cross-partition work, or row-sized retained state.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [BLK-028, BLK-029, BLK-030, BLK-031, BLK-032,
      BLK-033, BLK-034, BLK-035, TXN-001, TXN-010, TXN-011, TXN-012, TXN-013]
    evidence: Commit 8a40ee9f introduced the checked ordinary no_inbound delete shape, exact
      preimage runtime materialization, delete/outcome transaction-current validation, typed
      diagnostics, secret reveal metadata, and deterministic update/delete and delete/delete
      schedules. Commit a1dd0fbe retained the public generated surfaces and adds the redb
      race/reopen proof in unary_delete_race_commits_one_preimage_and_replays_after_reopen; the
      existing shared EntityMutation::Delete storage contract supplies identical memory/redb atomic
      application, history, changelog, provenance, event, index-removal, and replay semantics.
    known_limitations: The ordinary path remains exactly one complete-key partition-local
      no_inbound delete. Indexed restrict and cascade deletion remain compiler-bounded collection
      operations, and no physical-erasure or caller-selected delete surface is introduced.
    follow_ups: [WP-692]
  human_review_triggers: *id001
- id: WP-692
  name: Unary delete recovery generated surfaces and external acceptance
  depends_on: [WP-691]
  objective: Carry ADR-0151's unary preimage-return semantics through crash recovery, service and all
    generated application surfaces, generic one-time-token evidence, handbook guidance, and a fresh
    external consumer loopback without adding framework code to RiffDB.
  requirements: [BLK-031, BLK-032, BLK-033, BLK-034, BLK-035, API-001,
    MCP-001, MCP-020, MCP-021, MCP-023]
  required_adrs: [ADR-0055, ADR-0074, ADR-0106, ADR-0107, ADR-0111,
    ADR-0120, ADR-0124, ADR-0128, ADR-0151]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0151-atomic-delete-and-return-preimage.md, proto/**, fixtures/**,
    crates/riffdb-types/**, crates/riffdb-query-module/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-mcp-stdio/**, crates/riffdb-server/**, clients/**, templates/**,
    examples/**, tests/**, scripts/**, docs/**, release/distribution/**,
    release/version-topology-v1.json]
  deliverables:
  - Process crash and restart matrix around evaluation, staging, durability fence, publication,
    response, and replay proving one persisted outcome and complete-or-absent delete visibility
  - Identical Rust, Go, TypeScript, Python, CLI, and MCP generated command schemas, preimage results,
    secret metadata, missing outcomes, public errors, and uncertainty recovery
  - Framework-neutral one-time-token example and deterministic concurrent consume corpus over real
    memory/redb service paths, including update/delete, reveal revocation, and restart replay
  - Fresh external consumer loopback receipt proving the real atomic consume shape while retaining
    all external schemas, adapter code, routes, generated profiles, and authentication policy outside
    RiffDB
  - Handbook and generated-reference closure describing exact preimage, concurrency, replay, secret,
    compatibility, and initial no_inbound-only limitation
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-service -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-client-rust -p riffdb-driver-host -p riffdb-cli -p riffdb-server --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public surface and restart path returns the same authorized exact deleted preimage
    or missing outcome with atomic durability and replay, the external loopback passes, and RiffDB
    contains no framework schema, adapter, route, generated profile, or authentication behavior.
  standing_design_tests:
    interface_safety: Generated surfaces expose one compiled operation and closed outcome union only;
      no binding can express a transaction, raw delete, secret widening, weaker durability, or retry.
    scale: Requests, outcomes, transport frames, waits, retries, crash evidence, and generated buffers
      retain fixed ceilings; no consumer walk, scan, or retained token population is introduced.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [BLK-031, BLK-032, BLK-033, BLK-034, BLK-035,
      API-001, MCP-001, MCP-020, MCP-021, MCP-023]
    evidence: Commit a1dd0fbe completed the generated Rust, Go, TypeScript, Python, CLI, and MCP
      surfaces and froze the generic one-time-token contract, exact command identities, value-free
      secret metadata, and external four-language TLS receipt. The
      collection_create_and_delete_are_complete_or_absent_across_process_crash matrix includes unary
      consume before and after the durability boundary; unary_delete_race_commits_one_preimage_and_replays_after_reopen
      proves one redb winner, one missing result, exact replay, and absent current state after reopen.
      policy_revocation_between_outcome_checks_withholds_the_present_result proves fresh
      release-time authorization, while shared application-service and EntityMutation::Delete
      conformance retain the same authorization, atomic graph, and memory/redb storage paths.
    known_limitations: Ordinary unary delete remains single-target, exact-key, partition-local, and
      no_inbound only. The receipt is value-free and binds the four external runner sources and
      generated artifacts; framework schema, routes, adapters, profiles, and identity policy remain
      external.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-693
  name: Aggregate semantic registry and provider descriptor sketches
  depends_on: [WP-492, WP-565, WP-645]
  objective: Inventory every aggregate spelling, IR tag, evaluator, provider, result, wire, and
    generated surface; install ADR-0152's one semantic capability registry without changing existing
    bytes; and validate future variance, approximate-cardinality, and percentile/histogram shapes on
    paper without runtime or persisted speculative state.
  requirements: [OQ-011, OQ-012, OQ-013, OQ-017, OQ-022, OQ-024, OQ-044,
    OQ-045, OQ-049, OQ-050, OQ-052, OQ-053, OQ-054]
  required_adrs: [ADR-0051, ADR-0053, ADR-0055, ADR-0086, ADR-0087,
    ADR-0108, ADR-0111, ADR-0124, ADR-0129, ADR-0130, ADR-0131, ADR-0150,
    ADR-0152]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0152-closed-exact-aggregate-function-families.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-executor/**,
    crates/riffdb-query-module/**, crates/riffdb-projection/**,
    crates/riffdb-columnar/**, fixtures/**, tests/**, scripts/**,
    docs/architecture/**, docs/riffql/**]
  deliverables:
  - Complete inventory and architecture checks tying every existing count/exact_count/sum/min/max/
    group-by parser, IR, evaluator, provider, result, conversion, generator, and fixture arm to one
    compiler-owned semantic registry
  - Registry descriptors freezing input, NoValue, empty, result, partial-state, arithmetic, bounded/
    grouped/whole-result eligibility, budgets, and policy class while keeping existing plan hashes,
    query modules, wire values, generated outputs, and provider state byte-exact
  - Explicit transient-versus-release-significant classification for registry and partial-state data,
    with ADR-0124 topology registration only where an executable or persisted identity requires it
  - Non-normative checked sketches for exact variance, approximate cardinality, and percentile or
    histogram descriptors naming numerical/quality/state/epoch/policy/bound semantics and producing
    no runtime, sketch dependency, bridge, or persisted artifact
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-executor -p riffdb-query-module -p riffdb-projection -p riffdb-columnar --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One registry accounts for every existing aggregate semantic arm without byte or behavior
    drift, future statistical shapes fit on paper, durable identities are classified, and no public
    function, runtime sketch, bridge, dependency, or persisted speculative state lands.
  standing_design_tests:
    interface_safety: Registry descriptors are compiler artifacts and callers cannot select a
      function, field, precision, approximation, state, provider, policy, epoch, bound, or fallback.
    scale: Registry and descriptor validation are fixed/pay-once; sketches allocate no runtime state;
      existing bounded/grouped/provider work retains its current finite limits.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-011, OQ-012, OQ-013, OQ-017, OQ-022, OQ-024,
      OQ-044, OQ-045, OQ-049, OQ-050, OQ-052, OQ-053, OQ-054]
    evidence: One closed transient registry now owns the five existing RiffQL aggregate semantic
      descriptors and is consumed by source-to-semantic resolution, frozen operational tags,
      compiler cost derivation, authoritative execution, module explanation, and the existing
      columnar engine. Checked architecture evidence inventories parser/formatter, exact-count
      providers, group-by, result, wire, generated, fixture, and separate durable materialized-
      projection arms. Exact variance, approximate-cardinality, and percentile/histogram sketches
      cover numerical, quality, state, epoch, policy, bound, and durable-identity dimensions only on
      paper. Package tests, topology, generated, coverage, policy, formatting, and workspace Clippy
      gates pass without changing existing durable bytes or adding a topology identity.
    known_limitations: The registry describes only the five already executable semantics; ADR-0152's
      additional exact core remains WP-694. Materialized projection Count/Sum state remains a
      separately versioned write-time DSL and is not advertised as a RiffQL provider capability.
    follow_ups: [WP-694, WP-695, WP-696]
  human_review_triggers: *id001
- id: WP-694
  name: Exact aggregate core functions and generated result algebra
  depends_on: [WP-693]
  objective: Add ADR-0152's count_present, exact distinct counts, ExactMeanV1, any, and all through
    grammar, IR, reference and bounded/grouped execution, result carriage, and generated clients with
    exact NoValue, empty, arithmetic, state, and independent budget semantics.
  requirements: [OQ-011, OQ-012, OQ-013, OQ-044, OQ-045, OQ-046, OQ-047,
    OQ-048, OQ-049, OQ-050, OQ-051, OQ-053, OQ-055]
  required_adrs: [ADR-0055, ADR-0086, ADR-0087, ADR-0108, ADR-0111,
    ADR-0124, ADR-0129, ADR-0152]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0152-closed-exact-aggregate-function-families.md, proto/**,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-executor/**,
    crates/riffdb-query-module/**, crates/riffdb-columnar/**,
    crates/riffdb-projection/**, crates/riffdb-service/**, crates/riffdb-proto/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    clients/**, templates/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Additive syntax/formatter/resolved-IR identities and source diagnostics for count_present,
    count_distinct, count_distinct_present, mean, any, and all with old aggregate artifacts exact
  - Exact reference and bounded/grouped evaluators implementing NoValue inclusion/exclusion, empty
    identities, canonical distinct equality, checked state growth, Boolean required-field rules, and
    ExactMeanV1 total/count merge semantics without hidden division or floating point
  - Independent row/group/distinct/state/arithmetic/output budgets and typed whole-query refusal with
    no partial aggregate release
  - Complete service, wire, Rust/Go/TypeScript/Python, CLI, and MCP result schemas and decoders sharing
    one golden corpus and no target-language semantic implementation
  - Version-topology, old/new reader, plan/module, generated, and documentation rotation for every
    release-significant successor
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-executor -p riffdb-query-module --all-features
  - cargo +1.97.0 test -p riffdb-columnar -p riffdb-projection -p riffdb-service -p riffdb-proto -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-client-rust -p riffdb-driver-host -p riffdb-cli --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every new exact function has identical compiler/reference/runtime/provider/wire/generated
    semantics and independent bounds, old artifacts remain exact, partial results never release, and
    no approximation, implicit rounding, callback, framework branch, or unsafe fallback exists.
  standing_design_tests:
    interface_safety: Functions, fields, population, precision, grouping, provider, policy, and
      budgets are compiled; callers submit values only and cannot select an unsafe fold or fallback.
    scale: Source rows, groups, distinct values, state bytes, arithmetic, scans, cells, and output
      are independently finite; no page walk, unbounded set, or database-wide memory is introduced.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-011, OQ-012, OQ-013, OQ-044, OQ-045, OQ-046,
      OQ-047, OQ-048, OQ-049, OQ-050, OQ-051, OQ-053, OQ-055]
    evidence: RiffQL language V8, query IR V11, and query module V11 add count_present,
      count_distinct, count_distinct_present, mean, any, and all through the shared semantic
      registry. Compiler-owned type checks and independent row, group, distinct, state,
      arithmetic, and output budgets seal every plan. Authoritative and columnar evaluators share
      exact NoValue, empty-set, canonical-distinct, Boolean, and ExactMeanV1 total/count semantics,
      with typed whole-result refusal before release. Service, gRPC, MCP, CLI, and generated Rust,
      Go, TypeScript, and Python surfaces carry the structural mean result without floating point
      or target-language aggregation logic. Focused package suites, remote driver conformance,
      topology, generated-artifact, requirement-coverage, workspace-policy, handbook, formatting,
      and workspace Clippy gates pass.
    known_limitations: Matched-epoch provider capability advertisement and combined page, total,
      facets, and aggregate proofs remain WP-695. Differential recovery, security, compatibility,
      and external generic acceptance remain WP-696. Approximate and advanced statistical families
      remain descriptor sketches only and have no executable or persisted state.
    follow_ups: [WP-695, WP-696]
  human_review_triggers: *id001
- id: WP-695
  name: Aggregate columnar and exact-provider capability integration
  depends_on: [WP-694]
  objective: Exercise ADR-0152's registry through the existing columnar and exact-result provider
    families at matched policy-bound epochs, advertising only exactly supported functions and adding
    no cross-provider bridge or speculative statistical state.
  requirements: [OQ-013, OQ-017, OQ-019, OQ-020, OQ-021, OQ-022, OQ-024,
    OQ-044, OQ-046, OQ-047, OQ-048, OQ-049, OQ-050, OQ-051, OQ-052, OQ-053,
    OQ-055]
  required_adrs: [ADR-0086, ADR-0087, ADR-0108, ADR-0111, ADR-0124,
    ADR-0129, ADR-0130, ADR-0131, ADR-0152]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0152-closed-exact-aggregate-function-families.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-query-module/**,
    crates/riffdb-catalog/**, crates/riffdb-projection/**,
    crates/riffdb-columnar/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-service/**, fixtures/**, tests/**,
    scripts/**, docs/architecture/**, docs/riffql/**]
  deliverables:
  - Columnar and exact-result descriptors advertise only exact registry functions each provider can
    serve within partition/policy, epoch, retained-state, scan, distinct, group, and output bounds
  - Matched-epoch independent reference equivalence for whole-result and grouped functions under
    writes, deletes, rebuild, compaction, lag, retention, policy denial, and maximum bounds
  - One epoch proof for combined page, total, facets, and aggregate measures with fresh authorization
    and no page fold, cursor walk, post-policy contribution, mixed epoch, or unbounded materialization
  - Pay-once descriptor/partial-state compatibility evidence and topology fixtures for any persisted
    rebuildable state, with no cross-provider bridge or variance/sketch/percentile runtime
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-compiler -p riffdb-query-executor -p riffdb-catalog -p riffdb-projection -p riffdb-columnar -p riffdb-policy -p riffdb-service --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Both provider families match the exact registry and reference at one policy-bound epoch,
    unsupported capabilities fail closed, persisted state is governed, and no page fold, scan
    fallback, mixed epoch, bridge, or speculative analytical runtime exists.
  standing_design_tests:
    interface_safety: Provider and function selection, exactness, epoch, policy, state, and work are
      compiler-sealed; callers cannot request a provider superset, approximation, or fallback.
    scale: Provider work is partition-scoped with independent row/group/distinct/state/output bounds
      and pay-once proofs; no database-wide or cross-provider materialization is required.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-013, OQ-017, OQ-019, OQ-020, OQ-021, OQ-022,
      OQ-024, OQ-044, OQ-046, OQ-047, OQ-048, OQ-049, OQ-050, OQ-051,
      OQ-052, OQ-053, OQ-055]
    evidence: The sealed provider descriptor now exposes one closed exact aggregate semantic subset
      derived from its already-versioned provider kind and MEASURE stage. The real columnar adapter
      advertises and executes the ten bounded/grouped exact functions but not exact_count; the
      indexed exact-result adapter advertises only exact_count; vector and non-MEASURE descriptors
      advertise none. Compiler architecture tests enumerate all eleven registry identities and
      prove that provider subsets never collapse into their union, while real columnar and exact-
      text conformance tests bind those advertisements to their executable adapters. Existing
      result-set epoch proofs, partition-first policy admission, catalog-generation validation,
      exact page/count execution, columnar grouped/whole folds, rebuild/compaction suites, and
      memory/redb recovery suites pass as one provider integration matrix. The capability view is
      transient and preserves provider V1 bytes, digests, state layouts, and topology exactly.
    known_limitations: No provider bridge or composite executor is introduced. The indexed exact-
      result provider continues to offer only maintained exact_count; it does not claim columnar
      folds or facets. Differential merge-law, inference, mixed-version, generated-surface, and
      external generic application closure remains WP-696.
    follow_ups: [WP-696]
  human_review_triggers: *id001
- id: WP-696
  name: Aggregate differential security compatibility and final evidence
  depends_on: [WP-695]
  objective: Close ADR-0152 with generic differential and merge-law corpora, policy and inference
    isolation, recovery and compatibility campaigns, every generated surface, handbook guidance,
    and external generic acceptance without adding deferred aggregate families or framework code.
  requirements: [OQ-044, OQ-045, OQ-046, OQ-047, OQ-048, OQ-049, OQ-050,
    OQ-051, OQ-052, OQ-053, OQ-054, OQ-055]
  required_adrs: [ADR-0055, ADR-0108, ADR-0111, ADR-0124, ADR-0129,
    ADR-0130, ADR-0152]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0152-closed-exact-aggregate-function-families.md, proto/**,
    release/version-topology-v1.json, crates/**, clients/**, templates/**,
    fixtures/**, examples/**, tests/**, fuzz/**, scripts/**, docs/**]
  deliverables:
  - Differential empty/one/maximum/grouped/overflow/NoValue/duplicate/distinct-bound/canonical-order
    corpus across reference, ordinary, columnar, exact provider, memory/redb, and every public surface
  - Merge-law, rebuild, compaction, crash, retention, and mixed-version evidence for every exact
    partial state and registered identity
  - Authorization, revocation, minimum-group, redaction, timing-class, tenant-statistics, and policy-
    before-contribution proof with no partial values or hidden-cardinality disclosure
  - Architecture negatives for deferred statistics/ordered/collection/user-defined functions,
    per-row proof, callback, floating point, framework branch, provider bridge, and unsafe fallback
  - Complete handbook, generated-reference, compatibility, external generic application, and
    requirement-coverage closure
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every exact aggregate function is semantically identical, bounded, policy-safe,
    recoverable, compatible, and generated across all surfaces; deferred functions remain absent;
    and no unexplained warning, partial result, framework artifact, or unsafe escape remains.
  standing_design_tests:
    interface_safety: All functions and guarantees remain finite compiled named-query behavior with
      no caller-selected structure, approximation, provider, policy, precision, or fallback.
    scale: All row/group/distinct/state/scan/output and evidence dimensions are fixed and bounded;
      no database-wide memory, unbounded materialization, or duration-proportional state exists.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-044, OQ-045, OQ-046, OQ-047, OQ-048, OQ-049,
      OQ-050, OQ-051, OQ-052, OQ-053, OQ-054, OQ-055]
    evidence: The final closure corpus proves the merge identity, partition independence, and
      canonical order of every registered exact partial state over empty, NoValue, duplicate,
      one-row, and bounded multi-chunk inputs. Its architecture inventory binds those laws to the
      existing randomized reference differential, ordinary executor, grouped columnar,
      exact-provider, compaction, recovery, policy-before-contribution, generated-schema, and gRPC
      evidence. The public handbook matrix identifies all eleven exact functions, empty and
      NoValue results, refusal boundaries, provider subsets, epoch behavior, and deferred families.
      Architecture negatives keep approximation, ordered-set and collection aggregates,
      user-defined functions, floating-point state, per-row compatibility proof, callbacks,
      framework branches, and speculative provider bridges absent. Full workspace, remote
      four-language driver, topology, generated-artifact, requirement-coverage, policy, handbook,
      formatting, and Clippy gates pass together. The package-first driver fixture generator now
      selects its named CreateItem command rather than relying on role-command array position,
      preserving exact generated identity evidence when unrelated commands are added.
    known_limitations: Approximate statistics, ordered-set and collection aggregates, windows,
      user-defined functions, provider bridges, and BM25 execution remain deliberately deferred.
      Provider capability subsets remain exact; columnar supplies its bounded fold family,
      indexed exact results supply exact_count, and vector providers supply neither.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-697
  name: Initialized state-transition language compiler and V17 identity
  depends_on: [WP-560, WP-612, WP-678, WP-683]
  objective: Implement ADR-0153's exact-key `init_or_mutate` source and compiler semantics with
    state-independent typed initializer dataflow, path-sensitive definite assignment, complete
    create/update authority and cost proofs, and least-sufficient V17 identities without adding
    general branching, generic upsert, or a framework-specific path.
  requirements: [BLK-009, BLK-036, BLK-037, BLK-038, BLK-040, BLK-042,
    BLK-043, BLK-044, DSL-001, DSL-002, DSL-003, DSL-004, DSL-005, DSL-006,
    DSL-007, DSL-008, DSL-009, DSL-010, DSL-011, DSL-012, SAFE-004, SAFE-005]
  required_adrs: [ADR-0002, ADR-0003, ADR-0012, ADR-0031, ADR-0055,
    ADR-0107, ADR-0124, ADR-0129, ADR-0147, ADR-0149, ADR-0153]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0153-compiler-sealed-initialized-state-transitions.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-diagnostics/**,
    crates/riffdb-cli/assets/editor/**, clients/editor/**, fixtures/compiler/**,
    fixtures/contracts/**, fuzz/**, tests/**, scripts/**, docs/SUMMARY.md,
    docs/contracts/**, docs/architecture/**, docs/known-limitations.md]
  deliverables:
  - Exact init_or_mutate binding and initializer grammar, formatter, syntax AST, source spans,
    bounded item accounting, editor grammar, and diagnostics, with no absent/present blocks,
    state-origin expression, dynamic outcome, callback, conflict selector, or alternate target
  - One checked initialized mutable binding plan whose initializer fields are canonical FieldId-
    ordered, unique, non-key, typed, and limited to constants/input/service/transaction/current-
    element values, with all other entity, query, projection, storage, clock, randomness, and
    runtime-selected dependencies rejected
  - Path-sensitive command analysis proving absent-path read-before-initialize safety and complete
    create postimages while retaining present-preimage behavior, common requirements/effects,
    relationships, uniqueness, invariants, secrets, events, outcomes, and one exact target
  - Static authorization and explain metadata containing the union of create/update operation and
    field authority, plus row-policy applicability for both possible states without exposing state
    origin through a caller field or generated option
  - Conservative alternative cost proof using max(absent create, present replace), not the sum,
    under unchanged collection, byte, mutation, index-delta, affected-prefix, validation-position,
    correlated-work, and one-partition ceilings
  - Least-sufficient grammar/executable-IR/bundle V17 tags and canonical initializer encoding,
    topology registration, golden/hash/lock fixtures, old-reader rejection, and byte-exact V1-V16
    writer regression with no durable decoder retirement
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler --all-features
  - cargo +1.97.0 test -p riffdb-diagnostics --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-diagnostics --all-targets --all-features -- -D warnings
  exit_gate: Valid ordinary and collection initialized transitions lower to one exact V17 binding
    with complete dataflow, authority, locality, conflict, relationship, uniqueness, secret, and
    max-alternative cost proofs; invalid shapes fail at source spans; old contracts retain their
    exact bytes; and no general branch, upsert, origin flag, framework path, or unregistered identity
    exists.
  standing_design_tests:
    interface_safety: The application declares one exact entity/key, initializer, and common command
      suffix. It cannot choose runtime state, conflict target, overwrite policy, transaction,
      authority, budget, retry, or fallback, and generated callers receive no new unsafe option.
    scale: Compiler work is bounded by existing binding/field/expression/collection limits and
      charges the maximum of two sealed alternatives once; no population scan, dynamic graph,
      per-index initializer evaluation, or cross-partition state is introduced.
  closure:
    status: complete
    completed_at: '2026-09-03'
    requirements_satisfied: [BLK-009, BLK-036, BLK-037, BLK-038, BLK-040,
      BLK-042, BLK-043, BLK-044, DSL-001, DSL-002, DSL-003, DSL-004, DSL-005,
      DSL-006, DSL-007, DSL-008, DSL-009, DSL-010, DSL-011, DSL-012, SAFE-004,
      SAFE-005]
    evidence: Req-tagged parser and compiler tests prove the exact ordinary and bounded-collection
      syntax, canonical state-independent initializer, source-spanned rejection and absent-path
      definite assignment. IR estimator tests freeze max-of-create-or-replace charging, the V17
      fixture and retained V16 fixture remain byte exact, and the application-role proof refuses
      either incomplete policy operation before accepting the complete create/update union.
    known_limitations: General upsert, state-specific arms or origin predicates, caller-selected
      conflict behavior, callbacks, dynamic branching, cross-partition transitions, and runtime or
      framework-specific behavior remain unavailable; WP-698 and WP-699 own downstream execution,
      recovery, generation, and external acceptance.
    follow_ups: [WP-698, WP-699]
  human_review_triggers: *id001
- id: WP-698
  name: Initialized state-transition runtime coordinator and storage conformance
  depends_on: [WP-561, WP-679, WP-684, WP-697]
  objective: Execute ADR-0153 initialized mutable bindings as one absence-or-presence observation
    and exactly one create-or-replace mutation per target, preserving whole-command revalidation,
    atomicity, idempotency, policy, index work, recovery, and bounded bulk behavior across memory
    and redb without an internal command or dormant authoritative row.
  requirements: [BLK-036, BLK-038, BLK-039, BLK-040, BLK-041, BLK-042,
    BLK-043, BLK-045, TXN-010, TXN-011, TXN-012, TXN-013, PERF-004, PERF-005]
  required_adrs: [ADR-0002, ADR-0003, ADR-0004, ADR-0005, ADR-0012,
    ADR-0023, ADR-0055, ADR-0107, ADR-0124, ADR-0129, ADR-0147, ADR-0149,
    ADR-0153]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0153-compiler-sealed-initialized-state-transitions.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-contract-ir/**, crates/riffdb-invariant/**,
    crates/riffdb-runtime/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-policy/**, crates/riffdb-service/**,
    crates/riffdb-testkit/**, fixtures/**, tests/**, fuzz/**, scripts/**,
    docs/contracts/**, docs/architecture/**, docs/known-limitations.md]
  deliverables:
  - Snapshot materialization for one initialized mutable target that preserves exact absence or
    present version/hash evidence and constructs a bounded provisional absent-path record without
    changing storage or invoking another service operation
  - Deterministic common-suffix execution producing exactly Create for a revalidated absence or
    revision-checked Replace for a revalidated presence, with no create-plus-replace graph, dormant
    row, implicit default, state-origin result, or stale mutation conversion
  - Coordinator validation and deterministic schedules for absent/absent, absent/present,
    update/update, delete/recreate, cancellation, retry exhaustion, and policy revision, discarding
    every stale graph before durable outcome or event release
  - Create/update authority union plus selected row/field policy, relationship, uniqueness,
    invariant, secret, event, provenance, idempotency, uncertain response, replay, and redaction
    equivalence to the existing individual binding modes
  - Ordinary and bounded collection execution over all-absent, all-present, and mixed observations,
    including 1/9/19/100 elements, duplicates, aliasing, aggregate bytes, graph bytes, business
    rejection, complete-or-absent visibility, and exact memory/redb parity
  - Runtime physical work charges only the selected create or replace alternative; stage evidence
    proves one entity mutation per target and unchanged correlated index-work ceilings, with
    initializer/compatibility proof never repeated per field, index, validation target, or retry
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-runtime -p riffdb-commit -p riffdb-policy -p riffdb-service --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test bulk_command_recovery --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-runtime -p riffdb-commit -p riffdb-policy -p riffdb-service -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-targets --all-features -- -D warnings
  exit_gate: Every initialized binding commits exactly one transaction-current create or replace
    with one atomic outcome/event/provenance graph, exact replay and crash recovery, selected policy,
    bounded mixed collection behavior, and honest alternative work accounting; no stale result,
    dormant row, internal command, double mutation, framework behavior, or weakened ceiling remains.
  standing_design_tests:
    interface_safety: Runtime derives the mutation kind only from revalidated authoritative state;
      no caller value, transport, adapter, configuration, or backend can choose the path or bypass
      authorization, policy, validation, atomicity, idempotency, durability, or provenance.
    scale: Each target costs one point observation and one selected mutation beneath existing finite
      collection, graph, index, validation, byte, retry, and partition limits; no scan, cross-
      partition coordination, or duration/population-proportional state exists.
  human_review_triggers: *id001
- id: WP-699
  name: Initialized transition generated recovery performance and external acceptance
  depends_on: [WP-562, WP-565, WP-692, WP-698]
  objective: Close ADR-0153 across every generated/public surface, compatibility and crash recovery,
    generic lifecycle performance evidence, handbook guidance, and a value-free external-consumer
    receipt while retaining all adapter code and framework semantics in their owning repositories.
  requirements: [BLK-036, BLK-039, BLK-040, BLK-041, BLK-042, BLK-043,
    BLK-044, BLK-045, DRV-007, DRV-008, DRV-012, DRV-014, SAFE-004, SAFE-005,
    PERF-004, PERF-005]
  required_adrs: [ADR-0055, ADR-0107, ADR-0124, ADR-0129, ADR-0142,
    ADR-0147, ADR-0148, ADR-0149, ADR-0153]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0153-compiler-sealed-initialized-state-transitions.md,
    release/version-topology-v1.json, proto/**, crates/**, clients/**,
    templates/**, fixtures/**, examples/**, tests/**, benchmarks/**, scripts/**,
    docs/**]
  deliverables:
  - Deterministic generated Rust, Go, TypeScript, Python, CLI, MCP, local-driver, and remote-gRPC
    operation identity/outcome/error/authorization conformance without a caller-visible state path,
    conflict mode, transaction handle, or handwritten binding behavior
  - Generic lifecycle application using one initialized transition for absent, present, mixed bulk,
    business rejection, event, secret, policy, and replay cases, with all generated artifacts and
    documentation discoverable from docs/SUMMARY.md
  - Process-level memory/redb crash and restart campaign across admission, snapshot, evaluation,
    staging, durability, publication, lost response, retry, and retained V1-V16/V17 deployment
    windows, with no decoder retirement or unexplained recovery state
  - Stage-attributed benchmark proving one generated command and one entity mutation replace the
    generic dormant-create-plus-replace sequence, reporting service, validation, index, journal,
    durability, and response costs without changing ADR-0142 gates or claiming external parity
  - Value-free external capability receipt confirming the consumer can compile and execute its
    initialized bulk transition; the receipt may report bounded operation-count and timing deltas
    but contains no external schema, route, adapter source, generated profile, or framework-specific
    RiffDB branch
  - Final architecture inventory and handbook text separating database capability from consumer
    query batching, iterator buffering, redundant-read elimination, safe immutable-root caching, and
    its own business semantics
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public surface, backend, restart path, policy case, bound, and compatibility
    window preserves the exact initialized-transition semantics; generic evidence proves the
    redundant durable transition is removed; the external receipt is green; and no framework code,
    caller-selected upsert behavior, unsafe branch, stale artifact, warning, or unexplained failure
    remains.
  standing_design_tests:
    interface_safety: Generated callers see only the finite named command's existing typed input and
      closed outcome union; initialization, selected mutation, policy, work, retry, durability, and
      recovery remain compiler/runtime-owned with no unsafe public selector.
    scale: Evidence is bounded by declared ordinary/collection maxima and reports constant per-plan
      plus per-selected-target work; no unbounded benchmark state, cursor walk, scan, cross-
      partition operation, or application-sized retained proof is introduced.
  human_review_triggers: *id001
- id: WP-700
  name: Profile-aware binary text interval compiler and storage conformance
  depends_on: [WP-688, WP-690]
  objective: Implement ADR-0154's strict, inclusive, and complement intervals over the existing
    binary_utf8_v1 physical order through one compiler-sealed ordinary access shape and the shared
    memory/redb range schedule, without a ULID branch, residual filter, or durable-format change.
  requirements: [OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-008,
    OQ-009, OQ-015, OQ-032, OQ-033, OQ-034, OQ-036, OQ-037, OQ-039, OQ-040,
    OQ-043, OQ-056, OQ-057, OQ-058, OQ-059, OQ-060]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124, ADR-0129, ADR-0134, ADR-0145, ADR-0150, ADR-0154]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0154-profile-aware-binary-text-intervals.md,
    release/version-topology-v1.json, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-executor/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-testkit/**,
    fixtures/riffql/**, fixtures/query-modules/**, tests/**, fuzz/**, scripts/**,
    docs/SUMMARY.md, docs/riffql/**,
    docs/getting-started/OPERATIONAL-QUERIES.md, docs/known-limitations.md]
  deliverables:
  - Closed registry admission for binary_utf8_v1 range/complement only after an exact partition
    prefix and only when the component supplies the first remaining complete order term and key
    tie-breaker, with source-spanned refusal for every incomplete shape
  - Pay-once endpoint lowering for lower-only, upper-only, strict, inclusive, closed/open, empty,
    contradictory, and two-range complement cases using exact ordered-byte component prefixes
  - One memory/redb forward/reverse range and cursor truth table under global page, plus-one, scan,
    hydration, output, parameter, range-count, and encoded-byte bounds
  - Logical predicate, authorization, row-policy, cancellation, redaction, and activation-time
    unavailable conformance proving no post-page filter, physical-byte output, or per-row proof
  - Compatibility evidence that old plans and indexes remain byte-exact and no query-IR, cursor,
    storage, index-key, wire, or generated-method version is silently reinterpreted
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-executor --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test operational_queries --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One declared binary_utf8_v1 index physically enforces bytewise lower/upper/complement
    intervals before page formation with exact memory/redb order and cursor parity, unchanged global
    bounds and old artifacts, and no ULID/framework branch, scan, residual filter, or unsafe selector.
  standing_design_tests:
    interface_safety: Callers submit only typed bounded values to one generated named query and
      cannot choose a profile, index, collation, range shape, cursor binding, policy, cost, or
      fallback; every incomplete shape fails before storage.
    scale: At most one interval or two complement ranges exist beneath one exact partition prefix
      and one global finite work budget; no population materialization, page walk, cross-partition
      scan, or per-row compatibility proof is introduced.
  closure:
    status: complete
    completed_at: '2026-09-03'
    requirements_satisfied: [OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006,
      OQ-008, OQ-009, OQ-015, OQ-032, OQ-033, OQ-034, OQ-036, OQ-037, OQ-039,
      OQ-040, OQ-043, OQ-056, OQ-057, OQ-058, OQ-059, OQ-060]
    evidence: Req-tagged compiler admission/refusal, executor boundary-oracle, shared cursor,
      memory, and redb truth tables prove strict, inclusive, closed/open, contradictory, and
      complement schedules. Oversized endpoint and empty-schedule proofs fail before entity/index
      storage work; generated/version/workspace gates preserve all existing bytes and identities.
    known_limitations: Canonical-string ranges, locale or natural collation, multiple interval
      dimensions, intersections, skip scans, adaptive planning, residual filters, ULID-specific
      behavior, and caller-selected profiles or fallbacks remain unavailable and fail closed.
    follow_ups: [WP-701]
  human_review_triggers: *id001
- id: WP-701
  name: Binary text interval generated and public acceptance
  tier: surface
  depends_on: [WP-700, WP-565]
  objective: Close ADR-0154 across repository-local generated/public surfaces, handbook and
    architecture guidance, compatibility gates, and transport conformance while retaining every
    external schema, route, token convention, and adapter artifact in its owning repository.
  requirements: [OQ-004, OQ-005, OQ-006, OQ-015, OQ-016, OQ-043,
    OQ-056, OQ-057, OQ-058, OQ-059, OQ-060, OQ-061, DRV-007, DRV-008,
    DRV-012, DRV-014, SAFE-004, SAFE-005]
  required_adrs: [ADR-0055, ADR-0108, ADR-0112, ADR-0124, ADR-0129,
    ADR-0148, ADR-0150, ADR-0154, ADR-0193, ADR-0194]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0194-generated-mcp-named-query-pagination-envelope.md,
    release/version-topology-v1.json, crates/**, clients/**, fixtures/**,
    examples/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Framework-neutral changelog-shaped corpus over length-divergent and multibyte strings proving
    raw logical token return, strict horizon exclusion, total bytewise order, page continuation,
    policy, and bounds through every generated and transport surface
  - Final compatibility, topology, architecture, handbook, limitation, and requirement-coverage
    evidence with no external schema, adapter source, framework-specific branch, or parity claim
  - Closure of WP-701 proves only repository-local generated/public/transport conformance. It does
    not prove external consumer execution; WP-775 solely owns the fresh external execution receipt
    required by ADR-0154 section 7, and the existing source-only receipt remains historical,
    non-terminal evidence.
  - Generated paginated named-query MCP V5 catalogs with collision-safe result/page envelopes,
    protected cursor routing, exact 59/39-byte object and complete outbound-frame budget accounting,
    and hosted/stdio two-page parity; V5 retains V3 SDK-only and V4 vector registries under exact
    least-sufficient V2-V5 selection, unpaged entries remain byte-exact, and every generated schema,
    catalog, application-lock, and fixture rotation receives exact human diff and hash review,
    proved by
    generated_application_operations_v5_topology_and_selection_are_exact
  - MCP agent and operational-query handbook guidance for the required result/page envelope,
    nullable next_cursor, opaque after reuse, version-skew behavior, and value-free diagnostics
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-module -p riffdb-api-mcp -p riffdb-mcp-stdio --all-features
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/adapter-operational-query-acceptance --all-languages
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-adr-obligations
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every repository-local generated and transport surface agrees with the compiler/storage
    bytewise interval contract, documentation is complete, the external execution obligation is
    explicitly deferred to WP-775 without being claimed here, and no raw physical bytes, unsafe
    plan choice, framework code, or unexplained failure remains.
  standing_design_tests:
    interface_safety: Generated callers expose only the named query's typed values and opaque RiffDB
      cursor; they cannot select physical semantics, and returning a logical string does not grant
      storage, index, protocol, or cursor authority.
    scale: Every corpus and receipt uses one partition, finite endpoints, bounded pages, and declared
      scan/output ceilings; no external population or duration-proportional state is retained.
  closure:
    status: complete
    completed_at: '2026-09-03'
    requirements_satisfied: [OQ-004, OQ-005, OQ-006, OQ-015, OQ-016, OQ-043,
      OQ-056, OQ-057, OQ-058, OQ-059, OQ-060, OQ-061, DRV-007,
      DRV-008, DRV-012, DRV-014, SAFE-004, SAFE-005]
    evidence: Req-tagged compiler, generated Rust, Go, TypeScript, and Python, CLI, remote gRPC,
      local-driver, hosted MCP, and stdio MCP proofs execute one shared bounded binary-text interval
      corpus and preserve logical strings, exact bytewise order, horizon exclusion, page windows,
      opaque cursor authority, policy, and closed errors. OBL-0194-1 through OBL-0194-3 prove the
      collision-safe paginated envelope, two-page MCP authority and redaction, and exact V2-V5
      selection and rotations; generated, topology, handbook, compatibility, obligation, workspace,
      formatting, test, and lint gates pass.
    known_limitations: This closure proves repository-local generated, public, and transport
      conformance only. It does not claim fresh owning-consumer execution, framework parity, an
      external schema or route, or a database-owned external continuation token. The immutable V5
      fixture and historical source-only receipt remain non-terminal; WP-775 solely owns the fresh
      external execution-v1 receipt required by ADR-0154 section 7 and ADR-0193.
    follow_ups: [WP-775]
  human_review_triggers: *id001
- id: WP-702
  name: Sparse generated-surface source manifest and lock identities
  depends_on: [WP-602, WP-612]
  objective: Implement ADR-0155's closed sparse application-source V7 generation declaration,
    manifest V5, lock V8, and one compiler-owned target registry while preserving every older
    source/manifest/lock byte and preventing local project configuration from changing exact identity.
  requirements: [DX-004, DX-006, DX-013, DX-014, DX-015, DX-016, DX-017,
    DX-018, DX-042, DX-043, DX-044, DX-045, DX-047, DX-048, DX-049,
    VER-001, VER-002, VER-003, VER-004, VER-005, VER-006, VER-007, VER-008]
  required_adrs: [ADR-0052, ADR-0055, ADR-0057, ADR-0075, ADR-0112,
    ADR-0120, ADR-0121, ADR-0124, ADR-0155]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0155-selectively-declared-generated-application-surfaces.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-query-module/**, crates/riffdb-cli/**, fixtures/**,
    tests/**, scripts/**, docs/SUMMARY.md, docs/getting-started/**,
    docs/sdks/**, docs/versioning-and-retirement.md, docs/known-limitations.md]
  deliverables:
  - Application-source V7 parser and canonicalizer accepting a nonempty unique-path subset of the
    five closed target kinds, plus manifest V5 and lock V8 carrying exactly that declared set
  - One compiler-owned declared-surface registry used by parsing, generation, artifact-kind mapping,
    lock validation, publication, checking, installation, scaffolding, and diagnostics
  - Exact in-memory generation and hashing for every declared surface, zero generation/toolchain
    discovery for absent surfaces, and typed refusal when local configuration selects an undeclared
    kind or an artifact inventory is missing, extra, stale, unsafe, or inconsistent
  - Least-sufficient topology, canonical fixtures, old-reader rejection, migration preview, and
    byte-exact V1-V6 source, V1-V4 manifest, and V1-V7 lock compatibility with no decoder retirement
  - Architecture evidence that generator selection cannot change runtime commands, queries, roles,
    policy, hosted protocols, storage, outcomes, cost, or first-party Rust driver ownership
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-module -p riffdb-cli --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-query-module -p riffdb-cli --all-targets --all-features -- -D warnings
  exit_gate: Sparse V7 source deterministically yields exact V5 manifest and V8 lock inventories from
    one registry, older bytes remain exact, absent targets incur no artifact or toolchain work, and
    local configuration or generated language code cannot alter authoritative identity or semantics.
  standing_design_tests:
    interface_safety: Applications select only safe first-party generated artifact kinds and paths;
      they cannot supply implementations, alter runtime authority, bypass exact locking, or replace
      the Rust-owned driver and service boundary.
    scale: At most five targets are validated and generated once per compilation beneath existing
      path/artifact/lock byte ceilings, with no runtime, row, query, or database-sized work.
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [DX-004, DX-006, DX-013, DX-014, DX-015, DX-016, DX-017,
      DX-018, DX-042, DX-043, DX-044, DX-045, DX-047, DX-048, DX-049, VER-001,
      VER-002, VER-003, VER-004, VER-005, VER-006, VER-007, VER-008]
    evidence: The closed five-surface registry now owns canonical source and manifest iteration,
      artifact-kind conversion, exact in-memory generation, local selection, installation mapping,
      and V8 lock validation. Canonical singleton, representative multi-surface, Manifest V5, and
      Lock V8 fixtures plus req-tagged migration, identity-invariance, project, installation, and
      topology proofs retain predecessor readers and exact bytes while absent surfaces perform no
      generated-artifact work.
  human_review_triggers: *id001
- id: WP-703
  name: Selective generation project packaging and external conformance
  depends_on: [WP-611, WP-702, WP-779]
  objective: Close ADR-0155 with truthful singleton and multi-surface project workflows, installed
    package behavior, compatibility and filesystem recovery, cross-driver conformance, and a fresh
    Go-only external package that requires no unrelated generated target or toolchain.
  requirements: [DX-013, DX-014, DX-016, DX-017, DX-026, DX-027, DX-028,
    DX-029, DX-030, DX-031, DX-037, DX-038, DX-039, DX-040, DX-042, DX-043,
    DX-044, DX-045, DX-046, DX-047, DX-048, DX-049, DRV-007, DRV-008,
    DRV-012, DRV-014]
  required_adrs: [ADR-0055, ADR-0112, ADR-0120, ADR-0121, ADR-0124,
    ADR-0148, ADR-0155, ADR-0198]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0155-selectively-declared-generated-application-surfaces.md,
    release/version-topology-v1.json, crates/**, clients/**, templates/**,
    fixtures/**, examples/**, tests/**, scripts/**, release/distribution/**,
    docs/**, .github/workflows/**]
  deliverables:
  - Fresh Rust-only, Go-only, TypeScript-only, Python-only, MCP-only, and representative polyglot
    init/generate/check/diff/push-preview/install/dev workflows with exact selected artifact trees
  - Go-only external package proving native installation, generation, lock validation, go test, and
    local-driver execution without npm, TypeScript, Python, MCP output, or a generated Rust facade
  - Symmetric global generator and MCP semantic conformance proving omission from one package does
    not weaken any supported surface or change hosted protocol authority
  - Filesystem interruption, existing undeclared file, symlink, collision, migration preview,
    development publication inventory, compatibility, handbook, and architecture evidence
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-driver-development-registry
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every declared target set produces one exact bounded artifact inventory and truthful
    project/package workflow, the Go-only external acceptance is green without unrelated toolchains,
    all global surfaces retain parity, and no stale artifact, local identity drift, protocol change,
    target-language semantic branch, warning, or unexplained failure remains.
  standing_design_tests:
    interface_safety: Project and package users can select only compiler-generated safe facade kinds;
      no selector grants transport, transaction, authorization, storage, MCP, or guarantee bypass.
    scale: Target sets contain at most five entries and acceptance uses bounded fresh packages and
      fixture trees; generation retains no database state and performs no operation-proportional work.
  human_review_triggers: *id001
- id: WP-704
  name: Clean-close certificate durable interface and fast-startup safety closure
  depends_on: [WP-130, WP-422, WP-448, WP-489, WP-669]
  objective: Implement ADR-0156's private conventional clean-close lifecycle state, bounded
    readiness proof, durable consume-before-writers transition, and complete dirty fallback while
    moving every integrity fact skipped at clean startup onto the operational path that first uses
    it and preserving the existing exact-end validation stream for recovery and explicit scrub.
  requirements: [STO-001, STO-012, STO-020, STO-021, STO-022, STO-023,
    REC-001, REC-002, REC-004, PERF-007, PERF-013, PERF-014, PERF-019]
  required_adrs: [ADR-0004, ADR-0006, ADR-0019, ADR-0034, ADR-0050,
    ADR-0061, ADR-0073, ADR-0085, ADR-0101, ADR-0103, ADR-0104, ADR-0112,
    ADR-0156, ADR-0157]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0156-clean-close-certificate-fast-startup.md,
    adr/0157-clean-close-lifecycle-durable-identity.md,
    proto/riffdb/storage/v1/**, crates/riffdb-proto/**, fixtures/proto/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-catalog/**, crates/riffdb-auth/**, crates/riffdb-commit/**,
    crates/riffdb-server/**, crates/riffdb-observability/**,
    tests/storage_recovery/**, tests/recovery/**, tests/command_semantics/**,
    scripts/generate-proto*, scripts/check-generated,
    docs/SUMMARY.md, docs/architecture/**, docs/operations/**,
    docs/performance/**, docs/compatibility.md]
  deliverables:
  - ADR-0157 accepted BEFORE implementation freezes the exact private
    meta key, Protobuf fields/tags/presence, canonical hash domain, semantic byte ceiling, registry
    predecessor/successor digests, dirty-consumption representation, and old-reader behavior; no
    code may infer these durable choices from this package text
  - One sealed lifecycle typestate whose clean variant can be constructed only after final
    admission/writer/worker/journal drain and committed Immediate as the final authoritative
    mutation, and whose startup consumption becomes durable before any coordinator, outbox,
    projection, reactive, maintenance, or public writer can activate
  - A bounded clean readiness proof over engine status, database/incarnation/format/registry,
    allocators/frontiers, journal/checkpoint generation, bootstrap, active catalog lineage and
    executable artifacts, and initial authority roots, with every absence, mismatch, decode error,
    repair, incompatible version, migration, restore, or uncertainty selecting complete validation
  - A source and test inventory for every population row class skipped by clean startup, followed by
    fail-closed local envelope/checksum/canonical-key/schema/bound/required-reciprocity validation
    before that row can influence authorization, execution, output, delivery, projection, or
    mutation; no corruption becomes absence, partial output, business rejection, skip, or repair
  - The existing structural and historical exact-end path retained byte-for-byte for dirty recovery
    and explicit verification, including the golden evidence sequence, collector rejection order,
    pagination/ExactEnd semantics, validated-prefix behavior, bounded locator memory, and the full
    recovery matrix
  - Deterministic schedules and process kills before/after every drain, journal checkpoint,
    certificate commit, certificate consume/dirty commit, readiness, first write, and final close,
    plus negative copied/stale/torn/substituted certificate and write-after-certificate arms
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto --test durable_schema --all-features
  - cargo +1.97.0 test -p riffdb-storage-api -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-catalog -p riffdb-auth -p riffdb-commit -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --test storage_recovery_matrix --all-features
  - ./scripts/recovery_full
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A clean certificate is written only as the final mutation and consumed durably before
    writers; eligible clean state reaches an operational proof without a population walk; every
    uncertain lifecycle takes the unchanged complete path; every skipped row fails closed on first
    influence; and no caller, configuration, operator, transport, or backend can select or forge
    startup mode.
  standing_design_tests:
    interface_safety: The certificate and startup proof are sealed private typestates. Applications,
      agents, operators, transports, and configuration cannot force clean eligibility, suppress a
      fallback, activate a writer early, or opt out of durability, authorization, idempotency,
      schema validation, accessed-row integrity, or dirty recovery.
    scale: Clean open and close inspect only fixed metadata plus bounded catalog/authority roots and
      retain no population-sized plan. Dirty and explicit validation stream under existing page,
      byte, migration, diagnostic, and heap bounds rather than materializing live-row locators.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [STO-001, STO-012, STO-020, STO-021, STO-022, STO-023,
      REC-001, REC-002, REC-004, PERF-007, PERF-013, PERF-014, PERF-019]
    evidence: The accepted ADR-0157 lifecycle interface, generated registry successor, sealed
      startup typestate, Immediate dirty consumption, final clean-close commit, bounded-root
      readiness proof, unchanged exact-end fallback, deferred fail-closed population checks, and
      deterministic recovery matrix are implemented and covered by the package acceptance gates.
  human_review_triggers: *id001
- id: WP-705
  name: Clean-startup internal scrub compatibility and bounded dirty evidence
  depends_on: [WP-610, WP-668, WP-704, WP-773, WP-774, WP-778]
  objective: Close ADR-0156 with a sealed internal complete-integrity scrub primitive, backup/
    restore/migration/retention compatibility, operator and handbook guidance, production-scale
    clean lifecycle evidence, genuine 65,536-row complete-validation recovery evidence, and a
    fail-closed production-scale complete-path ceiling handoff to WP-760. ADR-0182 and WP-760 solely
    own production-scale dirty readiness without a population walk, the 64 MiB recovery-owned-state
    proof, the authorized public maintenance service, CLI command, and public scrub receipt.
  requirements: [STO-012, STO-023, REC-001, REC-002, REC-004,
    PERF-013, PERF-014, PERF-019, END-004, END-006, END-007, END-008]
  required_adrs: [ADR-0019, ADR-0041, ADR-0050, ADR-0056, ADR-0073,
    ADR-0085, ADR-0104, ADR-0112, ADR-0123, ADR-0156, ADR-0157, ADR-0182, ADR-0187,
    ADR-0188, ADR-0197, ADR-0205, ADR-0206, ADR-0208, ADR-0210, ADR-0213, ADR-0214]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0156-clean-close-certificate-fast-startup.md,
    adr/0157-clean-close-lifecycle-durable-identity.md,
    proto/**, crates/**, fixtures/**, tests/**, examples/**, benchmarks/**,
    scripts/**, docs/**, release/**, .github/workflows/**]
  deliverables:
  - One sealed internal offline integrity-scrub primitive and fixed bounded internal receipt that
    drive the complete exact-end structural/catalog validation path without forging dirty process
    state, publish no partial success, mutate no application authority, and do not create a clean
    certificate while an operational writer can run. The primitive is not publicly invocable and
    grants no path, storage handle, maintenance authority, or CLI surface; WP-760 exclusively owns
    the ADR-0050-authorized public operation and receipt beneath .maintenance
  - Backup, restore, registry/format migration, history-incarnation rotation, retention prune,
    maintenance replacement, and database-copy conformance proving exactly which operations preserve
    a matching clean certificate and which invalidate it into complete validation
  - Redaction-safe closed startup/shutdown mode and stage observations that expose no database path,
    identifier, frontier, table count, hash, key, application value, or unbounded label and cannot be
    supplied back as control input
  - A production-scale cleanly seeded and cleanly shut down database proving clean reopen and final
    close retain bounded heap and perform zero population-table evidence walks
  - Genuine writer-process kill and reopen evidence at exactly the 65,536-row checkpoint proving
    the unchanged complete-validation path reaches exact end, recovery and idempotency remain exact,
    and the in-flight probe resolves atomically as either wholly absent or wholly committed; a
    transaction durable before SIGKILL is not required to disappear. The database must exactly match
    one of two predeclared equivalently seeded clean twins, the dirty/clean wall-time ratio is
    recorded against the matching twin, the real engine-repair callback observation is reported
    truthfully, and a second restart proves recovery itself adds no command, event, outcome,
    provenance, audit, projection, outbox, or allocator advance beyond that atomic outcome
  - A separate dedicated PERF-014 process fixture using a genuine process kill at a deterministic
    real-engine crash boundary, proving the real repair callback is invoked, recovered state equals
    its equivalently seeded clean peer, and the repair/clean wall-time ratio is recorded without
    simulated or relabelled repair
  - One production-scale attempt of the unchanged complete path recording its typed fail-closed
    LimitExceeded result at the historical-evidence 512 MiB ceiling without retry, a raised ceiling,
    partial readiness, a passing performance claim, application-authority mutation, or allocator
    advance; this known-ceiling receipt is handed to WP-760 and does not satisfy production-scale
    dirty readiness
  - PERF-013/PERF-014/PERF-019 evidence distinguishing engine open, bounded roots, certificate
    consumption, readiness, drain, final certificate commit, and complete validation; PostgreSQL
    lifecycle numbers are excluded unless the harness owns equivalent process and integrity work
  - Handbook, troubleshooting, compatibility, backup/restore, maintenance, endurance, and known-
    limitation guidance stating that clean readiness is not a full scrub and latent unrelated
    corruption may fail closed on access; the bounded WP-578 handoff proves its restart cycles are
    configured to use the clean path while forced-kill arms retain complete evidence. WP-760 adds
    the authorized scheduled public scrub before WP-578 runs. The uninterrupted END-009 run remains
    solely WP-578's evidence and is not a WP-705 closure condition
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/recovery_full
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - cargo +1.97.0 doc --workspace --no-deps
  exit_gate: Clean production-scale reopen and shutdown are bounded independently of live and
    retained populations; a genuine 65,536-row dirty kill/reopen completes exact validation,
    resolves the in-flight probe atomically as either wholly absent or wholly committed, matches the
    corresponding predeclared clean twin, reports whether engine repair actually occurred, and
    proves a second recovery makes no further authoritative or allocator advance; a separate
    PERF-014 process fixture invokes the real repair callback and records its clean-peer ratio; the
    production-scale complete-path attempt preserves its typed LimitExceeded at the 512 MiB
    historical-evidence ceiling as a fail-closed WP-760 handoff rather than a pass; sealed internal
    scrub and compatibility paths retain complete fail-closed behavior; and documentation states the
    reduced clean-start assurance, known dirty ceiling, and deferred public scrub honestly; WP-705
    does not wait for WP-760.
    Production-scale dirty readiness without a population walk and under 64 MiB, plus the authorized
    public scrub, remain WP-760; the actual uninterrupted run remains pending in WP-578.
  standing_design_tests:
    interface_safety: WP-705 adds no public or operator action. Its scrub primitive and receipt are
      sealed internal values, and no public surface can request less validation, obtain a storage
      handle or path, force readiness, preserve a stale certificate, or convert an integrity
      incident into ordinary application behavior. WP-760 owns the later authorized operation.
    scale: Clean lifecycle work and observations are bounded independently of population; explicit
      validation streams within compiled bounds and fails closed at LimitExceeded rather than
      exceeding them. WP-705 records that production-scale ceiling but does not claim dirty
      readiness; WP-760 exclusively proves zero population-table enumeration and recovery-owned
      state under 64 MiB at production scale. Evidence uses fixed checkpoints, stages, labels,
      durations, artifacts, and diagnostic ceilings.
  human_review_triggers: *id001
- id: WP-706
  name: Compiler-declared bounded runtime page limits
  depends_on: [WP-690, WP-696, WP-701, WP-703]
  objective: Implement ADR-0158's closed Limit<MAX> query-only refinement through syntax,
    resolution, versioned immutable IR/module identity, exact whole-request cost, runtime admission,
    provider/storage execution, compatibility fixtures, and core handbook guidance without changing
    plain Limit or any global safety ceiling.
  requirements: [OQ-004, OQ-005, OQ-006, OQ-015, OQ-017, OQ-019, OQ-022,
    OQ-024, OQ-049, OQ-051, OQ-052, OQ-053, OQ-062, OQ-063, OQ-064, OQ-065,
    OQ-066, OQ-067]
  required_adrs: [ADR-0002, ADR-0013, ADR-0027, ADR-0051, ADR-0052,
    ADR-0055, ADR-0108, ADR-0112, ADR-0124, ADR-0129, ADR-0130, ADR-0134,
    ADR-0150, ADR-0155, ADR-0158]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0158-compiler-declared-bounded-runtime-page-limits.md,
    release/version-topology-v1.json, crates/riffdb-riffql-syntax/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-query-module/**,
    crates/riffdb-catalog/**, crates/riffdb-service/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-testkit/**,
    fixtures/riffql/**, fixtures/query-modules/**, fixtures/compatibility/**,
    tests/operational_queries.rs, tests/compatibility.rs, scripts/**,
    docs/SUMMARY.md, docs/riffql/**, docs/getting-started/**,
    docs/compatibility.md, docs/known-limitations.md]
  deliverables:
  - Limit<MAX> parser/formatter/resolver support with implicit minimum one, maximum 499, one canonical
    unsigned maximum, valid default enforcement, and source-spanned negative snapshots
  - RiffQL V9, query IR V12, and query-module V12 canonical successors carrying MAX through parameter,
    result-list, row-limit, cost, role, schema, plan, module, and lock identity while all old bytes remain exact
  - Planner and role costs use declared MAX cumulatively; executor and every provider/backend reject zero,
    wrong-type, missing-required, and MAX-plus-one before data work and execute one direct bounded page
  - Generic large-field, multi-binding, exact-result, projection, aggregate, vector, forward/reverse cursor,
    default, maximum-drift, backend-spy, compatibility, topology, and handbook evidence
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-executor -p riffdb-query-module --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test operational_queries --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every bounded-limit query has one canonical declared maximum shared by type, immutable
    identity, cost, authorization, schemas, runtime, and provider work; boundaries and cursors are exact,
    old artifacts remain byte-exact, and no default, submitted value, framework branch, or raised ceiling
    can weaken the proof.
  standing_design_tests:
    interface_safety: A caller may choose only a positive value within an immutable named query's
      compiler-declared maximum; it cannot alter cost, provider, access shape, policy, snapshot, or cursor.
    scale: Work is bounded by MAX beneath existing row, byte, step, provider, policy, and response ceilings;
      validation is per compilation/request, never per row, and no population materialization is introduced.
  human_review_triggers: *id001
- id: WP-707
  name: Bounded-limit generated surfaces and external conformance
  depends_on: [WP-611, WP-703, WP-706]
  objective: Close ADR-0158 with exact bounded-limit schemas and validation across every generated and
    driver surface, editor/LSP support, semantic invocation-count evidence, and a fresh sparse Go-only
    installed package, retaining all external adapter artifacts in their owning repositories.
  requirements: [OQ-015, OQ-016, OQ-030, OQ-062, OQ-063, OQ-064, OQ-065,
    OQ-066, OQ-067, DX-026, DX-027, DX-028, DX-029, DX-030, DX-031, DX-046,
    DX-047, DX-049, DX-050, DRV-007, DRV-008, DRV-012, DRV-014, DRV-015,
    DRV-016]
  required_adrs: [ADR-0055, ADR-0112, ADR-0120, ADR-0121, ADR-0124,
    ADR-0148, ADR-0155, ADR-0158]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0158-compiler-declared-bounded-runtime-page-limits.md,
    release/version-topology-v1.json, crates/**, clients/**, templates/**,
    fixtures/**, examples/**, tests/**, scripts/**, release/distribution/**,
    docs/**, .github/workflows/**]
  deliverables:
  - Rust, Go, TypeScript, and Python generated parameter validation plus canonical JSON schema, CLI, MCP,
    local-driver, and remote-gRPC parity for defaults, exact MAX, zero, MAX-plus-one, and wrong types
  - Editor grammar/highlighting/formatter/LSP understand Limit<MAX>; plain Limit outputs and every old
    generated fixture remain byte-exact, and changing MAX rotates every required immutable identity
  - A framework-neutral instrumented corpus proves one invocation and one backend page for a multi-row
    request, no client page walk or discard, exact forward/reverse continuation, and separately safe maxima
    for distinct bounded row sizes without wall-clock thresholds
  - Fresh sparse Go-only init/generate/check/install/dev execution with a bounded multi-row page and no
    MCP artifact, npm, TypeScript, Python, generated Rust facade, external-framework branch, or adapter file
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-driver-development-registry
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every generated and driver surface preserves one compiler-declared bounded-limit domain and
    direct page invocation, the sparse Go-only package runs without undeclared surfaces/toolchains, all old
    outputs remain exact, and no framework code, client-side pagination, hidden fallback, or warning remains.
  standing_design_tests:
    interface_safety: Generated callers can submit only a typed value checked against the immutable MAX;
      target code cannot bypass authoritative validation or select an uncompiled operation or cursor route.
    scale: Generated checks are constant-time, each page is one bounded invocation/backend request, fixtures
      are finite, and no page walk, overfetch, per-row proof, or machine-dependent timing gate is introduced.
  human_review_triggers: *id001
- id: WP-708
  name: Page-cardinality-independent named-query cursor identity
  depends_on: [WP-565, WP-703, WP-706, WP-707]
  objective: Implement ADR-0159's cursor-specific invariant-parameter hash so an ordinary ordered
    named query may resume from the exact prior position with a different valid runtime page
    cardinality while every semantic, authority, snapshot, provider, and compatibility binding
    remains fail-closed.
  requirements: [OQ-004, OQ-006, OQ-015, OQ-016, OQ-022, OQ-031, OQ-036,
    OQ-043, OQ-062, OQ-064, OQ-066, OQ-068, OQ-069, OQ-070, OQ-071,
    DRV-012, DRV-014, DRV-015, DRV-016, DRV-017]
  required_adrs: [ADR-0027, ADR-0051, ADR-0052, ADR-0055, ADR-0108,
    ADR-0112, ADR-0124, ADR-0130, ADR-0134, ADR-0150, ADR-0158, ADR-0159]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0159-page-cardinality-independent-query-cursors.md,
    crates/riffdb-query-ir/**, crates/riffdb-query-executor/**,
    crates/riffdb-query-module/**, crates/riffdb-service/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-testkit/**, tests/operational_queries.rs,
    tests/drivers/**, fixtures/driver/**, scripts/driver-conformance,
    docs/SUMMARY.md, docs/riffql/**, docs/getting-started/**,
    docs/compatibility.md, docs/known-limitations.md]
  deliverables:
  - >-
    One compiler-derived closed set of eligible runtime cardinality parameters: every use must be
    the row limit of an ordinary ordered cursor-paged access; nearest K, unpaged bindings, mixed
    use, literals, and every non-limit parameter remain identity-bearing
  - One cursor-specific canonical invariant-parameter hash domain used only by process-local named-
    query cursor lookup; shared query parameter, live-query, consumer, source, IR, module, plan,
    lock, token, protocol, storage, and durable identities remain byte-exact
  - Authoritative pre-data-work validation of the newly submitted limit followed by exact resumed
    execution at the prior snapshot/epoch position, with 1-to-maximum and maximum-to-1 forward and
    reverse pages having no duplicate, omission, restart, or authority drift
  - Negative semantic evidence for changed partition/filter/set/presence/plan/declared maximum,
    capability revision, snapshot/epoch, invalid limit, unpaged limit, and nearest K
  - Rust, Go, TypeScript, Python, CLI, MCP, local-driver, and remote-gRPC parity using the unchanged
    generated method and opaque token, plus handbook and compatibility guidance
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-ir -p riffdb-query-executor -p riffdb-query-module -p riffdb-service --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test operational_queries --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Only eligible invocation cardinality values are absent from cursor lookup; every other
    identity remains exact, changed valid page sizes traverse the same snapshot-bound ordered stream,
    invalid drift fails before data work, all language paths agree, and no durable/wire identity rotates.
  standing_design_tests:
    interface_safety: The compiler selects exclusion from immutable plan structure; callers cannot
      annotate hash fields or exceed the existing limit domain, and fresh Rust-owned authorization,
      policy, epoch, and continuation validation remains authoritative on every invocation.
    scale: Hash derivation is once per bounded request, resumed execution remains one compiled page,
      and no server materialization, hidden page walk, per-row proof, or raised ceiling is introduced.
  human_review_triggers: *id001
- id: WP-709
  name: Exact external larger-page coalescing acceptance
  depends_on: [WP-707, WP-708]
  objective: Close ADR-0159's narrow interoperability exception with framework-neutral exact append-
    only multi-page evidence and a value-free external-consumer receipt, retaining every framework
    artifact and upstream page allocation in its owning adapter repository.
  requirements: [OQ-004, OQ-016, OQ-030, OQ-031, OQ-068, OQ-069, OQ-070,
    OQ-071, OQ-072, OQ-073, OQ-074, DRV-012, DRV-014, DRV-016, DRV-017]
  required_adrs: [ADR-0055, ADR-0112, ADR-0120, ADR-0121, ADR-0124,
    ADR-0148, ADR-0155, ADR-0158, ADR-0159]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0159-page-cardinality-independent-query-cursors.md,
    crates/riffdb-query-executor/**, crates/riffdb-service/**,
    crates/riffdb-testkit/**, tests/drivers/**, fixtures/driver/**,
    scripts/driver-conformance, scripts/adapter-conformance,
    docs/SUMMARY.md, docs/riffql/**, docs/getting-started/**,
    docs/compatibility.md, docs/known-limitations.md]
  deliverables:
  - Framework-neutral client corpus requesting finite pages smaller than, equal to, and larger than
    one compiled RiffDB maximum; it requests min(remaining, MAX), follows one token chain, appends
    every row once in order, and returns the final token unchanged or none on true exhaustion
  - Deterministic cancellation and injected cursor/authorization/provider/transport failure at each
    boundary proving no partial success, restart, filter, sort, count, dedupe, discard, offset walk,
    raw query, storage access, or unbounded allocation enters the RiffDB side
  - Semantic invocation-count and backend-page evidence with no wall-clock threshold, plus large
    finite requested-size arithmetic that exhausts a small dataset without preallocating the request
  - Value-free external receipt pinning exact RiffDB/application identities and upstream conformance;
    no external schema, route, package, adapter source, framework name, or false parity claim lands here
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-executor -p riffdb-service -p riffdb-testkit --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/adapter-conformance --all-domains --all-languages
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Exact append-only external page translation passes generic and real-consumer evidence
    without weakening one RiffDB invocation, exposing partial success, adding framework code, or
    converting cursor walking into filtering, counting, sorting, offset, discard, or hidden fallback.
  standing_design_tests:
    interface_safety: RiffDB still exposes only one bounded generated page; the owning adapter may
      preserve its authoritative finite page contract solely by exact ordered append under one opaque
      cursor chain and cannot select database semantics or release partial protected results.
    scale: Every RiffDB call remains bounded by its compiled maximum; external count arithmetic is
      checked and finite, test populations remain bounded, and a future streaming optimization requires
      a separate accepted flow-control and partial-failure decision.
  human_review_triggers: *id001
- id: WP-710
  name: Columnar V2 workload ledger, format registry, and independent codec
  tier: surface
  depends_on: [WP-430, WP-455, WP-612, WP-645, WP-696]
  objective: After exact acceptance of ADR-0160, close the current row-framed columnar byte/CPU/
    allocation ledger and implement an independent bounded typed segment V2 codec with deterministic
    physical encodings and exact private statistics, without activating it in production or changing
    authoritative, public-query, or existing projection identities.
  requirements: [PRJ-001, PRJ-002, PRJ-003, PRJ-004, OQ-017, OQ-018, OQ-019,
    OQ-020, OQ-021, OQ-022, OQ-024, OQ-053, PERF-001, PERF-007, PERF-008,
    PERF-018]
  required_adrs: [ADR-0010, ADR-0017, ADR-0072, ADR-0085, ADR-0086,
    ADR-0111, ADR-0124, ADR-0130, ADR-0152, ADR-0160]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0160-rebuildable-columnar-segment-v2.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-columnar/**, crates/riffdb-projection/**,
    crates/riffdb-observability/**, fixtures/**, fuzz/**, tests/**, benchmarks/**,
    scripts/**, docs/architecture/**, docs/performance/**, docs/known-limitations.md]
  deliverables:
  - Fixed V1 row-framed baseline over incompressible, low-cardinality, clustered-selective, and
    full-scan projection corpora, recording logical/physical bytes, allocations, rows examined,
    canonical decode, merge, predicate, aggregate, sort, and output stages without application data
  - Additive topology-registered V2 segment and manifest identities binding definition, incarnation,
    generation, organization, frontier interval, row/column counts, primary-key/entity-version lanes,
    canonical directory order, complete checksums, and every fixed byte/cardinality ceiling
  - Closed deterministic encoding registry for fixed-width, offset/canonical, sorted dictionary plus
    bit-packed ordinal, Boolean/validity bitmap, and checked integer/timestamp delta lanes, with one
    stable smallest-exact-encoding selector and no external compression dependency
  - Exact min/max and missing/null/present statistic construction plus an independent pruning oracle;
    statistics are private, non-authoritative, non-policy-bearing, and unusable when type or policy
    proof is absent
  - Property/fuzz corpus for every encoding, type, optional state, offset, dictionary, bit width,
    delta, checksum, truncation, arithmetic, maximum, and noncanonical representation boundary
  - Mechanics receipt applying ADR-0160's byte, pruning, false-negative, and full-scan gates; a miss
    leaves production layout V1 and stops WP-711 rather than weakening a threshold after results
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-columnar -p riffdb-projection --all-features
  - cargo +nightly-2026-07-12 fuzz run columnar_segment_v2 -- -max_total_time=60
  - ./scripts/check-version-topology
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-types -p riffdb-columnar -p riffdb-projection --all-targets --all-features -- -D warnings
  exit_gate: The independent V2 codec is canonical, bounded, corruption-detecting, topology-
    registered, and logically byte-equivalent to V1; exact statistics have zero false-negative
    pruning; every ADR-0160 mechanics threshold passes before production activation is authorized.
  standing_design_tests:
    interface_safety: No application surface changes and no caller can select layout, encoding,
      statistics, pruning, validation, rebuild, or fallback; the codec owns no authority or query.
    scale: Segments, lanes, dictionaries, bitmaps, offsets, validation, fixtures, and diagnostics
      are statically bounded and stream per partition without full-database memory or rewrite.
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [PRJ-001, PRJ-002, PRJ-003, PRJ-004, OQ-017, OQ-018, OQ-019,
      OQ-020, OQ-021, OQ-022, OQ-024, OQ-053, PERF-001, PERF-007, PERF-008, PERF-018]
    evidence: The additive inactive Segment V2 and manifest V2 codecs are topology-registered,
      bounded, canonical, corruption-refusing, and byte-frozen without changing V1 production
      selection or authoritative semantics. Req-tagged projection, descriptor, epoch, fixture,
      pruning, validate-once, and ledger proofs pass. The exact 16,384-row release campaign passes
      high-cardinality bytes at 86.93 percent of V1, low-cardinality bytes at 55.25 percent,
      clustered pruning at 99 of 100 with zero false negatives, and full decode at 99.16 percent
      of V1 p50. The retained earlier 227.68- and 122.92-percent failures remain documented as
      rejected evidence, and the 60-second decoder fuzz campaign completed 29,050,758 executions
      without a crash. V2 remains inactive pending WP-711 lifecycle and recovery proof.
  human_review_triggers: *id001
- id: WP-776
  name: Schema-bound columnar identity and durable control foundation
  tier: guarantee
  depends_on: [WP-010, WP-596, WP-710]
  objective: Freeze and implement schema-bound scalar/vector columnar source bytes, a complete
    semantic hash, one specialized durable control/CAS, fresh authoritative V1 initialization,
    inert legacy validation, and symbolic configuration binding before V2 publication begins.
  requirements: [PRJ-002, PRJ-004, PRJ-005, PRJ-006, PRJ-007, PRJ-010,
    OQ-017, OQ-019, OQ-020, OQ-021, OQ-022, OQ-024, OQ-053]
  required_adrs: [ADR-0006, ADR-0010, ADR-0011, ADR-0017, ADR-0072, ADR-0085, ADR-0086,
    ADR-0111, ADR-0124, ADR-0130, ADR-0136, ADR-0160, ADR-0181, ADR-0190, ADR-0192]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0192-compiler-bound-columnar-publication-control.md,
    release/version-topology-v1.json, release/durable-format-manifest-v1.json,
    proto/riffdb/storage/**, crates/riffdb-proto/**, crates/riffdb-types/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-columnar/**,
    crates/riffdb-service/**, crates/riffdb-server/**,
    fixtures/proto/**, fixtures/storage/**,
    fixtures/compatibility/**, tests/**, scripts/**, templates/**,
    examples/app-baseline/**, docs/SUMMARY.md, docs/contracts/**,
    docs/architecture/**, docs/operations/**, docs/compatibility.md,
    docs/known-limitations.md]
  deliverables:
  - Exact ColumnarProjectionSourceV1 union over lineage plus accepted scalar
    DefinitionFingerprint or existing VectorProjectionSourceV1; exact definition-semantics bytes
    add primary-key schema and complete types without changing that legacy fingerprint
  - Dedicated ADR-0011 ColumnarProjectionSpec hash domain, typed semantics/spec hashes, exact
    frames/golden collision proofs, and complete provider/replay/vector semantic binding; no
    contract, Bundle, Executable IR, Lineage Ledger, query, or public identity rotates
  - StoredColumnarProjectionControlV1 tag 67 revision 1 with exact source/key/message/enum bytes,
    8,192-byte payload and 8,289-byte envelope maxima, columnar_projection_controls table,
    durable-manifest entry, registry migration, and memory/redb conformance
  - Closed expected-control operations proving transaction-current frontier checks, monotonic
    immutable V1 advancement alongside an unchanged V2 candidate, every exact pointer/failure/
    servability/retention shape, Unprepared/Prepared evidence transition, Candidate/Published-
    failure transition, semantic-hash rebuild, uncertainty reread, and no generic control
  - Fresh V1 rebuild from authoritative snapshot plus retained tail after every source begins at a
    durable BeforeFirst fence and advances it only after its captured snapshot is durably validated;
    legacy scalar files, name-derived paths, and tag-65 values never seed common control and remain
    inert until deterministic orphan cleanup or WP-757 removal
  - Existing symbolic projection_name and scalar configuration resolved through one checked bundle
    to one schema-bound source, with duplicate aliases and unsafe names rejected, all-or-nothing
    256-source admission, sibling isolation, bounded remediation, handbook coverage, and example
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-storage-api -p riffdb-storage-memory
    -p riffdb-storage-redb -p riffdb-proto
    -p riffdb-columnar -p riffdb-service -p riffdb-server --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-version-topology
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-types -p riffdb-storage-api -p riffdb-storage-memory
    -p riffdb-storage-redb -p riffdb-proto
    -p riffdb-columnar -p riffdb-service -p riffdb-server
    --all-targets --all-features -- -D warnings
  exit_gate: Every scalar or vector columnar source has one canonical schema-bound identity and one
    bounded durable selector; V1 advances without disturbing its V2 candidate; common state starts
    only from authority while legacy state is inert; public callers retain only symbolic names; and
    every new durable/topology identity plus the temporary tag-65 exception is fixture-frozen.
  standing_design_tests:
    interface_safety: Applications keep only accepted symbolic projection names and cannot
      supply a durable source, control, layout, generation, migration, validation, fallback, or bound.
    scale: One bundle/database has at most 256 specs/controls, structural legacy scanning stops at
      4,096 rows, keys are at most 291 bytes, definition semantics at most 1 MiB, control payloads
      at most 8,192 bytes, and rebuild streams within existing partition/segment/replay limits.
  closure:
    status: complete
    completed_at: "2026-09-05"
    requirements_satisfied: [PRJ-002, PRJ-004, PRJ-005, PRJ-006, PRJ-007, PRJ-010,
      OQ-017, OQ-019, OQ-020, OQ-021, OQ-022, OQ-024, OQ-053]
    evidence: Scalar and vector columnar sources now have schema-bound identities, complete
      semantic hashes, and one bounded tag-67 durable control whose expected-control transitions,
      retention inputs, uncertainty recovery, and V1 advancement preserve every disjoint V2
      candidate. Startup establishes fresh authoritative BeforeFirst controls before reopening
      readiness or pruning; legacy tag-65 values and name-derived files remain structurally
      validated but inert. Checked-bundle resolution retains symbolic application names, rejects
      duplicate or unsafe admission before I/O, and binds the closed provider, policy, replay, and
      definition semantics. All six ADR-0192 obligations are proven, and package acceptance passed
      all 9 gates with 1,526 of 1,526 tests; V2 publication remains owned by WP-711 and tag-65
      removal remains owned by WP-757.
  human_review_triggers: *id001
- id: WP-711
  name: Crash-safe columnar V2 rebuild, compaction, and zone-map activation
  tier: guarantee
  depends_on: [WP-610, WP-667, WP-710, WP-776, WP-781]
  objective: Activate ADR-0160's accepted V2 format through disjoint generation rebuild, atomic
    publication, immutable compaction, validate-once reopen, and exact segment pruning while V1
    remains readable and published until a complete matched-frontier V2 candidate succeeds.
  requirements: [REC-001, PRJ-002, PRJ-004, PRJ-006, PRJ-008, PRJ-009, PRJ-010,
    OQ-017, OQ-019, OQ-020, OQ-021, OQ-022, OQ-024, OQ-053,
    PERF-007, PERF-008]
  required_adrs: [ADR-0006, ADR-0010, ADR-0017, ADR-0072, ADR-0085, ADR-0086,
    ADR-0111, ADR-0124, ADR-0130, ADR-0136, ADR-0160, ADR-0181, ADR-0190, ADR-0192,
    ADR-0200]
  # Cargo.lock is in scope solely to record the approved acyclic
  # riffdb-storage-redb -> riffdb-columnar -> riffdb-storage-api dependency edge required for
  # columnar-owned, unforgeable prepared-generation witness trait ownership.
  allowed_paths: [SPEC.md, work_packages.yaml, Cargo.lock,
    adr/0160-rebuildable-columnar-segment-v2.md,
    adr/0190-atomic-columnar-v2-generation-root-publication.md,
    adr/0192-compiler-bound-columnar-publication-control.md,
    adr/0200-columnar-control-reset-for-history-incarnation.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-columnar/**,
    crates/riffdb-projection/**, crates/riffdb-service/**,
    crates/riffdb-server/**, crates/riffdb-observability/**,
    fixtures/**, tests/**, scripts/**, docs/architecture/**,
    docs/performance/**, docs/operations/**, docs/compatibility.md,
    docs/known-limitations.md]
  deliverables:
  - Prepared-root publication through one exact-control, transaction-current-head CAS, with a
    process-local query capture gate, immutable view installation before acknowledgement, and
    selected-V2 corruption refusing without V1 fallback
  - Canonical bounded ColumnarGenerationRootV1 over the complete partition inventory, with the
    root-only physical-generation fingerprint preserving Segment V2, Manifest V2, provider,
    query-artifact, and WP-710 fixture bytes exactly
  - Per-database V1/V2 lifecycle using the specialized never-reused common columnar control and
    transaction-current prepared-generation compare-and-set; V1 remains the ordinary readable and
    writable path until that database and source durably publishes one complete V2 root
  - Startup invokes only ResetForCurrentHistoryIncarnation for one exact uniformly stale tag-67
    control, reads the replacement incarnation inside the Immediate transaction, emits one fresh
    Building/Unprepared-V1/BeforeFirst Candidate, and keeps serving and retention closed through
    Applied, mismatch, storage-failure, unknown-commit, repeated-restart, and durable-reread arms
  - After reset and ordinary spec retarget, every final Unprepared V1 Candidate retires only its
    exact final, temporary, and deterministic quarantine paths through the bounded reference-safe
    crash-repeating state machine before fresh construction; no pre-restore artifact is adopted
  - V1-published/V2-candidate lifecycle that rebuilds from one authoritative snapshot plus retained
    tail, verifies independent logical equality at a frontier, and atomically publishes one-layout
    V2 generation without mixed manifests or request-visible partial state
  - Immutable temporary-write/sync/checksum/rename/manifest compaction with exact supersession,
    deletion/retraction, holdback, idempotent replay, visible/durable frontier, generation retirement,
    retention, cancellation, ENOSPC, and prior-generation rollback behavior
  - Open-time complete validation into immutable process-local segment views; production query hot
    paths perform no per-operation durable re-read/re-hash/re-proof while scrub, restore, unclean
    recovery, and rebuild retain full validation
  - Exact zone-map/dictionary pruning in the production scan path with policy-aligned or fixed-work
    handling for inference-sensitive policy shapes and no public statistic, skip count, or data-
    dependent diagnostic
  - Crash matrix at every segment/header/lane/manifest/checkpoint/publication/retirement boundary,
    including current, stale, future, zero, mixed, malformed and exhausted incarnation state;
    stale-plus-spec-drift; every reset/path remove/rename/sync edge; Applied, mismatch, failure and
    uncertainty; no-serving/no-retention; interrupted rebuild, publication, acknowledgement and
    repeated reopen, proved by all five OBL-0200 test identities
  - Current V1/V2 matched-frontier query, recovery, compaction, rebuild, bytes, CPU, allocation,
    projection-lag, and no-projection/no-V2 non-regression receipts
  acceptance_commands:
  - ./scripts/acceptance --wp WP-711
  - cargo +1.97.0 test -p riffdb-storage-api -p riffdb-storage-redb
    -p riffdb-columnar -p riffdb-projection -p riffdb-service -p riffdb-server --all-features
  - ./scripts/recovery_full
  - ./scripts/check-version-topology
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/check-adr-obligations
  - ./scripts/wp711-columnar-v2-receipt --self-test
  - ./scripts/wp711-columnar-v2-receipt --validate docs/performance/wp-711-columnar-v2-activation.json
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-columnar -p riffdb-projection -p riffdb-service -p riffdb-server --all-targets --all-features -- -D warnings
  exit_gate: A stale restored tag-67 control first reaches only one transaction-current fresh V1
    candidate through exact reset, reread, and bounded path retirement with all gates closed; a
    complete V2 generation then publishes only after matched-frontier equality and survives every
    crash/rebuild/compaction case; V1 remains exact and readable; pruning is exact and policy-safe;
    no request repeats durable proof or observes mixed/partial derived state.
  standing_design_tests:
    interface_safety: V2 selection and pruning are private generation facts; applications cannot
      weaken policy, freshness, validation, lifecycle, fallback, or any query/resource bound.
    scale: Rebuild, validation, compaction, pruning, retained handles, and recovery are partition/
      segment bounded and streaming; failed candidates cannot pin history beyond replay budgets.
  closure:
    status: complete
    completed_at: "2026-09-05"
    requirements_satisfied: [REC-001, PRJ-002, PRJ-004, PRJ-006, PRJ-008, PRJ-009, PRJ-010,
      OQ-017, OQ-019, OQ-020, OQ-021, OQ-022, OQ-024, OQ-053,
      PERF-007, PERF-008]
    evidence: Columnar V2 now rebuilds into a disjoint complete generation, independently proves
      matched-frontier equality, publishes through the exact transaction-current control CAS,
      installs one validated immutable view before reopening capture, and refuses corrupt selected
      state without V1 fallback. Immutable compaction, validate-once reopen, bounded exact pruning,
      retirement, and recovery preserve V1 until successful selection. Startup resets only one
      uniformly stale tag-67 control using the transaction-current history incarnation, durably
      rereads every result while gates remain closed, and retires only the exact colliding candidate
      paths before reconstruction. All ADR-0190, ADR-0192, and ADR-0200 obligations are proven; the
      process-level recovery matrix passed all 9 cases; the integrated production receipt proves
      two 8,192-row matched-frontier cases with zero lag and a smaller V2 footprint; and package
      acceptance passed all 16 gates with 1,293 of 1,293 selected tests.
  human_review_triggers: *id001
- id: WP-712
  name: Vectorized columnar batch executor and late materialization
  depends_on: [WP-696, WP-711]
  objective: After exact acceptance of ADR-0161, replace row-at-a-time V2 execution with one
    compiler-sealed bounded batch program, monotone selection vectors, exact aggregate partials,
    bounded top-N, and policy-first late materialization while retaining an independent scalar oracle.
  requirements: [PRJ-001, PRJ-002, PRJ-003, PRJ-004, QRY-001, QRY-002,
    QRY-003, QRY-004, QRY-005, OQ-017, OQ-019, OQ-021, OQ-022, OQ-024,
    OQ-044, OQ-045, OQ-046, OQ-047, OQ-048, OQ-049, OQ-050, OQ-051,
    OQ-052, OQ-055, PERF-001, PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0051, ADR-0053, ADR-0070, ADR-0071, ADR-0086,
    ADR-0087, ADR-0111, ADR-0129, ADR-0130, ADR-0152, ADR-0160, ADR-0161]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, governance/tiers.yaml,
    adr/0161-compiler-sealed-vectorized-columnar-execution.md,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-catalog/**,
    crates/riffdb-query-executor/**, crates/riffdb-columnar/**,
    crates/riffdb-policy/**, crates/riffdb-observability/**,
    fixtures/**, tests/**, benchmarks/**, scripts/**,
    docs/architecture/**, docs/performance/**, docs/known-limitations.md]
  deliverables:
  - Compiler/provider-owned column-use program ordering segment eligibility, policy fields,
    predicates, aggregate/order keys, bounded top-N, and output-only materialization with exact
    types, optional semantics, charges, and a maximum width from the closed batch-width set
  - Bounded borrowed/decoded lane batches, validity maps, and monotone selection vectors; policy
    runs before result shaping, rejected rows never decode output-only lanes, and no row/task grows
    retained process state
  - Exact deterministic aggregate/group partials using ADR-0152 merge laws and canonical segment/
    batch merge order, plus complete total-order bounded heap selection and unchanged cursor results
  - Scalar/vector randomized differential suite across every predicate, optional state, aggregate,
    group, ordering, overflow, cancellation, output budget, and maximum-width boundary
  - Architecture evidence for no caller physical inputs, no scalar fallback after V2 failure, no
    per-row proof/task/allocation, and no worker/storage/publication authority in the executor
  - Single-threaded V1, V2 scalar, and V2 batch CPU/allocation/throughput receipt satisfying
    ADR-0161's vectorized mechanics threshold without selective-query regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-executor -p riffdb-columnar -p riffdb-policy --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-query-compiler -p riffdb-query-executor -p riffdb-columnar -p riffdb-policy --all-targets --all-features -- -D warnings
  exit_gate: V2 batch results and failures equal the independent scalar evaluator under every
    deterministic schedule; policy precedes all shaping; late materialization is proven; and the
    predeclared CPU/throughput gate passes before the batch executor can become production default.
  standing_design_tests:
    interface_safety: Callers retain only finite compiled operations and cannot select width,
      lanes, phases, vectorization, top-N algorithm, fallback, policy order, or partial results.
    scale: Widths, vectors, partials, heaps, decoded lanes, outputs, arithmetic, and diagnostics are
      compiler bounded per batch/query with no population-sized materialization.
  human_review_triggers: *id001
- id: WP-713
  name: Bounded parallel columnar scans and public projected-query activation
  depends_on: [WP-432, WP-667, WP-712]
  objective: Activate ADR-0161's batch executor through one finite fair-share segment scheduler,
    immutable one-frontier snapshots, deterministic merge, cancellation, and existing public
    projected-query surfaces, without changing results, generated methods, cursors, or authority.
  requirements: [PRJ-001, PRJ-002, PRJ-003, PRJ-004, QRY-001, QRY-002,
    QRY-003, QRY-004, QRY-005, OQ-017, OQ-019, OQ-020, OQ-021, OQ-022,
    OQ-024, OQ-055, PERF-001, PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0053, ADR-0070, ADR-0071, ADR-0086, ADR-0087,
    ADR-0111, ADR-0129, ADR-0130, ADR-0160, ADR-0161]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0161-compiler-sealed-vectorized-columnar-execution.md,
    crates/riffdb-columnar/**, crates/riffdb-projection/**,
    crates/riffdb-policy/**, crates/riffdb-service/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, examples/app-baseline/**,
    fixtures/**, tests/**, benchmarks/**, scripts/**,
    docs/architecture/**, docs/performance/**, docs/operations/**,
    docs/known-limitations.md]
  deliverables:
  - One process-global bounded columnar scan scheduler with static worker ceiling, available-
    parallelism selection, bounded queue/tasks/buffers, per-query fair-share admission, explicit
    cancellation, and no thread/task per row, group, value, result, or completed operation
  - Workers retain one immutable published snapshot and provider epoch, own no authoritative or
    publication authority, and return bounded partials merged in canonical segment/batch order
  - Existing causal/bounded/available freshness, post-wake and pre-release authorization,
    revocation, lifecycle outcomes, cursor identity, exact result bytes, and no-partial-success
    semantics across cancellation, slow worker, saturation, compaction, rebuild, and shutdown
  - Policy-sensitive fixed-work/policy-aligned scheduling and redaction-safe fixed-cardinality
    telemetry for admission, active workers, batches, examined rows, pruning class, CPU, wait,
    cancellation, lag, and output without tenant/data labels
  - Public gRPC, MCP, CLI, and generated-client equivalence using unchanged schemas and one neutral
    query corpus; V1/scalar remains selected when V2 is not activated, never as silent error fallback
  - One/four-worker 100k-row and simultaneous interactive-load receipt satisfying ADR-0161's
    scaling and p95 guards with no write, catch-up, freshness, correctness, or no-projection regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-columnar -p riffdb-projection -p riffdb-policy -p riffdb-service -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-client-rust -p riffdb-server --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public surface returns unchanged exact results from one authorized frontier;
    scheduler work and shutdown are bounded; deterministic merges survive adversarial schedules;
    and the cross-load scaling/tail gate passes before parallel execution becomes production default.
  standing_design_tests:
    interface_safety: Applications cannot select workers, tasks, width, scheduling, physical path,
      fallback, freshness, policy, or partial success; every safe point and typed outcome remains.
    scale: Scheduler workers, queues, tasks, snapshots, partials, buffers, waits, and telemetry are
      globally and per-query bounded independently of population, tenants, or runtime duration.
  human_review_triggers: *id001
- id: WP-714
  name: Exact incremental bitmap, rollup, and top-K provider state
  depends_on: [WP-645, WP-696, WP-711, WP-712]
  objective: After exact acceptance of ADR-0162, implement one compiler-owned bounded incremental-
    structure registry and rebuildable provider-state generation for segment-local equality/presence
    bitmaps, exact mergeable rollups, and maintained exact top-K lanes without a general view language,
    approximation, framework branch, or caller-selected provider.
  requirements: [PRJ-001, PRJ-002, PRJ-003, PRJ-004, OQ-017, OQ-018,
    OQ-019, OQ-020, OQ-021, OQ-022, OQ-023, OQ-024, OQ-044, OQ-045,
    OQ-046, OQ-047, OQ-048, OQ-049, OQ-050, OQ-051, OQ-052, OQ-053,
    OQ-054, OQ-055, PERF-001, PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0010, ADR-0017, ADR-0051, ADR-0053, ADR-0085,
    ADR-0086, ADR-0111, ADR-0124, ADR-0130, ADR-0152, ADR-0160, ADR-0161,
    ADR-0162]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0162-incremental-analytical-provider-structures.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-catalog/**,
    crates/riffdb-columnar/**, crates/riffdb-projection/**,
    crates/riffdb-policy/**, crates/riffdb-observability/**,
    fixtures/**, tests/**, benchmarks/**, scripts/**,
    docs/architecture/**, docs/performance/**, docs/riffql/**,
    docs/getting-started/**, docs/known-limitations.md]
  deliverables:
  - Closed compiler/catalog descriptor registry binding structure kind, exact source/predicate/
    policy/order/aggregate semantics, provider identity, generation/epoch, state/write/rebuild
    bounds, and finite consuming named-query plans with source-spanned excessive/unsupported refusal
  - Segment-local exact equality and missing/null/present bitmaps over eligible V2 lanes with bounded
    cardinality/bytes and no implicit cross-provider row-ID bridge
  - Retraction-aware exact count/present/sum/mean/any/all rollups; min/max or distinct activate only
    with bounded counted exact state, never approximation or guessed replacement after deletion
  - Exact bounded top-K plus repair reserve under one complete total order and entity-key tie-breaker;
    exhausted proof becomes rebuilding/unavailable rather than approximate output
  - Atomic idempotent per-commit private apply and all-or-none frontier publication across row state
    and every affected structure, plus canonical rebuild, checkpoint, compaction, replay, retirement,
    corruption, cancellation, overflow, and retained-tail behavior
  - Policy-invariant or exact policy-aligned state identities proving denied rows influence no count,
    membership, rank, top-K, timing class, diagnostics, lifecycle, or shared availability
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog -p riffdb-columnar -p riffdb-projection -p riffdb-policy --all-features
  - ./scripts/recovery_full
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every incremental structure is compiler-declared, exact, bounded, policy-safe,
    retraction-correct, rebuildable, crash-safe, and frontier-atomic; unsupported/overflow state fails
    typed; no approximation, general view, framework branch, scan drift, or caller provider exists.
  standing_design_tests:
    interface_safety: Generated callers cannot request structures, fields, functions, precision,
      refresh, fallback, stale success, policy widening, or bounds; only exact compiled capabilities run.
    scale: Structures, keys, groups, entries, bytes, write amplification, apply/rebuild work, lag,
      retained history, and diagnostics are statically bounded per partition and provider generation.
  human_review_triggers: *id001
- id: WP-715
  name: Columnar Program B differential, security, and performance acceptance
  depends_on: [WP-713, WP-714]
  objective: Close ADR-0160 through ADR-0162 with cross-plane matched-frontier differential,
    crash/rebuild, policy/inference, no-regression, and workload performance evidence over a neutral
    ticket queue and run-metric dashboard before any Program B capability enters alpha qualification.
  requirements: [PRJ-001, PRJ-002, PRJ-003, PRJ-004, QRY-001, QRY-002,
    QRY-003, QRY-004, QRY-005, OQ-017, OQ-018, OQ-019, OQ-020, OQ-021,
    OQ-022, OQ-023, OQ-024, OQ-044, OQ-045, OQ-046, OQ-047, OQ-048,
    OQ-049, OQ-050, OQ-051, OQ-052, OQ-053, OQ-054, OQ-055, PERF-001,
    PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0010, ADR-0053, ADR-0070, ADR-0071, ADR-0085,
    ADR-0086, ADR-0087, ADR-0111, ADR-0124, ADR-0130, ADR-0152, ADR-0160,
    ADR-0161, ADR-0162]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0160-rebuildable-columnar-segment-v2.md,
    adr/0161-compiler-sealed-vectorized-columnar-execution.md,
    adr/0162-incremental-analytical-provider-structures.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-catalog/**,
    crates/riffdb-query-executor/**, crates/riffdb-columnar/**,
    crates/riffdb-projection/**, crates/riffdb-policy/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-client-rust/**,
    crates/riffdb-server/**, crates/riffdb-observability/**,
    clients/**, fixtures/**, examples/app-baseline/**, tests/**,
    benchmarks/**, scripts/**, release/evidence/**,
    docs/SUMMARY.md, docs/architecture/**, docs/performance/**,
    docs/operations/**, docs/riffql/**, docs/getting-started/**,
    docs/compatibility.md, docs/known-limitations.md]
  deliverables:
  - One independent materialized oracle comparing V1 scalar, V2 scalar, V2 vectorized, parallel V2,
    and incremental provider results/failures at identical history incarnation, generation, policy,
    parameters, snapshot, provider epoch, and frontier across randomized histories
  - Full crash/restart/rebuild/compaction/retention/cancellation/deploy-under-load matrix with exact
    final-state/frontier reconciliation, V1 readability, V2 corruption refusal, and bounded resources
  - Adversarial organization, row/field/secret policy, capability revision, revocation, hidden
    distribution, timing class, aggregate, bitmap, top-K, cursor, diagnostic, and lifecycle evidence
  - Fixed 1k/10k/100k/1M partition profiles across 1/10/100-percent selectivity, full/grouped
    aggregates, ordered top-N, concurrency 1/16/64, sustained writes, catch-up, compaction, cold/warm,
    recording CPU, p50/p95/p99, allocations, bytes, pruning, lag, amplification, and rebuild throughput
  - Neutral ticket-queue and run-metric generated/public application workloads plus value-free
    external-consumer receipts; no external framework schema, route, package, or implementation lands
  - Gate table for every ADR-0160/0161/0162 mechanics threshold and no-regression condition; any miss
    disables only the failed capability and is reported honestly rather than broadening scope or bounds
  - Handbook, compatibility, operations, rebuild, capacity-planning, and known-limitation guidance
    describing only activated behavior and retaining deferred approximation/general-view boundaries
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/recovery_full
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Activated Program B paths are exact, policy-safe, frontier-atomic, crash/rebuild safe,
    resource bounded, cross-surface equivalent, and meet every predeclared performance gate; failed
    candidates remain disabled with value-free receipts and no public or compatibility overclaim.
  standing_design_tests:
    interface_safety: Acceptance uses only compiled public/generated operations and cannot inject
      layout, executor, structure, policy, fallback, freshness, or resource choices unavailable to apps.
    scale: Evidence spans fixed large partitions but every runtime/evidence artifact, buffer, task,
      state structure, retention hold, and diagnostic remains bounded independently of total history.
  human_review_triggers: *id001
- id: WP-716
  name: Compiler-sealed command-decision language and V18 identities
  depends_on: [WP-697]
  objective: Implement ADR-0163's deferred initialized binding and one closed ordered decision
    algebra with apply, exact no-effect, and typed whole-command rejection arms, preserving finite
    dataflow, union authority, branch cost, same-partition target visibility, and least-sufficient
    V18 identities without adding general control flow or framework behavior.
  requirements: [BLK-046, BLK-047, BLK-048, BLK-049, BLK-051, BLK-053,
    BLK-054, BLK-055, DSL-001, DSL-002, DSL-003, DSL-004, DSL-005, DSL-006,
    DSL-007, DSL-008, DSL-009, DSL-010, DSL-011, DSL-012, SAFE-004, SAFE-005]
  required_adrs: [ADR-0002, ADR-0003, ADR-0012, ADR-0031, ADR-0055,
    ADR-0107, ADR-0124, ADR-0129, ADR-0147, ADR-0149, ADR-0153, ADR-0163]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0163-compiler-sealed-command-decisions.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-diagnostics/**,
    crates/riffdb-cli/assets/editor/**, clients/editor/**, fixtures/compiler/**,
    fixtures/contracts/**, fuzz/**, tests/**, scripts/**, docs/SUMMARY.md,
    docs/contracts/**, docs/architecture/**, docs/known-limitations.md]
  deliverables:
  - Source grammar, formatter, syntax AST, source spans, editor grammar, and bounded item accounting
    for observe_or_initialize plus immediately consuming decide blocks with one through eight ordered
    when arms, mandatory else, and exact apply/no_effect/reject arm kinds
  - Compiler-resolved deferred initialized binding and arm-local program whose predicates,
    expressions, targets, effects, outcomes, reveals, and dependencies are deterministic, typed,
    finite, same-partition, and current-element/input computable, with nesting and dynamic behavior
    rejected at exact source spans
  - Path-sensitive definite assignment for absent/present and every arm, complete apply postimages,
    unchanged no-effect/reject successors, common-success value safety, branch-local create safety,
    secret flow, relationship, uniqueness, invariant, and alias analysis
  - Static discovery/role/capability metadata containing the union of every reachable arm authority
    and selected-state create/update policy requirements without exposing state origin or arm choice
  - Conservative cost proof charging maximum selected application effects plus real union overhead
    beneath unchanged collection, byte, target, graph, index, validation, correlated-work, retry,
    outcome, and partition limits, with pay-once architecture assertions
  - Least-sufficient grammar/executable-IR/bundle V18 tags and canonical arm encoding, topology
    registration, golden/hash/lock fixtures, old-reader refusal, and byte-exact V1-V17 writer corpus
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler --all-features
  - cargo +1.97.0 test -p riffdb-diagnostics --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-diagnostics --all-targets --all-features -- -D warnings
  exit_gate: Valid V18 decisions lower to one finite exact arm program with complete dataflow,
    authority, policy, target, cost, and compatibility proofs; every invalid shape fails at a source
    span; old artifacts remain exact; and no general branch, callback, state-origin value, dynamic
    target/outcome, framework path, or unregistered identity exists.
  standing_design_tests:
    interface_safety: Applications declare only one finite arm set over compiler-known values and
      targets; they cannot submit control flow, select an arm, inspect origin, choose transaction or
      conflict behavior, weaken atomicity, or opt out of a guarantee.
    scale: Arms, depth, bindings, targets, expressions, bytes, effects, indexes, and collection
      elements are statically bounded; compiler proof is per plan and introduces no scan, dynamic
      graph, cross-partition work, or population-sized state.
  closure:
    status: complete
    completed_at: "2026-08-27"
    requirements_satisfied: [BLK-046, BLK-047, BLK-048, BLK-049, BLK-051,
      BLK-053, BLK-054, BLK-055, DSL-001, DSL-002, DSL-003, DSL-004, DSL-005,
      DSL-006, DSL-007, DSL-008, DSL-009, DSL-010, DSL-011, DSL-012, SAFE-004,
      SAFE-005]
    evidence: Contract grammar, typed AST/HIR, path-sensitive analysis, role derivation, maximum-arm
      cost proof, canonical V18 IR and bundle encoding, format registry, version topology, editor
      artifacts, old-reader refusal, byte-exact fixtures, source-spanned negative corpus, and
      compiler/generated-reference gates are green. Branch-local creates, field writes, embeddings,
      durable events, no-effect, and typed rejection lower to one finite sealed decision program.
    known_limitations: Workflow transition and lease instructions remain in their existing dedicated
      command forms and are source-spanned rejections inside decision arms. The decision construct
      intentionally exposes no general branch, callback, origin value, or per-item partial outcome.
    follow_ups: [WP-717, WP-718]
  human_review_triggers: *id001
- id: WP-717
  name: Atomic command-decision runtime policy and recovery
  depends_on: [WP-698, WP-716]
  objective: Execute ADR-0163 decisions from exact transaction-current observations so apply commits
    only its selected graph, no-effect commits no application effects while remaining revalidated,
    and rejection atomically discards the complete ordinary or bounded collection command across
    memory/redb, retries, idempotency, policy, uncertainty, and crash recovery.
  requirements: [BLK-046, BLK-048, BLK-049, BLK-050, BLK-051, BLK-052,
    BLK-053, BLK-054, BLK-056, TXN-010, TXN-011, TXN-012, TXN-013,
    PERF-004, PERF-005, SAFE-004, SAFE-005]
  required_adrs: [ADR-0002, ADR-0003, ADR-0004, ADR-0005, ADR-0012,
    ADR-0023, ADR-0055, ADR-0107, ADR-0124, ADR-0129, ADR-0147, ADR-0149,
    ADR-0153, ADR-0163]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0163-compiler-sealed-command-decisions.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-ir/**, crates/riffdb-invariant/**,
    crates/riffdb-runtime/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-policy/**, crates/riffdb-service/**,
    crates/riffdb-testkit/**, fixtures/**, tests/**, fuzz/**, scripts/**,
    docs/contracts/**, docs/architecture/**, docs/known-limitations.md]
  deliverables:
  - Snapshot materialization for deferred absent/present initialized records and selected branch-local
    dependencies, retaining exact absence/version/hash evidence and no state-origin value
  - Deterministic ordered predicate evaluation producing only the selected apply graph, an exact
    no-application-effect dependency, or one typed whole-command rejection with no unselected
    mutation/event/index/changelog construction
  - Coordinator capability acquisition and complete revalidation schedules proving stale apply,
    ignore, and rejection choices are discarded and every arm is reselected on bounded retry
  - Union invocation authority plus exact absent create and present update policy, unchanged-successor
    policy for no-effect/reject, branch-local policy, revocation, secrets, relationships, uniqueness,
    invariants, events, provenance, redaction, and response-release equivalence
  - Ordinary and 1/9/19/100-element all-apply, all-ignore, mixed, and each-rejection execution under
    duplicate, byte, graph, index-work, cancellation, saturation, and complete-or-absent boundaries
  - Memory/redb failpoint and replay corpus proving all-no-effect still has one durable outcome/audit/
    provenance/sequence, uncertain resolution is exact, and no application effect survives rejection
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-runtime -p riffdb-commit -p riffdb-policy -p riffdb-service --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test bulk_command_recovery --all-features
  - ./scripts/recovery_full
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-runtime -p riffdb-commit -p riffdb-policy -p riffdb-service -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-targets --all-features -- -D warnings
  exit_gate: Every decision uses transaction-current evidence and commits exactly its selected
    complete graph, no-effect, or whole-command rejection with exact policy, replay, uncertainty,
    crash, and bounded bulk behavior; no stale choice, partial effect, framework behavior, repeated
    proof, or weakened ceiling remains.
  standing_design_tests:
    interface_safety: Runtime derives the arm only from the compiled program and revalidated state;
      no caller, adapter, transport, backend, or configuration can choose it or bypass authority,
      policy, validation, atomicity, idempotency, durability, or provenance.
    scale: One bounded decision and selected arm execute per target beneath existing finite retry,
      graph, index, byte, collection, and partition bounds with no scan, task per row, dynamic target
      discovery, or cross-partition transaction.
  closure:
    status: complete
    completed_at: "2026-08-27"
    requirements_satisfied: [BLK-046, BLK-048, BLK-049, BLK-050, BLK-051,
      BLK-052, BLK-053, BLK-054, BLK-056, TXN-010, TXN-011, TXN-012, TXN-013,
      PERF-004, PERF-005, SAFE-004, SAFE-005]
    evidence: The deterministic runtime materializes absent and present deferred records, selects
      ordered arms, constructs only selected graphs, and discards the complete graph on rejection.
      Coordinator validation replays V18 evaluation once against exact evidence, policy covers
      absent-create and present-update no-effect states, and redb concurrency proves stale apply to
      no-effect reselection plus whole-command bulk rejection. The 1/9/19/100 runtime corpus and
      before/after-commit redb recovery cover apply, mixed, all-no-effect, events, durable outcomes,
      provenance, sequences, and exact replay. All package acceptance and recovery_full gates pass.
    known_limitations: Influential snapshot acquisition conservatively retains the compiler-sealed
      union of branch observations; this can cause a safe extra bounded retry when an unselected
      branch-local observation changes, but cannot admit a stale choice or repeated proof per row.
    follow_ups: [WP-718]
  human_review_triggers: *id001
- id: WP-718
  name: Command-decision generated compatibility and external acceptance
  depends_on: [WP-699, WP-717]
  objective: Close ADR-0163 across generated/public surfaces, compatibility topology, handbook,
    architecture checks, and one value-free real-consumer receipt while retaining all external
    schemas, policy names, routes, adapter code, and generated profiles in their owning repository.
  requirements: [BLK-046, BLK-049, BLK-051, BLK-052, BLK-054, BLK-055,
    BLK-056, BLK-057, DRV-007, DRV-008, DRV-012, DRV-014, SAFE-004,
    SAFE-005, PERF-004, PERF-005]
  required_adrs: [ADR-0055, ADR-0107, ADR-0124, ADR-0129, ADR-0142,
    ADR-0147, ADR-0148, ADR-0149, ADR-0153, ADR-0163]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0163-compiler-sealed-command-decisions.md,
    release/version-topology-v1.json, proto/**, crates/**, clients/**,
    templates/**, fixtures/**, examples/**, tests/**, benchmarks/**, scripts/**,
    docs/**, release/evidence/**]
  deliverables:
  - Rust, Go, TypeScript, Python, CLI, MCP, local-driver, and remote-gRPC exact operation/input/
    outcome/error/authority conformance without a caller-visible arm, origin, transaction, target,
    predicate, policy, callback, or handwritten semantic branch
  - Framework-neutral generated lifecycle application covering absent/present apply, no-effect,
    mixed bulk, typed rejection, event/changelog, policy, secret, uncertainty, and replay
  - V1-V17/V18 compatibility, deployment rotation, recovery, generated-artifact, application-lock,
    role, topology, and clean-install evidence with no durable decoder retirement
  - Stage-attributed operation/mutation/index/changelog receipt proving ignored elements emit no
    application work and unselected arms do not pay physical-effect cost
  - Value-free external capability receipt proving one real consumer's atomic apply/ignore/reject
    contract, with all external code and vocabulary absent from RiffDB
  - Handbook and architecture inventory distinguishing sealed decisions from general branching,
    callbacks, per-item partial outcomes, dynamic targets, and consumer-owned business policy
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every generated surface, backend, policy case, restart path, bound, and compatibility
    window preserves exact apply/no-effect/rejection semantics; generic and external receipts are
    green; and no framework code, caller branch, stale choice, false changelog, or unexplained
    generated drift remains.
  standing_design_tests:
    interface_safety: Generated callers receive only the named command's typed input and outcomes;
      no surface exposes branch control, origin, transaction machinery, target selection, or a
      guarantee opt-out.
    scale: Acceptance uses bounded generic corpora and value-free receipts; generation and transport
      add no per-row proof, unbounded allocation, dynamic program, or external population state.
  closure:
    status: complete_without_external_receipt
    completed_at: "2026-08-27"
    requirements_satisfied: [BLK-046, BLK-049, BLK-051, BLK-052, BLK-054,
      BLK-055, BLK-056, DRV-007, DRV-008, DRV-012, DRV-014, SAFE-004,
      SAFE-005, PERF-004, PERF-005]
    evidence: Generated Rust, Go, TypeScript, Python, and MCP conformance proves callers see only
      named command inputs and outcomes, never arm, origin, transaction, target, predicate, or
      policy selectors. The remote four-language driver corpus, canonical V18 fixture, generated
      freshness, topology, handbook, workspace Clippy, recovery, and generic redb lifecycle corpus
      are green. The workspace test command reaches only the pre-existing unrelated riffdb-sim
      dirty-shutdown oracle failure.
    known_limitations: BLK-057 remains open until an owning external repository records one
      value-free real-consumer apply/ignore/reject receipt. RiffDB contains no external schema,
      route, adapter, or framework vocabulary and does not claim that external acceptance early.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-719
  name: Admission-head service floor provider negotiation and cursor identity
  depends_on: [WP-645, WP-667, WP-696, WP-708]
  objective: Implement ADR-0164's stronger AdmissionHead query option through the API-neutral
    service by capturing one authorized application head, combining every causal floor, selecting
    one sufficient authoritative/provider/common epoch, freezing it across cursors, and waiting only
    through bounded cancellable readiness without stale fallback or process-local adapter state.
  requirements: [OQ-075, OQ-076, OQ-077, OQ-078, OQ-079, OQ-080,
    OQ-081, OQ-082, PRJ-001, PRJ-002, PRJ-003, PRJ-004, SAFE-004, SAFE-005]
  required_adrs: [ADR-0004, ADR-0010, ADR-0051, ADR-0052, ADR-0053,
    ADR-0055, ADR-0070, ADR-0071, ADR-0086, ADR-0108, ADR-0111, ADR-0124,
    ADR-0130, ADR-0134, ADR-0141, ADR-0150, ADR-0158, ADR-0159, ADR-0164]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0164-admission-head-fenced-query-consistency.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-errors/**, crates/riffdb-storage-api/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-executor/**,
    crates/riffdb-query-module/**, crates/riffdb-columnar/**,
    crates/riffdb-projection/**, crates/riffdb-policy/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-server/**,
    crates/riffdb-testkit/**, fixtures/**, tests/**, scripts/**,
    docs/SUMMARY.md, docs/concepts/**, docs/riffql/**, docs/architecture/**,
    docs/getting-started/**, docs/known-limitations.md]
  deliverables:
  - Closed service-level AdmissionHead option and request conversion whose absent value retains exact
    behavior and whose present value cannot weaken another causal/source/policy/provider requirement
  - Initial-auth-then-once head capture and maximum-floor calculation with deterministic barrier
    evidence around pre-admission and post-capture commits and no transport-local observation
  - Authoritative snapshot, projected provider, maintained exact-index, and composite common-epoch
    selection at or beyond the floor, with page/count/facet/rank/offset/aggregate alignment
  - Cursor registry binding for consistency class, computed floor, and selected epoch; continuations
    recapture nothing, page cardinality remains independent, and an old cursor cannot be upgraded
  - Bounded notification-based waiting, waiter admission/fairness/cancellation/deadline/saturation,
    lifecycle/refusal mapping, post-wake and pre-release reauthorization, and redacted telemetry
  - Pay-once instrumentation proving descriptor/policy compatibility per plan/provider epoch and
    capture/selection/negotiation/freshness proof once per logical query, never per row or page
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-service -p riffdb-query-executor -p riffdb-projection -p riffdb-columnar -p riffdb-policy --all-features
  - cargo +1.97.0 test -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server -p riffdb-testkit --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-service -p riffdb-query-executor -p riffdb-projection -p riffdb-columnar -p riffdb-policy -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server --all-targets --all-features -- -D warnings
  exit_gate: Every AdmissionHead request serves one authorized snapshot/epoch at or beyond its
    once-captured maximum floor or returns a bounded typed refusal; cursors freeze that proof under
    sustained writes; and no stale fallback, process cache, recapture, busy polling, policy bypass,
    transport branch, or repeated descriptor proof remains.
  standing_design_tests:
    interface_safety: Applications choose only the stronger closed class and cannot submit a head,
      epoch, provider, wait, poll, worker, fallback, isolation level, or stale-success opt-out.
    scale: One capture, negotiation, bounded waiter, and retained epoch occur per logical query;
      continuation, row, result, and provider subcalls add no repeated proof or duration/population-
      proportional state.
  closure:
    status: complete
    completed_at: "2026-08-28"
    requirements_satisfied: [OQ-075, OQ-076, OQ-077, OQ-078, OQ-079, OQ-080,
      OQ-081, OQ-082, PRJ-001, PRJ-002, PRJ-003, PRJ-004, SAFE-004, SAFE-005]
    evidence: Commit 5ce580ed implements admission-head request conversion, once-only authorized
      head capture, floor combination, snapshot/provider selection and waiting, cursor identity,
      post-wake authorization, service and transport coverage, generated clients, topology, and
      handbook evidence. This closure metadata was restored during WP-733's dependency audit; no
      WP-719 implementation changed.
    known_limitations: Candidate participant sets were not part of ADR-0164 and remain WP-735.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-722
  name: Public-source-only authoring acceptance
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [DX-002, DX-004, SAFE-004]
    evidence: scripts/wp722-authoring-acceptance verifies the content-addressed campaign,
      recompiles the Library-05, Clinic-04, and Fleet-04 contracts and named queries, requires
      three distinct fresh contexts with zero implementation-source reads or network use, checks
      every retained diagnostic byte-for-byte against its sufficient-help judgment, and scores
      every run 10/10. The ordinary_contract_and_query_plan_codes_all_name_a_concrete_repair,
      bounded_limit_syntax_repair_survives_the_authoring_boundary,
      missing_payload_comma_diagnostic_names_the_exact_separator, and
      query_name_mismatch_names_the_exact_manifest_source_repair tests make absent or non-actionable
      ordinary authoring help fail. docs/evaluations/wp-722/failed-runs retains the three original
      non-evidentiary reports without changing their negative judgments. release-source-install-smoke
      proves DX-002's inactive-service, configuration-refusal, private-journal, atomic-publication,
      and interruption-resume behavior; symbolic_application_values_cover_every_generated_scalar_without_numeric_ids
      proves DX-004 natural and tagged symbolic values; and
      relationship_diagnostic_names_the_exact_proof_and_its_safe_correction proves SAFE-004 refusal
      plus the exact compiling repair.
    known_limitations: This package qualifies public contract and named-query authoring only; it
      does not substitute for generated-driver, deployed-runtime, migration-execution, or endurance
      evidence. RDB-C023 now names the known bounded collection-delete repair and otherwise directs
      an author to preserve source and report the compiler invariant rather than guessing.
    follow_ups: []
  depends_on: [WP-569, WP-721]
  objective: Prove the agent-facing thesis the release rests on. A fresh agent with only the public
    handbook, scaffold, and diagnostics must author a compiling contract and named query for an
    unseen domain without reading RiffDB implementation source. Every rejection it cannot repair
    from the diagnostic alone is a defect in the diagnostic, not in the agent.
  requirements: [DX-002, DX-004, SAFE-004]
  required_adrs: [ADR-0167, ADR-0170]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-contract-compiler/**,
    crates/riffdb-diagnostics/**, crates/riffdb-cli/**, crates/riffdb-riffql-syntax/**,
    docs/**, tests/**, scripts/**]
  deliverables:
  - A scored authoring evaluation over at least three unseen domains, run with no RiffDB source in
    context, recording every diagnostic encountered and whether its help alone was sufficient
  - Every contract-semantic and query-plan code reachable from ordinary authoring carries help that
    states the repair, verified by a test that fails when a code's help is absent
  - AUTHORING.md content reachable without scaffolding a project, and referenced from the
    diagnostics that its sections answer
  - A worked-example corpus covering aggregate choice, delete policy, bounded reads, and
    cross-aggregate writes, each compiled by the repository checks so it cannot rot
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/check-adr-obligations
  - ./scripts/handbook check
  exit_gate: Three independent fresh-agent authoring runs produce compiling contracts and queries
    with zero reads of RiffDB implementation source, and every diagnostic they hit named its repair.
  standing_design_tests:
    diagnostic_completeness: No contract-semantic or query-plan diagnostic reachable from ordinary
      authoring renders without help text stating what a working source would look like.

- id: WP-723
  name: Continuous downstream adapter protection
  depends_on: [WP-569]
  objective: Make a contract or RiffQL language change unable to land green while it breaks an
    out-of-tree adapter. The adapter repositories are the only consumers exercising those languages
    end to end and no test in this repository compiles them, which is how ADR-0167 shipped and left
    riffdb-better-auth failing RDB-QS003.
  requirements: [VER-003, DRV-014]
  required_adrs: [ADR-0148]
  allowed_paths: [work_packages.yaml, .github/workflows/**, scripts/**, docs/**]
  deliverables:
  - Adapter repositories reachable from CI with pinned revisions, so scripts/downstream-adapter-check
    runs on every pull request rather than only on a developer workstation
  - A required CI job that compiles every pinned adapter against the working tree and fails the
    change rather than the adapter
  - A recorded procedure for the intended break, where a language change updates the pinned adapter
    revision in the same reviewed change
  acceptance_commands:
  - ./scripts/downstream-adapter-check
  exit_gate: A language change that breaks a pinned adapter fails CI on the change that caused it,
    demonstrated by a deliberate reproduction of the ADR-0167 break.

- id: WP-724
  name: Real adapter workload comprehensiveness
  tier: surface
  depends_on: [WP-569, WP-573, WP-782]
  objective: Close the gap between what the release gate claims and what it exercises. OpenFGA has
    live upstream-suite evidence; the general WP-578/WP-579 MLflow and Woodpecker rows are RiffDB-
    authored language-expressiveness matrices; and narrower completed MLflow and Better Auth claims
    retain only their exact RiffDB-matrix, RiffDB-live, or external-live evidence strength.
  requirements: [SAFE-001, DRV-014, BLK-014, BLK-064, BLK-070, DEL-012,
    OQ-016, OQ-030, OQ-100, OQ-112, WF-014, APE-001, APE-013, RAP-016, EXP-014,
    END-001, QSO-012]
  required_adrs: [ADR-0148, ADR-0201]
  allowed_paths: [SPEC.md, work_packages.yaml, fixtures/adapters/**, docs/**, scripts/**]
  deliverables:
  - Canonical `fixtures/adapters/adapter-evidence-v1.json` containing ADR-0201's exact ten scoped
    claim records and source locators, with the four general-alpha rows distinguished from every
    preserved capability-specific external receipt
  - MLflow and Woodpecker's WP-578/WP-579 general rows classified as language-expressiveness
    matrices without erasing or widening the separate external WP-738 and WP-743/OQ-112 receipts or
    the RiffDB-owned WP-744/BLK-064 live loopback
  - Better Auth's supported surface stated where an adopter meets it, including that additional
    fields and plugins require generation/configuration of a reviewed materialized profile, and
    that the framework's generic facade is an exact profile-bound dispatcher rather than generic
    RiffDB CRUD or a runtime fallback
  - The release gate's adapter claim rewritten to match what each adapter actually demonstrates
  acceptance_commands:
  - ./scripts/check-adapter-evidence-claims
  - ./scripts/check-adapter-evidence-claims --self-test
  - ./scripts/adapter-conformance --all-domains --all-languages
  - ./scripts/downstream-adapter-check
  exit_gate: Every named claim maps to one exact scoped inventory record; only OpenFGA carries a
    complete upstream-suite claim, each external, RiffDB-live, or matrix record remains truthful and
    narrow, and no gate sentence overstates what its evidence covers.
  closure:
    status: complete
    completed_at: "2026-09-06"
    requirements_satisfied: [SAFE-001, DRV-014, BLK-014, BLK-064, BLK-070, DEL-012,
      OQ-016, OQ-030, OQ-100, OQ-112, WF-014, APE-001, APE-013, RAP-016, EXP-014,
      END-001, QSO-012]
    evidence: The exact ten-record `riffdb.adapter-evidence-inventory/v1` fixture, bounded claim
      guard, normative-coverage proof, and adversarial self-test implement all three ADR-0201
      obligations. The handbook and release gate preserve the four general-alpha classifications,
      all six narrower records, the Better Auth profile-bound dispatcher, and the separate WP-578
      72-hour evidence boundary. Package acceptance, generated and handbook checks, the complete
      adapter-conformance matrix, and local sibling compilation checks against `riffdb-openfga`,
      `riffdb-better-auth`, and `riffdb-mlflow` pass with `--repo-root /home/kevin/dev`. These are
      workstation checks, not immutable pinned CI evidence. WP-723 remains open and separately owns
      adapter revision locks and required CI enforcement.
    decision: Classification and release wording now match retained evidence. Only OpenFGA carries
      a complete upstream-suite claim; external, RiffDB-live, matrix, and post-alpha records remain
      separate and exact. No runtime, public, durable, protocol, storage, authorization,
      transaction, evidence-result, threshold, or 72-hour execution behavior changed.

- id: WP-725
  name: Seeded-campaign determinism restoration
  depends_on: [WP-577]
  objective: Restore the seeded simulator campaign to green. Three arms fail on main --
    campaign::per_merge_sweep_holds_the_oracle_and_reaches_the_swept_territory,
    corpus::regression_corpus_replays_and_reproduces_its_territory, and
    subsumption::covered_rows_replay_as_pinned_campaign_schedules -- each asserting that a pinned
    schedule still reaches its recorded crash point. A release gate that does not notice its own red
    suite is not a gate.
  requirements: [SIM-001, SIM-002, SIM-004]
  required_adrs: []
  allowed_paths: [work_packages.yaml, crates/riffdb-sim/**, fixtures/**, docs/**]
  deliverables:
  - Diagnosis delivered in docs/performance/wp-725-seeded-campaign-regression.md. All three arms
    share one cause. The witness seed 0x51C2C147 no longer resolves an interrupted commit as
    present; bisected to fa5d906c clean-close fast startup (ADR-0156/ADR-0157), whose parent passes
    and which neither record anticipated. Atomicity is unaffected -- the partial-batch panic never
    fires across 32 crashes and 32 recoveries
  - Sweep delivered and decisive. 96 seeds under COMMIT_PRESENT_ARMS_CONFIG all complete, none
    wedge, 296 interrupted commits resolve absent, and none resolves present. The window is closed
    rather than narrow, so this is an engine question and re-pinning cannot work. Reproduction
    retained as wp725_commit_present_window_sweep, ignored by default
  - What clean-close fast startup changed about the frontier a recovery observes, stated against
    fa5d906c's diff rather than inferred, and whether a committed batch is still observable to
    recovery at the same point. Answered. The frontier is derived from the COMMITS table by
    command_authority_head_profiled, so a durable commit record is the frontier atomically, and
    fa5d906c touched neither command_authority.rs nor retention.rs. The change is the window's width
    in operations, not the meaning of the frontier
  - Zero width proved by exhaustive placement rather than inferred from a random draw.
    wp725_commit_present_window_ordinal_walk pins the countdown to one ordinal and walks every
    reachable ordinal in four complete plans -- 823 interrupted commits resolve absent, none
    resolves present. The benign reading is confirmed rather than assumed, because
    verify_model_against_inspection checks every family in both directions and diverged on none of
    those 823 recoveries, so the absences are genuine non-durability and not unobserved durable
    state. Clean-close fast startup made batch acknowledgement exact
  - A sweep-level assertion on total.in_flight_commit_present, whose absence is why a regression in
    the more dangerous recovery direction survived unnoticed; the sweep asserts the absent case and
    never asserted the present one. Delivered. The sweep now asserts the present count is zero with
    the reasoning attached, so a reopened in-doubt interval reds in the standard suite rather than
    waiting for a diagnostic run
  - Restored reproduction, or re-pinned schedules only after the two questions above are answered.
    Re-pinning first would turn the suite green while leaving a recovery-visibility change unexamined
    immediately before a release. Delivered, and the regression was six changes rather than one.
    wp725_corpus_territory_audit replays every corpus entry at once instead of stopping at the first
    failure, which showed fa5d906c moved windows in both directions -- it invalidated three active
    witnesses and restored two that earlier layout changes had rotated away. Five are ordinary
    rotations and restorations; only the commit-PRESENT territory is gone rather than moved
  - Typed retirement distinct from rotation, in both registries. A rotation names a successor that
    reaches the moved window; a retirement records that no successor exists, which is a claim a
    re-pin cannot check. CorpusTerritoryRetirement and ScenarioClass::ExcludedStorageStateUnreachable
    each keep the last witness replaying with the retired expectation inverted, so reproducing the
    state again fails rather than passing quietly. Both were drift-checked against a territory that
    is still live
  - Successors scouted rather than guessed. wp725_scout_successor_witnesses applies the bar the
    existing receipts record -- identical counters across 12 reruns before pinning -- and all three
    pinned successors met it. Restorations were verified against the diff, since the full corpus
    replay passes at fa5d906c~1
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-sim --all-features
  exit_gate: The seeded campaign is green, and a deliberately drifted pin fails with a diagnosis
    rather than a bare assertion.
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [SIM-001, SIM-002, SIM-004]
    evidence: docs/performance/wp-725-seeded-campaign-regression.md records the bisect, random
      sweep, exhaustive ordinal walk, frontier analysis, corpus audit, and stable successor scout.
      The seeded campaign, typed rotation and retirement replays, sweep-level zero-width guard,
      simulator determinism and fault-arm suites, and deliberately drifted-pin diagnostic all pass
      under the package acceptance command.
    known_limitations: The retained shape, ordinal, audit, and successor-scout campaigns remain
      ignored diagnostics because the per-merge suite runs their bounded standing consequences;
      they are invoked explicitly when durable-layout work moves a physical operation stream.
    follow_ups: []

- id: WP-726
  name: Unary transport floor and honest service levels
  depends_on: [WP-674]
  objective: Address the component ADR-0142 routed around rather than removed. On both cloud
    profiles the outside-service residual alone exceeds the retired 1.10x budget -- 415 us against a
    312 us target on N1 -- so eliminating all server query work could not have passed it. The
    replacement absolute levels are cleared with roughly five times headroom while RiffDB sits near
    2.3x PostgreSQL on point reads, which means nothing currently constrains unary latency.
  requirements: [PERF-008, PERF-018, DRV-004]
  required_adrs: [ADR-0142, ADR-0146, ADR-0148]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-api-grpc/**, crates/riffdb-service/**,
    crates/riffdb-driver-host/**, crates/riffdb-client-rust/**, docs/performance/**, release/**,
    scripts/**]
  deliverables:
  - A stage decomposition of the outside-service residual on both cloud profiles, separating client
    encode, transport, driver host, admission, and response release, measured on release builds
  - A stated decision, with evidence, on whether that floor is reducible for alpha or is the
    product's honest unary cost
  - Absolute service levels recalibrated so they constrain rather than merely admit the current
    artifact, or an explicit statement that they are floors and the frozen low-water bank is the
    operative gate
  - Published PostgreSQL unary ratios retained as disclosure, with the gap stated plainly in the
    handbook rather than only in performance evidence
  acceptance_commands:
  - ./scripts/check-alpha-performance-evidence
  - ./scripts/check-wp674-unary-baseline --self-test
  exit_gate: The unary residual is decomposed and either reduced or accepted on the record, and no
    published service level can be met by an artifact that has regressed against the frozen bank.

- id: WP-727
  name: Kubernetes packaging and operator-grade deployment
  tier: surface
  depends_on: [WP-558]
  objective: Give an operator a supported way to run RiffDB that is not a hand-edited manifest.
    release/kubernetes ships one raw riffdb.yaml and release/container ships a Compose file; there
    is no chart, no values surface, and no upgrade path between released versions.
  requirements: [NET-010, NET-012, APE-002]
  required_adrs: [ADR-0202, ADR-0055, ADR-0056, ADR-0105, ADR-0110, ADR-0112,
    ADR-0124]
  allowed_paths: [work_packages.yaml, release/**, docs/operations/**, docs/**, scripts/**,
    .github/workflows/**]
  deliverables:
  - Chart delivered at release/helm/riffdb. Values cover listener and TLS material, storage class
    and volume sizing, resources, probes, proxy, and backup targets, with no host-loopback shortcut.
    Secrets are never templated; image.tag resolves to appVersion so a chart cannot silently deploy
    a different RiffDB release than it declares; the backups claim carries a keep policy so it
    outlives the release and END-001's restore drill stays possible
  - Golden render delivered at release/helm/rendered with scripts/check-helm-render, verified
    load-bearing by confirming a values change moves the recorded manifest. The acceptance manifest
    it replaces hardcoded an image and namespace, stated no resource requests at all, and bundled
    proof Jobs with the workload; those Jobs are opt-in here
  - Exact opt-in application and operator proof Jobs use separate external Secrets, the chart-bound
    image, protected mounts, and service DNS without host networking or loopback; the default render
    contains neither Job, the chart creates no secret or authority, and authenticated readiness uses
    only a protected credential
  - Bounded local Helm lifecycle rehearsal packages the reviewed chart under two reserved
    non-release identities in temporary storage, renders install, upgrade, and rollback with frozen
    values, and proves stable object/PVC/Secret identity plus declared value movement without a
    registry, cluster, database, publication, durable-compatibility, or execution claim
  - Operator runbook covering install, controlled-network restriction, authenticated readiness,
    drain, verified backup, closed-database durable-format preflight and its sole authorized action,
    chart upgrade, certificate and successor-then-revoke credential rotation, restore, and safe
    deployment rollback or durable downgrade refusal, exercised structurally against the chart
  - Startup verification delivered as scripts/check-container-startup, which runs the release image
    read-only as uid 65532 with only the documented volumes and asserts the ready boundary and a
    projections root inside a mounted volume. Verified load-bearing by removing the fix and
    confirming the check exits nonzero naming class=exact_text_runtime. It found a defect that
    render-only checks cannot reach, where the shipped Compose and Kubernetes runtime
    configurations could not start riffdbd at all; both are now fixed
  acceptance_commands:
  - ./scripts/acceptance --wp WP-727
  - ./scripts/check-helm-render
  - ./scripts/check-helm-operator-package
  - ./scripts/check-helm-upgrade-rehearsal
  - ./scripts/check-container-startup
  - ./scripts/remote-compose-acceptance
  - ./scripts/handbook check
  - ./scripts/ci-all
  exit_gate: The chart and exact runbook locally prove their bounded install, upgrade, rotation,
    backup, restore, drain, and safe rollback/refusal mechanics; every repository-local NET-010 proof
    uses the chart rather than a bespoke manifest; the local rehearsal is explicitly non-release
    evidence; and no two-published-version or controlled-cluster execution claim is made.
  standing_design_tests:
    interface_safety: The chart assembles existing safe binaries and separately provisioned Secrets;
      it cannot mint capabilities, start an installation campaign for an application or MCP caller,
      expose raw storage or mutation, or provide force, reset, downgrade, or guarantee opt-outs.
    scale: Every chart object, proof Job, render, values profile, rehearsal step, runbook assertion,
      listener/stream/query/command setting, and retained receipt is explicitly bounded; no check
      scans a cluster, registry, database population, or unbounded history.
  closure:
    status: complete
    completed_at: "2026-09-05"
    requirements_satisfied: [NET-010, NET-012, APE-002]
    evidence: The supported chart now binds an empty or exact Chart.appVersion image tag, one fixed
      internal service-DNS endpoint and TLS name, exact protected read-only Secret projections, a
      payload-free unauthenticated liveness probe, and readiness that accepts only the canonical
      authenticated typed ready state. Its two opt-in health-only Jobs require nonempty pairwise
      distinct application, operator, and server Secrets; create no authority; disable service
      account token mounting; and carry finite schema, runtime, storage, resource, probe, and retry
      bounds. The parsed checker proves exact mounts, probes, identities, and least capabilities and
      rejects every unsafe override. The generated golden, bounded install/upgrade/rollback
      rehearsal, container startup, remote Compose acceptance, handbook check, package acceptance,
      and full ci-all pass. This surface change alters no durable encoding, protocol, application
      API, credential authority, or database semantics. Remaining published-version and controlled-
      cluster execution evidence belongs to release work rather than this repository-local proof.
  human_review_triggers: *id001

- id: WP-728
  name: Signed release automation, publication, and version enforcement
  depends_on: [WP-200, WP-727]
  objective: Turn a verified bundle into a release someone can consume. scripts/release-poc --verify
    assembles binaries, SBOM, and checksums, but no workflow runs it, no container image is built in
    CI, and nothing is published anywhere -- every workflow ends at upload-artifact, which expires.
    The versioning and retirement policy is written and unenforced by any release mechanism.
  requirements: [AFC-001, VER-001, VER-004, DRV-014]
  required_adrs: [ADR-0124]
  allowed_paths: [work_packages.yaml, .github/workflows/**, release/**, scripts/**, docs/**]
  deliverables:
  - A tag-triggered release workflow that runs scripts/release-poc --verify on a clean revision and
    refuses to publish anything if any release check fails
  - Published, immutable, checksummed release artifacts with their SBOM and durable-format manifest,
    plus a container image built and pushed from the tracked Dockerfile
  - Driver package publication for the Rust, Go, TypeScript, and Python surfaces, each carrying the
    conformance manifest DRV-014 requires
  - Version enforcement in the workflow, so a tag whose transition class, topology record, or
    durable-format manifest is inconsistent with the change fails before publication rather than
    after
  - Provenance and signature over every published artifact, and a documented consumer verification
    procedure
  acceptance_commands:
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  exit_gate: A tag produces signed, verifiable, published artifacts and a container image from a
    clean revision with no manual step, a consumer verifies them from the documented procedure, and
    an inconsistent version transition is refused before anything is published.

- id: WP-729
  name: Tokenized text-search declaration and frozen analyzers
  depends_on: [WP-646]
  objective: Begin ADR-0173 with the contract-owned identity and deterministic analysis boundary.
    A contract declares one bounded text_index with weighted source fields, one closed versioned
    analyzer, staleness and replay ceilings, the boolean-v1 result model, and compiler-owned term,
    candidate, and result-window budgets. Both v1 analyzers execute against a pinned conformance
    corpus. Durable segments, query matching, and ranking remain later packages.
  requirements: [FTS-001, FTS-002, FTS-003, FTS-004]
  required_adrs: [ADR-0092, ADR-0131, ADR-0164, ADR-0172, ADR-0173]
  allowed_paths: [SPEC.md, work_packages.yaml, Cargo.lock,
    adr/0092-native-full-text-search-projections.md,
    adr/0173-tokenized-text-search-provider.md,
    crates/riffdb-types/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-query-module/src/python_generation.rs,
    crates/riffdb-cli/assets/**, clients/editor/tree-sitter-riff/keywords.json,
    examples/agent-alpha/domains/rust/Cargo.lock, fixtures/**,
    release/version-topology-v1.json,
    docs/contracts/**, docs/reference/**]
  deliverables:
  - Contextual text_index grammar and AST with weighted source fields, exactly keyword_v1 or
    standard_v1, a positive stale-entity threshold, the complete replay clause, boolean_v1,
    and fixed compiler-owned term, candidate, and result-window budgets
  - Canonical checked IR and additive bundle successor carrying every declaration field; contracts
    without text_index retain their least-sufficient bytes, while analyzer or weight changes rotate
    bundle identity
  - Source-spanned compiler diagnostics naming empty, duplicate, unknown, non-string, zero,
    over-limit, or cross-bound failures without silent deduplication or truncation
  - One exact-pinned Unicode 17.0.0 UAX 29 tokenizer dependency and an analyzer conformance corpus
    proving whole-value keyword output, segmentation-before-fold, positions, compatibility folds,
    punctuation, scripts, emoji boundaries, and deterministic repeated analysis
  - >-
    Public handbook authoring guidance that states the stage-one limitation: declarations and
    analyzers are available, while durable indexing and named tokenized queries are not yet
  acceptance_commands:
  - cargo test -p riffdb-types -p riffdb-contract-syntax -p riffdb-contract-ir
    -p riffdb-contract-compiler --all-features
  - ./scripts/check-requirement-coverage
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace --all-features
  exit_gate: A valid text_index reaches canonical round-trippable bundle identity with every
    declaration field retained; both analyzers equal the pinned corpus; every invalid condition is
    rejected at its source span; analyzer and weight changes rotate identity; declaration-free
    contracts retain their least-sufficient bytes; generated, handbook, downstream, version,
    Clippy, and full-workspace checks pass. No segment, match executor, ranking, caller-supplied
    analyzer, boost, scoring parameter, field, operator, or cost hint exists.
  standing_design_tests:
    interface_safety: Application source selects only one closed compiler-validated declaration;
      runtime callers receive no analyzer, field, boost, score, provider, budget, scan, truncation,
      or consistency selector, and this package adds no invocable tokenized query.
    scale: Source fields and every weight, field byte bound, replay ceiling, term count, candidate
      set, and result window are finite and compiler checked; analyzer iteration is linear in the
      already bounded source value and retains no corpus-sized state.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [FTS-001, FTS-002, FTS-003, FTS-004]
    evidence: Contract parsing and source-spanned compiler tests cover the complete bounded
      declaration and every invalid condition. Canonical IR V20 fixtures prove round trips,
      identity rotation for analyzer and weight changes, and least-sufficient V1 bytes when no
      text index exists. The pinned Unicode 17.0.0 corpus proves keyword and standard analyzer
      output, positions, folds, scripts, emoji boundaries, and determinism. Targeted, generated,
      requirement-coverage, version-topology, handbook, downstream-adapter, formatting, workspace
      Clippy, and full-workspace test gates pass.
    known_limitations: This package establishes declaration, identity, and analysis only. Durable
      text-index segments, replay and freshness enforcement, query matching, authorized-set
      statistics, scoring, and ranking remain staged successor work under ADR-0173.
    follow_ups: []

- id: WP-730
  name: Durable tokenized-text segment and recovery
  depends_on: [WP-729]
  objective: Implement ADR-0173's complete first durable inverted-index format and its bounded
    atomic maintenance, rebuild, replay, compaction, checkpoint, corruption, and recovery behavior
    before any public tokenized query is activated.
  requirements: [FTS-005, FTS-006, FTS-007]
  required_adrs: [ADR-0004, ADR-0086, ADR-0092, ADR-0130, ADR-0131, ADR-0172, ADR-0173]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0173-tokenized-text-search-provider.md,
    release/version-topology-v1.json, crates/riffdb-types/**, crates/riffdb-contract-ir/**,
    crates/riffdb-projection/**, crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-testkit/**, fixtures/**, tests/**, scripts/**,
    docs/SUMMARY.md, docs/architecture/**, docs/operations/**, docs/known-limitations.md]
  deliverables:
  - One canonical provider-state V1 carrying index identity, analyzer, partition, generation,
    frontier, keys, outputs, postings, per-field frequencies and norms, and token positions from
    its first byte-frozen release
  - Atomic checked mutation, deletion, compaction, disjoint rebuild, contiguous replay, checkpoint,
    and recovery with strict mixed-version, corruption, identity, order, duplicate, and bound refusal
  - Memory and redb derived-state persistence behind existing projection storage boundaries, with
    never-reused generations and no authoritative dependency on tokenized state
  - Reference and crash tests across updates, deletes, Unicode analysis, maximum bounds, corrupt
    bytes, cancelled rebuild, restart, and replay proving no partial generation or frontier publishes
  acceptance_commands:
  - cargo test -p riffdb-types -p riffdb-projection -p riffdb-storage-api
    -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-testkit --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Provider-state V1 is byte-frozen with every future boolean/ranking primitive present;
    mutation, rebuild, compaction, replay, recovery, and restart are reference-equivalent and
    bounded; corrupt or partial state never publishes; and no public tokenized query exists.
  standing_design_tests:
    interface_safety: The segment is private derived machinery selected only from compiled contract
      identity; applications cannot submit a key, format, analyzer, provider, generation, frontier,
      budget, rebuild, checkpoint, or fallback choice.
    scale: Rows, fields, source bytes, analyzed terms, positions, postings, output, checkpoint,
      replay, mutation amplification, rebuild work, and diagnostics are bounded before allocation.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [FTS-005, FTS-006, FTS-007]
    evidence: crates/riffdb-projection/tests/tokenized_text_provider.rs
    decision: Provider-state V1 freezes the compiled index identity, analyzer, partition,
      generation, frontier, canonical keys and outputs, field token-count norms, and redundant
      explicit postings with frequencies and positions. Whole-epoch staging is atomic; update,
      delete, compaction, disjoint rebuild, checkpoint, recovery, unknown-version, corruption,
      canonical-order, duplicate, and bound tests pass. Recovery independently reconstructs
      postings from document terms and refuses a mismatch before publication. Targeted storage and
      recovery suites, topology, generated, coverage, handbook, formatting, and strict Clippy gates
      pass, while authoritative storage and every public query surface remain unchanged.
    known_limitations: Provider activation and named boolean/ranked execution remain WP-731 and
      WP-732. The V1 checkpoint is private derived state and never an application-facing format.
    follow_ups: []

- id: WP-731
  name: Exact bounded tokenized boolean named queries
  depends_on: [WP-647, WP-730]
  objective: Activate exact conjunction, capped disjunction, phrase, and bounded proximity through
    finite named RiffQL operations and the shared authorization/service/transports using only the
    maintained tokenized segment and typed all-or-refusal budgets.
  requirements: [FTS-008, FTS-009, FTS-010, FTS-011, FTS-015]
  required_adrs: [ADR-0038, ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0086, ADR-0092,
    ADR-0108, ADR-0111, ADR-0124, ADR-0130, ADR-0131, ADR-0173]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0173-tokenized-text-search-provider.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-contract-compiler/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-query-executor/**, crates/riffdb-catalog/**,
    crates/riffdb-projection/**, crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-policy/**, crates/riffdb-errors/**,
    crates/riffdb-service/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-client-rust/**, crates/riffdb-driver-host/**,
    crates/riffdb-cli/**, crates/riffdb-mcp-stdio/**, crates/riffdb-server/**, clients/**,
    templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - Additive RiffQL, query IR, module, plan, and generated-operation identities for exactly four
    compiler-sealed boolean shapes over one declared text index and one typed bounded string input
  - Exact maintained-posting execution with deterministic set, phrase, and proximity semantics,
    canonical entity-key order, typed term/candidate/result refusal, and no scan or partial result
  - Fresh authorization before provider selection and release through one API-neutral service path,
    with generated gRPC, MCP, CLI, Rust, Go, TypeScript, and Python parity and redacted failures
  - Independent reference, randomized history, concurrency, cursor, policy, lifecycle, recovery,
    cost, and architecture-negative evidence
  acceptance_commands:
  - cargo test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler
    -p riffdb-query-module -p riffdb-query-executor -p riffdb-projection --all-features
  - cargo test -p riffdb-service -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-client-rust
    -p riffdb-driver-host -p riffdb-cli -p riffdb-server --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every admitted named boolean operation equals the reference evaluator at one authorized
    provider epoch on every surface; budgets refuse rather than truncate; and no runtime structure,
    scan, stale fallback, scoring, caller field/operator/provider choice, or client shaping exists.
  standing_design_tests:
    interface_safety: Applications invoke only generated typed methods whose structure, field,
      analyzer, expression, distance, partition proof, provider, budgets, and output are compiled.
    scale: Query bytes, analyzed terms, disjunction arms, proximity, candidates, result rows, output,
      cursor, waits, retained epoch state, work, and diagnostics are independently bounded.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [FTS-008, FTS-009, FTS-010, FTS-011, FTS-015]
    evidence: RiffQL V10, query IR V13, and query-module V13 seal one declared text index,
      analyzer, partition proof, typed query input, exact conjunction/disjunction/phrase/proximity
      shape, provider descriptor, output, and term/candidate/result ceilings. The executor uses only
      maintained postings, produces canonical entity-key order, and refuses whole operations on
      every bound violation. The production request path has architecture-negative coverage proving
      it cannot scan or hydrate authoritative state; the background worker builds one policy-aligned
      durable generation from a pinned export snapshot. Fresh pre/post authorization and one shared
      service path feed generated gRPC, MCP, CLI, Rust, Go, TypeScript, and Python surfaces. Fixed,
      randomized-history, deletion, compaction, checkpoint-recovery, corruption, policy, lifecycle,
      topology, generated-artifact, handbook, downstream-adapter, four-language remote-conformance,
      formatting, strict Clippy, documentation, dependency-policy, and full workspace gates pass.
    known_limitations: Boolean pagination uses the compiled bounded numeric offset and does not
      freeze a corpus across separate requests. V1 intentionally has no wildcard, regex, fuzzy,
      stemming, highlighting, snippets, score output, caller tuning, hybrid search, or scan fallback.
    follow_ups: []

- id: WP-732
  name: Authorized-snapshot fixed-point tokenized ranking
  depends_on: [WP-719, WP-731]
  objective: Complete ADR-0173 with provider-owned deterministic relevance over the caller's full
    authorized set at one admission-head-fenced snapshot, exact total-order cursor pagination, and
    identical generated named-operation behavior on every public surface.
  requirements: [FTS-012, FTS-013, FTS-014, FTS-015]
  required_adrs: [ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0086, ADR-0092, ADR-0108,
    ADR-0111, ADR-0124, ADR-0130, ADR-0164, ADR-0173]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0092-native-full-text-search-projections.md,
    adr/0173-tokenized-text-search-provider.md, release/version-topology-v1.json, proto/**,
    crates/riffdb-types/**, crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-catalog/**, crates/riffdb-projection/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-errors/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-mcp-stdio/**, crates/riffdb-server/**, clients/**, templates/**, examples/**,
    fixtures/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - Frozen `riff_bm25_v1` fixed-point score and checked arithmetic with contract weights and
    canonical entity-key tie-breaking, exposed as an order only and never caller-tunable
  - Authorized-set-only document frequencies and field-length statistics at the same fenced snapshot
    as membership, with adversarial evidence that denied rows influence no observable result
  - Admission-head capture once on the first page and cursor binding of floor, epoch, generation,
    authorized-set/statistics identity, plan, parameters, and total order; retired state fails typed
  - Reference rank equality, concurrent-write pagination, cursor expiry, policy leakage, recovery,
    compaction, cross-language, and pay-once proof tests plus complete public/operational guidance
  acceptance_commands:
  - cargo test -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module
    -p riffdb-query-executor -p riffdb-projection -p riffdb-policy -p riffdb-service --all-features
  - cargo test -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-client-rust
    -p riffdb-driver-host -p riffdb-cli -p riffdb-server --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace --all-features
  exit_gate: Ranked membership and exact order equal the fixed-point reference over only the
    authorized fenced corpus; every page sequence is stable and complete; retired state fails
    typed; all generated surfaces agree; and callers receive no score, boost, field, analyzer,
    provider, cost, consistency downgrade, scan, or fallback control.
  standing_design_tests:
    interface_safety: Applications invoke one generated ranked operation and may request only the
      existing stronger admission-head consistency class; every relevance and authority choice is
      compiled or server-derived and cannot be weakened at runtime.
    scale: Authorized-set materialization is provider-maintained and snapshot-bounded; terms,
      candidates, statistics, score arithmetic, result rows, pages, cursors, epochs, waits, output,
      and diagnostics remain within compiler-declared ceilings.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [FTS-012, FTS-013, FTS-014, FTS-015]
    evidence: The provider-owned `riff_bm25_v1` freezes exact rational IDF and BM25 term-frequency
      normalization with checked u128 intermediates, u64 final scores, contract field weights, and
      canonical entity-key tie-breaking; scores and tuning controls never enter public schemas.
      Statistics are derived only from the complete policy-aligned provider snapshot. A ranked first
      page forces ADR-0164 admission-head consistency, while opaque continuations bind the causal
      floor, provider epoch and generation, authorized statistics identity, history incarnation,
      plan, invariant parameters, current capability identity/revision, order, and result ceiling.
      Retained-epoch selection and cursor-family tests prove exact continuation identity and typed
      retirement instead of rescoring. Reference score fixtures, stable ties, recovery, compaction,
      policy-aligned construction, no-request-scan architecture evidence, shared-service transport
      tests, four-language remote conformance, topology, generated artifacts, handbook, adapters,
      formatting, strict Clippy, documentation, dependency policy, and full workspace tests pass.
    known_limitations: The production POC retains eight provider epochs per registered ranked query
      shape; sustained writes may therefore retire a ranked cursor snapshot before its token TTL.
      Ranking is `riff_bm25_v1` only and exposes order rather than score.
    follow_ups: []

- id: WP-733
  name: Bounded filtered-result compatibility and identity inventory
  depends_on: [WP-690, WP-719, WP-731]
  objective: Freeze ADR-0174's framework-neutral source shapes, page and provider boundaries,
    compatibility identities, and failing real-consumer reproductions before executable semantics
    are added.
  requirements: [OQ-084, OQ-085, OQ-086, OQ-087, OQ-088, OQ-089, OQ-090,
    OQ-091, OQ-092, OQ-093, OQ-094, OQ-095, OQ-096, OQ-097, OQ-098, OQ-100]
  required_adrs: [ADR-0108, ADR-0124, ADR-0130, ADR-0131, ADR-0134, ADR-0150,
    ADR-0158, ADR-0159, ADR-0164, ADR-0167, ADR-0172, ADR-0173, ADR-0174]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0174-bounded-filtered-result-pipelines.md, release/version-topology-v1.json,
    fixtures/**, tests/**, scripts/**, docs/architecture/**, docs/known-limitations.md]
  deliverables:
  - Framework-neutral two-tag-before-root-order, combined text/tag, negation, empty, duplicate,
    overflow, Unicode, 8,000-byte value, and page-boundary fixtures with exact expected semantics
  - Complete active/reserved language, contract, query IR/module, executable plan, provider,
    checkpoint, role, generated-schema, cursor, and topology identity inventory
  - Exact candidate syntax corpus and public diagnostic allocation without executable acceptance
  - MLflow search capability matrix retained as generic values and a value-free downstream receipt,
    with adapter source and framework schema remaining outside this repository
  acceptance_commands:
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/check-adr-obligations
  - ./scripts/handbook check
  exit_gate: Every semantic and identity choice required by later packages is inventoried with
    failing generic fixtures; no source construct, executable plan, provider state, public schema,
    durable identity, or framework branch is activated.
  standing_design_tests:
    interface_safety: The inventory admits only named compiler-owned sources, algebra, orders,
      operators, and bounds; no generic join, runtime provider, cost, field, or fallback surface is
      proposed.
    scale: Every fixture declares independent rows, keys, bytes, probes, provider work, sort,
      output, and retained-state bounds, including the maximum and one-over cases.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [OQ-084, OQ-085, OQ-086, OQ-087, OQ-088, OQ-089,
      OQ-090, OQ-091, OQ-092, OQ-093, OQ-094, OQ-095, OQ-096, OQ-097,
      OQ-098, OQ-100]
    evidence: fixtures/riffql/bounded-filtered-result-v1 freezes candidate and pattern source,
      independent materialized oracle, boundary and refusal cases, exact diagnostic allocations,
      and the collision-free V11/V14/V14/V21/V6/provider-V1 identity inventory. The value-free
      consumer receipt retains only source digests and capability maxima. All declared paper-stage
      acceptance commands pass, and no executable syntax, provider, public schema, or durable
      identity is activated.
    known_limitations: Every ADR-0174 runtime capability remains unavailable until WP-734 through
      WP-737; the fixtures intentionally fail under the predecessor parsers and compilers.
    follow_ups: [WP-734, WP-735, WP-736, WP-737, WP-738]
  human_review_triggers: &id0174
  - Any change to candidate completion, policy placement, snapshot/provider compatibility, root
    ordering, cursor identity, wildcard semantics, global row ceiling, response bytes, or an
    accepted durable/public identity requires maintainer review.

- id: WP-734
  name: Compiler-sealed candidate algebra
  depends_on: [WP-733]
  objective: Add ADR-0174's non-output candidate syntax, checked identities, compiler proofs,
    reference semantics, and ordinary same-partition exact-index execution with all-or-refusal
    bounds and no observable result before complete root ordering.
  requirements: [OQ-084, OQ-085, OQ-086, OQ-087, OQ-088, OQ-090, OQ-098]
  required_adrs: [ADR-0038, ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124, ADR-0129, ADR-0130, ADR-0134, ADR-0150, ADR-0174]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0174-bounded-filtered-result-pipelines.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-errors/**, crates/riffdb-testkit/**,
    clients/editor/**, fixtures/**, tests/**, scripts/**, docs/riffql/**,
    docs/architecture/**, docs/reference/**, docs/known-limitations.md]
  deliverables:
  - Additive candidate source/single/intersection/union/difference AST, checked IR, module, plan,
    explain, role, topology, and compatibility identities with canonical source and byte fixtures
  - Compiler proof of one type-exact root key, same partition, declared relationship mapping,
    source count, independent budgets, authorized positive universe for difference, and one root
    consumer; every invalid shape has a source-span snapshot
  - Reference set evaluator plus memory/redb ordinary-index source execution with pay-once
    normalization, deterministic deduplication, whole-operation overflow, cancellation, and no
    intermediate `take`, page, cursor, or result exposure
  - Policy and architecture tests proving no arbitrary/cross-partition join, recursion, runtime
    optimizer, authoritative entity scan, partial result, hidden count, or application set value
  acceptance_commands:
  - cargo test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler
    -p riffdb-query-module -p riffdb-query-executor --all-features
  - cargo test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-policy
    -p riffdb-testkit --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Candidate algebra is byte-frozen and reference-equivalent for ordinary sources with
    complete independent bounds and policy, but provider bridges and public combined root results
    remain staged in WP-735 and WP-737.
  standing_design_tests:
    interface_safety: Applications cannot express or observe candidate structure; only a compiled
      root binding may consume it, and every source/operator/bound is compiler-owned.
    scale: At most eight sources and 65,535 distinct keys execute inside independent source, set,
      byte, probe, hydration, policy, and total-work ceilings with typed refusal.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [OQ-084, OQ-085, OQ-086, OQ-087, OQ-088, OQ-090,
      OQ-098]
    evidence: RiffQL V11, checked query IR/plan/module V14, and compiled-role V6 now seal one
      non-output candidate binding with at most eight ordinary same-partition declared-index
      sources, exact relationship/root-key proof, single/intersection/union/authorized-difference
      algebra, independent all-or-refusal ceilings, and exactly one root consumer. The executor
      completes and deduplicates every source before set evaluation, root hydration, policy,
      mixed total ordering, page, and cursor selection; a source continuation or bound failure
      releases no root read, partial row, count, or cursor. Memory and redb use the shared exact
      scan and candidate-root batch contracts. Canonical round-trip, generated-surface,
      role-authority, source-span, reference-algebra, cursor, storage, topology, generated,
      handbook, downstream-adapter, formatting, and strict workspace Clippy gates pass.
    known_limitations: WP-734 activates ordinary exact-index candidate sources only. Provider
      participant admission and finite multi-order public selection close in WP-735, and
      long_pattern_v1 sources close in WP-737. Candidate V1 intentionally remains one binding,
      eight sources, one type-exact single-field root key, same partition, and closed set algebra;
      it is not a general join or recursive query surface.
    follow_ups: [WP-735, WP-737, WP-738]
  human_review_triggers: *id0174

- id: WP-735
  name: Complete candidate root ordering and provider participation
  depends_on: [WP-719, WP-734]
  objective: Complete candidate populations at one admission-fenced participant set, then hydrate,
    authorize, sort, page, cursor, and expose root results through finite compiled order families
    and the shared service/transports.
  requirements: [OQ-085, OQ-087, OQ-088, OQ-089, OQ-090, OQ-091, OQ-098, OQ-099]
  required_adrs: [ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0086, ADR-0108,
    ADR-0111, ADR-0124, ADR-0130, ADR-0134, ADR-0150, ADR-0159, ADR-0164,
    ADR-0173, ADR-0174]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0174-bounded-filtered-result-pipelines.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-catalog/**,
    crates/riffdb-projection/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-errors/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-mcp-stdio/**, crates/riffdb-server/**, clients/**, templates/**,
    examples/**, fixtures/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - One participant-set descriptor binding ordinary and provider sources to the same admission head,
    snapshot, generation set, policy shape, plan, parameters, and retained cursor identity
  - Complete candidate hydration/cover, root predicate and policy evaluation, total-order sort, one
    continuation probe, page, and cursor after completion, with no early-stop optimization
  - Finite generated order-family identity and closed selector across Rust, Go, TypeScript, Python,
    gRPC, MCP, CLI, local, and remote paths without arbitrary runtime sort expressions
  - Reference, randomized, concurrent-write, retained/retired epoch, cancellation, policy,
    cursor-tamper, storage parity, and cross-surface tests for the two-tag-before-root-order shape
  acceptance_commands:
  - cargo test -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module
    -p riffdb-query-executor -p riffdb-projection -p riffdb-policy -p riffdb-service --all-features
  - cargo test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-api-grpc
    -p riffdb-api-mcp -p riffdb-client-rust -p riffdb-driver-host -p riffdb-cli
    -p riffdb-server --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every candidate source completes at one authorized participant set before exact root
    order/page/cursor formation on every surface; no hidden row affects results and no application
    can submit structure, provider, order fields, cost, or fallback.
  standing_design_tests:
    interface_safety: The only runtime structural input is a closed generated order variant whose
      complete immutable plan and authority already exist; callers cannot build an order or set.
    scale: Candidate, hydration, policy, sort-key, output, cursor, retained epoch, wait, and total
      work remain independently bounded and cancellation releases transient state.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [OQ-085, OQ-087, OQ-088, OQ-089, OQ-090, OQ-091,
      OQ-098, OQ-099]
    evidence: Query IR/module V15 and RiffQL V12 freeze finite complete order families, while the
      shared service obtains compiler-sealed provider batches, negotiates one plan/policy-bound
      result-set epoch proof, and invokes memory/redb final root hydration, policy, total sort,
      page, and cursor formation only after every source completes. Candidate execution tests
      combine an ordinary tag source and a provider source before a mixed root order; cursor,
      role, topology, generated-surface, cross-language remote, workspace, and architecture gates
      pass.
    known_limitations: Candidate V1 remains one same-partition root-key binding with at most eight
      sources and 32 order members. A continuation whose authoritative snapshot is no longer
      servable fails typed even when a derived provider generation remains retained.
    follow_ups: [WP-737, WP-738]
  human_review_triggers: *id0174

- id: WP-736
  name: Independently bounded 65534-row query pages
  depends_on: [WP-733]
  objective: Raise the structural query row ceiling to 65,535 scanned rows with one continuation
    probe while preserving independent encoded-result, cost, authority, transport, and every
    query-specific ceiling through additive identities for source bounds above 499.
  requirements: [OQ-087, OQ-097, OQ-098, OQ-099, OQ-100]
  required_adrs: [ADR-0027, ADR-0051, ADR-0052, ADR-0055, ADR-0112, ADR-0124,
    ADR-0130, ADR-0158, ADR-0159, ADR-0167, ADR-0174]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0158-compiler-declared-bounded-runtime-page-limits.md,
    adr/0167-required-bounded-limit-maximum.md, adr/0174-bounded-filtered-result-pipelines.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-errors/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    clients/**, templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - One shared 65,535 scan/65,534 result constant through parser, resolver, checked IR, compiler,
    executor, policy, storage, service, public messages, generators, schemas, and documentation
  - Least-sufficient successor identities only for bounds above 499; old plans/modules/cursors and
    sources at or below 499 retain exact semantics and accepted bytes
  - Boundary and conservative result-byte/cost/authority tests at 499, 500, 50,000, 65,534, and
    65,535 across static and parameter limits, plus no backend work on invalid input
  - Memory/redb forward/reverse cursor parity with one global probe and unchanged 4 MiB result,
    request/response/frame, and diagnostic ceilings
  acceptance_commands:
  - cargo test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler
    -p riffdb-query-module -p riffdb-query-executor --all-features
  - cargo test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-policy
    -p riffdb-service -p riffdb-proto --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Narrow queries return through 65,534 exact rows with one probe and cursor parity while
    excessive result bytes, costs, authority, transport bytes, and 65,535-row takes fail before
    partial release; old artifacts remain exact.
  standing_design_tests:
    interface_safety: Every query states its finite maximum and receives no way to weaken bytes,
      work, policy, authority, cursor, or transport limits when choosing more rows.
    scale: The structural u16 ceiling is independent from conservative result bytes, scanned work,
      hydration, policy, provider, memory, and output bounds; no unbounded allocation is admitted.
  closure:
    status: complete
    evidence: crates/riffdb-riffql-syntax/tests/bounded_limit_syntax.rs
    decision: ADR-0174's shared 65,535-row scan and 65,534-row page ceilings now flow through
      syntax, checked IR, compilation, execution, role authority, packed public results, generated
      artifacts, and handbook guidance. Bounds through 499 retain V9/V12/V12; larger bounds use
      V11/V14/V14. Static and runtime 500-row pages execute with one probe, 65,535 is refused,
      the 4 MiB result/transport ceiling and the exact-vector 500-row provider ceiling remain
      independent, and all WP-736 acceptance commands pass.
  human_review_triggers: *id0174

- id: WP-737
  name: Long-value exact wildcard pattern provider
  depends_on: [WP-734]
  objective: Implement `long_pattern_v1` declaration, derived checkpoint and recovery, exact
    binary/Unicode-fold literal and wildcard semantics for 8,000-byte source values, and bounded
    candidate-source integration without changing storage keys or existing text providers.
  requirements: [OQ-085, OQ-087, OQ-089, OQ-090, OQ-092, OQ-093, OQ-094,
    OQ-095, OQ-096, OQ-098, OQ-099]
  required_adrs: [ADR-0004, ADR-0086, ADR-0108, ADR-0124, ADR-0130, ADR-0131,
    ADR-0164, ADR-0172, ADR-0173, ADR-0174]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0174-bounded-filtered-result-pipelines.md,
    Cargo.lock, release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-riffql-syntax/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-query-executor/**,
    crates/riffdb-catalog/**, crates/riffdb-projection/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-policy/**, crates/riffdb-errors/**,
    crates/riffdb-service/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-cli/**, clients/**, templates/**,
    examples/**, fixtures/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - Contract grammar and checked declaration with profile, supported operators, source/matched
    bytes, rows, grams, postings, pattern, candidate, verification, result, replay, staleness, and
    retention bounds in least-sufficient bundle and topology identities
  - Canonical provider-state V1 retaining one matched value, digest, distinct byte trigrams,
    postings, and release data per key with atomic mutation, rebuild, replay, checkpoint,
    compaction, crash recovery, corruption refusal, and byte-frozen fixtures
  - Linear-space wildcard evaluator freezing `%`, `_`, escape, binary, Unicode-fold, literal
    prefix/suffix/contains, digest/gram candidate, collision verification, short/wildcard-only
    bounded provider scan, and authorized negation semantics
  - Candidate-source, authorization, participant, cursor, generated-surface, independent reference,
    randomized history, maximum expansion/value, storage parity, and architecture-negative tests
  acceptance_commands:
  - cargo test -p riffdb-types -p riffdb-contract-syntax -p riffdb-contract-ir
    -p riffdb-contract-compiler -p riffdb-projection --all-features
  - cargo test -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module
    -p riffdb-query-executor -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-policy -p riffdb-service -p riffdb-api-grpc -p riffdb-api-mcp
    -p riffdb-client-rust -p riffdb-driver-host -p riffdb-cli --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every accepted literal/wildcard pattern is exact and bounded through 8,000 source
    bytes across rebuild/recovery and every public surface; no key widening, quadratic substring
    state, authoritative scan, partial result, tokenized substitution, or hidden-row complement
    exists.
  standing_design_tests:
    interface_safety: Applications invoke only compiled operators over one declared field/profile;
      they cannot choose provider structure, grams, matcher, scan, budget, collation, or fallback.
    scale: Source/matched bytes, rows, grams, postings, candidates, verification, matcher states,
      result, checkpoint, replay, retained epochs, and diagnostics are independently bounded.
  closure:
    status: complete
    completed_at: "2026-08-31"
    requirements_satisfied: [OQ-085, OQ-087, OQ-089, OQ-090, OQ-092, OQ-093,
      OQ-094, OQ-095, OQ-096, OQ-098, OQ-099]
    evidence: Contract grammar/executable IR/bundle V21 compile checked pattern_index declarations;
      the exact binary and Unicode-fold matcher freezes literal, percent, underscore, escape,
      short-pattern, collision-verification, and 8,000-byte semantics. Rebuildable provider-state
      V1 has a byte-frozen fixture and topology domain; the production worker performs bounded
      policy admission and atomic checksummed activation, while an architecture-negative test
      proves the request port reads only retained epochs. Combined ordinary-tag and long-pattern
      candidate execution, corruption, work-observation, storage, service, server, workspace,
      handbook, generated, and strict Clippy gates pass.
    known_limitations: Direct negative provider sources remain forbidden; compiled difference
      requires an authorized positive universe. Provider lifecycle failures never fall back to an
      authoritative request-time scan.
    follow_ups: [WP-738]
  human_review_triggers: *id0174

- id: WP-738
  name: Bounded filtered-result generic and downstream closure
  depends_on: [WP-735, WP-736, WP-737]
  objective: Close ADR-0174 with one generic combined-search corpus, cross-language and
    cross-process conformance, downstream adapter receipts, compatibility, recovery, security,
    handbook, and full-workspace evidence.
  requirements: [OQ-084, OQ-085, OQ-086, OQ-087, OQ-088, OQ-089, OQ-090,
    OQ-091, OQ-092, OQ-093, OQ-094, OQ-095, OQ-096, OQ-097, OQ-098, OQ-099, OQ-100]
  required_adrs: [ADR-0055, ADR-0086, ADR-0108, ADR-0111, ADR-0124, ADR-0130,
    ADR-0131, ADR-0134, ADR-0150, ADR-0158, ADR-0159, ADR-0164, ADR-0167,
    ADR-0172, ADR-0173, ADR-0174]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0174-bounded-filtered-result-pipelines.md,
    release/version-topology-v1.json, proto/**, crates/**, clients/**, templates/**,
    examples/**, fixtures/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - Generic experiment/run-like corpus combining relationship tags, attributes, exact and wildcard
    text, null/existence and negation before multiple root orders, limits, and cursors
  - Four-language local/remote, gRPC, MCP, CLI, concurrency, cancellation, policy noninterference,
    cursor retirement/tamper, crash/rebuild/restore, compatibility, and maximum-bound evidence
  - Fresh downstream adapter check and value-free receipt proving no adapter filtering, sorting,
    counting, deduplication, denormalization, restart, framework branch, or semantic page walk
  - Complete handbook, generated reference, architecture, operations, compatibility, known-limit,
    requirement-coverage, topology, dependency, formatting, lint, documentation, and workspace gates
  acceptance_commands:
  - cargo test --workspace --all-features
  - cargo doc --workspace --no-deps
  - cargo deny check
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/check-adr-obligations
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The generic and downstream corpora prove exact complete filtering before order/page,
    scalable bounded rows, long wildcard semantics, policy, recovery, compatibility, and every
    surface with no unexplained failure or adapter workaround.
  standing_design_tests:
    interface_safety: All evidence uses generated named operations and typed values; no caller can
      express joins, set structure, fields, providers, arbitrary orders, budgets, scans, fallback,
      policy bypass, or partial semantics.
    scale: Every candidate, provider, sort, page, byte, work, epoch, response, process, and external
      assembly dimension is finite and independently tested at its maximum and one over.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [OQ-084, OQ-085, OQ-086, OQ-087, OQ-088, OQ-089,
      OQ-090, OQ-091, OQ-092, OQ-093, OQ-094, OQ-095, OQ-096, OQ-097,
      OQ-098, OQ-099, OQ-100]
    evidence: The generic bounded-filtered corpus, compiler/runtime semantic tests, byte-frozen
      provider fixture, full workspace tests and docs, cargo-deny, generated/application binding,
      topology, requirements, policy, ADR, handbook, four-language remote driver, standard
      downstream adapters, formatting, and strict Clippy gates pass. The current MLflow repository
      at 804b29aa2d53312e8b1342d7df312f6692f7bb77 compiles 13 named queries and passes 19 unit
      tests plus five fresh-process loopbacks covering exact and folded patterns, tag intersection,
      variable-cardinality cursors, atomic experiment/run creation, revision-guarded updates,
      lifecycle-aware bounded partition-set Run search, and first-use provider readiness. The
      value-free receipt is release/evidence/long-pattern-readiness-mlflow-loopback-v1.json.
    known_limitations: The external MLflow adapter remains an in-progress tracking-store rather
      than a complete MLflow backend; metric, parameter, input, batch, remaining Run predicate,
      remaining Run order, and upstream SQL-store conformance work is outside ADR-0174 closure.
      Unsupported adapter shapes fail before contacting RiffDB and add no filtering or sorting
      fallback.
    follow_ups: [external-riffdb-mlflow-full-parity]
  human_review_triggers: *id0174

- id: WP-739
  name: Partition-set compatibility inventory and accepted boundary
  depends_on: [WP-738]
  objective: Freeze ADR-0175's real-consumer source shape, explicit bounds, identity inventory,
    architecture amendments, and failing generic/downstream fixtures before executable changes.
  requirements: [RQL-007, QRY-010, OQ-101, OQ-102, OQ-106, OQ-109, OQ-112]
  required_adrs: [ADR-0035, ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124, ADR-0134, ADR-0150, ADR-0159, ADR-0164, ADR-0174, ADR-0175]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0175-bounded-partition-set-operational-queries.md, adr/README.md,
    release/version-topology-v1.json, fixtures/**, tests/**, scripts/**, docs/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**]
  deliverables:
  - Accepted ADR, normative requirements, work-package graph, and handbook discoverability
  - Exact failing multi-experiment Run source plus negative locality/bound/type corpus
  - Inventory of least-sufficient language, IR, module, plan, role, lock, generated-schema,
    explain, cursor, and topology successors with old-artifact byte-equality assertions
  acceptance_commands:
  - ./scripts/check-adr-obligations
  - ./scripts/check-requirement-coverage
  - ./scripts/check-version-topology
  - ./scripts/handbook check
  - cargo fmt --all -- --check
  exit_gate: The accepted source and identity inventory fail only because executable
    partition-set routing is not yet active, while every legacy artifact remains exact.
  standing_design_tests:
    interface_safety: Source exposes only typed values and a literal maximum; callers cannot
      submit partitions discovered from data, a plan, index, join, order expression, or budget.
    scale: The structural ceiling is 65,535 explicit routes, with independent query-specific
      partition, byte, row, probe, merge, result, and total-work maxima specified before runtime.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [RQL-007, QRY-010, OQ-101, OQ-102, OQ-106, OQ-109, OQ-112]
    evidence: ADR-0175 is accepted and discoverable from the handbook. The specification,
      work-package graph, version topology, compatibility fixtures, and negative compiler corpus
      freeze the explicit finite-route source shape, independent bounds, identity successors, and
      fail-closed locality boundary. Legacy artifacts remain byte-exact, and ADR, requirement,
      topology, handbook, formatting, and downstream source gates pass.
    known_limitations: V1 accepts only caller-supplied explicit finite partition identities for
      read-only operational queries. It does not discover partitions from data, perform
      cross-partition joins or writes, or define a distributed snapshot protocol.
    follow_ups: []
  human_review_triggers: &id0175
  - Partition discovery, cross-partition relationships, mutation, caller-authored plans,
    partial results, or a distributed snapshot protocol would be introduced.
  - An existing language, IR, module, plan, role, cursor, key, or durable identity would be
    reinterpreted rather than selecting a least-sufficient successor.
  - Any bound, authorization, snapshot, order, cursor, redaction, or fail-closed guarantee in
    ADR-0175 would be weakened.

- id: WP-740
  name: Bounded set syntax and compiler-sealed partition routes
  depends_on: [WP-739]
  objective: Implement additive Set<T, MAX> syntax, checked type and query identities, locality,
    access/index proof, exact diagnostics, cost, authority, and generated input schemas.
  requirements: [RQL-007, OQ-101, OQ-102, OQ-104, OQ-106, OQ-107, OQ-109]
  required_adrs: [ADR-0051, ADR-0055, ADR-0108, ADR-0111, ADR-0124, ADR-0134,
    ADR-0150, ADR-0159, ADR-0174, ADR-0175]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0175-bounded-partition-set-operational-queries.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-policy/**, crates/riffdb-errors/**, crates/riffdb-application/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, clients/**,
    templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Parser/formatter/resolver support for Set<T, MAX> with 1..65,535 source-spanned checks
  - Checked scalar-or-set route descriptor and compiler proof of one exact partition field,
    one route shape, compatible partition-prefixed total-order index, and closed local plan
  - Additive language/IR/module/plan/role/lock/generated-schema/topology identities and old-byte
    compatibility fixtures
  - Whole-operation authority and checked multiplied cost using declared rather than submitted max
  acceptance_commands:
  - cargo test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler
    -p riffdb-query-module -p riffdb-policy --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The real Run query compiles into additive exact artifacts with complete bounded
    authority, while unsafe route shapes fail at precise source spans and legacy bytes do not move.
  standing_design_tests:
    interface_safety: The compiler alone chooses route role, index, replicated plan, order,
      maxima, cost, and authority; generated callers submit only the bounded typed list.
    scale: Checked multiplication covers declared maximum partitions and every per-partition
      row/probe/candidate/policy/provider/merge dimension before a plan can activate.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [RQL-007, OQ-101, OQ-102, OQ-104, OQ-106, OQ-107, OQ-109]
    evidence: RiffQL V13 parses and formats Set<T, MAX> with source-spanned 1..65,535 bounds.
      Query IR and module V16 carry the compiler-sealed finite route, generated schemas expose
      only typed bounded values, policy receives checked whole-operation authority, and compiler
      cost uses declared maxima with checked multiplication. Compiler, syntax, IR, module,
      policy, generation, application-binding, topology, compatibility, and diagnostic tests pass,
      including the MLflow Run shape with Set<Run.experiment_id, 1000> and Limit<5000>.
    known_limitations: Each named query fixes its element type and maximum at compile time; callers
      cannot select route fields, indexes, orders, costs, or budgets dynamically. The structural
      set ceiling is 65,535 and stricter query-specific authority and work limits still apply.
    follow_ups: []
  human_review_triggers: *id0175

- id: WP-741
  name: One-snapshot partition-set execution and global merge
  depends_on: [WP-740]
  objective: Execute the sealed local plan over explicit routes in one memory/redb snapshot,
    apply policy before observation, and produce one bounded globally ordered page.
  requirements: [QRY-010, OQ-103, OQ-104, OQ-106, OQ-107, OQ-108, OQ-110]
  required_adrs: [ADR-0035, ADR-0038, ADR-0053, ADR-0070, ADR-0111, ADR-0130,
    ADR-0150, ADR-0164, ADR-0174, ADR-0175]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0175-bounded-partition-set-operational-queries.md,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-executor/**,
    crates/riffdb-projection/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-service/**, crates/riffdb-server/**,
    crates/riffdb-testkit/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - One engine-owned snapshot and frontier around all partition scans in memory and redb
  - Deterministic bounded k-way merge for ordered index streams plus bounded complete-sort path
    for candidate-root results, with limit/probe only after global order
  - Whole-set row-policy execution and refusal without per-partition counts or partial output
  - Independent oracle, memory/redb parity, cancellation, maximum/over-maximum, and concurrent
    mutation schedules without sleep-based synchronization
  acceptance_commands:
  - cargo test -p riffdb-query-executor -p riffdb-storage-memory
    -p riffdb-storage-redb -p riffdb-policy -p riffdb-service --all-features
  - cargo test --test operational_queries
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Empty, one, duplicate, many, maximum, and excessive partition sets have memory/redb
    parity and exact global order under one snapshot with no partial result on every failure.
  standing_design_tests:
    interface_safety: Storage receives only the compiler-sealed route descriptor and owned typed
      parameters; no iterator, transaction, per-partition result, or fallback crosses the port.
    scale: V1 uses a bounded merge heap or bounded candidate materialization and does not require
      all authoritative rows or all database partitions in memory.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [QRY-010, OQ-103, OQ-104, OQ-106, OQ-107, OQ-108, OQ-110]
    evidence: Memory and redb execute all normalized routes inside one engine-owned snapshot and
      frontier. Uniform-order plans use a bounded k-way heap; mixed-order and candidate-root plans
      use compiler-bounded complete groups. Candidate identities include partition and key,
      provider batches merge across routes, policy is evaluated before observation, and duplicate
      routes normalize deterministically. Independent-oracle, parity, cancellation, concurrency,
      maximum/over-maximum, service, storage, and recovery tests pass without partial results.
    known_limitations: Mixed-direction ordering uses the separately bounded complete-group path
      rather than the streaming heap. Execution is single-node and one-snapshot; it does not imply
      a snapshot spanning independent RiffDB processes.
    follow_ups: []
  human_review_triggers: *id0175

- id: WP-742
  name: Partition-set cursor, service, policy, and generated surface parity
  depends_on: [WP-741]
  objective: Bind global continuation to normalized routes and complete epoch observations, then
    carry exact semantics through service, transports, drivers, generated clients, CLI, and MCP.
  requirements: [QRY-010, OQ-105, OQ-106, OQ-107, OQ-109, OQ-111]
  required_adrs: [ADR-0007, ADR-0008, ADR-0027, ADR-0052, ADR-0053, ADR-0055,
    ADR-0111, ADR-0124, ADR-0134, ADR-0137, ADR-0148, ADR-0159, ADR-0164, ADR-0175]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0175-bounded-partition-set-operational-queries.md,
    release/version-topology-v1.json, proto/**, crates/**, clients/**,
    templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**,
    release/distribution/**]
  deliverables:
  - One compact cursor carrying the last global key and domain-separated complete observation digest
  - Whole-route canonical parameter hashing, page-cardinality independence, tamper/drift/expiry
    refusal, policy revalidation, and no process-global continuation state
  - Exact Rust/Go/TypeScript/Python, gRPC, MCP, CLI, local/remote schemas and conformance
  - Handbook authoring, planning, operations, compatibility, and diagnostic guidance
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-version-topology
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public surface returns the same globally ordered cursor page and refuses the
    same changed/tampered route or epoch without exposing physical per-partition state.
  standing_design_tests:
    interface_safety: Public callers receive one generated bounded list and opaque cursor; no
      surface exposes merge streams, epoch maps, partition cursors, plan knobs, or fallback.
    scale: Cursor bytes stay constant with partition count through a complete observation digest;
      request, work, response, and frame ceilings stay independent.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [QRY-010, OQ-105, OQ-106, OQ-107, OQ-109, OQ-111]
    evidence: One opaque global cursor binds the canonical distinct route set, query identity,
      invariant filters, complete observation digest, and last global key while excluding page
      cardinality from cursor identity. Reordered and duplicate routes resume identically, changed
      routes and tampered or stale observations fail closed, and Rust, Go, TypeScript, Python,
      local, remote, gRPC, generated binding, service, policy, CLI, and MCP conformance gates pass.
    known_limitations: Cursor size remains constant by retaining a digest rather than exposing
      per-partition cursors. The structural response page ceiling is 65,534 rows, the response
      envelope remains bounded to 4 MiB, and a named query may declare a lower maximum.
    follow_ups: []
  human_review_triggers: *id0175

- id: WP-743
  name: Partition-set generic and MLflow downstream closure
  depends_on: [WP-742]
  objective: Close ADR-0175 with generic cross-process evidence and a fresh real MLflow Run search
    receipt proving exact multi-experiment order, cursor, writes, policy, compatibility, and recovery.
  requirements: [RQL-007, QRY-010, OQ-101, OQ-102, OQ-103, OQ-104, OQ-105,
    OQ-106, OQ-107, OQ-108, OQ-109, OQ-110, OQ-111, OQ-112]
  required_adrs: [ADR-0035, ADR-0051, ADR-0053, ADR-0055, ADR-0108, ADR-0111,
    ADR-0124, ADR-0130, ADR-0134, ADR-0150, ADR-0159, ADR-0164, ADR-0174, ADR-0175]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0175-bounded-partition-set-operational-queries.md,
    release/version-topology-v1.json, proto/**, crates/**, clients/**,
    templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**,
    release/distribution/**]
  deliverables:
  - Generic experiment/run-like multi-partition corpus with local tag/metric/parameter candidates,
    multiple total orders, page-size changes, concurrent writes, policy, and typed stale cursors
  - Cross-language local/remote, gRPC, MCP, CLI, recovery, compatibility, cancellation, and maximum
    boundary evidence
  - Fresh value-free downstream receipt proving no adapter fan-out, filtering, sorting, merge,
    counting, cursor chains, hidden state, partition-model rewrite, or snapshot workaround
  - Complete documentation, generated references, topology, requirements, policy, dependency,
    formatting, lint, and workspace evidence with a clean committed main branch
  acceptance_commands:
  - cargo test --workspace --all-features
  - cargo doc --workspace --no-deps
  - cargo deny check
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/check-adr-obligations
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Real MLflow Run search over multiple experiment partitions passes one global ordered
    cursor loopback with surrounding revision-safe writes and no RiffDB or adapter blocker.
  standing_design_tests:
    interface_safety: Acceptance invokes generated named operations only; no application code owns
      database filtering, sorting, merge, snapshot, authority, or cursor semantics.
    scale: Partition count, route bytes, every local/global row and work class, merge state,
      provider/candidate state, output, cursor, and process resource are finite and tested at max+1.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [RQL-007, QRY-010, OQ-101, OQ-102, OQ-103, OQ-104,
      OQ-105, OQ-106, OQ-107, OQ-108, OQ-109, OQ-110, OQ-111, OQ-112]
    evidence: The generic workspace, documentation, dependency, generation, compatibility,
      application-binding, policy, recovery, and four-language remote conformance gates pass. A
      disposable copy of the current MLflow adapter deployed its contract, eleven-query module,
      and exact role, then used generated authenticated Python bindings to create three Runs in two
      experiment partitions and traverse one global start_time-desc/run_id-asc cursor from page
      size 1 to 100. Reversing route order and adding a duplicate preserved continuation and the
      independent oracle. Atomic experiment/tag creation and revision-guarded tag update/delete
      also passed. Follow-up exact lifecycle coverage proves both parameter and enum-constant
      predicates over `(partition, lifecycle, order...)`, V17 module round-trip, physical cursor
      resume, and the original application lock reproduction. The value-free receipts record that
      no adapter fan-out, filtering, sorting, merge, cursor chains, hidden state, or partition
      rewrite occurred.
    known_limitations: This capability is deliberately limited to explicit finite read routes and
      one single-node snapshot. Cross-partition relationships, writes, partition discovery, and
      caller-authored plans remain rejected. Query-specific route, row, work, byte, response, and
      page maxima remain mandatory; the validated MLflow query declares 1,000 partitions and 5,000
      results rather than relying on the former 499-row planning ceiling.
    follow_ups: []
  human_review_triggers: *id0175

- id: WP-744
  name: Compiler-bounded large atomic command envelope and MLflow closure
  depends_on: [WP-679, WP-743]
  objective: Implement ADR-0176 end to end so one compiler-proved atomic command may combine up
    to 4 MiB of decoded/canonical root input through one bounded 8 MiB command frame while every
    individual value, non-command request, effect, graph, idempotency, and atomicity guarantee
    remains unchanged, then prove the legal MLflow 100-tag Run shape in a real loopback.
  requirements: [BLK-058, BLK-059, BLK-060, BLK-061, BLK-062, BLK-063, BLK-064]
  required_adrs: [ADR-0002, ADR-0003, ADR-0005, ADR-0006, ADR-0007, ADR-0008,
    ADR-0011, ADR-0013, ADR-0031, ADR-0055, ADR-0058, ADR-0059, ADR-0124,
    ADR-0129, ADR-0137, ADR-0147, ADR-0148, ADR-0176]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0147-aggregate-byte-budgets-for-atomic-collection-commands.md,
    adr/0176-compiler-bounded-large-atomic-command-envelope.md, adr/README.md,
    release/version-topology-v1.json, release/evidence/**, proto/**, crates/**,
    clients/**, templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**,
    release/distribution/**]
  deliverables:
  - Purpose-specific canonical root command-input encoding with a fixed 4 MiB ceiling and exact
    old-byte/hash equality, while every nested value and ordinary canonical record retains 1 MiB
  - Compiler-derived maximum request bytes, least-sufficient grammar/executable IR/bundle V22,
    round-trip proof recomputation, plan hashing, exact catalog/runtime validation, and typed
    source-spanned rejection above 4 MiB
  - One shared service enforcement path before effects plus 8 MiB command-bearing gRPC,
    application-session, reaction, MCP, CLI, batch-item, and local-driver framing with all
    non-command and individual-value limits retained
  - Cross-language generation/driver parity, idempotency/replay/recovery/atomicity evidence,
    handbook/topology/compatibility updates, and a value-free real MLflow 100-tag Run receipt
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 doc --workspace --no-deps
  - cargo +1.97.0 deny check
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/check-adr-obligations
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A legal MLflow-sized atomic command commits and replays one complete 100-tag graph
    through generated bindings and redb with exact indexed/hydrated visibility, while max+1 and
    every unrelated or nested widening fail closed and no adapter split or reduced limit remains.
  standing_design_tests:
    interface_safety: Applications submit only generated typed values and cannot select an
      envelope version, limit, encoder, frame, transaction, split, retry subset, or fallback; the
      compiler and shared service own every complete-request and effect proof.
    scale: Decoded root input is fixed at 4 MiB, command framing at 8 MiB, individual documents at
      1 MiB, and the graph at 16 MiB, with independent count, depth, mutation, index, partition,
      concurrency, retry, response, and diagnostic ceilings tested at exact and plus-one bounds.
  closure:
    status: complete
    requirements_satisfied: [BLK-058, BLK-059, BLK-060, BLK-061, BLK-062, BLK-063, BLK-064]
    evidence: release/evidence/large-atomic-command-envelope-v1.json records implementation
      revision b72463e7d1f8e3293bcfd1f9454e18671394c549, exact fixed bounds, least-sufficient V22
      identity, negative and compatibility coverage, every acceptance gate, all-language remote
      conformance, three downstream adapter checks, and the value-free real MLflow 100-tag atomic
      commit plus same-commit replay.
    known_limitations: The wider envelope is command-root-only and fixed at 4 MiB decoded or
      canonical bytes inside an 8 MiB frame; individual and nested documents remain 1 MiB,
      complete command graphs remain 16 MiB, and ordinary query responses remain 4 MiB. The
      MLflow adapter therefore coalesces compiler-bounded four-row internal pages to satisfy
      larger finite caller pages without client-side filtering, sorting, or cursor invention.
    follow_ups: []
  human_review_triggers: &id0176
  - Any decoded root command limit above 4 MiB, command frame above 8 MiB, complete graph above
    16 MiB, or widening of an individual value or non-command request would be introduced.
  - A caller, contract, role, generated method, or operator could select an envelope, size budget,
    transaction split, compression, or fallback.
  - Existing grammar, IR, bundle, plan, canonical input, idempotency, durable, protocol, or driver
    identities would be reinterpreted instead of retaining old bytes or selecting V22.

- id: WP-745
  name: Compiler-bounded high-cardinality atomic collections
  depends_on: [WP-744]
  objective: Implement ADR-0177 end to end so one least-sufficient V23 collection plan may prove
    up to 1,024 submitted elements and 4,096 complete authoritative mutation instances while
    every legacy identity and independent locality, byte, work, durability, and atomicity bound
    remains unchanged, then prove one 1,000-element command plus a fixed root mutation.
  requirements: [BLK-065, BLK-066, BLK-067, BLK-068, BLK-069, BLK-070]
  required_adrs: [ADR-0002, ADR-0003, ADR-0013, ADR-0031, ADR-0055, ADR-0059,
    ADR-0107, ADR-0147, ADR-0176, ADR-0177]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0177-compiler-bounded-high-cardinality-atomic-collections.md, adr/README.md,
    release/version-topology-v1.json, release/evidence/**, crates/**, clients/**,
    templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Separate legacy 256/256 and successor 1,024/4,096 collection bounds with checked complete
    fixed-plus-expanded mutation-instance proof and the independent 256-row cascade ceiling
  - Least-sufficient grammar/executable-IR/bundle V23 selection, plan hashing, round-trip proof
    recomputation, old-version refusal, topology registration, and byte-exact legacy fixtures
  - Shared generated/service/runtime/storage enforcement with exact/plus-one diagnostics,
    independent resource refusals, cancellation, idempotency, replay, and no partial effects
  - Memory/redb atomic 1,000-element-plus-root acceptance, handbook/compatibility updates,
    downstream adapter compilation, and a value-free real store-loopback receipt
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 doc --workspace --no-deps
  - cargo +1.97.0 deny check
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/check-adr-obligations
  - ./scripts/handbook check
  - ./scripts/downstream-adapter-check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One compiler-sealed 1,000-element command plus a fixed root mutation commits,
    replays, and recovers atomically through redb and generated bindings; every exact/plus-one
    and independent bound is proved, legacy artifacts remain byte-exact, and no adapter split,
    staging protocol, reduced limit, or storage bypass remains.
  standing_design_tests:
    interface_safety: Applications submit only generated typed values and cannot select the
      process ceiling, V23, mutation count, transaction, split, retry subset, or fallback; the
      compiler and first-party runtime own all count, locality, authority, and atomicity proofs.
    scale: The successor caps elements at 1,024 and possible mutations at 4,096 while retaining
      every independent conflict, observation, dependency, index, event, 4-MiB input, 8-MiB
      frame, 16-MiB graph, deadline, cancellation, response, and diagnostic ceiling.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [BLK-065, BLK-066, BLK-067, BLK-068, BLK-069, BLK-070]
    evidence: release/evidence/high-cardinality-atomic-collections-v1.json records the accepted
      256/256 legacy and 1,024/4,096 successor limits, V23 identity and compatibility proofs,
      exact/plus-one compiler/runtime/storage coverage, all-language generated-surface checks,
      and the value-free real-redb 1,000-entity-plus-root store, replay, forced failure, and
      complete-or-absent follow-up receipt.
    known_limitations: The successor remains one compiler-owned list, one exact partition, and
      one atomic command. Conflict keys, cascade rows, index work, input/frame/graph bytes,
      deadlines, cancellation, and every other independent resource remain separately bounded;
      generated callers receive only the exact source-declared maximum.
    follow_ups: []
  human_review_triggers: &id0177
  - More than 1,024 submitted elements or 4,096 possible authoritative mutations would be
    admitted, or another independent count, byte, work, framing, or graph ceiling would widen.
  - More than one collection, more than one partition, a caller-selected capacity, adapter
    staging, partial commit, subset retry, or general transaction surface would be introduced.
  - V1 through V22 bytes or semantics would be reinterpreted instead of selecting V23.

- id: WP-721
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [PERF-005]
    evidence: crates/riffdb-contract-compiler/tests/cross_aggregate_writes.rs proves identical-route
      admission, per-binding conflict ownership, RDB-C017 cross-route refusal, V19 round-trip, and
      least-sufficient legacy identity. The
      a_cross_aggregate_union_with_a_state_derived_conflict_key_is_rejected,
      cross_aggregate_conflict_acquisition_is_globally_ordered_and_deadlock_free, and
      disjoint_cross_aggregate_union_conflicts_do_not_serialize arms prove the canonical global
      lease order and independent acquisition of disjoint two-aggregate unions without sleeps.
      crash_before_cross_aggregate_commit_leaves_both_aggregates_absent and
      crash_after_cross_aggregate_commit_preserves_both_aggregates prove atomicity and repeatable
      recovery under Standard and Hardened commit profiles; the post-commit arm carries indexes for
      both aggregates. a_cross_aggregate_index_entry_may_change_namespace_but_not_route proves
      per-owner index partitioning accepts only the compiler-proved route. Existing generated and
      compatibility suites retain the byte-exact single-aggregate formats and identities. Detailed
      implementation evidence is retained in docs/performance/wp-721-cross-aggregate-conflict-ownership.md.
    known_limitations: Cross-partition reads and writes remain rejected. Conflict ownership must be
      completely input-computable and bounded, and applications cannot select the conflict union,
      acquisition order, partition placement, transaction boundary, or durability behavior.
    follow_ups: []
  name: Partition-local cross-aggregate command writes
  depends_on: [WP-040, WP-080]
  objective: Close ADR-0170 by admitting a command whose create/mutate bindings derive one
    identical partition route but belong to different aggregates in that partition, deriving
    logical conflict ownership as the statically enumerable union of the conflict keys those
    bindings derive, and acquiring that union under a canonical deadlock-free lease order.
    Cross-partition writes remain rejected by RDB-C017.
  requirements: [PERF-005]
  required_adrs: [ADR-0094, ADR-0170]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0170-partition-local-cross-aggregate-writes.md,
    crates/riffdb-contract-compiler/**, crates/riffdb-contract-ir/**,
    crates/riffdb-diagnostics/**, crates/riffdb-commit/**,
    crates/riffdb-proto/**, examples/**, fixtures/**, tests/**]
  deliverables:
  - Compiler proof that every create/mutate binding derives one identical partition route,
    admitting bindings from different aggregates in that partition and continuing to reject a
    command whose bindings derive different routes with RDB-C017
  - Command conflict ownership derived as the union of its bindings' conflict keys, rejected
    when the complete union is not statically enumerable
  - Canonical total order over (aggregate, conflict key) proving lease acquisition across
    aggregates cannot deadlock, with a concurrency test that would fail under an arbitrary order
  - Executable IR V19 carrying per-conflict-key aggregate ownership, written only by a command
    that genuinely spans aggregates so every single-aggregate command keeps its current locality
    bytes and plan hash under the ADR-0126 least-sufficient writer rule; the format question is
    settled in docs/performance/wp-721-cross-aggregate-conflict-ownership.md
  - Crash-consistency coverage proving one command writing two aggregates is atomic across a
    restart, and a negative arm proving two commands with disjoint union conflict sets do not
    serialise against each other. The crash coverage is delivered as
    crash_before_cross_aggregate_commit_leaves_both_aggregates_absent and
    crash_after_cross_aggregate_commit_preserves_both_aggregates over both commit profiles
  - Per-aggregate index partitioning, resolving the restriction the crash arm found. Index entries
    follow their owning aggregate's partition key, and the write plan's one-partition rule relaxes
    to a same-route rule so cross-partition writes stay refused.
    a_cross_aggregate_index_entry_may_change_namespace_but_not_route pins both halves, and the crash
    arm carries an indexed second aggregate
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/check-adr-obligations
  - ./scripts/downstream-adapter-check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A partition-local command writing two aggregates compiles, commits atomically, and
    owns the union of its bindings' conflict keys; a cross-partition write is still rejected; the
    lease order is proved deadlock-free; and no existing contract, durable format, or conflict
    identity changes.
  standing_design_tests:
    interface_safety: No surface admits a write whose bindings derive different partition routes,
      and no caller can select conflict ownership, lease order, or a weaker atomicity.
    concurrency: Two commands whose union conflict sets are disjoint proceed without serialising;
      two whose sets intersect are ordered by the canonical lease order and never deadlock.
- id: WP-720
  name: Admission-head generated compatibility and cross-process acceptance
  depends_on: [WP-565, WP-703, WP-709, WP-719]
  objective: Close ADR-0164 across additive gRPC/driver options, generated Rust/Go/TypeScript/Python,
    CLI, MCP, local/remote execution, compatibility negotiation, cross-process conformance, handbook,
    and one value-free external receipt without exposing raw heads or framework vocabulary.
  requirements: [OQ-075, OQ-078, OQ-079, OQ-080, OQ-081, OQ-082,
    OQ-083, DRV-007, DRV-008, DRV-012, DRV-014, DRV-018, SAFE-004, SAFE-005]
  required_adrs: [ADR-0053, ADR-0055, ADR-0070, ADR-0071, ADR-0086,
    ADR-0124, ADR-0130, ADR-0141, ADR-0148, ADR-0150, ADR-0158, ADR-0159,
    ADR-0164]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0164-admission-head-fenced-query-consistency.md,
    release/version-topology-v1.json, proto/**, crates/riffdb-errors/**,
    crates/riffdb-proto/**, crates/riffdb-service/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-query-module/**,
    crates/riffdb-driver-host/**, crates/riffdb-cli/**, clients/**,
    templates/**, examples/**, fixtures/**, tests/**, scripts/**,
    release/evidence/**, docs/**]
  deliverables:
  - Additive closed gRPC and shared driver-core request value plus capability/version negotiation;
    omission preserves legacy behavior and older peers return typed refresh/upgrade guidance
  - Generated Rust, Go, TypeScript, and Python options and CLI/MCP routes carrying one semantic value
    with no raw sequence, caller wait, provider, transport-local capture, connection-order inference,
    retry of stale success, or silent downgrade
  - Installed local-driver and remote-gRPC cross-language corpus covering authoritative, projected,
    exact-index, composite, explicit-fence maximum, cursor, count/offset, error, cancellation, and
    revocation equivalence
  - Cross-client/process and sustained-write process tests proving an acknowledged pre-admission
    write is visible without adapter state and later cursor pages retain the first epoch
  - Protobuf/driver compatibility fixtures, topology, generated freshness, clean-install, old-client
    omission, old-server refusal, and unchanged query/lock/role/durable/cursor-token identities
  - Handbook and value-free external receipt mapping one stronger external preference to AdmissionHead
    while retaining the external enum, schema, adapter, and conformance source outside RiffDB
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public/generated surface implements one fresh-through-admission guarantee across
    processes and providers with exact cursor/epoch, error, cancellation, policy, and compatibility
    behavior; external evidence is green; and no raw head, unsafe wait, stale fallback, framework
    branch, or unexplained identity drift remains.
  standing_design_tests:
    interface_safety: Every public surface exposes only the same stronger enum and cannot express a
      raw head, wait, provider, fallback, transport-local policy, or guarantee downgrade.
    scale: Cross-surface tests retain bounded requests, waiters, pages, processes, epochs, and
      diagnostics; generated/driver layers add no per-row work or population-sized state.
  human_review_triggers: *id001
- id: WP-678
  name: Aggregate collection-byte language, proof, and IR successors
  depends_on: [WP-560, WP-612]
  objective: Implement ADR-0147's one compiler-owned aggregate canonical-element-byte constraint
    for the list expanded by an atomic collection command, deriving a finite conservative copy
    coefficient and least-sufficient versioned identities without raising any existing ceiling.
  requirements: [BLK-015, BLK-016, BLK-017, BLK-018, DSL-001, DSL-002,
    DSL-003, DSL-004, DSL-005, DSL-006, DSL-007, DSL-008, DSL-009, DSL-010,
    DSL-011, DSL-012, SAFE-004, SAFE-005, PERF-004, PERF-005]
  required_adrs: [ADR-0002, ADR-0011, ADR-0013, ADR-0031, ADR-0055,
    ADR-0107, ADR-0124, ADR-0147]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0147-aggregate-byte-budgets-for-atomic-collection-commands.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-diagnostics/**,
    crates/riffdb-cli/assets/editor/**, clients/editor/**, fixtures/compiler/**,
    fixtures/contracts/**, fuzz/**, tests/**, scripts/**, docs/SUMMARY.md,
    docs/contracts/**, docs/architecture/**]
  deliverables:
  - Source grammar for one positive `aggregate_bytes` constraint only on the single
    expanded list, with source-spanned refusals for duplicates, non-literals, zero/excess,
    ordinary commands, unexpanded/nested lists, and multiple pools
  - Independent canonical element-byte semantics retaining every per-value and cardinality bound,
    plus a closed conservative flow analysis deriving fixed bytes and maximum copy coefficient
    across command input, bindings, mutations, and events
  - Least-sufficient grammar/executable-IR/bundle V16 successor carrying the declared aggregate
    maximum and derived coefficient in plan/root hashes; V1 through V15 remain readable and
    byte-exact and declaration-free commands retain their prior writer
  - Positive neutral 1..100 bounded-context source and negative/property corpus covering exact
    bound, plus one, arithmetic overflow, optional values, copies, events, unsupported flows, and
    unchanged 16 MiB graph refusal
  - Topology, format registry, source-span snapshots, fixtures, editor assets, public contract
    documentation, and architecture checks proving no runtime, framework branch, transaction,
    split policy, or raised global ceiling lands in this package
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-diagnostics --all-features
  - cargo +nightly-2026-07-12 fuzz run contract_parser -- -max_total_time=60
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every admitted aggregate constraint has one canonical meaning, finite compiler-derived
    graph proof, versioned identity, independent boundary result, and source-spanned failure; old
    artifacts remain exact and no runtime or global resource ceiling changes.
  standing_design_tests:
    interface_safety: Callers provide only typed element values; they cannot choose the aggregate
      ceiling, copy coefficient, graph budget, split behavior, transaction, or encoding, and the
      compiler rejects any flow it cannot prove.
    scale: Cardinality, individual values, aggregate canonical bytes, coefficient, fixed bytes,
      complete graph, IR, fixtures, diagnostics, and compiler work are statically bounded with
      checked arithmetic and no compression or deduplication assumption.
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [BLK-015, BLK-016, BLK-017, BLK-018, DSL-001, DSL-002,
      DSL-003, DSL-004, DSL-005, DSL-006, DSL-007, DSL-008, DSL-009, DSL-010,
      DSL-011, DSL-012, SAFE-004, SAFE-005, PERF-004, PERF-005]
    evidence: The accepted aggregate_bytes language and V16 grammar, executable-IR, and bundle
      successor now have source-spanned nested-list and syntax refusals, exact graph-boundary,
      arithmetic/property, plan/root/bundle identity, old-reader rejection, declaration-free
      writer, and V1-V15 compatibility proofs. The generated format registry documents and
      conformance-checks both V16 proof fields without changing any runtime, transaction, split
      policy, or resource ceiling. The accepted formatter-scope amendment records that no new
      contract formatting operation or semantics is part of this package.
  human_review_triggers: *id001
- id: WP-679
  name: Aggregate collection-byte runtime and generated acceptance
  tier: guarantee
  depends_on: [WP-561, WP-562, WP-678, WP-682]
  objective: Enforce ADR-0147's compiled aggregate byte constraint before effects in generated
    clients and the first-party Rust service/runtime, preserving one atomic idempotent command and
    proving neutral 100-element conformance across every public language surface.
  requirements: [BLK-006, BLK-007, BLK-008, BLK-009, BLK-013, BLK-014,
    BLK-019, BLK-020, BLK-021, ID-001, ID-004, OUT-001, OUT-002, REC-001,
    REC-002, STO-001, STO-002, TXN-001, TXN-010, TXN-011, TXN-012, TXN-013,
    TXN-040, TXN-041, TXN-042, TXN-043, TXN-044, AAA-001, AAA-002, AAA-003,
    AAA-004, AAA-005, AAA-009, AAA-010]
  required_adrs: [ADR-0003, ADR-0004, ADR-0005, ADR-0011, ADR-0012, ADR-0031,
    ADR-0041, ADR-0055, ADR-0056, ADR-0058, ADR-0059, ADR-0064, ADR-0093,
    ADR-0095, ADR-0100, ADR-0101, ADR-0102, ADR-0104, ADR-0106, ADR-0107,
    ADR-0124, ADR-0129, ADR-0147, ADR-0148, ADR-0191]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, proto/**,
    fixtures/proto/**, crates/riffdb-types/**, crates/riffdb-contract-ir/**,
    crates/riffdb-invariant/**, crates/riffdb-catalog/**, crates/riffdb-runtime/**,
    crates/riffdb-auth/**, crates/riffdb-policy/**, crates/riffdb-idempotency/**,
    crates/riffdb-service/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-query-module/**,
    crates/riffdb-cli/**, crates/riffdb-server/**, crates/riffdb-observability/**,
    crates/riffdb-testkit/**, clients/**, templates/**,
    fixtures/adapters/bulk-conformance/**, fixtures/application-parity/**,
    fixtures/compatibility/operation-adapters-v1/riffdb-cli/app.rs.in,
    fixtures/compatibility/public-adapter-freeze-v1.tsv,
    fixtures/driver/**, examples/**, tests/**, scripts/**, release/**,
    docs/SUMMARY.md, docs/contracts/**, docs/getting-started/**,
    docs/integrations/**, docs/reference/**]
  deliverables:
  - Generated Rust, Go, TypeScript, and Python typed preflight computes the exact canonical
    aggregate sum; exact-bundle CLI does the same, while raw CLI retains generic bounded-input
    validation and MCP publishes the frozen annotation without estimating canonical bytes; every
    path retains count/individual checks and one unsplit command
  - Application-service validation recomputes the bound with checked arithmetic before effectful
    evaluation and runtime validates the concrete expanded graph before coordinator submission;
    typed redacted errors distinguish count, individual, aggregate, and graph overflow
  - Neutral `PolicyMutation` fixture proves 1, 9, 19, and 100 small elements; individual 524,288-
    byte context acceptance; individual/aggregate/graph plus-one refusal; authorization, duplicate,
    business-failure, cancellation, idempotency, overlap, event, provenance, and atomicity semantics
  - Memory/redb, deterministic schedule, process crash, remote TLS, driver-host, CLI, MCP, and all-
    language generated observations with static guards excluding external framework artifacts,
    raw mutation/transaction access, and handwritten transport encoding
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-runtime -p riffdb-service -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test bulk_command_recovery --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix --all-features
  - ./scripts/adapter-bulk-command-acceptance --all-languages
  - ./scripts/check-application-bindings
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public surface rejects the same over-budget value before effects and accepts one
    real 100-element command as one ordinary atomic durable result; crashes expose complete outcome
    or absence, and no split, partial result, framework branch, or alternate runtime path exists.
  standing_design_tests:
    interface_safety: Generated preflight is advisory and the Rust service remains authoritative;
      no caller chooses budgets, coefficients, graph size, transactions, partial success, or split
      behavior, and every accepted mutation follows ordinary authorization and commit semantics.
    scale: Canonical aggregate sizing is linear in one already count/byte-bounded input and paid
      once per invocation, never per authorization atom, conflict, retry, mutation row, or durable
      transition; allocations and diagnostics remain within existing request/graph ceilings.
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [BLK-006, BLK-007, BLK-008, BLK-009, BLK-013, BLK-014,
      BLK-019, BLK-020, BLK-021, ID-001, ID-004, OUT-001, OUT-002, REC-001,
      REC-002, STO-001, STO-002, TXN-001, TXN-010, TXN-011, TXN-012, TXN-013,
      TXN-040, TXN-041, TXN-042, TXN-043, TXN-044, AAA-001, AAA-002, AAA-003,
      AAA-004, AAA-005, AAA-009, AAA-010]
    evidence: Generated Rust, Go, TypeScript, and Python facades and the exact-bundle CLI perform
      count, UTF-8 or byte-leaf, and exact canonical aggregate preflight before transport, while
      raw CLI and MCP retain structural validation and the Rust service authoritatively repeats
      typed validation before effects. Req-tagged exact-bound and plus-one tests, including the
      multibyte string boundary, complete-graph refusal, memory/redb, concurrency, crash, TLS,
      driver-host, CLI, MCP, and cross-language observations prove one unsplit atomic command with
      complete-or-absent durability. Generated facade and application-lock hashes rotate under
      review while operation, plan, protocol, public-error, and durable identities remain exact.
    known_limitations: Only ADR-0147's single compiler-owned expanded collection budget is
      supported; nested or unexpanded pools, multiple pools, and runtime negotiation remain
      unavailable. Raw CLI and MCP deliberately leave exact canonical sizing to the authoritative
      service. Correlated index-work runtime and larger command envelopes remain separate packages.
    follow_ups: [WP-684, WP-744]
  human_review_triggers: *id001
- id: WP-680
  name: Generated named-query cursor routing correction
  depends_on: [WP-658, WP-677]
  objective: Route compiler-typed cursor parameters through the existing protected continuation
    option in every generated language facade, removing the need for handwritten adapter routing
    without changing cursor bytes, query semantics, or low-level transport APIs.
  requirements: [OQ-004, OQ-006, OQ-015, OQ-016, OQ-031, API-001, SAFE-001]
  required_adrs: [ADR-0040, ADR-0051, ADR-0052, ADR-0053, ADR-0055,
    ADR-0106, ADR-0108, ADR-0111, ADR-0120, ADR-0124, ADR-0147]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-query-module/**,
    crates/riffdb-application/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, clients/**, templates/**, fixtures/**,
    tests/**, scripts/**, docs/SUMMARY.md, docs/riffql/**, docs/sdks/**,
    docs/getting-started/**, docs/reference/**]
  deliverables:
  - Generated Rust, Go, TypeScript, and Python methods recognize the exact cursor/optional-cursor
    parameter, omit it from symbolic input, and route a present value only to `Options.Cursor`
  - Shared first-page, continuation, null/absent, malformed, wrong-operation, wrong-database/
    history, expiry, cancellation, and bounded-response fixtures preserving existing cursor bytes
  - Architecture negatives prohibit client page walks for count, offset, filtering, sorting, or
    other semantics; any iterator convenience has an explicit finite page/item limit
  - Regenerated clients, package fixtures, SDK documentation, and compatibility evidence proving
    existing low-level `Options.Cursor` callers remain unchanged
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-module -p riffdb-application -p riffdb-client-rust -p riffdb-driver-host --all-features
  - ./scripts/adapter-operational-query-acceptance --all-languages
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every generated language resumes the same named query by passing its typed cursor only
    through the protected continuation field, all identity/staleness failures remain exact, and no
    client implements database semantics by walking pages.
  standing_design_tests:
    interface_safety: A caller supplies only an opaque typed cursor to one generated named method;
      it cannot choose an operation, plan, provider, epoch, order, predicate, or fallback, and a
      mismatch fails before or inside the unchanged shared service path.
    scale: One call processes one bounded cursor and one bounded page; optional helpers require a
      finite caller-owned stop and no hidden population/page walk is generated.
  human_review_triggers: *id001
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [OQ-004, OQ-006, OQ-015, OQ-016, OQ-031,
      API-001, SAFE-001]
    evidence: Generated Rust, Go, TypeScript, and Python facades preserve exact required and
      optional cursor signatures, remove the compiler-owned cursor from symbolic input, and route
      its bytes only through the existing protected option with local collision rejection. Query
      module validation rejects cursor parameters without one matching continuation slot. The
      shared live corpus proves absent first page, bounded continuation, terminal page, and
      malformed-cursor failure in all four languages; the retained closed fixture covers identity,
      history, expiry, cancellation, and response bounds. Static generator assertions and adapter
      escape scans prohibit hidden semantic page walks, and low-level option behavior remains
      unchanged.
- id: WP-681
  name: Repository-confined Go development runner package
  depends_on: [WP-325, WP-611]
  objective: Let `riffdb dev --run` execute one explicit repository-relative Go `main` package in
    a library module while preserving the module-root default, protected driver-host boundary,
    direct argv execution, application identity, and source/installed workflow parity.
  requirements: [DX-004, DX-026, DX-029, DX-031, DX-041, NET-012, DRV-002,
    DRV-003, DRV-004, DRV-011]
  required_adrs: [ADR-0007, ADR-0040, ADR-0041, ADR-0055, ADR-0056,
    ADR-0105, ADR-0106, ADR-0120, ADR-0147]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-cli/**,
    fixtures/application-boundaries/**, fixtures/driver/**, templates/**,
    tests/**, scripts/riffdb-dev*, scripts/*driver*, scripts/check-generated,
    docs/SUMMARY.md, docs/getting-started/**, docs/sdks/**, docs/reference/**]
  deliverables:
  - Source and installed `riffdb dev --run --go-runner-package PATH` support with default `.`, one
    canonical repository-relative directory, direct `go run` argv, and a bounded startup summary
  - Validation rejects empty/absolute/parent/traversal/symlink escape, nested-module ambiguity,
    missing or non-main package, multiple runner manifests, argument injection, and missing Go
    before the runner receives credentials or a driver-host connection
  - Library-module fixture with dedicated command package plus interruption, source/installed
    parity, driver-host trust ownership, no-shell, and application-boundary conformance
  - Handbook/CLI reference updates stating the option is development process configuration and
    changes no contract, module, role, plan, lock, or production application identity
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-cli --all-features
  - ./scripts/riffdb-dev-acceptance
  - ./scripts/check-application-boundary
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A library Go module runs one checked nested `main` package through the ordinary
    generated driver path in source and installed workflows, while every escape/ambiguity/injection
    case fails before protected values are exposed and module-root behavior remains unchanged.
  standing_design_tests:
    interface_safety: The option chooses only one repository-confined Go source package for the
      local development process; it cannot choose an executable, flags, shell, endpoint, credential,
      database, role authority, transport, or application operation.
    scale: Validation visits one bounded path and Go package; process count, output, shutdown,
      driver connection, arguments, and diagnostics retain existing bounded runner limits.
  closure:
    status: complete
    completed_at: "2026-08-26"
    requirements_satisfied: [DX-004, DX-026, DX-029, DX-031, DX-041, NET-012,
      DRV-002, DRV-003, DRV-004, DRV-011]
    evidence: The public CLI retains whether --go-runner-package was explicitly supplied and
      forwards the option only in that case. Omitted runner configuration remains language-neutral,
      so the shared source and installed scripts detect package.json and preserve their npm build
      then start path; detected Go repositories still default to the module-root package. CLI parser,
      exact argument-vector, and external-process regressions prove omission versus explicit dot,
      while the repository-confined Go validation, symbolic development orchestration, application
      boundary, generated-reference, requirement, handbook, formatting, and workspace Clippy gates
      pass without changing the script, protected driver-host boundary, or application identity.
    known_limitations: The Go driver-host development path retains its documented Linux-only alpha
      limitation. This package adds no production runner selection or caller-selected executable.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-683
  name: Correlated command index-work planning and precise bound diagnostics
  depends_on: [WP-678]
  objective: Implement ADR-0149's compiler-owned correlated index-work proof while retaining the
    4,096 physical index-delta and all existing byte ceilings, and make every known RDB-C020
    ceiling violation report its closed resource identity, actual value, and maximum safely.
  requirements: [BLK-022, BLK-023, BLK-026, DSL-001, DSL-002, DSL-003,
    DSL-004, DSL-005, DSL-006, DSL-007, DSL-008, DSL-009, DSL-010, DSL-011,
    DSL-012, SAFE-004, SAFE-005, PERF-004, PERF-005]
  required_adrs: [ADR-0002, ADR-0004, ADR-0011, ADR-0013, ADR-0023,
    ADR-0031, ADR-0055, ADR-0107, ADR-0124, ADR-0147, ADR-0149]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0149-correlated-index-validation-work-budgets.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-diagnostics/**, crates/riffdb-cli/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    clients/editor/**, fixtures/compiler/**, fixtures/contracts/**,
    fixtures/proto/**, tests/**, scripts/**, docs/SUMMARY.md,
    docs/contracts/**, docs/reference/**, docs/architecture/**]
  deliverables:
  - Shared checked constants retaining 4,096 index-entry deltas while bounding affected prefixes
    and validation positions structurally at 65,535 and their exact D+A+V work at 65,535
  - Conservative estimator with once-per-index whole-prefix, once-per-partition-route prefix,
    unchanged-leading-component, old/new suffix, unique-occupancy, and collection-expansion
    formulas; no caller-value or cross-element equality assumption
  - ADR-0147-aware affine affected-state byte proof when exact value provenance is available, with
    safe independent-maximum fallback and unchanged 16 MiB ceiling
  - Closed RDB-C020 resource evidence carrying actual and maximum through compiler, CLI, MCP, gRPC,
    LSP, source-span snapshots, and bounded redacted public rendering without a required wire-field
    change
  - Independent reduced-model/property tests proving every concrete derivation is no larger than
    its compiled count/work/byte maxima, plus exact and plus-one neutral index-rich fixtures
  - SPEC, accepted-ADR registry, compiler reference, and architecture documentation with no
    external framework name, runtime mutation, storage change, coarse invalidation, or raised byte
    limit in this package
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-diagnostics --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-types -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-diagnostics --all-targets --all-features -- -D warnings
  exit_gate: Every admitted plan proves D, A, V, D+A+V, and affected-state bytes within the exact
    ADR-0149 limits; exact/plus-one failures report safe actionable evidence at the command span;
    old plan bytes remain exact unless a separately audited successor is strictly required; and no
    runtime, durable, framework, or public budget selector lands.
  standing_design_tests:
    interface_safety: Application authors declare only entities, indexes, typed inputs, and one
      compiled command; the compiler exclusively owns costs, correlations, limits, diagnostics,
      and admission, with no caller-selected work class or unsafe fallback.
    scale: Compiler work is bounded by existing schema/plan ceilings; D is at most 4,096, A and V
      at most 65,535, D+A+V at most 65,535, affected state at most 16 MiB, arithmetic is checked,
      and no estimate depends on database rows, history, runtime equality, or backend scans.
  closure:
    status: complete
    completed_at: "2026-09-03"
    requirements_satisfied: [BLK-022, BLK-023, BLK-026, DSL-001, DSL-002,
      DSL-003, DSL-004, DSL-005, DSL-006, DSL-007, DSL-008, DSL-009, DSL-010,
      DSL-011, DSL-012, SAFE-004, SAFE-005, PERF-004, PERF-005]
    evidence: Compiler-owned D, A, V, D+A+V, and affine affected-state byte estimates retain the
      accepted structural and byte ceilings, with exact, plus-one, neutral 100-element, and reduced
      reference-model proofs. RDB-C020 now carries a closed resource with checked actual and maximum
      for every representable known ceiling, while checked arithmetic overflow names the closed
      resource without inventing an actual value. IR validation errors preserve that evidence through
      lowering, and the shared bounded summary remains the source for diagnostics, CLI, MCP, gRPC, and
      LSP rendering. Generated compiler and protocol fixtures remain byte-exact, so no plan, command,
      protocol, durable, runtime, storage, or transaction identity changed.
    known_limitations: This package changes compiler admission and diagnostic evidence only; runtime
      target derivation and durable successor conformance remain owned by WP-684.
    follow_ups: [WP-684]
  human_review_triggers: *id001
- id: WP-684
  name: Correlated index-work runtime and durable successor conformance
  tier: guarantee
  depends_on: [WP-679, WP-683, WP-756, WP-757]
  objective: Carry ADR-0149's admitted index-rich commands through one pay-once canonical
    coordinator derivation and bounded redb transaction, proving atomic 100-element behavior
    against an independent riffdb-testkit authoritative model, the coordinator's sealed set, and
    complete immutable stopped-snapshot redb inspection without weakening any byte, durability,
    recovery, or interface guarantee.
  requirements: [BLK-006, BLK-007, BLK-008, BLK-009, BLK-013, BLK-014,
    BLK-019, BLK-020, BLK-021, BLK-022, BLK-024, BLK-025, BLK-027, ID-001,
    ID-004, STO-001, STO-002, TXN-001, TXN-002, TXN-030, TXN-031, TXN-040,
    TXN-041, TXN-042, TXN-043, TXN-044, SAFE-001, SAFE-004, SAFE-005,
    PERF-004, PERF-005]
  required_adrs: [ADR-0003, ADR-0004, ADR-0005, ADR-0006, ADR-0012,
    ADR-0023, ADR-0038, ADR-0055, ADR-0059, ADR-0104, ADR-0107, ADR-0123,
    ADR-0124, ADR-0129, ADR-0147, ADR-0149, ADR-0180, ADR-0204, ADR-0212]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, proto/**,
    release/version-topology-v1.json, release/durable-format-manifest-v1.json,
    crates/riffdb-types/**, crates/riffdb-contract-ir/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**,
    crates/riffdb-proto/**, crates/riffdb-runtime/**,
    crates/riffdb-service/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, crates/riffdb-testkit/**,
    crates/riffdb-testkit-server/**, crates/riffdb-sim/**,
    fixtures/proto/**, fixtures/contracts/**,
    fixtures/adapters/bulk-conformance/**, tests/**, fuzz/**, scripts/**,
    docs/SUMMARY.md, docs/contracts/**, docs/reference/**,
    docs/architecture/**, docs/operations/**, docs/performance/**]
  deliverables:
  - One bounded coordinator builder that derives candidate index mutations and affected prefixes,
    charges count/work/bytes incrementally, canonicalizes and deduplicates once, and seals the exact
    set reused by validation, epoch reads/advances, reservation, and durable construction
  - Storage semantic bounds separating 4,096 index deltas from 65,535 affected targets and
    validation positions while enforcing D+A+V and every existing 16 MiB semantic/encoded cap in
    redb without scans or partial staging, with the independently derived riffdb-testkit model,
    coordinator's sealed set, and complete inspected redb post-state agreeing exactly
  - Epoch-appropriate command-capsule and segment selection retaining least-sufficient pre-epoch-two
    successors and making only StoredCommandCapsuleV6 and StoredCommandSegmentV5 readable, writable,
    and current for 0..=65,535 transitions at epoch two, with retired identities and old
    descriptor-closure hashes reserved and refused, ADR-0204's exact registry-hash rotations, exact
    V6/V5 message bytes and semantics, bounded decoder refusal, backup, checkpoint, and crash recovery
  - Neutral index-rich 1, 9, 19, and 100 element corpus covering exact/plus-one count, work, byte,
    idempotency, business/unique failure, concurrency, cancellation, rollback, provenance, events,
    audit and changelog order, complete-or-absent visibility, and real riffdbd-plus-redb remote
    loopback, comparing the independent model, captured sealed set, and every semantic target in one
    complete immutable stopped-snapshot redb namespace through strictly ordered canonical pages of
    at most 256 targets with exact terminal exhaustion and every required divergence negative
  - Stage-attributed benchmark evidence for prefix derivation, canonicalization, epoch read,
    reservation, epoch write, capsule encoding, and commit, with guards against quadratic work,
    per-target plan decode, repeated canonical encoding, framework branches, split writes, coarse
    invalidation, or caller budget control
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-commit -p riffdb-proto -p riffdb-testkit -p riffdb-testkit-server -p riffdb-sim --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test bulk_command_recovery --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix --all-features
  - ./scripts/correlated-index-work-qualification
  - ./scripts/adapter-bulk-command-acceptance --all-languages
  - ./scripts/check-durable-format-manifest
  - ./scripts/check-version-topology
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One real neutral 100-element index-rich command atomically commits through redb under
    D+A+V and byte ceilings, uses the epoch-appropriate durable identity, survives every
    scheduled/crash boundary, and exactly agrees across the independent riffdb-testkit model, the
    coordinator's sealed target set, and complete immutable stopped-snapshot redb namespace
    inspection through canonical pages of at most 256 targets with exact terminal enumeration,
    while exposing no framework branch, split, partial result, coarse invalidation, unbounded
    allocation, or repeated plan-proof path.
  standing_design_tests:
    interface_safety: The generated invocation and shared service remain the only application
      boundary; callers cannot choose index targets, validation mode, transaction, work budget,
      durable identity, fallback, split, or partial behavior, and every result retains ordinary
      authorization, idempotency, provenance, audit, durability, and uncertainty semantics.
    scale: D is at most 4,096, A and V at most 65,535, D+A+V at most 65,535, all semantic and
      encoded records retain their byte ceilings, concrete derivation is paid once per attempt,
      storage work is linear in the sealed set, and no path scans data or history.
  human_review_triggers: *id001
- id: WP-474
  name: Sealed entity post-image materialization
  depends_on: [WP-060, WP-065, WP-367, WP-458, WP-459, WP-472]
  objective: Retain the canonical fields and bytes already checked when deterministic evaluation
    creates an EntityPostImage, then reuse that sealed immutable representation when constructing
    the commit-time StoredEntityRecordV1 instead of deep-cloning and re-encoding the same record.
  requirements: [PERF-008, STO-002, TXN-041, TXN-042, TXN-044]
  required_adrs: [ADR-0004, ADR-0006, ADR-0059, ADR-0060, ADR-0061]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-commit/**, tests/command_semantics/**, tests/storage_recovery/**,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - EntityPostImage owns one immutable Arc-backed canonical record and its exact canonical encoded
    bytes, both created together by the existing bounded canonical encoder
  - A narrow checked StoredEntityRecordV1 constructor consumes only a sealed EntityPostImage
    reference, commit-time version, and exact durable schema binding, revalidates the contract
    identity, and shares the immutable record and bytes without re-encoding
  - Ordinary command graph construction uses the sealed constructor; durable decoders, external
    fixtures, recovery, migration, and arbitrary semantic construction retain StoredEntityRecordV1::new
    and its complete canonical encoding, bound, and identity checks
  - Pointer-sharing and byte-equivalence tests prove the sealed and full constructors produce the
    same semantic record and exact durable envelope while malformed or mismatched bindings fail closed
  - No durable schema, envelope, payload, key, hash, record ordering, transaction membership,
    acknowledgement, uncertainty, authorization, or public application/transport behavior changes
  - Full public seed evidence records evaluation, validation/encoding/staging, commit, total seed,
    and representative unary latency, with revert required for a material regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --test lost_response_replay
      --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Evaluation and commit graph construction share one exact canonical entity post-image,
    external and recovered paths retain full validation, all semantic and recovery suites pass, and
    same-host evidence reduces validation/staging CPU or total seed without a material unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-474-sealed-entity-postimage.md
    decision: The sealed Arc-backed post-image path, exact-byte equivalence tests, recovery coverage,
      and retained same-host improvement shipped in f38e57cc and remain green on current main.
  human_review_triggers: *id001
- id: WP-475
  name: Retained canonical permission lookup
  depends_on: [WP-110, WP-378, WP-465]
  objective: Preserve complete fresh command authorization while retaining the exact ordered
    canonical keys already derived for an immutable capability permission set, eliminating one
    candidate-key allocation per binary-search comparison.
  requirements: [PERF-008, AAA-006, AAA-007]
  required_adrs: [ADR-0009, ADR-0060]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-types/**,
    crates/riffdb-policy/**, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - CapabilityPermissionsV1 constructs each exact canonical key once, sorts permission/key pairs by
    those bytes, rejects adjacent equal keys exactly as before, and retains keys in immutable Arc
    storage shared by grant clones
  - Exact permission membership builds only the requested target key and searches retained keys;
    all unparameterized, command, entity, index, projection, query, role, migration, stream, watch,
    and contextual-subscription permission identities retain byte-exact comparison
  - Current authorization still reloads the capability, samples fresh authorization time, validates
    principal/current identity, evaluates tenant/partition/field/approval obligations, and produces
    a new proof at every existing safe point
  - Public grant construction, slices, equality, canonical semantic bytes, durable encoding,
    decoding, redaction, denial taxonomy, and authorization results remain unchanged
  - Full public seed evidence records authorization CPU, total seed, and representative unary
    latency, with revert required for a material regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types --all-features
  - cargo +1.97.0 test -p riffdb-policy --all-features
  - cargo +1.97.0 test -p riffdb-auth --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every authorization result and canonical grant byte remains exact, every fresh safe
    point still reloads and fully evaluates current authority, permission-set clones share retained
    immutable keys, and same-host evidence reduces authorization CPU or total seed without a
    material unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-475-retained-permission-keys.md
    decision: Immutable retained permission keys shipped in aee0f8f9 with complete fresh-safe-point
      authorization intact and a retained same-host CPU and latency improvement.
  human_review_triggers: *id001
- id: WP-476
  name: Constant-time durable shape dispatch
  depends_on: [WP-020, WP-065, WP-459]
  objective: Preserve the complete allocation-free durable Protobuf structural preflight while
    replacing its per-field linear rule scan with one compile-time-checked direct dispatch table
    for each closed durable shape.
  requirements: [PERF-008, STO-002, STO-020, STO-021]
  required_adrs: [ADR-0006, ADR-0060]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-proto/**,
    tests/storage_recovery/**, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Every closed durable shape constructs a fixed direct field-number dispatch table at compile
    time, rejecting zero, out-of-range, duplicate, or over-limit internal rule declarations
  - Structural preflight performs one bounded table lookup for each parsed field while retaining
    the same allocation-free cursor, recursion depth, visited-field budget, packed-item budget,
    occurrence counts, wire-type checks, byte bounds, UTF-8 checks, and nested validation
  - Unknown field numbers, including numbers beyond the closed dispatch ceiling, remain ignored by
    structural preflight and are still rejected by the existing exact durable re-encode boundary
  - A recursive registry test compares direct dispatch with the prior linear rule definition for
    every field number and every root or nested durable shape
  - Malformed, noncanonical, and resource-limit errors retain their exact classification; durable
    schemas, descriptors, hashes, envelope bytes, record bytes, keys, and public interfaces do not change
  - Full public seed evidence records total seed and representative unary latency, with revert
    required for a material regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every durable structural and semantic result remains exact, the direct table is proven
    equivalent to every closed rule set, all malformed and recovery suites pass, and same-host
    evidence reduces durable preflight CPU or total seed without a material unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-476-durable-shape-dispatch.md
    decision: Compile-time durable-shape dispatch shipped in 0c39ad86; exhaustive equivalence,
      malformed-record, canonicality, and recovery tests remain green with retained improvement.
  human_review_triggers: *id001
- id: WP-477
  name: Canonical command capsule durable layout
  depends_on: [WP-065, WP-070, WP-362, WP-372, WP-373, WP-374, WP-409,
    WP-459, WP-470, WP-472, WP-473, WP-476]
  objective: Replace duplicated successful-command payloads in the commit, idempotency,
    provenance, and command-audit tables with one canonical sequence-keyed capsule and compact
    self-verifying locators, including a bounded restartable offline migration, without changing
    application semantics, lookup keys, atomicity, durability, recovery, or fail-closed validation.
  requirements: [STO-002, STO-020, STO-021, STO-022, REC-002, TXN-041,
    TXN-042, TXN-043, TXN-044, PERF-008, PERF-009, PERF-010, PERF-016]
  required_adrs: [ADR-0004, ADR-0005, ADR-0006, ADR-0007, ADR-0009,
    ADR-0021, ADR-0058, ADR-0060, ADR-0061, ADR-0082, ADR-0083, ADR-0085,
    ADR-0098, ADR-0099]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0099-canonical-command-capsules.md,
    proto/riffdb/storage/v1/**, crates/riffdb-proto/**, fixtures/proto/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-storage-memory/**, crates/riffdb-commit/**, crates/riffdb-server/**,
    tests/command_semantics/**, tests/recovery/**, tests/storage_recovery/**,
    benchmarks/command-growth/**, benchmarks/run-app-baseline,
    examples/app-baseline/**, scripts/generate-proto*, scripts/check-generated,
    docs/SUMMARY.md, docs/architecture/**, docs/reference/**, docs/performance/**]
  deliverables:
  - Exact StoredCommandCapsuleV1, StoredCommandLocatorV1, and
    StoredCommandAuditLocatorV1 schemas, closed registry entries, canonical codecs, bounds,
    semantic constructors, redacted debug, and immutable compatibility fixtures
  - Capsule construction proves byte-for-byte semantic reconstruction of the current commit,
    outcome, provenance, Started audit, and terminal audit views while storing each shared fact once
  - Successful command writes atomically stage unchanged entity/index/generation/event/route/outbox
    authority, one sequence-keyed capsule, exact-key idempotency/provenance/audit locators, request
    indexes, and allocators through the direct and deferred-tail paths
  - Every outcome, provenance, commit, audit, scan, replay, uncertainty, projection, event,
    statistics, backup, and recovery read follows locators within one snapshot and validates the
    complete requested key/view reciprocity before release
  - Pending and execution-failure rows and non-command audit rows retain their complete current
    schemas; no public service, application type, query result, protocol byte, or generated SDK changes
  - An offline migration processes at most 500 commands and 4 MiB per durable page, validates the
    complete predecessor graph before replacement, tolerates only internally complete mixed rows,
    resumes after interruption, and publishes the successor registry only after a complete validation
  - Startup, migration, corruption, uncertain commit, response-loss, retention, checkpoint,
    backup/restore, and old-fixture tests cover absent, substituted, reordered, cross-role, malformed,
    partial-page, pre-publication, and post-publication states
  - Full seed and c1/c32/c128 evidence records staging, commit/fence, physical groups, process write
    bytes, durable growth, lookup latency, correctness, and PostgreSQL ratios; retain only on a
    material write/seed improvement without a material unary/read regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --test lost_response_replay
      --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./scripts/generate-proto --check
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every successful command has one canonical capsule and only checked auxiliary
    locators, all predecessor databases migrate restartably with complete semantic equivalence and
    fail-closed recovery, every public behavior remains exact, and retained evidence materially
    reduces full-seed/write cost without material unary or read regression.
  closure:
    status: complete
    evidence: docs/performance/wp-477-command-capsules.md
    decision: Canonical capsules, checked locators, restartable predecessor migration, and complete
      public-view reconstruction shipped and remain covered by current semantic and recovery suites.
  human_review_triggers: *id001
- id: WP-478
  name: Pipelined standard-profile writer journal
  depends_on: [WP-362, WP-364, WP-452, WP-459, WP-466, WP-467, WP-469,
    WP-470, WP-472, WP-473, WP-476]
  objective: Implement ADR-0101's bounded first-party typed writer-frame journal so the sole ordered
    standard-profile writer can continue applying complete private command and standalone service-audit groups while prior frames
    flush, then publish one exact durable snapshot and independent command results without weakening
    crash, idempotency, atomicity, visibility, or hardened-profile semantics.
  requirements: [PERF-004, PERF-005, PERF-007, PERF-009, PERF-010, PERF-015,
    PERF-017, REC-001, REC-002, TXN-041, TXN-042, TXN-043, TXN-044]
  required_adrs: [ADR-0004, ADR-0005, ADR-0006, ADR-0007, ADR-0009,
    ADR-0021, ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0082, ADR-0083,
    ADR-0085, ADR-0098, ADR-0101]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0101-pipelined-standard-durability-journal.md,
    proto/riffdb/storage/v1/**, crates/riffdb-proto/**, fixtures/proto/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-storage-memory/**, crates/riffdb-catalog/**, crates/riffdb-commit/**,
    crates/riffdb-server/**, tests/command_semantics/**, tests/recovery/**,
    tests/storage_recovery/**, benchmarks/command-growth/**,
    benchmarks/run-app-baseline, examples/app-baseline/**,
    scripts/generate-proto*, scripts/check-generated, docs/SUMMARY.md,
    docs/architecture/**, docs/reference/**, docs/performance/**]
  deliverables:
  - A closed versioned bounded length-delimited checksummed hash-chained frame format carries complete
    canonical command graphs or validated standalone service-audit groups, exact database incarnation, dual predecessor/frontier sequences, and
    compatibility fixtures; every malformed, torn, substituted, reordered, or cross-database frame
    fails closed except an incomplete terminal tail
  - Standard-profile contention applies complete FIFO command and standalone service-audit subgroups through redb Durability::None,
    captures each exact post-subgroup snapshot, and submits immutable frames to a dedicated bounded
    journal lane while the sole ordered writer may continue; idle singletons remain direct one-phase
    Immediate and hardened groups remain direct two-phase Immediate
  - The writer drains every older unpublished command fence before executing a later unit with an
    overlapping compiler-derived conflict key; incompatible completion groups never retain leases
    while waiting on a fence that only the same writer can publish
  - The journal lane adds no fixed batching delay, drains only a ready FIFO prefix, performs one
    sync_data for at most 256 logical writer transitions and 16 MiB, preserves independent command and audit group limits, never permits more than those bounds to remain unpublished, and retains at most a byte-aware 8,192-transition/32-MiB complete published-plus-checkpoint-in-flight recovery suffix before redb checkpoint reclamation
    applied but unpublished process-wide
  - A successful journal fence atomically publishes the last covered redb snapshot before releasing
    any command or audit result, notification, transient index, projection, outbox, subscription, or response; every
    covered command retains independent identity, sequence, outcome, retry, audit, provenance, event,
    and uncertainty semantics
  - Append or fence errors retain the predecessor dual frontier, invalidate unpublished accelerators,
    release no affected result, and fence command and audit writes for exact restart resolution
  - Startup validates the checkpoint-plus-suffix relation, replays complete frames without command
    reevaluation, checkpoints before readiness, and supports crash-safe journal reclamation, backup,
    restore, and shutdown drains
  - Deterministic and process-level tests cover kills before append, during write, during sync, after
    sync before publication, after publication before response, during checkpoint, and during
    reclamation, proving previous-or-complete recovery and no unfenced visibility
  - Same-run c1/c32/c128 public comparisons record journal batch sizes, flush count/time, applied and
    published frontier lag, write latency, throughput, process bytes, durable growth, and correctness;
    retain only if c1 has no material regression and mean RiffDB throughput exceeds PostgreSQL safe-app
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --test lost_response_replay
      --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./scripts/generate-proto --check
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every acknowledged standard-profile command and standalone service-audit append is recoverable from exactly one validated
    checkpoint-plus-journal prefix, applied roots never outrun public durability, all crash and
    semantic suites pass, idle and hardened paths remain exact, and retained same-run evidence shows
    RiffDB exceeding PostgreSQL safe-app mean throughput without a material c1 regression.
  closure:
    status: complete
    evidence: docs/performance/wp-474-493-closure.md
    decision: The bounded journal format and recovery path shipped before ADR-0104 production
      activation; current crash/recovery suites and the retained historical activation comparison
      satisfy the package gate without making a current alpha-release claim.
  human_review_triggers: *id001
- id: WP-479
  name: Segmented command authority and rebuildable exact indexes
  depends_on: [WP-477, WP-478]
  objective: Replace per-command capsule locator, immutable event-route, outbox-intent, and repeated
    generation rows with bounded contiguous command/event segments, exact readiness-gated derived
    indexes, and one final physical generation per target while preserving every typed command,
    idempotency, audit, provenance, event, outbox, retention, and crash guarantee.
  requirements: [STO-002, STO-020, STO-021, STO-022, REC-001, REC-002,
    TXN-041, TXN-042, TXN-043, TXN-044, PERF-008, PERF-009, PERF-010,
    PERF-016, PERF-017]
  required_adrs: [ADR-0004, ADR-0005, ADR-0006, ADR-0007, ADR-0009,
    ADR-0021, ADR-0058, ADR-0060, ADR-0061, ADR-0080, ADR-0082, ADR-0083,
    ADR-0085, ADR-0098, ADR-0099, ADR-0101, ADR-0102]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0099-canonical-command-capsules.md,
    adr/0101-pipelined-standard-durability-journal.md,
    adr/0102-segmented-command-authority-and-derived-locators.md,
    proto/riffdb/storage/v1/**, crates/riffdb-proto/**, fixtures/proto/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-storage-memory/**, crates/riffdb-commit/**,
    crates/riffdb-server/**, tests/command_semantics/**, tests/recovery/**,
    tests/storage_recovery/**, benchmarks/command-growth/**,
    benchmarks/run-app-baseline, examples/app-baseline/**,
    scripts/generate-proto*, scripts/check-generated, docs/SUMMARY.md,
    docs/architecture/**, docs/reference/**, docs/performance/**]
  deliverables:
  - Closed StoredCommandSegmentV1, StoredCommandCapsuleV2, segment-manifest, and derived-index
    checkpoint schemas with canonical codecs, exact bounds, compatibility fixtures, and an offline
    restartable ADR-0099 migration
  - One segment owns every successful outcome, provenance, commit, command audit, immutable event,
    event route, outbox intent, and logical generation advance for at most 256 contiguous commands
    and 16 MiB; entity/index post-images and independently mutable state remain separate authority
  - Idempotency, provenance, audit sequence/request, event route, and pending-outbox indexes rebuild
    exactly from validated manifests before readiness, update only after durability, reject every
    duplicate/missing/substituted member, and use a digest-bound checkpoint plus verified suffix
  - Transaction-local generation overlays prove every per-command prior/successor chain while redb
    receives one final post-image per distinct target; journal recovery proves and applies the same
    final state without changing snapshot or conflict semantics
  - Point lookup, scans, replay, uncertainty recovery, retention, backup/restore, projection, event
    consumers, live queries, and outbox delivery reconstruct the current semantic types and fail
    closed under the same snapshot frontier
  - Retention handles whole and split segments, materializes cold views required beyond history,
    rebuilds derived indexes, and preserves exact tombstone/checkpoint semantics
  - Differential, deterministic-schedule, corruption, migration, and process-kill tests cover every
    segment boundary, manifest/index checkpoint, generation overlay, journal frame, retention split,
    and response-loss state
  - Same-run c1/c8/c32/c128 tri-backend evidence records correctness, throughput, latency, physical
    row/page work, durable growth, rebuild/startup cost, and proves RiffDB mean throughput exceeds
    PostgreSQL safe-app without a material unary/read regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --test lost_response_replay
      --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./scripts/generate-proto --check
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public and recovery semantic remains exact and fail closed, every retained database
    migrates restartably, derived indexes are positively proven complete before use, and retained
    same-run evidence shows RiffDB exceeding PostgreSQL safe-app mean throughput across the accepted
    client sweep without material c1, read, startup, or durable-growth regression.
  closure:
    status: complete
    evidence: docs/performance/wp-479-segmented-command-authority.md
    decision: Segmented authority, readiness-gated derived indexes, generation overlays, and
      restartable migration shipped; later overlay activation evidence supplies the completed public
      comparison while current semantic and recovery suites retain exactness.
  human_review_triggers: *id001
- id: WP-480
  name: Preallocated recyclable durability journal
  depends_on: [WP-479]
  objective: Replace hot-path extending journal appends with a pre-zeroed, fixed-capacity,
    positionally written and safely recyclable extent format only when same-device mechanics prove
    a material synchronous-fence advantage, while preserving acknowledgement, recovery, backup,
    publication, and replication boundaries.
  requirements: [REC-001, REC-002, TXN-041, TXN-042, TXN-043, TXN-044, PERF-004,
    PERF-005, PERF-009, PERF-015, PERF-017]
  required_adrs: [ADR-0005, ADR-0006, ADR-0009, ADR-0058, ADR-0061, ADR-0098,
    ADR-0100, ADR-0101, ADR-0103]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0101-pipelined-standard-durability-journal.md,
    adr/0103-preallocated-recyclable-durability-journal.md,
    benchmarks/journal-mechanics/**, benchmarks/run-app-baseline,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-server/**,
    tests/storage_recovery/**, tests/backup_restore/**,
    examples/app-baseline/**, docs/SUMMARY.md, docs/architecture/**,
    docs/operations/**, docs/performance/**]
  deliverables:
  - A same-filesystem mechanics probe compares extending append plus fdatasync with pre-zeroed
    positional overwrite plus fdatasync and O_DSYNC across bounded frame sizes and alignment; the
    format proceeds only if retained evidence shows at least a twofold fence-latency improvement
  - Fixed-capacity journal extents are fully allocated and zero-filled before readiness, so hot-path
    fences neither extend files nor convert unwritten extents; insufficient capacity fails during
    preflight rather than after command acceptance
  - Every extent and frame binds database incarnation, extent generation, physical position, logical
    predecessor/frontiers, and checksum; recovery treats a generation mismatch as end-of-log and
    rejects every same-generation gap, substitution, or mixed-position frame
  - Process-crash coverage includes a frame torn over stale residue, crash during extent recycle,
    mixed-generation residue, fence completion, checkpoint, and reuse; recovery observes only the
    prior frontier or the complete next durable prefix
  - Activation requires an empty legacy journal after a clean checkpoint; old-format nonempty
    journals fail with a typed path-bearing operator error, and no dual-format inference occurs
  - Backup and restore include the complete extent inventory with the checkpoint as one verified
    unit; replication and changelog contracts remain derived from published frontiers and do not
    depend on physical journal bytes
  - Same-run c8/c32/c128 public evidence records correctness, throughput, write latency, fence
    latency, and establishes whether remaining cost is durability, per-command CPU, or redb apply
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path benchmarks/journal-mechanics/Cargo.toml
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The mechanics threshold is met before format implementation; every journal generation,
    stale-residue, recycle, upgrade, ENOSPC, backup, and crash test passes; acknowledged state and
    published-frontier semantics remain exact; and c8/c32/c128 evidence identifies the post-journal
    performance frontier without a material c1 or read regression.
  closure:
    status: complete
    evidence: docs/performance/wp-480-journal-mechanics.md
    decision: The 5.09-times mechanics threshold justified activation; the generation-bound recycled
      format, recovery and backup coverage, and retained public-path checkpoint remain current.
  human_review_triggers: *id001
- id: WP-481
  name: Constant-time snapshot frontiers and concurrent named-query plan fanout
  depends_on: [WP-480]
  objective: Remove the post-segmentation read-path CPU and lock amplification exposed at 32 and
    128 clients without weakening snapshot identity, catalog selection, query authorization,
    bounded execution, or corruption handling.
  requirements: [PERF-001, PERF-002]
  required_adrs: [ADR-0009, ADR-0056, ADR-0060, ADR-0085, ADR-0099, ADR-0102]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-redb/src/reads.rs,
    crates/riffdb-storage-redb/src/query.rs, crates/riffdb-storage-redb/src/command_authority.rs,
    crates/riffdb-storage-redb/tests/**, crates/riffdb-server/src/read_adapters.rs,
    crates/riffdb-server/src/storage.rs, examples/app-baseline/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Query and snapshot frontiers derive in O(1) from the application sequence allocator observed in
    the same redb read transaction as entity/index state; empty, next, and exhausted states remain
    exact, gross allocator/authority presence mismatch fails closed, and commit scans still prove
    every requested logical segment member
  - The hot query path no longer decodes or allocates the complete last command segment merely to
    obtain its logical tail; startup, recovery, retention, projection scans, and explicit commit
    reads retain their complete segment integrity proofs
  - Warm immutable query-module plans admit concurrent cache readers without a mutually exclusive
    cache-hit lock or blocking-pool fallback; cold compilation and publication remain bounded,
    exact-hash keyed, and admission checked before any cache answer
  - Deterministic tests prove grouped-segment snapshot frontiers, allocator/authority mismatch,
    exhausted-head behavior, concurrent warm-plan fanout, cold single-publication behavior, and
    stopped-routing refusal
  - Fresh c1/c32/c128 public-path evidence decomposes plan lookup and execution CPU, RSS, read p50,
    write p50, correctness, and throughput against WP-480 and PostgreSQL safe-app
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: At c128, named-query plan lookup no longer grows through blocking-pool contention,
    snapshot-head work is independent of retained segment size, all operations remain error-free,
    and the public-path throughput/read-latency result materially improves over WP-480 without a
    material c1 or write regression.
  closure:
    status: complete
    evidence: docs/performance/wp-481-constant-time-query-frontiers.md
    decision: Constant-time frontier discovery and concurrent immutable plan lookup shipped with
      exact snapshot semantics and retained c1/c32/c128 evidence; current read and recovery suites pass.
  human_review_triggers: *id001
- id: WP-482
  name: Streaming command-segment sealing and complete serial writer evidence
  depends_on: [WP-481]
  objective: Remove whole-segment Protobuf graph materialization from the successful-command hot
    path and close the transaction-local serial-path telemetry blind spot without changing durable
    bytes, command ordering, validation, acknowledgement, recovery, or public behavior.
  requirements: [PERF-004, PERF-005, PERF-016, PERF-017]
  required_adrs: [ADR-0060, ADR-0099, ADR-0101, ADR-0102, ADR-0103]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/src/proto_codec/command_segment.rs,
    crates/riffdb-storage-api/src/proto_codec/tests/**,
    crates/riffdb-commit/src/command_execution.rs, crates/riffdb-commit/src/telemetry.rs,
    crates/riffdb-observability/src/**, examples/app-baseline/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - The authoritative command-segment seal writes canonical header fields, capsules, manifest
    entries, manifest digest, and segment digest incrementally into bounded buffers; at most one
    capsule wire graph exists at a time and manifest exact keys are borrowed rather than cloned
  - Streaming seal output is byte-identical to the independent generated-Protobuf encoder for
    empty/nonempty predecessor digests, multiple commands, nonzero ordinals, events, generation
    transitions, and maximum bounded manifests; decode and corruption behavior are unchanged
  - Transaction-local serial groups emit exactly one aggregate evaluation observation and one
    validation/encoding/staging observation on the successful path, so their elapsed time and
    command counts can be reconciled with writer busy time without retaining application values
  - Fresh write-only and interactive c32 evidence reports command CPU decomposition, writer busy
    time, write p50, throughput, RSS, and durable-byte growth against WP-481
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Durable bytes and all semantic/crash tests remain exact, serial stage counts cover the
    successful transaction-local command population, and the streaming encoder materially reduces
    writer CPU or public write latency without a read, c1, memory, or retained-byte regression.
  closure:
    status: complete
    evidence: docs/performance/wp-482-streaming-command-segments.md
    decision: Streaming segment sealing and complete serial-stage observations shipped with exact
      byte-equality tests and retained improvements in memory, durable growth, and throughput.
  human_review_triggers: *id001
- id: WP-483
  name: Fully streaming command-capsule sealing
  depends_on: [WP-482]
  objective: Remove the remaining whole-capsule Protobuf object graph and repeated-field vectors
    from successful-command segment sealing while preserving the exact durable bytes, hashes,
    bounds, recovery behavior, and independent generated-Protobuf validation path.
  requirements: [PERF-004, PERF-005, PERF-008, PERF-016]
  required_adrs: [ADR-0060, ADR-0099, ADR-0101, ADR-0102]
  allowed_paths: [work_packages.yaml,
    crates/riffdb-storage-api/src/proto_codec/command_capsule.rs,
    crates/riffdb-storage-api/src/proto_codec/command_segment.rs,
    crates/riffdb-storage-api/src/proto_codec/tests/**,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Segment sealing writes each command-capsule field, repeated reference, event, generation
    transition, and audit submessage in canonical field order without materializing the complete
    V1 or V2 generated-Protobuf graph
  - Fixed bytes and repeated scalar bytes are borrowed directly; at most one bounded nested
    generated message is materialized at a time and is dropped after insertion
  - Independent generated-Protobuf encoding remains byte-identical for multi-command,
    predecessor, event, transition, audit, optional, and large-manifest cases
  - Fresh c32 write-only and interactive evidence compares throughput, writer stages, write p50,
    peak RSS, and durable growth with WP-482
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Durable bytes and recovery remain exact, the authoritative seal retains no complete
    capsule wire graph, and c32 evidence shows lower encoding/staging cost or memory without a
    public latency, retained-byte, or correctness regression.
  closure:
    status: complete
    evidence: docs/performance/wp-483-streaming-command-capsules.md
    decision: Fully streaming capsule encoding shipped with independent generated encoding as the
      byte oracle; current recovery tests pass and retained controlled write evidence did not regress.
  human_review_triggers: *id001
- id: WP-484
  name: Proven command-segment envelope framing
  depends_on: [WP-483]
  objective: Remove the redundant generated durable-wire structural walk after the checked typed
    command-segment pipeline has already emitted canonical bounded payload bytes, while retaining
    exact compact framing, checksum, independent encoding, recovery decoding, and corruption
    rejection.
  requirements: [STO-002, STO-020, STO-021, STO-022, PERF-004, PERF-008, PERF-016, PERF-017]
  required_adrs: [ADR-0006, ADR-0060, ADR-0099, ADR-0101, ADR-0102]
  allowed_paths: [work_packages.yaml,
    crates/riffdb-proto/src/durable.rs,
    crates/riffdb-proto/src/envelope.rs,
    crates/riffdb-storage-api/src/proto_codec/mod.rs,
    crates/riffdb-storage-api/src/proto_codec/command_segment.rs,
    crates/riffdb-storage-api/src/proto_codec/tests/**,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - A narrowly named first-party framing entry point accepts only current-schema payloads whose
    caller has already proved the same canonical shape and bound normally established by durable
    preflight; it still checks the registered compact identity, exact payload bound, and CRC-32C
  - Only the authoritative checked command-segment seal uses the proven path; every general
    durable encoder, external/recovered byte path, independent command-segment encoder, and
    decoder retains generated structural preflight and complete semantic validation
  - Tests prove proven framing is byte-identical to ordinary current-schema framing for rich and
    maximum command segments, rejects an oversized payload, and leaves malformed/corrupt segment
    rejection unchanged
  - Fresh c32 write-only and interactive evidence compares throughput, command pipeline stages,
    write p50, RSS, retained bytes, and failures with WP-483
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Canonical command-segment bytes, checksums, bounds, recovery, and corruption behavior
    remain exact, no untyped or compatibility path can select the proven framing shortcut, and
    fresh evidence shows lower writer CPU or public write latency without a memory, retained-byte,
    read-path, c1, or correctness regression.
  closure:
    status: complete
    evidence: docs/performance/wp-484-proven-command-segment-framing.md
    decision: The typed proven-framing shortcut shipped only on the sealed segment path, with exact
      byte/bound equivalence and a retained public-path improvement.
  human_review_triggers: *id001
- id: WP-485
  name: State-bearing command-segment mechanics proof
  depends_on: [WP-484]
  objective: Determine with benchmark-only redb mechanics whether making one bounded command
    segment own entity post-images and secondary-index transitions can remove the remaining
    authoritative B-tree write amplification without changing production storage or read semantics.
  requirements: [STO-002, STO-020, STO-021, STO-022, PERF-004, PERF-005, PERF-008, PERF-016,
    PERF-017]
  required_adrs: [ADR-0006, ADR-0060, ADR-0099, ADR-0101, ADR-0102, ADR-0103]
  allowed_paths: [work_packages.yaml,
    crates/riffdb-storage-redb/src/benchmark_support.rs,
    benchmarks/command-growth/**,
    adr/README.md, adr/0104-*.md,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - A benchmark-only projection compares the exact current segmented-command table shape with a
    proposed state-bearing segment that also contains complete entity post-images and
    secondary-index transitions; only independently mutable rows remain separate
  - The probe covers group sizes 1, 32, 128, and 256 and distinct-create, retained-update, and
    mixed create/update workloads without allowing the proposed projection into production code
  - Evidence reports median table/page work, elapsed time, row/page inventory, stored bytes, and
    database bytes for both projections, with a predeclared two-times table-work gate at group
    sizes 32 and 128
  - If and only if the gate passes, a Proposed ADR-0104 and follow-on work packages define the
    exact state authority, rebuildable current-view indexes, checkpoint/recovery bounds,
    compaction, migration, corruption, backup, and read-validation rules for human exact-text
    review
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --manifest-path benchmarks/command-growth/Cargo.toml
  - cargo +1.97.0 run --release --manifest-path benchmarks/command-growth/Cargo.toml
    --bin state-segment-projection
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Production storage and reads are unchanged, the evidence is reproducible and labels
    the proposed layout benchmark-only, and ADR-0104 is absent after a failed gate or remains
    Proposed pending exact-text human review after a passed gate.
  closure:
    status: complete_negative_mechanics_result
    evidence: docs/performance/wp-485-state-bearing-segment-mechanics.md
    decision: The predeclared two-times mechanics gate failed, so the state-bearing segment was
      correctly rejected without changing production storage or read authority.
  human_review_triggers: *id001
- id: WP-486
  name: Journal-authoritative overlay mechanics and exact design
  depends_on: [WP-485]
  objective: Prove whether publishing a bounded immutable current-state overlay directly from the
    already-authoritative durability-journal suffix can remove synchronous redb apply amplification,
    and define the exact checkpoint, read-merge, recovery, backup, and backpressure semantics before
    any production authority or read-validation path changes.
  requirements: [STO-001, STO-002, REC-001, REC-002, TXN-040, TXN-041, TXN-042,
    TXN-043, TXN-044, PERF-004, PERF-005, PERF-007, PERF-008, PERF-009, PERF-010,
    PERF-015, PERF-016, PERF-017]
  required_adrs: [ADR-0004, ADR-0005, ADR-0006, ADR-0035, ADR-0053, ADR-0058,
    ADR-0060, ADR-0061, ADR-0070, ADR-0082, ADR-0098, ADR-0101, ADR-0102,
    ADR-0103]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0104-journal-authoritative-state-overlay.md,
    crates/riffdb-storage-redb/src/benchmark_support.rs,
    benchmarks/command-growth/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - A benchmark-only mechanics driver compares the current apply-to-redb-before-fence path with
    exact journal-mutation construction plus a bounded immutable overlay for distinct creates,
    retained updates, mixed mutations, point reads, and canonical index-page reads
  - Evidence separates journal-common work, redb begin/stage/non-durable commit work, overlay
    construction/publication work, merged-read overhead, process writes, retained bytes, and the
    bounded checkpoint cost at suffix sizes 32, 128, 256, and 4,096 transitions
  - The production proposal proceeds only if median pre-acknowledgement apply work is at least two
    times lower at 32 and 128 transitions, merged point and bounded-index reads remain within 1.25
    times the checkpoint-only control, and one bounded checkpoint clears the maximum suffix
  - Proposed ADR-0104 defines one frozen read view as a durable redb checkpoint plus an immutable
    gap-free journal overlay, exact tombstone/newest-value merge rules, FIFO writer-private state,
    acknowledgement after the journal fence, asynchronous ordered checkpointing, suffix
    backpressure, reclamation, recovery, backup, hardened-profile isolation, and every corrupt or
    uncertain state; the record remains Proposed pending exact-text human review
  - No production adapter can select the benchmark overlay, no accepted ADR is silently amended,
    and current storage, query, command, recovery, backup, and public behavior remain unchanged
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --manifest-path benchmarks/command-growth/Cargo.toml
  - cargo +1.97.0 run --release --manifest-path benchmarks/command-growth/Cargo.toml
    --bin journal-state-overlay
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Production behavior is unchanged; reproducible mechanics either reject the proposal or
    meet every apply, read, and checkpoint threshold; and ADR-0104 remains Proposed until the human
    maintainer accepts its exact authority, ordering, recovery, and read-validation text.
  closure:
    status: complete
    evidence: docs/performance/wp-486-journal-state-overlay.md
    decision: The overlay mechanics met every predeclared threshold and produced ADR-0104, whose
      exact authority, ordering, recovery, and read-validation text was subsequently accepted.
  human_review_triggers: *id001
- id: WP-487
  name: Frozen checkpoint-plus-overlay read views
  depends_on: [WP-486]
  objective: Implement the accepted ADR-0104 composite read-view and exact journal-mutation overlay
    primitives without yet selecting them for production command acknowledgement.
  requirements: [STO-001, STO-002, TXN-040, PERF-007, PERF-008, PERF-012,
    PERF-015, PERF-017]
  required_adrs: [ADR-0004, ADR-0035, ADR-0053, ADR-0070, ADR-0101, ADR-0102,
    ADR-0103, ADR-0104]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0104-journal-authoritative-state-overlay.md,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-storage-memory/**, tests/storage_conformance/**,
    docs/SUMMARY.md, docs/architecture/**, docs/performance/**]
  deliverables:
  - "`PERF-015` and `PERF-017` encode the exact accepted composite-view, idle-singleton, and
    deterministic overlay-recovery amendments without weakening their existing bounds or fences"
  - A closed immutable view binds one redb checkpoint root, one exact ordered overlay, checkpoint
    and published dual frontiers, database/history/registry identity, and terminal frame hash
  - Point reads implement overlay value, overlay tombstone, and checkpoint fallback; bounded scans
    merge canonical keys without materializing an unbounded union and observe the index generation
    from the same view
  - Overlay mutation application validates table membership, canonical bytes, expected prior hash,
    put absence, tombstones, allocators, segment manifests, generations, frontiers, and complete
    journal-frame order before a view can be frozen
  - Memory/redb conformance and deterministic schedules prove snapshot consistency across overlay
    publication and checkpoint compaction, cursor invalidation only on logical generation change,
    and fail-closed corruption handling
  - The production command path remains on apply-to-redb-before-fence until WP-488
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --test composite_view_conformance
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Exact-text ADR-0104 is Accepted before implementation; composite view construction and
    reads pass semantic parity, boundedness, corruption, and concurrency tests; and no production
    acknowledgement path selects the overlay.
  closure:
    status: complete
    evidence: docs/performance/wp-474-493-closure.md
    decision: Accepted ADR-0104's immutable composite view shipped in 5156e86c and 864ec779; the
      focused current-head conformance suite passes and production selection remained deferred to WP-488.
  human_review_triggers: *id001
- id: WP-488
  name: Frame-first standard writer and overlay publication
  depends_on: [WP-487]
  objective: Remove synchronous redb apply from every standard-profile command and service-audit
    acknowledgement while preserving sole-writer FIFO evaluation, transaction-current validation,
    atomic command graphs, independent outcomes, and journal-fence publication.
  requirements: [REC-001, REC-002, TXN-040, TXN-041, TXN-042, TXN-043,
    TXN-044, PERF-004, PERF-005, PERF-008, PERF-009, PERF-010, PERF-015,
    PERF-017]
  required_adrs: [ADR-0003, ADR-0005, ADR-0058, ADR-0059, ADR-0060, ADR-0061,
    ADR-0094, ADR-0095, ADR-0096, ADR-0098, ADR-0101, ADR-0102, ADR-0103,
    ADR-0104]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-storage-memory/**,
    crates/riffdb-commit/**, crates/riffdb-observability/**,
    tests/storage_recovery/**, tests/server_composition/**,
    examples/app-baseline/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/architecture/**, docs/performance/**]
  deliverables:
  - Standard command staging constructs, validates, charges, and applies exact journal mutations to
    the writer-private overlay without opening a redb write transaction; hardened staging remains
    the independent two-phase redb oracle
  - FIFO successors evaluate and transaction-current validate against every complete private
    predecessor, with conflict authority transfer and disjoint same-snapshot proofs unchanged
  - A known journal fence atomically publishes the complete covered composite view before any
    result, audit, transient index, event, projection, outbox, subscription, or response release
  - Idle singletons use one immediate journal submission with no batching wait or redb apply;
    grouped commands retain all 256-transition, 16-MiB, scheduler, and independent-identity bounds
  - Standalone service-audit frames use the same overlay publication order; every redb-writing
    control-plane or lifecycle message remains a hard drain/checkpoint barrier
  - Deterministic concurrency and failpoint tests cover frame construction failure, overlay
    capacity, same-identity races, overlapping FIFO successors, fence failure/uncertainty, stale
    publication, cancellation, and result withholding
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - cargo +1.97.0 test --test server_composition
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Standard acknowledgements perform no synchronous redb apply, hardened behavior is
    unchanged, all semantic/concurrency/failure suites pass, and focused c1/c32 write evidence
    materially improves without read or correctness regression.
  closure:
    status: complete
    evidence: docs/performance/wp-474-493-closure.md
    decision: Frame-first standard staging and ordered overlay publication shipped through
      790041aa, fa1e2896, and the singleton/publish follow-ups; hardened semantics remain the oracle.
  human_review_triggers: *id001
- id: WP-489
  name: Ordered asynchronous checkpoint, recovery, and maintenance barriers
  depends_on: [WP-488]
  objective: Make acknowledged overlay state operationally complete through bounded asynchronous
    redb checkpoints, deterministic restart rebuild, crash-safe reclamation, backup/restore, and
    exact redb-writing barriers.
  requirements: [STO-001, STO-002, REC-001, REC-002, TXN-043, TXN-044,
    PERF-007, PERF-009, PERF-015, PERF-017]
  required_adrs: [ADR-0006, ADR-0050, ADR-0061, ADR-0070, ADR-0082, ADR-0085,
    ADR-0098, ADR-0101, ADR-0102, ADR-0103, ADR-0104]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-server/**,
    tests/storage_recovery/**, tests/backup_restore/**,
    tests/server_composition/**, examples/app-baseline/**,
    benchmarks/run-app-baseline, docs/SUMMARY.md, docs/architecture/**,
    docs/operations/**, docs/performance/**]
  deliverables:
  - One bounded checkpointer applies a captured published suffix in exact writer order, validates
    every expected prior and frontier, commits one Immediate redb checkpoint, and compacts only the
    proven covered overlay prefix while retaining concurrent newer entries
  - Checkpoint admission starts at 4,096 transitions or 16 MiB and before the physical extent reserve;
    the complete published-plus-checkpoint-in-flight suffix remains below 8,192 transitions and
    32 MiB, the overlay remains below 128 MiB, and insufficient headroom backpressures writes while
    reads remain live
  - Standard restart deterministically validates checkpoint plus suffix, rebuilds the exact overlay,
    reaches readiness without a mandatory redb rewrite, and starts the ordinary checkpointer
  - Redb-writing barriers drain unpublished fences and checkpoint the complete published suffix
    before their own transaction; checkpoint failure or uncertainty prevents barrier execution
  - Backup/restore preserve the checkpoint-plus-extent unit, and reclamation/recycle occurs only
    after a successor checkpoint and compacted view independently prove recovery sufficiency
  - Process crash arms cover every ADR-0104 boundary, including checkpoint apply/commit,
    compacted-view publication, concurrent suffix growth, barrier drain, backup, reclamation, and
    repeated restart
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - cargo +1.97.0 test --test backup_restore
  - cargo +1.97.0 test --test server_composition
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every acknowledged command survives all crash arms, every reconstructed view is exact,
    checkpoints and barriers remain bounded and fail closed, backup/restore is complete, and no
    accepted suffix can be lost or exposed partially.
  closure:
    status: complete
    evidence: docs/performance/wp-474-493-closure.md
    decision: Ordered asynchronous checkpointing shipped in 351ae33a with deterministic suffix
      recovery, bounded barriers, backup integration, and current process-level recovery coverage.
  human_review_triggers: *id001
- id: WP-490
  name: Journal-overlay production enablement and safe-app throughput gate
  depends_on: [WP-489]
  objective: Enable the accepted overlay path for the standard application profile and prove that
    RiffDB beats PostgreSQL safe-app on same-run mean public-application throughput without
    weakening correctness or hiding a regressed client point.
  requirements: [PERF-001, PERF-002, PERF-003, PERF-004, PERF-005, PERF-007,
    PERF-008, PERF-009, PERF-015, PERF-017]
  required_adrs: [ADR-0053, ADR-0055, ADR-0056, ADR-0060, ADR-0061, ADR-0070,
    ADR-0098, ADR-0101, ADR-0102, ADR-0103, ADR-0104]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-server/**, crates/riffdb-observability/**,
    examples/app-baseline/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/architecture/**, docs/operations/**, docs/performance/**]
  deliverables:
  - Standard production composition selects frame-first overlay publication; hardened composition
    remains selectable and semantically equivalent through its redb two-phase oracle
  - Telemetry reports journal frames/fences, published and private overlay transitions/bytes,
    checkpoint queue/apply/commit/reclaimed counts, backpressure, barrier drains, and read merge
    classes with closed low-cardinality labels
  - Same-host proxy-free sequential-exclusive tri-backend evidence uses three 30-second measured
    repetitions after five-second warmup at 1, 8, 32, and 128 clients with zero semantic failures
  - The arithmetic mean of the four per-client RiffDB mean-throughput points exceeds the matching
    PostgreSQL safe-app arithmetic mean; every individual RiffDB point reaches at least 0.90 times
    safe-app, and c1/read/write p50 plus p99 satisfy their existing gates
  - Write-only, interactive, agent, hot-key contention, history-growth, crash-under-load, and
    deploy-under-load profiles remain bounded, exact, and free of unavailable/storage errors
  - Documentation states the checkpoint-plus-overlay mechanism, operational degraded state,
    metrics, bounds, standard/hardened profiles, and retained POC limitations
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./benchmarks/run-app-baseline --full --load interactive --load-concurrency-sweep
    --postgres-comparator safe-app --reps 3 --require-stable
  - ./benchmarks/run-app-baseline --full --load write_only --load-concurrency-sweep
    --postgres-comparator safe-app --reps 3 --require-stable
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Same-run retained evidence proves RiffDB's four-point arithmetic mean throughput beats
    PostgreSQL safe-app, no point falls below 0.90 parity, every correctness/recovery/latency gate
    passes, and the complete standard-profile mechanism is documented and operationally bounded.
  closure:
    status: complete_with_superseded_release_gate
    evidence: docs/performance/wp-474-493-closure.md
    decision: Production overlay activation is complete and its historical 30-second sweep exceeded
      safe-app by 1.05 times in the four-point mean with every point at or above 0.98 and zero
      failures. Later PERF-018 and PERF-008 revisions supersede that activation receipt for release;
      WP-623 and WP-674 remain open and no alpha-performance pass is claimed here.
  human_review_triggers: *id001
- id: WP-493
  name: Contract-migration preflight reads the published overlay
  depends_on: [WP-487, WP-488, WP-490]
  objective: Restore fail-closed contract-migration preflight after ADR-0104 routed every
    standard-profile command, including the idle singleton, through the journal suffix, by reading
    predecessor rows from the published checkpoint-plus-overlay view instead of the redb checkpoint
    alone, without reintroducing the retired direct singleton path.
  requirements: [STO-001, STO-002, REC-001, TXN-041, VAL-003]
  required_adrs: [ADR-0076, ADR-0088, ADR-0101, ADR-0103, ADR-0104]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-redb/**]
  deliverables:
  - Live migration preflight row scan, target probe, and unresolved-admission probe capture one
    published composite read view and resolve every ADR-0104 overlay table through the explicit
    point and range accessors rather than the checkpoint-only deref
  - The open-ended overlay range accessor merges the published overlay when the upper bound is
    absent, so an unbounded preflight page cannot silently fall back to the checkpoint
  - A predecessor row that is durable in the journal suffix but not yet checkpointed is inspected
    by preflight, so an invalid predecessor still fails closed as InvalidPredecessor
  - The private staged database and the offline post-recovery driver keep their existing
    checkpoint reads, which are exact because a staged store publishes no overlay
  - An architecture check freezes the structure and reds if any preflight read returns to the
    checkpoint-only deref of an overlay table
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test contract_migration_gate_a --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Contract-migration preflight refuses an invalid predecessor whose rows are only
    journal-suffix durable, the retired direct singleton path is not reintroduced, ADR-0101/0103/0104
    semantics are unchanged, and an architecture check reds if a preflight read returns to the
    checkpoint alone.
  closure:
    status: complete
    evidence: docs/performance/wp-474-493-closure.md
    decision: Overlay-aware migration preflight shipped in 0e3f56e4; the architecture pin and the
      focused current-head migration gate pass while the direct singleton path remains retired.
  human_review_triggers: *id001
- id: WP-020
  closure:
    status: complete
    evidence: crates/riffdb-proto/fixtures
    decision: Public and durable Protobuf envelopes are versioned, bounded, checksummed, schema-hashed, reproducibly generated, and covered by byte-frozen compatibility fixtures and structural validation tests.
  name: Protobuf and durable envelope
  depends_on:
  - WP-010
  objective: Define the phase-zero common public messages, fixed service/RPC shells, StoredEnvelope, and pure-Rust
    Protobuf generation pipeline without pre-empting later semantic schema owners.
  requirements:
  - STO-002
  - VAL-003
  - STO-020
  - STO-021
  - STO-022
  - API-001
  required_adrs:
  - ADR-0006
  - ADR-0011
  allowed_paths:
  - Cargo.lock
  - proto/**
  - crates/riffdb-proto/**
  - fixtures/proto/**
  - scripts/generate-proto*
  deliverables:
  - Versioned phase-zero common messages, fixed public service/RPC shells, and StoredEnvelope only; WP-065 owns later
    semantic durable records and WP-127 owns every remaining public message field
  - Protox/Prost generation and Tonic-compatible service descriptors; Tonic Rust stubs are deferred to WP-130
  - Size limits and decode validation
  - Golden compatibility fixtures
  - Reserved-field policy
  acceptance_commands:
  - cargo test -p riffdb-proto
  - ./scripts/generate-proto --check
  exit_gate: Golden fixtures decode and generated sources reproduce with a clean worktree.
  human_review_triggers: *id001
- id: WP-030
  closure:
    status: complete
    evidence: crates/riffdb-contract-syntax/LANGUAGE.md
    decision: The bounded contract lexer, parser, source spans, diagnostics, reserved words, comments, and canonical syntax are implemented with parser, snapshot, generation, and fuzz coverage.
  name: Contract syntax
  depends_on:
  - WP-010
  objective: Implement the bounded contract language lexer, grammar, source spans, syntax AST, and parser diagnostics.
  requirements:
  - CMP-001
  - DSL-001
  - DSL-002
  - DSL-006
  - DSL-008
  required_adrs:
  - ADR-0002
  - ADR-0011
  - ADR-0015
  allowed_paths:
  - .github/workflows/ci.yml
  - Cargo.lock
  - deny.toml
  - crates/riffdb-contract-syntax/**
  - contracts/parser-fixtures/**
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - fuzz/.gitignore
  - fuzz/deny.toml
  - fuzz/fuzz_targets/contract_*
  - scripts/tool-versions
  deliverables:
  - Logos lexer
  - LALRPOP grammar
  - Spanned syntax AST
  - Valid and invalid parser corpus
  - Parser fuzz target
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax
  - cargo +nightly-2026-07-12 fuzz run contract_parser -- -max_total_time=30
  exit_gate: Budget source parses; invalid corpus yields bounded, stable span diagnostics; fuzz smoke passes.
  human_review_triggers: *id001
- id: WP-040
  closure:
    status: complete
    evidence: crates/riffdb-contract-compiler
    decision: Typed IR, stable symbolic identities, compiler-allocated IDs, command dependency plans, compatibility classification, deterministic artifacts, and semantic diagnostics are implemented and current compiler fixtures pass.
  name: Typed IR and compiler
  depends_on:
  - WP-010
  - WP-030
  objective: Compile parsed contracts into deterministic typed IR, execution plans, compatibility metadata, JSON
    Schemas, and explain diagnostics.
  requirements:
  - CMP-001
  - CMP-010
  - CMP-011
  - CMP-012
  - CMP-013
  - CMP-020
  - CMP-021
  - CMP-022
  - DSL-001
  - DSL-002
  - DSL-003
  - DSL-004
  - DSL-005
  - DSL-007
  - DSL-008
  - DSL-009
  - DSL-010
  - DSL-011
  - DSL-012
  - MCP-020
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0010
  - ADR-0011
  - ADR-0013
  - ADR-0014
  - ADR-0015
  - ADR-0016
  - ADR-0017
  - ADR-0020
  - ADR-0038
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-contract-ir/**
  - crates/riffdb-contract-compiler/**
  - fixtures/compiler/**
  - contracts/examples/budget.riff
  - scripts/generate-contract-fixtures*
  deliverables:
  - Name resolution and type checking
  - Invariant, conflict, read/write, outcome, and event plans
  - Deterministic bundle hashing
  - JSON input/output Schema generation
  - Generated canonical IR and JSON Schema format registries
  - Explain and compatibility reports
  - ProjectionGroupSchema and BoundProjectionGroupSchema with exact group/measure validation and complete key and
    stored-row maximum analysis
  - Deterministic projection identity, plan, group-key, and maximum-size fixtures
  - Rejection fixtures for dynamic or cross-partition mutation acquisition and hidden write-influencing indexed
    range reads
  - Versioned deterministic command tool-name registry with exact ADR-0020 v1 derivation, stable CommandId ordering,
    grammar, length, and normalized-collision rejection, compatibility reporting, source-span diagnostics, golden
    fixtures, and declaration-order-independent property tests
  acceptance_commands:
  - cargo test -p riffdb-contract-ir -p riffdb-contract-compiler
  - ./scripts/generate-contract-fixtures --check
  exit_gate: Budget contract produces stable IR, plan hash, schemas, command tool-name registry, generated docs, and
    diagnostic snapshots; invalid or colliding ADR-0020 names reject deterministically.
  human_review_triggers: *id001
- id: WP-045
  closure:
    status: complete
    evidence: examples/budget-comparison/fixtures/workload-v1.json
    decision: The canonical PostgreSQL and reference-model budget workload, guarantees, observations, and safety profiles are frozen. The repaired nested comparison workspace passes its complete semantic and architecture suite.
  name: Budget comparison baseline
  depends_on:
  - WP-040
  objective: Establish the long-lived budget workload, normalized observation oracle, and isolated PostgreSQL
    implementation used for implementation comparison and later benchmarks.
  requirements:
  - POC-001
  required_adrs:
  - ADR-0002
  - ADR-0011
  - ADR-0015
  allowed_paths:
  - examples/budget-comparison/Cargo.toml
  - examples/budget-comparison/Cargo.lock
  - examples/budget-comparison/.gitignore
  - examples/budget-comparison/README.md
  - examples/budget-comparison/core/**
  - examples/budget-comparison/fixtures/**
  - examples/budget-comparison/postgres/**
  - examples/budget-comparison/tests/postgres_*
  - .github/workflows/budget-comparison.yml
  deliverables:
  - Backend-neutral workload and normalized observation oracle
  - Explicit semantic guarantee profiles
  - PostgreSQL schema and adapter in an isolated nested workspace
  - Golden workload and observation fixtures
  - >-
    Exact isolated dependencies: postgres 0.19.14 with default features disabled and synchronous NoTls only, plus
    serde_json 1.0.150 with default features disabled and only std; no ORM, pool, TLS integration, native dependency,
    or root-workspace production dependency
  - CI live-adapter evidence against exactly
    postgres:18.4-bookworm@sha256:d9c83446333daec3f0588cc709adb80c26090b7f9f0f7ec8d43c243385d79818,
    running the declared acceptance command with PostgreSQL required rather than treating a local skipped-live test
    as exit-gate evidence
  acceptance_commands:
  - cargo test --manifest-path examples/budget-comparison/Cargo.toml --workspace
  exit_gate: The PostgreSQL adapter passes the budget workload oracle and records its exact isolation, durability,
    and concurrency assumptions.
  human_review_triggers: *id001
- id: WP-050
  closure:
    status: complete
    evidence: crates/riffdb-catalog
    decision: Contract history, activation, exact plan lookup, stable-ID resolution, compatibility, migration identity, and fail-closed startup validation are catalog-owned and pass catalog, compiler, service, and recovery suites.
  name: Contract catalog
  depends_on:
  - WP-020
  - WP-040
  - WP-060
  objective: Define immutable bundle persistence, catalog-owned historical-plan validation and opaque command
    materialization evidence, and a typed catalog-activation intent for coordinator-owned, optimistic expected-version
    deployment.
  requirements:
  - CMP-001
  - CMP-020
  - CMP-021
  - CMP-022
  - ENT-002
  - ENT-003
  - MCP-032
  - REC-001
  - REC-002
  required_adrs:
  - ADR-0002
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0013
  - ADR-0016
  - ADR-0020
  - ADR-0023
  - ADR-0030
  - ADR-0038
  - ADR-0039
  - ADR-0042
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-catalog/**
  - crates/riffdb-contract-ir/src/key_schema.rs
  - crates/riffdb-storage-api/src/catalog*
  - tests/contract_deploy*
  deliverables:
  - Bundle persistence interface
  - Active-version pointer
  - Typed expected-version activation intent for the commit coordinator
  - Compatibility checks
  - Catalog notifications
  - Deployment failpoint and redaction-safe telemetry hooks
  - Activation-time revalidation of compiled command tool-name registry completeness, deterministic v1 derivation,
    canonical ordering, CommandId and lineage binding, and uniqueness, with rejection rather than repair or
    regeneration of invalid metadata
  - IR-aware, bounded historical bundle and executable-plan resolver that consumes every same-session
    HistoricalSemanticEvidence page through exact end and returns an opaque catalog-owned ValidatedCatalogHistory
    bound to that startup session; truncated, skipped, repeated, reordered, mismatched, or unknown evidence fails closed
  - Catalog-owned MAX_ACTIVE_LINEAGE_BUNDLES_V1=4,096, MAX_ACTIVE_LINEAGE_CANONICAL_BYTES_V1=67,108,864,
    and MAX_LINEAGE_MATERIALIZATION_PROOF_BYTES_V1=2,097,152, with checked exact canonical-byte sums and compile-time
    arithmetic assertions for the 1,810,703-byte maximum proof framing and independent 2 MiB structural mask bound;
    normalized snapshot bytes plus retained nonempty mask bitset payload bytes share the existing 16 MiB cap, with
    aligned binding/root and mask vectors making position implicit and uncharged
  - ActiveCatalogSnapshot and historical-plan resolution that walk active-to-genesis by exact parent version/hash,
    revalidate adjacent bundles forward with the shared comparator, reject gaps/cycles/substitution/numeric-version
    ancestry, and attach one shared nonserializable catalog-owned lineage proof to ResolvedExecutablePlan
  - Schema-derived, canonical field-introduction ledgers and opaque exact-writer null-fill masks for entity/event
    owners, preserving unknown fields and authorizing only strict ancestor optional-null omissions without trusting a
    compatibility report as proof
  - Opaque process-local command-materialization API whose successful Ready evidence supplies a bounded normalized
    snapshot and opaque current-recheck evidence, while valid over-limit resource evidence retains only the original
    bounded raw snapshot plus the exact resolved plan/proof and no over-budget normalized data or masks
  - Pure catalog-owned current-recheck operation consumed only after coordinator dependency equality; it proves exact
    raw physical observation equality and deterministically repeats the same normalization and exact charge without a
    catalog lookup, storage I/O, runtime evaluation, or exposure of masks/proof internals to WP-100
  - Opaque process-local projection event-materialization view that binds the exact resolved projection bundle and
    plan to an enclosing commit's exact writer-plan reference and immutable durable event, reuses the catalog-proved
    omission authority, preserves unknown fields, and never changes event bytes/hash or crosses a storage trait
  - Activation preparation checks for count, exact canonical bytes, and proof charge before coordinator submission,
    plus startup InvalidHistoricalEvidence classification for any broken or over-limit active chain; storage reads
    remain CatalogError::Storage and expected-active comparison remains coordinator-owned
  - Boundary fixtures for 4,096/4,097 bundles, 64 MiB/equal-plus-one canonical bytes, a synthetic checked proof-charge
    calculator at 2 MiB/equal-plus-one without claiming a valid proof reaches the cap, the separate valid 1,810,703-byte
    proof maximum/headroom, combined 16 MiB snapshot-plus-mask equality/equal-plus-one, one-byte-over returned resource
    evidence retaining no normalized data/masks, Ready/resource current-recheck equality and deterministic charge,
    parent gaps/cycles/hash substitution, and ancestor/equal/descendant writer masks
  - Exact historical KeySchema validation for every IR-opaque persisted entity-key, index-entry-key, and range-prefix-key
    evidence item, with unknown owners/schemas, component-incomplete bytes, omissions, and non-exact stream ends rejected
  - Catalog-owned pure bounded capability-partition schema validator shared by service admission and startup history,
    plus consumption of every 0x05 capability-partition evidence item after persisted-key evidence against the active
    validated bundle before ValidatedCatalogHistory can be produced
  - Architecture checks proving ValidatedCatalogHistory and command materialization evidence never cross a storage
    trait, concrete storage never directly imports or interprets contract IR, WP-100 consumes but does not implement
    or inspect the opaque evidence API, and neither side accepts a generic validation callback
  - Catalog consumption of the replacement-only IndexMigrationRow historical-evidence variant with its exact 0x04
    ordering, charge, single-consumption, V1/V2 coverage, and unchanged 0x05 capability-partition ordering
  - Pure bounded historical partition derivation through the sole narrow riffdb-catalog-to-riffdb-invariant edge,
    using the exact retained bundle and positional aggregate-root key prefix for roots, children, and embedded index
    entity keys, with wrong ownership, lineage, schema, partition, incomplete consumption, or trailing bytes rejected
  - Non-readiness CatalogIndexMigrationContext plus catalog-owned fields-private move-only V1Rewrite/V2Confirm,
    instruction-batch, pending, completion, and consuming driver types; every scan/bundle/apply/finish exchange
    consumes a request/proof branded by concrete backend type B, response factories consume only that exact request,
    row derivation accepts the exact owned canonical bundle and never a caller partition, and only a fresh V2-only
    validation can produce ValidatedCatalogHistory
  - Compile-fail/type-state fixtures proving no external crate can construct catalog progress/completion, a local fake
    backend cannot replay Batch<Fake> as Batch<Redb>, and server code cannot receive a branded request/response or
    instruction
  acceptance_commands:
  - cargo test -p riffdb-catalog
  - cargo test -p riffdb-catalog --test contract_deploy_recovery
  external_test_targets:
  - name: contract_deploy_recovery
    owner_crate: riffdb-catalog
    manifest: crates/riffdb-catalog/Cargo.toml
    path: ../../tests/contract_deploy_recovery.rs
  exit_gate: Deployment crash tests leave exactly the old or new bundle active, retries resolve by version, exact
    active-lineage count/byte/proof boundaries, opaque Ready/resource evidence retention, current-recheck, and writer
    masks pass, malformed chains or ADR-0020 command-name registries reject, and only complete same-session historical
    evidence, including every qualifying capability-partition item, can produce ValidatedCatalogHistory.
  human_review_triggers: *id001
- id: WP-060
  closure:
    status: complete
    evidence: crates/riffdb-storage-api
    decision: Storage exposes semantic versioned ports rather than backend handles, with bounded scans, snapshots, transactions, typed records, startup validation, and sealed authority boundaries. Architecture and backend conformance tests pass.
  name: Storage semantic API
  depends_on:
  - WP-010
  - WP-020
  - WP-040
  objective: Define the narrow storage contract required by snapshots, atomic command commits, catalog state,
    exclusive startup evidence, scans, and derived workers.
  requirements:
  - SYS-004
  - STO-001
  - STO-002
  - STO-010
  - STO-011
  - STO-012
  - STO-020
  - STO-021
  - STO-022
  - LOG-001
  - ENT-001
  - ENT-002
  - ENT-003
  - TXN-030
  - TXN-031
  - TXN-041
  - TXN-042
  - EFF-001
  - EFF-003
  - REC-003
  - PRJ-001
  - PRJ-002
  - PRJ-003
  - PRJ-004
  - MCP-046
  required_adrs:
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0010
  - ADR-0011
  - ADR-0012
  - ADR-0013
  - ADR-0016
  - ADR-0017
  - ADR-0018
  - ADR-0019
  - ADR-0021
  - ADR-0023
  - ADR-0030
  - ADR-0035
  - ADR-0038
  - ADR-0039
  - ADR-0042
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-types/src/codec.rs
  - crates/riffdb-types/src/ids.rs
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-testkit/Cargo.toml
  - crates/riffdb-testkit/src/lib.rs
  - crates/riffdb-testkit/src/model/**
  deliverables:
  - Bounded materialized ReadSnapshot and coordinator-driven atomic commit interfaces
  - Consuming command-candidate type state with count-only start, admission/current-dependency recheck, private
    validation and mutation-affected prefix derivation, same-transaction affected-epoch reads, exact sequence-free
    CommandWriteSetPlanV1 and semantic/conservative encoded-capacity reservation, sequence assignment only after
    reservation, retained intent/plan/assignment verification, and complete graph staging; the WP-060 interface and
    memory conformance use explicit synthetic charges and do not depend on the later WP-065 codec
  - Storage-API-owned EventHash derivation and constructor validation over the exact ADR-0011 EventId, EventTypeId,
    u32-length-framed canonical Value::Record preimage, including reciprocal commit/event/outbox mismatch rejection
  - Closed Next(nonzero) or Exhausted application/administration allocator semantics, including atomic multi-slot
    preflight for compound bootstrap
  - Semantic key/value records
  - Catalog, capability, outbox, and projection persistence ports
  - In-memory storage implementation
  - Ordered commit scan with a dedicated internal ceiling of 500 complete records and 16 MiB encoded page content;
    semantic commit records are never chunked and generic scan pages remain limited to 500 entries and 4 MiB
  - Integrity-check interface
  - Checked EvaluatedCommand and complete dependency-validation request, terminal non-commit admission transition,
    and memory conformance cases
  - Additive non-durable CandidateValidationRejection::CommitCheckArithmeticFault control result, distinct from a
    false commit predicate and integrity fault, with memory conformance proving rollback before sequence assignment
  - Source-free Existing(DatabaseId) or NeedsInitialization probe, checked candidate initialization that atomically
    re-proves emptiness and returns any concurrent durable winner without candidate comparison, plus
    CommitIntent-carried ProvenanceId, collision/no-overwrite results, and architecture checks excluding
    clock/entropy from storage
  - StoredAdmittedProvenanceClaimsV1 admission fields and storage-structural DTOs with coordinator-private proof and
    write-authority conformance cases
  - StoredServiceAuditRecordV1 plus specialized ordered append/read, administration sequence, and compound bootstrap
    transitions, exact linked-result sum type, checked ADR-0021 target collection preserved without storage-side
    derivation, and pre-start/post-start phase invariants
  - Bounded capability record, token-lookup, bootstrap-marker, and closed capability transition semantics
  - Nonsecret readable digest-scheme/key inventories and fail-closed integrity results for every pending/terminal
    idempotency identity and active capability, without exposing operational key bytes to storage
  - Canonical absent outbox-status as initial Pending, no-orphan status transitions, reciprocal event/intent checks,
    and component-scoped projection/outbox structural findings distinct from authoritative readiness
  - Exact projection state, control, marker, lifecycle, retirement, checked apply/hash, query snapshot, lower
    continuation, status-read, and epoch/frontier-position semantics
  - Exact ADR-0019 retained metadata surface, canonical validity of absent deferred operational metadata, and
    architecture checks rejecting NodeId, clean-shutdown, persisted-integrity-result, or substitute placeholder
    types, keys, ports, and records
  - Engine-neutral StructuralEvidenceSession type-state, bounded HistoricalSemanticEvidence pages with an unambiguous
    exact-end result, and StructurallyOpened dormant-port handoff bound to one database/open session; initialization
    precedes evidence, no mutation port is available during the session, and failure releases no operational ports
  - Bounded IR-opaque exact-end evidence for every persisted entity key, index-entry key, and range-prefix key, carrying
    only the durable owner/reference facts needed for catalog-owned exact historical KeySchema validation
  - Process-local HistoricalCapabilityPartitionEvidenceV1 and memory-engine production for every explicit entry whose
    durable owner is Active and authorization_time is strictly earlier than expires_at, with exact 0x05 ordering,
    31-plus-lineage-plus-key charge, redacted diagnostics, future-issued inclusion, and exact-expiry exclusion
  - Memory conformance for initialization-before-evidence, exclusive noninterleaving scans, page continuation and
    exact-end coverage, session/database mismatch rejection, dropped/failed session behavior, and dormant ports that
    cannot become operational without the separate catalog-owned same-session ValidatedCatalogHistory
  - Codec-owned migration semantic row, fields-private process-local migration-row evidence wrapper, replacement-only
    IndexMigrationRow historical variant, exact 0x04 ordering and dual charges, checked cursor/bounds, and an identity-
    only StartupIndexMigrationPort contract with no forgeable codec-evidence constructor or public scan, bundle-read,
    instruction, page/batch, apply, exact-end, completion, or finish authority
  - Memory structural startup outcomes for Ready/Clean versus paired MigrationRequired states with a privately
    constructed concrete port implementing only the identity contract; complete catalog-driven memory migration and
    shared backend conformance are sequenced under WP-070 after WP-050
  acceptance_commands:
  - cargo test -p riffdb-storage-api -p riffdb-storage-memory
  exit_gate: Reference storage satisfies atomicity, pre-sequence reservation and no-gap ordering, exact event-hash,
    snapshot, metadata-deferral, startup type-state, exact-end historical-evidence, and model-state properties without
    introducing a storage-to-IR dependency; capability-partition inventory fixtures prove complete exact-end emission.
  human_review_triggers: *id001
- id: WP-065
  closure:
    status: complete
    evidence: crates/riffdb-proto/fixtures/durable-schema-hashes.bin
    decision: Authoritative, derived, audit, outbox, projection, idempotency, migration, and maintenance records have versioned bounded canonical schemas with generated golden hashes and reader/writer compatibility windows.
  name: Durable semantic record schema
  depends_on:
  - WP-020
  - WP-060
  objective: Freeze versioned durable semantic-record Protobuf messages and the one-way checked storage codec bridge
    before a concrete engine persists them.
  requirements:
  - STO-002
  - STO-020
  - STO-021
  - STO-022
  - VAL-003
  - TXN-031
  - EFF-003
  - MCP-046
  required_adrs:
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0010
  - ADR-0011
  - ADR-0012
  - ADR-0013
  - ADR-0014
  - ADR-0016
  - ADR-0017
  - ADR-0018
  - ADR-0019
  - ADR-0021
  - ADR-0022
  - ADR-0038
  - ADR-0039
  - ADR-0042
  allowed_paths:
  - Cargo.lock
  - proto/**
  - crates/riffdb-proto/**
  - fixtures/proto/**
  - scripts/generate-proto*
  - crates/riffdb-storage-api/Cargo.toml
  - crates/riffdb-storage-api/src/lib.rs
  - crates/riffdb-storage-api/src/proto_codec/**
  - crates/riffdb-storage-api/tests/architecture.rs
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - fuzz/fuzz_targets/proto_durable.rs
  deliverables:
  - Exactly nine riffdb.storage.v1 semantic-record source modules named common.proto, metadata.proto,
    application.proto, catalog.proto, capability.proto, audit.proto, outbox.proto, projection.proto, and the isolated
    index_v2.proto, in addition to envelope.proto; the unchanged first eight retain the accepted exact 26 payload
    tuples and index_v2 adds only the V2 index payload to the readable registry
  - Exact application and administration allocator-state messages/codecs for Next(nonzero) and Exhausted, with
    first, maximum, exhausted, malformed, and compound-bootstrap capacity fixtures
  - Descriptors, schema hashes, wire-structural validation, decoder bounds, golden bytes, and historical compatibility
    registrations
  - Checked storage DTO mappings through a one-way riffdb-storage-api proto_codec bridge with no reverse
    riffdb-proto dependency on storage
  - Linked durable records for the compound bootstrap transition and reviewed projection generation/frontier records
  - Exact DatabaseId initialization metadata, ProvenanceId commit linkage, UUIDv7 validation, audit linked-result,
    and absent-initial-outbox-status mappings with deterministic wire fixtures
  - Exact EventHash preimage/domain-frame codec, descriptor and golden fixtures, including EventId/type/ordinal,
    payload-length, canonical-record ordering, cross-domain, malformed, and durable round-trip cases
  - Separately registered top-level StoredDurableEventV1 envelope and codec in addition to its exact reciprocal
    nested commit/outbox representations, with no missing, orphaned, unequal, or hash-invalid copy accepted
  - Conservative per-record-class upper bounds for the complete canonical StoredEnvelope, with proofs against the
    real codec, exact actual byte-charge reporting, equal-bound success, one-byte-over rejection, and aggregate
    reservation fixtures consumed by WP-070 before sequence staging; WP-060 remains responsible only for
    synthetic-charge conformance
  - Reviewed durable representation and total mapping for all nine ADR-0021 target variants, preserving canonical
    order while rejecting unknown/zero kinds, malformed lineage scope, duplicates, noncanonical order, and over 16
  - Generated schema inventory proving the exact ADR-0019 retained metadata records and no NodeId, clean-shutdown,
    last-successful-integrity-check, reserved-field, key-codec, or envelope placeholder for deferred metadata
  - Terminal-idempotency fixtures proving that one full StoredOutcomeV1 envelope is both terminal state and persisted
    outcome, with no pointer, tombstone, or second terminal envelope, and malformed outcome/commit/provenance
    reciprocity vectors for WP-070 startup validation
  - Encoded-capacity fixtures proving that atomic pending-row deletion and index-entry deletion write no envelope
    bytes, mixed index classes sum only canonical Put post-images, and their distinct semantic charges remain nonzero
  - Isolated index_v2.proto source defining only StoredIndexEntryV2 while all nine existing durable source files and
    their accepted descriptor closures, hashes, payload goldens, and envelope goldens remain byte-identical
  - Exact ordered 27-readable/26-writable durable registries, with StoredIndexEntryV1 decode-only and V2 the sole
    normal-write index role, plus V1/V2 decoder fuzz and unknown/ambiguous tuple rejection
  - Sole durable-codec factories for canonical migration-row evidence and conservative V2 replacement envelopes,
    without owning a catalog instruction, tracker, or completion; together with the fields-private Fits or
    ExceedsAcceptedAggregateCap sequence-free result and one-maximum-row proof under the independent 500-row/4-MiB
    evidence and instruction ledgers
  acceptance_commands:
  - cargo test -p riffdb-proto -p riffdb-storage-api
  - ./scripts/generate-proto --check
  - cargo +nightly-2026-07-12 fuzz run proto_durable -- -max_len=16777216 -max_total_time=30
  exit_gate: Every durable semantic record has bounded validation, stable generated descriptors and goldens, checked
    storage mappings, exact event-hash and canonical-envelope upper-bound evidence, no deferred operational-metadata
    placeholder, and a clean generation diff before redb persistence begins.
  human_review_triggers: *id001
- id: WP-070
  closure:
    status: complete
    evidence: crates/riffdb-storage-redb
    decision: Redb implements the semantic storage ports, atomic cross-table commands, snapshots, startup validation, journal/checkpoint recovery, migration, maintenance, retention, and crash semantics. Full storage and recovery matrices pass.
  name: Redb storage engine
  depends_on:
  - WP-020
  - WP-050
  - WP-060
  - WP-065
  objective: Implement the POC durable backend with redb, versioned tables, atomic commits, recovery checks, and
    offline backup.
  requirements:
  - STO-001
  - STO-002
  - STO-010
  - STO-011
  - STO-012
  - STO-020
  - STO-021
  - STO-022
  - REC-001
  - REC-002
  - REC-003
  - TXN-041
  - TXN-042
  - EFF-001
  - EFF-003
  - POC-009
  - PRJ-001
  - PRJ-002
  - PRJ-003
  - PRJ-004
  - MCP-046
  required_adrs:
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0010
  - ADR-0011
  - ADR-0012
  - ADR-0013
  - ADR-0016
  - ADR-0017
  - ADR-0018
  - ADR-0019
  - ADR-0021
  - ADR-0030
  - ADR-0032
  - ADR-0035
  - ADR-0038
  - ADR-0039
  - ADR-0042
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-storage-api/src/startup.rs
  - crates/riffdb-storage-api/tests/core_boundaries.rs
  - crates/riffdb-storage-api/tests/architecture.rs
  - crates/riffdb-storage-memory/Cargo.toml
  - crates/riffdb-storage-memory/src/startup.rs
  - crates/riffdb-storage-redb/**
  - tests/service_audit_recovery/**
  - tests/storage_recovery/**
  - scripts/storage_*
  deliverables:
  - Frozen redb table/key layout using lineage-framed contract-bundle keys, one catalog_active/0x01 row, exact
    canonical entity/index/range-prefix keys, the exact retained metadata keys, and no durable memory accelerators
  - Atomic entity/index/outcome/event/provenance/log commit
  - Authoritative events table keyed by exact EventId, atomically written and reciprocally validated against the
    equal commit and outbox copies
  - Exact consuming candidate ordering from count-only start through current validation, mutation-affected epoch
    reads, sequence-free write-plan reservation, sequence assignment, graph verification, canonical encoding, and
    staging, with no assigned sequence on capacity failure
  - Pre-stage recomputation of every actual canonical StoredEnvelope byte charge against WP-065's retained per-class
    upper bounds, aborting the complete transaction before any write on mismatch or excess
  - Durable EventHash recomputation using the exact ADR-0011 preimage during decode, startup integrity, and reciprocal
    commit/event/outbox validation, with every missing, orphaned, duplicate, unequal, or hash-invalid standalone
    event row rejected rather than repaired
  - Complete storage-structural startup scan and exact-end historical semantic evidence
  - Complete bounded enumeration of every persisted entity key, index-entry key, and range-prefix key with IR-opaque
    durable owner/reference evidence for catalog validation, without interpreting KeySchema in the redb adapter
  - Complete exact-end enumeration of every qualifying active, unexpired explicit capability-partition entry from the
    one frozen startup snapshot, preserving all sibling ordinals across pages without interpreting KeySchema
  - Offline backup and restore
  - Storage benchmarks
  - Dependency-free repeated-run storage benchmark target; no Criterion dependency
  - Named storage failpoints and redaction-safe telemetry hooks
  - Exact redb 4.1.0 dependency with default features disabled, no optional features, and no engine-specific public
    surface
  - Exact sha2 0.11.0 dependency with default features disabled, used only for SHA-256 backup manifests
  - Default-feature-disabled riffdb-contract-compiler dev-dependencies used only by memory conformance and redb
    recovery fixtures to build a real canonical indexed bundle and exercise full catalog validation, with no
    production compiler/IR edge or backend-owned expression interpretation
  - Durable standalone service-audit records and indexes, admitted-provenance admission fields, and compound
    bootstrap and replay persistence using only WP-065-reviewed schemas and mappings
  - Startup integrity rejection of unknown, malformed, duplicate, noncanonically ordered, or over-limit ADR-0021
    target lists without repair
  - Exact projection state/control/marker tables, integrity checks, generation retirement, and atomic frontier updates
  - Atomic new-store DatabaseId installation, reopen/restore preservation, initialized-store missing-ID rejection,
    and provenance-ID collision rollback with no sequence or overwrite
  - Fail-closed startup validation for both contiguous sequence spaces and metadata, every pending/terminal
    idempotency digest-key reference, capability lookup/bootstrap/audit cross-links, and every active capability
    digest-key reference, plus reciprocal commit/event/outbox-intent linkage; the POC performs no online sequence
    repair or referenced-key retirement
  - Service-audit, bootstrap, replay, projection, digest-provider retirement, sequence-metadata, crash, and reopen
    recovery evidence
  - Authoritative recovery result separated from bounded projection/outbox component findings; redb reports but does
    not invent WP-160 retry or WP-170 rebuild policy
  - Complete read-only storage-structural integrity validation on every startup after graceful close or crash, with no
    marker/history fast path, deferred operational-metadata write, or online repair, plus retained-metadata invariant
    and canonical-absence evidence
  - Exclusive StructuralEvidenceSession implementation that withholds every mutation/operational port while scanning
    and yields StructurallyOpened dormant redb ports plus the exact same-snapshot RetainedMetadataV1 bound to the
    DatabaseId and startup session only after the complete structural scan and historical evidence cursor both reach
    exact end
  - Explicit evidence that StructurallyOpened is not IR-aware or operational readiness; redb neither validates command
    plans nor consumes ValidatedCatalogHistory, and any scan failure drops the open rather than publishing ports
  - Named MemoryStartupIndexMigrationPort and RedbStartupIndexMigrationPort, privately constructed by their consumed
    structural sessions and privately implementing storage API's identity-only contract, with the sole narrow
    production backend-to-catalog dependency confined to startup migration and no direct IR/invariant import
  - Catalog-owned migration-only backend trait implementation in both concrete engines where every cross-crate scan,
    one-bundle read, batch apply, and finish consumes a fields-private move-only request/proof branded by the exact
    backend type, its response factory consumes that request, and all concrete page/read/apply/end helper states remain
    module-private and server-inaccessible
  - Exclusive linear V1/V2 migration through the catalog-owned driver with strict physical ordering, short read
    transactions, same-session codec evidence, bounded one-bundle point reads, paired 500-row/4-MiB evidence and
    instruction ledgers, whole-batch exact compare-and-rewrite, V2Confirm no-write replay, cancellation release, and
    no migration marker
  - Crash/reopen failpoints around every migration batch, dormant-unopened completion, fresh OpenSessionId and full
    post-migration structural/catalog revalidation, with instrumentation proving no storage transaction is held during
    catalog key decoding, expression evaluation, or instruction retention
  acceptance_commands:
  - cargo test -p riffdb-storage-api
  - cargo test -p riffdb-storage-memory
  - cargo test -p riffdb-storage-redb
  - cargo test -p riffdb-storage-redb --test storage_recovery_matrix
  - cargo test -p riffdb-storage-redb --test service_audit_recovery
  - cargo bench -p riffdb-storage-redb --no-run
  - cargo deny check
  external_test_targets:
  - name: storage_recovery_matrix
    owner_crate: riffdb-storage-redb
    manifest: crates/riffdb-storage-redb/Cargo.toml
    path: ../../tests/storage_recovery/storage_recovery_matrix.rs
  - name: service_audit_recovery
    owner_crate: riffdb-storage-redb
    manifest: crates/riffdb-storage-redb/Cargo.toml
    path: ../../tests/service_audit_recovery/service_audit_recovery.rs
  exit_gate: Storage property suite and process recovery matrix pass with no torn command state, pre-sequence gap, or
    event-hash/envelope-bound mismatch; every open completes the full structural scan and exact-end persisted-key and
    capability-partition evidence handoff
    before yielding StructurallyOpened dormant ports; no open claims IR-aware readiness, and no ADR-0019 deferred
    record is persisted.
  human_review_triggers: *id001
- id: WP-075
  closure:
    status: complete
    evidence: benchmarks/storage-fjall/reports/conformance-v1.tsv
    decision: The isolated exact-pinned Fjall comparison proves substrate mechanics while failing semantic/performance eligibility closed. Its current durable-registry inventory, architecture, substrate, bounds, and frozen gap report all pass.
  name: Fjall semantic comparison
  depends_on:
  - WP-060
  - WP-070
  objective: Implement the unchanged storage semantic conformance suite and benchmark workload against Fjall in
    a non-production nested workspace for the POC engine decision.
  requirements:
  - STO-001
  - POC-010
  - PRJ-001
  - PRJ-002
  - PRJ-003
  - PRJ-004
  required_adrs:
  - ADR-0004
  - ADR-0006
  - ADR-0010
  - ADR-0017
  - ADR-0039
  - ADR-0042
  allowed_paths:
  - benchmarks/storage-fjall/**
  deliverables:
  - Isolated Fjall storage adapter
  - Unchanged semantic storage conformance results
  - Recovery and benchmark harness compatible with the redb report
  - Separate lockfile and dependency inventory
  - Unchanged projection storage conformance results across identity, generation, markers, lifecycle, and frontier
  - Conformance against the accepted V2 startup outcome, 27-readable/26-writable registry semantics, and dual-ledger
    migration pagination through an identity-only port plus catalog-branded consuming driver contract, reporting an
    explicit unchanged-suite failure rather than freezing the pre-migration shape or inventing a catalog authority
  acceptance_commands:
  - cargo test --manifest-path benchmarks/storage-fjall/Cargo.toml --workspace
  - cargo bench --manifest-path benchmarks/storage-fjall/Cargo.toml --no-run
  exit_gate: Fjall passes or explicitly fails the unchanged semantic suite, and comparable benchmark artifacts
    are ready for WP-200 without linking Fjall into riffdbd.
  human_review_triggers: *id001
- id: WP-080
  closure:
    status: complete
    evidence: crates/riffdb-runtime
    decision: The deterministic command runtime evaluates only compiler-visible plans with injected transaction facts and no network, filesystem, clock, process-global mutation, or untracked randomness. Runtime semantic and deterministic tests pass.
  name: Deterministic command runtime
  depends_on:
  - WP-040
  - WP-060
  objective: Interpret compiler-produced command plans against a snapshot and produce deterministic typed outcomes
    and EvaluatedCommands without I/O, limited to expressions, predicates, invariants, postconditions, and commit
    checks with no contract state-machine source, IR, or execution surface.
  requirements:
  - TXN-002
  - TXN-010
  - TXN-011
  - TXN-012
  - TXN-013
  - TXN-030
  - TXN-031
  - DSL-004
  - DSL-005
  - DSL-006
  - DSL-007
  - DSL-008
  - ENT-002
  - ENT-003
  - ENT-004
  - POC-003
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0007
  - ADR-0012
  - ADR-0013
  - ADR-0016
  - ADR-0018
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-runtime/**
  - crates/riffdb-invariant/**
  - crates/riffdb-testkit/Cargo.toml
  - crates/riffdb-testkit/src/lib.rs
  - crates/riffdb-testkit/src/histories/**
  deliverables:
  - Shared pure riffdb-invariant evaluator for checked input-computable expressions plus predicate, invariant,
    postcondition, and commit-check evaluation over snapshot IR, with no service-owned logic duplicated in runtime
    and no runtime-owned combined request-preparation DTO
  - Durable-reservation-supplied logical time and no command randomness
  - Read dependency tracking
  - Architecture and negative-corpus proof that POC grammar/IR v1 and runtime contain no contract state-machine node,
    transition instruction, placeholder, or execution path
  - EvaluatedCommand construction without stored provenance claims or final CommitIntent assembly
  - Architecture proof that runtime receives only checked identifier values and no UUIDv7 clock, entropy, source,
    provider, or source-ordering state and never derives logical time or order from UUID fields
  - Immutable fixed-budget and deterministic arithmetic/resource-fault tests
  - Boundary tests proving pre-admission arithmetic is left to service preparation while arithmetic after an owned
    snapshot is the closed ADR-0012 ExecutionFault::Arithmetic path
  - One-partition cross-domain read evidence with complete influential dependency tracking
  - Stored-record evaluation accepts only already lineage-normalized complete declared fields, preserves present
    unknown fields in post-images while hiding them from historical expressions, and returns Integrity for every
    remaining missing declared field; blind optional fill is removed without changing exact new-record construction
  - Runtime fixtures for accepted pre-normalized ancestor records, rejected same-version/descendant/genesis/required
    omissions, and unknown-field preservation; proof construction and application remain outside runtime
  acceptance_commands:
  - cargo test -p riffdb-runtime -p riffdb-invariant
  - cargo test -p riffdb-testkit generated_histories
  exit_gate: Differential generated-history tests match the single-threaded reference model.
  human_review_triggers: *id001
- id: WP-090
  closure:
    status: complete
    evidence: crates/riffdb-conflict
    decision: Bounded sharded conflict ownership, canonical key ordering, cancellation release, fairness, and deterministic schedule behavior are implemented without assigning sequence or storage authority. Conflict and coordinator suites pass.
  name: Conflict manager
  depends_on:
  - WP-010
  objective: Provide cancellation-safe, canonical, multi-key exclusive logical capabilities and hot-key diagnostics.
  requirements:
  - TXN-001
  - TXN-013
  - TXN-020
  - TXN-021
  - TXN-022
  - TXN-023
  required_adrs:
  - ADR-0003
  - ADR-0011
  - ADR-0016
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-conflict/**
  deliverables:
  - Canonical key ordering
  - Sharded lock table
  - Wait queues, deadlines, cancellation, panic cleanup
  - Metrics hooks
  - Redaction-safe telemetry hooks
  - Loom and Shuttle models
  acceptance_commands:
  - cargo test -p riffdb-conflict
  - cargo test -p riffdb-conflict --features loom
  - cargo test -p riffdb-conflict --features shuttle
  exit_gate: Explored schedules find no double grant, leaked capability, deadlock cycle, or lost wakeup.
  human_review_triggers: *id001
- id: WP-100
  closure:
    status: complete
    evidence: crates/riffdb-commit
    decision: The sole commit coordinator owns admission order, current validation, conflict ownership, sequence assignment, atomic mutation/outcome/event/provenance records, idempotency, uncertainty recovery, and publication. Semantic, crash, simulation, and recovery suites pass.
  name: Commit coordinator and idempotency
  depends_on:
  - WP-050
  - WP-070
  - WP-080
  - WP-090
  - WP-110
  objective: Orchestrate admission, locking, deterministic evaluation, revalidation, sequence assignment, atomic
    persistence, and same-key recovery.
  requirements:
  - SYS-004
  - TXN-001
  - TXN-002
  - TXN-010
  - TXN-012
  - TXN-013
  - TXN-030
  - TXN-040
  - TXN-041
  - TXN-042
  - TXN-043
  - TXN-044
  - LOG-001
  - ENT-002
  - ENT-003
  - ENT-004
  - OUT-001
  - OUT-002
  - OUT-003
  - OUT-004
  - EFF-001
  - POC-002
  - POC-003
  - POC-004
  - POC-005
  - MCP-046
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0011
  - ADR-0012
  - ADR-0013
  - ADR-0016
  - ADR-0018
  - ADR-0021
  - ADR-0023
  - ADR-0033
  - ADR-0038
  - ADR-0039
  - ADR-0042
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-commit/**
  - crates/riffdb-idempotency/**
  - crates/riffdb-types/src/limits.rs
  - crates/riffdb-storage-api/src/bounds.rs
  - crates/riffdb-storage-api/src/command.rs
  - crates/riffdb-storage-api/src/snapshot.rs
  - crates/riffdb-contract-ir/src/plan.rs
  - crates/riffdb-contract-compiler/src/compiler.rs
  - tests/command_semantics/**
  - tests/service_audit/**
  deliverables:
  - Commit coordinator actor
  - Explicit least-authority ApplicationCommitNotificationSink injected into production coordinator construction;
    exactly one sequence-only publication follows each first durable application commit, while replay, read-only,
    failures, audit, initialization, and control-plane work publish none; sink error or panic preserves the known
    committed result but stops coordinator admission and readiness
  - Durable pending idempotency reservation with fixed tx.time and canonical input hash
  - Typed catalog activation and capability create/revoke coordination
  - Read/predicate revalidation
  - Persisted typed outcomes
  - Uncertain-response recovery
  - Named transaction failpoints and redaction-safe telemetry hooks
  - Bounded idempotency inspect/confirm plus command, control-plane, and AdministrationAuditExecutor ports
  - One public CommandExecutor boundary, published only with its real completion driver, whose closed result is
    Committed(CommittedOutcome), ExecutionFailed(ExecutionFailureCode), PreparationChanged, or InputMismatch; whose
    redacted executor-error kind is Cancelled, DeadlineExceeded, RetryBudgetExhausted, StorageUnavailable,
    OutcomeUnknown, InternalDefect, CoordinatorStopped, or CoordinatorFenced; and which never exposes a durable
    StoredExecutionFailed record
  - Executor queue-reservation cancellation by dropping or selecting away the service-owned reservation future,
    followed by the accepted cancellation token after capacity is acquired; submitted work is actor-owned and
    survives a dropped completion receiver
  - Commit-owned AdministrationAuditInputView with no field or method for the new audit record's assigned sequence,
    while preserving an already-authoritative transition sequence only inside the unchanged ControlPlane result link;
    plus opaque BootstrapCompoundAuditProof, exact control-plane transition sequence recovery metadata, and typed
    CapabilityPreparationChanged for an absent revoke target that appears
  - Commit-owned DatabaseInitializationExecutor that exposes the source-free identity probe and is the sole
    production caller of the authoritative initialization transition, accepting a checked candidate only after
    NeedsInitialization and never exposing a storage mutation handle to server composition
  - Commit-owned injected AdmissionClock, AdministrationClock, and ProvenanceIdSource ports with exact sampling,
    failure, and retry behavior and no ambient clock or entropy access in the coordinator
  - Coordinator-only final CommitIntent assembly from EvaluatedCommand and the exact immutable stored admission
  - Coordinator-owned orchestration of WP-050's opaque command-materialization API after the owned read view closes
    and of its pure current-recheck operation after dependency equality; WP-100 consumes Ready/resource evidence but
    neither applies/interprets lineage masks nor implements the catalog proof algorithm
  - Checked six-byte charge per inserted null under existing record/snapshot/evaluation limits; valid expansion
    excess follows dependency-sensitive ExecutionFault::ResourceLimit and ADR-0012 equality revalidation; successful
    normalized snapshot semantic bytes plus retained nonempty mask bitset payload bytes share the 16 MiB cap with
    aligned vector position implicit and uncharged, while returned overflow resource evidence retains only the
    original bounded raw snapshot and exact resolved plan/proof and no over-budget normalized value or mask; foreign writers, unproved
    omissions, malformed proof/mask, or a post-activation proof/cap breach remain integrity,
    leave Pending unchanged, write no sequence or ExecutionFailed graph, and stop authoritative readiness when durable
  - Transaction-current ordering that compares dependencies first and reevaluates a change before invoking the
    catalog-owned opaque current-recheck; that pure operation compares exact raw physical observations across
    target/version/writer/schema binding and all known/unknown fields and returns normalized current values, with
    same-version drift as stopped-readiness InternalDefect and no catalog lookup/additional storage I/O in the transaction
  - Lineage-overflow terminalization that compares all dependencies first, then invokes the catalog-owned resource
    evidence current-recheck to compare exact raw physical binding/root observations and deterministically repeat the
    same normalization/exact charge; only reproduction of the same valid over-limit ResourceLimit may replace Pending
    with ExecutionFailed
  - Normalization integration tests for ancestor/equal/descendant writers, same-version/genesis/required omission,
    exact +6-byte expansion, canonical and unknown-field preservation through an older-plan mutation, dependency
    change versus ResourceLimit terminalization, combined snapshot-plus-mask 16 MiB equality/equal-plus-one, raw
    current equality and same-version drift, returned resource-evidence absence of over-budget normalized data/masks,
    dependency-then-current-recheck-then-overflow replay order, and identical first/current-recheck proof behavior
  - Exact zero/nonzero mutation branch after universal influential-dependency revalidation, where zero mutations skip
    commit-check evaluation and index/affected-epoch derivation but still atomically persist the declared outcome,
    sequence, provenance, and commit record; nonzero mutations prove exactly one complete mutation per mutable
    historical-plan binding before the complete commit-check plan and index derivation, with no pre-image fallback
  - Narrow direct riffdb-contract-ir and riffdb-invariant use for exact historical-plan matching, grammar-v1 index
    derivation, and pure transaction-current commit-check evaluation through a private owned positional value source;
    no compiler, storage handle/callback, I/O, clock, entropy, async operation, or duplicate evaluator crosses it
  - Exact transaction progression that distinguishes influential range dependencies from mutation-affected epochs,
    derives and reads the latter after private validation, freezes/reserves the sequence-free write plan before
    sequence assignment, and verifies the final assigned graph before staging
  - EventId/EventHash generation only from the retained sequence assignment, event type, and canonical record payload,
    with exact ordinal order and no payload-only or Protobuf hash path
  - New-admission provenance freezing, stored-claim reuse, and complete mechanical storage-record-to-policy-facts
    lowering with transaction-current verifier and authorization-clock use after capability queue waits
  - ProvenanceId generation only for a successfully evaluated new attempt before transaction open, with
    collision-before-sequence handling, no regeneration during uncertain same-attempt resolution or terminal replay,
    a new candidate permitted only for a proven-noncommit reevaluation attempt, and same-key resolution before any
    retry or source call after an unknown response
  - Dependency-validated execution-failure terminalization, including the late commit-check arithmetic path that
    rolls back before sequence assignment, discards its one unpersisted provenance candidate, sources no second
    candidate, and follows changed-dependency reevaluation plus proven-abort versus unknown-commit recovery
  - Fixed private MAX_COMMAND_EVALUATION_ATTEMPTS_V1 ceiling of three attempt slots per invocation, each consumed
    immediately before catalog snapshot materialization; Ready continues into exactly one runtime evaluation in that
    slot and valid lineage-expansion ResourceLimit consumes it without runtime entry, with no fourth attempt,
    byte-identical Pending and no durable graph on internal RetryBudgetExhausted, and no reevaluation while resolving
    CommitStatusUnknown
  - Explicit process-local production coordinator durability value with the closed variants Sync or Group, no Default
    or backend-inferred/zero-argument constructor path, Memory admitted only by test-only construction, and
    outcome/commit records carrying the exact selected mode
  - Grammar/IR-v1 index derivation that always writes the canonical empty CanonicalRecord as covered_values while
    preserving generic storage conformance for covered-value epoch changes
  - Maintainer-approved format-neutral checked-plan correction that conservatively rejects a v1 mutation shape above
    4,096 index-entry deltas, 4,096 affected prefixes, 4,096 combined validation positions, or 16 MiB affected
    target/current-epoch state before plan hashing; exact runtime/storage guards remain defense in depth and are an
    internal defect if reached from a canonical checked plan
  - Mutation-affected index-epoch exhaustion that aborts before application-sequence assignment, leaves Pending
    byte-identical, returns opaque InternalDefect for the current call, stops readiness, maps queued/future work to
    CoordinatorStopped, and never enters uncertain-write CoordinatorFenced
  - Bounded executor-capacity permit between durable Started and fresh final authorization, with synchronous
    nonretroactive admission and no business work when standalone/Started append is not proven durable
  - Exact Started-to-one-selected-and-attempted-terminal behavior for new admission, pending resume, terminal replay,
    and compound bootstrap, with at most one durable terminal and none possibly visible after append failure/crash,
    including preservation of the already checked ADR-0021 target list, typed Command or ControlPlane links, and the
    approved append-failure map
  - Standalone failed/denied audit behavior after RequestContext construction, including every explicit policy Deny,
    fail-closed append handling, and no audit for ordinary allowed standard reads without an obligation
  - Unjournaled read-only execution with no command admission, persisted outcome, provenance, or application sequence
  - Exhaustive origin-specific conversion of only codec-proved ExceedsAcceptedAggregateCap into sequence-free
    CapacityUnavailable/StorageUnavailable, retaining Pending byte-for-byte with no sequence or graph, while every
    per-record, checked-sum, encoding, schema, or integrity failure remains fatal and stops readiness
  acceptance_commands:
  - cargo test -p riffdb-commit -p riffdb-idempotency
  - cargo test -p riffdb-types -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-storage-api
  - cargo test -p riffdb-commit --test command_concurrency
  - cargo test -p riffdb-commit --test lost_response_replay
  - cargo test -p riffdb-commit --test service_audit_ordering
  external_test_targets:
  - name: command_concurrency
    owner_crate: riffdb-commit
    manifest: crates/riffdb-commit/Cargo.toml
    path: ../../tests/command_semantics/command_concurrency.rs
  - name: lost_response_replay
    owner_crate: riffdb-commit
    manifest: crates/riffdb-commit/Cargo.toml
    path: ../../tests/command_semantics/lost_response_replay.rs
  - name: service_audit_ordering
    owner_crate: riffdb-commit
    manifest: crates/riffdb-commit/Cargo.toml
    path: ../../tests/service_audit/service_audit_ordering.rs
  exit_gate: Concurrent budget, lineage normalization and unknown-field preservation, zero-mutation rejection, exact
    nonzero mutation coverage, pre-sequence capacity/failpoint, event-hash, and fail-after-commit tests prove one
    atomic logical execution per idempotency identity without sequence gaps; resource expansion and zero-mutation
    fixtures prove dependency-first/catalog-current-recheck/re-derived-overflow terminalization, correct opaque
    evidence consumption, no forbidden commit-check/index derivation, and unchanged terminal replay; constructor tests
    prove every production coordinator explicitly receives Sync or Group while Memory remains test-only.
  human_review_triggers: *id001
- id: WP-110
  closure:
    status: complete
    evidence: tests/authorization/authorization_non_bypass.rs
    decision: Opaque capabilities resolve through auth-owned credential proof and deny-by-default policy with exact scope, revocation, approval, redaction, current-fact, entropy, key-custody, and dependency boundaries. Current auth, policy, workspace, and cargo-deny suites pass.
  name: Authorization and capability service
  depends_on:
  - WP-010
  - WP-070
  objective: Resolve opaque development capability tokens into deny-by-default policy decisions and redaction obligations.
  requirements:
  - ID-005
  - SEC-001
  - SEC-002
  - SEC-003
  - SEC-004
  required_adrs:
  - ADR-0004
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0011
  - ADR-0016
  - ADR-0018
  - ADR-0023
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - tests/authorization/**
  deliverables:
  - Exact reviewed dependency graph using getrandom =0.3.4 without optional features, base64 =0.22.1 with only
    alloc, and zeroize =1.8.1 with only alloc, all with default features disabled
  - Capability entropy, HMAC, canonical base64, zeroization, digest-generation, and key-custody implementations
  - Separate checked UUIDv7 bootstrap CapabilityId and capability-token entropy fills, with both retained across a
    bootstrap retry and all partially filled secret material zeroized on failure; normal create preserves only its
    caller-selected CapabilityId and retains ADR-0009's unrecoverable-token result after a lost committed response
  - Isolated bootstrap-secret generation and validation module
  - Auth-owned CredentialAuthenticator with privately constructible AuthenticatedPrincipal
  - Policy decision and obligations
  - Field/tenant filters
  - Policy-owned synchronous AuthorizationClock, value-only AuthorizedCapabilityMutationPreparation and
    TransactionCurrentCapabilityFacts, and pure TransactionCurrentCapabilityVerifier
  - Present-target revoke facts with ordinary complete-subset authorization, plus a distinct absent-target
    preparation authorized only by exact database/environment AdministerCapabilities and carrying no invented target
    grant, lifecycle, revision, principal, audience, issue-time, or expiry facts
  - One fresh transaction-current AuthorizationClock sample shared by verifier facts, issued_at or revoked_at,
    expiry validation, and the corresponding capability administration audit timestamp
  - Exhaustive service-operation-to-permission mapping and privately constructible AuthorizedProvenanceClaims
  - Verifier, clock-failure, denial, delegation, approval, and redaction tests without ownership of durable records or
    public capability results
  - Lockfile, feature-tree, direct-owner, license, and unreviewed-transitive dependency assertions
  - Redaction-safe authorization telemetry hooks
  acceptance_commands:
  - cargo test -p riffdb-auth -p riffdb-policy
  - cargo test -p riffdb-policy --test authorization_non_bypass
  - cargo deny check
  external_test_targets:
  - name: authorization_non_bypass
    owner_crate: riffdb-policy
    manifest: crates/riffdb-policy/Cargo.toml
    path: ../../tests/authorization/authorization_non_bypass.rs
  exit_gate: Revocation, scope, environment, command visibility, and field-redaction tests pass.
  human_review_triggers: *id001
- id: WP-120
  closure:
    status: complete
    evidence: tests/service/service_end_to_end.rs
    decision: All application, administration, read, projection, outbox, provenance, and health operations share API-neutral service ports, current authorization, bounded shaping, and audit orchestration. Complete service and workspace suites pass.
  name: API-neutral application service
  depends_on:
  - WP-050
  - WP-080
  - WP-100
  - WP-110
  objective: Expose transport-independent command, contract, entity, commit, provenance, projection, and health
    services.
  requirements:
  - SYS-004
  - API-001
  - POC-008
  - MCP-046
  required_adrs:
  - ADR-0004
  - ADR-0007
  - ADR-0009
  - ADR-0010
  - ADR-0012
  - ADR-0013
  - ADR-0016
  - ADR-0017
  - ADR-0018
  - ADR-0020
  - ADR-0021
  - ADR-0023
  - ADR-0025
  - ADR-0026
  - ADR-0027
  - ADR-0029
  - ADR-0030
  - ADR-0031
  - ADR-0034
  - ADR-0035
  - ADR-0036
  - ADR-0038
  - ADR-0039
  - ADR-0042
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-service/**
  - tests/service/**
  deliverables:
  - Service traits and implementations
  - Service-owned bounded pre-schema submitted-value family and exact-plan schema-directed materialization into the
    sole CanonicalRecord before hashing, policy, facts, admission, runtime, or persistence, including field ID/name,
    decimal, money, enum, list, nested-record, and historical optional-field validation
  - Combined input normalization and input-computable partition/conflict/authorization-fact preparation using the
    shared pure riffdb-invariant evaluator, mapping checked arithmetic to root ValidationCode::OutOfRange before
    admission and impossible checked-plan/input state to internal integrity
  - Exact candidate catalog-lineage limit mapping before coordinator submission; 4,097 bundles becomes one root
    ValidationCode::TooManyItems issue; more than 64 MiB exact canonical bytes or 2 MiB proof charge becomes one root
    ValidationCode::TooLong issue; all use public Validation with CorrectRequest and ordinary authenticated deployment
    audit, while catalog storage errors and startup historical-integrity failures retain their existing closed maps
  - Shared request context
  - Service-owned privately constructible and nonserializable PreBootstrapHealthContext plus closed HealthContext and
    HealthResult types used only by the unchanged Health operation during initializing validation/bootstrap before the
    durable marker
  - Exact bounded pre-bootstrap health result containing only lifecycle, liveness, and readiness, with no active-
    contract detail, commit position, component inventory, storage identity or handle, policy fact, build/start-time
    detail, extensible text, or map
  - Authorization enforcement
  - Bounded pagination and waits
  - In-process integration harness
  - Redaction-safe service telemetry and health hooks
  - Six API-neutral service traits with checked RequestContext and typed command, control-plane, authoritative-read,
    projection, outbox-status, and operational-status ports; Health alone accepts the closed authenticated-or-pre-
    bootstrap HealthContext, and no other operation accepts PreBootstrapHealthContext
  - Source-free checked RequestContext and AgentSessionId handling with no UUID generator, clock, entropy dependency,
    protocol-ID substitution, or identifier-order interpretation in the service
  - Capability create, revoke, and bootstrap service DTOs with checked principal-less bootstrap context and typed
    executor-result mapping that never receives a storage transition
  - Catalog-owned explicit capability-partition schema validation before policy, token issuance, capacity admission,
    or coordinator submission, including mixed valid/invalid scopes, exact control-plane call exclusion, bootstrap
    rejection before an active contract, and a barrier-controlled compatible-activation race
  - Absent-revoke CapabilityPreparationChanged recovery that reloads now-present complete facts, reruns current
    policy, obtains a fresh bounded executor permit, and resubmits without appending a second Started record
  - Current-policy safe points, obligation application, bounded pages/cursors/waits/streams, and deterministic fake
    cursor generation
  - Service-owned CursorMonotonicClock consumer port with explicit-tick tests, checked regression/overflow failure,
    no sleeps, no serialization, and no reuse of authentication, authorization, admission, or administration time
  - Exact-facts initial authorization, durable Started, bounded executor permit, fresh exact-facts authorization,
    synchronous admission, fully filtered and bounded result shaping, and terminal-audit-before-release orchestration
  - One-selected-and-attempted-terminal audit orchestration, with at most one durable terminal and none possibly
    visible after append failure/crash, including Denied, Failed, Cancelled, resume/replay, append-failure mapping,
    post-commit bootstrap success handling, and no conversion of audit uncertainty into command OutcomeUnknown
    before business work begins
  - Service-owned ServiceAuditInput implementation of commit-owned AdministrationAuditInputView, exposing an original
    authoritative transition sequence only inside its ControlPlane result link and never exposing a field or method
    for the new audit record's assigned sequence; plus the exact control-plane terminal matrix covering known
    new/replayed transition Succeeded with its original ControlPlane sequence, authorized typed no-transition Failed
    with None, and unknown transition OutcomeUncertain before typed release
  - Internal RetryBudgetExhausted mapping to the unchanged public ConcurrencyDeadlineExceeded error, retry action, and
    protocol-independent safe message
  - Standalone audit orchestration for every mutation and administration attempt after exact classification, every
    explicit authenticated policy Deny, and allowed standard reads only when an Allow obligation requires it, with
    bootstrap as the sole principal-less mutation and sole operation allowed to create durable service-audit records
    without a principal; pre-bootstrap Health reaches no audit executor and emits no durable audit
  - Stream-establishment audit only; later per-item reauthorization failure closes the stream with bounded telemetry
    and no additional durable audit record
  - Stage-two validation of every affected entity key in GetCommit, ScanCommits, SubscribeToCommits, and
    TraceProvenance against each snapshot's exact historical lineage/version before release; catalog waits precede the
    final policy check, page lookups are deduplicated, and failure publishes no cursor, item, advanced frontier, or
    success audit terminal
  - Deterministic commit-subscription coverage for post-establishment cancellation, deadline, lifetime, policy denial,
    scan gap, lower-source shutdown, stale notification, contiguous catch-up/live delivery, resume, the 128-subscriber
    limit, and the service-owned exact 256-item lower-source buffer contract, with no sleep synchronization
  - Exhaustive ADR-0021 request-to-target mapping for all 22 operations, with identical Started/terminal lists and
    tests proving authorizing capabilities, result links, traversed objects, and returned rows are never inferred
  - Policy-filtered command descriptors carrying the compiler-owned ADR-0020 name verbatim, with no service-side
    normalization, derivation, collision repair, or transport-specific substitute identity
  - Service read/filter integration proving migrated V2 index rows participate in the accepted lower partition filter
    and return-time reauthorization, with migration or crossed startup outcomes never admitted as readiness and the
    service receiving no branded driver value, instruction, or backend helper
  acceptance_commands:
  - cargo test -p riffdb-service
  - cargo test -p riffdb-service --test service_end_to_end
  - cargo test -p riffdb-service --test service_audit_orchestration
  external_test_targets:
  - name: service_end_to_end
    owner_crate: riffdb-service
    manifest: crates/riffdb-service/Cargo.toml
    path: ../../tests/service/service_end_to_end.rs
  - name: service_audit_orchestration
    owner_crate: riffdb-service
    manifest: crates/riffdb-service/Cargo.toml
    path: ../../tests/service/service_audit_orchestration.rs
  exit_gate: All operations pass through shared service code with no transport-specific mutation path or command-name
    derivation; except for the exact pre-marker Health context, they pass through current policy. Tests prove the
    pre-bootstrap result has only lifecycle/liveness/readiness and reaches no policy, storage, or durable audit, that
    no new principal-less Health call is admitted after the marker, and that catalog count/byte/proof limit fixtures
    produce the exact root validation codes and audit behavior before any coordinator submission; every public commit
    or provenance entity key is schema-valid and every commit-stream terminal path preserves the last visible frontier.
  human_review_triggers: *id001
- id: WP-125
  closure:
    status: complete
    evidence: examples/budget-comparison/tests/service_comparison.rs
    decision: The storage-free service adapter and PostgreSQL adapter satisfy the same canonical workload and observation oracle. The formerly drifted nested workspace, lock, safety inventory, full test suite, formatting, and Clippy are green on current HEAD.
  name: Service comparison adapter
  depends_on:
  - WP-045
  - WP-120
  objective: Add the first valid RiffDB budget comparison adapter through the API-neutral service and run the same
    workload oracle used by PostgreSQL.
  requirements:
  - API-001
  - POC-001
  - POC-002
  - POC-003
  - POC-004
  - POC-005
  - POC-008
  required_adrs:
  - ADR-0007
  - ADR-0031
  - ADR-0039
  - ADR-0042
  allowed_paths:
  - .github/workflows/budget-comparison.yml
  - examples/budget-comparison/Cargo.toml
  - examples/budget-comparison/Cargo.lock
  - examples/budget-comparison/riffdb-service/**
  - examples/budget-comparison/tests/service_*
  deliverables:
  - RiffDB service adapter with no storage access
  - Parameterized cross-backend semantic conformance suite
  - Correctness preflight for comparison workloads
  - V2-only startup compatibility fixture that joins only Ready with Clean and fails closed on either migration or a
    crossed catalog/storage startup outcome while treating the identity-only concrete port as opaque and gaining no
    migration ownership, branded driver value, instruction, or backend helper
  acceptance_commands:
  - cargo test --manifest-path examples/budget-comparison/Cargo.toml --workspace service_comparison
  exit_gate: The service adapter and PostgreSQL adapter satisfy their declared guarantee profiles under the same
    workload and normalized observation oracle.
  human_review_triggers: *id001
- id: WP-127
  closure:
    status: complete
    evidence: fixtures/proto
    decision: Every supported public operation has bounded structurally validated Protobuf messages, descriptors, schema hashes, golden bytes, UUID checks, pages, cursors, errors, and compatibility fixtures. Generated-proto and workspace tests pass.
  name: Public Protobuf API completion
  depends_on:
  - WP-020
  - WP-120
  objective: Complete the accepted phase-zero public Protobuf messages after API-neutral service DTOs stabilize and
    before gRPC adapters consume them.
  requirements:
  - API-001
  - VAL-003
  - POC-006
  required_adrs:
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0010
  - ADR-0011
  - ADR-0012
  - ADR-0013
  - ADR-0017
  - ADR-0018
  - ADR-0027
  - ADR-0028
  - ADR-0029
  - ADR-0030
  - ADR-0031
  - ADR-0035
  - ADR-0036
  allowed_paths:
  - Cargo.lock
  - proto/**
  - crates/riffdb-proto/**
  - fixtures/proto/**
  - scripts/generate-proto*
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - fuzz/fuzz_targets/proto_public.rs
  deliverables:
  - Completed public phase-zero message fields for every WP-130-supported RPC without changing accepted service or
    RPC names
  - EXECUTED_READ_ONLY = 3 plus its exact zero/empty wire sentinels and structural consistency validation
  - Public pages and cursors, command results, capability administration results and create mode, health, streams,
    and every typed projection query/status/frontier/lifecycle/failure/unavailable result
  - One unchanged Health RPC with a closed discriminated response boundary whose pre-bootstrap variant contains
    exactly lifecycle/liveness/readiness and cannot co-occur with active-contract detail, commit position, component
    inventory, storage identity, policy fact, build/start-time detail, extensible text, or map, plus the detailed
    authenticated post-bootstrap variant
  - Compatibility preservation and integration of the WP-010 CommandExecutionFailed public error symbols while all
    remaining public fields are assigned and frozen here
  - Descriptors, schema hashes, wire-structural checks, golden bytes, and client fixtures
  - Structural UUIDv7 validation for every public shared-identifier field, including wrong-version, wrong-variant,
    wrong-length, and ADR-0018 golden vectors without adding a wire-level generation policy
  - Allocation-free bounded stage-one envelope validation for every public entity, index-entry, and partition-key
    position, with fixtures that distinguish envelope validity from catalog-owned schema validity
  - No durable record schema and no service-to-Protobuf semantic conversion code
  acceptance_commands:
  - cargo test -p riffdb-proto
  - ./scripts/generate-proto --check
  - cargo +nightly-2026-07-12 fuzz run proto_public -- -max_len=4194304 -max_total_time=30
  exit_gate: Every supported public operation has bounded, structurally validated, reproducibly generated wire
    artifacts before transport conversion begins.
  human_review_triggers: *id001
- id: WP-130
  closure:
    status: complete
    evidence: tests/grpc/p1_riffdbd_restart.rs
    decision: Tonic ingress, typed Rust client, exact public-error carriage, authentication, limits, retry mapping, server composition, generated client, and restart readiness all use the shared service boundary. Current gRPC, client, and server suites pass.
  name: gRPC server and Rust SDK
  depends_on:
  - WP-020
  - WP-120
  - WP-127
  objective: Expose the public application and administration protocol through Tonic and a typed Rust client, and
    compose the minimum production riffdbd required for the first runnable P1 restart checkpoint.
  requirements:
  - SYS-001
  - SYS-004
  - ID-001
  - ID-005
  - STO-001
  - STO-012
  - REC-001
  - REC-002
  - API-001
  - POC-001
  - POC-004
  - POC-008
  - POC-009
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0010
  - ADR-0012
  - ADR-0013
  - ADR-0017
  - ADR-0018
  - ADR-0019
  - ADR-0020
  - ADR-0021
  - ADR-0023
  - ADR-0025
  - ADR-0026
  - ADR-0027
  - ADR-0028
  - ADR-0029
  - ADR-0030
  - ADR-0031
  - ADR-0032
  - ADR-0033
  - ADR-0034
  - ADR-0035
  - ADR-0036
  - ADR-0037
  - ADR-0038
  - ADR-0039
  - ADR-0042
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-api-grpc/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-server/**
  - tests/grpc/**
  - scripts/generate-rust-sdk
  deliverables:
  - Tonic services and interceptors
  - Exact Tonic 0.14.6 default-disabled feature graph with API production codegen plus tonic-prost, API build-only
    tonic-prost-build transport, client channel/codegen plus tonic-prost, and server router/server; the one locked
    base64 0.22.1 may gain std only through this graph while riffdb-auth remains its sole direct first-party owner,
    with no TLS, compression, transport-wide production feature, cryptography, or second base64 version
  - Authentication and request limits
  - Rust transport client
  - Direct default-feature-disabled riffdb-errors and riffdb-types dependencies solely for the single checked
    PublicError and foundational public identifier/value owners, with architecture tests forbidding every
    authority-bearing server crate and server-only API feature
  - Fresh client RequestId plus CapabilityId and AgentSessionId convenience generation through reviewed direct
    getrandom 0.3.4 sources; every retry uses a fresh RequestId and UUID ordering is never a semantic signal
  - Generated ergonomic command module with a fixture-scoped deterministic emitter and byte-currentness test
  - Error and retry mapping
  - Exact PublicError carriage using Tonic Status::with_details with the bounded riffdb.v1.PublicError bytes directly
    in grpc-status-details-bin, canonical ErrorClass-to-gRPC status, only the static safe grpc-message, no
    google.rpc.Status/Any/second envelope, and closed client protocol failures for missing, malformed, oversized,
    unknown, status-inconsistent, or message-inconsistent details
  - Conformance evidence that retry-budget exhaustion preserves the existing public ConcurrencyDeadlineExceeded code,
    safe message, retry action, and gRPC DEADLINE_EXCEEDED mapping
  - Projection protocol mapping against an injected application-service stub
  - gRPC-path telemetry, limits, and server lifecycle hooks
  - Credential handoff only through CredentialAuthenticator with exact bootstrap metadata extraction and normal
    bearer handling
  - Total service-to-Protobuf conversion, including status-dependent Execute validation, sentinel-to-absence
    mapping, public errors, streams, capability results, and projection variants
  - Architecture tests proving no direct policy, commit, storage, or lower semantic bypass
  - One private checked server SystemUuidV7Source primitive plus hosted RequestId, IncidentId, DatabaseId-candidate,
    and ProvenanceIdSource consumer wrappers, with one clock conversion and entropy fill per candidate, source-failure
    propagation, and no process-global generator state or uniqueness registry
  - Minimal production riffdbd component graph wiring redb, catalog, commit coordinator, authentication, policy,
    API-neutral service, and Tonic exactly once, with no storage or policy bypass and no MCP, outbox dispatcher,
    projection worker, observability aggregation, or derived-health semantics
  - P1 component graph explicitly constructs the production coordinator with the code-level Sync value and no
    constructor/backend default; it exposes no operator durability selector, admits no production Memory path, and
    keeps Group server-disabled until its existing scheduling, fairness, latency, crash-evidence, and human-review gate
  - Initialization composition only through the commit-owned DatabaseInitializationExecutor; the server invokes no
    storage mutation transition directly, does not request a DatabaseId candidate on reopen, and preserves the
    durable winner across restart
  - Four disjoint concrete wall-clock providers for auth-owned authentication time, policy-owned AuthorizationClock,
    and commit-owned AdmissionClock and AdministrationClock; one implementation may be shared, but consumer ports and
    call sites remain distinct and bootstrap shares exactly one AdministrationClock sample
  - Production CursorTokenGenerator and CursorMonotonicClock providers, including reviewed cursor entropy and one
    private std::time::Instant origin, with restart invalidation, checked regression/overflow, and no test sleeps
  - Production capability-token and idempotency digest-key providers, cross-namespace key-material reuse rejection,
    and nonsecret readable scheme/key inventories supplied to fail-closed startup validation
  - Staged startup gate that performs identity initialization first, drives redb structural evidence to exact end,
    obtains catalog-owned opaque same-session ValidatedCatalogHistory, and only then activates the matching
    StructurallyOpened dormant ports; the catalog history includes ADR-0030 validation of every qualifying explicit
    capability partition, while StructurallyOpened carries the exact same-snapshot RetainedMetadataV1 used to check
    database identity, active-pointer agreement, bootstrap lifecycle, and allocator capacity; no catalog proof crosses
    the storage trait, and any mismatch leaves the process unready
  - Migration startup composition that only joins matching CatalogIndexMigrationContext and the privately constructed
    RedbStartupIndexMigrationPort, invokes the catalog-owned consuming driver, and receives dormant unopened state;
    server source cannot import or receive catalog-branded requests/responses, instructions, batches, completions, or
    concrete backend page/read/apply/end helpers
  - Exact 256-item lower commit-subscription buffers and retained supervision handles for every accepted service task;
    buffer overflow remains resumable and any accepted task panic or disappearance stops routing rather than silently
    dropping work
  - P1 lifecycle exposing only the unchanged Health RPC's restricted principal-less lifecycle/liveness/readiness view
    during initializing validation/bootstrap and one-time loopback bootstrap after validation while the marker is
    absent; the server constructs no pre-bootstrap health context after the marker and requires normal credential
    handoff, RequestContext, current policy, and obligations for Health in deployment-required, ready, and later states
  - Bootstrap remains the sole principal-less mutation and sole operation allowed to create durable service-audit
    records without a principal; pre-bootstrap Health has no policy or storage access and emits no durable audit
  - Authenticated deployment after bootstrap and general command/read readiness only after a valid active catalog;
    exact bootstrap replay uses the caller-retained credential, returns the existing capability result without echoing
    or reissuing the token, and appends only the required linked audit records
  - Real child-process temporary-redb restart test over public gRPC covering bootstrap, budget deployment,
    CreateBudget, entity read, AllocateBudget, outcome resolution, clean stop, reopen, repeated entity/outcome reads,
    and retained bootstrap replay without an in-process storage or service shortcut
  - Restart-from-V1 integration proving the exclusive migration completes before readiness, a fresh V2-only startup
    pass joins Ready with Clean, migrated rows participate in partition-filtered reads and return-time authorization,
    and no transport or server path becomes a migration constructor or backend transition consumer
  acceptance_commands:
  - cargo test -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server
  - cargo test -p riffdb-api-grpc --features server,client --test grpc_end_to_end
  - cargo test -p riffdb-server --test p1_riffdbd_restart
  external_test_targets:
  - name: grpc_end_to_end
    owner_crate: riffdb-api-grpc
    manifest: crates/riffdb-api-grpc/Cargo.toml
    path: ../../tests/grpc/grpc_end_to_end.rs
  - name: p1_riffdbd_restart
    owner_crate: riffdb-server
    manifest: crates/riffdb-server/Cargo.toml
    path: ../../tests/grpc/p1_riffdbd_restart.rs
  exit_gate: A real riffdbd process reaches staged readiness on redb and the bootstrap/deploy/budget command, outcome,
    entity-read, clean-restart, durable reread, and retained-bootstrap-replay sequence works through public gRPC;
    coordinated lifecycle tests prove the Health RPC exposes only the restricted unaudited three-field result before
    the marker and rejects new principal-less admission afterward in favor of authenticated current-policy handling;
    every projection result variant maps correctly against an injected service stub, and error conformance proves
    exact direct PublicError detail bytes plus fail-closed status/message/detail validation at every boundary case;
    component-graph tests prove explicit code-level Sync construction with no constructor/backend default or
    operator-facing POC durability selector, and startup cannot bypass capability-partition history validation or
    accepted-task supervision.
  human_review_triggers: *id001
- id: WP-135
  closure:
    status: complete
    evidence: examples/budget-comparison/riffdb-grpc
    decision: The public SDK adapter executes the frozen workload through the generated client and shared daemon without storage access. Its process, fixture, protocol-boundary, and semantic-oracle tests pass in the repaired nested workspace.
  name: Public SDK comparison adapter
  depends_on:
  - WP-125
  - WP-130
  - WP-137
  objective: Make the Rust SDK and public gRPC path the canonical RiffDB adapter for the long-lived comparison
    workload and benchmark runner.
  requirements:
  - API-001
  - OUT-001
  - POC-001
  - POC-002
  - POC-008
  required_adrs:
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0011
  - ADR-0015
  - ADR-0018
  - ADR-0028
  - ADR-0031
  - ADR-0037
  - ADR-0040
  - ADR-0041
  allowed_paths:
  - examples/budget-comparison/Cargo.toml
  - examples/budget-comparison/Cargo.lock
  - examples/budget-comparison/riffdb-grpc/**
  - examples/budget-comparison/tests/public_*
  deliverables:
  - Public Rust SDK and gRPC comparison adapter
  - Same-key uncertainty-recovery comparison scenario
  - Process-runner correctness preflight and normalized observations
  - Nested-workspace package riffdb-budget-comparison-riffdb-grpc with a public-only adapter library and the named
    riffdb-budget-public evidence binary; normal dependencies are limited to the comparison core, public client,
    public protocol, foundational values, and the exact reviewed Tokio/Tonic rows
  - Root comparison workspace registration of riffdb-grpc and the explicit public_comparison integration-test target
    despite autotests=false, with the exact package library, binary, three fixture files, and process-harness paths
  - Closed riffdb.budget.public-run/v1 eight-argument subprocess protocol, protected credential-file loading through
    WP-137, three fresh-database cases, exact one-line success JSON, fixed failure stderr/exit behavior, no ambient
    configuration, and checked 8,192-byte argv plus endpoint/path/output bounds
  - Exact success, checked-error, and invalid-invocation process fixtures and discarded-response same-key replay
    evidence; actual post-commit TCP-loss/restart uncertainty remains WP-190/WP-200 evidence
  acceptance_commands:
  - cargo test --manifest-path examples/budget-comparison/Cargo.toml --workspace public_comparison
  - cargo build -p riffdb-server --bin riffdbd --target-dir target/wp135-root
  - cargo build --manifest-path examples/budget-comparison/Cargo.toml -p riffdb-budget-comparison-riffdb-grpc --bin
    riffdb-budget-public --target-dir target/wp135-comparison
  - RIFFDB_BUDGET_RIFFDBD_BIN="$PWD/target/wp135-root/debug/riffdbd" RIFFDB_BUDGET_RUNNER_BIN="$PWD/target/wp135-comparison/debug/riffdb-budget-public"
    cargo test --manifest-path examples/budget-comparison/Cargo.toml -p riffdb-budget-comparison --test
    public_comparison --target-dir target/wp135-comparison -- --test-threads=1
  exit_gate: The canonical RiffDB comparison uses only public APIs and passes the same workload oracle before any
    comparative benchmark can run.
  human_review_triggers: *id001
- id: WP-137
  closure:
    status: complete
    evidence: examples/budget-comparison/tests/public_comparison.rs
    decision: Public gRPC and MCP paths preserve fresh request identity, replay recovery, audit/provenance observations, checked errors, and shared semantics. Public-comparison and transport-conformance tests pass.
  name: Public gRPC MCP parity bridge
  depends_on:
  - WP-130
  objective: Freeze and implement the additive public protocol, API-neutral discovery/locator DTOs, gRPC conversions,
    and Rust-client surfaces needed by MCP stdio, the public comparison runner, and the CLI without introducing a
    transport-specific authority path.
  requirements:
  - API-001
  - ID-005
  - MCP-001
  - MCP-010
  - MCP-011
  - MCP-030
  - MCP-031
  - MCP-032
  - MCP-033
  - MCP-040
  - MCP-041
  - MCP-043
  - MCP-045
  - MCP-046
  - MCP-047
  - MCP-048
  - POC-001
  - POC-004
  - POC-007
  - POC-008
  required_adrs:
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0008
  - ADR-0009
  - ADR-0013
  - ADR-0018
  - ADR-0020
  - ADR-0024
  - ADR-0026
  - ADR-0027
  - ADR-0028
  - ADR-0037
  - ADR-0040
  - ADR-0041
  - ADR-0046
  allowed_paths:
  - proto/riffdb/v1/**
  - crates/riffdb-proto/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-auth/tests/architecture.rs
  - crates/riffdb-service/schema/riffdb.command-operation-envelope-v1.schema.json
  - crates/riffdb-service/schema/riffdb.command-get-outcome-result-v1.schema.json
  - crates/riffdb-service/Cargo.toml
  - crates/riffdb-service/src/cursor.rs
  - crates/riffdb-service/src/contract_operations.rs
  - crates/riffdb-service/src/dto.rs
  - crates/riffdb-service/src/command_operations.rs
  - crates/riffdb-service/src/query_discovery_operations.rs
  - crates/riffdb-service/src/response.rs
  - crates/riffdb-service/src/submitted.rs
  - crates/riffdb-server/src/lib.rs
  - crates/riffdb-server/src/lifecycle.rs
  - crates/riffdb-server/src/read_adapters.rs
  - crates/riffdb-server/src/process_graph.rs
  - crates/riffdb-server/src/server_generation.rs
  - tests/grpc/**
  - tests/service/**
  - fixtures/proto/**
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - fuzz/fuzz_targets/proto_public.rs
  - scripts/generate-proto
  - Cargo.lock
  deliverables:
  - Additive five-service, 22-RPC public descriptor with exactly six new unary operations and one total streaming RPC,
    preserving every prior symbol, field number/type/presence, enum value, RPC name, and streaming shape
  - ADR-0046 additive Decimal precision and bounded ContractCompatibilitySummary fields, exact old/new presence
    compatibility, generated fixtures, decoder fuzzing, and total public-client conversion without durable or
    compiler-format changes
  - Exact name-only enum submitted form plus service-owned schema-bound declared-outcome presentation, with public
    field/enum names populated only from the exact checked historical schema and no adapter-side inference
  - Language-neutral messages, tags, enums, full/compact representations, ResourceDiscoveryKind, conditional catalog
    fences, bounded pages/cursors, exact PublicError carriage, compatibility goldens, and decoder fuzz coverage
  - API-neutral policy-filtered command, contract/resource, provenance, outbox, and projection discovery/read DTOs,
    including representation/kind-bound cursors, pre-pagination resource-kind filtering, and audited
    CatalogUnchanged safe-point handling without MCP types or adapter-side filtering
  - Human-accepted canonical operation-envelope and GetOutcome-result Draft 2020-12 sources plus generated IDs,
    lengths, hashes, identity/full catalog fixtures, and representative compiler-outcome composition golden, with no
    compiler artifact or bundle-hash change
  - Service-owned authoritative outcome-locator tuple encoding/decoding and raw-key-or-locator resolution through one
    authorized point-lookup path; Proto owns only structural locator validation and adapters receive no raw digest key
  - One private 16-byte server-generation entropy fill at startup, shared by gRPC and HTTP, with no clock, retry,
    fallback, storage meaning, or alternate source and fail-closed startup on source failure
  - Total gRPC conversion and public-client round trips for all six operations, exact response charging through the
    4,194,304-byte ledger, legacy outcome-locator compatibility, and strict new-server mandatory emission
  - Public-client checked error views and three operation-specific execute/bootstrap-create/normal-create retry
    helpers with fresh RequestIds, stable semantic identity, bounded attempts, and no generic arbitrary-RPC helper
  - Linux protected normal-bearer loader returning only redacted BearerCredential, exact object/owner/mode/race and
    byte bounds, zeroized staging, non-exposing presentation comparison, and no client auth/base64 dependency
  - Staged exact dependency-owner architecture registry for base64 and zeroize, with only the accepted owner sets and
    no auth production/manifest change, service/storage/policy/commit bypass, or server-only stdio dependency
  acceptance_commands:
  - cargo test -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust
  - cargo test -p riffdb-api-grpc --features server,client --test grpc_end_to_end
  - cargo test -p riffdb-service -p riffdb-server
  - ./scripts/generate-proto --check
  - cargo +nightly-2026-07-12 fuzz run proto_public -- -max_len=4194304 -max_total_time=30
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-service -p riffdb-server
    --all-targets --all-features -- -D warnings
  - cargo doc -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-service -p riffdb-server --no-deps
  exit_gate: Exact descriptor, schema, and wire artifacts reproduce; every new branch converts and round-trips through
    public clients within bounds; raw-key and locator outcome lookup share one authorized point path; operation-
    specific retry and protected-credential evidence passes; and architecture checks prove stdio needs no private,
    authority-bearing, or server dependency.
  human_review_triggers: *id001
- id: WP-139
  closure:
    status: complete
    evidence: examples/budget-comparison/fixtures/safety/report-v1.json
    decision: The safety comparison distinguishes RiffDB's supported surface from PostgreSQL counterexamples and excludes unsafe variants from benchmark claims. Architecture and live safety-evidence tests pass.
  name: Budget safety counterexample evidence
  depends_on:
  - WP-045
  - WP-135
  objective: Make the contract-first enforcement value executable by contrasting four isolated PostgreSQL negative
    controls with the same Budget behavior through the public RiffDB Rust SDK and gRPC path, without contaminating
    the correct comparison or benchmark baseline.
  requirements:
  - SYS-004
  - DSL-009
  - OUT-001
  - OUT-002
  - OUT-004
  - TXN-001
  - API-001
  - POC-002
  - POC-003
  - POC-005
  - POC-008
  required_adrs:
  - ADR-0001
  - ADR-0002
  - ADR-0003
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0011
  - ADR-0015
  - ADR-0018
  - ADR-0024
  - ADR-0027
  - ADR-0028
  - ADR-0031
  - ADR-0033
  - ADR-0035
  - ADR-0037
  - ADR-0040
  - ADR-0041
  - ADR-0045
  allowed_paths:
  - examples/budget-comparison/Cargo.toml
  - examples/budget-comparison/Cargo.lock
  - examples/budget-comparison/README.md
  - examples/budget-comparison/safety-evidence/**
  - examples/budget-comparison/fixtures/safety/**
  - examples/budget-comparison/riffdb-grpc/src/lib.rs
  - examples/budget-comparison/tests/safety_*
  - examples/budget-comparison/tests/postgres_isolation.rs
  - .github/workflows/budget-comparison.yml
  - scripts/budget-safety-demo
  deliverables:
  - Separate safe Rust package riffdb-budget-safety-evidence and non-shipped riffdb-budget-safety runner
  - Exact riffdb.budget.safety-evidence/v1 typed report, pretty fixture, compact JSONL fixture, invocation grammar,
    bounds, exit codes, and redaction-safe failure bytes from ADR-0045
  - Deterministically scheduled PostgreSQL lost-update negative control plus direct-DML precondition bypass,
    duplicate discarded-response retry, and same-key/different-input negative controls
  - Public RiffDB contrasts using only normal Rust SDK/gRPC application operations after the existing bounded WP-135
    test-only bootstrap provisioning exception
  - Exact commit observations for declared outcomes, affected entities, durable events, replay identity, outcome
    resolution, mismatch error shape, and exact-end post-mismatch application frontier
  - Architecture evidence that the canonical PostgreSQL adapter/profile and frozen riffdb.budget.public-run/v1
    runner remain unchanged, unsafe controls cannot enter a benchmark target, and PostgreSQL remains outside the
    production workspace
  - Corrected actual-membership assertion replacing the existing comment-matching false positive
  - Required-live PostgreSQL 18.4 and fresh-riffdbd process evidence plus fail-closed scripts/budget-safety-demo
  acceptance_commands:
  - cargo test --manifest-path examples/budget-comparison/Cargo.toml --workspace
  - cargo run --manifest-path examples/budget-comparison/Cargo.toml -p riffdb-budget-safety-evidence --bin
    budget-safety-fixtures -- --check
  - cargo build -p riffdb-server --bin riffdbd --target-dir target/wp139-root
  - cargo build --manifest-path examples/budget-comparison/Cargo.toml -p riffdb-budget-safety-evidence --bin
    riffdb-budget-safety --target-dir target/wp139-comparison
  - RIFFDB_BUDGET_POSTGRES_REQUIRED=1 RIFFDB_BUDGET_RIFFDBD_BIN="$PWD/target/wp139-root/debug/riffdbd"
    RIFFDB_BUDGET_SAFETY_BIN="$PWD/target/wp139-comparison/debug/riffdb-budget-safety" cargo test
    --manifest-path examples/budget-comparison/Cargo.toml -p riffdb-budget-comparison --test safety_evidence
    --target-dir target/wp139-comparison -- --test-threads=1
  - cargo fmt --manifest-path examples/budget-comparison/Cargo.toml --all -- --check
  - cargo clippy --manifest-path examples/budget-comparison/Cargo.toml --workspace --all-targets --all-features --
    -D warnings
  - RUSTDOCFLAGS="-D warnings" cargo doc --manifest-path examples/budget-comparison/Cargo.toml --workspace --no-deps
  - cargo deny --manifest-path examples/budget-comparison/Cargo.toml check
  exit_gate: All four exact live contrasts and report fixtures pass; PostgreSQL negative controls are isolated from
    the correct adapter and every benchmark path; the existing public runner remains byte-stable; skipped live
    evidence fails required mode; and no product binary, production dependency, contract, public/durable interface,
    or P0/P1/P2 gate membership changes.
  human_review_triggers: *id001
- id: WP-140
  closure:
    status: complete
    evidence: docs/mcp/agent-cookbook.md
    decision: Native MCP exposes the same authorized application service without storage or policy bypass, with generated schemas, bounded resources, credential-safe transport, and conformance coverage. MCP and workspace suites pass.
  name: Native MCP interface
  depends_on:
  - WP-040
  - WP-120
  - WP-130
  - WP-137
  objective: Expose authorized contract commands as dynamic MCP tools and database context as policy-filtered resources
    through stdio and Streamable HTTP.
  requirements:
  - API-001
  - MCP-001
  - MCP-010
  - MCP-011
  - MCP-020
  - MCP-021
  - MCP-022
  - MCP-023
  - MCP-024
  - MCP-030
  - MCP-031
  - MCP-032
  - MCP-033
  - MCP-040
  - MCP-041
  - MCP-042
  - MCP-043
  - MCP-044
  - MCP-045
  - MCP-046
  - MCP-047
  - MCP-048
  - MCP-049
  - POC-007
  - POC-008
  required_adrs:
  - ADR-0002
  - ADR-0007
  - ADR-0008
  - ADR-0009
  - ADR-0010
  - ADR-0012
  - ADR-0013
  - ADR-0017
  - ADR-0018
  - ADR-0020
  - ADR-0024
  - ADR-0026
  - ADR-0027
  - ADR-0028
  - ADR-0037
  - ADR-0040
  - ADR-0041
  - ADR-0043
  - ADR-0044
  - ADR-0046
  - ADR-0047
  - ADR-0048
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-api-mcp/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-auth/src/authenticator.rs
  - crates/riffdb-auth/tests/architecture.rs
  - crates/riffdb-service/src/context.rs
  - crates/riffdb-service/tests/architecture.rs
  - tests/mcp/**
  - scripts/mcp-inspector-smoke
  deliverables:
  - rmcp adapter boundary
  - Stdio server and loopback Streamable HTTP
  - Generated command tools with input/output schemas
  - Fixed admin/read tools
  - Resources, subscriptions, list-change notifications
  - Progress, cancellation, pagination, security limits
  - Correct active-version links, bounded compatibility metadata, command-plan schemas, projection lag, and factual
    generated command documentation/examples validated against the compiler-owned schemas
  - Hosted MCP RequestId creation through the injected server source, distinct from MCP JSON-RPC identifiers and
    progress tokens; v1 has no AgentSessionId carrier, and any future externally supplied value requires a reviewed
    carrier plus the shared UUIDv7 validation contract
  - Service-owned hosted-MCP RequestContext construction with fixed McpHttp ingress, exactly five absent claims, no
    policy vocabulary exposed to the adapter, and no MCP/session-derived request, agent-session, claim, or trace
    identity
  - Redaction-safe protocol telemetry and server registration hooks
  - One ungated hard tracing-target predicate, closed safe-event vocabulary, non-reloadable stdio installation, and
    hosted registration hook that suppress exact rmcp and rmcp descendant targets regardless of user directives
  - Auth-owned non-Clone/nonserializable redacted RetainedOpaqueCredential in the exact approved auth source, backed
    by the existing Zeroizing 43-byte buffer, accepting any at-most-43-byte bearer-stripped slice by bounded copy
    only, and exposing credential data solely as an OpaqueCredential borrow
  - MCP HTTP credential handoff using RetainedOpaqueCredential only to borrow into CredentialAuthenticator, with no
    helper syntax/decode/auth decision, retained session credential, local authentication authority, or change to
    stdio-over-gRPC semantic parity
  - Service-only invocation and discovery with no bootstrap discovery/invocation and durable authorization/audit
    conformance evidence
  - Discovery and invocation dispatch using the compiler-owned ADR-0020 command tool name verbatim, with no adapter
    normalization or suffixing and fail-closed stale, unknown, or unauthorized name handling
  - Exact MCP 2025-11-25 initialization and pinned rmcp 2.2.0 boundary with default-empty crate features, common
    protocol/presentation code, stdio-only transport-io, HTTP-only optional auth/service edges, and no client, TLS,
    OAuth, sampling, roots, elicitation, task, prompt, or unrelated transport feature
  - Exact unconditional foundational types/errors and tracing rows, one checked RequestId source and common
    PublicError renderer, exact fmt-only stdio subscriber row, and a current-thread stdio Tokio runtime
  - One common MCP schema composer and renderer for HTTP service DTOs and stdio public-gRPC DTOs, proving byte-
    identical authorized catalogs/results while stdio has no auth, service, server, storage, policy, runtime, commit,
    catalog, or gRPC-server dependency
  - Human-accepted 27-source fixed-tool schema registry and resource/template registry with exact source paths, IDs,
    lengths, hashes, annotations, URI/MIME mappings, list-surface membership, subscription flags, and generated-
    artifact reproducibility; compiler-owned dynamic schemas are mechanically composed without drift
  - Exact canonical resource locators, outcome links, lowercase fixed-width cursor presentation, point lookup,
    existence-blind two-phase disclosure, pagination, compact/full observation, and catalog/resource notification
    behavior under the accepted item and byte ledgers
  - Stateful loopback-only Streamable HTTP route with strict authority/origin/protocol/auth/session ordering, wrapper-
    owned pre-initialize ping, custom bounded Pending/Active SessionManager, capability binding, rate/in-flight limits,
    and exact 1-MiB request/4-MiB response boundaries
  - Hosted-only post-authentication rate admission keyed by authenticated principal, policy tenant, and exact target;
    stdio derives no local principal/tenant and remains subject to normal gRPC/service admission
  - Production stdio credential-file flow through WP-137's protected public-client loader and HTTP credential handoff
    only through CredentialAuthenticator, with no local bearer decode, raw credential exposure, or policy bypass
  - Progress, cancellation, observer/subscription bounds, list-change/resource-update coalescing, five-second polling,
    idle/lifetime expiry, reconnect/restart fences, redaction canaries, and protocol-only stdout
  acceptance_commands:
  - cargo test -p riffdb-api-mcp --no-default-features
  - cargo test -p riffdb-api-mcp --no-default-features --features stdio
  - cargo test -p riffdb-api-mcp --no-default-features --features streamable-http
  - cargo test -p riffdb-service -p riffdb-auth -p riffdb-api-mcp -p riffdb-mcp-stdio --all-features
  - cargo test -p riffdb-api-mcp --all-features --test mcp_conformance
  - ./scripts/mcp-inspector-smoke
  - cargo deny check
  external_test_targets:
  - name: mcp_conformance
    owner_crate: riffdb-api-mcp
    manifest: crates/riffdb-api-mcp/Cargo.toml
    path: ../../tests/mcp/mcp_conformance.rs
  exit_gate: Auth architecture tests freeze the exact retained-credential ownership, zeroizing bound, opaque-only
    accessor, and unchanged dependency-owner set; service architecture tests freeze closed hosted-MCP context
    construction with truthful ingress and empty claims; authorized dynamic tool catalogs expose only exact compiled
    ADR-0020 names; structured results, resource reads, stale-name denial, progress, cancellation, hard telemetry
    suppression, and both adapter transports pass against the shared service harness with every HTTP authentication
    decision delegated to CredentialAuthenticator.
  human_review_triggers: *id001
- id: WP-150
  closure:
    status: complete
    evidence: scripts/demo
    decision: The CLI owns install, bootstrap, deploy, query, command, backup, and demo workflows over public services with exact credentials, diagnostics, and no direct storage path. CLI and generated-reference suites pass; the later WP-200 evidence refresh remains separately open.
  name: CLI and local developer flow
  depends_on:
  - WP-130
  - WP-135
  - WP-137
  objective: Provide an operator and demo CLI that uses public APIs rather than internal storage access.
  requirements:
  - API-001
  - ID-005
  - OUT-001
  - POC-001
  - POC-008
  - POC-010
  required_adrs:
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0011
  - ADR-0018
  - ADR-0028
  - ADR-0031
  - ADR-0037
  - ADR-0040
  - ADR-0041
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-cli/**
  - examples/**
  - scripts/demo*
  deliverables:
  - Contract validate/deploy commands
  - Command execute and outcome lookup
  - Entity/commit/projection inspection
  - Capability administration
  - JSON and human output
  - Offline bootstrap credential generator with exact stdin/protected-file flow and public loopback-gRPC invocation
  - Retry flow that retains the bootstrap CapabilityId and token after an uncertain response, submits a fresh
    RequestId, and resolves the original operation before replacement; normal create retains only its caller-selected
    CapabilityId and surfaces AlreadyCreatedTokenUnavailable after a lost committed response
  - Crash-safe retained credential creation with successful file sync, close, containing-directory sync, and
    revalidation before any bootstrap RPC; unsupported or failed synchronization aborts locally
  - Module-boundary architecture tests limiting the CLI auth dependency to riffdb-auth::bootstrap_secret
  - Budget-comparison sources owned by WP-045, WP-125, and WP-135 are read-only to WP-150 until WP-135 merges; demo
    packaging may then add integration without rewriting their manifest, lockfile, adapters, fixtures, or guarantee
    profiles unless a separately approved owner-package amendment permits it
  - Human-accepted interface-only checkpoint freezing the complete v1 command grammar, stable command identities,
    configuration/precedence table, exit codes, closed local/public error DTOs, JSONL and human output shapes, and
    exact golden bytes before command implementation begins
  - Exact direct dependency and feature allowlist, protected normal credentials only through WP-137's public-client
    loader, bootstrap document handling only through riffdb-auth::bootstrap_secret, zeroized CLI buffers, and source
    architecture checks forbidding internal service/server/storage/policy or unchecked transport text
  - Typed invocation of WP-137's execute, bootstrap-create, and normal-create retry helpers using fresh RequestIds and
    stable semantic identities, consuming only terminal Result/ClientError values and never private dispositions
  - Direct no-shell riffdb-budget-public launch for the closed riffdb.budget.public-run/v1 protocol with a cleared
    environment, closed stdin, independent 4,096-byte output caps, fixed 180-second kill/reap deadline, and CLI-owned
    checked result rendering without relaying child output
  - Process integration covering all acceptance-demo operations against riffdbd, all three public-runner cases and
    exit/output branches, no redb path access, and exact machine output/uncertainty fixtures
  acceptance_commands:
  - cargo test -p riffdb-cli
  - ./scripts/demo --dry-run
  - cargo build -p riffdb-server --bin riffdbd --target-dir target/wp150-root
  - cargo build --manifest-path examples/budget-comparison/Cargo.toml -p riffdb-budget-comparison-riffdb-grpc --bin
    riffdb-budget-public --target-dir target/wp150-comparison
  - RIFFDB_TEST_RIFFDBD_BIN="$PWD/target/wp150-root/debug/riffdbd" RIFFDB_TEST_BUDGET_RUNNER_BIN="$PWD/target/wp150-comparison/debug/riffdb-budget-public"
    CARGO_TARGET_DIR=target/wp150-root cargo test -p riffdb-cli --test public_process -- --ignored --exact
    public_process_all_acceptance_operations
  exit_gate: Every acceptance-demo operation is available through the CLI and public gRPC.
  human_review_triggers: *id001
- id: WP-160
  closure:
    status: complete
    evidence: crates/riffdb-outbox
    decision: Outbox intent is atomic with command state and events, delivery state is derived and recoverable, and dispatcher retries preserve stable identity without authority bypass. Workspace, recovery, and conformance suites pass.
  name: Durable outbox
  depends_on:
  - WP-100
  - WP-120
  objective: Deliver atomically committed event intent outside transaction execution with explicit at-least-once
    behavior.
  requirements:
  - EFF-001
  - EFF-002
  - EFF-003
  - POC-005
  required_adrs:
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-storage-api/src/lib.rs
  - crates/riffdb-storage-api/src/outbox.rs
  - crates/riffdb-storage-memory/src/derived.rs
  - crates/riffdb-storage-memory/src/lib.rs
  - crates/riffdb-storage-redb/src/derived.rs
  - crates/riffdb-storage-redb/src/lib.rs
  - crates/riffdb-outbox/**
  - tests/outbox/**
  deliverables:
  - Complete bounded outbox-recovery scanner with one frozen upper EventId fence, strict exclusive continuation,
    500-item/4-MiB page limits, explicit exact end, and source observations for absent Pending, explicit Pending,
    Delivering, DeadLetter, and Delivered
  - Ordinary pending-event scanner that may use a rebuilt process-local accelerator but never treats that accelerator
    as recovery evidence
  - Delivery state and backoff
  - Deterministic test connector
  - Optional disabled HTTP webhook connector
  - Duplicate and crash simulations
  - Named delivery failpoints and redaction-safe telemetry hooks
  - Bounded payload-free outbox-status source semantics for the mechanical service adapter
  - Canonical interpretation of a missing outbox-status row as never-attempted Pending, rejection of orphan status
    rows, and preservation of retry metadata when an explicit Pending row exists
  - Idempotent recovery normalization of interrupted Delivering rows before the worker is declared ready, with
    bounded findings reported separately from WP-070 structural evidence and WP-130 authoritative readiness
  - No-destination behavior that leaves committed intent Pending without inventing a delivery attempt and reports a
    bounded derived degradation
  acceptance_commands:
  - cargo test -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb
  - cargo test -p riffdb-outbox
  - cargo test -p riffdb-outbox --test outbox_crash_and_duplicate
  external_test_targets:
  - name: outbox_crash_and_duplicate
    owner_crate: riffdb-outbox
    manifest: crates/riffdb-outbox/Cargo.toml
    path: ../../tests/outbox/outbox_crash_and_duplicate.rs
  exit_gate: Every interrupted Delivering row at the frozen recovery fence is normalized through exact end before
    worker readiness; no committed event is lost, no-destination configuration preserves Pending intent, and
    duplicate dispatch scenarios are visible and policy-controlled.
  human_review_triggers: *id001
- id: WP-170
  closure:
    status: complete
    evidence: crates/riffdb-projection
    decision: Projection state is rebuildable, generation and frontier fenced, policy-filtered, and never authoritative. Rebuild, degradation, publication, query, and recovery tests pass.
  name: Projection core
  depends_on:
  - WP-050
  - WP-100
  - WP-120
  objective: Maintain event-derived count and sum projections with a durable monotonic frontier and typed read-after-sequence
    outcomes.
  requirements:
  - PRJ-001
  - PRJ-002
  - PRJ-003
  - PRJ-004
  - POC-006
  required_adrs:
  - ADR-0004
  - ADR-0006
  - ADR-0007
  - ADR-0010
  - ADR-0013
  - ADR-0014
  - ADR-0017
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-catalog/src/lib.rs
  - crates/riffdb-catalog/src/projection_materialization.rs
  - crates/riffdb-catalog/tests/architecture.rs
  - crates/riffdb-catalog/tests/projection_event_materialization.rs
  - crates/riffdb-storage-api/src/lib.rs
  - crates/riffdb-storage-api/src/projection.rs
  - crates/riffdb-storage-memory/src/derived.rs
  - crates/riffdb-storage-memory/src/lib.rs
  - crates/riffdb-storage-redb/src/derived.rs
  - crates/riffdb-storage-redb/src/lib.rs
  - crates/riffdb-projection/**
  - tests/projection/**
  deliverables:
  - Filter/count/sum/group execution only over the catalog-owned opaque event-materialization view for the exact
    resolved projection bundle, never over an unnormalized durable payload
  - Identical live-catch-up and rebuild normalization for strict-ancestor optional-null event fields, with immutable
    durable event bytes/hash, unknown-field preservation, and fail-closed foreign-writer or exact/descendant/genesis/
    required-field omission handling
  - Durable state and frontier
  - Idempotent commit application
  - Waiter notification
  - Rebuild and degraded status
  - Derived-state recovery that degrades an inconsistent projection without weakening authoritative readiness and
    rebuilds into a new ProjectionGeneration rather than repairing a published generation in place
  - Named state/frontier failpoints and redaction-safe telemetry hooks
  - Bounded query, status, and wait source using exact projection identity, generation, FrontierPosition, lower
    continuation, one-snapshot pagination, marker-hash equality, retirement, and lifecycle semantics
  - Atomic internal degraded/invalid query fences carrying the exact identity, optional or failed generation,
    FrontierPosition, and closed reason from one storage read transaction, with no service-side status join
  - Narrow default-feature-disabled projection dependencies on riffdb-contract-ir and riffdb-invariant solely for
    checked filter, key/group, and measure execution over the catalog-owned opaque materialization view
  - Projection fixtures proving ancestor optional event-field materialization, foreign-writer and descendant/required
    omission rejection, unknown-field preservation, raw-event immutability, and identical rebuilt state/frontier
  acceptance_commands:
  - cargo test -p riffdb-catalog -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb
  - cargo test -p riffdb-projection
  - cargo test -p riffdb-projection --test projection_prefix_and_recovery
  external_test_targets:
  - name: projection_prefix_and_recovery
    owner_crate: riffdb-projection
    manifest: crates/riffdb-projection/Cargo.toml
    path: ../../tests/projection/projection_prefix_and_recovery.rs
  exit_gate: Catalog-normalized ancestor optional event fields produce identical live and rebuilt projection state,
    foreign writers and descendant/required omissions fail closed without changing durable event bytes/hash, unknown
    fields remain preserved, and projection prefix, monotonic frontier, restart, rebuild, and query-after-sequence
    properties pass.
  human_review_triggers: *id001
- id: WP-180
  closure:
    status: complete
    evidence: crates/riffdb-observability
    decision: Fixed-cardinality metrics, structured tracing, incident linkage, health hooks, and redaction-before-subscriber telemetry are implemented. Current observability suites pass; one order-sensitive test flaked once in a prior full run and passed exact and complete reruns.
  name: Observability and diagnostics
  depends_on:
  - WP-120
  - WP-140
  - WP-160
  - WP-170
  objective: Add structured traces, bounded metrics, health, redaction, and explain diagnostics across the public
    and transaction paths.
  requirements:
  - ID-005
  - POC-010
  required_adrs:
  - ADR-0004
  - ADR-0007
  - ADR-0008
  - ADR-0009
  - ADR-0018
  - ADR-0043
  - ADR-0044
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-service/src/lib.rs
  - crates/riffdb-service/src/telemetry.rs
  - crates/riffdb-service/tests/architecture.rs
  - crates/riffdb-commit/src/lib.rs
  - crates/riffdb-commit/src/telemetry.rs
  - crates/riffdb-commit/tests/architecture.rs
  - crates/riffdb-conflict/src/telemetry.rs
  - crates/riffdb-storage-api/src/lib.rs
  - crates/riffdb-storage-api/src/telemetry.rs
  - crates/riffdb-storage-api/tests/architecture.rs
  - crates/riffdb-storage-redb/src/hooks.rs
  - crates/riffdb-storage-redb/src/lib.rs
  - crates/riffdb-storage-redb/tests/architecture.rs
  - crates/riffdb-api-mcp/src/lib.rs
  - crates/riffdb-api-mcp/src/telemetry.rs
  - crates/riffdb-api-mcp/tests/hosted_architecture.rs
  - crates/riffdb-observability/**
  - crates/riffdb-diagnostics/**
  - tests/observability/**
  deliverables:
  - Closed owner-defined least-authority telemetry sink traits and event enums for service, commit, conflict,
    storage, MCP, outbox, and projection, accepting no arbitrary text, map, business value, credential, raw
    identifier, entity key, event payload, or Debug rendering
  - Pre-subscriber validation and redaction, including a permissive sibling-subscriber canary proving that sensitive
    input cannot reach any tracing layer
  - Tracing layers and frozen span fields
  - In-process metrics registry with closed labels, checked cardinality/capacity, explicit overflow/drop counters,
    and no network listener or exporter
  - Health model
  - Health classification that preserves authoritative readiness while reporting bounded outbox/projection findings
    and worker readiness as separate derived Degraded state
  - Empty-database health representation with no fabricated zero CommitSequence
  - Bounded redacted IncidentIdSource consumer integration with explicit test fakes, no concrete provider or ambient
    entropy in diagnostics, fail-closed source errors, and no assumption that UUID order is event order
  - Redaction tests
  - Explain output and snapshots
  - Integration over telemetry and health hooks owned by upstream packages
  acceptance_commands:
  - cargo test -p riffdb-service -p riffdb-commit -p riffdb-conflict -p riffdb-storage-api -p riffdb-storage-redb
    -p riffdb-api-mcp
  - cargo test -p riffdb-observability -p riffdb-diagnostics
  - cargo test -p riffdb-observability --test telemetry_redaction
  external_test_targets:
  - name: telemetry_redaction
    owner_crate: riffdb-observability
    manifest: crates/riffdb-observability/Cargo.toml
    path: ../../tests/observability/telemetry_redaction.rs
  exit_gate: The frozen owner-event and metric inventory is present, bounded, and free of prohibited raw values even
    with a permissive sibling subscriber; no network metrics exporter is configured.
  human_review_triggers: *id001
- id: WP-185
  closure:
    status: complete
    evidence: crates/riffdb-server/src/process_graph.rs
    decision: Production composition wires every authoritative and derived component exactly once, and readiness reflects complete authoritative validation plus bounded derived health without crossed startup outcomes. Server, service, architecture, and recovery tests pass.
  name: Server composition
  depends_on:
  - WP-130
  - WP-140
  - WP-160
  - WP-170
  - WP-180
  objective: Extend the runnable WP-130 riffdbd graph with loopback MCP HTTP, outbox, projection, observability, and
    derived-health components without replacing its P1 providers, startup gate, lifecycle, or business semantics.
  requirements:
  - SYS-001
  - API-001
  - MCP-010
  - MCP-011
  - POC-001
  - POC-006
  - POC-007
  - POC-008
  required_adrs:
  - ADR-0004
  - ADR-0007
  - ADR-0008
  - ADR-0009
  - ADR-0010
  - ADR-0012
  - ADR-0017
  - ADR-0018
  - ADR-0019
  - ADR-0020
  - ADR-0043
  - ADR-0044
  - ADR-0049
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-server/**
  - tests/server_composition/**
  - tests/grpc/p1_riffdbd_restart.rs
  deliverables:
  - P2 extension of the existing WP-130 riffdbd component graph and bounded lifecycle
  - Existing gRPC plus loopback MCP HTTP endpoint composition
  - Optional repeatable --mcp-origin plus --mcp-listen flags with literal loopback/nonzero-port validation, an exact
    derived http://<canonical-listen-authority>/mcp audience, and no operator-supplied audience
  - Exact default-disabled Axum 0.8.9 http1/tokio server edge plus Tokio net/signal features, with no TLS, HTTP/2,
    proxy trust, remote bind, OAuth, alternate route, or network metrics endpoint
  - Outbox and projection worker startup and shutdown
  - Authoritative versus derived health aggregation
  - Authoritative readiness preserved when only derived outbox or projection recovery is degraded, with bounded
    derived findings and worker-specific readiness surfaced separately
  - Real projection gRPC and MCP end-to-end coverage
  - Mechanical authoritative, projection, outbox, and operational service-port adapters without new semantics
  - Reuse and regression evidence for WP-130's exact UUID wrappers, four clocks, cursor providers, disjoint digest
    providers/inventories, initialization executor, same-session structural/ValidatedCatalogHistory startup proof, bootstrap/deploy/
    active lifecycle, and graceful/crash authoritative validation; WP-185 defines no replacement provider or gate
  - MCP registration and dispatch composition that preserves compiler-owned ADR-0020 command names without an
    alternate derivation, normalization, or repair path
  - Shared lifecycle proxy for gRPC and hosted MCP, SIGINT/SIGTERM and exact test-control shutdown handling, stdin EOF
    that disables only the watcher, and bounded shutdown ordering across admission, MCP, workers, service/coordinator,
    providers, and storage
  - No-destination composition that installs no discarding connector, leaves outbox intent Pending, and reports the
    derived degradation without weakening authoritative readiness
  acceptance_commands:
  - cargo test -p riffdb-server --all-features
  - cargo test -p riffdb-server --test p1_riffdbd_restart
  - cargo test -p riffdb-server --test server_composition
  external_test_targets:
  - name: server_composition
    owner_crate: riffdb-server
    manifest: crates/riffdb-server/Cargo.toml
    path: ../../tests/server_composition/server_composition.rs
  exit_gate: The same riffdbd graph accepted in WP-130 still passes its P1 restart sequence while also serving optional
    loopback MCP HTTP with exact audience/origin policy and compiled command names, running both workers, preserving
    the startup/lifecycle gate, reporting separate authoritative and derived health, surviving stdin EOF, draining on
    SIGTERM in the accepted order, preserving Pending intent without a destination, and proving real
    read-after-sequence projection behavior through public APIs.
  human_review_triggers: *id001
- id: WP-155
  closure:
    status: complete
    evidence: docs/backup-restore.md
    decision: Offline backup and restore use authorized public administration, exact receipted phases, staged validation, destructive confirmation, crash reconciliation, and fail-closed readiness. Maintenance and recovery matrices pass.
  name: Public offline backup and restore maintenance
  depends_on:
  - WP-070
  - WP-120
  - WP-137
  - WP-150
  - WP-185
  objective: Expose WP-070's immutable offline backup and restore mechanics through three public, policy-filtered,
    uncertainty-recoverable operations without creating a privileged CLI, MCP, storage, or durable-audit path.
  requirements:
  - SYS-001
  - API-001
  - ID-001
  - ID-005
  - STO-020
  - STO-021
  - STO-022
  - REC-001
  - REC-002
  - SEC-001
  - SEC-002
  - SEC-003
  - POC-008
  - POC-009
  - POC-010
  required_adrs:
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0018
  - ADR-0019
  - ADR-0021
  - ADR-0025
  - ADR-0026
  - ADR-0027
  - ADR-0028
  - ADR-0032
  - ADR-0034
  - ADR-0037
  - ADR-0040
  - ADR-0041
  - ADR-0049
  - ADR-0050
  allowed_paths:
  - Cargo.lock
  - proto/riffdb/v1/admin.proto
  - proto/riffdb/v1/common.proto
  - proto/riffdb/v1/services.proto
  - crates/riffdb-proto/**
  - fixtures/proto/**
  - scripts/generate-proto*
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - fuzz/fuzz_targets/proto_public.rs
  - fuzz/fuzz_targets/maintenance_receipt.rs
  - crates/riffdb-types/**
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-storage-api/src/backup.rs
  - crates/riffdb-storage-api/src/lib.rs
  - crates/riffdb-storage-redb/Cargo.toml
  - crates/riffdb-storage-redb/src/backup.rs
  - crates/riffdb-storage-redb/src/error.rs
  - crates/riffdb-storage-redb/src/lib.rs
  - crates/riffdb-storage-redb/src/maintenance/**
  - crates/riffdb-storage-redb/tests/architecture.rs
  - crates/riffdb-storage-redb/tests/maintenance.rs
  - crates/riffdb-service/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-server/**
  - crates/riffdb-cli/**
  - tests/maintenance/**
  - docs/backup-restore.md
  deliverables:
  - Exact additive AdminService RPCs CreateOfflineBackup, RestoreOfflineBackup, and
    GetOfflineMaintenanceOperation, bringing the public inventory to five services and 25 RPCs without changing an
    existing field, service, RPC, durable message, contract/IR value, or MCP schema/resource/tool
  - Checked caller-stable UUIDv7 OfflineMaintenanceOperationId, exact 1..=64-byte BackupNameV1 grammar, stable
    semantic input hash, fresh transport RequestId per retry, input-mismatch rejection, and bounded closed operation
    observations
  - Exact public-client and CLI identities backup.create, backup.restore, and backup.operation with grammar
    riffdb backup create|restore|operation; no CLI path resolution or direct redb access
  - Exact wire confirmation ALLOW_REPLACE_NONEMPTY_TARGET emitted only by
    --confirm-replace-current-database and required for every healthy, unreadable, or corrupt nonempty target, with no
    expected-database-id input and no implication that confirmation grants authority
  - API-neutral maintenance service and narrow policy decision reusing the existing global AdministerCapabilities
    permission, ordinary approval obligations, and deny-by-default current facts without adding a durable permission
    or ServiceOperationV1 variant
  - No StoredServiceAuditRecordV1 Started or terminal row for an offline maintenance operation; the external receipt
    is the sole narrow audit and uncertainty-recovery exception and never synthesizes a terminal after reopen
  - Auth-owned non-Clone, nonserializable, redacted, bounded zeroizing ordinary-bearer handoff used only to borrow
    OpaqueCredential for staged authentication, never logged or persisted, and dropped before publication or terminal
    receipt persistence
  - Healthy overwrite authorization against the current database before drain plus a fresh independent
    authentication and authorization of the same bearer against the fully validated staged backup; empty/corrupt
    recovery performs staged authorization only, and no prior principal or decision can satisfy the staged check
  - Private exclusive Ready -> Draining -> Offline -> Validating -> Ready/FailedClosed lifecycle with a fixed drain
    deadline, no protected request or polling while offline, restricted recovery-mode restore admission only when
    ordinary readiness cannot validate the target, and derived-worker restart only after authoritative readiness
  - Service-owned backup_root resolution and reserved, caller-inaccessible .maintenance subtree disjoint from named
    immutable artifacts and the database path, with no public filesystem path or URI
  - One versioned, checksummed, canonically encoded, atomically replaced and parent-synced receipt per operation ID,
    with immutable semantic input, at most 16 monotonic phase transitions, bounded safe failures, exact goldens, and
    fail-closed startup reconciliation from receipt plus filesystem checksum evidence
  - Backup close/publish/reopen and restore private-stage/manifest-check/full-validation/staged-authorization/
    destructive-publication/fresh-validation orchestration using WP-070 mechanics, with no second backup format and
    no readiness before the terminal receipt is durable
  - Restore preservation of DatabaseId and explicit invalidation of every post-backup observation, locator,
    idempotency assumption, audit assumption, capability fact, cursor, session, and projection/outbox state; destroyed
    application and administration sequence suffixes may be reused and no incarnation/history-epoch claim is made
  - Descriptor, wire, SDK, CLI JSONL, receipt, authorization, redaction, parser-fuzz, process-failpoint, quiescence,
    crash/retry, empty/corrupt recovery, identity-preservation, and rewind fixtures
  acceptance_commands:
  - cargo test -p riffdb-types -p riffdb-auth -p riffdb-policy -p riffdb-storage-api -p riffdb-storage-redb -p
    riffdb-service
  - cargo test -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server -p riffdb-cli --all-features
  - cargo test -p riffdb-server --test offline_maintenance_recovery -- --ignored
  - ./scripts/generate-proto --check
  - cargo +nightly-2026-07-12 fuzz run proto_public -- -max_len=4194304 -max_total_time=30
  - cargo +nightly-2026-07-12 fuzz run maintenance_receipt -- -max_len=1048576 -max_total_time=30
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-types -p riffdb-auth -p riffdb-policy -p riffdb-storage-api -p riffdb-storage-redb -p
    riffdb-service -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server -p riffdb-cli
    --all-targets --all-features -- -D warnings
  - RUSTDOCFLAGS="-D warnings" cargo doc -p riffdb-types -p riffdb-auth -p riffdb-policy -p riffdb-storage-api -p
    riffdb-storage-redb -p riffdb-service -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server -p
    riffdb-cli --all-features --no-deps
  - cargo deny check
  external_test_targets:
  - name: offline_maintenance_recovery
    owner_crate: riffdb-server
    manifest: crates/riffdb-server/Cargo.toml
    path: ../../tests/maintenance/offline_maintenance_recovery.rs
  exit_gate: All three public operations pass only through gRPC, the shared service/policy boundary, and the private
    exclusive lifecycle; receipt and process failpoints resolve uncertain retries without inventing success; staged
    authorization and destructive confirmation fail closed; restored readiness reproves the complete database; and
    exact fixtures document DatabaseId-preserving history rewind and possible suffix reuse.
  human_review_triggers: *id001
- id: WP-190
  closure:
    status: complete
    evidence: tests/recovery/fixtures/wp190-report-v1.json
    decision: The named process failpoint inventory, child controller, durable inspector, full recovery matrix, offline maintenance matrix, lost-response replay, repeated-open, and migration recovery arms pass with the checked receipted report.
  name: Integrated crash harness
  depends_on:
  - WP-070
  - WP-100
  - WP-155
  - WP-160
  - WP-170
  - WP-185
  objective: Inject named process-level failures at every authoritative boundary and verify recovery against the
    reference model.
  requirements:
  - TEST-001
  - REC-001
  - REC-002
  - REC-003
  - POC-004
  - POC-009
  - TXN-041
  - TXN-042
  - TXN-044
  - EFF-003
  - MCP-046
  required_adrs:
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0010
  - ADR-0012
  - ADR-0017
  - ADR-0018
  - ADR-0019
  - ADR-0021
  - ADR-0038
  - ADR-0039
  - ADR-0040
  - ADR-0041
  - ADR-0042
  - ADR-0049
  - ADR-0050
  allowed_paths:
  - Cargo.lock
  - crates/riffdb-testkit/**
  - crates/riffdb-cli/Cargo.toml
  - crates/riffdb-cli/src/credential.rs
  - crates/riffdb-cli/src/lib.rs
  - crates/riffdb-cli/tests/architecture.rs
  - crates/riffdb-server/Cargo.toml
  - crates/riffdb-server/src/daemon.rs
  - crates/riffdb-server/src/lib.rs
  - crates/riffdb-server/src/maintenance_driver.rs
  - crates/riffdb-server/src/maintenance_recovery_controller.rs
  - crates/riffdb-server/src/process_graph.rs
  - crates/riffdb-server/tests/architecture.rs
  - tests/recovery/**
  - scripts/recovery_*
  deliverables:
  - Named failpoint API
  - Child-process controller
  - Durable state inspector
  - Recovery matrix and reports
  - Repeated-open idempotence tests
  - Process crash/restart evidence for started and terminal service audit, retry/resume audit linkage, admitted
    provenance preservation, exact ADR-0021 target order/identity preservation, and cursor invalidation
  - Process evidence for DatabaseId install-only-on-empty behavior, UUIDv7 identifier validation, ProvenanceId
    generation timing/collision/replay, fresh RequestId retries, and no sequence or durable write on identifier
    collision
  - Process failpoints around bootstrap credential file and containing-directory synchronization proving no RPC is
    sent before the retained credential is crash-durable
  - Projection identity/generation/frontier, compound-bootstrap, idempotency digest-provider retirement, both
    sequence-metadata spaces, capability lookup plus bootstrap/create/revoke audit kind/target/sequence/timestamp
    cross-links, and terminal execution-failure recovery evidence
  - Process failpoints before/after sequence-free capacity reservation, sequence assignment, canonical-envelope
    verification, and staging, proving a failed reservation or envelope excess consumes no sequence and exposes no
    partial graph
  - Crash/reopen evidence that every durable event hash recomputes from its exact EventId/type/length-framed canonical
    record payload and that any commit/event/outbox hash mismatch fails authoritative readiness without repair
  - Separate WP-070 structural evidence, WP-050 ValidatedCatalogHistory, WP-130 authoritative readiness, WP-160
    Delivering normalization, WP-170 degradation/new-generation rebuild, and WP-185 aggregate-health evidence with
    no online repair of authoritative state
  - Graceful-close and crash/reopen evidence that complete authoritative integrity validation runs every time,
    repeated recovery creates no deferred ADR-0019 metadata, and canonical absence remains valid and idempotent
  - Named process failpoints throughout V1/V2 startup migration, including before and after each bounded compare-and-
    rewrite commit, proving restart from a complete fresh scan, no partial batch, catalog-owned branded completion,
    dormant-only backend finish, V2-only completion, and full fresh structural/catalog exact-end validation before
    readiness without exposing driver states to the process harness
  - Maintainer-approved narrow owner-package fixture hooks in riffdb-cli and riffdb-server expose only synchronized
    child-process crash boundaries to riffdb-testkit; they add no public API, durable record, or production bypass
  - Two explicitly synchronized post-commit/pre-response public Execute cases, one losing TCP response bytes before a
    complete frame and one kills then restarts riffdbd; each retries through WP-137 with the same command, raw key,
    canonical input, and capability but a fresh RequestId, returning the original replayed outcome, sequence, and
    provenance with no second mutation, event, outbox intent, provenance, commit record, or sequence
  - Process failpoints before and after maintenance receipt sync, drain, database close, immutable backup publication,
    staged restore, staged validation/authorization, destructive target publication, fresh post-publication
    validation, and terminal receipt persistence, including kill/restart reconciliation by exact checksum evidence
  - Healthy current-plus-staged and empty/corrupt staged-only authorization recovery, credential-loss retry, protected
    polling unavailability while offline, DatabaseId preservation, process-state invalidation, destroyed-suffix
    idempotency and sequence reuse, and no invented service-audit terminal
  acceptance_commands:
  - cargo test -p riffdb-testkit
  - cargo test -p riffdb-testkit --test full_recovery_matrix -- --ignored
  - cargo test -p riffdb-testkit --test offline_maintenance_matrix -- --ignored
  external_test_targets:
  - name: full_recovery_matrix
    owner_crate: riffdb-testkit
    manifest: crates/riffdb-testkit/Cargo.toml
    path: ../../tests/recovery/full_recovery_matrix.rs
  - name: offline_maintenance_matrix
    owner_crate: riffdb-testkit
    manifest: crates/riffdb-testkit/Cargo.toml
    path: ../../tests/recovery/offline_maintenance_matrix.rs
  exit_gate: Every failpoint produces exactly the allowed pre-commit or post-commit state with no sequence consumed by
    failed capacity/envelope checks and exact durable event-hash recomputation; migration failpoints restart through
    the complete V2-only validation path; synchronized TCP-response loss and post-commit process termination both
    recover the original result through a fresh public-client request without a second authoritative graph; offline
    maintenance failpoints reconcile only from exact receipt/filesystem evidence and preserve the accepted rewind
    semantics; every reopen performs complete authoritative integrity validation; and repeated recovery is stable
    without deferred metadata writes.
  human_review_triggers: *id001
- id: WP-200
  name: POC acceptance and release
  depends_on:
  - WP-050
  - WP-075
  - WP-130
  - WP-135
  - WP-139
  - WP-140
  - WP-150
  - WP-155
  - WP-160
  - WP-170
  - WP-180
  - WP-185
  - WP-190
  objective: Assemble the self-verifying budget demonstration, reproducible benchmarks, security notes, and release
    artifacts that decide POC exit.
  requirements:
  - POC-001
  - POC-002
  - POC-003
  - POC-004
  - POC-005
  - POC-006
  - POC-007
  - POC-008
  - POC-009
  - POC-010
  required_adrs:
  - ADR-0001
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0008
  - ADR-0009
  - ADR-0010
  - ADR-0011
  - ADR-0012
  - ADR-0013
  - ADR-0014
  - ADR-0015
  - ADR-0016
  - ADR-0017
  - ADR-0018
  - ADR-0019
  - ADR-0020
  - ADR-0021
  - ADR-0022
  - ADR-0023
  - ADR-0038
  - ADR-0039
  - ADR-0040
  - ADR-0041
  - ADR-0042
  - ADR-0045
  - ADR-0049
  - ADR-0050
  - ADR-0144
  allowed_paths:
  - README.md
  - scripts/demo*
  - scripts/release*
  - docs/**
  - examples/**
  - benchmarks/**
  - release/**
  deliverables:
  - One-command acceptance demo
  - Machine-readable requirement report
  - Benchmark report
  - Threat model and dependency inventory
  - Release binaries, checksums, SBOM, examples, known limitations
  - Root README with architecture and quick-start boundaries, installation/upgrade/removal instructions, configuration
    reference, and hardened systemd units for riffdbd and the optional public-client riffdb-mcp bridge
  - Machine-readable consumption of WP-139's checked safety report with its exact claim qualifier and every
    deliberately unsafe PostgreSQL variant excluded from performance results
  - Existing WP-075 Fjall and WP-045/WP-125/WP-135/WP-139 comparison sources are read-only evidence inputs; WP-200
    may add scenarios, reports, and release packaging under its broad paths but may not rewrite their manifests,
    lockfiles, adapters, fixtures, or guarantee profiles without a separately approved owner-package amendment
  - The maintainer-approved WP-200 owner-package amendment is limited to the nested comparison manifests/lockfile,
    safety architecture scanner, and performance-support harness needed for dependency-free semantic benchmark
    evidence; it does not change an adapter, frozen fixture, public-run protocol, or guarantee profile
  - ADR-0144 additionally permits the public budget adapter, its semantic/public-process/replay/safety/architecture
    tests, and guarantee profile to record and reconcile the exact closed production durability set {sync, group};
    response, replay, notification, and exact-end commit evidence must agree, while memory, absent, malformed,
    unknown, or mismatched durability fails closed and the frozen workload and public-run JSONL fixture remain exact
  - Cross-transport semantic and durable-audit parity plus generated durable/public artifact evidence in the final
    requirement report
  - Final transaction-order evidence from current validation through mutation-affected epoch reads, sequence-free
    reservation, sequence assignment, canonical-envelope verification, staging, durability, and same-key recovery,
    including the exact EventHash preimage/golden/restart chain
  - Final evidence that every startup validates authoritative integrity, the durable schema contains only the
    ADR-0019 retained metadata set, and graceful shutdown or recovery creates no deferred operational record
  - End-to-end compiler, catalog, shared-service, and MCP evidence that the exact ADR-0020 name is stable, collision
    checked before activation, consumed verbatim, authorization filtered, and never repaired or rederived
  - Cross-transport and crash/restart evidence that the exact ADR-0021 target registry, canonical order, and
    request-only construction rule are preserved without adapter/storage derivation or result expansion
  - Final ADR-0023 evidence for the three-attempt reevaluation ceiling, proven-noncommit versus uncertain same-attempt
    provenance recovery, canonical empty v1 covered values, late commit-check arithmetic terminalization, absent
    capability revoke, exact transaction-current evaluator boundary, audit-input ownership inversion, and
    known/no-transition control-plane audit classifications
  - Final V2 migration generated-artifact, restart/recovery, authorization, cross-transport, and rollback-unsupported
    evidence, including exact 27-readable/26-writable registry, sealed catalog/backend authority architecture, and no
    post-migration V1 record
  - Machine-readable consumption of WP-190's synchronized TCP-loss and process-restart same-key replay evidence for
    POC-004, plus final WP-137/WP-140/WP-150 public protocol, MCP, CLI, generated-artifact, and release conformance
  - Final WP-155 public-schema, receipt, current/staged authorization, offline lifecycle, process recovery, and
    DatabaseId-preserving rewind evidence, with destroyed-suffix locator/idempotency/sequence limitations prominent in
    README, backup/restore instructions, CLI confirmation, and release notes
  - Systemd verification that riffdbd starts with closed stdin, stops by SIGTERM through the bounded graceful drain,
    uses explicit state/config/backup roots and restrictive permissions, and never introduces a direct storage path
    for riffdb, riffdb-mcp, or an MCP transport
  acceptance_commands:
  - ./scripts/ci-all
  - ./scripts/demo --assert
  - ./scripts/release-poc --verify
  exit_gate: POC-001 through POC-010 are signed off and architecture review records the next-stage decision.
  human_review_triggers: *id001
- id: WP-205
  name: Application-path latency decomposition and optimization baseline
  depends_on:
  - WP-200
  objective: Reproduce TicketDesk's public-path latency, measure every semantic stage over a reused connection, and
    remove repeated catalog/capability work without weakening current-policy or authoritative-read guarantees.
  requirements:
  - PERF-001
  - PERF-002
  required_adrs:
  - ADR-0007
  - ADR-0009
  - ADR-0025
  - ADR-0026
  - ADR-0027
  - ADR-0053
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-auth/**
  - crates/riffdb-catalog/**
  - crates/riffdb-observability/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-testkit/**
  - examples/app-baseline/**
  - benchmarks/application-path/**
  - scripts/benchmark-application-path
  - docs/performance/**
  deliverables:
  - Reproducible warm/cold reference profile with a reused HTTP/2 connection
  - Bounded redaction-safe per-stage latency and operation-count telemetry
  - Immutable rebuildable current catalog and capability views with startup/readiness and publication tests
  - Before/after TicketDesk report proving whether unary transport remains within its accepted budget
  acceptance_commands:
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - ./scripts/benchmark-application-path --assert-wp205
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every semantic stage is measured; point and command paths meet PERF-002 or have an evidence-backed
    blocking diagnosis; current-view gaps stop readiness; no authorization, audit, durability, or compatibility
    guarantee is weakened.
  human_review_triggers: *id001
- id: WP-210
  name: RiffQL syntax, formatter, and TicketDesk corpus
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: The textual grammar, formatter, and TicketDesk named-query corpus are shipped and covered by current compiler fixtures.
  depends_on:
  - WP-205
  objective: Freeze the versioned formal read-only RiffQL source language and prove it can express every bounded
    TicketDesk page without admitting SQL, mutation, natural-language execution, or unbounded computation.
  requirements:
  - RQL-001
  - RQL-002
  required_adrs:
  - ADR-0002
  - ADR-0011
  - ADR-0013
  - ADR-0016
  - ADR-0051
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-riffql-syntax/**
  - crates/riffdb-types/**
  - crates/riffdb-diagnostics/**
  - queries/ticketdesk/**
  - docs/riffql/**
  deliverables:
  - Versioned lexer, parser, source AST, formatter, and bounded parser diagnostics
  - Canonical formatting and parse/format/parse stability fixtures
  - TicketDesk list, detail, and summary query corpus
  - Negative corpus for mutation, recursion, unbounded many, and unsupported access forms
  acceptance_commands:
  - cargo test -p riffdb-riffql-syntax --all-features
  - cargo test -p riffdb-riffql-syntax --test ticketdesk_corpus
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-riffql-syntax --all-targets --all-features -- -D warnings
  exit_gate: The grammar and formatter are deterministic, every accepted many binding has explicit bounds, and the
    complete TicketDesk corpus parses without storage IDs or encoded keys.
  human_review_triggers: *id001
- id: WP-220
  name: Symbolic catalog, source maps, schemas, and diagnostics
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Symbolic catalog resolution, source maps, public schemas, and bounded diagnostics are active across the public authoring path.
  depends_on:
  - WP-210
  objective: Resolve RiffQL entirely through exact contract symbols and publish bounded name-addressed parameter and
    result schemas plus source-spanned diagnostics.
  requirements:
  - RQL-005
  - QRY-004
  required_adrs:
  - ADR-0013
  - ADR-0020
  - ADR-0027
  - ADR-0046
  - ADR-0051
  - ADR-0052
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-riffql-syntax/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-catalog/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-errors/**
  - crates/riffdb-types/**
  - fixtures/riffql/**
  - docs/riffql/**
  deliverables:
  - Exact-contract symbolic catalog with deterministic name lookup and ambiguity rejection
  - Versioned canonical typed query IR and source maps
  - Name-addressed parameter and result schemas
  - Stable bounded diagnostics and source-span snapshots for every semantic failure class
  acceptance_commands:
  - cargo test -p riffdb-query-ir --all-features
  - cargo test -p riffdb-catalog --all-features
  - cargo test -p riffdb-diagnostics --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: TicketDesk queries resolve from names to exact types with reproducible schemas/source maps, and no
    application-facing artifact requires a numeric semantic ID.
  human_review_triggers: *id001
- id: WP-230
  name: RiffQL type checker, authorization analysis, and deterministic planner
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Named queries compile through one typed, authorized, locality-checked, bounded deterministic planner.
  depends_on:
  - WP-210
  - WP-220
  objective: Compile symbolic queries into a closed, bounded, same-partition access program with complete
    compiler-derived authorization requirements and stable explain plans.
  requirements:
  - RQL-003
  - RQL-004
  required_adrs:
  - ADR-0007
  - ADR-0013
  - ADR-0016
  - ADR-0026
  - ADR-0027
  - ADR-0038
  - ADR-0051
  - ADR-0053
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-riffql-syntax/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-catalog/**
  - crates/riffdb-policy/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-errors/**
  - crates/riffdb-types/**
  - fixtures/riffql/**
  - docs/riffql/**
  deliverables:
  - Resolver and type checker for the accepted RiffQL subset
  - Deterministic rule planner with partition, cardinality, fan-out, order, and index proofs
  - Canonical closed QueryAccessProgram, plan identity, and bounded explain form
  - Complete pre-execution field/row-scope authorization analysis with no silent omission
  - Suggested compatible index diagnostics and deterministic plan fixtures
  acceptance_commands:
  - cargo test -p riffdb-query-compiler --all-features
  - cargo test -p riffdb-query-compiler --test ticketdesk_plans
  - cargo test -p riffdb-query-compiler --test authorization
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-query-compiler --all-targets --all-features -- -D warnings
  exit_gate: Every TicketDesk query has one stable bounded same-partition plan and access set; unbounded, nonlocal,
    unordered, inaccessible, or unindexed variants fail with named source-spanned diagnostics.
  human_review_triggers: *id001
- id: WP-240
  name: One-snapshot composite query execution
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Page-shaped one-snapshot execution, dependent reads, cursors, and read-after-commit behavior are shipped and tested.
  depends_on:
  - WP-205
  - WP-230
  objective: Execute a closed query access program inside one authoritative read snapshot, batch dependent reads,
    enforce cardinality and bounds, and return one typed page with epoch-safe pagination.
  requirements:
  - QRY-001
  - QRY-002
  required_adrs:
  - ADR-0004
  - ADR-0007
  - ADR-0011
  - ADR-0016
  - ADR-0027
  - ADR-0035
  - ADR-0038
  - ADR-0051
  - ADR-0053
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-policy/**
  - crates/riffdb-errors/**
  - crates/riffdb-types/**
  - crates/riffdb-testkit/**
  - fixtures/riffql/**
  - benchmarks/application-path/**
  deliverables:
  - Storage-neutral query execution port and closed owned QuerySnapshot result
  - Memory and redb adapters using one read transaction per query request
  - Deterministic dependent point-read batching, cardinality checks, and whole-query budgets
  - Canonical cursor binding and stale-epoch rejection
  - Cross-backend semantic, snapshot, pagination, and performance tests
  acceptance_commands:
  - cargo test -p riffdb-query-executor --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-service --all-features
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Each TicketDesk page executes in one service request and one authoritative snapshot, cross-backend
    outputs agree, stale cursors fail closed, and list/detail latency meets PERF-002 without covering-format changes.
  human_review_triggers: *id001
- id: WP-250
  name: RiffQL application gRPC, CLI, REPL, and MCP surfaces
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: gRPC, CLI/REPL, and MCP route textual operations through the shared API-neutral application service without storage bypass.
  depends_on:
  - WP-220
  - WP-230
  - WP-240
  objective: Publish the symbolic application interface through additive unary gRPC, CLI, and MCP adapters while
    preserving the existing kernel protocol and the single shared-service authorization path.
  requirements:
  - DX-001
  - DX-002
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0025
  - ADR-0026
  - ADR-0027
  - ADR-0028
  - ADR-0040
  - ADR-0046
  - ADR-0052
  - ADR-0053
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/app/v1/**
  - crates/riffdb-proto/**
  - crates/riffdb-service/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-cli/**
  - crates/riffdb-server/**
  - crates/riffdb-errors/**
  - crates/riffdb-types/**
  - fixtures/proto/**
  - fixtures/mcp/**
  - docs/riffql/**
  deliverables:
  - Additive versioned application Protobuf and generated compatibility fixtures
  - Shared-service check, explain, ad-hoc execute, named execute, and symbolic command operations
  - CLI query/check/explain/REPL and symbolic command invocation
  - Small MCP symbolic tool/resource surface with separate read and mutation risk
  - Kernel/application compatibility, authorization, schema, redaction, and transport-parity tests
  acceptance_commands:
  - cargo test -p riffdb-proto --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-cli --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The same RiffQL and command text works through CLI and MCP over the shared application service, old
    kernel fixtures remain compatible, and no transport derives policy or touches storage.
  human_review_triggers: *id001
- id: WP-260
  name: Immutable named query modules and optional client generation
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Immutable hash-bound query modules and generated operation facades are deployed and exact-lock checked.
  depends_on:
  - WP-220
  - WP-230
  - WP-240
  - WP-250
  objective: Compile, deploy, activate, execute, inspect, and generate clients from immutable query modules pinned to
    exact contract identities.
  requirements:
  - QRY-003
  - DX-003
  required_adrs:
  - ADR-0006
  - ADR-0013
  - ADR-0020
  - ADR-0021
  - ADR-0022
  - ADR-0027
  - ADR-0052
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/app/v1/**
  - proto/riffdb/storage/v1/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-query-module/**
  - crates/riffdb-catalog/**
  - crates/riffdb-commit/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-proto/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-cli/**
  - crates/riffdb-server/**
  - crates/riffdb-types/**
  - crates/riffdb-errors/**
  - crates/riffdb-testkit/**
  - clients/typescript/**
  - fixtures/query-modules/**
  - fixtures/proto/**
  - fixtures/mcp/**
  - scripts/check-generated
  - docs/riffql/**
  deliverables:
  - Canonical query-module codec, identity, compatibility fixtures, and startup validation
  - Audited compare-and-swap deployment and active-pointer lifecycle
  - Exact active/module-selector execution and plan cache
  - Reproducible Rust and TypeScript generation from named queries and commands
  - Generated MCP tools for named operations without removing the general symbolic surface
  acceptance_commands:
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-catalog --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-client-rust --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Query modules survive restart with exact identity, activation is audited and CAS-safe, named execution
    is equivalent to ad-hoc execution, and both generated clients compile against checked fixtures.
  human_review_triggers: *id001
- id: WP-270
  name: Local development workflow and independent-agent TicketDesk gate
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: The local bootstrap/deploy/grant/seed/watch workflow and independent TicketDesk acceptance path are shipped.
  depends_on:
  - WP-250
  - WP-260
  objective: Deliver the ten-minute symbolic local workflow, then independently rebuild TicketDesk without
    storage-shaped application plumbing and enforce the application performance gates.
  requirements:
  - DX-004
  - DX-005
  - PERF-002
  required_adrs:
  - ADR-0007
  - ADR-0009
  - ADR-0026
  - ADR-0041
  - ADR-0051
  - ADR-0052
  - ADR-0053
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-cli/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-server/**
  - crates/riffdb-service/**
  - crates/riffdb-auth/**
  - crates/riffdb-catalog/**
  - crates/riffdb-query-module/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - examples/ticketdesk/**
  - examples/app-baseline/**
  - queries/ticketdesk/**
  - scripts/riffdb-dev*
  - scripts/benchmark-application-path
  - benchmarks/application-path/**
  - scripts/check-workspace-policy
  - docs/getting-started/**
  - docs/riffql/**
  - README.md
  - release/**
  deliverables:
  - One-command bounded local bootstrap with named dev-role presets
  - Contract/query watch mode and bounded concurrent seed commands
  - Fresh-agent TicketDesk implementation using only symbolic application surfaces
  - Machine-checked source scan rejecting IDs, protobuf construction, encoded-key parsing, masks, and public N+1 reads
  - Reproducible application-DX transcript, independent rating, and PERF-002 report
  acceptance_commands:
  - cargo test -p riffdb-cli --all-features
  - ./scripts/riffdb-dev-acceptance
  - ./scripts/benchmark-application-path --assert-wp270
  - ./scripts/check-ticketdesk-symbolic-boundary
  - ./scripts/ci-all
  - cargo doc --workspace --no-deps
  - cargo deny check
  exit_gate: A fresh coding agent builds and exercises TicketDesk with one symbolic request per page or mutation, no
    forbidden low-level glue, a documented rating of at least 8/10, and all PERF-002 gates passing.
  human_review_triggers: *id001
- id: WP-275
  name: Bounded dependent key batches and complete TicketDesk detail parity
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Compiler-bounded junction-to-entity dependent batches close TicketDesk label parity in one named snapshot query.
  depends_on:
  - WP-270
  objective: Close the TicketDesk junction-read gap with one bounded same-partition collection-to-complete-key
    operator, execute it inside the existing one-snapshot boundary, and verify the complete detail result through
    generated symbolic application APIs.
  requirements:
  - RQL-006
  - QRY-005
  - DX-006
  - PERF-002
  required_adrs:
  - ADR-0013
  - ADR-0016
  - ADR-0027
  - ADR-0035
  - ADR-0038
  - ADR-0051
  - ADR-0052
  - ADR-0053
  - ADR-0054
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - PLAN.md
  - adr/**
  - crates/riffdb-riffql-syntax/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-query-module/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-client-rust/**
  - clients/typescript/**
  - examples/ticketdesk/**
  - examples/app-baseline/**
  - queries/ticketdesk/**
  - fixtures/riffql/**
  - fixtures/query-modules/**
  - scripts/check-generated
  - scripts/check-ticketdesk-symbolic-boundary
  - scripts/riffdb-dev*
  - scripts/benchmark-application-path
  - benchmarks/application-path/**
  - docs/riffql/**
  - docs/getting-started/**
  - README.md
  deliverables:
  - Source-spanned scalar/collection cardinality diagnostics and canonical dependent-batch query IR
  - Deterministic bounded dependent point execution with explicit missing-target outcomes
  - Memory/redb one-snapshot parity for empty, ordered, missing, duplicate, null, and over-bound batches
  - One TicketPage query/result containing project, organization, comments, assignee, and junction-reached labels
  - Generated Rust/TypeScript/MCP fixtures plus demo and benchmark verification of the complete page
  acceptance_commands:
  - cargo test -p riffdb-riffql-syntax --all-features
  - cargo test -p riffdb-query-ir --all-features
  - cargo test -p riffdb-query-compiler --all-features
  - cargo test -p riffdb-query-executor --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-query-module --all-features
  - cargo test --manifest-path examples/ticketdesk/Cargo.toml
  - ./scripts/check-generated
  - ./scripts/check-ticketdesk-symbolic-boundary
  - ./scripts/benchmark-application-path --assert-wp275
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace --all-features
  - cargo doc --workspace --no-deps
  - cargo deny check
  exit_gate: TicketDesk returns its complete detail shape through one named symbolic request and one authoritative
    snapshot; invalid collection fan-out fails closed; generated artifacts are reproducible; and PERF-002 remains
    satisfied without SQL, a second label query, or a public N+1 path.
  human_review_triggers: *id001
- id: WP-280
  name: Closed application-query authority and private execution proofs
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Query authority is compiler-derived, execution proofs remain private, and application callers cannot supply masks or storage authority.
  depends_on:
  - WP-275
  objective: Separate exact named-application, explicitly ad-hoc-agent, and raw kernel read authority, then authorize
    one complete query plan through a private non-reusable proof without changing compatible kernel RPC bytes.
  requirements:
  - SAFE-001
  - SAFE-002
  - SAFE-003
  required_adrs:
  - ADR-0007
  - ADR-0009
  - ADR-0013
  - ADR-0021
  - ADR-0022
  - ADR-0026
  - ADR-0027
  - ADR-0052
  - ADR-0055
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/app/v1/**
  - proto/riffdb/storage/v1/**
  - crates/riffdb-types/**
  - crates/riffdb-errors/**
  - crates/riffdb-proto/**
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-module/**
  - crates/riffdb-catalog/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-commit/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - tests/authorization/**
  - tests/service/**
  - tests/grpc/**
  - tests/mcp/**
  - fixtures/proto/**
  - fixtures/query-modules/**
  - fixtures/mcp/**
  - docs/riffql/**
  - docs/safety-by-construction.md
  deliverables:
  - Additive exact named-query and explicit ad-hoc check/explain/execute capability permissions with old/current
    durable and public compatibility fixtures
  - One policy-owned application-query authorization request containing exact contract/module/query/plan, partition,
    field/index, principal/revision, and output requirements
  - Private non-Clone, non-Serialize exact-plan authorization proof consumed once by the query executor
  - Disjoint raw ReadEntity/ScanIndex kernel authorization that cannot satisfy application-query policy
  - Revocation, substitution, cursor, field, partition, approval, audit, and transport-parity adversarial tests
  acceptance_commands:
  - cargo test -p riffdb-types --all-features
  - cargo test -p riffdb-proto --all-features
  - cargo test -p riffdb-policy --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-client-rust --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A named application capability executes only its exact module/query and cannot execute ad-hoc RiffQL or
    a raw kernel read; an ad-hoc agent capability cannot execute kernel reads; exact-plan proof substitution,
    retention, serialization, or cross-principal reuse is structurally impossible.
  human_review_triggers: *id001
- id: WP-285
  name: Whole-query cost authorization and execution fuel
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Whole-query cost admission and bounded runtime fuel are enforced before and during execution.
  depends_on:
  - WP-280
  objective: Replace per-step-only query budgeting with one canonical compiler-derived cost vector authorized once
    and enforced as decrementing execution fuel across every access and result-shaping path.
  requirements:
  - SAFE-006
  required_adrs:
  - ADR-0013
  - ADR-0027
  - ADR-0035
  - ADR-0038
  - ADR-0051
  - ADR-0053
  - ADR-0054
  - ADR-0055
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-policy/**
  - crates/riffdb-service/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-errors/**
  - crates/riffdb-types/**
  - crates/riffdb-testkit/**
  - fixtures/riffql/**
  - benchmarks/application-path/**
  - docs/riffql/**
  - docs/safety-by-construction.md
  deliverables:
  - Canonical plan-hashed cost vector for steps, scans, points, dependent keys, intermediates, projected values, and
    encoded response bytes
  - Capability budget comparison over the complete vector rather than independent step reuse
  - Consuming execution-fuel type with backend work accounting and no partial result/cursor on exhaustion
  - Boundary fixtures for exact budget, one-over, repeated scans, chained batches, empty work, and backend
    over-report/under-report defects
  - Updated explain output and application-path cost/latency evidence
  acceptance_commands:
  - cargo test -p riffdb-query-ir --all-features
  - cargo test -p riffdb-query-compiler --all-features
  - cargo test -p riffdb-query-executor --all-features
  - cargo test -p riffdb-policy --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - ./scripts/check-generated
  - ./scripts/benchmark-application-path --assert-wp275
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Repeating individually valid steps cannot exceed the admitted whole-query cost, backend work cannot
    exceed fuel, exhausted work releases no fields or cursor, and existing TicketDesk performance gates remain green.
  human_review_triggers: *id001
- id: WP-290
  name: Declared same-partition relationships
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Relationship traversal requires compiler-proven same-partition declarations and rejects unsafe locality.
  depends_on:
  - WP-275
  objective: Make required entity relationships explicit in contracts and reject every command that can establish a
    dangling reference without a visible exact target read, declared missing outcome, dependency, and commit recheck.
  requirements:
  - SAFE-004
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0013
  - ADR-0016
  - ADR-0051
  - ADR-0055
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-contract-syntax/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-invariant/**
  - crates/riffdb-runtime/**
  - crates/riffdb-catalog/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-errors/**
  - crates/riffdb-types/**
  - contracts/parser-fixtures/**
  - contracts/examples/**
  - examples/app-baseline/contracts/**
  - queries/ticketdesk/**
  - fixtures/compiler/**
  - fixtures/riffql/**
  - docs/riffql/**
  - docs/safety-by-construction.md
  deliverables:
  - Exact reviewed reference grammar and additive canonical IR/source-map/compatibility fixtures
  - Same-partition complete-target-key and exact-type validation with required relationships only
  - Compiler dataflow proof that every create/change is dominated by a visible exact target read and declared
    missing-target outcome
  - Ordinary dependency and commit-revalidation lowering with no hidden read or new runtime authority
  - TicketDesk project, reporter, assignee, member, comment, and label relationships plus dangling-reference negative
    corpus
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax --all-features
  - cargo test -p riffdb-contract-ir --all-features
  - cargo test -p riffdb-contract-compiler --all-features
  - cargo test -p riffdb-invariant --all-features
  - cargo test -p riffdb-runtime --all-features
  - cargo test -p riffdb-query-compiler --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: TicketDesk cannot compile a command that creates a dangling declared relationship; every accepted
    relationship check is visible in source/explain and revalidated at commit; no cross-partition or implicit
    relationship is accepted.
  human_review_triggers: *id001
- id: WP-295
  name: Declared same-partition uniqueness
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Symbolic uniqueness is statically scoped to a proven partition and enforced through compiled commands.
  depends_on:
  - WP-290
  objective: Make bounded same-partition uniqueness a contract property enforced under concurrency by compiler-
    derived conflict capabilities, exact unique-key validation, and atomic authoritative index maintenance.
  requirements:
  - SAFE-005
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0013
  - ADR-0016
  - ADR-0035
  - ADR-0055
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/storage/v1/**
  - crates/riffdb-contract-syntax/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-invariant/**
  - crates/riffdb-runtime/**
  - crates/riffdb-conflict/**
  - crates/riffdb-commit/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-catalog/**
  - crates/riffdb-types/**
  - crates/riffdb-proto/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-errors/**
  - crates/riffdb-testkit/**
  - tests/command_semantics/**
  - tests/recovery/**
  - contracts/parser-fixtures/**
  - examples/app-baseline/contracts/**
  - fixtures/compiler/**
  - fixtures/proto/**
  - docs/riffql/**
  - docs/safety-by-construction.md
  deliverables:
  - Exact reviewed unique-key grammar, canonical IR, conflict derivation, and compatibility fixtures
  - Input-computable same-partition unique conflict capability and exact transaction-current occupancy validation
  - Atomic entity/unique-index create and change semantics with no sequence allocation on collision
  - Concurrent equal-value, changed-value, replay, crash, recovery, corruption, and one-byte-boundary tests
  - TicketDesk scoped uniqueness declarations selected by the contract review, with no inferred global uniqueness
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax --all-features
  - cargo test -p riffdb-contract-ir --all-features
  - cargo test -p riffdb-contract-compiler --all-features
  - cargo test -p riffdb-conflict --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --test command_concurrency
  - cargo test -p riffdb-testkit --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Two concurrent commands cannot commit the same declared same-partition unique value, retries preserve the
    original result, crash recovery detects every torn or nonreciprocal unique index, and undeclared/global uniqueness
    is never claimed.
  human_review_triggers: *id001
- id: WP-300
  name: Safe application surface cutover and negative acceptance
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: The symbolic application surface is the documented default and negative gates reject kernel IDs, masks, keys, and handwritten protocol glue.
  depends_on:
  - WP-280
  - WP-285
  - WP-290
  - WP-295
  objective: Make named-only application authority the default across SDK, CLI presets, MCP, and TicketDesk, hide
    kernel/ad-hoc escape hatches behind separate credentials, and prove the product rule with intentionally bad apps.
  requirements:
  - SAFE-007
  - SAFE-008
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0009
  - ADR-0020
  - ADR-0027
  - ADR-0040
  - ADR-0041
  - ADR-0046
  - ADR-0052
  - ADR-0055
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-service/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-cli/**
  - crates/riffdb-server/**
  - crates/riffdb-query-module/**
  - crates/riffdb-testkit/**
  - clients/typescript/**
  - examples/ticketdesk/**
  - examples/app-baseline/**
  - queries/ticketdesk/**
  - tests/authorization/**
  - tests/service/**
  - tests/grpc/**
  - tests/mcp/**
  - fixtures/query-modules/**
  - fixtures/mcp/**
  - scripts/check-generated
  - scripts/check-ticketdesk-symbolic-boundary
  - scripts/riffdb-dev*
  - scripts/safety-by-construction*
  - benchmarks/application-path/**
  - docs/getting-started/**
  - docs/riffql/**
  - docs/safety-by-construction.md
  - README.md
  deliverables:
  - Separate stable-application, scoped-agent, and kernel/admin dev presets with no combined default
  - Named-only generated Rust/TypeScript clients and generated MCP application tools
  - Normal MCP/application catalogs without raw entity/index tools or ad-hoc source construction
  - Machine-checked bad-application corpus covering every SAFE-008 rejection and positive transport parity
  - Updated TicketDesk acceptance using declared relationships/uniqueness, exact named authority, and whole-query fuel
  - Published safety claim, limitations, migration instructions, and independent fresh-agent review
  acceptance_commands:
  - cargo test -p riffdb-auth --all-features
  - cargo test -p riffdb-policy --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-client-rust --all-features
  - cargo test -p riffdb-cli --all-features
  - cargo test --manifest-path examples/ticketdesk/Cargo.toml
  - ./scripts/safety-by-construction-acceptance
  - ./scripts/check-ticketdesk-symbolic-boundary
  - ./scripts/riffdb-dev-acceptance
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace --all-features
  - cargo doc --workspace --no-deps
  - cargo deny check
  exit_gate: Stable application credentials can invoke only their exact generated named operations; every bad-
    application canary fails before output/mutation; agent and kernel credentials remain explicitly separate; and the
    documented safety claim exactly matches tested supported behavior.
  human_review_triggers: *id001
- id: WP-305
  name: Complete generated application bindings and manifest
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Exact manifests generate complete typed query/command facades, outcomes, cursors, errors, and read fences without application transport glue.
  depends_on:
  - WP-300
  objective: Replace generated type/metadata fragments and handwritten application glue with one versioned application
    manifest and complete Rust/TypeScript named-operation clients that own transport, encoding, decoding, identity
    negotiation, cursors, read fences, errors, and retry-safe command recovery.
  requirements:
  - AAA-001
  - AAA-002
  required_adrs:
  - ADR-0005
  - ADR-0006
  - ADR-0018
  - ADR-0027
  - ADR-0040
  - ADR-0052
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/app/v1/**
  - crates/riffdb-types/**
  - crates/riffdb-errors/**
  - crates/riffdb-proto/**
  - crates/riffdb-query-module/**
  - crates/riffdb-service/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-cli/**
  - clients/typescript/**
  - fixtures/application-manifests/**
  - fixtures/query-modules/**
  - fixtures/proto/**
  - fixtures/mcp/**
  - scripts/generate-query-clients
  - scripts/check-application-bindings
  - docs/getting-started/**
  - docs/riffql/**
  - docs/agent-application-alpha.md
  deliverables:
  - Exact reviewed canonical application-manifest grammar, source maps, canonical bytes/hash, schemas, and
    old/current fixtures binding contract, query modules, generation targets, roles, and seed inputs
  - Generated Rust and TypeScript facades with typed named-query invocation, parameters, results, outcomes, optional
    fields, nested rows, cursors, and read-after-commit options
  - Exact contract/module negotiation and fail-closed mismatch behavior with no silent downgrade or rediscovery from
    ambient active state
  - Retry-safe generated command methods that preserve the caller/configured idempotency identity, resolve uncertainty,
    and expose every typed declared outcome
  - Generated MCP operation schemas from the same operation registry and zero-glue application boundary scanner
  acceptance_commands:
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-client-rust --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The same exact named operation is callable through generated Rust, TypeScript, and MCP bindings with
    identical typed observations; incompatible identities and uncertain responses fail safely; and the acceptance
    sources contain no handwritten map, codec, transport-status parser, RPC wrapper, or replacement retry identity.
  human_review_triggers: *id001
- id: WP-310
  name: Symbolic application roles and exact capability binding
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Symbolic roles compile and bind exact least-authority query, command, field, lineage, and bound permissions.
  depends_on:
  - WP-305
  objective: Compile manifest roles that name only commands and named queries into exact safe-application authority,
    then bind those roles to principals and tenant scope without exposing field masks, raw permission bits, kernel
    grants, or a manual ReadContract ritual.
  requirements:
  - AAA-003
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0009
  - ADR-0020
  - ADR-0021
  - ADR-0026
  - ADR-0052
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/app/v1/**
  - proto/riffdb/storage/v1/**
  - crates/riffdb-types/**
  - crates/riffdb-errors/**
  - crates/riffdb-proto/**
  - crates/riffdb-query-module/**
  - crates/riffdb-catalog/**
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-service/**
  - crates/riffdb-commit/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-cli/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - clients/typescript/**
  - tests/authorization/**
  - tests/service/**
  - tests/grpc/**
  - tests/mcp/**
  - fixtures/application-manifests/**
  - fixtures/proto/**
  - fixtures/mcp/**
  - docs/getting-started/**
  - docs/agent-application-alpha.md
  deliverables:
  - Exact role grammar and canonical role identity bound to one immutable application/contract/query-module identity
  - Compiler-derived contract description, named command/query, private field/index/partition/cost/result, lineage, and
    MCP visibility requirements with no reusable raw kernel grants
  - Audited exact role binding and revocation for principal, environment, tenant scope, and application identity
  - CLI and MCP role describe/check/bind surfaces using symbols only, including bounded missing-requirement diagnostics
  - Adversarial role substitution, stale module, hidden tool, field, tenant, revocation, mask/ID injection, and
    application-to-kernel escalation tests
  acceptance_commands:
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-auth --all-features
  - cargo test -p riffdb-policy --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-cli --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: An application author can describe, bind, revoke, and diagnose a symbolic role without field/index IDs,
    masks, capability bits, or explicit ReadContract grants; the role authorizes only exact named operations and cannot
    be converted into ad-hoc or kernel authority.
  human_review_triggers: *id001
- id: WP-315
  name: Structured semantic application errors across public surfaces
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: CLI, SDK, gRPC, and MCP preserve one bounded redaction-safe symbolic application error envelope.
  depends_on:
  - WP-310
  objective: Preserve bounded redaction-safe operation, contract, symbol, requirement, remediation, retry, and trace
    context from compiler/service failures through gRPC, Rust, TypeScript, CLI, and MCP without changing compatible
    kernel error bytes or exposing hidden schema and submitted values.
  requirements:
  - AAA-004
  - AAA-005
  required_adrs:
  - ADR-0006
  - ADR-0007
  - ADR-0008
  - ADR-0026
  - ADR-0027
  - ADR-0037
  - ADR-0040
  - ADR-0046
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/app/v1/**
  - crates/riffdb-types/**
  - crates/riffdb-errors/**
  - crates/riffdb-proto/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-query-module/**
  - crates/riffdb-catalog/**
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-service/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-cli/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - clients/typescript/**
  - tests/authorization/**
  - tests/service/**
  - tests/grpc/**
  - tests/mcp/**
  - fixtures/application-errors/**
  - fixtures/proto/**
  - fixtures/mcp/**
  - docs/getting-started/**
  - docs/agent-application-alpha.md
  deliverables:
  - Additive versioned application-error envelope with exact tags, bounds, closed codes/categories/recovery actions,
    symbolic context, source spans, fix codes, and trace/incident identity fixtures
  - One service-owned error-context builder that applies authorization visibility and redaction before construction
  - Total semantic conversion and equivalent machine-readable decoding/rendering for gRPC, Rust, TypeScript, CLI, MCP,
    logs, and traces
  - Contextual authorization, contract/module/input/decode/resource/cursor/revocation/storage/uncertainty/internal error
    matrix with no fabricated symbol for context-free failures
  - Secret, hidden-schema, arbitrary-prose, oversize, unknown-tag, malformed, downgrade, and cross-principal canaries
  acceptance_commands:
  - cargo test -p riffdb-errors --all-features
  - cargo test -p riffdb-proto --all-features
  - cargo test -p riffdb-diagnostics --all-features
  - cargo test -p riffdb-policy --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-client-rust --all-features
  - cargo test -p riffdb-cli --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every application-semantic failure carries the same bounded actionable machine context through every
    public surface, infrastructure failures carry honest operation/recovery/trace context, and no value, credential,
    hidden symbol, internal source, or unbounded prose crosses the public boundary.
  human_review_triggers: *id001
- id: WP-320
  name: Resumable bounded command batches and development seed
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Seed/import uses bounded resumable ordinary commands with independent idempotency, typed outcomes, progress, and backpressure.
  depends_on:
  - WP-310
  - WP-315
  objective: Productize seed/import as bounded concurrent orchestration of ordinary exact named commands with per-item
    idempotency, typed outcomes, progress, cancellation, backpressure, provenance correlation, and crash-safe resume,
    without adding generic bulk writes or collection-wide transaction semantics.
  requirements:
  - AAA-006
  - AAA-007
  required_adrs:
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0018
  - ADR-0027
  - ADR-0040
  - ADR-0041
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - proto/riffdb/app/v1/**
  - proto/riffdb/storage/v1/**
  - crates/riffdb-types/**
  - crates/riffdb-errors/**
  - crates/riffdb-proto/**
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-runtime/**
  - crates/riffdb-commit/**
  - crates/riffdb-service/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-cli/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - clients/typescript/**
  - tests/command_semantics/**
  - tests/service/**
  - tests/grpc/**
  - tests/mcp/**
  - tests/recovery/**
  - fixtures/batches/**
  - fixtures/proto/**
  - fixtures/mcp/**
  - scripts/riffdb-dev*
  - docs/getting-started/**
  - docs/agent-application-alpha.md
  deliverables:
  - Exact reviewed bounded batch request/progress/item-result/resume model and compatibility fixtures before choosing
    client-streamed orchestration, an additive RPC, or durable correlation fields
  - JSONL/stream batch runner with explicit command, concurrency, idempotency extraction, outcome policy, limits,
    backpressure, cancellation, stable progress, and redaction-safe reporting
  - Per-item ordinary service/authorization/runtime/coordinator execution and one bounded seed/import session
    provenance correlation with no shared application transaction
  - Checksummed resumable checkpoint/receipt that safely replays the same per-item keys after client/server crash
  - riffdb dev seed migration plus duplicate, partial, reorder, malformed, cancellation, retry, crash, revocation,
    deadline, and bounded-memory tests
  acceptance_commands:
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-client-rust --all-features
  - cargo test -p riffdb-cli --all-features
  - cargo test -p riffdb-testkit --all-features
  - ./scripts/riffdb-dev-acceptance
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A killed and resumed import returns one final typed result per input and no duplicate committed effect,
    bounds memory/concurrency/output, uses only ordinary commands, and makes no generic-write or batch-atomicity claim.
  human_review_triggers: *id001
- id: WP-325
  name: Canonical application scaffold, dev orchestration, and package boundary
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Deterministic scaffolding and dev orchestration generate, deploy, bind, seed, watch, and lint the public package boundary.
  depends_on:
  - WP-305
  - WP-310
  - WP-315
  - WP-320
  objective: Make the correct product surface the generated default through riffdb new, one canonical riffdb dev
    orchestrator, application-only Rust/TypeScript facades, templates and documentation, and mandatory boundary linting
    that rejects kernel dependencies and handwritten transport glue.
  requirements:
  - AAA-008
  - AAA-009
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0040
  - ADR-0041
  - ADR-0052
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-client-rust/**
  - crates/riffdb-query-module/**
  - crates/riffdb-auth/**
  - crates/riffdb-service/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-cli/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - clients/typescript/**
  - templates/application/**
  - fixtures/application-manifests/**
  - fixtures/application-boundaries/**
  - examples/agent-alpha/**
  - scripts/check-application-boundary
  - scripts/riffdb-new*
  - scripts/riffdb-dev*
  - docs/getting-started/**
  - docs/riffql/**
  - docs/agent-application-alpha.md
  - README.md
  deliverables:
  - Deterministic riffdb new Rust/TypeScript scaffold containing manifest, contract, queries, roles, seed, generated
    output roots, protected credential configuration, MCP setup, and one tested page/command
  - One riffdb dev state machine for process start, readiness, compile/deploy, role bind, generation, resumable seed,
    watch/reload, shutdown, and actionable recovery
  - Application facade/package defaults that exclude kernel operations, with explicit reviewed unstable/admin opt-in
    and separate credential for low-level use
  - Mandatory manifest/dependency/source boundary lint rejecting kernel crates/packages, requests, masks, encoded keys,
    protobuf/transport construction, and handwritten RiffDB adapters
  - Application-first quickstart/search/navigation and reproducible ten-minute empty-repository transcript
  acceptance_commands:
  - cargo test -p riffdb-cli --all-features
  - cargo test -p riffdb-client-rust --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/riffdb-new-acceptance
  - ./scripts/riffdb-dev-acceptance
  - ./scripts/check-application-boundary
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: From an empty directory, riffdb new followed by riffdb dev reaches one typed write and page-shaped read
    through generated application-only APIs, while dependency/source checks make accidental kernel use and handwritten
    transport glue unavailable in the generated project.
  human_review_triggers: *id001
- id: WP-330
  name: First-class TypeScript web-application parity
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: The generated TypeScript web path performs the same typed writes, nested reads, errors, cursors, and read fences as Rust.
  depends_on:
  - WP-325
  objective: Prove the TypeScript application path as a real generated-client web application with the same manifest,
    operation schemas, observations, errors, credentials, reload behavior, and safety boundary as the Rust path.
  requirements:
  - AAA-010
  required_adrs:
  - ADR-0027
  - ADR-0040
  - ADR-0052
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-query-module/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-cli/**
  - crates/riffdb-testkit/**
  - clients/typescript/**
  - examples/agent-alpha/**
  - fixtures/application-manifests/**
  - fixtures/application-errors/**
  - fixtures/application-parity/**
  - fixtures/query-modules/**
  - fixtures/mcp/**
  - scripts/agent-application-typescript-acceptance
  - scripts/check-application-bindings
  - scripts/check-application-boundary
  - docs/getting-started/**
  - docs/agent-application-alpha.md
  deliverables:
  - Pinned reviewed TypeScript runtime/build/test dependency graph and reproducible generated package
  - Working web application exercising generated queries, commands, typed outcomes, optional/nested fields, cursors,
    read-after-commit, structured errors, development credentials, hot reload, and MCP-assisted discovery
  - Language-neutral operation/observation/error fixtures consumed unchanged by Rust and TypeScript
  - Cross-language canonical input, result, outcome, cursor, identity mismatch, retry, and error differential tests
  - TypeScript dependency/source boundary proof with zero handwritten RiffDB transport or codec
  acceptance_commands:
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-client-rust --all-features
  - ./scripts/agent-application-typescript-acceptance
  - ./scripts/check-application-bindings
  - ./scripts/check-application-boundary
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The real TypeScript web application and Rust reference produce the same golden observations and recovery
    classifications using only generated application bindings, and neither language contains a handwritten adapter or
    kernel dependency.
  human_review_triggers: *id001
- id: WP-335
  name: Unfamiliar-domain evidence and bounded RiffQL closure
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Blog/CMS and Orders/Inventory compile through bounded RiffQL with their unsupported-shape evidence and approved additions retained.
  depends_on:
  - WP-330
  objective: Build blog/CMS and orders/inventory corpora through the complete Rust and TypeScript application paths,
    preserve every unsupported shape as diagnostic evidence, and close only repeated bounded RiffQL gaps after a
    separate accepted language/IR decision.
  requirements:
  - AAA-011
  required_adrs:
  - ADR-0002
  - ADR-0013
  - ADR-0051
  - ADR-0054
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-contract-syntax/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-riffql-syntax/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-query-module/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-client-rust/**
  - clients/typescript/**
  - contracts/agent-alpha/**
  - queries/agent-alpha/**
  - examples/agent-alpha/**
  - fixtures/agent-alpha/**
  - fixtures/compiler/**
  - fixtures/riffql/**
  - fixtures/query-modules/**
  - scripts/agent-domain-evidence
  - scripts/check-application-boundary
  - docs/riffql/**
  - docs/agent-application-alpha.md
  deliverables:
  - Blog/CMS and orders/inventory application manifests, contracts, roles, seeds, named queries, generated clients, and
    language-neutral golden workload observations
  - Versioned gap report classifying every failed shape by bounds, locality, index, authorization, cardinality,
    projection suitability, or missing bounded expressiveness
  - Source-spanned negative diagnostics explaining why a shape is rejected and the smallest existing
    index/relation/projection/decomposition remedy when available
  - Mandatory stop and separate accepted ADR for any new grammar/IR node, plan-hash domain input, access operator,
    authorization requirement, storage path, cursor state, or result semantic
  - If repeated evidence requires a language addition, after that review only the smallest accepted construct plus
    rejection fixtures for broader exists/aggregate/computed/fragment/relation/search/projection/general-join variants;
    otherwise fixtures proving the existing language completes both domains unchanged
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax --all-features
  - cargo test -p riffdb-contract-compiler --all-features
  - cargo test -p riffdb-riffql-syntax --all-features
  - cargo test -p riffdb-query-ir --all-features
  - cargo test -p riffdb-query-compiler --all-features
  - cargo test -p riffdb-query-executor --all-features
  - cargo test -p riffdb-service --all-features
  - ./scripts/agent-domain-evidence --assert-complete
  - ./scripts/check-application-boundary
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Both unfamiliar domains complete their bounded application workloads in Rust and TypeScript; every
    rejected shape is actionable; every added construct has repeated evidence and a separately accepted exact ADR; and
    no general SQL, arbitrary join, unbounded aggregate, or client-side semantic fallback enters the application path.
  human_review_triggers: *id001
- id: WP-340
  name: Agent Application Alpha sealed evaluation and release gate
  closure:
    status: complete_original_gate
    evidence: release/evidence/agent-application-alpha-gate-v3.json
    decision: The original four-run sealed Rust/TypeScript Agent Application Alpha gate is eligible; WP-579 owns the later six-run four-language deployable-alpha successor.
  depends_on:
  - WP-305
  - WP-310
  - WP-315
  - WP-320
  - WP-325
  - WP-330
  - WP-335
  objective: Evaluate the complete public application-authoring product in four sealed fresh-agent runs covering
    blog/CMS and orders/inventory in Rust and TypeScript, publish raw measurements and ratings, and block operational
    alpha or replication until every Agent Application Alpha threshold passes.
  requirements:
  - AAA-012
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0041
  - ADR-0051
  - ADR-0052
  - ADR-0055
  - ADR-0056
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - evaluations/agent-application-alpha/**
  - examples/agent-alpha/**
  - fixtures/agent-alpha/**
  - scripts/agent-application-alpha*
  - scripts/check-application-bindings
  - scripts/check-application-boundary
  - release/**
  - docs/getting-started/**
  - docs/riffql/**
  - docs/agent-application-alpha.md
  - docs/safety-by-construction.md
  - README.md
  deliverables:
  - Sealed release-derived evaluation bundle containing public binaries, docs, builder MCP, generated packages, and no
    RiffDB implementation or TicketDesk source
  - Four isolated empty-repository briefs/runs covering both domains in both languages with no shared agent context
  - Versioned redaction-safe event transcript and JSON report for interventions, kernel attempts/successes, handwritten
    glue, failures per feature, unsupported shapes, first-write/read/completion time, source access, and rating
  - Machine checks for generated hashes, zero kernel imports/success, zero handwritten RiffDB glue, no prohibited
    source access, complete golden observations, and all time/rating thresholds
  - Published gate decision, raw measurements, independent explanations, limitations, and owner-routed failures with
    no evaluator-side product workaround
  acceptance_commands:
  - ./scripts/agent-application-alpha-acceptance --runs 4 --sealed --assert-gate
  - ./scripts/check-application-bindings
  - ./scripts/check-application-boundary
  - ./scripts/agent-domain-evidence --assert-complete
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo test --workspace --all-features
  - cargo doc --workspace --no-deps
  - cargo deny check
  exit_gate: All four sealed independent runs meet every AAA-012 threshold, each rates the experience at least 8.5/10,
    raw evidence is published, and no unresolved product workaround, kernel escape, glue, source access, query gap, or
    cross-language semantic difference remains before single-node operational alpha.
  human_review_triggers: *id001
- id: WP-345
  name: Compiler-owned application source and exact lock
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Compiler-owned source, lock, generated identities, and deploy inputs share one canonical exact application identity.
  depends_on:
  - WP-340
  objective: Remove compiler-derived identities from author-owned application input while preserving exact fail-closed
    generation, authorization, and deployment through one deterministic compiler-owned lock and explicit local
    authoring operations.
  requirements:
  - AAA-013
  required_adrs:
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0013
  - ADR-0040
  - ADR-0052
  - ADR-0055
  - ADR-0056
  - ADR-0057
  allowed_paths:
  - work_packages.yaml
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-policy/**
  - crates/riffdb-cli/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-query-module/**
  - clients/typescript/**
  - crates/riffdb-types/**
  - crates/riffdb-query-module/**
  - crates/riffdb-cli/**
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-service/**
  - clients/typescript/**
  - templates/application/**
  - fixtures/application-manifests/**
  - fixtures/application-locks/**
  - scripts/check-application-bindings
  - scripts/check-generated
  - scripts/riffdb-dev*
  - docs/getting-started/**
  - docs/agent-application-alpha.md
  deliverables:
  - Additive author-owned symbolic application-source schema and compiler-owned exact lock schema with canonical
    bytes, source maps, domain-separated identities, old/current fixtures, and explicit V1 migration
  - Read-only application check, atomic lock write/check, exact locked generation, and bounded symbolic operation and
    role-authority diff
  - Lock coverage for normalized sources, contract/module/query/plan/schema/role identities, compiler formats, and
    generated artifacts without clocks, machine paths, credentials, active pointers, or runtime values
  - Development-only local lock refresh that leaves the last accepted generation active on failure and no production
    auto-refresh or ambient identity selection
  - Interrupted-write, partial-generation, stale source/lock, role widening, path, symlink, determinism, and V1
    compatibility tests
  acceptance_commands:
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-cli --all-features
  - ./scripts/check-application-bindings
  - ./scripts/riffdb-dev-acceptance
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: An author edits only symbolic source and one explicit compiler operation deterministically produces an
    exact reviewed lock and bindings; all drift fails before authorization or deployment; no lock operation performs a
    server-side or authority-bearing action.
  human_review_triggers: *id001
- id: WP-350
  name: Source-spanned authoring diagnostics
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Contract, query, role, lock, module, and deployment failures preserve actionable symbolic spans and bounded fixes.
  depends_on:
  - WP-345
  objective: Preserve bounded actionable compiler, planner, manifest, role, lock, scaffold, and generation diagnostics
    through CLI and builder MCP without leaking values, hidden schema, internal sources, or arbitrary engine prose.
  requirements:
  - AAA-014
  required_adrs:
  - ADR-0002
  - ADR-0006
  - ADR-0007
  - ADR-0008
  - ADR-0037
  - ADR-0040
  - ADR-0051
  - ADR-0055
  - ADR-0057
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-contract-syntax/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-riffql-syntax/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-query-module/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-cli/**
  - clients/typescript/**
  - fixtures/application-diagnostics/**
  - fixtures/compiler/**
  - fixtures/riffql/**
  - fixtures/mcp/**
  - docs/getting-started/**
  - docs/agent-application-alpha.md
  deliverables:
  - One closed versioned authoring-diagnostic value with bounded stage/code, path/span, symbol path, cause/fix codes,
    file-change disposition, and retry classification
  - Total conversions retaining existing parser/compiler/planner diagnostics instead of generic CompileContract,
    CompileQuery, Manifest, or role errors
  - Equivalent human and JSON CLI plus builder-MCP rendering with local source pointers but no persisted source values
  - Closed filesystem diagnostic classes for destination, parent, permission, regular-file/symlink, and interrupted
    staging failures
  - Source-span snapshots and semantic assertions for every changed diagnostic plus secret, hidden-schema, arbitrary-
    prose, oversize, malformed, and unknown-tag canaries
  acceptance_commands:
  - cargo test -p riffdb-contract-compiler --all-features
  - cargo test -p riffdb-query-compiler --all-features
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-diagnostics --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-cli --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Each representative source, plan, role, lock, manifest, scaffold, and output error names its actionable
    symbolic cause and exact local span when available across CLI and MCP, while every redaction and bound canary
    passes.
  human_review_triggers: *id001
- id: WP-355
  name: Empty-repository scaffold and complete public authoring kit
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: A fresh repository receives the complete public contract/query/role/seed/generated-client kit without internal source access.
  depends_on:
  - WP-350
  objective: Make a fresh application replaceable through bundled public material alone by supporting existing empty
    destinations and shipping the complete contract, manifest, command, query, role, inspection, and correction
    reference.
  requirements:
  - AAA-015
  required_adrs:
  - ADR-0002
  - ADR-0007
  - ADR-0008
  - ADR-0041
  - ADR-0051
  - ADR-0055
  - ADR-0057
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-contract-syntax/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-query-module/**
  - crates/riffdb-cli/**
  - clients/typescript/**
  - templates/application/**
  - fixtures/application-manifests/**
  - fixtures/application-locks/**
  - fixtures/application-diagnostics/**
  - examples/agent-alpha/**
  - release/agent-application-alpha/**
  - evaluations/agent-application-alpha/**
  - scripts/riffdb-new*
  - scripts/riffdb-dev*
  - scripts/agent-application-alpha-package
  - scripts/check-application-boundary
  - docs/contracts/**
  - docs/getting-started/**
  - docs/riffql/**
  - docs/agent-application-alpha.md
  - README.md
  deliverables:
  - Safe new-child and existing-empty-directory scaffold flows with bounded recoverable staging and no overwrite or
    destination-symlink traversal
  - Generated edit/check/lock/dev instructions that contain no manual hash, numeric ID, mask, encoded key, grant, or
    transport step
  - Complete versioned contract-language reference, command/invariant cookbook, application JSON Schema, inspection
    reference, domain-neutral examples, and negative examples with expected diagnostics and remedies
  - Release bundle and builder resources containing the same authoring kit without RiffDB implementation, TicketDesk,
    Blog, or Orders source
  - Public-only multi-entity replacement acceptance from an already-created empty repository
  acceptance_commands:
  - cargo test -p riffdb-cli --all-features
  - ./scripts/riffdb-new-acceptance
  - ./scripts/riffdb-dev-acceptance
  - ./scripts/agent-application-alpha-package-acceptance
  - ./scripts/check-application-boundary
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A public-only user starts in an existing empty directory, replaces the sample with a new multi-entity
    application, receives actionable diagnostics for mistakes, and completes a generated typed write/page read without
    private source or manual compiler identity.
  human_review_triggers: *id001
- id: WP-360
  name: Offline TypeScript and builder-MCP authoring parity
  closure:
    status: complete
    evidence: docs/architecture/wp-210-360-closure.md
    decision: Offline TypeScript generation/runtime and builder MCP expose the same exact public application identities and diagnostics.
  depends_on:
  - WP-355
  objective: Make the generated TypeScript repository and builder MCP complete first-class consumers of the exact
    application source, lock, diagnostics, and generation model without granting builder tools runtime authority.
  requirements:
  - AAA-016
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0027
  - ADR-0040
  - ADR-0052
  - ADR-0055
  - ADR-0057
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-query-module/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-cli/**
  - clients/typescript/**
  - templates/application/**
  - fixtures/application-locks/**
  - fixtures/application-diagnostics/**
  - fixtures/application-parity/**
  - fixtures/mcp/**
  - examples/agent-alpha/**
  - release/agent-application-alpha/**
  - scripts/agent-application-typescript-acceptance
  - scripts/check-application-bindings
  - scripts/check-application-boundary
  - scripts/agent-application-alpha-package*
  - docs/getting-started/**
  - docs/agent-application-alpha.md
  deliverables:
  - Complete offline TypeScript runtime/compiler/Node-type/lockfile/check/build/start setup and real server-side HTTP
    starter generated without network or manual package edits
  - Same exact application lock, operation schemas, diagnostics, read fences, typed outcomes, cursors, and errors as
    Rust
  - Builder-MCP resources for grammar/schema/examples and tools for describe, check, diagnostic explanation,
    lock/authority preview, explicit lock write, and exact generation
  - Static and runtime proof that builder MCP has no credential, storage, deployment, role-binding, command execution,
    query execution, or kernel dependency
  - CLI/MCP and Rust/TypeScript differential fixtures for locks, diagnostics, operation schemas, and golden observations
  acceptance_commands:
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-mcp-stdio --all-features
  - cargo test -p riffdb-cli --all-features
  - ./scripts/agent-application-typescript-acceptance
  - ./scripts/check-application-bindings
  - ./scripts/check-application-boundary
  - ./scripts/agent-application-alpha-package-acceptance
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A generated TypeScript web application builds and serves offline, while CLI and credential-less builder
    MCP produce identical checked lock/diagnostic/generation semantics and no builder operation can reach runtime or
    authority.
  human_review_triggers: *id001
- id: WP-362
  name: Growing-database command throughput and durability investigation
  closure:
    status: complete
    evidence: docs/performance/wp-362-command-growth.md
    decision: The O(history) audit defect was isolated and removed, with flat growth evidence and full startup validation retained.
  depends_on:
  - WP-340
  objective: Reproduce, decompose, and safely correct the observed synchronous redb command degradation as retained
    state grows without weakening acknowledgement durability, atomic command records, crash recovery, or sole
    coordinator ownership.
  requirements:
  - PERF-003
  required_adrs:
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0012
  - ADR-0057
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-observability/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - benchmarks/command-growth/**
  - fixtures/performance/**
  - scripts/benchmark-command-growth
  - docs/performance/**
  - docs/agent-application-alpha.md
  deliverables:
  - Reproducible size-sweep benchmark separating evaluation, transaction-current reads, table/page work, commit call,
    durable flush, queue wait, file growth, and recovery cost under the exact configured durability
  - Equivalent memory/redb semantic preflight and crash/reopen/idempotency/provenance/outcome/commit reciprocity checks
    before every reported performance result
  - Evidence comparing one-phase/two-phase engine mechanics and bounded multi-command group scheduling without changing
    production behavior before a separate accepted durability/performance ADR
  - Smallest accepted optimization plus deterministic scheduling, cancellation, fairness, crash, uncertainty, replay,
    sequence, and atomicity tests
  - Checked reference-machine report satisfying PERF-003 without weaker durability or acknowledgement semantics
  acceptance_commands:
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-server --all-features
  - cargo test --manifest-path benchmarks/command-growth/Cargo.toml --workspace
  - ./scripts/benchmark-command-growth --assert-perf-003
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Growing retained state preserves at least half of initial steady-state command throughput and at least 50
    committed commands per second on the checked profile, with unchanged configured durability, complete crash/replay
    evidence, and an accepted ADR for any scheduling, engine-durability, or atomic-layout change.
  human_review_triggers: *id001
- id: WP-364
  name: Bounded group durability and two-transition audited commands
  closure:
    status: complete
    evidence: docs/performance/wp-364-group-durability.md
    decision: Bounded group durability and audited transition semantics shipped with exact crash, replay, and acknowledgement coverage.
  depends_on:
  - WP-362
  objective: Amortize redb Immediate durability across independently safe command transitions and reduce a normal
    successful application command from four durable transitions to two without weakening authorization, audit,
    idempotency, provenance, atomicity, uncertainty recovery, or acknowledgement.
  requirements:
  - PERF-004
  required_adrs:
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0006
  - ADR-0007
  - ADR-0009
  - ADR-0012
  - ADR-0021
  - ADR-0023
  - ADR-0057
  - ADR-0058
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-client-rust/**
  - crates/riffdb-query-module/**
  - crates/riffdb-cli/**
  - crates/riffdb-observability/**
  - crates/riffdb-testkit/**
  - tests/command_semantics/**
  - tests/grpc/**
  - tests/service_audit/**
  - tests/service_audit_recovery/**
  - tests/storage_recovery/**
  - clients/typescript/**
  - benchmarks/command-growth/**
  - fixtures/performance/**
  - fixtures/application-parity/**
  - fixtures/query-modules/**
  - scripts/benchmark-command-growth
  - scripts/check-application-bindings
  - scripts/check-application-boundary
  - docs/getting-started/**
  - docs/performance/**
  - docs/agent-application-alpha.md
  deliverables:
  - One typed FIFO online-writer scheduler with a 16-transition, 200-microsecond, 16-MiB grouping policy and explicit
    Sync oracle
  - Production Group durability over unchanged redb Immediate two-phase commits, with independent per-command
    acknowledgement and idempotent unknown-commit resolution
  - Atomic fresh-policy-authorized Started plus Pending admission and atomic successful command graph plus linked
    terminal audit, guarded by a private complete-outcome release proof
  - Generated Rust and TypeScript bounded batch helpers with typed per-item outcomes, stable idempotency, progress,
    checkpoint, and backpressure over the ordinary symbolic command path
  - Deterministic fairness, cancellation, bounds, sequence, crash, reopen, replay, audit, provenance, and uncertainty
    tests plus comparative Sync and Group performance evidence satisfying PERF-004
  acceptance_commands:
  - cargo test -p riffdb-storage-api --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - cargo test -p riffdb-client-rust --all-features
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-cli --all-features
  - cargo test --manifest-path benchmarks/command-growth/Cargo.toml --workspace
  - ./scripts/benchmark-command-growth --assert-perf-004
  - ./scripts/check-application-bindings
  - ./scripts/check-application-boundary
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Production commands use bounded Group durability and retain independent semantic results; a newly
    executed successful command has exactly two durable transitions; crash and unknown-commit tests preserve every
    command and audit invariant; generated batches expose no bulk-write escape hatch; and checked evidence satisfies
    PERF-004 against the explicit Sync oracle.
  human_review_triggers: *id001
- id: WP-365
  name: Public-only rehearsal, qualified evidence, and canary gate
  closure:
    status: complete
    evidence: release/evidence/agent-application-alpha-rehearsal-v1.json
    decision: Public-only package rehearsal, recovery cases, exact qualified events, and sealed canaries pass and retain immutable prior failures.
  depends_on:
  - WP-360
  - WP-362
  - WP-364
  objective: Prove the repaired application-authoring product through sealed public-only four-way rehearsals and two
    fresh canary agents before spending another official four-agent campaign.
  requirements:
  - AAA-017
  - AAA-018
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0041
  - ADR-0051
  - ADR-0052
  - ADR-0055
  - ADR-0056
  - ADR-0057
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - adr/0055-safe-application-surface-and-declared-integrity.md
  - crates/riffdb-cli/**
  - crates/riffdb-diagnostics/**
  - crates/riffdb-query-module/**
  - evaluations/agent-application-alpha/**
  - examples/agent-alpha/**
  - contracts/agent-alpha/**
  - queries/agent-alpha/**
  - fixtures/agent-alpha/**
  - fixtures/application-diagnostics/**
  - fixtures/query-modules/**
  - fixtures/performance/**
  - release/agent-application-alpha/**
  - release/evidence/**
  - scripts/agent-application-alpha*
  - scripts/agent-domain-evidence
  - scripts/benchmark-command-growth
  - scripts/check-application-bindings
  - scripts/check-application-boundary
  - scripts/generate-query-clients
  - scripts/riffdb-dev
  - scripts/riffdb-dev-acceptance
  - scripts/riffdb-dev-installed
  - scripts/riffdb-new-acceptance
  - templates/application/**
  - docs/getting-started/**
  - docs/riffql/**
  - docs/agent-application-alpha.md
  deliverables:
  - One release-derived bundle that completes Blog and Orders in Rust and TypeScript from existing empty directories
    using only public inputs
  - Public-only injected invalid-source, stale-lock, missing-index, unsafe-role, interrupted-generation, and
    growing-database performance recovery evidence
  - Qualified event semantics requiring exact generated RiffDB identity evidence for first write/page read
  - Immutable campaign directories and exact campaign/bundle selection without deleting or rewriting prior failures
  - Two fresh isolated canary runs covering Blog/Rust and Orders/TypeScript at every AAA-012 threshold
  acceptance_commands:
  - ./scripts/agent-application-alpha-package-acceptance
  - ./scripts/agent-application-alpha-rehearsal --assert-complete
  - ./scripts/agent-application-alpha-canary-acceptance --runs 2 --sealed --assert-gate
  - ./scripts/benchmark-command-growth --assert-perf-003
  - ./scripts/check-application-bindings
  - ./scripts/check-application-boundary
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: All public-only rehearsals and failure recoveries pass, both fresh canaries meet every official threshold,
    every first-success metric carries exact RiffDB identity evidence, and campaign 01 remains byte-identical.
  human_review_triggers: *id001
- id: WP-366
  name: Same-partition aggregate concurrency and PostgreSQL write parity
  closure:
    status: complete_with_superseded_first_receipt
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Same-partition external-read proofs, one mutation aggregate, bounded public batches, and fixed group evidence shipped; the flawed first comparison is explicitly superseded by corrected later app-baseline evidence.
  depends_on:
  - WP-364
  objective: Recover ordinary application write concurrency by permitting exact same-partition observations across
    aggregates while retaining one mutation aggregate, static conflict ownership, transaction-current dependency
    validation, redb Immediate durability, and the complete command/audit protocol.
  requirements:
  - PERF-005
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0005
  - ADR-0007
  - ADR-0016
  - ADR-0023
  - ADR-0058
  - ADR-0059
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-commit/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-proto/**
  - crates/riffdb-query-module/**
  - crates/riffdb-client-rust/**
  - clients/typescript/**
  - examples/app-baseline/**
  - fixtures/application-manifests/**
  - fixtures/query-modules/**
  - fixtures/proto/**
  - fixtures/performance/**
  - tests/command_semantics/**
  - tests/grpc/**
  - scripts/check-generated
  - scripts/generate-query-clients
  - scripts/generate-proto
  - benchmarks/run-app-baseline
  - docs/performance/**
  deliverables:
  - Compiler locality analysis and lowering that accept same-partition external reads, reject cross-partition reads and
    multi-aggregate create/mutate bindings, anchor locality on the mutation aggregate, and derive conflicts only from it
  - TicketDesk aggregate model whose ticket/comment/label-link and project/member atomic operations retain one aggregate
    while independent application records acquire fine-grained conflict keys
  - Fixed-cardinality server evidence for effective completion-group sizes 1 through 64 with no sensitive or
    high-cardinality labels
  - Bounded public command-batch transport whose items retain independent authorization, idempotency, outcomes,
    provenance, audit lifecycle, uncertainty recovery, and non-atomic semantics
  - Full same-run public TicketDesk versus PostgreSQL report satisfying the seed and representative unary mutation
    two-times parity gates under unchanged redb Immediate durability
  - Compiler, command semantic, grouping, crash, uncertainty, generation, formatting, and lint evidence
  acceptance_commands:
  - cargo test -p riffdb-contract-compiler --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-server --all-features
  - cargo test -p riffdb-commit --test command_concurrency --test lost_response_replay --all-features
  - ./scripts/generate-query-clients --check
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./benchmarks/run-app-baseline --full --assert-write-parity
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Compiler proofs preserve one mutation aggregate and exact same-partition external-read revalidation;
    TicketDesk retains its atomic application operations while producing compatible effective groups; and the full
    same-run public seed plus unary mutation p50 are each no more than twice PostgreSQL with unchanged durability,
    authorization, idempotency, provenance, audit, and uncertainty semantics.
  human_review_triggers: *id001
- id: WP-367
  name: Bounded scheduler utilization and admission selection
  closure:
    status: complete
    evidence: docs/performance/wp-367-369-command-pipeline.md
    decision: Ordered bounded drain, oldest-item window, hard barriers, grouped replay selection, and closed scheduler telemetry are implemented and tested.
  depends_on:
  - WP-366
  objective: Make the accepted group window effective, prevent safe observational traffic from truncating compatible
    command groups, and amortize historical idempotency selection and replay audit writes without allowing commands to
    overtake policy, catalog, administrative-write, shutdown, fencing, or readiness barriers.
  requirements:
  - PERF-006
  required_adrs:
  - ADR-0003
  - ADR-0005
  - ADR-0007
  - ADR-0023
  - ADR-0058
  - ADR-0059
  - ADR-0060
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-idempotency/**
  - crates/riffdb-commit/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-proto/**
  - crates/riffdb-client-rust/**
  - clients/typescript/**
  - tests/observability/**
  - tests/command_semantics/**
  - tests/grpc/**
  - benchmarks/command-growth/**
  - benchmarks/run-app-baseline
  - fixtures/performance/**
  - scripts/benchmark-command-growth
  - scripts/check-generated
  - docs/performance/**
  deliverables:
  - Deterministic 200-microsecond oldest-item group window with explicit full, byte-bound, expired, barrier,
    incompatible, shutdown, and fencing dispatch reasons
  - Complete bounded receiver drain into an ordered actor-local deque, stable deferral, 64-command selection, hard
    control-plane barriers, and deterministic fairness/cancellation/shutdown schedules
  - Bounded read-only historical idempotency selection for one public batch with transaction-current admission recheck
    unchanged, plus grouped replay Started transitions
  - Count-and-byte-bounded coordinator capacity above one maximum group and measured client/server concurrency sweep
    retaining the public 16-command transport ceiling
  - Redaction-safe group-fill, queue-depth, selection-batch, replay, wait, and dispatch-reason evidence
  acceptance_commands:
  - cargo test -p riffdb-idempotency --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - ./scripts/benchmark-command-growth --assert-perf-006
  - ./benchmarks/run-app-baseline --full --assert-write-parity
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The saturated conflict-free profile fills bounded groups without an intermediate empty poll forcing
    dispatch, replay and fresh admission amortize physical transactions, every hard barrier remains ordered, p99
    scheduler wait stays below two milliseconds, and all crash/idempotency/audit semantics remain unchanged.
  human_review_triggers: *id001
- id: WP-368
  name: Activated redb storage lanes and concurrent application reads
  closure:
    status: complete
    evidence: docs/performance/wp-367-369-command-pipeline.md
    decision: Activation yields one private writer and cloneable least-authority MVCC readers without a process-wide operational read mutex.
  depends_on:
  - WP-367
  objective: Release one private authoritative writer and cloneable least-authority MVCC readers from one completed
    activation so admission fsync, no-destination publication, and unrelated reads no longer serialize on a
    process-wide storage mutex.
  requirements:
  - PERF-007
  required_adrs:
  - ADR-0004
  - ADR-0007
  - ADR-0033
  - ADR-0034
  - ADR-0053
  - ADR-0060
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-query-module/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - crates/riffdb-testkit/**
  - tests/command_semantics/**
  - tests/grpc/**
  - benchmarks/application-path/**
  - benchmarks/run-app-baseline
  - benchmarks/run-concurrent-application
  - fixtures/performance/**
  - scripts/benchmark-concurrent-application
  - scripts/check-generated
  - docs/performance/**
  deliverables:
  - Activation-owned split yielding exactly one non-cloneable writer and narrow cloneable readers without exposing redb
    or arbitrary transaction callbacks
  - Admission and completion transaction ownership with no outer process-wide mutex held across redb work or flush
  - Independent redb MVCC read transactions for authoritative, catalog, capability, projection, idempotency, and
    RiffQL consumers with bounded port admission
  - Recovery-seeded exact no-destination outbox latch updated from commit metadata without a per-command storage scan
  - Exact-identity named-query parse cache and measured bounded runtime/port-worker configuration
  - Concurrent reader/writer, snapshot, activation, poison, fencing, shutdown, crash, reopen, and outbox-readiness tests
  acceptance_commands:
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-query-executor --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/benchmark-concurrent-application --assert-perf-007
  - ./benchmarks/run-app-baseline --full --assert-write-parity
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Public reads open independent MVCC transactions and scale under concurrent command fsync without
    process-wide mutex wait, no-destination readiness remains exact across crash/reopen, named queries do not reparse
    deployed source per request, and all activation and fail-closed boundaries pass.
  human_review_triggers: *id001
- id: WP-369
  name: Immutable command artifacts, bounded parallel preparation, and application parity
  closure:
    status: complete_with_failed_performance_gate
    evidence: docs/performance/wp-367-369-command-pipeline.md
    decision: Immutable artifacts, pay-once normalization, bounded preparation, and ordered commit behavior shipped, but the then-current strict parity gate remained red and no pass is claimed.
  depends_on:
  - WP-368
  objective: Remove the remaining serialized clone, normalization, lookup, sizing, encoding, verification, and
    deterministic-evaluation amplification while retaining immutable exact identities, fresh authorization, one
    admission-ordered writer, and transaction-current validation.
  requirements:
  - PERF-008
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0006
  - ADR-0007
  - ADR-0012
  - ADR-0022
  - ADR-0023
  - ADR-0058
  - ADR-0059
  - ADR-0060
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-conflict/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-catalog/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-runtime/**
  - crates/riffdb-invariant/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-module/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - crates/riffdb-testkit/**
  - crates/riffdb-client-rust/**
  - clients/typescript/**
  - examples/app-baseline/**
  - tests/command_semantics/**
  - tests/grpc/**
  - tests/observability/**
  - tests/service/**
  - benchmarks/command-growth/**
  - benchmarks/application-path/**
  - benchmarks/run-app-baseline
  - benchmarks/run-concurrent-application
  - fixtures/performance/**
  - fixtures/proto/**
  - scripts/benchmark-command-growth
  - scripts/benchmark-concurrent-application
  - scripts/check-generated
  - docs/performance/**
  deliverables:
  - Immutable Arc-owned exact contract, schema, command-plan, and query-program artifacts with complete hash-keyed
    invalidation and no pointer-only authority
  - Single command-input materialization plus exact post-wait identity revalidation and every required fresh
    authorization/response-release safe point
  - Non-materializing durable structural reservation, encode-once exact charge reuse, and byte-identical compatibility
    fixtures with full external/recovery validation retained
  - Bounded preparation workers for compiler-declared snapshot reads, deterministic evaluation, validation, and typed
    record preparation with canonical conflict acquisition and an admission-ordinal reorder buffer
  - One sole writer committing only the earliest contiguous compatible prepared commands with exact current-state
    revalidation, retry, cancellation, sequence, crash, and uncertainty behavior
  - Same-run 1/8/32/64-client read, write, and mixed PostgreSQL comparison satisfying PERF-008
  acceptance_commands:
  - cargo test -p riffdb-contract-ir --all-features
  - cargo test -p riffdb-storage-api --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-runtime --all-features
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/benchmark-command-growth --assert-perf-008
  - ./scripts/benchmark-concurrent-application --assert-perf-008
  - ./benchmarks/run-app-baseline --full --assert-all-parity
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every command and read remains on the safe symbolic path; one ordered writer retains all durable
    semantics; the full seed and every unary scenario are within 1.10 times same-run PostgreSQL; the 32-client mixed
    workload meets throughput and p95 parity; and no compatibility, safety, crash, or recovery fixture changes.
  human_review_triggers: *id001
- id: WP-370
  name: Agent Application Alpha sealed evaluation campaign 02
  closure:
    status: complete
    evidence: docs/agent-application-alpha.md
    decision: Campaign 02 passed its sealed unfamiliar-domain gate and is retained as immutable predecessor evidence to the later four-language campaign.
  depends_on:
  - WP-365
  - WP-367
  - WP-368
  - WP-369
  - WP-379
  objective: Repeat the official Blog and Orders Rust/TypeScript evaluation against one new sealed bundle and publish
    an eligible gate only when all unchanged Agent Application Alpha thresholds pass.
  requirements:
  - AAA-012
  - AAA-018
  required_adrs:
  - ADR-0007
  - ADR-0008
  - ADR-0041
  - ADR-0051
  - ADR-0052
  - ADR-0055
  - ADR-0056
  - ADR-0057
  allowed_paths:
  - evaluations/agent-application-alpha/**
  - release/evidence/**
  - scripts/agent-application-alpha*
  - docs/agent-application-alpha.md
  - benchmarks/command-growth/Cargo.lock
  deliverables:
  - New campaign ID, bundle hash, four fresh isolated agent identities, and exact domain/language coverage
  - Four immutable qualified reports/transcripts with exact generated-operation identity evidence and raw explanations
  - Machine-checked unchanged time, golden, boundary, source, glue, kernel, shape, performance, and rating thresholds
  - Published eligible or not-eligible decision retaining every prior campaign without waiver or evaluator workaround
  acceptance_commands:
  - ./scripts/agent-application-alpha-acceptance --campaign campaign-02 --runs 4 --sealed --assert-gate
  - ./scripts/agent-application-alpha-rehearsal --assert-complete
  - ./scripts/benchmark-command-growth --assert-perf-003
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  exit_gate: All four fresh campaign-02 runs complete the unfamiliar-domain workloads, satisfy every unchanged AAA-012
    and PERF-003 threshold, rate the product at least 8.5, and publish immutable raw evidence before operational alpha
    or replication.
  human_review_triggers: *id001
- id: WP-371
  name: Semantic durability profiles and standard one-phase commits
  closure:
    status: complete
    evidence: docs/performance/wp-371-semantic-durability-profiles.md
    decision: Acknowledged-crash durability is the public guarantee; standard one-phase and hardened oracle profiles preserve exact semantics.
  depends_on:
  - WP-369
  objective: Separate the application acknowledgement guarantee from the redb mechanism and make Immediate one-phase
    checksum commits the standard hot application profile while retaining a hardened two-phase oracle.
  requirements:
  - PERF-009
  required_adrs:
  - ADR-0004
  - ADR-0023
  - ADR-0058
  - ADR-0060
  - ADR-0061
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - crates/riffdb-testkit/**
  - tests/command_semantics/**
  - tests/recovery/**
  - tests/grpc/**
  - benchmarks/command-growth/**
  - benchmarks/run-app-baseline
  - scripts/benchmark-command-growth
  - docs/performance/**
  deliverables:
  - Closed standard-one-phase and hardened-two-phase engine profiles with identical application semantics
  - Standard application writes using Immediate one-phase commits while initialization, migration, and the oracle may
    remain hardened
  - Explicit non-Byzantine host/storage threat statement and no public command-controlled durability downgrade
  - Process crash, reopen, unknown-status, complete-or-absent, and same-key recovery evidence for both profiles
  - Same-run application and engine A/B report without assuming a fixed improvement
  acceptance_commands:
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/benchmark-command-growth --assert-perf-009
  - ./benchmarks/run-app-baseline --full
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The standard profile acknowledges only complete one-phase Immediate commits, the hardened oracle retains
    two-phase behavior, both pass the same crash/idempotency graph, and the measured report names the actual gain.
  human_review_triggers: *id001
- id: WP-372
  name: Atomic terminal admission for synchronous commands
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Ordinary synchronous commands use the fused atomic terminal path only where pre-commit crash is observationally prior admission.
  depends_on:
  - WP-371
  objective: Remove durable Pending churn from new synchronous commands by moving only bounded side-effect-free
    preparation before one freshly authorized vacant-to-terminal authoritative transaction.
  requirements:
  - PERF-010
  required_adrs:
  - ADR-0003
  - ADR-0005
  - ADR-0007
  - ADR-0012
  - ADR-0023
  - ADR-0058
  - ADR-0061
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-catalog/**
  - crates/riffdb-runtime/**
  - crates/riffdb-conflict/**
  - crates/riffdb-idempotency/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - crates/riffdb-testkit/**
  - tests/command_semantics/**
  - tests/service/**
  - tests/service_audit/**
  - tests/service_audit_recovery/**
  - tests/grpc/**
  - examples/app-baseline/**
  - benchmarks/run-app-baseline
  - docs/performance/**
  deliverables:
  - Move-only terminal-admission preparation joining initial authorization, fixed invocation time, conflict lease,
    evaluated command, fresh post-evaluation authorization, and complete release proof
  - One transaction from vacant identity to Started plus complete terminal command graph plus linked terminal audit
  - Direct dependency-validated vacant-to-ExecutionFailed transition
  - Exact concurrent equal, different-input, replay, deployment, cancellation, revocation, retry, and unknown-status
    semantics with one terminal winner and no leaked preparation
  - Historical Pending resume compatibility and startup fixtures retained
  - Process failpoints proving pre-terminal absence or complete terminal recovery with no partial audit or graph
  acceptance_commands:
  - cargo test -p riffdb-idempotency --all-features
  - cargo test -p riffdb-storage-api --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - ./benchmarks/run-app-baseline --full
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: New synchronous commands create no Pending row, expose no speculative result or effect, freshly authorize
    before one complete terminal transaction, and retain exact replay, crash, audit, provenance, and uncertainty safety.
  human_review_triggers: *id001
- id: WP-373
  name: Compact durable storage format V2 and migration
  closure:
    status: complete
    evidence: docs/performance/wp-373-compact-storage-v2.md
    decision: Compact versioned durable records and the bounded restartable predecessor migration are shipped with compatibility fixtures.
  depends_on:
  - WP-372
  objective: Replace repeated full StoredEnvelope framing with a compact checked V2 record header and migrate V1
    databases through one bounded restartable fail-closed format transition.
  requirements:
  - PERF-011
  required_adrs:
  - ADR-0004
  - ADR-0006
  - ADR-0022
  - ADR-0039
  - ADR-0061
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - proto/riffdb/storage/**
  - crates/riffdb-proto/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-catalog/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - fixtures/proto/**
  - fixtures/storage/**
  - tests/recovery/**
  - tests/grpc/**
  - scripts/generate-proto
  - scripts/check-generated
  - docs/performance/**
  deliverables:
  - Compact V2 header with format marker/version, closed record tag, nonzero schema revision, and payload CRC32C
  - One durable database registry digest binding all tag/revision pairs to canonical descriptor hashes
  - Canonical V2 encoder/decoder with strict re-encode, key/value, bound, registry, checksum, and unknown-state rejection
  - Bounded resumable V1-to-V2 migration with crash checkpoints, mixed-revision startup state, and final verification
  - Immutable V1 fixtures plus new V2 golden vectors and measured bytes by record class
  acceptance_commands:
  - cargo test -p riffdb-proto --all-features
  - cargo test -p riffdb-storage-api --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-catalog --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every new durable row uses compact V2 framing, V1 migrates restartably without mixed unproved activation,
    and every corruption, substitution, unknown revision, and crash state fails closed.
  human_review_triggers: *id001
- id: WP-374
  name: Single authoritative event payload and reciprocal references
  closure:
    status: complete
    evidence: docs/performance/wp-374-authoritative-event-references.md
    decision: Events have one authoritative payload with checked reciprocal references from commits, routes, and outbox state.
  depends_on:
  - WP-373
  objective: Store each complete durable event exactly once and replace commit/outbox payload copies with atomic exact
    ID-and-hash references while preserving dispatch, projection, recovery, and provenance guarantees.
  requirements:
  - PERF-011
  required_adrs:
  - ADR-0004
  - ADR-0010
  - ADR-0017
  - ADR-0022
  - ADR-0049
  - ADR-0061
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - proto/riffdb/storage/**
  - crates/riffdb-proto/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-catalog/**
  - crates/riffdb-outbox/**
  - crates/riffdb-projection/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - fixtures/proto/**
  - fixtures/storage/**
  - tests/command_semantics/**
  - tests/recovery/**
  - scripts/generate-proto
  - scripts/check-generated
  - docs/performance/**
  deliverables:
  - Versioned EventReference carrying exact EventId and EventHash
  - Commit and outbox records containing references rather than complete event payloads
  - Atomic event/reference staging and exact startup three-way reciprocity
  - Dispatcher and projection materialization loading one authoritative event with hash verification
  - Compatibility migration and byte-amplification evidence
  acceptance_commands:
  - cargo test -p riffdb-storage-api --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-outbox --all-features
  - cargo test -p riffdb-projection --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Event payload bytes have one authoritative owner, every reference is atomic and reciprocal, and dispatch,
    projection, crash recovery, and corruption detection retain exact behavior.
  human_review_triggers: *id001
- id: WP-375
  name: Conservative partition-index generations and renewed parity gate
  closure:
    status: complete
    evidence: docs/performance/wp-375-partition-index-generations.md
    decision: Generation invalidation remains conservative and bounded while avoiding redundant prefix rows; the renewed gate retained every miss.
  depends_on:
  - WP-374
  objective: Replace prefix epoch write amplification with one conservative generation per affected partition/index
    pair, then rerun the complete semantic and PostgreSQL parity campaign before agent alpha.
  requirements:
  - PERF-012
  - PERF-008
  required_adrs:
  - ADR-0003
  - ADR-0004
  - ADR-0038
  - ADR-0055
  - ADR-0059
  - ADR-0061
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - proto/**
  - fixtures/proto/**
  - fixtures/application-manifests/**
  - fixtures/query-modules/**
  - clients/typescript/**
  - crates/riffdb-proto/**
  - crates/riffdb-types/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-catalog/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-compiler/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-testkit/**
  - tests/command_semantics/**
  - tests/grpc/**
  - examples/app-baseline/**
  - benchmarks/**
  - scripts/benchmark-command-growth
  - scripts/benchmark-concurrent-application
  - docs/performance/**
  deliverables:
  - Canonical partition/index generation identity and exactly one advance per affected pair per command
  - Range dependencies and cursors bound to conservative generations with no stale acceptance
  - Compiler rejection and source-spanned diagnostics for an unprovable write-influencing range
  - Migration from prefix epoch rows and removal of prefix write fan-out
  - Full crash, safety, seed, unary, 1/8/32/64-client, and same-run PostgreSQL evidence
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/benchmark-command-growth --assert-perf-012
  - ./scripts/benchmark-concurrent-application --assert-perf-008
  - ./benchmarks/run-app-baseline --full --assert-all-parity
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every range remains conservatively safe with at most one generation write per affected partition/index,
    all crash and corruption evidence passes, and the complete application parity gate determines WP-370 eligibility.
  human_review_triggers: *id001
- id: WP-376
  name: Timer-free self-clocking command scheduler
  closure:
    status: complete
    evidence: docs/performance/wp-376-timer-free-scheduler.md
    decision: Solo commands no longer pay rounded Tokio timer parks; saturated grouping remains naturally self-clocking and bounded.
  depends_on:
  - WP-375
  objective: Remove sub-millisecond Tokio timer-wheel parks from low-load command and historical-idempotency lanes
    while retaining bounded drain grouping, FIFO barriers, replay selection, fairness, and every durable command
    guarantee.
  requirements:
  - PERF-006
  required_adrs:
  - ADR-0003
  - ADR-0005
  - ADR-0007
  - ADR-0023
  - ADR-0058
  - ADR-0060
  - ADR-0061
  allowed_paths:
  - SPEC.md
  - work_packages.yaml
  - crates/riffdb-commit/**
  - crates/riffdb-observability/**
  - crates/riffdb-server/**
  - tests/command_semantics/**
  - tests/observability/**
  - benchmarks/run-app-baseline
  - benchmarks/command-growth/**
  - scripts/benchmark-command-growth
  - docs/performance/**
  deliverables:
  - Drain-only bounded historical-idempotency grouping with no independent timed wait
  - Self-clocking writer collection that dispatches an idle singleton immediately and drains work accumulated during
    prior commits
  - Deterministic solo, queued, saturated, barrier, cancellation, fencing, receiver-close, and fairness schedules
  - Redaction-safe telemetry proving no timer-wheel park and retaining exact group-size and dispatch-reason evidence
  - Same-run unary mutation and seed evidence under the unchanged semantic acknowledgement profile
  acceptance_commands:
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-server --all-features
  - cargo test -p riffdb-observability --all-features
  - ./scripts/benchmark-command-growth --assert-perf-006
  - ./benchmarks/run-app-baseline --full
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A lone command and lone historical inspection perform no timed group-formation park, saturated work still
    fills bounded compatible groups from queued arrivals, every hard barrier remains ordered, and the measured unary
    mutation regression is removed without weaker durability or safety. Full seed parity remains WP-378/WP-379.
  human_review_triggers: *id001
- id: WP-377
  name: Exact-identity read hot path
  closure:
    status: complete
    evidence: docs/performance/wp-377-safe-read-hot-path.md
    decision: Exact query programs and trusted-in-generation durable records reuse pay-once proof material without weakening authorization or corruption checks.
  depends_on:
  - WP-376
  objective: Remove immutable query-artifact cloning, repeated capability reconstruction, and redundant durable
    decoding from named reads while preserving every authorization safe point, canonicality check, semantic
    reconstruction, partition proof, and snapshot guarantee.
  requirements:
  - PERF-007
  - PERF-008
  required_adrs:
  - ADR-0004
  - ADR-0006
  - ADR-0007
  - ADR-0033
  - ADR-0053
  - ADR-0060
  - ADR-0061
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - crates/riffdb-auth/**
  - crates/riffdb-policy/**
  - crates/riffdb-proto/**
  - crates/riffdb-query-ir/**
  - crates/riffdb-query-module/**
  - crates/riffdb-query-executor/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - tests/grpc/**
  - tests/observability/**
  - benchmarks/application-path/**
  - benchmarks/run-app-baseline
  - benchmarks/run-concurrent-application
  - scripts/benchmark-concurrent-application
  - fixtures/proto/**
  - fixtures/query-modules/**
  - docs/performance/**
  deliverables:
  - Arc-owned parsed named-query document and access program under exact contract/module/query identities
  - Cheap exact capability-revision revalidation at existing pre-execution and pre-release safe points
  - One checked durable decode path reusing structural and typed evidence without reducing canonical or semantic checks
  - Independent MVCC reader evidence and explicit storage-bridge wait telemetry
  - Same-run point, list, detail, and 1/8/32/64-client public read evidence
  acceptance_commands:
  - cargo test -p riffdb-auth --all-features
  - cargo test -p riffdb-proto --all-features
  - cargo test -p riffdb-query-module --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/benchmark-concurrent-application --assert-perf-007
  - ./benchmarks/run-app-baseline --full
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Named reads allocate no deep query artifact clone, retain all three current-policy safe points through exact
    revision proofs, decode each durable value through one fail-closed checked reconstruction, and materially improve
    public read latency and concurrency without changing results or compatibility bytes.
  human_review_triggers: *id001
- id: WP-378
  name: Incremental command preparation and structural reservation
  closure:
    status: complete
    evidence: docs/performance/wp-378-command-cpu-pipeline.md
    decision: Group compatibility, command preparation, ownership, and wire reservation are incremental and bounded with exact durable bytes.
  depends_on:
  - WP-377
  objective: Remove residual serialized command CPU through measured stage decomposition, incremental compatibility,
    structural sequence-free sizing, move/shared ownership, and deduplicated same-transaction observations while
    retaining transaction-current validation and one admission-ordered writer.
  requirements:
  - PERF-008
  required_adrs:
  - ADR-0002
  - ADR-0003
  - ADR-0004
  - ADR-0006
  - ADR-0007
  - ADR-0012
  - ADR-0022
  - ADR-0023
  - ADR-0059
  - ADR-0060
  - ADR-0061
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - crates/riffdb-contract-ir/**
  - crates/riffdb-conflict/**
  - crates/riffdb-runtime/**
  - crates/riffdb-invariant/**
  - crates/riffdb-proto/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-memory/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-commit/**
  - crates/riffdb-service/**
  - crates/riffdb-server/**
  - crates/riffdb-observability/**
  - crates/riffdb-testkit/**
  - tests/command_semantics/**
  - tests/recovery/**
  - tests/observability/**
  - benchmarks/command-growth/**
  - benchmarks/application-path/**
  - benchmarks/run-app-baseline
  - scripts/benchmark-command-growth
  - fixtures/proto/**
  - docs/performance/**
  deliverables:
  - Bounded redaction-safe prep, evaluate, validate, encode, stage, commit, and publish decomposition
  - Incremental exact compatibility accumulator with linear group construction and unchanged FIFO partition result
  - Non-materializing sequence-free structural reservation dominating every final compact durable encoding
  - Move or immutable shared ownership for repeated mutation, event, plan, and schema graphs
  - One transaction-local observation per exact identity where repeated reads add no fresher semantic evidence
  acceptance_commands:
  - cargo test -p riffdb-contract-ir --all-features
  - cargo test -p riffdb-proto --all-features
  - cargo test -p riffdb-storage-api --all-features
  - cargo test -p riffdb-storage-memory --all-features
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --all-features
  - cargo test -p riffdb-runtime --all-features
  - cargo test -p riffdb-service --all-features
  - cargo test -p riffdb-server --all-features
  - ./scripts/benchmark-command-growth --assert-perf-008
  - ./benchmarks/run-app-baseline --full
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Stage evidence shows no unexplained serialized command cost, compatible group construction is linear,
    reservations remain conservative and fixtures byte-identical, transaction-current checks remain fresh, and the full
    seed materially closes the remaining same-run PostgreSQL gap.
  human_review_triggers: *id001
- id: WP-379
  name: Renewed public application parity and alpha eligibility
  closure:
    status: complete_failed_performance_gate
    evidence: docs/performance/wp-379-parity-decision.md
    decision: The complete renewed comparison published an immutable not-eligible decision with every miss retained; current qualification belongs to WP-623/WP-674.
  depends_on:
  - WP-378
  objective: Run the complete semantic, crash, unary, seed, and concurrent PostgreSQL comparison after the measured
    timer, read, and command-CPU corrections and publish an eligible decision only when unchanged PERF-008 thresholds
    pass.
  requirements:
  - PERF-008
  - AAA-012
  required_adrs:
  - ADR-0004
  - ADR-0007
  - ADR-0055
  - ADR-0058
  - ADR-0059
  - ADR-0060
  - ADR-0061
  allowed_paths:
  - SPEC.md
  - work_packages.yaml
  - examples/app-baseline/**
  - benchmarks/**
  - fixtures/performance/**
  - scripts/benchmark-command-growth
  - scripts/benchmark-concurrent-application
  - docs/performance/**
  - release/evidence/**
  deliverables:
  - Complete semantic and crash preflight under the exact checked standard durability profile
  - Same-run full TicketDesk seed and every representative unary comparison
  - Same-run public 1/8/32/64-client read, write, and mixed comparison
  - Immutable machine-readable eligible or not-eligible report retaining every miss without waiver
  - Exact WP-370 bundle prerequisite when and only when every PERF-008 threshold passes
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/benchmark-command-growth --assert-perf-008
  - ./scripts/benchmark-concurrent-application --assert-perf-008
  - ./benchmarks/run-app-baseline --full --assert-all-parity
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The full seed and every representative unary p50 are at most 1.10 times same-run PostgreSQL; at 32
    clients public mixed throughput is at least 0.90 times PostgreSQL and p95 is at most 1.25 times PostgreSQL; every
    semantic and crash gate passes; only then may WP-370 start.
  human_review_triggers: *id001
- id: WP-380
  name: Multi-database and MCP compatibility interface freeze
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Multi-database selectors, MCP compatibility, isolation, naming, and fail-closed ambiguity rules are frozen and implemented.
  depends_on:
  - WP-200
  objective: Freeze the approved bounded database-selector boundary and underscore-only MCP compatibility cut before
    server, client, compiler, and generated artifacts change concurrently.
  requirements:
  - MDB-001
  - MDB-002
  - MDB-003
  - MDB-004
  - MDB-005
  - MDB-006
  - MDB-007
  - MDB-008
  - MDB-009
  - MDB-010
  - MCP-025
  - MCP-026
  - MCP-034
  required_adrs:
  - ADR-0001
  - ADR-0007
  - ADR-0008
  - ADR-0020
  - ADR-0024
  - ADR-0040
  - ADR-0063
  - ADR-0064
  allowed_paths:
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-types/**
  - fixtures/types/**
  - docs/architecture/**
  deliverables:
  - Accepted selector-before-authentication and per-database isolation record
  - Accepted underscore-only MCP tool-name and agent-presentation record
  - Canonical bounded DatabaseAlias owned by riffdb-types
  - Requirement and package coverage for implementation slices
  acceptance_commands:
  - cargo test -p riffdb-types --all-features
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-types --all-targets --all-features -- -D warnings
  exit_gate: Shared ownership, selector grammar, compatibility classification, public routing behavior, and
    implementation package boundaries are accepted and machine covered before dependent code changes.
  human_review_triggers: *id001
- id: WP-381
  name: Underscore MCP compatibility and agent ergonomics
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: MCP tool/resource naming accepts the documented compatibility spelling while retaining one canonical symbolic operation identity.
  depends_on:
  - WP-380
  objective: Regenerate every fixed and dynamic MCP surface with underscore-only tool names and add actionable
    redacted input diagnostics, invocation-oriented command documentation, and an agent cookbook.
  requirements:
  - MCP-020
  - MCP-021
  - MCP-023
  - MCP-025
  - MCP-026
  - MCP-034
  - MCP-040
  - MCP-043
  - MCP-045
  - MCP-049
  required_adrs:
  - ADR-0008
  - ADR-0020
  - ADR-0040
  - ADR-0046
  - ADR-0047
  - ADR-0064
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - adr/**
  - crates/riffdb-contract-ir/**
  - crates/riffdb-contract-compiler/**
  - crates/riffdb-catalog/**
  - crates/riffdb-api-mcp/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-proto/**
  - crates/riffdb-sdk-rust/**
  - crates/riffdb-service/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-server/**
  - crates/riffdb-cli/**
  - fixtures/**
  - tests/mcp/**
  - tests/grpc/**
  - tests/service/**
  - scripts/**
  - examples/**
  - docs/mcp/**
  - docs/generated/**
  deliverables:
  - Compiler and catalog owned underscore-only command tool registry
  - Underscore-only fixed MCP registry with no dotted aliases
  - Deterministic bounded redacted first-violation input errors
  - Invocation-oriented generated command documentation with separate plan resources
  - Agent cookbook and regenerated compatibility fixtures
  acceptance_commands:
  - cargo test -p riffdb-contract-compiler --all-features
  - cargo test -p riffdb-catalog --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-mcp-stdio --all-features
  - cargo test --test mcp_conformance --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every advertised tool name is underscore-only, stale dotted names fail closed, invalid inputs identify one
    redacted schema location, and authorized command docs are directly callable without exposing IR dumps.
  human_review_triggers: *id001
- id: WP-382
  name: Database selector configuration and public clients
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Database aliases propagate through configuration, metadata, CLI, generated clients, MCP, and errors without becoming application authority.
  depends_on:
  - WP-380
  objective: Add bounded multi-database configuration and propagate one exact database alias through gRPC, hosted MCP,
    stdio MCP, CLI, and Rust SDK request construction before authentication.
  requirements:
  - MDB-002
  - MDB-003
  - MDB-004
  - MDB-005
  - MDB-008
  - MDB-009
  required_adrs:
  - ADR-0006
  - ADR-0007
  - ADR-0008
  - ADR-0040
  - ADR-0041
  - ADR-0044
  - ADR-0063
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - crates/riffdb-types/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-server/src/config.rs
  - crates/riffdb-server/src/hosted_mcp.rs
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-sdk-rust/**
  - crates/riffdb-cli/**
  - release/config/**
  - fixtures/**
  - tests/grpc/**
  - tests/mcp/**
  - docs/installation.md
  - docs/mcp/**
  deliverables:
  - Legacy-compatible one-database and bounded multi-database TOML parsing
  - Exact selector extraction and pre-authentication failure behavior
  - Database-bound gRPC, hosted MCP, stdio MCP, CLI, and SDK clients
  - Public selector and configuration fixtures
  acceptance_commands:
  - cargo test -p riffdb-types --all-features
  - cargo test -p riffdb-api-grpc --all-features
  - cargo test -p riffdb-server --all-features
  - cargo test -p riffdb-mcp-stdio --all-features
  - cargo test -p riffdb-sdk-rust --all-features
  - cargo test -p riffdb-cli --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public client binds one canonical alias, routing occurs before authentication, and the old
    single-database configuration remains an exact implicit-default compatibility path.
  human_review_triggers: *id001
- id: WP-383
  name: Isolated multi-database server composition
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Each configured database owns isolated storage, runtime, readiness, credentials, backup roots, and lifecycle under one server process.
  depends_on:
  - WP-382
  objective: Replace the single production graph with a bounded alias registry of independently started, routed,
    maintained, and drained database graphs without sharing semantic state.
  requirements:
  - MDB-001
  - MDB-003
  - MDB-005
  - MDB-006
  - MDB-007
  - MDB-009
  - MDB-010
  - REC-003
  required_adrs:
  - ADR-0001
  - ADR-0004
  - ADR-0007
  - ADR-0024
  - ADR-0029
  - ADR-0049
  - ADR-0050
  - ADR-0063
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - SPEC.md
  - work_packages.yaml
  - crates/riffdb-types/**
  - crates/riffdb-auth/**
  - crates/riffdb-api-grpc/**
  - crates/riffdb-server/**
  - crates/riffdb-storage-api/**
  - crates/riffdb-storage-redb/**
  - crates/riffdb-service/**
  - crates/riffdb-observability/**
  - fixtures/**
  - tests/server_composition/**
  - tests/grpc/**
  - tests/mcp/**
  - tests/maintenance/**
  - tests/recovery/**
  deliverables:
  - Bounded canonical database registry with one complete production graph per alias
  - Per-database lifecycle, readiness, maintenance, recovery, sequence, authorization, and notification isolation
  - All-or-nothing initial structural startup and bounded all-graph shutdown
  - Two-database semantic and crash-isolation evidence
  acceptance_commands:
  - cargo test -p riffdb-server --all-features
  - cargo test --test server_composition --all-features
  - cargo test --test grpc_end_to_end --all-features
  - cargo test --test offline_maintenance_recovery --all-features
  - cargo test --test full_recovery_matrix --all-features
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Two databases with distinct contracts and credentials operate and recover independently in one process,
    while selector failures, startup failure, and maintenance remain fail closed.
  human_review_triggers: *id001
- id: WP-384
  name: Multi-database operator workflow
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Install, add-database, bootstrap, deploy, bind, seed, health, backup, and removal workflows are alias-aware and receipted.
  depends_on:
  - WP-381
  - WP-383
  objective: Make named databases installable and usable through the source installer, systemd unit, bootstrap,
    capability, CLI, SDK, MCP, and example-application workflows.
  requirements:
  - MDB-004
  - MDB-008
  - MDB-009
  - MCP-010
  required_adrs:
  - ADR-0041
  - ADR-0062
  - ADR-0063
  - ADR-0064
  allowed_paths:
  - Cargo.toml
  - Cargo.lock
  - README.md
  - SPEC.md
  - work_packages.yaml
  - crates/riffdb-cli/**
  - crates/riffdb-sdk-rust/**
  - crates/riffdb-mcp-stdio/**
  - crates/riffdb-server/**
  - release/**
  - scripts/**
  - docs/**
  - examples/**
  - tests/**
  deliverables:
  - User and system installation configuration for one or more named databases
  - Database-aware bootstrap, capability, CLI, SDK, and MCP examples
  - Generic empty-database workflow with no bundled application assumption
  - PostgreSQL-versus-RiffDB example selectable as its own database
  acceptance_commands:
  - ./scripts/release-source-bootstrap-smoke
  - ./scripts/release-source-install-smoke
  - cargo test -p riffdb-cli --all-features
  - cargo test -p riffdb-mcp-stdio --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A user can install one daemon, create and select independent application databases, and connect through
    CLI or MCP without application-specific installer authority.
  human_review_triggers: *id001
- id: WP-385
  name: Multi-database and MCP end-to-end proof
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: End-to-end tests prove target identity, cross-database isolation, MCP selection, and fail-closed wrong-alias behavior.
  depends_on:
  - WP-384
  objective: Publish final conformance and crash evidence for two isolated application databases and the
    underscore-only agent-facing MCP surface through the installed one-process workflow.
  requirements:
  - MDB-001
  - MDB-003
  - MDB-004
  - MDB-005
  - MDB-006
  - MDB-007
  - MDB-008
  - MDB-009
  - MDB-010
  - MCP-025
  - MCP-026
  - MCP-034
  required_adrs:
  - ADR-0063
  - ADR-0064
  allowed_paths:
  - README.md
  - SPEC.md
  - work_packages.yaml
  - crates/**
  - fixtures/**
  - tests/**
  - scripts/**
  - release/**
  - docs/**
  - examples/**
  deliverables:
  - Installed two-database gRPC, CLI, stdio MCP, and hosted MCP conformance evidence
  - Cross-database credential, data, sequence, idempotency, maintenance, locator, and recovery isolation evidence
  - Underscore-only tool catalog and actionable error compatibility evidence
  - Final operator and agent documentation
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/release-source-bootstrap-smoke
  - ./scripts/release-source-install-smoke
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One installed daemon serves two isolated applications through every supported client, all MCP tools are
    underscore-only and agent-callable, and cross-database and crash tests preserve every existing semantic guarantee.
  human_review_triggers: *id001
- id: WP-386
  name: First real application interface freeze
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: The first-real-app public contract freezes one exact lock/deploy/role/query identity and the two-credential authoring/application model.
  depends_on:
  - WP-385
  objective: Freeze offline database addition, natural symbolic JSON, lock-only deployment, selected-database response
    identity, application MCP discovery, and safe diagnostics before their implementations change public surfaces.
  requirements: [DX-001, DX-002, DX-003, DX-004, DX-005, DX-006, DX-007, DX-008, DX-009, DX-010, DX-011, DX-012]
  required_adrs: [ADR-0055, ADR-0056, ADR-0057, ADR-0062, ADR-0063, ADR-0065]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, proto/**, fixtures/proto/**, docs/architecture/**]
  deliverables:
  - Accepted first-real-application record and exact additive public interface fixture
  - Requirement and work-package coverage for every implementation slice
  acceptance_commands:
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  exit_gate: Every public, operational, authorization, recovery, and compatibility decision is accepted before code changes.
  human_review_triggers: *id001
- id: WP-387
  name: Journaled database addition and safe startup diagnostics
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Database addition is journaled and restartable; invalid path containment and startup failures surface typed actionable diagnostics.
  depends_on: [WP-386]
  objective: Add one database to installer-owned user or system configuration without manual edits or loss of legacy state.
  requirements: [DX-001, DX-002, DX-003]
  required_adrs: [ADR-0063, ADR-0065]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-server/**, scripts/**, xtask/**, release/**, tests/**, docs/**]
  deliverables:
  - User and system --add-database workflow with private resumable phase state
  - Legacy default preservation and sibling backup-root migration
  - Safe config-check and startup diagnostics
  acceptance_commands:
  - cargo test -p riffdb-server --all-features
  - ./scripts/release-source-install-smoke
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-server --all-targets --all-features -- -D warnings
  exit_gate: Interrupted legacy-to-multi migration resumes and startup failures name a safe actionable configuration cause.
  human_review_triggers: *id001
- id: WP-388
  name: Schema-directed application JSON and symbolic failures
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: CLI and MCP JSON values are schema-directed, and failures name symbolic fields/types rather than only numeric IDs.
  depends_on: [WP-386]
  objective: Make every application surface consume the same natural schema-directed JSON and return name-only errors.
  requirements: [DX-004, DX-005, DX-008]
  required_adrs: [ADR-0055, ADR-0065]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-errors/**, crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-client-rust/**, crates/riffdb-cli/**, proto/**, fixtures/**, tests/**, docs/**]
  deliverables:
  - Natural value parity across CLI, batch, MCP, Rust, and TypeScript
  - Inline, @file, stdin, and legacy input sources with distinct diagnostics
  - Symbolic field/type and module/query failures with no numeric IDs
  acceptance_commands:
  - cargo test -p riffdb-service -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-cli --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Equivalent natural input behaves identically on every surface and all public failures are actionable and redacted.
  human_review_triggers: *id001
- id: WP-389
  name: Locked installed application deployment and provisioning
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: Installed deployment verifies the exact lock before mutation, deploys contract/query together, reconciles widened roles, provisions credentials, and seeds resumably.
  depends_on: [WP-387, WP-388]
  objective: Deploy, explicitly provision, and seed an exact locked application through one resumable installed workflow.
  requirements: [DX-006, DX-007, DX-008]
  required_adrs: [ADR-0057, ADR-0062, ADR-0065]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-cli/**, crates/riffdb-query-module/**, crates/riffdb-client-rust/**,
    scripts/**, fixtures/**, tests/**, docs/**]
  deliverables:
  - Lock-only application deploy with deterministic resumable stages
  - Explicit short-lived role provisioning and per-database private state
  - Application-credential command-batch seed and standalone bind-dev-role
  acceptance_commands:
  - cargo test -p riffdb-cli -p riffdb-query-module --all-features
  - ./scripts/check-application-bindings
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-cli --all-targets --all-features -- -D warnings
  exit_gate: One command resumes exact deployment/provision/seed without hidden authority or handwritten identities.
  human_review_triggers: *id001
- id: WP-390
  name: Application MCP tools, selected database identity, and doctor
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: MCP exposes selected database identity, health/active state, and role-filtered application tools with doctor coverage and no privileged bypass.
  depends_on: [WP-388, WP-389]
  objective: Make the selected database and authorized named application operations obvious on every public agent surface.
  requirements: [DX-009, DX-010, DX-011]
  required_adrs: [ADR-0040, ADR-0064, ADR-0065]
  allowed_paths: [SPEC.md, work_packages.yaml, proto/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**, crates/riffdb-client-rust/**, crates/riffdb-cli/**,
    crates/riffdb-service/**, crates/riffdb-policy/**, crates/riffdb-server/**, fixtures/**, tests/**, scripts/**,
    docs/**]
  deliverables:
  - Database alias and audience on public health/active-contract responses
  - Authorized deployed named-query MCP tools
  - Application MCP configuration, doctor operation, and pre-initialize EOF diagnostic
  acceptance_commands:
  - cargo test -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-mcp-stdio --all-features
  - cargo test --test mcp_conformance --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  exit_gate: Agents can verify their database and discover only role-authorized application tools with a distinct credential.
  human_review_triggers: *id001
- id: WP-391
  name: First real application authoring reference
  closure:
    status: complete
    evidence: docs/architecture/wp-362-391-closure.md
    decision: The handbook documents single-to-multi migration, deployment, value tags, reserved identifiers, credentials, MCP wiring, and troubleshooting.
  depends_on: [WP-386]
  objective: Publish a complete generated and tested authoring reference for contracts, RiffQL, values, deployment, and MCP.
  requirements: [DX-003, DX-004, DX-005, DX-012]
  required_adrs: [ADR-0002, ADR-0055, ADR-0065]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-contract-syntax/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-query-compiler/**, crates/riffdb-cli/**, docs/**, fixtures/**, scripts/**, tests/**]
  deliverables:
  - Generated reserved-word, comment, and exact namespace reference
  - Fixed-take-first RiffQL guidance and actionable cost diagnostics
  - Installed multi-database application and two-credential MCP recipe
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-compiler -p riffdb-query-compiler --all-features
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  exit_gate: A fresh agent can complete the installed workflow from public docs without searching implementation source.
  human_review_triggers: *id001
- id: WP-392
  closure:
    status: complete
    evidence: evaluations/installed-first-application/campaigns/wp392-installed-01.json
    decision: The immutable independent-agent campaign passed the installed multi-database workflow with least authority. Current HEAD also passes both source install/bootstrap smokes, the complete workspace suite, generation, requirements, formatting, Clippy, and dependency policy.
  name: Installed first real application acceptance
  depends_on: [WP-387, WP-388, WP-389, WP-390, WP-391]
  objective: Prove the complete legacy-to-multi installed application workflow under interruption and independent agent use.
  requirements: [DX-001, DX-002, DX-003, DX-004, DX-005, DX-006, DX-007, DX-008, DX-009, DX-010, DX-011, DX-012]
  required_adrs: [ADR-0065]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/**, proto/**, fixtures/**, tests/**, scripts/**, release/**, docs/**,
    examples/**]
  deliverables:
  - User and simulated-system legacy migration and application acceptance
  - Process interruption matrix for migration, deployment, role binding, and seed
  - Fresh independent-agent report with no manual configuration or source inspection
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/release-source-install-smoke
  - ./scripts/release-source-bootstrap-smoke
  - ./scripts/installed-first-application-agent-acceptance wp392-installed-01
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A fresh agent completes the installed multi-database application using only public surfaces and least authority.
  human_review_triggers: *id001
- id: WP-393
  closure:
    status: complete
    evidence: docs/getting-started/APPLICATION-MANIFEST.md
    decision: Successor evolution preserves append-only compatibility, reports source-named incompatibilities, and keeps lock, deployment, and role identity exact. Current compiler, deployment, recovery, and generated-binding suites pass.
  name: Additive contract evolution and actionable deployment diagnostics
  depends_on: [WP-386]
  objective: Permit the approved bounded additive schema subset and preserve safe compiler and compatibility detail
    through every deployment surface.
  requirements: [EVL-001, EVL-002, EVL-003, EVL-004, EVL-005, EVL-006]
  required_adrs: [ADR-0002, ADR-0007, ADR-0013, ADR-0028, ADR-0041, ADR-0046, ADR-0066]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-catalog/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-mcp-stdio/**, crates/riffdb-client-rust/**, crates/riffdb-cli/**,
    proto/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Stable compatibility codes and comparator support for the approved additive schema subset
  - Exact-version catalog activation for RequiresExplicitVersion successors
  - Structured invalid-source and incompatible-candidate deployment results through every public transport
  - Contract evolution and explicit pre-alpha reset documentation
  acceptance_commands:
  - cargo test -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-catalog --all-features
  - cargo test -p riffdb-service -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-cli --all-features
  - cargo test --test mcp_conformance --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: An additive EA-shaped successor activates without data reset, incompatible source/schema reports an
    actionable bounded result on every surface, and no existing schema or durable row is reinterpreted.
  human_review_triggers: *id001
- id: WP-394
  closure:
    status: complete
    evidence: adr/0074-python-application-driver.md
    decision: ADR-0074 is accepted and freezes the native Rust-backed Python surface, compatibility, retry, packaging, platform, and authority boundaries implemented by the successor packages.
  name: Python application-driver interface freeze
  depends_on: [WP-390]
  objective: Freeze the application-only Python package, native Rust bridge, value model, sync/async facade,
    compatibility formats, packaging targets, and dependency boundary before implementation.
  requirements: [PYD-001, PYD-002, PYD-003, PYD-004, PYD-005, PYD-006, PYD-007, PYD-008, PYD-009, PYD-010,
    PYD-011, PYD-012]
  required_adrs: [ADR-0037, ADR-0040, ADR-0041, ADR-0052, ADR-0055, ADR-0056, ADR-0057, ADR-0063, ADR-0065,
    ADR-0074]
  allowed_paths: [AGENTS.md, SPEC.md, work_packages.yaml, adr/**, fixtures/application-parity/**]
  deliverables:
  - Exact accepted ADR reconciling SYS-002 with non-Rust generated application bindings and approving native dependencies
  - Frozen public Python type stub and private native-bridge ownership rules
  - Complete requirement and dependency mapping for implementation packages
  acceptance_commands:
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  exit_gate: Every public, native-dependency, retry, compatibility, packaging, and platform decision is accepted before
    Python or PyO3 production code is added.
  human_review_triggers: *id001
- id: WP-395
  closure:
    status: complete
    evidence: docs/python-driver.md
    decision: Application manifest V2 and deterministic Python generation are implemented without changing V1 identity. Canonical binding generation and the installed Python scaffold acceptance pass on current HEAD.
  name: Python manifest V2 and generated bindings
  depends_on: [WP-393, WP-394]
  objective: Add exact V2 source/lock formats, explicit migration, and deterministic complete Python bindings while
    preserving V1 byte-for-byte.
  requirements: [PYD-004, PYD-005, PYD-008, PYD-009, PYD-012]
  required_adrs: [ADR-0052, ADR-0055, ADR-0056, ADR-0057, ADR-0065, ADR-0074]
  allowed_paths: [Cargo.toml, Cargo.lock, crates/riffdb-query-module/**, crates/riffdb-cli/**, clients/python/**,
    fixtures/application-manifests/**, fixtures/application-locks/**, fixtures/application-parity/**, scripts/**,
    docs/**]
  deliverables:
  - Exact V2 source and lock decoders/encoders with required Python target and V1 compatibility fixtures
  - Read-only migration preview and atomic source-only V2 write
  - Deterministic generated Python values, operations, sync/async clients, names, and diagnostics
  acceptance_commands:
  - cargo test -p riffdb-query-module -p riffdb-cli --all-features
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: V1 remains exact, V2 deterministically locks Python, and every generated binding matches the accepted API.
  human_review_triggers: *id001
- id: WP-396
  closure:
    status: complete
    evidence: clients/python/runtime
    decision: The Rust-backed Python runtime owns transport, cancellation, typed errors, exact values, credentials, and retry safety. Native architecture tests, wheel build, sync and async runtime tests, type checking, and fail-closed conversion tests pass.
  name: Rust-backed Python application runtime
  depends_on: [WP-393, WP-394]
  objective: Expose the existing stable Rust application client through a safe bounded PyO3 sync/async bridge without
    duplicating protocol or retry semantics.
  requirements: [PYD-001, PYD-002, PYD-003, PYD-004, PYD-006, PYD-007, PYD-010]
  required_adrs: [ADR-0037, ADR-0040, ADR-0041, ADR-0052, ADR-0055, ADR-0063, ADR-0074]
  allowed_paths: [Cargo.toml, Cargo.lock, deny.toml, crates/riffdb-client-python-native/**, clients/python/runtime/**,
    fixtures/application-parity/**, scripts/**, tests/**, docs/**]
  deliverables:
  - Safe-Rust native extension reusing riffdb-client-rust transport, metadata, validation, and uncertainty recovery
  - Python 3.13+ sync/async runtime, exact values, redacted credentials, checked failures, and typed stubs
  - Cancellation, GIL-release, no-panic, malformed-bridge, and two-database tests
  acceptance_commands:
  - cargo test -p riffdb-client-python-native --all-features
  - ./scripts/check-python-runtime
  - cargo deny check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Python reaches native gRPC only through the checked Rust client and exposes no raw protocol or secret.
  human_review_triggers: *id001
- id: WP-397
  closure:
    status: complete
    evidence: scripts/check-python-package
    decision: Reproducible cp313-abi3 wheels and source distributions install offline and expose the documented scaffold/runtime surface. Package inventory, reproducibility, installation, architecture, cargo-deny, and Python 3.14 acceptance pass; the documented ABI floor remains Python 3.13.
  name: Python scaffold and release artifacts
  depends_on: [WP-395, WP-396]
  objective: Make Python applications installable, scaffoldable, offline-buildable, and release-ready through pip and uv.
  requirements: [PYD-001, PYD-005, PYD-008, PYD-009, PYD-010, PYD-011, PYD-012]
  required_adrs: [ADR-0056, ADR-0057, ADR-0065, ADR-0074]
  allowed_paths: [Cargo.toml, Cargo.lock, crates/riffdb-cli/**, crates/riffdb-query-module/**, clients/python/**,
    templates/application/**, fixtures/**, scripts/**, release/**, docs/**, examples/**, .github/**]
  deliverables:
  - Complete riffdb new --language python source-layout application and boundary linting
  - Reproducible abi3 manylinux x86_64/aarch64 wheels and self-contained source distribution
  - Offline bundled-wheel uv workflow plus conventional pip/uv dependency documentation
  acceptance_commands:
  - ./scripts/riffdb-new-acceptance
  - ./scripts/agent-application-python-acceptance
  - ./scripts/check-python-package
  - ./scripts/check-generated
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A fresh Python 3.13 application builds and runs from the installed release without source-tree repair.
  human_review_triggers: *id001
- id: WP-398
  closure:
    status: complete
    evidence: scripts/agent-application-python-acceptance
    decision: Installed Python sync and async clients pass two-database isolation, generated commands and queries, exact values, reactive values, error, cancellation, batch, and boundary parity with no raw-protocol fallback.
  name: Python cross-language and installed parity proof
  depends_on: [WP-397]
  objective: Publish final sync/async, cross-language, multi-database, packaging, and reproducibility evidence without
    rewriting prior sealed evaluations.
  requirements: [PYD-001, PYD-002, PYD-003, PYD-004, PYD-005, PYD-006, PYD-007, PYD-008, PYD-009, PYD-010,
    PYD-011, PYD-012]
  required_adrs: [ADR-0055, ADR-0056, ADR-0057, ADR-0063, ADR-0065, ADR-0074]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/**, clients/python/**, fixtures/**, tests/**, scripts/**, release/**,
    docs/**, examples/**, .github/**]
  deliverables:
  - Same-workload Rust, TypeScript, Python-sync, and Python-async golden observations
  - Installed two-database authorization, identity, command, query, batch, retry, and recovery evidence
  - Python 3.13/3.14 wheel and source-install reproducibility report for both supported Linux architectures
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/agent-application-python-acceptance
  - ./scripts/check-python-package
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo deny check
  exit_gate: The installed Python driver proves full generated application parity without a raw protocol path or
    semantic exception.
  human_review_triggers: *id001
- id: WP-399
  closure:
    status: complete
    evidence: docs/getting-started/APPLICATION-MANIFEST.md
    decision: Lock and successor deployment share one canonical bundle identity and stale or raced identities fail before mutation. Exact application and migration recovery suites pass on current HEAD.
  name: Exact successor application identity before mutation
  depends_on: [WP-393, WP-395]
  objective: Make successor locking and deployment share one exact parent-aware candidate identity and reject every
    disagreement before authoritative mutation.
  requirements: [SAI-001, SAI-002, SAI-003, SAI-004, SAI-005, SAI-006]
  required_adrs: [ADR-0052, ADR-0057, ADR-0065, ADR-0066, ADR-0075]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**, crates/riffdb-proto/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-service/**, crates/riffdb-policy/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-cli/**, crates/riffdb-mcp-stdio/**,
    examples/**, fixtures/**, tests/**, scripts/**, docs/**, templates/**]
  deliverables:
  - Read-only authorized parent-aware candidate preview with bounded canonical bundle bytes
  - Application lock V3 canonical contract-bundle artifact with offline exact check and generation
  - Exact parent and candidate deployment compare-before-mutation with already-active recovery
  - Truthful source-only versus exact-lock application check and resumable partial deployment state
  acceptance_commands:
  - cargo test -p riffdb-query-module -p riffdb-service -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-cli --all-features
  - cargo test --test grpc_end_to_end --all-features
  - ./scripts/release-source-bootstrap-smoke
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A byte-identical additive successor locks and deploys under one bundle hash, stale or raced identities
    perform no mutation, exact retries resume, and application check never reports stale artifacts as exact.
  human_review_triggers: *id001
- id: WP-400
  closure:
    status: complete
    evidence: docs/getting-started/APPLICATION-MANIFEST.md
    decision: Successor role widening preflights and explicitly reconciles the retained binding from the exact lock, with actionable stale-identity diagnostics and no authority widening outside the declared role. Deployment and generated-binding suites pass.
  name: Successor role reconciliation and pre-mutation application preflight
  depends_on: [WP-399]
  objective: Make every locked role path consume the exact parent-aware bundle and move every fallible local
    application preflight before the first remote mutation.
  requirements: [SAI-003, SAI-006, DX-007, AAA-013]
  required_adrs: [ADR-0052, ADR-0057, ADR-0065, ADR-0075]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0075-successor-application-identity.md,
    crates/riffdb-query-module/**, crates/riffdb-diagnostics/**, crates/riffdb-cli/**,
    fixtures/application-diagnostics/**, tests/**, docs/**, templates/**]
  deliverables:
  - Pinned-bundle role compilation for application and standalone check, describe, bind, and provisioning paths
  - Complete local query and requested-role preflight before contract or query-module deployment
  - Widened-role regression proving a successor lock binds the new exact least-authority definition
  - Exact application-lock CLI identity and unambiguous replacement/progress diagnostics
  acceptance_commands:
  - cargo test -p riffdb-query-module -p riffdb-diagnostics -p riffdb-cli --all-features
  - ./scripts/check-application-bindings
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A successor that widens one symbolic role preflights, deploys, and explicitly replaces its retained
    credential without RDB-AR003, no local authoring failure follows remote mutation, and every CLI operation names
    the action actually performed.
  human_review_triggers: *id001
- id: WP-401
  closure:
    status: complete
    evidence: docs/SUMMARY.md
    decision: The repository-local handbook is navigable, generated-reference aware, and buildable without external runtime assets. The complete handbook check passes on current HEAD.
  name: Handbook foundation and documentation governance
  depends_on: [WP-400]
  objective: Establish the reproducible public handbook, curated navigation, maintenance rules, generated diagrams,
    and a local verification entry point without changing product semantics.
  requirements: [HBK-001, HBK-002, HBK-007, HBK-008, HBK-009, HBK-011]
  required_adrs: []
  allowed_paths: [AGENTS.md, SPEC.md, work_packages.yaml, README.md, book.toml, .github/**, docs/**, diagrams/**,
    scripts/**]
  deliverables:
  - Pinned mdBook configuration with local theme, search, POC status treatment, and curated public navigation
  - Repository documentation-maintenance protocol and pull-request documentation-impact field
  - Reproducible handbook diagram generation and contributor source-of-truth map
  - One local handbook build and verification command
  acceptance_commands:
  - ./scripts/handbook build
  - ./scripts/generate-handbook-diagrams --check
  - ./scripts/check-requirement-coverage
  exit_gate: A fresh checkout builds a navigable local handbook with no external runtime asset and future public
    changes have an explicit same-change documentation rule.
  human_review_triggers: *id001
- id: WP-402
  closure:
    status: complete
    evidence: docs/getting-started/FIRST-APPLICATION.md
    decision: The public onboarding path covers install, contract authoring, named reads, typed commands, exact generation, local development, and MCP without implementation-source dependencies. Handbook snippets and installed smokes pass.
  name: Application-builder handbook
  depends_on: [WP-401]
  objective: Teach the implemented contract-first application workflow progressively across the supported public
    language and MCP surfaces using reproducible examples.
  requirements: [HBK-002, HBK-003, HBK-004, HBK-006, HBK-011]
  required_adrs: [ADR-0054, ADR-0057, ADR-0063, ADR-0064, ADR-0065, ADR-0074, ADR-0075]
  allowed_paths: [README.md, docs/**, examples/**, scripts/**]
  deliverables:
  - First-application tutorial followed by TicketDesk and PostgreSQL safety-comparison learning paths
  - Rust, TypeScript, Python, CLI, and MCP guides over the same application-service semantics
  - Current contract, RiffQL, values, errors, idempotency, and authorization cross-links
  acceptance_commands:
  - ./scripts/handbook check
  - ./scripts/check-application-bindings
  - ./scripts/check-requirement-coverage
  exit_gate: A new application author can install RiffDB, build and deploy a contract, execute a typed command,
    query the result, and understand the demonstrated PostgreSQL safety contrast using only the handbook.
  human_review_triggers: *id001
- id: WP-403
  closure:
    status: complete
    evidence: docs/operations/TROUBLESHOOTING.md
    decision: Operations, security, backup/recovery, compatibility, diagnostics, and troubleshooting guidance are published and linked from the handbook. Generated references, links, snippets, and published Rust API checks pass.
  name: Operator and reference handbook
  depends_on: [WP-401]
  objective: Publish current operational, architecture, protocol, value, compatibility, and public-client references
    while keeping internal implementation crates outside the public API surface.
  requirements: [HBK-002, HBK-004, HBK-005, HBK-006, HBK-007, HBK-011]
  required_adrs: [ADR-0001, ADR-0004, ADR-0007, ADR-0008, ADR-0012, ADR-0063, ADR-0064, ADR-0065, ADR-0066]
  allowed_paths: [README.md, docs/**, diagrams/**, scripts/**]
  deliverables:
  - Installation, configuration, multi-database, credential, backup, restore, upgrade, reset, and troubleshooting path
  - Architecture and command-path reference tied to generated diagrams
  - Generated CLI reference and stable riffdb-client-rust API publication boundary
  acceptance_commands:
  - ./scripts/handbook check
  - ./scripts/generate-cli-reference --check
  - ./scripts/check-requirement-coverage
  exit_gate: Operators and application authors can resolve routine setup and runtime tasks without relying on the
    specification, source code, or historical work-package reports.
  human_review_triggers: *id001
- id: WP-404
  closure:
    status: complete
    evidence: scripts/handbook
    decision: CI-grade handbook validation rejects stale generated references, broken links and snippets, and public API drift; the current handbook, generated artifacts, and application bindings all pass.
  name: Handbook acceptance and GitHub Pages publication
  depends_on: [WP-402, WP-403]
  objective: Enforce handbook freshness and publish the verified static artifact from the main branch.
  requirements: [HBK-001, HBK-003, HBK-004, HBK-005, HBK-007, HBK-008, HBK-009, HBK-010, HBK-011]
  required_adrs: []
  allowed_paths: [README.md, book.toml, .github/**, docs/**, scripts/**]
  deliverables:
  - Link, fragment, snippet, navigation, generated-reference, runtime-asset, and Rustdoc verification
  - GitHub Pages workflow publishing the same artifact produced by the local handbook build
  - Responsive desktop and mobile visual acceptance evidence
  acceptance_commands:
  - ./scripts/handbook check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  exit_gate: Pull requests reject stale or broken public documentation and a main-branch build publishes the exact
    locally reproducible handbook artifact.
  human_review_triggers: *id001
- id: WP-405
  closure:
    status: complete
    evidence: adr/0076-offline-contract-migration-semantics.md
    decision: ADR-0076 through ADR-0079 are accepted, the MIG requirement family is assigned, and the migration format, authority, recovery, and evidence boundaries are frozen before implementation.
  name: Contract migration architecture and format freeze
  depends_on: [WP-404]
  objective: Freeze robust offline contract-migration semantics, artifacts, storage recovery, authority, public
    operations, staged delivery, and verification before production implementation begins.
  requirements: [MIG-001, MIG-002, MIG-003, MIG-004, MIG-005, MIG-006, MIG-007, MIG-008, MIG-009, MIG-010,
    MIG-011, MIG-012, MIG-013, MIG-014, MIG-015, MIG-016, MIG-017, MIG-018, MIG-019, MIG-020]
  required_adrs: [ADR-0003, ADR-0004, ADR-0006, ADR-0007, ADR-0010, ADR-0013, ADR-0050, ADR-0057, ADR-0063,
    ADR-0066, ADR-0072, ADR-0073, ADR-0075, ADR-0076, ADR-0077, ADR-0078, ADR-0079]
  allowed_paths: [AGENTS.md, SPEC.md, PLAN.md, work_packages.yaml, adr/**, diagrams/**, fixtures/migrations/**,
    docs/architecture/**, docs/contributing/**]
  deliverables:
  - Exact accepted ADRs for migration compatibility/cutover, language/IR/lock, staged storage/recovery, and public
    administration
  - MIG requirement set, P7 dependency graph, stable change-class gate inventory, and explicit current-unavailable
    documentation
  - Reviewed public/durable/IR owner map and golden fixture manifest with no production placeholder interface
  acceptance_commands:
  - ./scripts/check-requirement-coverage
  - ./scripts/generate-handbook-diagrams --check
  - ./scripts/handbook check
  exit_gate: ADR-0076 through ADR-0079 are accepted exactly, every MIG requirement has one owner and final evidence,
    and no production migration symbol or durable/public format exists before the freeze.
  human_review_triggers: *id001
- id: WP-406
  closure:
    status: complete
    evidence: docs/contracts/MIGRATIONS.md
    decision: Exact parent, candidate, riffm source, migration IR, compatibility classification, and application-lock identity deterministically reproduce the strict migration bundle while retaining predecessor readers.
  name: Migration source, IR, compatibility, and application lock
  depends_on: [WP-405]
  objective: Compile exact parent-specific bounded migration source into canonical typed artifacts and make locked
    applications describe every supported upgrade without mutation.
  requirements: [MIG-001, MIG-002, MIG-003, MIG-004, MIG-005, MIG-017, MIG-018, MIG-019, MIG-020]
  required_adrs: [ADR-0002, ADR-0011, ADR-0013, ADR-0057, ADR-0066, ADR-0075, ADR-0076, ADR-0077,
    ADR-0081]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-types/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-diagnostics/**, crates/riffdb-catalog/**, crates/riffdb-service/**, crates/riffdb-proto/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**, crates/riffdb-cli/**,
    proto/riffdb/v1/**, fixtures/proto/**, fixtures/compiler/**, fixtures/migrations/**, fixtures/application-manifests/**,
    fixtures/application-locks/**, fuzz/**, scripts/**, docs/**, templates/**]
  deliverables:
  - Separate bounded riffm grammar, formatter, source spans, typed MigrationBundleV1, canonical codec/hash, and all
    frozen V1 step tags
  - RequiresMigration compatibility codes plus exact proof coverage and structured migration-required deployment result
  - Application source V3, lock V4, at most 32 exact direct-parent artifacts, and read-only migration plan CLI
  - Parser/decoder fuzzing, canonical goldens, strict prior-format compatibility, and generated-artifact checks
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-query-module -p riffdb-diagnostics -p riffdb-cli --all-features
  - cargo +nightly-2026-07-12 fuzz run contract_source -- -max_len=1048576 -max_total_time=30
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Exact parent, candidate, and riffm source deterministically reproduce one strict migration bundle and V4
    lock; every unsupported, ambiguous, stale, excessive, or unimplemented transform fails with a bounded span diagnostic.
  human_review_triggers: *id001
- id: WP-407
  closure:
    status: complete
    evidence: tests/migrations/contract_migration_model.rs
    decision: The semantic coordinator, compiler-derived finite work plan, deterministic transforms, and independent reference model are implemented. Gate-A and generated-history tests pass in the complete workspace suite.
  name: Migration semantic coordinator and reference model
  depends_on: [WP-406]
  objective: Implement pure bounded transform/preflight semantics and the sole commit-owned migration mutation
    authority against the deterministic memory backend before concrete staging.
  requirements: [MIG-004, MIG-005, MIG-009, MIG-011, MIG-012, MIG-013, MIG-014, MIG-015, MIG-019, MIG-020]
  required_adrs: [ADR-0003, ADR-0004, ADR-0010, ADR-0023, ADR-0049, ADR-0076, ADR-0077, ADR-0078]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, crates/riffdb-types/**, crates/riffdb-contract-ir/**,
    crates/riffdb-catalog/**, crates/riffdb-invariant/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-commit/**, crates/riffdb-projection/**,
    crates/riffdb-testkit/Cargo.toml, fixtures/migrations/**, tests/migrations/**, scripts/**, docs/**]
  deliverables:
  - Catalog-sealed validated migration plans, pure deterministic row transforms, complete preflight, and stable
    value-free findings
  - Commit-owned MigrationCoordinator with sealed 256-row/64-mutation bounded batch and final-cutover ports
  - Memory migration journal, retained-data archive, predecessor-write fence, migration record, and projection candidate
    semantics
  - Generated-history/reference-model and property evidence proving immutable history and exact entity-version changes
  acceptance_commands:
  - cargo test -p riffdb-catalog -p riffdb-invariant -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-commit -p riffdb-projection --all-features
  - cargo test -p riffdb-testkit --test contract_migration_model --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every Gate-A plan matches the reference model under generated histories, no non-commit owner can construct
    a mutation, and commits/events/outcomes/provenance/idempotency/sequences remain byte-identical.
  human_review_triggers: *id001
- id: WP-408
  closure:
    status: complete
    evidence: tests/recovery/contract_migration_recovery.rs
    decision: Staged redb migration, private successor validation, atomic activation or predecessor restoration, and startup repair are implemented. All three process-level ignored recovery arms pass on current HEAD.
  name: Redb staged migration and crash recovery
  depends_on: [WP-407]
  objective: Add protected staged-copy, receipt/journal recovery, complete validation, atomic publication, and automatic
    rollback without exposing concrete storage or partially migrated state.
  requirements: [MIG-007, MIG-008, MIG-009, MIG-010, MIG-011, MIG-012, MIG-013, MIG-014, MIG-015, MIG-016, MIG-020]
  required_adrs: [ADR-0004, ADR-0006, ADR-0019, ADR-0022, ADR-0032, ADR-0042, ADR-0050, ADR-0061, ADR-0063,
    ADR-0072, ADR-0073, ADR-0078]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/0078-staged-migration-storage-and-recovery.md,
    proto/riffdb/storage/**, crates/riffdb-proto/**, fixtures/proto/**, crates/riffdb-types/**,
    crates/riffdb-catalog/**, crates/riffdb-commit/src/lib.rs, crates/riffdb-commit/src/migration.rs,
    crates/riffdb-commit/tests/architecture.rs,
    crates/riffdb-projection/src/migration.rs, crates/riffdb-storage-api/**,
    crates/riffdb-policy/src/operation.rs, crates/riffdb-service/src/audit.rs, crates/riffdb-storage-memory/src/migration.rs,
    crates/riffdb-storage-memory/src/startup.rs, crates/riffdb-storage-redb/**, crates/riffdb-server/src/maintenance_*/**,
    crates/riffdb-observability/src/metrics.rs,
    crates/riffdb-server/src/maintenance_*.rs, crates/riffdb-server/src/startup/**, crates/riffdb-server/src/startup.rs,
    crates/riffdb-server/src/daemon.rs, crates/riffdb-server/src/lib.rs,
    crates/riffdb-server/src/projection_worker.rs, crates/riffdb-server/src/storage.rs,
    crates/riffdb-server/Cargo.toml, crates/riffdb-server/tests/**, fixtures/storage/**, fixtures/migrations/**, fuzz/**, tests/maintenance/**,
    tests/migrations/**, tests/recovery/**, scripts/**, docs/**]
  deliverables:
  - Versioned receipt, journal, permanent migration, retired-row, table-key, envelope, and integrity formats with goldens
  - Protected same-filesystem stage from automatic immutable backup, conservative disk preflight, bounded journaled
    batches, projection rebuild, complete stage validation, and atomic publication
  - Exact startup reconciliation and pre-publication resume/recreate plus post-publication automatic rollback
  - Redb conformance, parser fuzzing, disk/version exhaustion, corrupt evidence, and process failpoint matrix
  acceptance_commands:
  - cargo test -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-server --all-features
  - cargo test -p riffdb-server --test contract_migration_recovery -- --ignored
  - cargo +nightly-2026-07-12 fuzz run migration_receipt -- -max_len=1048576 -max_total_time=30
  - cargo +nightly-2026-07-12 fuzz run migration_journal -- -max_len=4194304 -max_total_time=30
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every failpoint recovers to the exact validated predecessor or successor, failed publication restores the
    exact backup before readiness, and sibling databases remain isolated throughout live and restarted maintenance.
  human_review_triggers: *id001
- id: WP-409
  closure:
    status: complete
    evidence: docs/operations/CONTRACT-MIGRATION-ACCEPTANCE.md
    decision: Exact current migration authority gates start, status, retry, and public administration; retries resolve by operation identity and diagnostics redact private migration detail. CLI, service, gRPC, MCP, and authorization tests pass.
  name: Migration authorization and public administration
  depends_on: [WP-407, WP-408]
  objective: Expose check, apply, and observation only through dedicated least-authority gRPC, Rust SDK, and CLI
    administration over the shared service and database lifecycle.
  requirements: [MIG-006, MIG-007, MIG-008, MIG-009, MIG-010, MIG-016, MIG-017, MIG-018]
  required_adrs: [ADR-0007, ADR-0009, ADR-0026, ADR-0027, ADR-0028, ADR-0034, ADR-0037, ADR-0040, ADR-0041,
    ADR-0050, ADR-0055, ADR-0062, ADR-0063, ADR-0064, ADR-0079, ADR-0088, ADR-0089]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/riffdb/v1/**, proto/riffdb/storage/**,
    crates/riffdb-types/**, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-auth/**, crates/riffdb-policy/**, crates/riffdb-service/**, crates/riffdb-proto/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**, crates/riffdb-server/**, crates/riffdb-cli/**,
    crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**, fixtures/proto/**, fixtures/storage/**,
    fixtures/migrations/**,
    tests/grpc/**, tests/mcp/**, tests/migrations/**, scripts/**, docs/**]
  deliverables:
  - Dedicated scoped MigrateContract permission and exact check/apply/poll policy proofs
  - Three additive AdminService RPCs, API-neutral DTOs/service methods, Rust SDK retries, top-level CLI, exact hash
    confirmation, and 29-RPC descriptor inventory
  - Accepted-then-reconnect cancellation/uncertainty behavior and resumable application-deploy handoff
  - Complete authorization/redaction/response-budget matrix and negative MCP/application-driver inventory
  acceptance_commands:
  - cargo test -p riffdb-types -p riffdb-auth -p riffdb-policy -p riffdb-service -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server -p riffdb-cli --all-features
  - cargo test --test grpc_end_to_end --all-features
  - cargo test --test mcp_conformance --all-features
  - ./scripts/generate-proto --check
  - ./scripts/generate-cli-reference --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Only an exact current migration capability can start or observe work; retries resolve by operation identity,
    CLI uses no direct storage path, and every MCP/application catalog remains migration-free.
  human_review_triggers: *id001
- id: WP-410
  closure:
    status: complete
    evidence: docs/operations/CONTRACT-MIGRATION-ACCEPTANCE.md
    decision: Populated additive migrations preserve values and identities, backfill deterministically, rebuild declared derived state, and survive injected failure without mixed publication. Gate A and recovery suites pass.
  name: Additive data migration gate
  depends_on: [WP-409]
  objective: Prove the complete public workflow for required fields, indexes, relationships, uniqueness, invariants,
    and new projections on existing authoritative data.
  requirements: [MIG-001, MIG-002, MIG-003, MIG-004, MIG-005, MIG-006, MIG-007, MIG-008, MIG-009, MIG-010,
    MIG-011, MIG-012, MIG-013, MIG-014, MIG-015, MIG-016, MIG-017, MIG-018, MIG-019, MIG-020]
  required_adrs: [ADR-0076, ADR-0077, ADR-0078, ADR-0079]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/**, proto/**, fixtures/**, fuzz/**, tests/**, examples/**,
    scripts/**, docs/**, templates/**]
  deliverables:
  - End-to-end exact-lock migration for every additive Gate-A change and combined successor
  - Constraint-violation, pending-admission, stale-parent, wrong-hash, cancellation, rollback, restart, and sibling tests
  - EA/TicketDesk retained-data fixture plus current CLI/SDK/operator handbook workflow
  - Migration check/apply throughput, downtime, and peak-disk baseline
  acceptance_commands:
  - cargo test --workspace --all-features
  - cargo test -p riffdb-testkit --test contract_migration_gate_a --all-features
  - cargo test -p riffdb-server --test contract_migration_recovery -- --ignored
  - ./scripts/handbook check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo deny check
  exit_gate: A populated installed application reaches its exact successor with every new value/constraint/projection
    correct, no partial state or history rewrite, and every injected failure returns a validated predecessor or successor.
  human_review_triggers: *id001
- id: WP-411
  closure:
    status: complete
    evidence: docs/contracts/MIGRATIONS.md
    decision: Supported structural entity changes migrate deterministically with retained old identity/data, while unsupported or lossy classes fail before publication with source diagnostics. Structural migration tests pass.
  name: Structural entity migration gate
  depends_on: [WP-410]
  objective: Enable semantic renames, logical retirement, replacement fields/types, exhaustive enum mappings, and
    replacement projections without stable-ID reuse or historical reinterpretation.
  requirements: [MIG-001, MIG-002, MIG-003, MIG-004, MIG-005, MIG-009, MIG-011, MIG-013, MIG-014, MIG-015,
    MIG-018, MIG-019, MIG-020]
  required_adrs: [ADR-0013, ADR-0066, ADR-0076, ADR-0077, ADR-0078, ADR-0090]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-catalog/**, crates/riffdb-invariant/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-projection/**, crates/riffdb-service/**, crates/riffdb-cli/**,
    fixtures/migrations/**, tests/migrations/**, fuzz/**, scripts/**, docs/**, examples/**]
  deliverables:
  - Stable-ID-preserving rename proof, fresh-ID replacement, tombstone/archive, closed conversion, and enum-map execution
  - Logical command/event/outcome/projection retirement with exact historical resolution and no physical purge
  - Exhaustive conversion/assertion/collision/property/recovery fixtures and public diagnostics
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-catalog -p riffdb-invariant -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit -p riffdb-projection -p riffdb-service -p riffdb-cli --all-features
  - cargo test -p riffdb-testkit --test contract_migration_gate_b --all-features
  - ./scripts/handbook check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every structural class either migrates deterministically with retained old identity/data or fails before
    backup, and no old ID, value, event, outcome, or projection history is reinterpreted.
  human_review_triggers: *id001
- id: WP-412
  closure:
    status: complete
    evidence: docs/contracts/MIGRATIONS.md
    decision: Rekey and repartition migrations publish only after complete collision, reference, and ownership proof; stale writers and ambiguous ownership fail closed. Key-ownership, recovery, and model tests pass.
  name: Key ownership and conflict migration gate
  depends_on: [WP-411]
  objective: Enable primary-key, partition, aggregate-membership, relationship-target, and conflict-domain evolution
    with complete collision/reference validation and predecessor-write retirement.
  requirements: [MIG-001, MIG-002, MIG-003, MIG-004, MIG-005, MIG-009, MIG-011, MIG-012, MIG-013, MIG-015,
    MIG-018, MIG-019, MIG-020]
  required_adrs: [ADR-0003, ADR-0013, ADR-0016, ADR-0059, ADR-0066, ADR-0076, ADR-0077, ADR-0078]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-catalog/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**, crates/riffdb-commit/**,
    crates/riffdb-service/**, crates/riffdb-cli/**, crates/riffdb-testkit/Cargo.toml,
    fixtures/migrations/**, tests/migrations/**, fuzz/**, scripts/**, docs/**, examples/**]
  deliverables:
  - Deterministic rekey/repartition plans, dependency DAG, new authoritative index/key materialization, and old-key archive
  - Complete target collision, relationship, aggregate, partition, conflict, and old-plan compatibility validation
  - Generated histories and crash matrix covering combined key/ownership changes without old-writer admission
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-catalog -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit -p riffdb-service -p riffdb-cli --all-features
  - cargo test -p riffdb-testkit --test contract_migration_gate_c --all-features
  - ./scripts/handbook check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Rekeyed and repartitioned datasets publish only after complete collision/reference proof, old writers cannot
    enter, and the crash matrix exposes neither old/new duplicates nor a partial conflict domain.
  human_review_triggers: *id001
- id: WP-413
  closure:
    status: complete
    evidence: release/evidence/wp413-migration-evolution-v1.json
    decision: The receipted installed migration acceptance covers populated additive, structural, rekey, repartition, interruption, authorization, and performance cases. Current migration recovery, workspace, fuzz, handbook, generation, and requirement gates pass.
  name: Migration installed acceptance and operational proof
  depends_on: [WP-410, WP-411, WP-412]
  objective: Publish installed, documented, benchmarked, cross-version, authorization, and recovery evidence for the
    complete robust migration surface and close P7.
  requirements: [MIG-001, MIG-002, MIG-003, MIG-004, MIG-005, MIG-006, MIG-007, MIG-008, MIG-009, MIG-010,
    MIG-011, MIG-012, MIG-013, MIG-014, MIG-015, MIG-016, MIG-017, MIG-018, MIG-019, MIG-020]
  required_adrs: [ADR-0076, ADR-0077, ADR-0078, ADR-0079]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/**, proto/**, fixtures/**, fuzz/**, tests/**, examples/**,
    scripts/**, docs/**, templates/**, release/**, .github/**]
  deliverables:
  - Direct-parent upgrade matrix across all supported migration gates and prior application source/lock formats
  - Installed user/system multi-database migration, retry, crash, rollback, authorization, and backup-retention proof
  - EA/TicketDesk dogfood plus PostgreSQL/RiffDB evolution comparison with bounded benchmark and failure evidence
  - Complete handbook, generated CLI/protocol/reference updates, release notes, known limitations, and P7 review report
  acceptance_commands:
  - cargo test --workspace --all-features
  - cargo test -p riffdb-server --test contract_migration_recovery -- --ignored
  - cargo test -p riffdb-testkit --test contract_migration_acceptance --all-features
  - ./scripts/release-source-bootstrap-smoke
  - ./scripts/handbook check
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo deny check
  exit_gate: Installed RiffDB proves every MIG guarantee over populated data and injected failure, preserves exact
    immutable history, retains a verified rollback backup, isolates sibling databases, and publishes reproducible docs
    and PostgreSQL comparison evidence.
  human_review_triggers: *id001
- id: WP-414
  closure:
    status: complete
    evidence: adr/0080-partitioned-events-consumers-and-live-queries.md
    decision: ADR-0080 is accepted, the P8 event, consumer, live-query, and contextual-subscription requirements have implementation and final-evidence owners, and the reactive durable-format boundaries are frozen.
  name: Reactive application architecture and format freeze
  depends_on: [WP-405]
  objective: Freeze partitioned event, durable consumer, live named-query, contextual subscription, causation,
    authorization, public protocol, generated-client, and recovery semantics before production implementation.
  requirements: [EVT-001, EVT-002, EVT-003, EVT-004, EVT-005, EVT-006, EVT-007, EVT-008, CON-001, CON-002,
    CON-003, CON-004, CON-005, CON-006, CON-007, CON-008, LIVE-001, LIVE-002, LIVE-003, LIVE-004, LIVE-005,
    LIVE-006, LIVE-007, LIVE-008, CTX-001, CTX-002, CTX-003, CTX-004, CTX-005, CTX-006, CTX-007, CTX-008]
  required_adrs: [ADR-0013, ADR-0017, ADR-0049, ADR-0055, ADR-0056, ADR-0057, ADR-0063, ADR-0064, ADR-0072,
    ADR-0075, ADR-0080]
  allowed_paths: [AGENTS.md, SPEC.md, PLAN.md, work_packages.yaml, adr/**, diagrams/**, fixtures/reactive/**,
    docs/architecture/**, docs/contributing/**]
  deliverables:
  - Exact accepted ADR-0080 covering authoritative event reuse, partition proof, routing, consumer state, live queries,
    contextual work, causation, bounds, authorization, redaction, and explicit deferrals
  - EVT/CON/LIVE/CTX requirements, P8 dependency graph, format-owner map, and exact compatibility boundary
  - Frozen public/durable/IR fixture manifest and no production placeholder implementation before acceptance
  acceptance_commands:
  - ./scripts/check-requirement-coverage
  - ./scripts/generate-handbook-diagrams --check
  - ./scripts/handbook check
  exit_gate: ADR-0080 is accepted exactly, every P8 requirement has one implementation owner and final evidence,
    and existing event, projection, outbox, query, cursor, and application-format meanings remain explicit.
  human_review_triggers: *id001
- id: WP-415
  closure:
    status: complete
    evidence: docs/contracts/DOMAIN-EVENTS.md
    decision: Typed partitioned domain events carry stable identity, version, causality, provenance, and exact partition ordering atomically with their commands. Symbolic catalog and replay tests pass.
  name: Partitioned domain event catalog and replay
  depends_on: [WP-414]
  objective: Make existing authoritative domain events symbolically streamable through compiler-proved partitions,
    a resumable routing index, catalog materialization, bounded replay, and public inspection.
  requirements: [EVT-001, EVT-002, EVT-003, EVT-004, EVT-005, EVT-006, EVT-007, EVT-008]
  required_adrs: [ADR-0013, ADR-0049, ADR-0063, ADR-0072, ADR-0073, ADR-0080]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/riffdb/storage/**,
    proto/riffdb/v1/**, fixtures/proto/**,
    crates/riffdb-types/**, crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-catalog/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**, crates/riffdb-commit/**,
    crates/riffdb-policy/**, crates/riffdb-service/**, crates/riffdb-proto/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, crates/riffdb-cli/**, fixtures/reactive/**,
    fixtures/storage/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Optional event partition grammar, IR/codec/hash successors, compiler emit-equivalence proof, compatibility, and spans
  - Versioned event-route record/key, atomic maintenance, resumable historical backfill, startup validation, and recovery
  - Catalog-owned historical event materialization and safe symbolic event envelope with bounded replay/tail/describe
  - Storage-memory/redb conformance, durable goldens, corruption, restore-incarnation, and multi-database isolation tests
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-catalog --all-features
  - cargo test -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit --all-features
  - cargo test -p riffdb-service -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-cli --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A partition-proved event can be described and replayed symbolically in exact partition order, historical
    routing is complete and recoverable, and no raw event/storage identity is required by application code.
  human_review_triggers: *id001
- id: WP-416
  closure:
    status: complete
    evidence: docs/reactive/MODULES.md
    decision: Exact reactive modules bind contract, query, stream, subscription, role, and generated identities in one lock; unsafe or ambiguous reactive shapes fail at compile time with symbolic diagnostics.
  name: Reactive modules, exact locks, and symbolic roles
  depends_on: [WP-406, WP-414, WP-415]
  objective: Compile named streams and contextual subscriptions into exact reactive modules and derive least-authority
    application roles without colliding with migration-owned manifest and lock formats.
  requirements: [EVT-005, EVT-006, EVT-007, CON-001, CON-005, CON-007, LIVE-002, LIVE-003, LIVE-004,
    LIVE-005, LIVE-007, CTX-001, CTX-003, CTX-004, CTX-008]
  required_adrs: [ADR-0055, ADR-0057, ADR-0064, ADR-0065, ADR-0075, ADR-0077, ADR-0080]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/riffdb/storage/**,
    proto/riffdb/v1/**, fixtures/proto/**, crates/riffdb-query-syntax/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-diagnostics/**, crates/riffdb-types/**, crates/riffdb-auth/**,
    crates/riffdb-storage-api/**, crates/riffdb-policy/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-server/**,
    crates/riffdb-cli/**, fixtures/reactive/**, fixtures/application-manifests/**,
    fixtures/application-locks/**, scripts/**, docs/**, templates/**]
  deliverables:
  - Bounded stream/subscription syntax, formatter, source spans, typed reactive IR, canonical module hash, and diagnostics
  - Application Source V4 and Lock V5 preserving V1-V3/V1-V4, explicit migration, and exact generated artifact closure
  - Role event_streams, watch_queries, and agent_subscriptions with compiler-derived fields, bounds, and exact permissions
  - Cross-partition, hidden-field, unkeyed-patch, mismatched-event-field, excessive-cost, and definition-drift negatives
  acceptance_commands:
  - cargo test -p riffdb-query-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module --all-features
  - cargo test -p riffdb-types -p riffdb-policy -p riffdb-service -p riffdb-cli --all-features
  - ./scripts/check-application-bindings
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One exact lock deterministically binds contract, query, reactive, role, and generated identities; authors
    name only domain operations, and every unsafe or unbounded shape fails before deployment.
  human_review_triggers: *id001
- id: WP-417
  closure:
    status: complete
    evidence: docs/reactive/CLIENTS.md
    decision: Durable consumers implement bounded pull, checkpoint, lease, ack, nack, retry, dead-letter, replay, revocation, and restart semantics. Deterministic and process-recovery tests show no lost eligible event or duplicated business state.
  name: Durable event consumers and delivery recovery
  depends_on: [WP-415, WP-416]
  objective: Implement bounded partition-ordered at-least-once consumers with durable checkpoints, leases, ack/nack,
    retry, dead-letter, seek, restart recovery, and shared public adapters.
  requirements: [CON-001, CON-002, CON-003, CON-004, CON-005, CON-006, CON-007, CON-008]
  required_adrs: [ADR-0004, ADR-0007, ADR-0019, ADR-0049, ADR-0063, ADR-0072, ADR-0073, ADR-0080,
    ADR-0085]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**, crates/riffdb-types/**,
    crates/riffdb-errors/**, crates/riffdb-query-module/**, crates/riffdb-catalog/**, crates/riffdb-commit/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-service/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-server/**, crates/riffdb-cli/**,
    crates/riffdb-observability/**, fixtures/reactive/**, fixtures/proto/**, tests/**, scripts/**,
    docs/**]
  deliverables:
  - Immutable exact reactive-module publication and catalog resolution through the shared control plane
  - Versioned consumer/checkpoint/lease/sparse-ack/dead-letter formats and one atomic ConsumerCoordinator
  - Bounded next/ack/nack/seek/status service operations, pull and gRPC streaming, long poll, and exact authorization
  - Startup lease recovery, incarnation mismatch, module change, corruption, backup/restore, and sibling isolation behavior
  - Consumer-aware retention fencing plus explicit authorized seek/retire release behavior
  - Deterministic schedule and process-crash matrix covering every delivery/checkpoint transition without sleeps
  acceptance_commands:
  - cargo test -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-service --all-features
  - cargo test -p riffdb-policy -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server -p riffdb-cli --all-features
  - cargo test -p riffdb-testkit --test durable_event_consumers --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Kill/restart and duplicate delivery lose no eligible event, skip no checkpoint, duplicate no leased attempt,
    and every public transition remains bounded, authorized, and recoverable.
  human_review_triggers: *id001
- id: WP-418
  closure:
    status: complete
    evidence: docs/reactive/LIVE-QUERIES.md
    decision: Named bounded queries support race-free snapshot plus frontier, relevant-change catch-up, keyed patch/reset, exact resume, definition evolution, and authorization revalidation. Live-query and two-browser convergence tests pass.
  name: Live named RiffQL execution
  depends_on: [WP-415, WP-416]
  objective: Add race-free live execution for exact named queries using snapshot frontiers, durable commit catch-up,
    conservative invalidation, bounded diff/reset, resumption, and authorization revalidation.
  requirements: [LIVE-001, LIVE-002, LIVE-003, LIVE-004, LIVE-005, LIVE-006, LIVE-007]
  required_adrs: [ADR-0038, ADR-0053, ADR-0054, ADR-0055, ADR-0070, ADR-0072, ADR-0080]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/riffdb/v1/**, crates/riffdb-types/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**, crates/riffdb-policy/**,
    crates/riffdb-service/**, crates/riffdb-observability/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-server/**, crates/riffdb-testkit/**, fixtures/reactive/**, fixtures/proto/**, tests/**,
    scripts/**, docs/**]
  deliverables:
  - Watchability analysis, relevant entity/key dependencies, public-key patch proof, and exact live-plan identity
  - Snapshot/patch/reset/checkpoint/terminal service model, self-contained fenced cursor, and gRPC server stream
  - Snapshot-to-catch-up race closure, bounded coalescing/diff, buffer terminal, definition reset, and revocation clearing
  - Barrier-controlled concurrency, patch equivalence, reconnect, restore, multi-database, and saturation evidence
  acceptance_commands:
  - cargo test -p riffdb-query-ir -p riffdb-query-executor -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-policy -p riffdb-service -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server --all-features
  - cargo test -p riffdb-testkit --test live_named_queries --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A commit at every snapshot/catch-up interleaving is reflected, reconnect converges without polling, and
    drift, revocation, restore, or pressure yields a typed reset/terminal without protected-data retention.
  human_review_triggers: *id001
- id: WP-419
  closure:
    status: complete
    evidence: docs/reactive/CLIENTS.md
    decision: Rust, TypeScript, Python, CLI, MCP, SSE relay, and generated schemas expose the same exact reactive operations without raw protocol code or browser credentials. Cross-language bindings and relay acceptance pass.
  name: Generated reactive clients, CLI, MCP, and browser relay
  depends_on: [WP-417, WP-418]
  objective: Expose identical typed consumer and live-query behavior through generated Rust, TypeScript, Python, CLI,
    MCP, and an application-owned authenticated SSE relay without handwritten transport glue.
  requirements: [EVT-006, EVT-007, CON-005, CON-007, CON-008, LIVE-004, LIVE-005, LIVE-006, LIVE-007, LIVE-008]
  required_adrs: [ADR-0037, ADR-0040, ADR-0041, ADR-0055, ADR-0057, ADR-0064, ADR-0074, ADR-0080]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**, crates/riffdb-query-module/**,
    crates/riffdb-client-rust/**, crates/riffdb-client-python-native/**, crates/riffdb-cli/**,
    crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**, clients/**, templates/**, fixtures/reactive/**,
    fixtures/application-parity/**, examples/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Generated Rust streams, TypeScript/Python async iterators, typed update unions, cursors, reconnect, and clear-on-close
  - CLI event operations and generated MCP next/ack/nack/seek/status with payload-free resource wakeups
  - Framework-neutral TypeScript live store and generated application-server SSE relay retaining credentials server-side
  - Cross-language golden requests, observations, errors, cursor persistence, boundary linting, and offline scaffold tests
  acceptance_commands:
  - cargo test -p riffdb-client-rust -p riffdb-query-module -p riffdb-client-python-native -p riffdb-cli -p riffdb-api-mcp -p riffdb-mcp-stdio --all-features
  - ./scripts/check-application-bindings
  - ./scripts/agent-application-typescript-acceptance
  - ./scripts/agent-application-python-acceptance
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every supported audience consumes the same exact reactive operations without raw protocol code, browser
    JavaScript receives no RiffDB credential, and MCP wakeups disclose no event or context payload.
  human_review_triggers: *id001
- id: WP-420
  closure:
    status: complete
    evidence: docs/reactive/CONTEXTUAL-SUBSCRIPTIONS.md
    decision: Contextual subscriptions deliver one bounded authorized event-plus-query snapshot with causal fences, leases, capability-filtered reactions, idempotent command provenance, and payload-free MCP wakeups. Crash and retry suites pass.
  name: Contextual agent subscriptions and causal reactions
  depends_on: [WP-417, WP-418, WP-419]
  objective: Deliver durable event work with one-snapshot named-query context, capability-filtered commands,
    server-validated causation, and reaction-safe generated idempotency.
  requirements: [CTX-001, CTX-002, CTX-003, CTX-004, CTX-005, CTX-006, CTX-007, CTX-008]
  required_adrs: [ADR-0013, ADR-0038, ADR-0054, ADR-0055, ADR-0056, ADR-0072, ADR-0080]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**, crates/riffdb-types/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**, crates/riffdb-query-ir/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-catalog/**, crates/riffdb-commit/**, crates/riffdb-policy/**,
    crates/riffdb-auth/**,
    crates/riffdb-service/**, crates/riffdb-proto/**, crates/riffdb-observability/**, crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-client-python-native/**, crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**,
    crates/riffdb-server/**, crates/riffdb-testkit/Cargo.toml, clients/**,
    fixtures/compiler/diagnostics.txt, fixtures/proto/**, fixtures/reactive/**, tests/**, scripts/**,
    docs/**]
  deliverables:
  - Exact contextual plan combining stream, shared-snapshot hydration, commands, bounds, and immutable definition hash
  - One-snapshot multi-query execution at/after event, bounded work items, authorized-command filtering, and fresh context
  - Versioned provenance causation/correlation successor plus sealed lease-bound causation tokens and replay resolution
  - Generated reaction helpers and crash schedules proving commit-before-ack redelivery duplicates no application state
  acceptance_commands:
  - cargo test -p riffdb-query-ir -p riffdb-query-module -p riffdb-query-executor -p riffdb-catalog --all-features
  - cargo test -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit --all-features
  - cargo test -p riffdb-policy -p riffdb-service -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-api-mcp -p riffdb-mcp-stdio --all-features
  - cargo test -p riffdb-testkit --test contextual_agent_subscriptions --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One bounded call gives an authorized agent the event and causally fenced domain context, and every crash
    before or after command/ack produces neither lost work nor duplicated business state.
  human_review_triggers: *id001
- id: WP-421
  closure:
    status: complete
    evidence: release/evidence/wp421-reactive-fresh-agent-v1.json
    decision: The immutable fresh-agent campaign and installed TicketDesk evidence close polling-free browser updates, reconnect/reset, contextual triage, reaction recovery, revocation, MCP wakeup, and boundary requirements. Current workspace and generated-binding regressions pass; current bundle resealing intentionally cannot replace the immutable historical campaign.
  name: TicketDesk reactive acceptance and safety proof
  depends_on: [WP-420]
  objective: Close P8 with installed TicketDesk browser, worker, agent, evolution, authorization, recovery, performance,
    documentation, and fresh-agent evidence over only public symbolic surfaces.
  requirements: [EVT-001, EVT-002, EVT-003, EVT-004, EVT-005, EVT-006, EVT-007, EVT-008, CON-001, CON-002,
    CON-003, CON-004, CON-005, CON-006, CON-007, CON-008, LIVE-001, LIVE-002, LIVE-003, LIVE-004, LIVE-005,
    LIVE-006, LIVE-007, LIVE-008, CTX-001, CTX-002, CTX-003, CTX-004, CTX-005, CTX-006, CTX-007, CTX-008]
  required_adrs: [ADR-0055, ADR-0056, ADR-0057, ADR-0063, ADR-0064, ADR-0072, ADR-0075, ADR-0080]
  allowed_paths: [SPEC.md, PLAN.md, work_packages.yaml, crates/**, proto/**, clients/**, fixtures/**, tests/**,
    examples/**, benchmarks/**, scripts/**, docs/**, templates/**, release/**, .github/**]
  deliverables:
  - Two-browser queue/detail live update and reconnect/reset proof through generated application SSE relay
  - TicketCreated contextual triage with one-snapshot TicketPage, generated reaction, MCP wakeup, and kill-point matrix
  - Revocation clearing, definition evolution, restore incarnation, saturation, multi-database, and redaction evidence
  - Fresh-agent implementation evaluation, complete handbook/reference/release updates, and explicit deferred roadmap
  acceptance_commands:
  - cargo test --workspace --all-features
  - cargo test -p riffdb-testkit --test reactive_ticketdesk_acceptance --all-features
  - cargo test -p riffdb-testkit --test contextual_agent_subscriptions --all-features
  - ./scripts/reactive-ticketdesk-evaluation-acceptance --campaign wp421-reactive-01 --assert-gate
  - ./scripts/release-source-bootstrap-smoke
  - ./scripts/handbook check
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - cargo deny check
  exit_gate: Installed TicketDesk provides polling-free live state and crash-safe contextual agent reaction through
    symbolic generated surfaces, revocation leaks no retained data, and a fresh agent completes the feature without
    internal source or kernel access.
  human_review_triggers: *id001
- id: WP-422
  closure:
    status: complete
    evidence: docs/performance/perf-013-014.md
    decision: Validated-prefix checkpoints are durable hints only, fail open to full validation when absent or invalid, and are proven equivalent to full startup validation across clean, crash, and corruption arms. PERF-014 remains receipted evidence rather than a weakened correctness gate.
  name: Validated-prefix startup checkpoint (RT-A)
  depends_on: [WP-421]
  objective: Ship ADR-0085 Amendment 1 proof-carrying validated-prefix startup checkpoints that fail closed on
    invalid bindings, skip validated sequence-keyed prefix rows, and emit PERF-014 unclean recovery evidence.
  requirements: [PERF-013, PERF-014]
  required_adrs: [ADR-0019, ADR-0073, ADR-0085]
  allowed_paths: [Cargo.lock, work_packages.yaml, crates/riffdb-storage-redb/**, crates/riffdb-storage-api/**,
    crates/riffdb-proto/**, proto/riffdb/storage/v1/**, tests/storage_recovery/**, benchmarks/command-growth/**,
    docs/performance/perf-013-014.md, fixtures/proto/**, scripts/generate-proto,
    tests/command_semantics/command_concurrency.rs]
  deliverables:
  - StoredValidatedPrefixCheckpointV1 durable format, registry migration PRE_VALIDATED → current, meta key write
  - Checkpoint write after clean finish + public write_validated_prefix_checkpoint; verify/ignore on open
  - Prefix-skipping structural walk, below-S count verification, entity-chain fingerprint, sample windows
  - Crash arms, PERF-014 unclean-vs-clean evidence, --startup-scale-checkpointed measurement
  acceptance_commands:
  - cargo test -p riffdb-proto --test durable_schema --all-features
  - cargo test -p riffdb-storage-api -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-storage-redb --test storage_recovery_matrix --all-features
  - scripts/generate-proto --check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Invalid checkpoints never block open; second open after clean finish verifies the checkpoint; crash
    before/after checkpoint commit reopens green; PERF-014 emits honest unclean/clean ratio without a hard gate.
  human_review_triggers: *id001
- id: WP-423
  closure:
    status: complete
    evidence: docs/compatibility.md
    decision: Retention requires complete watermarks and same-transaction tombstones, is offline and fail-closed, preserves durable cursors and backup/restore coverage, and cannot expose partially pruned history. Recovery, storage, and compatibility suites pass.
  name: Retention watermark, tombstones, offline prune (RT-B)
  depends_on: [WP-422]
  objective: Ship ADR-0085 Amendment 2 hard-fence retention watermark, history tombstones, offline
    prune of commits/events/outbox below the watermark, startup tombstone tolerance, typed
    RDB-HISTORY-0102 pruned outcome, and backup watermark field so startup cost scales with
    retained history (PERF-013).
  requirements: [PERF-013]
  required_adrs: [ADR-0019, ADR-0085]
  allowed_paths: [Cargo.lock, work_packages.yaml, adr/0085-history-retention-and-startup-scaling.md,
    crates/riffdb-storage-redb/**, crates/riffdb-storage-api/**, crates/riffdb-storage-memory/src/**,
    crates/riffdb-proto/**, proto/riffdb/storage/v1/**, proto/riffdb/v1/**, proto/riffdb/app/**,
    crates/riffdb-errors/**, crates/riffdb-observability/**, crates/riffdb-client-rust/**,
    crates/riffdb-catalog/**, crates/riffdb-cli/**, tests/storage_recovery/**, tests/service_audit/**,
    tests/service/support/**, fixtures/proto/**, fixtures/application-errors/registry-v1.tsv,
    scripts/generate-proto, crates/riffdb-api-grpc/src/error.rs,
    crates/riffdb-api-mcp/src/presentation.rs, crates/riffdb-auth/src/authenticator.rs,
    crates/riffdb-commit/src/control_plane.rs, crates/riffdb-projection/src/error.rs,
    crates/riffdb-projection/src/evaluator.rs, crates/riffdb-server/src/maintenance_adapter.rs,
    crates/riffdb-server/src/maintenance_driver.rs, crates/riffdb-server/src/maintenance_migration.rs,
    crates/riffdb-server/src/outbox_adapter.rs, crates/riffdb-server/src/read_adapters.rs,
    crates/riffdb-service/src/administration_operations.rs, crates/riffdb-service/src/command_operations.rs,
    crates/riffdb-service/src/commit_operations.rs, crates/riffdb-service/src/ports.rs,
    crates/riffdb-service/src/query_discovery_operations.rs, crates/riffdb-service/src/service.rs]
  deliverables:
  - StoredRetentionWatermarkV1/Holds/HistoryTombstoneV1 durable formats and registry migration PRE_RETENTION → current
  - Offline exclusive prune with fencing min, atomic sub-range tombstone+delete+watermark, checkpoint invalidation
  - Startup tombstone-chain verification and below-watermark absence tolerance; typed history_pruned reads
  - RDB-HISTORY-0102 five-site surface (errors, client status, metrics); backup watermark field
  acceptance_commands:
  - cargo test -p riffdb-proto --test durable_schema --all-features
  - cargo test -p riffdb-storage-api -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-storage-redb --test retention --all-features
  - scripts/generate-proto --check
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Prune is impossible without complete fencing; deleted ranges always covered by same-txn tombstones;
    reopen after prune verifies the chain; below-watermark reads return typed pruned; crash mid-sub-range reopens valid.
  human_review_triggers: *id001
- id: WP-430
  name: Columnar projection engine core
  depends_on: []
  objective: Deliver the ADR-0086 columnar projection engine as a pure library with definition
    validation, supersession-safe apply, durable checkpoint recovery, bounded query execution,
    and crash-injection equivalence coverage without public API surface.
  requirements: []
  required_adrs: [ADR-0086, ADR-0087]
  allowed_paths: [Cargo.toml, Cargo.lock, work_packages.yaml, scripts/check-workspace-policy,
    crates/riffdb-columnar/**]
  deliverables:
  - riffdb-columnar crate with definition registration, fingerprint, and typed rejection classes
  - Delta plus immutable segment store, D4 apply protocol with deferred supersession holdback
  - Checkpoint manifest with checksums, torn-write recovery, and replay idempotence
  - Org-scoped query executor with budgets, aggregates, and typed lifecycle outcomes
  - Independent oracle equivalence, compaction invariance, and four-boundary crash matrix
  acceptance_commands:
  - cargo test -p riffdb-columnar --all-features
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-columnar --all-targets --all-features -- -D warnings
  - ./scripts/check-workspace-policy
  exit_gate: Columnar engine results match an independent oracle at every published frontier and
    after recovery; crash injection never overclaims a durable frontier; org scope is airtight.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: The CP1 columnar core shipped in 47f626a7 and its independent-oracle, recovery,
      publication, and organization-isolation suites pass on current main.
  human_review_triggers: *id001
- id: WP-431
  name: Columnar projected-read surface engine additions (CP2a)
  depends_on: [WP-430]
  objective: Extend the columnar engine for the ADR-0086 projected read surface — select
    narrowing, primary-key row exposure and order, public snapshot query entry, closed lifecycle
    reason enums, structured storage errors, org-key encoding hook, and incarnation-bound
    ProjectionFrontier/CommitToken types — without public wire/RPC surface (CP2b).
  requirements: []
  required_adrs: [ADR-0086, ADR-0087, ADR-0072]
  allowed_paths: [work_packages.yaml, crates/riffdb-columnar/**, crates/riffdb-types/src/freshness.rs,
    crates/riffdb-types/src/lib.rs]
  deliverables:
  - riffdb-types freshness module (CommitToken, ProjectionFrontier, FreshnessPolicy) with
    incarnation binding and redacted opaque bytes
  - "select: Vec<FieldId> on ColumnarQueryRequest (empty = all projected fields)"
  - Decoded primary-key values on QueryRow; PK components addressable in OrderSpec with
    typed rejection for non-value-order-preserving key types
  - Public query_snapshot entry; engine.query as thin wrapper
  - Closed RebuildingReason/DegradedReason enums with exhaustiveness pin
  - ColumnarError::Storage as structured StorageFailure (never stringified StorageError)
  - Public encode_org_scope_key / OrgKey::from_value hook for CP2b equivalence
  - Engine open takes history_incarnation; frontier accessors return ProjectionFrontier
  acceptance_commands:
  - cargo test -p riffdb-types -p riffdb-columnar --all-features
  - cargo test -p riffdb-columnar --all-features --test cp2a
  - cargo fmt --all -- --check
  - cargo clippy -p riffdb-types -p riffdb-columnar --all-targets --all-features -- -D warnings
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  exit_gate: CP1 acceptance suite remains green under empty-select semantics; select narrowing
    cannot widen; PK-order proof holds for fixed-width key components; frontiers never satisfy
    cross-incarnation tokens; structured storage errors expose only closed kinds.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: CP2a shipped in 0cc5250d; the focused current-head CP2a suite passes all eleven
      selection, ordering, frontier, scope, and structured-error cases.
  human_review_triggers: *id001
- id: WP-432
  name: Columnar projected-read surface wiring and acceptance spine (CP2b)
  depends_on: [WP-431]
  objective: Wire the ADR-0086 projected read surface end to end — config registration,
    columnar apply worker and notifier, freshness wait loop with post-wake reauthorization,
    authorization composition with single-source org/partition derivation, typed §7 outcomes,
    ExecuteProjectedQuery RPC — and ship the acceptance spine (live cross-path board
    equivalence gate, freshness matrix, real-revocation mid-wait denial).
  requirements: []
  required_adrs: [ADR-0086, ADR-0087, ADR-0072]
  allowed_paths: [work_packages.yaml, Cargo.lock, proto/riffdb/app/v1/application.proto,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-service/**,
    crates/riffdb-server/**, crates/riffdb-policy/**, crates/riffdb-types/src/service.rs,
    crates/riffdb-observability/**, crates/riffdb-errors/**, fixtures/proto/**, tests/**]
  deliverables:
  - Config-registered board projection with ProjectionsRoot path role and typed startup errors
  - RunningColumnarWorker apply/checkpoint thread, ColumnarNotifier, and server adapter keeping
    engine co-location behind one port (no storage handle in riffdb-service)
  - projected_query service module with begin/pre-execute/post-wake/pre-release safe points,
    Causal/Bounded/Available freshness, and typed Ready/Lagging/Building/Rebuilding/Degraded/Invalid outcomes
  - ExecuteProjectedQuery on ApplicationQueryService with opaque token/frontier wire carriage
  - Live cross-path equivalence gate; the projected board query is byte-identical to the
    compiled board_page rows at the same frontier over real commands and real apply
  - Freshness matrix acceptance including stale-incarnation refusal and real revocation
    mid-wait denied at the post-wake safe point; columnar worker readiness folded into health
  acceptance_commands:
  - cargo test -p riffdb-service --test projected_read_acceptance
  - cargo test -p riffdb-server --lib
  - cargo test --workspace --all-features
  - cargo fmt --all -- --check
  - cargo clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/generate-proto --check
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  exit_gate: Projected and compiled board reads are byte-identical at the same frontier under
    real writes and real apply; every freshness policy resolves to its typed outcome; a stale
    incarnation token never serves; a capability revoked mid-wait is denied at the post-wake
    safe point without serving rows.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: CP2b shipped in 4184b171 and remains covered by current exact-frontier,
      byte-equivalence, freshness, revocation, and recovery acceptance tests.
  human_review_triggers: *id001
- id: WP-433
  name: Board benchmark through the projected read path (CP3)
  depends_on: [WP-432]
  objective: Close the analytics arc measurement — add board_page_projected_{50,200,450} to the
    app-baseline harness over ExecuteProjectedQuery (generated tonic wire path), with Causal
    catch-up after seed, run-aborting compiled-vs-projected row-content equivalence, and
    board_projected_marginal_ns_per_row in the report (same (p50_450−p50_50)/400 formula).
  requirements: []
  required_adrs: [ADR-0086]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**]
  deliverables:
  - riffdbd harness bootstrap with --projections-root and board projection registration
    (two-phase start so ColumnarRuntime opens after contract deploy)
  - board_page_projected_{50,200,450} scenarios on RiffDB via generated ApplicationQueryService
    client; select/predicate/order/limit/org_scope/freshness AVAILABLE match CP2b board shape
  - Pre-timing Causal catch-up to last seed commit token and run-aborting cross-path equivalence
  - Report field board_projected_marginal_ns_per_row plus per-N projected p50s (additive only)
  - Unit coverage for request construction and equivalence gate discriminating power
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 fmt --all -- --check
  - cd examples/app-baseline && cargo +1.97.0 clippy --workspace --all-targets -- -D warnings
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: Smoke run WITH --board-density >= 50 executes projected board scenarios with gates passing (plain --smoke skips board cells by design); report carries
    board_projected_marginal_ns_per_row when 50/450 projected samples exist; PG path unchanged.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: The CP3 projected-board path shipped in fc0b78f4; current app-baseline tests freeze
      board density, page sizes, projected marginal accounting, and comparator separation.
  human_review_triggers: *id001
- id: F-A
  name: Deterministic saturation for flaky capacity and gate-a families
  depends_on: [WP-120, WP-410]
  objective: Eliminate load-sensitive false alarms in the service capacity/admission family
    and contract_migration_gate_a by making assertions state-driven (constructed full channel
    or terminal migration phase) rather than wall-clock races under full-workspace parallel load.
  requirements: []
  required_adrs: [ADR-0071, ADR-0079]
  allowed_paths: [work_packages.yaml, tests/service/**, tests/migrations/contract_migration_gate_a.rs,
    crates/riffdb-testkit/**, crates/riffdb-service/src/command_operations.rs, crates/riffdb-service/tests/architecture.rs]
  deliverables:
  - Seven capacity/admission service_end_to_end tests rewritten to constructed saturation
    (held workload permits or retained-byte budget) with generous request deadlines
  - contract_migration_gate_a terminal wait ends on migration phase state with a hang ceiling only
  - Sub-MIN_REMAINING immediate-reject coverage moved to unit level (riffdb-service
    admission_budget_* tests) now that no end-to-end probe carries a near-expired deadline, with
    a source-level pin that both admission stages shed a sub-floor budget through one shared
    mapping. Falsifiability - deleting the early return in admission_deadline reddens
    admission_budget_rejects_below_min_remaining_and_reserves_it_above; mapping the rejection to
    DeadlineExceeded reddens admission_budget_rejection_is_typed_capacity_overload_not_deadline_exceeded
    (the mapping rule) or a_sub_floor_admission_budget_is_shed_as_overload_at_both_admission_stages
    (either consuming arm).
  - Queue-delay shed asserts zero bounded admission-wait registrations, the only
    shed-distinguishing observable. Typed Overloaded, CapacityRejected QueueDepth, and zero durable
    audit rows are NOT shed-distinguishing - the 150 ms admission-cap fallthrough produces all
    three identically, so the earlier claim that disabling the shed reddens those asserts was
    wrong. Falsifiability - disabling the shed condition reddens
    queue_delay_shed_rejects_before_admission_with_zero_durable_audit_rows on the registration
    assert alone; admission_deadline_while_queued_is_overloaded_not_deadline_exceeded carries the
    nonzero positive control so the zero assert cannot pass vacuously.
  - Full-workspace battery green three times under parallel load (acceptance proof)
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-service --test service_end_to_end --all-features --
      --exact
      saturated_command_capacity_rejects_before_reauthorization_with_typed_overload
      capacity_n_concurrent_commands_yield_only_typed_overload_or_commit
      retained_bytes_exhaustion_rejects_with_capacity_stage
      read_only_capacity_rejects_before_audit_start_with_typed_overload
      queue_delay_shed_rejects_before_admission_with_zero_durable_audit_rows
      capacity_overload_mutation_leaves_no_terminal_audit_row
      admission_deadline_while_queued_is_overloaded_not_deadline_exceeded
  - cargo +1.97.0 test -p riffdb-service --lib --all-features -- admission_budget
  - cargo +1.97.0 test -p riffdb-service --test architecture --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test contract_migration_gate_a --all-features
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - cargo +1.97.0 doc --workspace --no-deps
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: Capacity-family probes reject with typed Overloaded from constructed full state without
    short Instant deadlines; the sub-MIN_REMAINING immediate-reject path stays covered at unit level
    and its typed-overload mapping is pinned at both admission stages; the queue-delay shed carries
    a shed-distinguishing observable with a nonzero positive control; gate_a completes on terminal
    migration phase; three consecutive full workspace batteries report zero failures.
  human_review_triggers: *id001
- id: WP-434
  name: Projected-read SDK facade, CLI verb, and packed encode-into (CP4)
  depends_on: [WP-433]
  objective: Give first-party callers a typed projected-read surface — a riffdb-client-rust
    request builder and outcome facade with hostile-shape packed decoding, a riffdb query
    projected CLI verb over it, and a canonical encode-into codec entry point that removes
    per-cell intermediate allocations from the api-grpc packed path without changing bytes.
  requirements: []
  required_adrs: [ADR-0086, ADR-0011]
  allowed_paths: [work_packages.yaml, docs/reference/CLI.md, crates/riffdb-client-rust/**,
    crates/riffdb-cli/**, crates/riffdb-types/src/codec.rs, crates/riffdb-api-grpc/**]
  deliverables:
  - ProjectedQuery builder plus ProjectedQueryOutcome mirroring every wire arm, on both
    RiffDbClient and StableApplicationClient, with opaque frontier/token carriage
  - Packed Ready block decoder with fail-closed offset validation (length, first zero,
    last equals data length, monotone spans, exact cell documents) and a nonzero row
    count refused without columns so remote input never sizes an allocation
  - Encoding-arm agreement enforced both ways; the row arm is refused under a PACKED
    request and the packed arm under a row request
  - riffdb query projected with org scope, equality and JSON-array range predicates,
    order, limit, freshness policies with hex or raw commit-token input, and --packed
  - encode_canonical_value_into with document-relative size accounting, byte-identical
    to encode_canonical_value and all-or-nothing on error, reserved through
    canonical_value_encoded_len on the api-grpc pack path
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-client-rust --lib -- projected::tests
  - cargo +1.97.0 test -p riffdb-types --lib -- codec::tests
  - cargo +1.97.0 test -p riffdb-cli --lib
  - cargo +1.97.0 test -p riffdb-api-grpc --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/generate-cli-reference --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: A projected read is expressible from Rust and from the CLI under every freshness
    policy; the packed decoder rejects each hostile offset and row-count shape without
    panicking or reserving remote-chosen capacity; encode-into is byte-identical to the
    allocating encoder and leaves a rejected cell out of the caller's buffer entirely.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: The CP4 SDK, CLI, and packed encode-into surfaces shipped in 208a355d; hostile-shape
      and exact-byte tests remain green.
  human_review_triggers: *id001
- id: WP-435
  name: DeployReactiveModule audit link allowlist repair and cross-crate pin (FX)
  depends_on: [WP-434]
  objective: Repair the confirmed production defect in which a successful DeployReactiveModule
    publication could not record its own terminal audit record — every storage-side
    phase/link and link-reciprocity validator omitted the operation from its control-plane
    arm, so the coordinator's Succeeded append failed and the RPC reported outcome_unknown
    for an already durable module — and pin the service and storage allowlists against
    each other so they cannot drift apart again.
  requirements: []
  required_adrs: [ADR-0021]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/src/audit.rs, crates/riffdb-service/src/audit.rs,
    crates/riffdb-storage-redb/src/administration.rs, crates/riffdb-storage-redb/src/startup.rs,
    crates/riffdb-storage-memory/src/administration.rs, crates/riffdb-storage-memory/src/integrity_administration.rs,
    tests/grpc/**]
  deliverables:
  - DeployReactiveModule accepted with a Succeeded control-plane link by the storage-api
    phase/link validator, by the redb and memory write-time link-reciprocity checks, and
    by the redb startup and memory integrity structural passes, with no existing allowance
    narrowed and no durable encoding, schema hash, or registry pin touched
  - Storage-api unit matrix driven by declarative operation tables instead of a copy of
    the implementation match, plus a named regression case asserting the exact previously
    rejected DeployReactiveModule/Succeeded/ControlPlane append intent and stored record
  - Cross-crate pin in riffdb-service asserting the service pre-check and the durable
    storage validator name the same operations for each authoritative link kind, with the
    single storage-only exception (ApplyContractMigration, appended by the redb migration
    cutover stage) listed with evidence rather than silently tolerated
  - Real-daemon acceptance that the DeployReactiveModule RPC reports PUBLISHED and that
    the durable administration stream carries exactly one Started and one Succeeded record
    whose control-plane link names the reactive publication's own transition
  - The pruned-history acceptance publishes its stream module through the daemon RPC
    instead of routing around the defect through the offline storage repository
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --lib --all-features -- audit::tests
  - cargo +1.97.0 test -p riffdb-service --lib --all-features -- audit::tests
  - cargo +1.97.0 test -p riffdb-server --test p1_riffdbd_restart --all-features
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: A reactive module published through the real daemon returns PUBLISHED rather than
    outcome_unknown and its Succeeded control-plane audit record is durable and reciprocal
    across a full startup structural pass; re-removing the operation from the storage-api
    control-plane arm turns the unit matrix, the cross-crate pin, and the real-daemon
    acceptance red; the full workspace battery reports zero failures.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: The DeployReactiveModule audit allowlist repair and cross-crate pin shipped in
      73a15cf1 and remain covered by the current all-feature workspace battery.
  human_review_triggers: *id001
- id: WP-436
  name: Reactive republish audit parity, memory publish conformance, and write-time linkless
    tightening (FY)
  depends_on: [WP-435]
  objective: Close the three audit-semantics items the WP-435 review confirmed — a successful
    idempotent reactive republish durably recorded a Failed audit record because the
    already-published storage result carried no administration sequence; the memory
    write-path control-plane allowlist omitted DeployQueryModule while the same crate's
    integrity pass accepted it, and both memory validator sites had zero test coverage; and
    a DeployReactiveModule linkless success remained durably writable even though every one
    of its successes is linked.
  requirements: []
  required_adrs: [ADR-0021]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/src/audit.rs, crates/riffdb-storage-api/src/reactive_module.rs,
    crates/riffdb-commit/src/control_plane.rs, crates/riffdb-storage-redb/src/administration.rs,
    crates/riffdb-storage-redb/src/startup.rs, crates/riffdb-storage-memory/src/administration.rs,
    crates/riffdb-storage-memory/src/integrity_administration.rs, tests/grpc/**]
  deliverables:
  - ReactiveModulePublicationResult::AlreadyPublished carries the original publication's
    shared administration sequence, threaded out of both backends, so the coordinator
    records Succeeded with a control-plane link naming that publication — exact parity with
    the catalog and query-module already-active arms — with no durable encoding, schema
    hash, or registry pin touched
  - DeployQueryModule added to the memory write-path control-plane link allowlist, removing
    the backend drift against the same crate's integrity pass
  - Memory-backend publish conformance for both reactive and query modules, driving the real
    repository traits, the real ServiceAuditAppendRepository write path, and the real
    structural pass, so neither memory validator site is covered by nothing
  - DeployReactiveModule refused a linkless success at append time only, through
    ServiceAuditAppendIntentV1::new, while durable reconstruction and both structural passes
    keep the released tolerance so no historical database is refused at open, with the
    one-sided asymmetry documented where each list lives and pinned by tests on both sides
  - Real-daemon acceptance that republishing an identical reactive module reports
    ALREADY_PUBLISHED, writes no second publication record, and records a Succeeded terminal
    whose control-plane link names the original publication's transition
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --lib --all-features -- audit::tests
  - cargo +1.97.0 test -p riffdb-storage-memory --lib --all-features -- administration::tests
  - cargo +1.97.0 test -p riffdb-storage-redb --lib --all-features -- startup::tests
  - cargo +1.97.0 test -p riffdb-server --test p1_riffdbd_restart --all-features
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: An idempotent reactive republish over the real daemon reports ALREADY_PUBLISHED
    and durably records Succeeded linked to the original publication, and reverting the
    coordinator arm to sequence-less turns that acceptance red with a Failed record;
    re-narrowing either memory control-plane allowlist turns the new memory conformance
    cases red; a DeployReactiveModule linkless success cannot be appended yet a synthetic
    durable one still opens clean on both structural passes; the full workspace battery
    reports zero failures.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Idempotent reactive republish audit parity and memory/redb conformance shipped in
      01f28c34; current structural and daemon acceptance suites pass.
  human_review_triggers: *id001
- id: WP-437
  name: App-baseline evidence integrity and repeatability
  depends_on: [WP-379, WP-433]
  objective: Make every load comparison self-describing, order-balanced, repetition-aware, snapshot-isolated by
    default, and fail-closed when final application state does not match recorded outcomes.
  requirements: [PERF-001, PERF-008]
  required_adrs: [ADR-0001, ADR-0056, ADR-0060]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    crates/riffdb-bench-root/**, docs/performance/**]
  deliverables:
  - Load-suite environment, storage, device, PostgreSQL durability, backend order, and same-device evidence matching
    the parity report, with an evidentiary refusal when required fields are absent
  - Deterministic balanced isolated backend order across repetitions without concurrent engine interference
  - Median/min/max/spread summaries for every backend/client load point and --require-stable enforcement
  - Fresh identically seeded process/storage state per concurrency point by default; accumulated history retained as
    an explicitly selected history-growth mode
  - Post-load semantic reconciliation of successful, replayed, and rejected effects
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - cd examples/app-baseline && cargo +1.97.0 clippy --workspace --all-targets -- -D warnings
  - ./benchmarks/run-app-baseline --self-test
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: An evidentiary load report cannot omit environment/durability identity, cannot pass unstable repetitions,
    cannot compare different history states implicitly, and cannot report clean when durable application state
    disagrees with recorded operation outcomes.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Evidence identity, balanced isolation, stability refusal, fresh-history selection, and
      post-load reconciliation shipped in a5a0cde3 and remain pinned by the current self-test.
  human_review_triggers: *id001
- id: WP-438
  name: Open-loop arrival, tenant fairness, contention, and data-shape matrix
  depends_on: [WP-437]
  objective: Measure offered demand rather than only self-throttled closed-loop completions and cover the partition,
    contention, payload, fan-out, page-size, and history shapes expected from alpha applications.
  requirements: [PERF-003, PERF-008]
  required_adrs: [ADR-0001, ADR-0056, ADR-0060]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    docs/performance/**]
  deliverables:
  - Bounded deterministic Poisson open-loop driver with offered/admitted/completed/rejected rates, queue delay,
    per-operation p50/p95/p99, overload, and saturation-knee evidence
  - One/eight/sixty-four tenant routing plus hot-tenant/cold-tenant fairness and per-tenant tail summaries
  - Required isolated, same-ticket, and membership-contention matrix modes
  - Bounded payload, comment-history, label-fanout, board-density, page-size, cursor-depth, and retained-history tiers
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - cd examples/app-baseline && cargo +1.97.0 clippy --workspace --all-targets -- -D warnings
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The suite locates the offered-load saturation knee, reports fairness without high-cardinality labels,
    and records every selected tenant/contention/data-shape dimension in machine-readable output.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Bounded open-loop accounting, tenant routing/fairness, contention, and data-shape
      profiles shipped in a5a0cde3; current harness tests retain their machine-readable bounds.
  human_review_triggers: *id001
- id: WP-439
  name: Stateful application journeys and semantic reconciliation
  depends_on: [WP-438]
  objective: Add causal browser, agent, and durable-worker journeys whose end-to-end results are reconciled against
    final state rather than treating independent successful RPCs as a complete application proof.
  requirements: [PERF-008, AAA-018]
  required_adrs: [ADR-0056, ADR-0060]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    docs/performance/**]
  deliverables:
  - Browser list/detail/command/read-after-commit journey
  - Agent page/query/declared-outcome/causal-reread journey
  - Durable event next/command/event-derived-idempotency/ack journey
  - End-to-end journey histograms and exact post-run missing/duplicate/state-divergence checks
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - cd examples/app-baseline && cargo +1.97.0 clippy --workspace --all-targets -- -D warnings
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: Every supported journey measures one causal application action and proves its final durable effect exactly
    once; a dropped, duplicated, stale, or incorrectly shaped result turns the run red.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Browser, agent, and durable-worker journey reconciliation shipped in a5a0cde3 with
      exact once-only final-state checks retained by the current harness.
  human_review_triggers: *id001
- id: WP-440
  name: Crash, history, deployment, authorization, and recovery under load
  depends_on: [WP-439, WP-422, WP-423]
  objective: Exercise the application surface through real process death, history growth, contract/query evolution,
    role change, overload, restart, and recovery while load remains active.
  requirements: [PERF-003, PERF-013, PERF-014]
  required_adrs: [ADR-0001, ADR-0056, ADR-0060]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    crates/riffdb-bench-root/**, docs/performance/**]
  deliverables:
  - Real kill-under-load cases at bounded command lifecycle points with no-lost/no-duplicate outcome reconciliation
  - Identical-profile retained-history checkpoints and restart/recovery latency curves
  - Query-module deployment and application-role rebind/revoke under active reads and writes
  - Post-overload and post-restart time-to-ready and time-to-baseline-latency measurements
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - ./benchmarks/run-app-baseline --self-test
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: Every injected failure or evolution transition yields a typed expected public result, recovery preserves
    exact command semantics, and the report distinguishes correctness, downtime, and latency recovery.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Crash, history, deployment, role, overload, and restart profiles shipped in a5a0cde3;
      the current self-test includes bounded dead-peer process-abort coverage.
  human_review_triggers: *id001
- id: WP-441
  name: Benchmark attribution and resource accounting
  depends_on: [WP-437]
  objective: Attach bounded redaction-safe stage, scheduler, process, and storage evidence so throughput or latency
    gaps identify a product subsystem rather than merely producing a ratio.
  requirements: [PERF-001, PERF-008]
  required_adrs: [ADR-0056, ADR-0060]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    crates/riffdb-observability/**, crates/riffdb-server/**, docs/performance/**]
  deliverables:
  - Command queue/preparation/evaluation/admission/stage/flush/publication and read auth/plan/storage/encode summaries
  - Group count/bytes, queue depth, durable flush count, CPU, RSS, disk bytes, database size, and write-amplification
    evidence with closed low-cardinality labels
  - Per-client completed-operation and longest-pause fairness summaries
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - cargo +1.97.0 test -p riffdb-observability --all-features
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: Every evidentiary run carries enough bounded stage and resource evidence to distinguish transport,
    queueing, CPU, durable I/O, storage growth, and unfair scheduling without exposing application values.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Redaction-safe stage, scheduler, process, storage, and fairness attribution shipped in
      a5a0cde3 and remains part of the versioned report surface.
  human_review_triggers: *id001
- id: WP-442
  name: Minimal and safety-equivalent PostgreSQL comparisons
  depends_on: [WP-437]
  objective: Preserve PostgreSQL as both the optimized conventional floor and an honest comparison implementing the
    idempotency, provenance/audit, durable-event/outbox, and authorization obligations performed by RiffDB.
  requirements: [PERF-008]
  required_adrs: [ADR-0001, ADR-0045, ADR-0056]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    docs/performance/**]
  deliverables:
  - Explicit postgres_minimal and postgres_safe_app backend identities sharing one application workload
  - Safety-equivalent transactional idempotency, audit/provenance, event/outbox-intent, and authorization checks
  - Reports and prose that retain the minimal performance floor and never claim PostgreSQL cannot implement safety
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - cd examples/app-baseline && cargo +1.97.0 clippy --workspace --all-targets -- -D warnings
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: Both PostgreSQL profiles execute the same domain operation shapes, the safety profile proves its extra
    obligations atomically, and every report identifies which comparator produced each number.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Minimal and safety-equivalent PostgreSQL profiles shipped in a5a0cde3; current tests
      freeze comparator identities, durability, obligations, and SQLSTATE-based outcomes.
  human_review_triggers: *id001
- id: WP-443
  name: Rust TypeScript Python application conformance
  depends_on: [WP-439]
  objective: Keep precise database load generation in Rust while proving Rust, TypeScript, and Python public generated
    clients observe identical application semantics without handwritten transport or kernel imports.
  requirements: [AAA-003, AAA-009, AAA-018]
  required_adrs: [ADR-0001, ADR-0056]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, clients/typescript/**, clients/python/**,
    benchmarks/run-app-baseline, scripts/**, docs/performance/**]
  deliverables:
  - One golden language-neutral command/query/error/cursor/read-after-commit workload
  - Executable Rust, TypeScript, and Python conformance runners over their public generated application clients
  - Canonical semantic observation comparison and boundary lint for raw IDs, kernel imports, maps, encoders,
    decoders, and handwritten RPC wrappers
  - Language runtime/setup time reported separately and excluded from database parity ratios
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - ./scripts/check-generated
  - ./scripts/check-workspace-policy
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: All three languages emit byte-equivalent canonical semantic observations for the golden workload and no
    runner imports kernel APIs or owns application transport/encoding logic.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Rust, TypeScript, and Python golden conformance shipped in a5a0cde3; current generated
      binding and boundary checks retain the no-kernel/no-handwritten-transport rule.
  human_review_triggers: *id001
- id: WP-444
  name: Benchmark release matrix and evidence gate
  depends_on: [WP-438, WP-439, WP-440, WP-441, WP-442, WP-443]
  objective: Compose parity, load, and resilience into one bounded release matrix whose eligible result cannot hide
    an omitted dimension, unstable repetition, correctness failure, or comparison mismatch.
  requirements: [PERF-008, PERF-013, PERF-014, AAA-012]
  required_adrs: [ADR-0001, ADR-0056, ADR-0060]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    benchmarks/reports/**, release/evidence/**, docs/performance/**]
  deliverables:
  - Separate parity, load, and resilience reports plus one manifest of required cells and evidence hashes
  - Required contention, tenant, data-shape, stateful-journey, crash, evolution, comparator, and language cells
  - Eligible/not-eligible decision retaining every miss without waiver
  acceptance_commands:
  - ./benchmarks/run-app-baseline --self-test
  - cargo +1.97.0 fmt --all -- --check
  - ./scripts/check-requirement-coverage
  exit_gate: The alpha benchmark gate is eligible only when every required cell is present, stable, semantically
    reconciled, and measured under recorded comparable storage and durability conditions.
  closure:
    status: complete_framework_not_release_evidence
    evidence: docs/architecture/wp-430-448-closure.md
    decision: The fail-closed release-matrix manifest shipped in a5a0cde3. It correctly refuses
      incomplete or unstable evidence; closing the framework does not qualify the current alpha.
  human_review_triggers: *id001
- id: WP-445
  name: App-baseline realistic-suite documentation and evidence refresh
  depends_on: [WP-444]
  objective: Publish the exact runbook, claim boundaries, report schemas, interpretation rules, and refreshed evidence
    for the realistic alpha benchmark without collapsing its three suites into one misleading score.
  requirements: [PERF-001, PERF-008, PERF-013, PERF-014]
  required_adrs: [ADR-0001, ADR-0045, ADR-0056, ADR-0060]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/reports/**,
    release/evidence/**, docs/performance/**]
  deliverables:
  - Operator runbook for parity, closed/open-loop load, matrix, resilience, comparators, and language conformance
  - Versioned report-schema reference and compatibility policy
  - Refreshed full evidence plus bounded conclusions that distinguish product guarantees from benchmark assumptions
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: A new operator can reproduce and correctly interpret every alpha benchmark cell from public documentation,
    and checked evidence contains no unsupported safety or performance claim.
  closure:
    status: complete
    evidence: docs/performance/app-baseline-alpha.md
    decision: The realistic-suite runbook, report interpretation, comparator boundaries, and evidence
      eligibility rules shipped in a5a0cde3 and pass the current handbook check.
  human_review_triggers: *id001
- id: WP-446
  name: Reactive-module storage hygiene — republish off the write lock, memory structural
    inspection, and the read-path cost ruling (FZ)
  depends_on: [WP-436]
  objective: Close the three storage-hygiene items the WP-436 review confirmed — the redb
    republish path walked the unbounded, never-pruned AUDIT table while holding the redb
    write lock, stalling every other writer including commands; the memory structural pass
    never inspected state.reactive_modules at all, so a retained reactive-module row that no
    publication record names, or whose identity disagreed with its own artifact bytes,
    produced no finding while redb reported both; and redb read_reactive_module did not fail
    closed on a module with no publication record, where the cost of the only correct check
    had to be established before shipping it rather than after.
  requirements: []
  required_adrs: [ADR-0021]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/src/audit.rs, crates/riffdb-storage-redb/src/administration.rs,
    crates/riffdb-storage-redb/src/gate.rs, crates/riffdb-storage-redb/src/store.rs, crates/riffdb-storage-memory/src/administration.rs,
    crates/riffdb-storage-memory/src/integrity_administration.rs, crates/riffdb-storage-memory/src/startup.rs]
  deliverables:
  - The redb idempotent-republish answer — allocator/tail proof, contract and query-module
    preconditions, presence probe, and the original publication's sequence — computed in a
    read transaction, so the unbounded AUDIT walk no longer runs under the exclusive
    mutation gate; a restructure only, with no durable encoding, schema hash, registry pin,
    or reverse index touched
  - The write path retained unchanged behind that probe and re-probing presence under its own
    write transaction, so a publish that lands between the read answer and begin_write is
    served the same republish outcome instead of writing a duplicate, with the rare
    under-lock scan documented as the bounded-probability exception
  - Shared table-generic tail validation and precondition predicates so the read probe and
    the write path cannot drift about which streams and dependencies are admissible
  - Memory structural inspection of every retained reactive-module row, mirroring redb's
    inspect_reactive_module_row arm for arm and code for code — artifact and source hash
    agreement plus ordering (CrossLinkMismatch), the exact contract artifact, every exact
    query-module dependency, and the row's own publication record (MissingCrossLink)
  - A recorded cost ruling for redb read_reactive_module's fail-closed check rather than the
    check itself — its callers are every consumer consume, acknowledge, negative-acknowledge,
    seek, retire, and status RPC plus WatchNamedQuery setup with no cache, and the frozen
    durable layout offers no bounded witness for "a publication record exists for this
    module_hash", so the check would turn an O(1) read into an O(|AUDIT|) decode scan over a
    stream that those same RPCs grow — deliberately not shipped, and reported for a
    maintainer decision
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --lib --all-features -- administration::tests
  - cargo +1.97.0 test -p riffdb-storage-memory --lib --all-features -- administration::tests
  - cargo +1.97.0 test -p riffdb-storage-api --lib --all-features -- audit::tests
  - cargo +1.97.0 test -p riffdb-server --test p1_riffdbd_restart --all-features
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: An idempotent redb republish returns the original publication's sequence while
    the exclusive mutation gate issues zero new tickets, and routing that answer through the
    write transaction unconditionally turns that pin red; driving the locked path directly
    still serves the same republish answer and writes nothing; a retained redb module with no
    publication record is still refused with CorruptData; a retained memory reactive-module
    row that no publication record names, and one whose identity disagrees with its artifact,
    each yield exactly one typed authoritative finding, while a published module validates
    clean, and removing the new inspection arm returns all of them to zero findings; the full
    workspace battery reports zero failures.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Write-lock-free idempotent republish and memory structural inspection shipped in
      abadb5e2; the current storage suites retain the no-write and corruption-refusal pins.
  human_review_triggers: *id001
- id: WP-447
  name: Storage-scan hygiene — single-pass startup verification of reactive publications,
    memory's redundant per-read scan removed, and memory consumer/delivery structural
    inspection (GA)
  depends_on: [WP-446]
  objective: Close the three storage-hygiene items the WP-437 review ledgered — redb's
    startup proof that every retained reactive-module row still has its publication record
    scanned the entire unbounded, never-pruned AUDIT table once PER ROW, so a long-lived
    database paid up to MAX_RETAINED_REACTIVE_MODULES full protobuf decode passes at every
    open; memory still walked its whole administration audit vector on every consumer RPC to
    re-prove the same property its own structural pass now proves at open, the redundant scan
    the WP-437 ruling identified; and memory's structural pass never inspected
    state.event_consumers or state.event_consumer_deliveries at all, so a consumer row naming
    an unretained reactive module, or a delivery row whose consumer is gone, produced no
    finding while redb reported both.
  requirements: []
  required_adrs: [ADR-0021, ADR-0085]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-redb/src/startup.rs, crates/riffdb-storage-memory/src/administration.rs,
    crates/riffdb-storage-memory/src/consumer.rs, crates/riffdb-storage-memory/src/integrity_administration.rs,
    crates/riffdb-storage-memory/src/startup.rs]
  deliverables:
  - The redb reactive-publication proof served from one transient witness set built in a
    single forward AUDIT pass and answered per row by set lookup, replacing the per-row full
    table scan; identical findings — same codes, scopes, and counts — on every fixture, a
    pure cost change with no durable encoding, schema hash, registry pin, or index touched
  - That witness pass kept UNCONDITIONAL and untruncated under the ADR-0085 validated-prefix
    checkpoint fast path — it is driven by the REACTIVE_MODULES phase, whose rows are an
    additive structural count the checkpoint guard never exempts, and it reads the whole AUDIT
    table itself rather than the checkpoint-truncated suffix the AUDIT phase walks, because
    the unconditional open-time proof is what licenses both backends' read paths to skip it
  - The witness set bounded by the table under inspection through a REACTIVE_MODULES
    membership filter, and released with its phase, plus a decode counter that pins the cost
    (one pass, not one per row) rather than asserting it in prose
  - Memory's per-read publication scan in read_reactive_module removed, converging its read
    with redb's O(1) read, with the ruling recorded at the code site in the style of redb's own
    note — the structural pass proves row-presence implies publication-record for every row,
    the sole writer commits row and record in one gated mutation, nothing removes either — and
    with the two honest caveats that memory's base case is weaker (crate tests reach
    operational ports without a pass) and is acceptable only because the backend has no
    production consumer, plus the recorded decision not to keep the check behind a
    debug_assert
  - Memory structural inspection of every retained event-consumer and delivery row, mirroring
    redb's inspect_event_consumer_row and inspect_event_consumer_delivery_row check for check
    — identity-hash derivation, retained database binding, and ordering standing in for redb's
    key agreement plus snapshot round trip (CrossLinkMismatch), and the reactive module a
    consumer names and the consumer a delivery names (MissingCrossLink)
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --lib --all-features -- startup::tests
  - cargo +1.97.0 test -p riffdb-storage-memory --lib --all-features -- administration::tests
  - cargo +1.97.0 test -p riffdb-storage-redb --lib --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --lib --all-features
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: A cleanly published redb database with three retained reactive modules validates
    with zero findings while decoding the audit stream exactly once for the whole
    REACTIVE_MODULES phase, and rebuilding the witness per row turns that count red at three
    times the stream length; a retained row that no publication record names still yields
    exactly one authoritative MissingCrossLink, and leaving the witness set unpopulated
    instead reds every retained module; the same orphan is still reported, and the same full
    audit stream still read, on an open driven by a verified validated-prefix checkpoint; a
    memory reactive-module row with no publication record is returned by read_reactive_module
    and reported by the structural pass, so the detection moved rather than vanished; a
    written memory consumer and its leased delivery validate clean while a consumer naming an
    unretained reactive module and a delivery whose consumer is absent each yield exactly one
    authoritative MissingCrossLink, and removing either new inspection arm returns its case to
    zero findings; the full workspace battery reports zero failures.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Single-pass reactive publication verification and complete memory consumer/delivery
      inspection shipped in 163bf294 and remain covered by current structural tests.
  human_review_triggers: *id001
- id: WP-448
  name: Graceful-shutdown checkpoint in O(1) — validated-prefix row counts read from table
    metadata instead of walking all history (GB)
  depends_on: [WP-447]
  objective: Close the measured graceful-shutdown defect the app-baseline sweep exposed —
    write_validated_prefix_checkpoint rebuilt every checkpoint field by WALKING, so a ~550k-op
    database spent 20,705ms in shutdown against 143ms with the write disabled (99.3% of
    shutdown), overrunning the harness PROCESS_STOP_TIMEOUT of 15s into SIGKILL, and the
    production mirror spends minutes at 10M commits. The eight recorded below-S row counts are
    the whole cost, and they are recoverable without touching a row — S and the audit bound are
    the LAST keys of COMMITS and AUDIT in the same snapshot, so "rows with sequence <= S" is
    "every row in the table" for every count class, and redb already maintains each table's row
    count in its table header. The single exception is idempotency_count, which admits only
    StoredOutcome rows, so the terminal ExecutionFailed census — the one quantity a row count
    cannot express — is maintained in process memory, seeded exactly by the startup walk that
    opens the checkpoint write gate and advanced by the single lane that writes such a row.
  requirements: []
  required_adrs: [ADR-0019, ADR-0085]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-redb/src/validated_prefix.rs,
    crates/riffdb-storage-redb/src/store.rs, crates/riffdb-storage-redb/src/startup.rs,
    crates/riffdb-storage-redb/src/application.rs, crates/riffdb-storage-redb/tests/architecture.rs]
  deliverables:
  - The checkpoint build given a closed CheckpointCountSource — Walked (the unchanged reference
    walk, retained as the definition of each count) and DurableLengths (eight redb table row
    counts plus the terminal census) — with the production write selecting DurableLengths and
    the record's format, semantics, hashing, and bindings untouched — this changes how the eight
    numbers are obtained, never what is written
  - The derivation's preconditions stated where it lives and CHECKED where checking is cheap —
    the three event-keyed tables have "no row above S" verified from their last key by one
    B-tree descent, an impossible census or a checked table that fails the probe falls back to
    the reference walk rather than writing counts the next open would refuse, and the S == 0 /
    audit-bound == 0 cases return the same zeros the walks return without classifying a row
  - The terminal ExecutionFailed census seeded in the same statement pair that opens the
    checkpoint write gate — so no checkpoint can ever be built from an unseeded census — from a
    single terminal-class decode the IDEMPOTENCY phase already performs on the full walk and on
    the checkpoint fast path alike, with the phase-8 prefix skip answered from that same decode
    rather than a second protobuf pass
  - That census advanced inside the mutation lease that made the row durable, through one
    commit path used by the one lane that writes a terminal ExecutionFailed row, so a checkpoint
    write acquiring the lease afterwards can never see the row without its census entry, plus a
    source guard that a second uncounted writer cannot be added silently
  - The byte-identity property test as the acceptance centrepiece — on randomized history shapes
    covering execution failures, empty history, a retention-pruned prefix, and a fully populated
    database, the two count sources encode the SAME checkpoint bytes and the O(1) source
    iterates zero rows; plus a zero-walk pin on the real graceful-shutdown write, a
    fail-closed test proving a drifted census is refused at the next open rather than trusted,
    and an ignored scale demonstration timing walked against derived
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --lib --all-features -- startup::tests
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --workspace --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: On randomized history shapes the metadata-derived counts encode byte-identical
    checkpoints to the reference walk while iterating zero rows, and dropping the ExecutionFailed
    subtraction turns those bytes unequal; the graceful-shutdown write on a real clean open
    iterates zero history rows and the checkpoint it writes is accepted by a reopen that
    verifies every recorded count against redb's own row counts, while re-pointing that write at
    CheckpointCountSource::Walked turns the row tally nonzero; one committed terminal execution
    failure advances the census and keeps the derived bytes equal to the walk's, while neutering
    that lane's counting turns them unequal; a census drifted by one is refused at the next open
    rather than silently trusted, and an impossible census falls back to the reference walk
    before the write; at 550k rows per counted table the build drops from 3,060ms over 4,400,000
    iterated rows to 432ms over zero, and to 63us with no live entities, leaving the
    entity-chain fingerprint's current-state ENTITIES pass as the sole remaining
    history-independent residual; the full workspace battery reports zero failures.
  closure:
    status: complete
    evidence: docs/architecture/wp-430-448-closure.md
    decision: Constant-time metadata-derived shutdown checkpoint counts shipped in 66966813 with
      byte-identity, fallback, census-drift refusal, reopen, and scale coverage intact.
  human_review_triggers: *id001
- id: WP-449
  name: Mixed-load writer evidence and fresh-history diagnosis
  depends_on: [WP-445, WP-448]
  objective: Turn the failed PERF-008 mixed-load curve into per-point causal evidence before changing
    scheduling or durable semantics, and prevent accumulated history from being presented as a pure
    concurrency curve.
  requirements: [PERF-001, PERF-008]
  required_adrs: [ADR-0056, ADR-0059, ADR-0060, ADR-0061]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**, benchmarks/run-app-baseline,
    crates/riffdb-commit/**, crates/riffdb-observability/**, crates/riffdb-server/**,
    docs/performance/**]
  deliverables:
  - Per-point delta evidence for commit/flush latency, durable batch size, writer busy/idle time,
    command queue delay, dispatch selection, and compatibility splitting, with closed fixed labels
  - Fresh identical database state by default for 1/8/32/128-client read-only, write-only, and mixed
    sweeps; retained-history mode remains explicit and cannot satisfy concurrency-only gates
  - Exact per-operation logical and physical command rates plus process CPU, RSS, durable growth, and
    kernel-write amplification normalized by successful mutation
  - A measured diagnosis naming the first saturated stage without weakening or bypassing any command
    obligation
  acceptance_commands:
  - cd examples/app-baseline && cargo +1.97.0 test --workspace
  - cargo +1.97.0 test -p riffdb-observability --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - ./benchmarks/run-app-baseline --self-test
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every concurrency point owns an independent telemetry delta and fresh database by
    default, write-only saturation exposes the physical durable ceiling, and no PERF-008 conclusion
    depends on cumulative shutdown-only counters or an accumulated-history curve.
  closure:
    status: complete
    evidence: docs/performance/wp-449-writer-evidence.md
    decision: Independent per-point writer deltas and fresh-history defaults shipped in 182fabec,
      with the saturated writer and compatibility split identified without weakening semantics.
  human_review_triggers: *id001
- id: WP-450
  name: Independent read and command admission topology
  depends_on: [WP-449]
  objective: Prevent long durable commands from consuming the complete blocking application-port
    worker set while retaining bounded admission, one authoritative writer, cancellation, shutdown,
    and fail-closed readiness.
  requirements: [PERF-007, PERF-008]
  required_adrs: [ADR-0007, ADR-0060, ADR-0071]
  allowed_paths: [work_packages.yaml, crates/riffdb-service/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, crates/riffdb-testkit/**, tests/grpc/**,
    examples/app-baseline/**, benchmarks/run-app-baseline, docs/performance/**]
  deliverables:
  - Separately bounded command and read blocking-port lanes with CPU-aware closed worker counts and
    independent permits, queues, waiter limits, lifecycle ownership, and shutdown evidence
  - Typed routing that cannot send an authoritative mutation through a reader lane or expose a
    storage handle outside the API-neutral service
  - Deterministic saturation, cancellation, poisoning, shutdown, and reader-progress tests proving
    that a full command lane neither starves reads nor creates a second writer
  - Same-run 32/128-client evidence attributing any remaining ceiling beyond port admission
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-service --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-server --test p1_riffdbd_restart --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Slow commands cannot occupy read-lane capacity, every lane remains bounded and drains
    deterministically, and the sole coordinator is still the only authoritative mutation owner.
  closure:
    status: complete
    evidence: docs/performance/wp-449-473-closure.md
    decision: Current production composition uses an independently bounded read-port driver and
      sole command coordinator, with deterministic drain and no second mutation owner.
  human_review_triggers: *id001
- id: WP-451
  name: Compiler-proved commutative child-append grouping
  depends_on: [WP-450]
  objective: Increase safe durable group fill for exact-key-disjoint child appends sharing one hot
    aggregate without delaying lone commands, bypassing barriers, weakening invariants, or changing
    FIFO order.
  requirements: [PERF-004, PERF-005, PERF-006, PERF-008]
  required_adrs: [ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0094]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-conflict/**,
    crates/riffdb-contract-ir/**, crates/riffdb-commit/**, crates/riffdb-observability/**,
    crates/riffdb-server/**, tests/command_semantics/**, tests/observability/**,
    examples/app-baseline/**, benchmarks/run-app-baseline, docs/performance/**]
  deliverables:
  - Bounded scheduler evidence distinguishing intake group size, compatible completion groups,
    strict conflict cuts, proved shared-key groups, hard barriers, byte/count limits, and oldest-item
    age
  - An ephemeral compiler proof limited to input-keyed child creates with no root/existing-row
    mutation, uniqueness, requirement, root/range validation, commit invariant, or unclassified
    dependency
  - One sealed group-owned union conflict capability retained through commit or proven rollback,
    while FIFO order, exact bidirectional compatibility, and independent command semantics remain
    unchanged
  - Deterministic adversarial schedules for same/different child keys, root mutation, uniqueness,
    invariants, cancellation, retry, fencing, barriers, crash recovery, and shutdown
  - Measured group-fill and mutation-throughput improvement under isolated, uniform, and Zipfian
    child-append workloads
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-ir --all-features
  - cargo +1.97.0 test -p riffdb-conflict --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - ./benchmarks/run-app-baseline --self-test
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Proved disjoint child appends sharing one hot aggregate co-commit and remain equivalent
    to FIFO serial execution; every unproved or overlapping command remains strictly serialized,
    barriers never move, and unary latency does not regain a timer park.
  closure:
    status: complete
    evidence: docs/performance/wp-451-commutative-child-appends.md
    decision: Compiler-proved child-append grouping shipped in d7b897ba; the implementation found
      and fixed an unproved-member admission bug and retains deterministic regression coverage.
  human_review_triggers: *id001
- id: WP-452
  name: Authoritative write-amplification budget
  depends_on: [WP-449]
  objective: Attribute and reduce the observed redb kernel-write amplification per successful
    command without deleting required durable facts, weakening recovery, changing compatibility
    bytes silently, or replacing redb.
  requirements: [PERF-008, PERF-009, PERF-010, PERF-011, PERF-012]
  required_adrs: [ADR-0004, ADR-0021, ADR-0061, ADR-0083, ADR-0085]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-commit/**, crates/riffdb-observability/**,
    benchmarks/command-growth/**, examples/app-baseline/**, fixtures/proto/**,
    benchmarks/run-app-baseline, scripts/benchmark-command-growth, docs/performance/**]
  deliverables:
  - Per-table logical row/byte and redb physical-write attribution for representative commands and
    retained database sizes
  - Evidence-backed removal of redundant hot-path reads, encodes, table opens, page rewrites, or
    records only where the same durable proof remains available
  - Explicit page-size/cache/layout experiments kept non-production until crash, reopen, migration,
    and full compatibility evidence justify a selected setting
  - A fixed write-amplification report and regression budget normalized per successful command
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - ./scripts/benchmark-command-growth --assert-perf-008
  - ./scripts/check-generated
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The dominant physical writes are named by table and cause, selected reductions retain
    byte compatibility or a reviewed migration, and crash/reopen proves the complete command graph.
  closure:
    status: complete
    evidence: docs/performance/wp-452-write-amplification.md
    decision: The table-by-table write budget shipped in 972aa7cb; no unreviewed page, cache, or
      durable-layout experiment was activated.
  human_review_triggers: *id001
- id: WP-453
  name: Serial physical micro-batch decision and prototype
  depends_on: [WP-451, WP-452]
  objective: Decide from WP-449 through WP-452 evidence whether compatible-only grouping can satisfy
    PERF-008; if not, prototype a bounded transaction-local serial command batch without releasing
    partial state, acknowledgement, or authority.
  requirements: [PERF-004, PERF-005, PERF-008, PERF-009, PERF-010]
  required_adrs: [ADR-0004, ADR-0005, ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0094,
    ADR-0095, ADR-0096]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-conflict/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-runtime/**, crates/riffdb-observability/**,
    tests/command_semantics/**, tests/recovery/**, benchmarks/command-growth/**,
    examples/app-baseline/**, fixtures/proto/**, docs/performance/**]
  deliverables:
  - A machine-readable proceed/defer decision tied to fresh saturation evidence and the unchanged
    PERF-008 gate
  - If proceeding, an accepted ADR defining transaction-local revalidation, per-conflict-key serial
    visibility, independent command outcomes and identities, all-or-absent physical recovery,
    sequence order, capacity bounds, cancellation, and unknown-commit resolution
  - A memory-backend semantic prototype before redb activation, with deterministic schedules proving
    equivalence to separately committed FIFO commands for every supported overlap
  - No non-durable visible redb commit chain and no acknowledgement before one complete durable tail
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test --test command_concurrency --all-features
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The evidence either proves compatible grouping sufficient and records a defer decision,
    or the accepted prototype is observationally equivalent to sequential commands, exposes no
    partial state, and retains every fail-closed boundary before redb production activation.
  closure:
    status: complete
    evidence: docs/performance/wp-453-decision-v1.json
    decision: The proceed decision, accepted ADR-0095, memory semantics, and bounded FIFO serial
      implementation shipped without partial visibility or premature acknowledgement.
  human_review_triggers: *id001
- id: WP-454
  name: Renewed mixed-load PostgreSQL parity evidence
  depends_on: [WP-450, WP-451, WP-452, WP-453]
  objective: Re-run the complete fresh-history, read-only, write-only, mixed, contention, history,
    crash, and language matrix and publish alpha eligibility only under the unchanged PERF-008 gate.
  requirements: [PERF-001, PERF-008, PERF-013, PERF-014, AAA-012]
  required_adrs: [ADR-0056, ADR-0060, ADR-0061]
  allowed_paths: [SPEC.md, work_packages.yaml, examples/app-baseline/**, benchmarks/**,
    fixtures/performance/**, release/evidence/**, docs/performance/**]
  deliverables:
  - Same-run PostgreSQL minimal and safe-app comparisons at 1/8/32/128 clients from fresh identical
    seeds, with read-only, write-only, and interactive mixed profiles reported separately
  - Retained-history and contention curves labeled as distinct questions, never substituted for the
    fresh parity gate
  - Complete semantic reconciliation, crash recovery, public-language conformance, and resource
    attribution retained with every result
  - Immutable eligible or not-eligible decision with no threshold waiver
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./benchmarks/run-app-baseline --full --assert-all-parity --reps 3 --require-stable
  - ./scripts/benchmark-command-growth --assert-perf-008
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every PERF-008 threshold passes from fresh same-run evidence and all semantic, crash,
    compatibility, documentation, and generated-artifact checks remain green; otherwise alpha stays
    blocked with every miss retained.
  closure:
    status: complete_failed_performance_gate
    evidence: docs/performance/wp-449-473-closure.md
    decision: The renewed matrix completed and honestly failed the then-current parity threshold,
      leaving alpha blocked. Later PERF-018 packages own current qualification; no pass is inferred.
  human_review_triggers: *id001
- id: WP-455
  name: Columnar catch-up publication amplification
  depends_on: [WP-432, WP-453]
  objective: Remove unobservable whole-delta snapshot clones from columnar catch-up without
    weakening commit-atomic visibility, monotonic frontiers, supersession holdback, freshness
    notification, or checkpoint recovery semantics.
  requirements: [PERF-008]
  required_adrs: [ADR-0072, ADR-0086, ADR-0087]
  allowed_paths: [work_packages.yaml, crates/riffdb-columnar/**, crates/riffdb-server/src/columnar_worker.rs,
    examples/app-baseline/**, docs/performance/**]
  deliverables:
  - A deterministic publication observer test proving one catch-up pass does not materialize
    intermediate snapshots that cannot escape the engine lock
  - A supersession schedule proving the maximal fully applied prefix remains publishable while a
    later commit is held back and the complete frontier becomes visible after the superseder arrives
  - One bounded snapshot materialization per observable publication boundary, with unchanged
    projected query results and checkpoint/reopen behavior
  - Before/after CPU attribution and full-seed evidence naming the remaining authoritative and
    derived-state costs
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-columnar --all-features
  - cargo +1.97.0 test -p riffdb-server --lib
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Catch-up publishes the same latest race-free result and frontier with no reader-visible
    partial commit, holdback never overclaims state, checkpoint/reopen remains equivalent, and the
    full-seed run shows no regression.
  closure:
    status: complete
    evidence: docs/performance/wp-455-columnar-publication.md
    decision: Single-observable-boundary columnar publication shipped in 2fd9581d with holdback,
      frontier, checkpoint/reopen, and no-regression coverage.
  human_review_triggers: *id001
- id: WP-456
  name: Dynamic physical groups under a larger static safety ceiling
  depends_on: [WP-453, WP-455]
  objective: Raise the internal authoritative transaction count ceiling from 64 to 256 while
    retaining dynamic FIFO prefix selection, the unchanged 16 MiB byte ceiling, the existing
    oldest-command deadline, independent command semantics, and every fail-closed recovery bound.
  requirements: [PERF-004, PERF-005, PERF-006, PERF-008, PERF-009, PERF-010]
  required_adrs: [ADR-0004, ADR-0005, ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0094,
    ADR-0095, ADR-0096]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**, crates/riffdb-commit/**,
    crates/riffdb-observability/**, crates/riffdb-server/**, tests/command_semantics/**,
    tests/recovery/**, benchmarks/command-growth/**, examples/app-baseline/**,
    benchmarks/run-app-baseline, docs/performance/**]
  deliverables:
  - Accepted amendments defining 256 as a static safety ceiling and dynamic actual cardinality from
    1 through 256 without extending the 200-microsecond oldest-command deadline
  - Count, 16 MiB byte, FIFO, hard-barrier, cancellation, shutdown, and queue-capacity tests at the
    exact new boundary and one above it
  - Memory and redb all-or-absent recovery plus independent unknown-commit classification for the
    enlarged bounded group
  - Fixed-cardinality 1-through-256 completion telemetry and benchmark parsing with no
    high-cardinality labels
  - Full-seed concurrency sweep recording effective group distributions, physical commits,
    staging, flush, latency, and the sub-3-second decision
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-observability --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --test command_concurrency --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Actual groups remain dynamically bounded by ready FIFO work, bytes, eligibility,
    barriers, and the oldest deadline; exact-bound recovery is all-or-absent; unary latency does not
    regress; and same-run seed evidence determines whether the enlarged ceiling reaches 3 seconds.
  closure:
    status: complete
    evidence: docs/performance/wp-456-dynamic-group-ceiling.md
    decision: Dynamic grouping under the fixed 256 ceiling shipped in 5424b5a0; safety inputs and
      all-or-absent recovery remain authoritative, with the measured result retained honestly.
  human_review_triggers: *id001
- id: WP-457
  name: Bounded generated-batch writer feeding
  depends_on: [WP-320, WP-398, WP-456]
  objective: Evaluate 192- and 256-item generated-batch concurrency candidates, retain the fastest
    safe public ceiling, and size the coordinator queue for two maximum physical groups without
    changing the 16-item transport request, unary scheduling, durability, or independent command
    semantics.
  requirements: [AAA-006, AAA-007, AAA-010, PYD-005, PYD-012, PERF-004, PERF-005, PERF-006,
    PERF-008, PERF-009, PERF-010]
  required_adrs: [ADR-0056, ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0071, ADR-0084,
    ADR-0095, ADR-0096, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-client-rust/**,
    crates/riffdb-query-module/**, crates/riffdb-server/src/process_graph.rs,
    clients/python/runtime/**, clients/python/ticketdesk/**, clients/typescript/ticketdesk/**,
    examples/ticketdesk/generated/**, examples/ticketdesk/web/src/generated/**,
    examples/ticketdesk/riffdb.application.lock.json,
    examples/agent-alpha/**/generated/typescript/client.ts,
    examples/agent-alpha/web/src/generated/client.ts,
    scripts/generate-query-clients,
    examples/app-baseline/**, benchmarks/run-app-baseline, fixtures/application-parity/**,
    docs/getting-started/COMMAND-BATCHES.md, docs/sdks/RUST.md, docs/python-driver.md,
    docs/getting-started/TYPESCRIPT-APPLICATIONS.md, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Exact shared Rust, TypeScript, and Python generated-batch concurrency range selected by the
    measured retain-or-revert gate, with zero and one above the maximum rejected before submission
    and no change to the 4,096-item collection bound
  - Unchanged maximum 16 ordinary commands per ExecuteBatch request; the retained 128 concurrency
    maps to at most eight simultaneous transport requests and every item retains independent
    identity, result, retry, progress, checkpoint, cancellation, and uncertainty semantics
  - Production coordinator admission default of 512 messages, independently byte-bounded, so two
    maximum physical groups may wait while one commits without making the physical group or queue
    bound runtime-configurable for application callers
  - Existing fixed-cardinality dispatch reason and group-size evidence used to distinguish queue
    starvation, count saturation, barriers, and compatibility splits without new identifiers
  - Full-seed sweep at 128, 192, and 256 requested concurrency recording overloads, group
    distribution, physical commits, flush/staging cost, seed duration, and unary mutation p50
  - Retain-or-revert decision recording why 128 remains public after the 192/256 candidates reduced
    commit count but regressed total seed or unary time; never weaken durability to meet the target
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-client-rust --all-features
  - cargo +1.97.0 test -p riffdb-query-module --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - TMPDIR=/home/kevin/tmp PYTHON=python3 ./scripts/check-python-runtime
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Generated clients reject one above the shared retained bound before submission, transport
    requests remain at most 16 items, the coordinator remains independently count/byte bounded,
    the 128/192/256 sweep has no overload or semantic failure, unary p50 has no material regression,
    and the evidence records whether the full seed reaches 3 seconds.
  closure:
    status: complete
    evidence: docs/performance/wp-457-writer-feeding.md
    decision: The first bounded writer-feeding sweep shipped in a7009339 and retained its measured
      knee rather than raising concurrency without evidence.
  human_review_triggers: *id001
- id: WP-458
  name: Shared immutable command intent graph
  depends_on: [WP-378, WP-457]
  objective: Remove residual deep clones of the exact pre-evaluation context and commit intent
    across the command typestate pipeline by sharing their immutable checked graphs, without
    changing validation, ordering, authorization, durable bytes, storage staging, or durability.
  requirements: [PERF-008]
  required_adrs: [ADR-0059, ADR-0060, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**, crates/riffdb-commit/**,
    tests/command_semantics/**, tests/recovery/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Arc-backed immutable ownership for PreEvaluationCommitContext and CommitIntent, with clones
    proven to share one checked allocation rather than duplicate pending identity, conflict hashes,
    evaluated graph, or admission expectation
  - Unchanged semantic equality and accessor behavior at every admission, validation, reservation,
    sequence assignment, record construction, memory staging, and redb staging boundary
  - Existing exact candidate/write-plan/record-graph validation retained, with no trusted bypass,
    no durable-format change, and byte-identical generated records
  - Before/after full public seed evidence at retained concurrency 128; retain only if total seed,
    validation/encoding/staging, and representative unary latency do not materially regress
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --test lost_response_replay
      --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Command-context and intent clones share immutable checked allocations; every existing
    semantic, recovery, byte-compatibility, and backend test passes; and the full public seed shows
    no material total, staging, or unary regression against the WP-457 retained point.
  closure:
    status: complete
    evidence: docs/performance/wp-458-shared-command-intent.md
    decision: Shared immutable command contexts and intents shipped in 3d595eb8 with exact semantic,
      byte, recovery, and retain-or-revert evidence.
  human_review_triggers: *id001
- id: WP-459
  name: Consume staged command graphs after physical staging
  depends_on: [WP-452, WP-458]
  objective: Stop retaining and repeatedly deep-cloning the complete authoritative command graph
    after a backend has physically staged it by consuming the graph into minimal immutable commit
    evidence, without moving validation after commit or changing durable bytes, transaction
    ordering, authorization, recovery, or acknowledgement durability.
  requirements: [PERF-008]
  required_adrs: [ADR-0059, ADR-0060, ADR-0095, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**, crates/riffdb-commit/**,
    tests/command_semantics/**, tests/recovery/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - A consuming AtomicCommandRecordSet boundary that yields only the already-validated terminal
    outcome and ordered event identities needed after physical staging
  - Memory and redb batches retain minimal staged evidence instead of complete entity, index,
    event, outbox, provenance, commit, and write-plan graphs after their writes are applied
  - CommittedBatchV1 remains completely constructed and validated before the durable commit, while
    grouped integrity comparison borrows retained expected outcomes without a second clone vector
  - Unchanged durable codecs, backend writes, transaction-local reads, uncertain-commit evidence,
    replay behavior, and all-or-absent recovery
  - Before/after full public seed evidence at retained concurrency 128; retain only if total seed,
    validation/encoding/staging, and representative unary latency do not materially regress
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --test lost_response_replay
      --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Backends retain only minimal post-staging evidence, grouped comparison creates no
    cloned expected-outcome vector, all semantic, recovery, and byte-compatibility suites pass, and
    the full public seed shows no material total, staging, or unary regression against WP-458.
  closure:
    status: complete
    evidence: docs/performance/wp-459-consumed-staged-graphs.md
    decision: Minimal post-staging evidence ownership shipped in ce309311 with exact grouped
      outcomes and a retained paired improvement.
  human_review_triggers: *id001
- id: WP-460
  name: Allocation-free reciprocal record validation
  depends_on: [WP-452, WP-459]
  objective: Remove bounded but repeated temporary-vector allocation from complete command-record
    reciprocal validation and post-staging evidence extraction without weakening any comparison,
    changing durable bytes, transaction ordering, authorization, recovery, or acknowledgement
    durability.
  requirements: [PERF-008]
  required_adrs: [ADR-0060, ADR-0095, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    benchmarks/run-app-baseline, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Outbox, affected-entity, and event-identity reciprocal checks compare their existing bounded
    sequences directly without materializing comparison vectors
  - Post-staging evidence takes ownership of the exact event-identity vector that already passed
    provenance and commit validation instead of allocating and copying another vector
  - Tests prove post-staging event-identity storage preserves the checked allocation and that every
    existing semantic rejection remains fail closed
  - Before/after full public seed evidence at retained concurrency 128; retain only if total seed,
    validation/encoding/staging, and representative unary latency do not materially regress
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Complete reciprocal validation remains semantically identical while allocating no
    temporary comparison vectors, post-staging evidence reuses the checked provenance event-ID
    allocation, all storage semantic and codec tests pass, and the public seed does not regress.
  closure:
    status: complete
    evidence: docs/performance/wp-460-allocation-free-validation.md
    decision: Allocation-free reciprocal validation shipped in cb09094d; all mismatch, codec,
      storage, and public no-regression checks remain intact.
  human_review_triggers: *id001
- id: WP-461
  name: Single-owner staged event collection
  depends_on: [WP-452, WP-459, WP-460]
  objective: Remove the redundant complete durable-event collection retained beside the exact
    same collection in each command's authoritative commit record without weakening reciprocal
    validation, changing durable bytes, transaction ordering, authorization, recovery, or
    acknowledgement durability.
  requirements: [PERF-008]
  required_adrs: [ADR-0060, ADR-0083, ADR-0095, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-commit/**, tests/storage_recovery/**,
    benchmarks/run-app-baseline, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Atomic command graphs expose the complete durable events through their checked commit record
    rather than retaining a second full collection
  - Construction still proves event derivation, commit membership, outbox equality, provenance
    identities, canonical ordering, and all aggregate bounds before storage
  - Tests prove the graph and commit expose the same event allocation and every existing malformed
    or mismatched graph remains rejected
  - Before/after full public seed evidence at retained concurrency 128; retain only if total seed,
    validation/encoding/staging, and representative unary latency do not materially regress
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One authoritative commit-owned durable-event collection supplies the complete staged
    graph, all reciprocal validation and recovery suites pass, durable bytes remain exact, and the
    public seed does not regress.
  closure:
    status: complete
    evidence: docs/performance/wp-461-single-owner-events.md
    decision: Commit-owned single-owner event storage shipped in 84b85ab0; the rejected Arc variant
      was not retained and current semantics/recovery stay exact.
  human_review_triggers: *id001
- id: WP-462
  name: Allocation-free sequence-free wire reservation
  depends_on: [WP-452, WP-458, WP-460, WP-461]
  objective: Remove intermediate length-vector allocations from the per-command sequence-free
    Protobuf reservation pass without weakening the conservative bound, exact final encoding
    comparison, durable bytes, transaction ordering, authorization, recovery, or acknowledgement
    durability.
  requirements: [PERF-008]
  required_adrs: [ADR-0006, ADR-0061, ADR-0095, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    benchmarks/run-app-baseline, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Sequence-free entity, event, route, conflict, provenance, and commit length accounting folds
    directly over the already bounded semantic collections
  - The reservation remains conservative for maximum-width future sequences and versions, and
    the final encoder still rejects any exact charge above the reservation
  - Architecture and boundary tests freeze allocation-free sizing and equal/one-over behavior
  - Before/after full public seed evidence at retained concurrency 128; retain only if total seed,
    validation/encoding/staging, and representative unary latency do not materially regress
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The hot sequence-free reservation pass retains no intermediate length vectors, every
    existing bound and codec test passes unchanged, and the full public seed does not regress.
  closure:
    status: complete
    evidence: docs/performance/wp-462-allocation-free-wire-reservation.md
    decision: Direct bounded wire reservation shipped in 4bf20cc3 with one-over-limit, exact-bound,
      codec, and architecture tests unchanged.
  human_review_triggers: *id001
- id: WP-463
  name: Allocation-free read-dependency reciprocity
  depends_on: [WP-452, WP-460, WP-461, WP-462]
  objective: Compare commit-owned durable read dependencies directly with the already canonical
    live dependency set during complete command-graph validation, removing a redundant bounded
    clone, sort, and target-key allocation pass without weakening transaction-current validation.
  requirements: [PERF-008]
  required_adrs: [ADR-0006, ADR-0061, ADR-0095, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    benchmarks/run-app-baseline, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Durable dependency sets expose an exact allocation-free comparison with canonical live entity
    and index-range dependencies, including target bytes and expected state
  - Complete command-record construction uses that comparison rather than reconstructing, sorting,
    and comparing a second durable dependency set
  - Semantic tests cover both dependency kinds, target and expected-state mismatches, canonical
    insertion-order independence, and length mismatches; architecture tests freeze the
    allocation-free production path
  - Before/after full public seed evidence at retained concurrency 128; retain only if total seed,
    validation/encoding/staging, and representative unary latency do not materially regress
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Complete command-record validation retains exact durable/live dependency reciprocity
    without constructing a comparison collection, every mismatch remains fail closed, durable
    bytes and recovery remain unchanged, and the full public seed does not regress.
  closure:
    status: complete
    evidence: docs/performance/wp-463-allocation-free-read-dependencies.md
    decision: Direct canonical dependency reciprocity shipped in caa60a31 with every mismatch still
      fail closed and durable/recovery behavior unchanged.
  human_review_triggers: *id001
- id: WP-464
  name: Bounded authenticated batch-ingress coalescing
  depends_on: [WP-457, WP-463]
  objective: Admit and authenticate one public ExecuteBatch envelope once, then construct
    independent per-item request contexts and poll the ordinary API-neutral command futures as one
    bounded collection, reducing writer-arrival fragmentation without sharing authorization,
    changing coordinator order, or adding batch transaction semantics.
  requirements: [PERF-001, PERF-004, PERF-006, PERF-008, AAA-006, AAA-007]
  required_adrs: [ADR-0056, ADR-0058, ADR-0060, ADR-0061, ADR-0071, ADR-0084, ADR-0095,
    ADR-0096, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-api-grpc/**, tests/grpc/**,
    benchmarks/run-app-baseline, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - One bounded ExecuteBatch envelope resolves its database lifecycle route, checked transport
    security context, deadline, and authenticated principal exactly once before any item begins
  - Every item receives an independent request ID, request control and cancellation guard and
    continues through the ordinary API-neutral ExecuteCommand service with every current-policy,
    response-release, coordinator, idempotency, audit, provenance, and recovery safe point intact
  - The adapter polls the bounded item futures directly and no longer allocates one outer Tokio
    task and abort handle per item; dropping the RPC still cancels every unfinished item
  - End-to-end tests prove one authentication per transport batch, input-ordered independent
    results, per-item typed errors, and unchanged all-success legacy mirroring; architecture tests
    freeze the no-bypass and no-outer-task path
  - Before/after full public seed evidence at retained concurrency 128 records total seed, physical
    group distribution, validation/encoding/staging, and representative unary latency; retain only
    if correctness remains exact and total or grouping materially improves without unary regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-api-grpc --all-features
  - cargo +1.97.0 test --test grpc_end_to_end execute_batch --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: ExecuteBatch authenticates once while every item remains an ordinary independently
    authorized command, cancellation and per-item carriage remain exact, no outer Tokio item task
    remains, durable and public bytes are unchanged, and the retained full seed does not regress.
  closure:
    status: complete
    evidence: docs/performance/wp-464-bounded-batch-ingress.md
    decision: Bounded batch ingress shipped in 206a24fe with one envelope authentication and
      independent per-command authorization, identity, cancellation, and result semantics.
  human_review_triggers: *id001
- id: WP-465
  name: Shared immutable capability grant collections
  depends_on: [WP-378, WP-464]
  objective: Remove repeated deep cloning of already canonical and validated capability permission,
    field-visibility, and approval collections across authentication and authorization handoffs by
    sharing their immutable storage, without changing public construction, bounded validation,
    policy decisions, semantic bytes, durable encoding, or fail-closed behavior.
  requirements: [PERF-008, AAA-006, AAA-007]
  required_adrs: [ADR-0009, ADR-0060, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-types/**,
    benchmarks/run-app-baseline, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Arc-backed immutable storage for canonical permission atoms, per-entity visible fields,
    field-visibility entries, and approval-required permission kinds after all existing bounded
    sorting, duplicate rejection, subset validation, and payload-size validation complete
  - Unchanged public constructors and slice accessors, value equality, canonical order,
    semantic-byte output, capability codecs, authentication, authorization, and redaction behavior
  - Unit tests prove clones share the checked backing allocations while independently constructed
    equal grants remain value-equal and emit byte-identical semantic encodings
  - Before/after full public seed evidence at retained concurrency 128; retain only if authorization
    CPU and total seed do not materially regress, physical grouping remains bounded and equivalent,
    and representative unary latency does not materially regress
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types --all-features
  - cargo +1.97.0 test -p riffdb-auth --all-features
  - cargo +1.97.0 test -p riffdb-policy --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Capability-grant clones share only immutable already-validated collections; canonical
    bytes, codecs, value equality, authorization, and redaction remain exact; all type, auth, and
    policy suites pass; and the retained public seed shows no material total or unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-449-473-closure.md
    decision: Immutable validated capability collections shipped in 5d1abadd; current type, auth,
      policy, codec, equality, and redaction suites remain green.
  human_review_triggers: *id001
- id: WP-466
  name: Redb physical commit amplification
  depends_on: [WP-452, WP-456, WP-457, WP-464]
  objective: Reduce redb page, table, encoding, and flush work for an already-maximal physical
    command commit without weakening acknowledgement durability, atomic command graphs, recovery,
    read visibility, or the hardened oracle; retain bounded durability epochs only for evidence
    containing multiple already-accepted physical subgroups.
  requirements: [PERF-004, PERF-005, PERF-007, PERF-008, PERF-009, PERF-010, PERF-015,
    REC-002, TXN-040, TXN-041, TXN-042, TXN-043, TXN-044]
  required_adrs: [ADR-0058, ADR-0060, ADR-0061, ADR-0095, ADR-0096, ADR-0097,
    ADR-0098]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-storage-memory/**, crates/riffdb-commit/**,
    crates/riffdb-server/**, tests/storage_recovery/**, tests/server_composition/**,
    benchmarks/command-growth/**, benchmarks/run-app-baseline, docs/SUMMARY.md,
    docs/performance/**]
  deliverables:
  - A checked engine harness measures staging-order candidates, physical database growth, per-table
    tree shape, deferred-tail mechanics, staging time, and durable commit time on the complete
    synthetic command graph; unavailable redb production knobs are reported rather than forked
  - Production changes reduce dirty-page or encoded-record work without changing durable keys or
    values incompatibly; any durable format proposal remains a separately reviewed follow-up
  - Standard and hardened profiles retain their existing Immediate acknowledgement and recovery
    semantics; an idle singleton receives no speculative group delay
  - A completion-edge window opens only after a busy unit leaves 2-32 commands queued, remains
    fixed at 2 ms, never delays a singleton, already-amortized larger prefix, or barrier, and first
    attempts one larger atomic Immediate transaction; deferred epochs remain a follow-up for
    unavoidable subgroup splits
  - Compatibility, reopen, crash, idempotency, audit, provenance, event, outbox, and query-visible
    entity/index tests cover the selected production optimization
  - Focused c1/c32/c128 public evidence records throughput, latency, physical subgroup count, durable
    commits, physical bytes, and correctness against the retained tri-backend baseline
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every semantic and recovery suite passes; the selected production change is format
    compatible, keeps standard and hardened durability exact, materially reduces measured physical
    commit work or c32 commit time, improves focused throughput, and causes no material c1 regression.
  closure:
    status: complete
    evidence: docs/performance/wp-466-durability-epochs.md
    decision: Contended completion coalescing shipped in b9a9c960 with exact standard/hardened
      semantics; deferred multi-group durability epochs remained disabled.
  human_review_triggers: *id001
- id: WP-467
  name: Last-durable frontier and unpublished command typestate
  depends_on: [WP-466]
  objective: Implement ADR-0098's storage safety boundary for bounded standard-profile durability
    epochs so an unfenced redb root is neither readable nor representable as a committed application
    result, without yet widening the coordinator's epoch-selection policy.
  requirements: [PERF-004, PERF-007, PERF-009, PERF-015, REC-002, TXN-040, TXN-041,
    TXN-042, TXN-043, TXN-044]
  required_adrs: [ADR-0058, ADR-0060, ADR-0061, ADR-0095, ADR-0096, ADR-0097,
    ADR-0098]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0098-bounded-redb-durability-epochs.md,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-storage-memory/**, tests/storage_recovery/**,
    benchmarks/command-growth/**, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - A closed unpublished-applied result type is distinct from AuditedCommittedBatchV1 and exposes
    no public acknowledgement, notification, transient-index, or committed-outcome conversion
  - One standard-profile redb epoch owns the mutation lease from its first deferred subgroup through
    its Immediate tail, while hardened storage and every ordinary write retain their current path
  - Operational and derived reads use the predecessor last-durable snapshot for the whole epoch;
    writer-private subgroup transactions alone may observe prior deferred roots
  - The known-successful Immediate tail atomically publishes the successor read frontier before
    returning releasable committed results and accumulated transient-index deltas
  - Tail failure retains the predecessor frontier, invalidates unpublished accelerators, fences
    writes, and returns no committed result
  - Storage and process-recovery tests prove before-tail invisibility, after-tail visibility,
    complete-or-absent reopen, independent command identity, and hardened-profile rejection
  - Mechanics evidence records deferred subgroup work, tail-fence work, read-frontier publication,
    physical commit count, and database growth without claiming coordinator-path availability
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./scripts/benchmark-command-growth --smoke
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Unpublished redb roots are visible only to the epoch writer, cannot become committed
    application results before a known-successful Immediate tail, remain invisible to every reader
    and derived worker, and recover as a complete fenced prefix or the preceding durable frontier.
  closure:
    status: complete
    evidence: docs/performance/wp-467-last-durable-frontier.md
    decision: Last-durable frontier and unpublished typestate shipped in 9196b7ac; recovery and
      visibility retain the preceding-or-complete fenced-prefix rule.
  human_review_triggers: *id001
- id: WP-468
  name: Revisited bounded generated-batch writer feeding
  depends_on: [WP-457, WP-458, WP-459, WP-460, WP-461, WP-462, WP-463, WP-464,
    WP-466]
  objective: Re-evaluate and retain a larger cross-language generated-command concurrency ceiling
    after the intervening command-CPU reductions, reaching the three-second full public seed target
    without changing transport batch size, coordinator/storage bounds, or independent command
    semantics.
  requirements: [AAA-006, AAA-007, AAA-010, PYD-005, PYD-012, PERF-004, PERF-005,
    PERF-006, PERF-008, PERF-009, PERF-010]
  required_adrs: [ADR-0056, ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0071,
    ADR-0084, ADR-0095, ADR-0096, ADR-0097]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-client-rust/**,
    crates/riffdb-query-module/**, crates/riffdb-cli/**, clients/python/runtime/**,
    clients/python/ticketdesk/**, clients/typescript/ticketdesk/**,
    examples/ticketdesk/generated/**, examples/ticketdesk/web/src/generated/**,
    examples/ticketdesk/riffdb.application.lock.json,
    examples/agent-alpha/**/generated/typescript/client.ts,
    examples/agent-alpha/web/src/generated/client.ts, scripts/generate-query-clients,
    examples/app-baseline/**, benchmarks/run-app-baseline, fixtures/application-parity/**,
    fixtures/application-locks/ticketdesk-migration-v4.json,
    docs/getting-started/COMMAND-BATCHES.md, docs/sdks/RUST.md, docs/python-driver.md,
    docs/getting-started/TYPESCRIPT-APPLICATIONS.md, docs/SUMMARY.md,
    docs/performance/wp-457-writer-feeding.md, docs/performance/**]
  deliverables:
  - One shared generated-command concurrency range of 1 through 384 for Rust, TypeScript, and
    Python, with zero and 385 rejected before submission and the 4,096-item collection bound
    unchanged
  - Each Rust transport exchange remains capped at 16 ordinary commands, so concurrency 384 uses
    at most 24 simultaneous exchanges and never creates a public batch transaction
  - Every item retains independent authorization, idempotency identity, typed outcome, retries,
    cancellation, progress, checkpoint, uncertainty classification, and response release
  - The coordinator remains independently bounded at 512 queued messages and 32 MiB, while each
    physical transaction remains dynamically selected under the 256-command and 16 MiB ceilings
  - Generated TypeScript fixtures, Python runtime tests, Rust tests, and public handbook pages agree
    on the exact shared bound and transport shape
  - Same-run full public seed evidence records the 128, 256, and 384 candidates, group distribution,
    physical commits, staging and flush cost, representative unary latency, and a retained median
    below three seconds without overload or semantic failure
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-client-rust --all-features
  - cargo +1.97.0 test -p riffdb-query-module --all-features
  - cargo +1.97.0 test -p riffdb-cli --all-features
  - TMPDIR=/home/kevin/tmp PYTHON=python3 ./scripts/check-python-runtime
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: All three generated-language paths reject one above the shared bound before work,
    transport and coordinator safety bounds are unchanged, every semantic and recovery suite stays
    exact, and retained full-seed evidence is below three seconds with no material unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-468-revisited-writer-feeding.md
    decision: The evidence-driven generated batch increase shipped in 1de932d6 after intervening CPU
      reductions, preserving all transport/coordinator bounds and clearing the engineering seed target.
  human_review_triggers: *id001
- id: WP-469
  name: Fenced single-subgroup standard commit
  depends_on: [WP-452, WP-459, WP-466, WP-467, WP-468]
  objective: Route one already-selected standard-profile physical command group containing at
    least two commands through ADR-0098's unpublished-root typestate and immediate empty tail,
    reducing redb Immediate commit work without changing logical transaction grouping, FIFO order,
    acknowledgement durability, read visibility, independent command semantics, or the hardened
    recovery oracle.
  requirements: [PERF-004, PERF-005, PERF-007, PERF-008, PERF-009, PERF-010,
    PERF-015, REC-002, TXN-040, TXN-041, TXN-042, TXN-043, TXN-044]
  required_adrs: [ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0095, ADR-0096,
    ADR-0097, ADR-0098]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0098-bounded-redb-durability-epochs.md,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-commit/**, crates/riffdb-server/**,
    tests/command_semantics/**, tests/recovery/**, tests/storage_recovery/**,
    benchmarks/command-growth/**, benchmarks/run-app-baseline,
    examples/app-baseline/**, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Standard-profile compatible and transaction-local serial groups containing 2 through 256
    commands apply their already-validated complete graph once through a private non-durable redb
    root followed immediately by one empty Immediate tail fence
  - Idle or contended singleton commands retain the direct one-phase Immediate path with no new
    wait, transaction, or timer; hardened Sync groups retain direct two-phase Immediate commits
  - Operational and derived reads, command preparation, transient indexes, notifications, and
    public responses remain pinned to the predecessor durable frontier until the tail is known
    successful and the successor frontier is atomically published
  - Each command retains independent identity, sequence, outcome, audit, provenance, event,
    idempotency, retry, and uncertainty resolution; the optimization is not a public batch
    transaction and does not merge incompatible logical groups
  - Any unpublished-apply or tail error releases no result, retains the predecessor frontier,
    invalidates unpublished accelerators, and fences authoritative writes for bounded exact
    identity resolution
  - Memory semantic coverage provides the same unpublished/publication typestate without adding a
    volatile durability claim; redb crash tests cover before-tail, during-tail, and after-tail
    response-loss boundaries
  - Engine mechanics and focused c1/c32/c128 plus write-only evidence record direct versus fenced
    subgroup cost, throughput, write latency, physical transactions, correctness, and unary guardrails
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-commit --test command_concurrency --test lost_response_replay
      --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./scripts/benchmark-command-growth --smoke
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Non-singleton standard groups use exactly one unpublished application root and one
    known-successful tail before release; singleton and hardened paths remain direct; every semantic,
    crash, recovery, and compatibility suite passes; focused evidence materially reduces physical
    commit cost or write latency without a material c1 regression.
  closure:
    status: complete
    evidence: docs/performance/wp-469-fenced-single-subgroup.md
    decision: The narrow fenced single-subgroup protocol shipped in 1780cdd9; multi-subgroup
      collection stayed deferred and current crash/recovery coverage remains exact.
  human_review_triggers: *id001
- id: WP-470
  name: Batched command-audit table access
  depends_on: [WP-364, WP-449, WP-464, WP-469]
  objective: Remove repeated redb table-open and handle-construction work while staging bounded
    audited admission and terminal command groups, without removing or weakening any lifecycle,
    linked commit/event/provenance, request-index, allocator, canonical-byte, atomicity, or
    result-release validation.
  requirements: [PERF-004, PERF-008, PERF-015, TXN-040, TXN-041, TXN-042,
    TXN-043, TXN-044]
  required_adrs: [ADR-0007, ADR-0009, ADR-0021, ADR-0058, ADR-0060, ADR-0098]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-redb/**,
    tests/service_audit/**, tests/command_semantics/**, benchmarks/run-app-baseline,
    docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - One bounded audited admission group opens AUDIT and AUDIT_BY_REQUEST once for lifecycle lookup
    while retaining an independent exact lookup and validation for every request identity
  - One bounded command terminal-audit group opens the commit, event, and provenance tables once and
    validates every Command link against those shared transaction-local handles
  - The same group opens AUDIT_BY_REQUEST once and inserts every independently encoded, self-verifying
    request-index row while retaining duplicate rejection and input order
  - Fused Started and terminal lifecycle validation, full commit-with-events decoding, provenance
    reciprocity, audit canonicalization, allocator validation, and atomic command-plus-audit commit
    remain exact and fail closed
  - Standalone service-audit operations retain their existing single-row helper and no public or
    durable format, operation registry, result, error, transaction, or acknowledgement changes
  - Focused service-audit tests and full public seed evidence record exact audit/index parity,
    validation/encoding/staging, commit time, total seed, and representative unary latency
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --test command_concurrency --test lost_response_replay --all-features
  - cargo +1.97.0 test --test service_audit_orchestration --test service_audit_ordering --test service_audit_recovery --all-features
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every admission lifecycle, terminal link, and request-index row retains exact validation
    and durable bytes, all service-audit and recovery semantics pass, and retained full-seed evidence
    reduces audited command CPU or total seed without a material unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-470-batched-command-audit-tables.md
    decision: Batched command-audit table access shipped in af94a78a without caching or weakening
      authoritative rows, link validation, durable bytes, or recovery.
  human_review_triggers: *id001
- id: WP-471
  name: Revision-aware commit materialization
  depends_on: [WP-065, WP-070, WP-460, WP-470]
  objective: Eliminate the current commit hot path's speculative compatibility dispatch and deep
    clone of loaded durable events by carrying the already-decoded exact commit revision from
    event-reference discovery into final materialization, without changing any readable durable
    revision, canonical byte, event-table join, validation, corruption, or recovery behavior.
  requirements: [STO-002, PERF-008, TXN-041, TXN-042, TXN-044]
  required_adrs: [ADR-0004, ADR-0006, ADR-0009, ADR-0021, ADR-0058]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, tests/storage_recovery/**, docs/SUMMARY.md,
    docs/performance/**]
  deliverables:
  - Event-reference decoding returns one closed current-V3, historical-V2, or legacy-V1 revision
    witness together with the same ordered checked references and exact encoded charge
  - Redb loads every referenced authoritative event row once, preserves exact reference/hash
    comparison, and dispatches directly to the witnessed decoder while moving the loaded event
    collection exactly once
  - Legacy embedded-event commits still compare their complete decoded event collection against the
    separately authoritative event rows; V2 and V3 retain complete semantic construction
  - Unknown type, malformed envelope, checksum, schema, noncanonical bytes, malformed payload,
    missing event, mismatched reference/hash, and semantic invalidity remain fail-closed corruption
  - Current, V2, and legacy fixtures prove the revision witness and final decoded record are exactly
    equivalent to the existing compatibility decoder
  - Durable schemas, record registry, encoded bytes, storage keys, transaction ordering,
    acknowledgement, audit linking, and application/transport public interfaces remain unchanged;
    the engine-neutral codec adds only the closed first-party revision witness
  - Full public seed evidence records materialization/staging CPU, total seed, and representative
    unary latency, with revert required for a material regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: All three readable commit revisions retain exact semantic and compatibility behavior,
    every corruption and recovery suite passes, and same-host evidence reduces current commit
    materialization CPU or total seed without a material unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-471-revision-aware-commit.md
    decision: Revision-aware materialization shipped in d64030cc with exact V1/V2/V3 semantics,
      compatibility, corruption classification, and retained no-regression evidence.
  human_review_triggers: *id001
- id: WP-472
  name: Single-pass commit materialization
  depends_on: [WP-065, WP-070, WP-460, WP-471]
  objective: Retain the already canonical, revision-witnessed commit payload through the
    authoritative event-table join and finish complete semantic construction without decoding the
    same durable envelope and Protobuf payload a second time, while preserving every compatibility,
    canonicality, event-reference, semantic-record, corruption, and recovery check.
  requirements: [STO-002, PERF-008, TXN-041, TXN-042, TXN-044]
  required_adrs: [ADR-0004, ADR-0006, ADR-0009, ADR-0021, ADR-0058]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, tests/storage_recovery/**, docs/SUMMARY.md,
    docs/performance/**]
  deliverables:
  - The exact readable decode produces one move-only prepared current-V3, historical-V2, or
    legacy-V1 commit value plus its ordered checked event references and complete envelope charge
  - Prepared debug output exposes only the closed revision and bounded reference count, never
    payloads, event identities, hashes, actor values, or business data
  - Redb loads the same authoritative event rows in the same order, performs the same exact
    reference/hash comparisons, and consumes the prepared value plus loaded events once
  - V3 and V2 finish all existing field conversion and StoredCommitRecordV1 construction checks;
    legacy V1 retains its complete embedded-event equality comparison
  - Unknown type, malformed envelope, checksum, schema, noncanonical bytes, malformed payload,
    missing event, mismatched reference/hash, and semantic invalidity remain fail-closed corruption
  - Direct V3, V2, and legacy decoders remain available for compatibility tests and produce records
    exactly equal to prepared materialization with the same encoded charge
  - Durable schemas, record registry, bytes, keys, transaction ordering, acknowledgement, audit
    linking, and application/transport public interfaces remain unchanged
  - Full public seed evidence records validation/encoding/staging CPU, total seed, and representative
    unary latency, with revert required for a material regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every readable revision retains exact bytes, event-table authority, semantic result,
    corruption classification, and recovery behavior, while same-host evidence reduces current
    commit materialization CPU or total seed without a material unary regression.
  closure:
    status: complete
    evidence: docs/performance/wp-472-single-pass-commit.md
    decision: Prepared single-pass commit decode shipped in 39881e3f; exact bytes, event authority,
      corruption, recovery, and validation order remain pinned.
  human_review_triggers: *id001
- id: WP-473
  name: Sealed command-audit link evidence
  depends_on: [WP-364, WP-458, WP-459, WP-470, WP-472]
  objective: Reuse the move-only checked evidence produced by a complete command graph staged in
    the same redb transaction to validate its linked terminal service-audit row without re-reading
    and decoding that transaction's commit, event, and provenance rows, while retaining the full
    authoritative decoder for every independently submitted, recovered, migrated, or external link.
  requirements: [PERF-004, PERF-008, TXN-041, TXN-042, TXN-043, TXN-044]
  required_adrs: [ADR-0007, ADR-0009, ADR-0021, ADR-0058, ADR-0060]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, tests/service_audit/**, tests/command_semantics/**,
    tests/storage_recovery/**, docs/SUMMARY.md, docs/performance/**]
  deliverables:
  - Consuming one completely validated AtomicCommandRecordSet after successful physical staging
    retains its exact outcome-owned commit sequence and provenance identity as unforgeable
    move-only command-link evidence
  - The audited command commit path requires exactly one checked evidence item for every Command
    audit link, in command order, and rejects missing, extra, duplicate, reordered, or mismatched
    evidence before appending any audit row
  - The same-transaction sealed path performs no commit, event, or provenance table read/decode for
    terminal-link validation; it does not skip lifecycle, allocator, audit-tail, request-index,
    canonical audit encoding, or staged command-graph validation
  - Standalone service-audit and all independently supplied links retain complete commit-with-events
    and provenance decoding plus exact reciprocal identity checks
  - Unknown framing, malformed bytes, absent or mismatched authoritative rows, and recovery/startup
    validation remain fail closed; the sealed path is unavailable after process or transaction loss
  - Durable schemas, record bytes, keys, transaction membership, ordering, acknowledgement,
    uncertainty, and application/transport public interfaces remain unchanged
  - Full public seed evidence records validation/encoding/staging CPU, total seed, commit/fence time,
    and representative unary latency, with revert required for a material regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --test service_audit_orchestration --test service_audit_ordering
      --test service_audit_recovery --all-features
  - cargo +1.97.0 test --test command_concurrency --test lost_response_replay --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - ./benchmarks/run-app-baseline --self-test
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Same-transaction command audit links use only exact sealed staging evidence, every other
    link retains full authoritative decoding, all audit/command/recovery semantics pass, and
    same-host evidence reduces validation/staging CPU or total seed without a material unary
    regression.
  closure:
    status: complete
    evidence: docs/performance/wp-473-sealed-command-audit-links.md
    decision: Sealed same-transaction audit-link evidence shipped in b11909d0 while every external,
      recovered, and non-command link keeps full authoritative decoding.
  human_review_triggers: *id001
- id: WP-494
  name: Segmented independent command-audit link resolution
  depends_on: [WP-473, WP-479]
  objective: Restore replay of a known command outcome after ADR-0102 segmentation by resolving an
    independently supplied Command audit link through the canonical segment member instead of the
    pre-segmentation per-command commit and provenance rows, on both the audit append path and the
    startup structural-validation walk.
  requirements: [STO-002, STO-020, TXN-041, TXN-042, TXN-043, TXN-044, REC-001, REC-002]
  required_adrs: [ADR-0021, ADR-0058, ADR-0060, ADR-0099, ADR-0102]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-redb/**,
    tests/service/**, tests/service_audit/**, tests/storage_recovery/**]
  deliverables:
  - An independently supplied Command audit link resolves its commit through the shared segmented
    lookup, which proves the requested sequence lies inside the canonical segment, rather than an
    exact-key commit-row read that decodes a segment as a single commit record
  - Both halves of the link reciprocity (commit-to-provenance and provenance-to-commit) are proven
    from the one canonical segment member, because a segmented command owns no independent
    provenance row
  - The startup structural-validation walk reciprocates a Command audit link against the segment
    member when no provenance row exists, preserving the existing retention/prune tolerance and the
    permissive treatment of already-durable records
  - Pre-segmentation per-command commit rows, capsule revisions, and provenance rows retain their
    complete existing decoding and reciprocal identity checks
  - Replaying a known command returns its persisted outcome with the exact original commit sequence,
    provenance identity, and Succeeded audit link, and submits exactly one fused coordinator message
  - No durable format, key, transaction membership, acknowledgement, uncertainty, or public
    application/transport interface changes
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-service --all-features
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A known-command replay and a control-plane already-active replay each keep the exact
    original Succeeded link, the audit append and startup validation paths agree about which
    Command links are admissible under segmented authority, legacy row shapes remain fully decoded,
    and the full workspace suite passes.
  standing_design_tests:
    interface_safety: No. This restores an existing guarantee rather than adding surface. It changes
      only how storage internals resolve a durable link that is already established below the public
      boundary; no application-facing type, request, or option is added or relaxed, and the repaired
      behaviour makes idempotent replay honour its persisted outcome instead of returning a public
      outcome-unknown error.
    scale: No. Both resolutions are bounded point lookups against the same co-located redb authority
      already assumed by ADR-0102 — one predecessor range lookup plus in-segment membership proof.
      No new single-node memory residency, full-state rewrite, or whole-history scan is introduced,
      so the billion-row tier is not foreclosed any further than the accepted segmented design
      already does.
  closure:
    status: complete
    evidence: tests/storage_recovery/storage_recovery_matrix.rs
    decision: Commit 73727d78 restored independent command-audit link resolution through the exact
      canonical segment member while retaining full legacy-row reciprocity and fail-closed startup
      validation. Current full-workspace, storage, commit, service, audit replay, recovery-matrix,
      policy, formatting, and Clippy gates pass; known-command and control-plane replays retain one
      exact original Succeeded link without adding any public or durable surface.
  human_review_triggers: *id001
- id: WP-491
  name: Changelog emitter core at the storage boundary
  depends_on: [WP-487, WP-488, WP-489]
  objective: Open the replication arc by attaching an ADR-0100 changelog emitter to the ADR-0101
    publication edge, so a first-party in-process consumer obtains exact, gap-free, checksummed
    changelog frames derived only from published durable snapshots, with no wire format, no
    capability, no RPC, and no durable state of its own.
  requirements: [PERF-015, PERF-017]
  required_adrs: [ADR-0019, ADR-0061, ADR-0082, ADR-0083, ADR-0085, ADR-0086,
    ADR-0093, ADR-0098, ADR-0100, ADR-0101, ADR-0102, ADR-0104]
  allowed_paths: [work_packages.yaml, crates/riffdb-types/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    tests/storage_recovery/**]
  deliverables:
  - A dual-frontier pair type in riffdb-types addresses a changelog frame by application
    commit-sequence and administration-sequence, each independently optional with explicit
    unchanged encoding, compared under a partial componentwise order that refuses to call an
    incomparable pair an advance; CommitToken and ProjectionFrontier are untouched
  - A publication observer port in riffdb-storage-api receives one published durable-frontier
    advancement per publication as a wakeup carrying a pinned published snapshot; it is
    fire-and-forget, cannot fail, cannot wait on its consumer, and absorbs overflow as a typed
    lagging state instead of applying backpressure to the writer
  - A closed, versioned, canonically ordered, checksummed changelog frame and entry encoding that
    shares no magic, version space, or code path with the durability journal (ADR-0101 section 6,
    ADR-0104 section 8), with a length-delimited footer that discriminates a torn tail, plus a
    fail-closed validating decode covering database identity, history incarnation, frontier gaps,
    hash-chain continuity, per-class counts, canonical order, and checksum
  - The emitter derives every frame by range-reading and point-reading the pinned published
    snapshot for the covered interval only; it holds no storage handle, opens no transaction, and
    reads no journal byte or overlay internal, pinned by a source guard architecture test
  - The publication observer is called at all three durable-frontier publication sites strictly
    after the frontier swap, at the same release rank as every other covered effect; the default
    port observes nothing and changes no durable byte
  - Frame-boundary coalescing for a lagging emitter is a named deferral; v1 emits exactly one
    frame per observed advancement, and a resynced emitter re-anchors through the later bootstrap
    path rather than inventing boundaries ADR-0100 section 1 defines as observed facts
  - Differential exactness evidence applies every emitted frame to a bootstrap anchor and compares
    byte-for-byte against the materialized final published snapshot across command and
    service-audit frames; a supersession fixture writes one entity key in two separate fenced
    epochs and asserts the earlier frame carries the earlier post-image, so prefix exactness is
    proven at an intermediate frame boundary and not only at the final state
  - The redb-Immediate control-plane lane, which publishes durable frontiers for catalog,
    capability, query-module, reactive-module, migration and retention writes, is deliberately not
    observed; no frame covers it, rows it writes are only incidentally swept into a later frame's
    range read, and rows before the first or after the last observed advancement are never emitted.
    The frame chain is an exact gap-free tail, never a complete history from empty, and RE3's
    bootstrap is the only correct way to close that. Recorded on ChangelogPublicationPort and at
    the lane itself
  - The ADR-0100 section 2 gating obligation is discharged by a falsifiable test in which a
    sealed applied-but-unflushed sibling subgroup is invisible to the framing pass, and neutering
    the gate turns exactly that test red
  - No durable table, meta key, ServiceOperationV1 variant, capability kind, proto message, server
    configuration, lifecycle stage, or MCP surface is added
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  design_tests:
    interface_safety: RE1 adds no application-facing interface. The port, snapshot, frame, and
      consumer types are internal crate surfaces consumed only by first-party storage and by the
      later replication packages; no application developer or agent can reach them, and no public
      operation, capability, tool, or configuration is added. Unsafety is unexpressible rather than
      merely discouraged. The one read capability an emitter is handed is a pinned published
      snapshot, so opting out of the durability guarantee is not a call it can make wrongly; it is
      a call it cannot make at all. Every decode fails closed, and every stop is typed rather than
      silent.
    scale: The emitter reads published snapshots through the same bounded range-scan discipline as
      every other reader, over the covered sequence interval only; it never rewrites or rescans
      full state, and its per-frame work is bounded by the same flush-cover ceiling ADR-0101
      section 3 already imposes. It holds no durable state and no second bookkeeping structure.
      The only co-location it assumes is the in-process consumer ADR-0093 section 1 already
      records for a local emitter; nothing in the frame, its addressing, or its validation assumes
      one node, and the bounded delivery buffer converts a slow consumer into typed lag rather than
      into memory growth.
  exit_gate: An in-process consumer receives a gap-free checksummed frame chain whose entries
    reconstruct the published snapshot byte-for-byte, an applied-but-unflushed subgroup is provably
    invisible with a recorded falsifiability transcript, a stalled consumer never delays
    publication, and no durable, registry, transport, or public surface changes.
  closure:
    status: complete
    evidence: tests/storage_recovery/storage_recovery_matrix.rs
    decision: Commit c48802b3 delivered the internal dual-frontier publication observer and closed
      checksummed frame format at the storage boundary. Current full-workspace and recovery tests
      prove gap-free reconstruction, intermediate post-image exactness, invisibility before fence,
      typed lag without writer backpressure, fail-closed chain validation, and the absence of new
      durable, capability, transport, MCP, or application surfaces; RE2 and later replication work
      remain separately governed.
  human_review_triggers: *id001
- id: WP-492
  name: Projected aggregate and group-by wire carriage (CP2b deferral closure)
  depends_on: [WP-433, WP-434]
  objective: Finish the deliberately deferred half of the ADR-0087 projected grammar — the engine
    has folded count/sum/min/max and bounded group-by since CP2a and the service has authorized
    and carried those results since CP2b, but the wire rejected every aggregate body typed because
    no response arm existed. Add the response arm, the conversion lowering, the SDK facade, and
    the CLI verb so a bounded aggregate is expressible and readable end to end.
  requirements: []
  required_adrs: [ADR-0086, ADR-0087, ADR-0011]
  allowed_paths: [work_packages.yaml, docs/reference/CLI.md, proto/riffdb/app/v1/application.proto,
    fixtures/proto/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-service/**,
    crates/riffdb-client-rust/**, crates/riffdb-cli/**, tests/service/**]
  deliverables:
  - ExecuteProjectedQueryResponse gains one aggregate arm at tag 8 carrying group key names, an
    echo of the request's aggregate descriptors, groups, and the same frontier/head/commit_token
    a row Ready carries; a whole-set aggregate is exactly one group with an empty key list, with
    no second ungrouped response shape
  - ProjectedQueryBody field 7 becomes repeated aggregates; the singular form was rejected typed
    from the day it was defined so no client can have used it, and proto3 encodes both identically
  - Aggregate sums ride as a scale-0 riffdb.v1.Decimal through one encoder/decoder pair shared by
    the adapter and the SDK, exact across the full i128 range, with no asserted precision and no
    floating point anywhere on the path
  - An empty group's MIN/MAX is carried by field absence and stays distinguishable from a real
    NULL extreme over an optional column
  - Response encoding governs the two row arms only; requesting PACKED with an aggregate body is
    documented and pinned as inapplicable rather than an error
  - Group emission order is the engine's encoded-group-key byte order, documented on the wire,
    never re-sorted by the service, the adapter, the SDK, or the CLI, and verified strictly
    ascending on decode so a repeated key cannot double-count in a folding caller
  - The declared row limit bounds grouped output volume - the service clamps the grouping budget
    to the authorized row count before execution, and an overrun stays a typed rejection
  - The SDK checks every aggregate response against the request that produced it - echoed group
    key names, echoed descriptors, and exactly one empty-key group for a whole-set fold - so a
    self-consistent answer to a different question cannot pass
  - The conversion handles every engine result variant exhaustively; the wildcard that silently
    dropped non-row results is gone and a future variant breaks compilation
  - Projected plan hashes incorporate group keys and aggregate op kinds so a filter and an
    aggregate over the same columns no longer collide, with row-shaped plan hashes pinned
    byte-identical to their pre-extension values
  - SDK builder gains repeatable aggregate and group_by, a typed ReadyAggregates outcome, and
    decode rigor mirroring the packed decoder — per-group arity, descriptor/arm agreement,
    bounded sum coefficients, and arm-versus-request cross-checks in both directions
  - riffdb query projected gains repeatable --aggregate OP:FIELD (bare count) and --group-by FIELD
    with a JSON renderer that keeps wide sums as decimal strings
  - Differential truth acceptance over real storage, real columnar apply, and real authorization
    — every engine fold equals the same value recomputed client-side from the row results of the
    same query, plus org isolation, typed budget rejections, empty-set identity values, a typed
    actionable money-sum rejection, and shape-independent causal frontier and token
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto --all-features
  - cargo +1.97.0 test -p riffdb-api-grpc --all-features
  - cargo +1.97.0 test -p riffdb-api-grpc
  - cargo +1.97.0 test -p riffdb-service --test projected_read_acceptance --all-features
  - cargo +1.97.0 test -p riffdb-client-rust --all-features
  - cargo +1.97.0 test -p riffdb-cli --all-features
  - ./scripts/generate-proto --check
  - ./scripts/generate-cli-reference --check
  - ./scripts/check-handbook-source
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  design_tests:
    interface_safety: An aggregate adds no unsafe expressibility, on either the field axis or
      the volume axis. Fields - an aggregate is authorized against the union of selected,
      predicate, order, group-key, and aggregate-input fields that ADR-0086 section 5 already
      required, so it cannot read a column a row read could not, and it is confined to exactly
      one organization scope. Volume - grouped output is bounded by the minimum of the caller's
      declared row limit and the server grouping budget; the service clamps
      max_group_cardinality to the authorized row count before execution, because the engine
      ignores limit on the grouped path and would otherwise return up to the full server budget
      in group rows of raw key-column values to a caller that declared one row. Ungrouped
      aggregates return a fixed cardinality - one value per requested descriptor - independent of
      how many rows were folded. Budgets stay server-side with no wire vocabulary, so a caller
      cannot raise a bound, and an exceeded budget is a typed rejection, never a partial fold.
      Accepted current semantics - a pure COUNT with primary-key-only field visibility discloses
      row cardinality without naming a field; this matches the disclosure a caller already has by
      listing rows under the same grant, and is not a new capability.
    scale: Aggregation folds over the same published-snapshot org partition scan a row read
      already performs, adding no co-location assumption, no cross-partition fan-out, and no new
      storage access. Group cardinality is bounded before results are materialized.
    reversibility: The arm is additive at tag 8 and the request field change is wire-compatible;
      clients that never send aggregates are byte-for-byte unaffected, and row plan hashes are
      pinned unchanged.
  deferred:
  - Wire budget vocabulary (ADR-0087 per-query scan/grouping/memory/wall-time budgets remain
    server-side defaults; callers cannot yet declare or observe them)
  - Explain output for projected queries (ADR-0087 requires reporting access shape and budget
    consumption without executing; no ExplainProjected surface exists)
  - Decimal and money column sums (engine limitation - sum accepts i64/u64 only; rejected typed
    with a distinct type_mismatch code and an actionable fix, not silently coerced)
  - Two or more whole-set aggregates in one ungrouped request (the engine computes a single
    whole-set function and is the only path that answers an empty matching set with identity
    values rather than zero groups; rejected typed, and accepted today with group keys)
  exit_gate: A bounded aggregate or grouped projected query is expressible from the proto, the
    Rust SDK, and the CLI, and every value it returns equals the same value recomputed
    client-side from the rows it summarized over a real service, real storage, and real columnar
    apply; row-path bytes, packed goldens, preflight pins, and row plan hashes are unchanged.
  closure:
    status: complete
    evidence: tests/service/projected_read_acceptance.rs
    decision: Commit 078802c9 completed bounded projected aggregate/group-by carriage through the
      additive wire arm, exhaustive adapter, Rust SDK, and CLI. Current full-workspace and focused
      acceptance prove exact i128 decimal sums, absent-versus-null extrema, canonical group order,
      policy and organization isolation, row-limit-clamped group volume, causal frontier parity,
      and unchanged row/packed identities; the four explicitly listed query-language limitations
      remain typed and documented.
  human_review_triggers: *id001
- id: WP-495
  name: Public marketing website and early-access signup
  depends_on: [WP-404]
  objective: Publish an honest, accessible, vision-led RiffDB marketing website and a bounded double-opt-in
    early-access path without changing database semantics or overstating the current POC.
  requirements: [WEB-001, WEB-002, WEB-003, WEB-004, WEB-005, WEB-006, WEB-007, WEB-008, WEB-009]
  required_adrs: []
  allowed_paths: [SPEC.md, work_packages.yaml, README.md, .gitignore, .github/workflows/website.yml,
    assets/riffdb-mark.svg, docs/CNAME, website/**]
  deliverables:
  - Pinned Astro static site with one responsive product page, privacy notice, 404 page, canonical metadata,
    social preview, sitemap, and original first-party visual system
  - Vision-led product narrative grounded in implemented contract-first semantics, with prominent POC,
    security, compatibility, and known-limitations boundaries
  - Bounded same-origin waitlist function with server-side Turnstile validation, generic public outcomes,
    Loops double-opt-in forwarding, no address enumeration, and no analytics or cookies
  - Deterministic unit and browser acceptance covering waitlist failures, links, responsive layouts,
    keyboard interaction, reduced motion, and automated WCAG 2.2 AA checks
  - Least-privilege GitHub Actions verification and Cloudflare Pages publication, plus the canonical
    `docs.riffdb.com` handbook domain
  acceptance_commands:
  - npm --prefix website ci
  - npm --prefix website run check
  - npm --prefix website test
  - npm --prefix website run build
  - npm --prefix website run test:e2e
  - ./scripts/handbook check
  - ./scripts/check-requirement-coverage
  design_tests:
    interface_safety: The only new public input accepts one bounded email address and one bot-verification token;
      it cannot express a database operation, bypass RiffDB authorization, or acquire application authority.
      Server-side validation precedes provider submission, public outcomes do not enumerate addresses, provider
      details and credentials remain private, and confirmed consent is owned by the double-opt-in email flow.
    scale: The site is static except for one bounded request handler. Each accepted request performs one
      verification call and one provider call, retains no local state, and inherits provider and edge rate limits.
    reversibility: The website, DNS names, mailing-list adapter, and deployment workflow are operationally
      independent from database protocols and durable formats and may be replaced without a compatibility change.
  exit_gate: A clean checkout reproducibly builds and tests the public site, every marketing claim remains within
    the documented POC boundary, a verified signup enters the provider's double-opt-in flow without enumeration,
    and the pinned main-branch workflow can publish the artifact after environment credentials are provisioned.
  closure:
    status: complete
    evidence: website/README.md
    decision: Commit ea3f8ac6 delivered the pinned static Astro site, bounded same-origin double-opt-in
      waitlist adapter, privacy and POC boundaries, accessibility and responsive behavior, and
      least-privilege publication workflow. A clean npm install plus static checks, ten unit tests,
      reproducible build, and sixteen desktop/mobile Playwright tests pass at current HEAD alongside
      handbook and requirement checks; no database authority or semantic surface is introduced.
  human_review_triggers: *id001
- id: WP-496
  name: Two-phase admitted command audit overlap at startup
  depends_on: [WP-473, WP-494]
  objective: Stop the startup allocator matcher from refusing to open a database whose durable
    state it wrote itself. `administration_allocator_matches` assumed physical AUDIT sequences and
    segment-derived audit sequences are disjoint sets, but a two-phase admitted command legitimately
    occupies one administration sequence with both carriers, and the overlap was reported as an
    authoritative SequenceDiscontinuity.
  requirements: [STO-002, REC-001, REC-002]
  required_adrs: [ADR-0060, ADR-0099, ADR-0102]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-redb/**, tests/storage_recovery/**]
  deliverables:
  - The startup allocator matcher resolves an equal physical/derived pair at one administration
    sequence to one record, matching the join every neighbouring physical/derived audit reader
    already applies (`read_administration_record_readonly`, `service_lifecycle_in_write`,
    `validate_administration_tail`, and `merge_audit_sequences`)
  - Only a provably equal pair collapses; a pair whose records disagree, or whose physical bytes
    cannot be decoded, still refuses, and no other discontinuity detection is weakened
  - A two-phase admitted command (durable `Pending` plus physical `Started` from the audited
    admission port, then a terminal-only command commit) reopens with no structural findings and
    reads back as one contiguous Started/Succeeded lifecycle
  - A physical/derived pair that disagrees at one sequence, and an administration allocator that
    runs ahead of complete physical-plus-derived coverage, are both still refused as authoritative
    SequenceDiscontinuity
  - First test coverage anywhere for `admit_or_resolve_audited_group` on redb - the durable
    `Pending` row and its physical `Started` audit row are one atomic transition that survives
    reopen together, and re-admitting the same identity resumes the unchanged pending state while
    appending exactly one further start
  - No durable format change, no manifest emission change, no new production caller of the durable
    Pending writers, and no public interface change
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A two-phase admitted command reopens clean, a conflicting physical/derived pair and an
    allocator ahead of its stream each still refuse with an authoritative SequenceDiscontinuity, the
    audited-admission port has durable atomicity and replay coverage, and the full workspace suite
    passes.
  reachability: The durable-Pending writers (`admit_or_resolve`, `admit_or_resolve_group`,
    `admit_or_resolve_audited_group`) have no production callers today and riffdb-commit's
    architecture test forbids them, and `CommandServiceAuditTransitionV1::terminal_only` arises
    only from `resume_pending` on `IdempotencyRecheckResultV1::Pending`. The refusal is therefore
    reachable on a legacy or migration-carried database holding durable Pending rows, or the moment
    the plumbed-but-dormant audited-admission port is enabled - a loaded gun rather than a live
    outage. Refuse-open on legitimate self-written state is brick-class, so it is fixed now rather
    than when the port is turned on.
  standing_design_tests:
    interface_safety: No. The change has no public surface at all - `administration_allocator_matches`
      and its new equality helper are private functions inside the redb startup module, and no
      application-facing type, request, option, or error is added or relaxed. It removes a refusal
      that fired on legitimate durable state; the refusal for genuinely inconsistent state is
      preserved and separately pinned in both neuter directions.
    scale: No. The equality proof is per-sequence-pair local and runs only where a physical row and
      a derived entry actually collide, which is rare and bounded by the audit stream the matcher
      already walks once. No new scan, no new index, no additional memory residency - the same
      single pass over the AUDIT table and the same segment-derived cache the matcher already built.
  closure:
    status: complete
    evidence: tests/storage_recovery/storage_recovery_matrix.rs
    decision: Commit 47803ac3 made startup collapse only byte-equivalent physical and segment-derived
      audit records at one sequence, preserving refusal for disagreement, malformed bytes, gaps, and
      allocator drift. Current full-workspace and recovery tests prove atomic admitted Pending plus
      Started durability, clean reopen and replay, one contiguous lifecycle, and both negative
      neuter directions without changing durable formats or activating the dormant writer surface.
  human_review_triggers: *id001
- id: WP-550
  name: Deployable application alpha architecture and requirement freeze
  depends_on: [WP-398, WP-414, WP-484, WP-491, WP-492, WP-496]
  objective: Resolve the cross-cutting alpha blockers into exact accepted trust, driver,
    compiler, query, workflow, provisioning, compatibility, and release-gate requirements before
    implementation changes any public, durable, language, IR, authority, or dependency boundary.
  requirements: []
  required_adrs: [ADR-0055, ADR-0056, ADR-0065, ADR-0074, ADR-0076, ADR-0079,
    ADR-0080, ADR-0087, ADR-0095, ADR-0105, ADR-0106, ADR-0107, ADR-0108,
    ADR-0109, ADR-0110, ADR-0111, ADR-0112]
  allowed_paths: [AGENTS.md, SPEC.md, work_packages.yaml, adr/**, diagrams/**,
    fixtures/alpha-release/**, docs/SUMMARY.md, docs/architecture/**,
    docs/contributing/**, docs/known-limitations.md]
  deliverables:
  - Exact human acceptance or explicit revision/rejection of ADR-0105 through ADR-0112
  - Normative NET, DRV, BLK, OQ, WF, APE, RAP, AFC, EXP, and endurance requirement families in
    SPEC.md, with one owning implementation package and one final-evidence package per requirement
  - Frozen format-owner map for listener configuration, driver-host protocol, Go/TypeScript/Python
    public APIs, bulk command grammar/IR/durable graph, operational query grammar/IR/cursors/text
    profiles, workflow plans, installation plans/receipts, and adapter manifests
  - Dependency and threat-model review for TLS, Unicode normalization, language packaging, and
    every proposed new critical dependency; no production placeholder implementation before review
  - Exact replacement posture for the two server TLS architecture pins, a recorded sequencing
    revision against active RE2 ShipChangelog transport work, and reservation of WP-550 through
    WP-570 plus WP-572 through WP-579 exclusively for this alpha program; WP-571 remains P11
  - ADR-0100 delete/tombstone amendment and validated-prefix/entity-chain audit ownership precede
    delete activation; ADR-0108 identity changes have one receipted repository-wide rotation
  - Every implementing package in WP-553 through WP-570 and WP-572 through WP-578 carries explicit
    interface-safety and scale standing-design-test answers; omission is a work-package definition
    failure
  - Per-row policy is enforced before every read shape/rank/aggregate/export and transaction-current
    on writes; the alpha format, export/reimport, disaster drill, and endurance policy are exact
  - A fresh normative performance-comparator requirement ID is assigned without reusing PERF-017,
    which remains the accepted journal-authoritative-state requirement
  - Alpha negative-capability matrix proving that insecure remote transport, raw transactions,
    arbitrary predicates, unfenced workflow writes, silent authority widening, and adapter hooks
    remain unexpressible
  acceptance_commands:
  - ./scripts/check-requirement-coverage
  - ./scripts/generate-handbook-diagrams --check
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: All eight ADRs are Accepted exactly, provisional requirement families are normative and
    completely owned, compatibility fixtures are enumerated, critical dependencies are reviewed,
    and no implementation package can weaken a safety invariant to satisfy an adapter.
  standing_design_tests:
    interface_safety: This package adds no runtime surface. It freezes the exact interfaces and
      negative capabilities before implementation, and rejects any track whose safe path is only
      documented rather than made unexpressible by its public types and generated operations.
    scale: This package adds no data path. Every accepted track must state bounded work and future
      placement assumptions explicitly, and no package may require full-state memory, a global
      scan, or co-location beyond an already accepted authority boundary.
  closure:
    status: complete
    evidence: docs/architecture/DEPLOYABLE-APPLICATION-ALPHA-FREEZE.md
    decision: The accepted ADR-0105 through ADR-0112 architecture and requirement freeze, format
      ownership, dependency reviews, negative-capability matrix, and implementation sequencing
      shipped in commit 84f836e9. Current-head requirement coverage, generated diagrams,
      handbook validation, and formatting remain green.
  human_review_triggers: *id001
- id: WP-551
  name: Restore exact application-binding closure
  depends_on: [WP-468, WP-550]
  objective: Repair the currently red application-binding gate by regenerating agent-alpha's
    manifest, exact locks, role identities, and generated clients from one canonical source closure,
    then pin false-green and stale-binding regressions before any further module identity change.
  requirements: [AAA-003, AAA-005, AAA-009, AAA-010, SAFE-007, SAFE-008]
  required_adrs: [ADR-0056, ADR-0057, ADR-0075, ADR-0080]
  allowed_paths: [work_packages.yaml, examples/agent-alpha/**, contracts/agent-alpha/**,
    queries/agent-alpha/**, fixtures/agent-alpha/**, fixtures/application-manifests/**,
    fixtures/application-locks/**, crates/riffdb-query-module/**,
    scripts/check-application-bindings, scripts/generate-query-clients, docs/**]
  deliverables:
  - One canonical regeneration updates the agent-alpha manifest and every Rust/TypeScript/MCP
    binding to identical contract bundle, query module, plan, role, and application-lock identities
  - The repair is generated through the compiler/application-lock path; no manifest, hash literal,
    role binding, or generated client is hand-edited to make the check green
  - Regression fixtures make `application check`, lock checking, deploy identity, role
    reconciliation, and generated binding verification fail together on either side of a mismatch
  - A safe diagnostic reports expected manifest, active module, lock, role, and generated-artifact
    identities without credentials, submitted values, hidden schema, or absolute paths
  acceptance_commands:
  - ./scripts/check-application-bindings
  - ./scripts/generate-query-clients --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The previously red agent-alpha exact-identity comparison is green through canonical
    regeneration, every identity consumer agrees, and both stale-lock and stale-client neuters turn
    the same gate red before server mutation.
  standing_design_tests:
    interface_safety: This restores exact generated closure and adds no new operation. Applications
      remain unable to override a hash or bind a stale role; the only repair path is canonical
      regeneration and reconciliation through existing symbolic surfaces.
    scale: Identity comparison and regeneration are bounded by one selected application manifest
      and its declared artifacts; no database rows or global application catalog are scanned.
  closure:
    status: complete
    evidence: docs/architecture/DEPLOYABLE-APPLICATION-ALPHA.md
    decision: Canonical agent-alpha regeneration and the exact lock, role, module, manifest, and
      generated-client mismatch gates shipped in commit 91d04316. Current-head application-binding,
      query-client generation, generated-artifact, requirement, and handbook checks all pass.
  human_review_triggers: *id001
- id: WP-552
  name: Bank the alpha performance comparator evidence
  depends_on: [WP-490, WP-550]
  objective: Freeze the safe-app Postgres comparator and retain an idle-host evidentiary 90-second
    same-run performance corpus so the alpha gate depends on banked evidence rather than short
    diagnostic runs or an operator scratchpad.
  requirements: [PERF-001, PERF-002, PERF-003, PERF-004, PERF-005, PERF-007,
    PERF-008, PERF-009, PERF-013, PERF-014, PERF-018]
  required_adrs: [ADR-0055, ADR-0058, ADR-0061, ADR-0101, ADR-0104]
  allowed_paths: [SPEC.md, work_packages.yaml, examples/app-baseline/**,
    benchmarks/run-app-baseline, fixtures/app-baseline/**, scripts/**,
    docs/performance/**, release/evidence/**]
  deliverables:
  - A new normative comparator requirement freezes the safe-app obligations, backend isolation,
    durable profiles, client/transport shapes, dataset, operation weights, correctness checks, and
    ratio calculation without repurposing the existing PERF-017 identifier
  - At least three 90-second measured repetitions after warmup for the retained interactive and
    write-only concurrency sweeps, run on an idle host with process/load inventory in the receipt
  - Same-run RiffDB, minimal Postgres, and safe-app Postgres raw artifacts, environment metadata,
    correctness reconciliation, percentile/throughput summaries, and stable-run classification
  - A typed non-evidentiary result when host interference, daemon readiness failure, insufficient
    duration, comparator drift, or correctness failure invalidates a run
  acceptance_commands:
  - ./benchmarks/run-app-baseline --self-test
  - ./benchmarks/run-app-baseline --full --load interactive --load-concurrency-sweep --load-duration-secs 90 --postgres-comparator safe-app --reps 3 --require-stable
  - ./benchmarks/run-app-baseline --full --load write_only --load-concurrency-sweep --load-duration-secs 90 --postgres-comparator safe-app --reps 3 --require-stable
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Retained idle-host 90-second evidence proves the exact frozen comparator and current
    performance gates with zero correctness mismatch, or blocks alpha with a typed failed result;
    short-window diagnostics cannot satisfy this package.
  standing_design_tests:
    interface_safety: The harness observes existing public operations and cannot relax a command,
      authorization, durability, or application boundary. A comparator change invalidates evidence
      rather than silently improving the reported ratio.
    scale: Sweeps bound clients, time, dataset, artifacts, and retained diagnostics. Evidence tests
      concurrency and history without making benchmark-only state or full-memory operation part of
      the product contract.
  closure:
    status: complete
    evidence: release/evidence/wp-552/README.md
    decision: The frozen safe-app comparator and retained idle-host interactive and write-only
      90-second, three-repetition corpus shipped in commit 6c436a48. The current-head self-test and
      retained-evidence verifier pass with the recorded hashes, eligibility, stability, durability,
      and zero-mismatch requirements intact.
  human_review_triggers: *id001
- id: WP-553
  name: TLS-only remote application ingress and endpoint identity
  depends_on: [WP-550]
  objective: Add authenticated non-loopback application gRPC and protected local-socket ingress
    with mandatory peer verification, bounded lifecycle, and no proxy-asserted application
    authority while preserving the current loopback development profile.
  requirements: [API-001, SYS-002, SEC-001, SEC-002, SEC-003, SEC-004, VAL-003,
    AAA-004, AAA-005, NET-001, NET-002, NET-003, NET-004, NET-005, NET-006,
    NET-007, NET-011, NET-012]
  required_adrs: [ADR-0007, ADR-0009, ADR-0029, ADR-0040, ADR-0055, ADR-0063,
    ADR-0071, ADR-0105]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**,
    fixtures/proto/**, crates/riffdb-types/**, crates/riffdb-errors/**,
    crates/riffdb-config/**, crates/riffdb-auth/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-server/**, crates/riffdb-observability/**, crates/riffdb-cli/**,
    crates/riffdb-client-python-native/**, crates/riffdb-mcp-stdio/**, examples/**,
    tests/**, scripts/**, docs/**]
  deliverables:
  - Closed loopback-cleartext, direct-TLS, and protected-local-socket listener configuration with
    canonical endpoint identity and pre-bind validation
  - Rust-owned TLS handshake, CA/server-name verification, HTTP/2, connection/stream/drain bounds,
    and atomic certificate/trust snapshot reload
  - Deliberate replacement tests for `production_transport_features_are_exact_and_default_disabled`
    and `lockfile_has_one_base64_and_no_tls_or_compression_stack`, preserving default-disabled
    loopback and exact one-stack/no-compression dependency closure
  - Transport/protocol work is based on the exact RE2 ShipChangelog revision recorded by WP-550;
    ingress wrapping neither edits nor reinterprets replication stream semantics
  - Proxy interoperability that ignores forwarded identity, database, tenant, principal, and
    authorization assertions and repeats normal bearer authentication/current authorization
  - Public endpoint/client configuration, structured safe diagnostics, and old/current protocol and
    configuration fixtures without an insecure-remote selector
  - Real-process hostile certificate, downgrade, header-injection, saturation, reload, revocation,
    and graceful-drain tests
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-config -p riffdb-auth -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test remote_ingress --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - cargo deny check
  exit_gate: A separate host/container reaches the application service only through verified TLS
    or a protected local socket, every insecure/downgrade/proxy-identity attempt fails before
    protected output, and loopback compatibility plus shared-service parity remain exact.
  standing_design_tests:
    interface_safety: The configuration enum has no insecure non-loopback variant and proxy/TLS
      identity cannot become application authority. Architecture pins make an unreviewed transport
      feature or dependency fail at build/test time rather than rely on deployment guidance.
    scale: Listener, handshake, stream, queue, and drain resources are bounded independently of
      database size. TLS wraps the partition-routable service and adds no co-located data premise.
  closure:
    status: complete
    evidence: docs/operations/REMOTE-INGRESS.md
    decision: TLS-only remote ingress, endpoint identity, proxy-header distrust, saturation bounds,
      retained-last-good reload, and the confined ring/rustls dependency posture shipped in commit
      175c0902. Current-head crate, real-process hostile-ingress, architecture, Protobuf, generated,
      requirement, handbook, Clippy, and dependency-policy gates pass.
  human_review_triggers: *id001
- id: WP-554
  name: Container deployment, health, and credential rotation
  depends_on: [WP-553]
  objective: Make remote ingress operable through Compose and Kubernetes with truthful health,
    certificate reload, least-authority application credential rotation, protected secrets, and
    bounded connection drain/recovery.
  requirements: [API-001, SEC-001, SEC-002, SEC-003, SEC-004, DX-010, DX-011,
    DX-012, AAA-003, AAA-004, AAA-005, NET-008, NET-009, NET-010]
  required_adrs: [ADR-0041, ADR-0055, ADR-0063, ADR-0065, ADR-0072, ADR-0105]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml, crates/riffdb-config/**, crates/riffdb-auth/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-proto/**,
    crates/riffdb-server/**, crates/riffdb-cli/**, crates/riffdb-observability/**,
    crates/riffdb-testkit/**, proto/**, fixtures/**, tests/**, scripts/**, templates/**,
    release/**, docs/**, .github/**]
  deliverables:
  - Payload-free unauthenticated liveness and authenticated database-selected readiness across
    gRPC, CLI, and deployment probes
  - Resumable role-exact credential rotation with successor proof, bounded overlap, explicit old
    revocation, protected atomic credential publication, and no bearer output
  - Certificate/key/trust rotation and bounded connection drain independent from capability rotation
  - Compose and Kubernetes examples using separate application/operator credentials, sibling
    services, TLS, health checks, secret mounts, proxy forwarding, backup roots, and graceful stop
  - Crash/retry matrix at every rotation phase plus pooled-connection revocation evidence
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-auth -p riffdb-service -p riffdb-client-rust -p riffdb-server -p riffdb-cli --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test remote_deployment_rotation --all-features
  - ./scripts/remote-compose-acceptance
  - ./scripts/remote-kubernetes-render-check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A fresh container deployment reaches Ready, survives certificate and application-
    credential rotation without protected downtime or authority widening, rejects the predecessor
    after revocation, and publishes no secret through health, config, logs, or artifacts.
  standing_design_tests:
    interface_safety: Health cannot disclose protected state, credential rotation cannot widen a
      role or overwrite the only proven secret, and deployment manifests contain no insecure
      listener or application-mounted database authority.
    scale: Rotation and health operate on bounded identity/configuration snapshots and connection
      pools; they do not scan application state or coordinate all future partitions atomically.
  closure:
    status: complete
    evidence: release/container/README.md
    decision: Authenticated health, bounded certificate and application-credential rotation,
      Compose/Kubernetes deployment assets, and crash/retry coverage shipped in commit 184d78f0.
      Current-head real-process rotation tests and Kubernetes rendering pass. This closure audit
      could not rerun the Compose cell because this workstation lacks Docker Compose v2; it relies
      on the accepted implementation evidence for that host-dependent cell and does not represent
      the missing local prerequisite as a fresh pass.
  human_review_triggers: *id001
- id: WP-555
  name: Rust driver host and local application protocol
  depends_on: [WP-550, WP-553, WP-419]
  objective: Centralize remote transport trust, pooling, retry, cancellation, errors,
    read-after-commit, and reactive resumption in one long-lived Rust driver host that exposes only
    exact generated application operations to local target-language bindings.
  requirements: [API-001, SYS-002, AAA-001, AAA-002, AAA-004, AAA-005, AAA-009,
    AAA-010, PYD-002, PYD-003, PYD-006, PYD-007, DRV-001, DRV-002, DRV-003,
    DRV-004, DRV-005, DRV-006, DRV-010, DRV-011, DRV-012]
  required_adrs: [ADR-0037, ADR-0040, ADR-0055, ADR-0056, ADR-0074, ADR-0080,
    ADR-0105, ADR-0106]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**,
    fixtures/driver/**, crates/riffdb-types/**, crates/riffdb-errors/**,
    crates/riffdb-client-rust/**, crates/riffdb-config/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-query-module/**, crates/riffdb-testkit/**, fuzz/**,
    clients/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Versioned bounded driver-host protocol and exact handshake binding application lock, operation
    schemas, roles, database, remote endpoint, and error/value registries
  - Protected Unix-socket ownership and peer checks, one child/sidecar lifecycle, graceful drain,
    crash containment, and no bearer or remote endpoint carriage on the local protocol
  - Bounded Rust connection pools, stream queues, cancellation/deadline propagation, retries,
    uncertainty resolution, read-after-commit, reactive cursor resumption, and typed local overload
  - Application-only catalog dispatch with architecture checks rejecting kernel, administration,
    ad-hoc query, deployment, raw Protobuf, and arbitrary method surfaces
  - Host protocol goldens, fuzzing, hostile identities/lengths, concurrency schedules, and real
    TLS daemon acceptance
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-client-rust -p riffdb-driver-host --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test driver_host --all-features
  - cargo +nightly-2026-07-12 fuzz run driver_host_protocol -- -max_total_time=60
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A long-lived host safely executes every stable application/reactive operation over
    pooled verified remote connections, cancellation and uncertainty remain exact, and no local
    caller can express a kernel operation or weaken transport trust.
  standing_design_tests:
    interface_safety: The local protocol dispatches only generated application operations and
      carries neither raw method names nor credentials. Transport trust and retries have one Rust
      owner, so target-language code cannot weaken them.
    scale: Pools, queues, messages, streams, deadlines, and reconnect state are bounded per host;
      no application dataset is mirrored into driver memory.
  closure:
    status: complete
    evidence: docs/sdks/DRIVER-HOST.md
    decision: The application-only long-lived Rust host, bounded versioned local protocol,
      protected socket, pooled remote transport, cancellation, uncertainty, and generated-catalog
      boundary shipped in commit 04593f2e. Current-head crate and testkit suites, application and
      workspace boundary checks, and a fresh 60-second driver_host_protocol fuzz run pass.
  human_review_triggers: *id001
- id: WP-556
  name: Stable Go bindings and long-lived TypeScript transport
  depends_on: [WP-555]
  objective: Generate complete Go and TypeScript stable-application facades over the Rust driver
    host with typed values/outcomes/errors, cancellation, streaming, and no handwritten transport
    or language-owned remote trust semantics.
  requirements: [SYS-002, AAA-001, AAA-002, AAA-004, AAA-005, AAA-009, AAA-010,
    LIVE-004, LIVE-005, LIVE-006, LIVE-007, DRV-007, DRV-008, DRV-009]
  required_adrs: [ADR-0055, ADR-0056, ADR-0057, ADR-0080, ADR-0106]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml,
    crates/riffdb-application/**, crates/riffdb-driver-host/**,
    crates/riffdb-query-module/**, crates/riffdb-cli/**,
    crates/riffdb-commit/tests/architecture.rs,
    clients/go/**, clients/typescript/**, templates/**,
    fixtures/application-parity/**, fixtures/driver/**, examples/**, tests/**, scripts/**,
    docs/**, release/**, .github/**]
  deliverables:
  - Deterministic generated Go values, params, outcomes, query pages, errors, batches, and reactive
    iterators with context cancellation and exact identity constants
  - Long-lived generated TypeScript transport with AbortSignal, async iterators, server-runtime
    shutdown, one retained driver-host session, and no per-operation subprocess
  - Offline-buildable Go and TypeScript scaffolds using the same application manifest, lock,
    schemas, driver-host protocol, and golden observations
  - Boundary lint proving neither language contains a gRPC client, RiffDB credential parser,
    remote retry classifier, raw protocol import, or kernel/admin operation
  - Cross-language daemon acceptance for success, declared outcomes, typed errors, replay,
    cancellation, read-after-commit, cursor reset, stream reconnect, and saturation
  acceptance_commands:
  - ./scripts/agent-application-go-acceptance
  - ./scripts/agent-application-typescript-acceptance
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Installed Go and TypeScript applications complete the common workload through one
    long-lived Rust host with zero handwritten RiffDB glue and semantically identical errors,
    retries, cancellation, read-after-commit, and reactive behavior.
  standing_design_tests:
    interface_safety: Generated facades expose only typed operation values and cannot construct a
      remote request, capability, retry policy, or kernel/admin method outside the Rust host.
    scale: Language clients stream bounded messages through one retained host session and do not
      buffer unbounded pages, subscriptions, or complete datasets.
  closure:
    status: complete
    evidence: docs/sdks/DRIVER-HOST.md
    decision: Stable generated Go bindings and the retained TypeScript transport shipped in commit
      d7830f05. Fresh installed Go and TypeScript application acceptances, canonical binding and
      generation checks, requirement coverage, and handbook validation pass on current HEAD.
  human_review_triggers: *id001
- id: WP-557
  name: Expanded Python wheel and runtime support
  depends_on: [WP-398, WP-555]
  objective: Expand the accepted Rust-backed Python driver across a reviewed CPython/platform
    matrix while preserving the exact native transport, public API, packaging, cancellation,
    credential, and source-distribution boundaries.
  requirements: [PYD-001, PYD-002, PYD-003, PYD-004, PYD-005, PYD-006, PYD-007,
    PYD-008, PYD-009, PYD-010, PYD-011, PYD-012, AAA-001, AAA-002, DRV-013]
  required_adrs: [ADR-0037, ADR-0055, ADR-0056, ADR-0074, ADR-0106]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml,
    crates/riffdb-client-python-native/**, clients/python/**, templates/python/**,
    fixtures/application-parity/**, tests/**, scripts/**, docs/**, release/**, .github/**]
  deliverables:
  - Clean-host probe and exact supported CPython, manylinux, musllinux, macOS, Windows, x86_64,
    and aarch64 tier table with unsupported combinations explicit
  - Reproducible abi3 wheels and self-contained sdist for every accepted tier, with linkage,
    inventory, hashes, licenses, advisories, and offline installation evidence
  - Unchanged sync/async public facade, Rust-owned transport semantics, cancellation, GIL release,
    typed errors, pooling, read-after-commit, batches, and reactive iterators
  - Installed cross-database parity corpus and negative packaging/credential/native panic tests
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-client-python-native --all-features
  - ./scripts/agent-application-python-acceptance
  - ./scripts/build-python-distributions --check
  - ./scripts/test-python-platform-matrix
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo deny check
  exit_gate: Every claimed Python tier installs a reproducible artifact without a checkout,
    completes the common workload with Rust-owned semantics, and exposes neither a pure-Python
    fallback nor native/credential/internal details.
  standing_design_tests:
    interface_safety: Python values cross the accepted native facade only; no pure-Python transport,
      credential parser, generic request, or unsafe fallback is available.
    scale: GIL release and bounded native pooling permit concurrency without per-row Python
      transport work or dataset residency, while platform claims remain a finite tested matrix.
  closure:
    status: complete
    evidence: docs/python-driver.md
    decision: The reviewed abi3 platform matrix, reproducible wheel and self-contained sdist,
      unchanged Rust-owned sync/async facade, and packaging negatives shipped in commit 53b1a3c1.
      A fresh protected-scratch audit reproduced both artifacts, installed each offline, ran the
      native/runtime tests, verified all supported and unsupported platform cells, and passed
      binding, generated, requirement, and dependency-policy checks.
  human_review_triggers: *id001
- id: WP-558
  name: Cross-language driver conformance and deployment proof
  depends_on: [WP-554, WP-556, WP-557]
  objective: Freeze and prove one installed Rust, Go, TypeScript, and Python driver contract for
    pooling, cancellation, typed errors, retry/uncertainty, read-after-commit, reactive streams,
    remote TLS, and overload before adapters claim support.
  requirements: [API-001, AAA-001, AAA-002, AAA-004, AAA-005, AAA-009, AAA-010,
    PYD-004, PYD-005, PYD-006, PYD-007, LIVE-004, LIVE-005, LIVE-006, LIVE-007,
    DRV-014]
  required_adrs: [ADR-0055, ADR-0056, ADR-0074, ADR-0080, ADR-0105, ADR-0106]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml,
    crates/riffdb-auth/**, crates/riffdb-api-grpc/**, crates/riffdb-errors/**,
    crates/riffdb-service/**,
    crates/riffdb-client-python-native/**, fixtures/driver/**,
    crates/riffdb-query-module/**, crates/riffdb-cli/**, crates/riffdb-types/**,
    fixtures/application-parity/**, crates/riffdb-testkit/**, clients/**, examples/**,
    tests/**, scripts/**, docs/**, release/**, .github/**]
  deliverables:
  - Machine-readable conformance manifest and exact golden digest for every released driver
  - Same remote daemon, database, contract, role, operation, and fault corpus across all languages
  - Pool/queue saturation, cancellation, uncertain command, retry exhaustion, revocation,
    read-after-commit, cursor, reactive reconnect, shutdown, and malformed-host evidence
  - Installed artifact/platform matrix with explicit unavailable feature cells and no silently
    emulated behavior
  acceptance_commands:
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Every released language passes identical semantic observations against one remote
    service, every claimed feature is backed by installed evidence, and an absent feature is
    explicit rather than implemented by handwritten application glue.
  standing_design_tests:
    interface_safety: Conformance checks generated public surfaces and rejects hidden emulation,
      kernel imports, raw protocol access, or language-specific semantic weakening.
    scale: The corpus bounds clients, queues, streams, faults, and observations and tests pool
      saturation without assuming all data, tenants, or nodes are co-located.
  closure:
    status: complete
    evidence: docs/architecture/DEPLOYABLE-APPLICATION-ALPHA.md
    decision: The exact shared Rust, Go, TypeScript, and Python remote conformance corpus and
      machine-readable manifests shipped in commit 7978947b. A fresh all-language remote run passes
      with canonical bindings, generated artifacts, requirement coverage, and handbook checks.
  human_review_triggers: *id001
- id: WP-559
  name: Replication tombstones and delete-aware durable validation
  depends_on: [WP-491, WP-550]
  objective: Amend ADR-0100's insert-or-replace changelog algebra with exact command-time delete
    tombstones and repair every validated-prefix, entity-chain, bootstrap, and follower proof that
    currently assumes counted authoritative tables never lose live rows.
  requirements: [REC-001, REC-002, STO-001, STO-002, TXN-001, TXN-040,
    TXN-041, TXN-042, TXN-043, TXN-044, BLK-011, BLK-012]
  required_adrs: [ADR-0019, ADR-0083, ADR-0085, ADR-0093, ADR-0100, ADR-0107]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0100-*.md, adr/0107-*.md,
    proto/**, fixtures/proto/**, crates/riffdb-types/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    tests/storage_recovery/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Accepted ADR-0100 amendment defining one versioned canonical delete/tombstone entry class with
    exact table/key, prior-value, sequence, entity-chain, ordering, checksum, and idempotency rules
  - Emitter, decoder, follower apply, bootstrap, and resume fixtures prove create-update-delete-
    recreate equality and reject missing, duplicated, stale, reordered, cross-history, or
    prior-value-mismatched tombstones
  - Delete-aware validated-prefix proof replaces every `len() == count-at-bound` assumption that
    command-time deletion invalidates and keeps retention-below-bound semantics separately typed
  - Entity-chain fingerprints incorporate tombstone transitions and distinguish live cardinality
    from historical transition count without requiring a full-state rewrite at each checkpoint
  - Crash and mixed-version recovery refuses a primary/follower format combination that cannot
    represent deletion; no delete plan can activate before this package passes
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-storage-api -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A command-time delete is represented exactly in every changelog/bootstrap/recovery
    path and every checkpoint/entity-chain proof remains valid over deletion histories; without
    that proof, delete compilation and execution remain unavailable.
  standing_design_tests:
    interface_safety: Tombstones are internal authoritative transitions emitted only by a compiled
      delete command; no application can submit a changelog entry, erase history, or bypass the
      declared deletion policy. Unknown or mismatched deletion state fails closed.
    scale: Frames remain bounded by the existing published-frontier ceiling. Validation retains
      bounded checkpoints and chain proofs rather than rescanning or rewriting all live state for
      each delete, and the representation is follower/partition routable.
  closure:
    status: complete
    evidence: docs/architecture/DEPLOYABLE-APPLICATION-ALPHA.md
    decision: Delete-aware changelog V2 tombstones, reciprocal entity transitions, and substituted-
      checkpoint rejection shipped through commits 6985f11f, c2bba5ce, 2f38ae7a, and a222f682.
      Current-head storage, full recovery-matrix, Protobuf, architecture, generated, requirement,
      handbook, formatting, and Clippy gates pass.
  human_review_triggers: *id001
- id: WP-560
  name: Bounded collection command language and IR
  depends_on: [WP-300, WP-550, WP-559]
  objective: Freeze and implement compiler-visible bounded collection inputs, one non-nestable
    element expansion, statically enumerable aggregate/conflict/access sets, and checked deletion
    policy without introducing general loops, transactions, callbacks, or raw mutations.
  requirements: [DSL-001, DSL-002, DSL-003, DSL-004, DSL-005, DSL-006, DSL-007,
    DSL-008, DSL-009, DSL-010, DSL-011, DSL-012, SAFE-004, SAFE-005, PERF-004,
    PERF-005, BLK-001, BLK-002, BLK-003, BLK-004, BLK-005, BLK-007, BLK-010]
  required_adrs: [ADR-0002, ADR-0003, ADR-0013, ADR-0055, ADR-0059, ADR-0093,
    ADR-0100, ADR-0107]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**,
    crates/riffdb-types/**, crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-diagnostics/**,
    fixtures/compiler/**, fixtures/contracts/**, fuzz/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Versioned bulk-command/list/delete grammar, formatter, parser, source spans, diagnostics, IR,
    canonical codec, plan hash, old/current fixtures, and compatibility classification
  - Static proof of one partition, finite element plan, at most 256 elements/aggregate instances,
    canonical conflict acquisition, complete capacity charge, and no nested/dynamic iteration
  - Explicit delete-policy model with no-inbound or indexed-restrict proof; cascade, set-null,
    unindexed discovery, cross-partition delete, and physical history purge rejected
  - Delete syntax is feature-gated on WP-559's accepted changelog and validation format; an
    unavailable/mixed-version target receives a typed source-spanned rejection before deployment
  - Compiler negative corpus for every unsafe/unbounded construct and adapter-shaped positive
    sources for tuple, metric, document-graph, and pipeline-step mutations
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-diagnostics --all-features
  - cargo +nightly-2026-07-12 fuzz run contract_parser -- -max_total_time=60
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The compiler emits one bounded complete mutation/access/conflict plan for every
    accepted collection command, every dynamic/unbounded/delete-unsafe shape fails with a span,
    and ordinary command and batch meanings remain byte-identical.
  standing_design_tests:
    interface_safety: The compiler accepts only one finite non-nestable element expansion and no
      caller-supplied mutation, callback, transaction, or deletion policy. Delete cannot activate
      when replication/durable validation cannot represent it.
    scale: Element, aggregate, byte, partition, and conflict sets are statically bounded and fully
      charged; no runtime scan or cross-partition discovery is introduced.
  closure:
    status: complete
    evidence: docs/contracts/BOUNDED-COMMANDS.md
    decision: The bounded collection grammar, V5 IR, exact create/delete expansion, cascade proof,
      diagnostics, and compatibility fixtures shipped through commits c81d5dad through 47f30229.
      Current-head compiler suites and a fresh 60-second contract-parser fuzz run pass with generated,
      requirement, handbook, formatting, and Clippy checks green.
  human_review_triggers: *id001
- id: WP-561
  name: Atomic collection command runtime, storage, and recovery
  depends_on: [WP-490, WP-559, WP-560]
  objective: Execute one accepted bounded collection command as one typed idempotent atomic command
    graph through the ordinary runtime, authorization, coordinator, journal overlay, recovery,
    audit, provenance, and event paths.
  requirements: [ID-001, ID-004, OUT-001, OUT-002, REC-001, REC-002, STO-001,
    STO-002, TXN-001, TXN-010, TXN-011, TXN-012, TXN-013, TXN-040,
    TXN-041, TXN-042, TXN-043, TXN-044, PERF-004, PERF-005, BLK-006, BLK-008,
    BLK-009, BLK-013]
  required_adrs: [ADR-0003, ADR-0004, ADR-0005, ADR-0012, ADR-0058, ADR-0059,
    ADR-0093, ADR-0095, ADR-0100, ADR-0101, ADR-0102, ADR-0104, ADR-0107]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, proto/**, fixtures/proto/**,
    crates/riffdb-types/**, crates/riffdb-contract-ir/**, crates/riffdb-invariant/**,
    crates/riffdb-catalog/**, crates/riffdb-runtime/**,
    crates/riffdb-auth/**, crates/riffdb-policy/**, crates/riffdb-idempotency/**,
    crates/riffdb-service/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-commit/**, crates/riffdb-proto/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-observability/**, crates/riffdb-testkit/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Sealed runtime collection execution with precharged bounds and no storage iterator/callback/raw
    transaction handle crossing the semantic port
  - One whole-command outcome/idempotency/audit/provenance/sequence and one atomic complete-or-
    absent graph preserving submitted element/event ordinal and canonical conflict order
  - Complete relationship, uniqueness, revision, delete-restrict, transaction-current, capacity,
    and current-policy validation before publication
  - Memory/redb conformance, deterministic overlap schedules, cancellation, same-identity races,
    journal fence uncertainty, replay, crash recovery, startup integrity, backup, and migration
    compatibility evidence
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-runtime -p riffdb-service -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test bulk_command_recovery --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every accepted collection command is one ordinary atomic durable command, every crash
    yields the complete original outcome or absence, and no partial element, raw mutation, weaker
    authorization, or alternate recovery path exists.
  standing_design_tests:
    interface_safety: Runtime accepts only a sealed compiler plan and emits one ordinary atomic
      graph; no iterator, raw batch, transaction handle, partial-success switch, or tombstone input
      crosses the semantic port.
    scale: Evaluation and staging are bounded by the compiled 256-element/16-MiB ceilings and one
      partition, with no hidden scan, unbounded recovery, or full-state copy.
  closure:
    status: complete
    evidence: docs/contracts/BOUNDED-COMMANDS.md
    decision: Atomic collection expansion, validation, storage mutation, tombstone derivation,
      deterministic conflict ownership, and crash recovery shipped through commits c2f78f77,
      b3bedb5e, and 6c23f49e. Current-head runtime/storage/commit suites, full storage recovery, and
      the process-level bulk-command recovery matrix pass.
  human_review_triggers: *id001
- id: WP-562
  name: Generated collection clients and adapter corpus
  depends_on: [WP-558, WP-561]
  objective: Expose typed bounded collection commands and delete outcomes across generated Rust,
    Go, TypeScript, Python, CLI, and MCP application surfaces and prove real adapter-shaped atomic
    workloads without handwritten transactions.
  requirements: [AAA-001, AAA-002, AAA-003, AAA-004, AAA-005, AAA-009, AAA-010,
    PYD-004, PYD-005, PYD-006, SAFE-007, SAFE-008, BLK-014]
  required_adrs: [ADR-0055, ADR-0056, ADR-0074, ADR-0106, ADR-0107]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-application/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-api-mcp/**, crates/riffdb-query-module/**, clients/**, templates/**,
    fixtures/application-parity/**, fixtures/adapters/**, fixtures/driver/**, examples/**, tests/**,
    scripts/**, docs/**]
  deliverables:
  - Generated bounded collection inputs, methods, whole-command outcomes, error decoding,
    idempotency helpers, and exact identities in every supported language and MCP schema
  - CLI natural JSON and files/streams with pre-submit count/byte validation and no generic row
    batch or transaction option
  - OpenFGA tuple, MLflow metric, Payload document graph, and Woodpecker pipeline-step acceptance
    with atomicity, replay, conflict, delete-restrict, and negative kernel/glue assertions
  - Cross-language golden observations and boundary lint for raw write/transaction/storage access
  acceptance_commands:
  - ./scripts/adapter-bulk-command-acceptance --all-languages
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: All supported languages perform each adapter-shaped atomic collection mutation through
    one generated command and one declared outcome, with zero raw mutations, transaction callbacks,
    kernel imports, or handwritten encoding/transport glue.
  standing_design_tests:
    interface_safety: Generated list inputs preserve compiled maxima and expose no generic delete,
      row mutation, transaction, or raw element-outcome path; all safety failures remain typed.
    scale: Clients preflight bounded counts/bytes and stream within the fixed command ceiling rather
      than buffering an unbounded import or splitting one claimed atomic command.
  closure:
    status: complete
    evidence: docs/contracts/BOUNDED-COMMANDS.md
    decision: Generated bounded-collection facades and the adapter corpus shipped in commit
      4143470d. A fresh Rust, Go, TypeScript, Python, CLI, and MCP acceptance passes with canonical
      bindings, generated artifacts, requirements, and handbook pages current.
  human_review_triggers: *id001
- id: WP-563
  name: Operational RiffQL grammar, finite plan families, and cursors
  depends_on: [WP-492, WP-550, WP-551]
  objective: Add bounded optional/dynamic predicate declarations, null/existence tests, stable
    top-N/cursor ordering, multiple exact aggregate declarations, and safe symbolic catalog schemas
    while keeping runtime selection inside a finite compiler-owned access family.
  requirements: [RQL-001, RQL-002, RQL-003, RQL-004, RQL-005, RQL-006, QRY-001,
    QRY-002, QRY-003, QRY-004, QRY-005, SAFE-001, SAFE-003, SAFE-006,
    OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-007, OQ-008, OQ-009,
    OQ-010, OQ-015]
  required_adrs: [ADR-0051, ADR-0052, ADR-0053, ADR-0054, ADR-0055, ADR-0087,
    ADR-0108]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**,
    crates/riffdb-query-syntax/**, crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-diagnostics/**, crates/riffdb-catalog/**, crates/riffdb-service/**,
    crates/riffdb-cli/**, fixtures/riffql/**,
    fixtures/query-modules/**, fixtures/application-manifests/**,
    fixtures/application-locks/**, examples/agent-alpha/**, examples/ticketdesk/**,
    clients/**, fuzz/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Versioned syntax/formatter/AST/IR/module/cost/hash fixtures for optional predicate presence,
    bounded AND/OR/in, null/existence, prefix, top-N order, and multiple aggregates
  - Finite plan-family enumeration with one partition, complete authorization union, per-member
    index/projection access, total order, cursor identity, and whole-family cost ceiling
  - Source-spanned rejection of excessive combinations, unindexed members, offsets, unstable
    ordering, caller field/operator/index names, unbounded candidates, and unsupported aggregation
  - Versioned bounded symbolic catalog response schemas with indistinguishable unauthorized absence
  - Receipted old/new grammar, IR, plan, module, lock, role, and generated-artifact rotation across
    every frozen fixture and example, explicitly including agent-alpha; partial rotation is typed
  - Exact aggregate lowering reuses WP-492 descriptors/result carriage/empty-set/Decimal/group-order
    semantics and group cardinality remains clamped by the requested row limit
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog --all-features
  - cargo +nightly-2026-07-12 fuzz run riffql_parser -- -max_total_time=60
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every accepted dynamic parameter shape selects one precompiled bounded authorized plan
    and cursor identity, no caller supplies query structure, and every missing safe access path is
    rejected before deployment with an actionable span; all repository identities agree through a
    receipted canonical rotation.
  standing_design_tests:
    interface_safety: Callers supply only typed values/presence choices in a finite compiled family;
      they cannot submit structure, select an index, bypass group-volume clamps, or activate a
      module whose lock/role/generated identities are stale.
    scale: Plan-family cardinality, candidates, rows, groups, memory, output, and one-partition route
      are compile-time bounded; identity rotation is per declared application, not a data scan.
  closure:
    status: complete
    evidence: docs/getting-started/OPERATIONAL-QUERIES.md
    decision: Finite operational RiffQL plan families, cursor identity, exact predicates, ordering,
      aggregates, and receipted module rotation shipped through commits 17bb8d5e, 131c7bbe, and
      a11ae663. Current-head syntax/IR/compiler/module/catalog tests and a fresh 60-second RiffQL
      parser fuzz run pass with exact bindings and generated artifacts.
  human_review_triggers: *id001
- id: WP-564
  name: Operational query execution, text indexes, exact aggregates, and catalog service
  depends_on: [WP-490, WP-563]
  objective: Execute operational plan families against one published snapshot with declared
    versioned text indexes, null/existence semantics, stable pagination, exact decimal aggregates,
    inference protection, and policy-filtered catalog introspection.
  requirements: [QRY-001, QRY-002, QRY-004, SAFE-002, SAFE-003, SAFE-006, STO-001,
    STO-002, VAL-003, PERF-001, PERF-007, PERF-008, OQ-011, OQ-012, OQ-013,
    OQ-014]
  required_adrs: [ADR-0011, ADR-0035, ADR-0038, ADR-0053, ADR-0055, ADR-0070,
    ADR-0086, ADR-0087, ADR-0092, ADR-0104, ADR-0108]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml, proto/**,
    fixtures/proto/**, fixtures/riffql/**, crates/riffdb-types/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-commit/**,
    crates/riffdb-diagnostics/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-query-executor/**,
    crates/riffdb-catalog/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-testkit/**,
    crates/riffdb-policy/**, crates/riffdb-service/**, crates/riffdb-proto/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-mcp-stdio/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Canonical versioned text-key transform and declared index maintenance with Unicode/profile
    fixtures, prefix bounds, migration/rebuild, and no locale/platform collation; tokenization,
    scoring, corpus statistics, and relevance remain exclusively ADR-0092 FTS projection work
  - One-snapshot finite-plan selection and execution for null/existence, bounded in/OR, stable
    top-N pages, cursor validation, multiple aggregates, exact decimal overflow, currency rules,
    WP-492 empty-set identity, canonical group order, and row-limit group-cardinality clamp
  - Complete query fuel, projection/read priority, cancellation, inference minimums, authorization
    union, no partial result, and redaction enforcement
  - Bounded symbolic catalog service and protocol through the same authentication/policy/service
    boundary, with hidden-symbol and revocation tests
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-executor -p riffdb-catalog -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-policy -p riffdb-service -p riffdb-api-grpc -p riffdb-client-rust --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test operational_queries --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Operational queries return exact bounded authorized results and stable pages from one
    snapshot, text/aggregate behavior is platform-independent and migration-safe, and catalog
    introspection leaks no hidden symbol or numeric/storage identity.
  standing_design_tests:
    interface_safety: Execution consumes only a sealed finite plan; no runtime scan fallback,
      second FTS semantics, unclamped grouping, hidden catalog symbol, or partial result is exposed.
    scale: All reads are one-partition, indexed/projection-backed, snapshot-bound, and charged by
      row/group/memory/output ceilings; catalog pages are independently bounded.
  closure:
    status: complete
    evidence: docs/getting-started/OPERATIONAL-QUERIES.md
    decision: Authorized catalog pages, exact aggregate execution, binary-text predicates, null and
      existence tests, and indexed bounded execution shipped through commits 2cd47a23, cabac61d,
      37488df8, and ff57eabe. Current-head executor/storage/service/client suites and the focused
      operational-query conformance test pass.
  human_review_triggers: *id001
- id: WP-565
  name: Operational query generated clients and adapter acceptance
  depends_on: [WP-558, WP-564]
  objective: Generate ergonomic operational query builders and catalog feature preflight across all
    supported languages and prove adapter pages/search/aggregates without runtime query structure,
    client filtering, N+1 reads, or kernel inspection.
  requirements: [AAA-001, AAA-003, AAA-004, AAA-005, AAA-009, AAA-010,
    PYD-004, PYD-005, QRY-004, SAFE-007, SAFE-008, OQ-016]
  required_adrs: [ADR-0055, ADR-0056, ADR-0074, ADR-0106, ADR-0108]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-application/**,
    crates/riffdb-commit/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-api-mcp/**, crates/riffdb-query-module/**, crates/riffdb-testkit/**,
    clients/**, templates/**, fixtures/application-parity/**, fixtures/application-locks/**,
    fixtures/application-manifests/**, fixtures/query-modules/**, fixtures/adapters/**,
    examples/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Generated typed optional filters, null/existence/prefix values, page cursors, total order,
    aggregate/group results, and catalog feature views in Rust, Go, TypeScript, and Python
  - Equivalent CLI/MCP symbolic schemas with no caller field/operator/index/plan identifiers
  - Adapter acceptance for OpenFGA tuple lookup, MLflow metrics dashboards, Payload document
    lists/search, and Woodpecker pipeline queues with exact cross-language observations
  - Negative boundary evidence for client filtering, raw catalog/kernel access, N+1 composition,
    unauthorized aggregates, stale cursors, and feature-claim mismatch
  acceptance_commands:
  - ./scripts/adapter-operational-query-acceptance --all-languages
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Every required adapter page/search/dashboard is one generated bounded query operation
    with identical typed observations in all supported languages and no handwritten query,
    filtering, decoding, catalog, or kernel glue.
  standing_design_tests:
    interface_safety: Generated builders expose only compiler-declared optional values and typed
      cursors/results, never field/operator/index names, client filters, raw catalog, or N+1 escape.
    scale: Page, aggregate, and catalog bounds remain visible in generated schemas and cannot be
      raised by a language adapter; result decoding is streaming/bounded.
  closure:
    status: complete
    evidence: docs/getting-started/OPERATIONAL-QUERIES.md
    decision: Generated operational-query facades and the complete adapter corpus shipped in commit
      c6ff11b7. A fresh Rust, Go, TypeScript, Python, CLI, and MCP acceptance passes with bindings,
      generated artifacts, requirements, and handbook pages current.
  human_review_triggers: *id001
- id: WP-566
  name: Revision transitions, fenced leases, and service-owned values
  depends_on: [WP-420, WP-550]
  objective: Add compiler-lowered workflow state transitions, exact revisions, fenced lease
    claim/renew/release/expire commands, and replayable service-owned UUID/time values without a
    generic CAS, lock, caller clock, callback, or hidden mutation path.
  requirements: [DSL-001, DSL-002, DSL-003, DSL-004, DSL-005, ID-001, ID-002,
    ID-003, ID-004, OUT-001, OUT-002, TXN-001, TXN-010, TXN-011, TXN-012,
    TXN-013, REC-001, SAFE-001, SAFE-008, WF-001, WF-002, WF-003, WF-004,
    WF-005, WF-006, WF-007, WF-008, WF-009, WF-010]
  required_adrs: [ADR-0003, ADR-0005, ADR-0012, ADR-0018, ADR-0055, ADR-0059,
    ADR-0080, ADR-0095, ADR-0109]
  allowed_paths: [.gitattributes, Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**,
    fixtures/proto/**, crates/riffdb-types/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-runtime/**, crates/riffdb-auth/**, crates/riffdb-policy/**,
    crates/riffdb-service/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-server/**,
    fixtures/compatibility/**, fixtures/compiler/**, fixtures/workflows/**,
    release/durable-format-manifest-v1.json, tests/**, scripts/**, docs/**]
  deliverables:
  - Versioned workflow/transition/lease/service-value grammar, IR, plans, diagnostics, old/current
    fixtures, generated schemas, and compatibility/migration classifications
  - Required exact revision and legal-state requirements visible in source/explain, with generated
    successor revisions and no unconditional update or automatic revision substitution
  - Fenced claim/renew/release/expire semantics using transaction time, checked durations, exact
    owner/revision/token, and current authorization; lease possession grants no authority
  - Service-owned UUID/time is observed before runtime and sealed into deterministic transaction
    context; the runtime performs no clock/randomness/OS call and replays the exact persisted value
  - Lease fencing tokens are distinct from ADR-0093 history incarnation and leadership epoch;
    promotion validation binds both leadership/routing epoch and current aggregate fence
  - Deterministic and crash schedules for transition, claim, expiration, stale holder, clock
    rollback/overflow, service UUID/time replay, cancellation, and same-key uncertainty
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-runtime --all-features
  - cargo +1.97.0 test -p riffdb-service -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit -p riffdb-client-rust --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test workflow_concurrency --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Lost-update, illegal-transition, stale-lease, caller-clock, and duplicate-scheduler
    patterns are unexpressible through generated workflow operations, while every mutation remains
    an ordinary compiled idempotent command with exact recovery.
  standing_design_tests:
    interface_safety: Required revisions/fences are generated parameters, service values are not
      caller-settable, lease state grants no authority, and no generic lock/CAS/update is exposed.
    scale: Leases and transitions are aggregate-local and partition-routed with bounded durations
      and attempts; no global lock, clock scan, or all-workflow coordinator is introduced.
  closure:
    status: complete
    evidence: docs/contracts/WORKFLOWS.md
    decision: Revision-checked transitions, service-owned time and UUID values, fenced claims,
      successor revisions, and strictly increasing post-reimport fences shipped through commits
      6865f04e through 6d9cc1eb. Current-head compiler/runtime/service/storage/client suites and the
      focused workflow-concurrency tests pass.
  human_review_triggers: *id001
- id: WP-567
  name: Bounded scheduler and workflow adapter acceptance
  depends_on: [WP-558, WP-566]
  objective: Run due work through named queries/events and exact fenced commands under a symbolic
    scheduler role, with bounded attempts, durable idempotency, restart recovery, and generated
    clients for workflow-shaped adapters.
  requirements: [CON-001, CON-002, CON-003, CON-004, CON-005, CON-006, CON-007,
    CON-008, EVT-005, EVT-006, ID-004, REC-001, REC-002, SAFE-001, SAFE-007,
    WF-011, WF-012, WF-013, WF-014]
  required_adrs: [ADR-0049, ADR-0055, ADR-0056, ADR-0080, ADR-0106, ADR-0109]
  allowed_paths: [Cargo.lock, Cargo.toml, SPEC.md, work_packages.yaml, crates/riffdb-types/**,
    crates/riffdb-query-module/**, crates/riffdb-catalog/**, crates/riffdb-policy/**,
    crates/riffdb-service/**, crates/riffdb-scheduler/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-server/**, crates/riffdb-cli/**,
    crates/riffdb-observability/**, clients/**, templates/**, fixtures/workflows/**,
    fixtures/adapters/**, examples/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Bounded scheduler worker using only named query/event consumption and exact compiled commands
    under a least-authority symbolic role
  - Stable attempt identity from schedule/due instant/target/kind, at-least-once recovery, fenced
    claim, bounded retry/backoff, payload-free wakeup, and no exact-once external-effect claim
  - Generated workflow methods/types and equivalent Rust, Go, TypeScript, and Python observations
  - MLflow run and Woodpecker pipeline scheduler acceptance with kill points before/after query,
    claim, business command, event, ack/checkpoint, revocation, and restart
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-scheduler -p riffdb-service -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test scheduler_recovery --all-features
  - ./scripts/adapter-workflow-acceptance --all-languages
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Scheduler death, duplicate wakeup, lease expiry, stale worker, revocation, and restart
    lose no eligible work and duplicate no business state, while the scheduler has no mutation or
    authority path outside compiled commands.
  standing_design_tests:
    interface_safety: The scheduler owns only a least-authority role and invokes named reads/events
      plus compiled fenced commands; it has no storage handle, callback, generic lock, or authority
      derived from lease possession.
    scale: Selection, wakeups, claims, attempts, retries, and in-flight work are bounded and
      partition-local; future leaders can run local schedulers without a global queue.
  closure:
    status: complete
    evidence: docs/contracts/SCHEDULERS.md
    decision: The bounded scheduler, durable claim/retry/dead-letter state, restart recovery, and
      least-authority workflow adapters shipped in commit 62ae5026 with the exact Woodpecker role
      correction in 9bcaf9b9. Current-head scheduler recovery and all-language workflow acceptance
      pass without unfenced or callback-shaped escape hatches.
  human_review_triggers: *id001
- id: WP-568
  name: Programmatic exact application installation and upgrade campaigns
  depends_on: [WP-413, WP-550, WP-551, WP-554, WP-558]
  objective: Compile, start, observe, resume, and recover one exact application installation or
    upgrade campaign across contract/module publication, migration, role reconciliation,
    credential rotation, driver proof, and ordinary seed commands without claiming false atomicity
    or adding a privileged installer bypass.
  requirements: [AAA-003, AAA-004, AAA-005, AAA-008, AAA-013, AAA-014, DX-007,
    DX-008, DX-009, MIG-006, MIG-007, MIG-008, MIG-009, MIG-010, MIG-016,
    MIG-017, MIG-018, SAI-001, SAI-002, SAI-003, SAI-004, SAI-005, SAI-006,
    SEC-001, SEC-002, SEC-003, APE-001, APE-002, APE-003, APE-004, APE-005,
    APE-006, APE-007, APE-008, APE-009, APE-012, APE-014]
  required_adrs: [ADR-0057, ADR-0062, ADR-0065, ADR-0066, ADR-0075, ADR-0076,
    ADR-0079, ADR-0105, ADR-0106, ADR-0110]
  allowed_paths: [Cargo.lock, Cargo.toml, SPEC.md, work_packages.yaml, adr/**, proto/**,
    fixtures/proto/**, fixtures/application-manifests/**, fixtures/application-locks/**,
    fixtures/installation/**, crates/riffdb-types/**, crates/riffdb-application/**,
    crates/riffdb-query-module/**,
    crates/riffdb-auth/**, crates/riffdb-policy/**, crates/riffdb-service/**,
    crates/riffdb-commit/**, crates/riffdb-proto/**, crates/riffdb-proto/examples/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-server/**, crates/riffdb-cli/**,
    crates/riffdb-observability/**, fixtures/compatibility/**, release/**,
    examples/agent-alpha/domains/rust/Cargo.lock,
    tests/**, scripts/**, docs/**]
  deliverables:
  - Versioned deterministic installation plan/diff, permission, operation identity, service
    start/observe protocol, phase model, receipt, CLI/operator SDK, and compatibility fixtures
  - Exact resumable stage composition for preflight, deployment/migration, query/reactive modules,
    role diff/reconciliation, successor credential proof/rotation, driver handshake, and seed batch
  - Explicit migration hash/confirmation/backup/downtime gate and explicit approval for role
    widening; no automatic rollback, skipped invalid data, credential overwrite, or second bootstrap
  - Crash/retry/same-ID-different-plan/partial-campaign tests with safe completed identities and
    exact next-action diagnostics after every stage
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-application -p riffdb-auth -p riffdb-policy -p riffdb-service -p riffdb-commit -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server -p riffdb-cli --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test application_installation --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A programmatic campaign installs or upgrades exactly the reviewed application and
    authority, every interruption resumes from verified identities, partial completion is never
    reported as success, and no stage bypasses its existing compiler/service/coordinator owner.
  standing_design_tests:
    interface_safety: The campaign composes existing authoritative operations, requires explicit
      role widening/migration approval, never exposes credentials, and cannot report partial state
      as installed success or invoke an adapter hook.
    scale: Campaign state and receipts are bounded by one selected application's declared artifacts;
      data migration/seed retains its existing bounded mechanisms and no cross-cluster transaction.
  closure:
    status: complete
    evidence: docs/operations/APPLICATION-INSTALLATION-CAMPAIGNS.md
    decision: Exact installation planning, durable campaign state, remote reconciliation, resumable
      CLI execution, terminal receipts, and reimport gating shipped through commits 1e90d6e7 through
      cfe2cfb3 and d3168bfa. Current-head application/control-plane suites and every installation
      interruption/resume acceptance pass.
  human_review_triggers: *id001
- id: WP-569
  name: Adapter conformance manifests and four-domain evolution proof
  depends_on: [WP-562, WP-565, WP-567, WP-568]
  objective: Let adapter repositories declare exact RiffDB feature, artifact, role, driver,
    platform, conformance, and evolution requirements as bounded data and prove four unfamiliar
    adapter shapes without executable server plugins or hidden product workarounds.
  requirements: [AAA-003, AAA-010, AAA-011, AAA-012, AAA-013, AAA-014, AAA-018,
    MIG-001, MIG-002, MIG-003, MIG-019, SAFE-007, SAFE-008, APE-010, APE-011,
    APE-013]
  required_adrs: [ADR-0055, ADR-0056, ADR-0057, ADR-0075, ADR-0076, ADR-0106,
    ADR-0107, ADR-0108, ADR-0109, ADR-0110]
  allowed_paths: [SPEC.md, work_packages.yaml, fixtures/installation/**,
    fixtures/adapters/**, crates/riffdb-application/**, crates/riffdb-catalog/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    clients/**, templates/**, examples/**, tests/**, scripts/**, docs/**, release/**]
  deliverables:
  - Versioned content-addressed adapter conformance manifest, schema, deterministic validation,
    feature catalog, receipt binding, and old/current fixtures
  - Manifest restrictions rejecting raw permissions/IDs, storage/kernel access, arbitrary methods,
    server hooks, unbounded version ranges, shell execution, and unsupported silent fallback
  - Empty install and populated upgrade/evolution campaigns for OpenFGA, MLflow, Better Auth, and
    Woodpecker shapes, including role/credential rotation and exact conformance observations
  - Immutable failed-campaign evidence, feature/platform gaps, no source access, no handwritten
    transport/query/mutation glue, and actionable product-defect classification
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-application -p riffdb-catalog -p riffdb-cli --all-features
  - ./scripts/adapter-conformance --all-domains --all-languages
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: All four adapter shapes install, operate, and upgrade through exact public symbolic
    surfaces; every claimed feature has manifest-backed evidence; and no adapter executes server
    code, imports kernel APIs, hardcodes identities, or obtains broader authority than its role.
  standing_design_tests:
    interface_safety: Manifests are bounded data and cannot request raw permissions, server code,
      arbitrary methods, hooks, kernel access, or silent fallback; every claim maps to an exact
      public feature and conformance observation.
    scale: Each manifest and campaign is bounded per adapter/application and declares platform and
      workload ceilings; no adapter requires global discovery or whole-database materialization.
  closure:
    status: complete
    evidence: docs/architecture/DEPLOYABLE-APPLICATION-ALPHA.md
    decision: Exact adapter manifests, generated operation/role/driver claims, safe evolution, and
      the four-domain/four-language corpus shipped through commits f53af5eb and aad779c6. Fresh
      OpenFGA, MLflow, Better Auth, and Woodpecker install/evolution and public-client conformance
      passes; the current observation now includes Better Auth GetUser and DeleteUsers. Payload
      remains the declared post-alpha regression fixture.
  human_review_triggers: *id001
- id: WP-570
  name: Principal-aware row-policy language, IR, roles, and identity
  depends_on: [WP-550, WP-563]
  objective: Freeze one closed compiler-owned row-policy language and role/capability fact model so
    principal-specific entity access is statically visible, bounded, index-backed, and impossible
    for application requests or generated clients to bypass.
  requirements: [SEC-001, SEC-002, SEC-003, SAFE-001, SAFE-002, SAFE-003,
    SAFE-006, SAFE-007, QRY-001, QRY-002, QRY-004, RAP-001, RAP-002, RAP-003,
    RAP-004, RAP-005, RAP-012, RAP-013, RAP-014]
  required_adrs: [ADR-0007, ADR-0009, ADR-0038, ADR-0055, ADR-0065,
    ADR-0086, ADR-0087, ADR-0108, ADR-0111]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**,
    crates/riffdb-types/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-auth/**, crates/riffdb-policy/**,
    crates/riffdb-diagnostics/**, fixtures/compiler/**, fixtures/riffql/**,
    fixtures/application-manifests/**, fixtures/application-locks/**,
    tests/**, fuzz/**, scripts/**, docs/**]
  deliverables:
  - Versioned row-policy/principal-fact grammar, formatter, source spans, typed IR, plan codec,
    role/module/application identity, old/current fixtures, and receipted rotation closure
  - Closed predicates over row fields, principal ID/kind, bounded current capability facts,
    constants, null/existence, bounded membership/AND/OR, and one indexed local relationship exists
  - Static field/fact/index/relationship/partition/cost proof with exact fixed ceilings and source-
    spanned rejection of scans, callbacks, dynamic fields, recursion, cross-partition or caller ASTs
  - Role compilation and delegation rules make principal facts canonical, revisioned, expiring,
    audience/tenant bound, and narrowing-only; operation requests cannot add or override them
  - Explain/catalog output names visible symbolic policies and fact schemas without policy bytecode,
    capability contents, hidden branches, row values, numeric IDs, or existence leaks
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-auth -p riffdb-policy -p riffdb-diagnostics --all-features
  - cargo +nightly-2026-07-12 fuzz run contract_parser -- -max_total_time=60
  - cargo +nightly-2026-07-12 fuzz run riffql_parser -- -max_total_time=60
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every accepted policy is finite, single-partition, index-backed, identity-pinned, and
    attached symbolically to exact roles/operations; callers cannot submit, alter, skip, or locally
    evaluate it, and every unsafe/unbounded form fails at compile time with an actionable span.
  standing_design_tests:
    interface_safety: Public requests contain values only and no policy structure or bypass; role
      compilation derives one closed policy proof, so a forgotten application middleware check is
      unexpressible rather than discouraged.
    scale: Policy nodes, facts, values, probes, bytes, and partitions have compiler maxima and use
      declared indexes; no full-state or global-principal expansion enters authorization.
  closure:
    status: complete
    evidence: docs/security/ROW-POLICIES.md
    decision: Principal-aware policy syntax/IR, role-bound exact identities, relationship-index
      proofs, bounded evaluation, and V7 capability authority shipped through commits 4e1f45e7,
      03029c4f, bac8c0db, and df2ea781. Current-head compiler/auth/policy tests, parser fuzzing,
      binding generation, requirements, and handbook checks pass.
  human_review_triggers: *id001
- id: WP-572
  name: Transaction-current row-policy enforcement across every application surface
  depends_on: [WP-559, WP-561, WP-564, WP-566, WP-570]
  objective: Enforce one compiled row-policy decision before every read disclosure and against
    transaction-current plus successor state for every mutation, including projections, search,
    reactive contexts, bulk commands, workflows, and later export.
  requirements: [SEC-001, SEC-002, SEC-003, SAFE-001, SAFE-002, SAFE-003,
    SAFE-006, SAFE-008, TXN-001, TXN-010, TXN-011, TXN-012, QRY-001,
    QRY-002, QRY-004, REC-001, RAP-006, RAP-007, RAP-008, RAP-009, RAP-010,
    RAP-011, RAP-015]
  required_adrs: [ADR-0007, ADR-0038, ADR-0055, ADR-0080, ADR-0086,
    ADR-0087, ADR-0091, ADR-0092, ADR-0107, ADR-0108, ADR-0109, ADR-0111,
    ADR-0114, ADR-0116]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**, fixtures/proto/**,
    fixtures/compatibility/**, fixtures/adapters/**, fixtures/riffql/**, release/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-contract-ir/**,
    crates/riffdb-catalog/**,
    crates/riffdb-types/**, crates/riffdb-auth/**, crates/riffdb-policy/**,
    crates/riffdb-service/**, crates/riffdb-runtime/**,
    crates/riffdb-query-executor/**, crates/riffdb-projection/**,
    crates/riffdb-columnar/**,
    crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**,
    crates/riffdb-query-module/**, crates/riffdb-cli/**, crates/riffdb-server/**,
    crates/riffdb-client-rust/**, crates/riffdb-client-python-native/**,
    crates/riffdb-driver-host/**, crates/riffdb-diagnostics/**, examples/**,
    tests/**, fuzz/**, scripts/**, docs/**]
  deliverables:
  - Shared policy proof and evaluator used before selection, hydration, limit/cursor, aggregation,
    FTS/vector scoring/statistics/snippets, live diff, contextual hydration, and protected release
  - Protected projection admission reloads the complete bounded candidate set and relationship
    evidence in one authoritative snapshot, returns an entity/candidate-bound opaque proof, and
    filters before request predicates, scan charging, limit, grouping, aggregation, or ranking
  - Durable event consumption remains denied until declarations carry an explicit compiler-owned
    source-entity/key or event-policy anchor; payload field names are never treated as authority
  - Create evaluates proposed state; update evaluates transaction-current and successor state;
    delete evaluates transaction-current state; owner/visibility/ACL escape requires a named policy
  - Commit verifier rechecks row/relationship evidence and capability revision after evaluation;
    stale/revoked facts produce no mutation and no hidden-row oracle
  - Policy narrowing removes no-longer-visible live values and closes/resets streams before further
    delivery; cached plans contain no frozen principal-specific allow decision
  - Differential and inference corpus proves identical decisions across authoritative/projected/
    search/reactive paths and no hidden row affects counts, rank, page identity, output, or work class
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-auth -p riffdb-policy -p riffdb-service -p riffdb-runtime -p riffdb-query-executor -p riffdb-projection -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test row_policy_conformance --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test row_policy_inference --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: No supported read, mutation, projection, search, reactive, scheduler, or bulk path can
    observe or change an unauthorized row; policy/ACL races fail closed transaction-current, and
    every surface agrees with the pure reference evaluator without post-filter leakage.
  standing_design_tests:
    interface_safety: Every service path consumes the same typed policy proof and storage/runtime
      ports cannot accept an application-created allow decision; policy omission is an architecture
      test failure, not an endpoint convention.
    scale: Evaluation is per bounded candidate/current row plus bounded indexed relationship probes,
      with one partition and existing page/work ceilings; no global ACL scan is introduced.
  closure:
    status: complete
    evidence: docs/security/ROW-POLICIES.md
    decision: Policy enforcement before query shaping, transaction-current command checks, live
      query resets, contextual hydration/reactions, projection admission, and inference protection
      shipped through commits 2ec0009d, e485e6a3, e04943a5, aa7739e6, 0bc66c43, and 46e7e58f.
      Current-head conformance and inference suites pass across the production read/write paths.
  human_review_triggers: *id001
- id: WP-573
  name: Generated row-policy clients and realistic Better Auth/MLflow acceptance
  depends_on: [WP-565, WP-567, WP-572]
  objective: Generate principal-fact administration and policy-aware application operations and
    prove Better Auth per-user records plus MLflow experiment/run permissions without client filtering,
    middleware authorization, hidden queries, or broader roles.
  requirements: [AAA-001, AAA-002, AAA-003, AAA-004, AAA-005, AAA-009,
    AAA-010, AAA-012, AAA-018, PYD-004, PYD-005, SAFE-007, SAFE-008, RAP-016]
  required_adrs: [ADR-0055, ADR-0056, ADR-0065, ADR-0074, ADR-0106,
    ADR-0108, ADR-0111, ADR-0114, ADR-0116]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-application/**,
    crates/riffdb-client-rust/**, crates/riffdb-commit/**, crates/riffdb-driver-host/**,
    crates/riffdb-query-module/**, crates/riffdb-cli/**, crates/riffdb-api-mcp/**,
    crates/riffdb-testkit/Cargo.toml,
    clients/**, templates/**,
    fixtures/application-parity/**, fixtures/adapters/**, examples/**,
    tests/**, scripts/**, docs/**, release/**]
  deliverables:
  - Rust, Go, TypeScript, Python, CLI, and MCP parity for policy-aware queries/commands/errors and
    operator-only bounded principal-fact/role binding; applications never receive policy bytecode
  - Better Auth corpus covers per-user account/session/token visibility, lifecycle mutation,
    revocation, and cross-principal isolation; the document ACL corpus remains a post-alpha Payload
    regression fixture for owner/team/public lists, search, live update, and aggregate inference
  - MLflow corpus covers experiment/run owners and groups, nested metrics/artifacts, transition
    races, revocation, dashboards, events, and agents under least-authority roles
  - The alpha protected-event corpus contains no deletion event; adding a protected Better Auth,
    MLflow, OpenFGA, or Woodpecker deletion notification requires the accepted immutable-event-
    policy successor and a human-reviewed amendment before it can enter the gate
  - Boundary lint rejects middleware/client filtering, raw capability facts, alternate queries,
    administrator credentials, hidden row IDs, and per-language policy implementations
  acceptance_commands:
  - ./scripts/adapter-row-policy-acceptance --all-languages --domains better-auth,mlflow
  - ./scripts/check-application-bindings
  - ./scripts/driver-conformance --all-languages --row-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Realistic Better Auth and MLflow workloads enforce identical per-record decisions in every
    language and surface, including revocation/races/inference, with no application-owned policy
    code, client filter, raw identity, or authority widening.
  standing_design_tests:
    interface_safety: Generated application APIs expose only ordinary typed operations; principal-
      fact administration is operator-only and no language can evaluate or override policy locally.
    scale: Adapter corpora use bounded indexed ACL relations, page/query ceilings, and finite
      principals/groups; no generated client materializes all ACLs or rows.
  closure:
    status: complete
    evidence: docs/security/ROW-POLICIES.md
    decision: Generated row-policy operations and realistic Better Auth/MLflow applications shipped
      in commit 50354949. Fresh Rust, Go, TypeScript, Python, CLI, and MCP acceptance covers protected
      commands, revision races, live resets, nested hydration, aggregate inference, tenant isolation,
      and revocation without application-owned policy code.
  human_review_triggers: *id001
- id: WP-574
  name: Alpha durable-format manifest and upgrade/refusal policy
  depends_on: [WP-486, WP-550, WP-551]
  objective: Freeze an honest release-to-release durable-format contract with generated manifests,
    refusal-before-mutation, supported upgrade edges, and explicit export/reimport-only epochs so
    an alpha binary never discovers incompatibility by damaging or resetting existing data.
  requirements: [STO-001, STO-002, STO-020, STO-021, STO-022, REC-001,
    REC-002, MIG-001, MIG-002, MIG-003, AFC-001, AFC-002, AFC-003, AFC-004,
    AFC-005, AFC-006, AFC-007, AFC-008, AFC-009, AFC-010, AFC-011, AFC-012]
  required_adrs: [ADR-0019, ADR-0050, ADR-0061, ADR-0072, ADR-0082,
    ADR-0085, ADR-0101, ADR-0102, ADR-0103, ADR-0104, ADR-0112]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, adr/**, proto/**,
    fixtures/proto/**, fixtures/storage/**, fixtures/compatibility/**,
    crates/riffdb-types/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-proto/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-server/**, crates/riffdb-cli/**,
    crates/riffdb-testkit/Cargo.toml, tests/storage_recovery/**, tests/**,
    scripts/**, docs/**, release/**]
  deliverables:
  - Generated durable-format manifest naming alpha epoch, writer, complete readable/writable
    registry/record/storage/journal/backup/receipt versions, supported release edges, and fixture hash
  - Startup/preflight comparison before mutation with typed current/binary epoch, compatible action,
    backup/free-space/downtime requirements, and no automatic reset or decoder-shaped error
  - Same-epoch release-pair matrix proves preservation or refusal-before-mutation; breaking epochs
    require explicit export/reimport-only classification, release notes, and retained old binary
  - Backup manifests state compatible restore range; incompatible restore fails before target
    replacement; downgrade remains explicitly unsupported
  - Handbook compatibility table and release-template check prevent publishing an artifact without
    exact format/upgrade/export posture
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-service -p riffdb-server -p riffdb-cli --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test release_format_compatibility --all-features
  - ./scripts/check-durable-format-manifest
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Every supported release pair preserves existing data or refuses before mutation with
    one exact recovery path, every breaking epoch has an export/reimport ceremony, and no release
    artifact can omit or overstate its durable-format compatibility.
  standing_design_tests:
    interface_safety: A binary cannot select ignore/force/reset on incompatible data; preflight and
      manifest types admit only supported upgrade, export/reimport, or refusal outcomes.
    scale: Compatibility reads bounded manifests/registries and uses existing restartable migration;
      it neither loads nor rewrites full state merely to decide whether opening is safe.
  closure:
    status: complete
    evidence: docs/compatibility.md
    decision: The generated alpha durable-format manifest, release-pair refusal/upgrade policy,
      backup compatibility range, fixture inventory, and pre-mutation format gate shipped in commit
      ce852c46. Current-head manifest generation, release compatibility tests, storage/service/server/
      CLI suites, generated artifacts, requirements, and handbook checks pass.
  human_review_triggers: *id001
- id: WP-575
  name: Snapshot-consistent symbolic application export and reimport receipts
  depends_on: [WP-558, WP-568, WP-570, WP-572, WP-574, WP-599]
  objective: Provide a public operator-authorized, resumable, snapshot-consistent export of symbolic
    entities/events and optional provenance/audit, plus adapter-declared compiled reimport mappings,
    without exposing storage bytes or creating a generic import write path.
  requirements: [API-001, SEC-001, SEC-002, SEC-003, SAFE-001, SAFE-002,
    SAFE-003, SAFE-006, SAFE-007, REC-001, REC-002, MIG-001, MIG-002,
    EXP-001, EXP-002, EXP-003, EXP-004, EXP-005, EXP-006, EXP-007, EXP-008,
    EXP-009, EXP-010, EXP-011, EXP-012, EXP-013, EXP-014]
  required_adrs: [ADR-0007, ADR-0055, ADR-0057, ADR-0065, ADR-0072,
    ADR-0080, ADR-0110, ADR-0111, ADR-0112, ADR-0114, ADR-0115, ADR-0116,
    ADR-0118, ADR-0119]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml, proto/**, fixtures/proto/**,
    fixtures/export/**, crates/riffdb-types/**, crates/riffdb-auth/**,
    crates/riffdb-policy/**, crates/riffdb-service/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-observability/**, crates/riffdb-server/**,
    crates/riffdb-application/**, crates/riffdb-testkit/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**,
    crates/riffdb-cli/**, clients/**, examples/**, tests/**, scripts/**, docs/**, release/**]
  deliverables:
  - Closed start/page/status/cancel export operations and CLI over one immutable published snapshot,
    bounded rows/bytes/time/state, opaque scoped cursor, lease, durable checkpoint, and receipt
  - Canonical JSONL entity/event files plus separately authorized provenance/public-audit files and
    manifest binding database/history, contract/module, row-policy scope, frontier, counts, hashes,
    omissions, operation identity, and terminal state
  - Natural typed values and symbolic names only; numeric/storage IDs, keys, envelopes, journal/
    changelog bytes, credentials, hidden schema, paths, and unredacted submitted secrets are absent
  - Whole-application export uses distinct operator authority; principal export applies row/field
    policy before serialization; audit/provenance never follows from entity visibility alone
  - Adapter manifests map portable classes to declared idempotent compiled commands/migrations and
    reconcile observations after empty-database reimport without claiming stable commit sequences
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-policy -p riffdb-service -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-cli --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test application_export --all-features
  - ./scripts/adapter-export-reimport-acceptance --all-domains --all-languages
  - ./scripts/generate-proto --check
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Each adapter exports a complete receipted symbolic snapshot, an empty database is
    repopulated only through declared compiled behavior, observations reconcile exactly within
    declared omissions, and neither export nor reimport exposes a storage/policy/write bypass.
  standing_design_tests:
    interface_safety: Export selectors are closed symbolic classes/scopes, not tables or field IDs;
      whole-app/audit authority is explicit, and reimport can invoke only declared compiled plans.
    scale: Export is paged, leased, resumable, snapshot-bound, and byte/row/time bounded; neither
      server nor client holds the complete dataset in memory.
  closure:
    status: complete
    evidence: docs/operations/APPLICATION-EXPORT.md
    decision: Distinct export authority, snapshot-consistent paging, dependency-ordered portability
      manifests, compiled-command reimport, resumable receipts, and four-domain operators shipped
      through commits e5c43f07 through a1f8eff3. Fresh all-domain/all-language portability acceptance
      passes and refreshes the OpenFGA receipt to its current exact contract and manifest identities.
  human_review_triggers: *id001
- id: WP-576
  name: Remote offline backup, destructive restore, and adapter disaster drills
  depends_on: [WP-155, WP-554, WP-569, WP-574]
  objective: Exercise the existing public offline maintenance protocol from a separate operator
    container for every adapter, destroy the live database, restore its verified checkpoint-plus-
    journal unit, and prove the restored application rather than treating backup as a paper claim.
  requirements: [API-001, SEC-001, SEC-002, SEC-003, REC-001, REC-002,
    STO-001, STO-002, STO-020, STO-021, STO-022, END-001, END-002, END-003,
    END-004]
  required_adrs: [ADR-0019, ADR-0050, ADR-0072, ADR-0085, ADR-0101,
    ADR-0103, ADR-0104, ADR-0105, ADR-0110, ADR-0112]
  allowed_paths: [SPEC.md, work_packages.yaml, fixtures/backup/**, fixtures/adapters/**,
    fixtures/export/**,
    crates/riffdb-storage-redb/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**,
    crates/riffdb-server/**, crates/riffdb-cli/**, templates/**, examples/**,
    tests/**, scripts/**, docs/**, release/**, .github/**]
  deliverables:
  - Separate operator container invokes public create/status/restore with least-authority credential;
    server-owned configured roots remain invisible and application credentials cannot maintain data
  - Each adapter reaches quiescence, creates and externally retains a verified immutable backup,
    loses/destroys its database volume, restores into an empty replacement, and reaches readiness
  - Post-restore structural/startup validation, history-incarnation handling, credential/role/module
    identity, events/projections, and complete adapter conformance match the backup frontier
  - Corrupt/missing/mixed/incompatible manifest, redb, extent, receipt, credential, database, and
    confirmation cases fail before target replacement with exact recovery guidance
  - Disaster receipt records backup/restore operation identities, manifests, frontiers, checksums,
    expected destroyed suffix, validation result, and adapter observation digest without secrets
  acceptance_commands:
  - ./scripts/adapter-disaster-recovery-acceptance --all-domains --remote
  - ./scripts/remote-compose-acceptance --backup-restore
  - cargo +1.97.0 test -p riffdb-testkit --test remote_backup_restore --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Every alpha adapter can lose its database volume and recover the exact backed-up state
    through public remote maintenance with full conformance, while corruption, incompatible format,
    insufficient authority, or ambiguous publication replaces nothing.
  standing_design_tests:
    interface_safety: Only the accepted operator maintenance operations can quiesce/replace a
      database; clients cannot supply paths, bypass confirmation, reuse application authority, or
      convert a corrupt/incompatible artifact into a target.
    scale: Backup/restore streams/copies bounded files on the server and validates the exact closed
      inventory; it does not require database contents in memory or all databases offline together.
  closure:
    status: complete
    evidence: release/evidence/alpha/disaster-recovery-v1.json
    decision: Public remote offline backup, typed destructive restore, exact inventory validation,
      restored-state conformance, and Better Auth disaster coverage shipped through commits 5c8c0a2c,
      a2f7e416, and aa22e4e4. Current-head remote-backup tests pass. This audit could not rerun the
      Compose container cell because Docker Compose v2 is unavailable on the workstation; it relies
      on the tracked accepted remote receipt and does not claim a fresh container execution.
  human_review_triggers: *id001
- id: WP-577
  name: Long-duration lifecycle and resource-growth harness
  depends_on: [WP-490, WP-552, WP-554, WP-558, WP-559]
  objective: Build a reproducible remote mixed-load endurance harness that forces history growth,
    checkpoints, retention windows, journal extent recycling, reactive consumers, backup windows,
    and restart cycles while measuring bounded memory/file/queue/resource behavior.
  requirements: [PERF-001, PERF-002, PERF-003, PERF-004, PERF-005, PERF-007,
    PERF-008, PERF-009, PERF-013, PERF-014, REC-001, REC-002, STO-002,
    END-005, END-006, END-007, END-008]
  required_adrs: [ADR-0050, ADR-0058, ADR-0061, ADR-0080, ADR-0085,
    ADR-0101, ADR-0103, ADR-0104]
  allowed_paths: [SPEC.md, work_packages.yaml, Cargo.lock, examples/app-baseline/**,
    examples/ticketdesk/**,
    benchmarks/**, fixtures/endurance/**, crates/riffdb-observability/**,
    crates/riffdb-testkit/Cargo.toml, tests/**, scripts/**, docs/SUMMARY.md,
    docs/performance/**, release/evidence/**]
  deliverables:
  - Closed workload manifest with remote Rust/Go/TypeScript/Python clients, mixed reads/writes/
    workflows/events/live queries, hot/cold keys, tenant mix, bounded rates, seeds, and fault schedule
  - Instrumentation/receipts for RSS/allocator, database/journal/backup bytes, extent generations,
    checkpoints, retention watermarks/tombstones, queues, consumers, projections, errors, latency,
    throughput, restarts, and conformance checkpoints
  - Harness forces many journal recycle generations, multiple checkpoint/recovery and supported
    offline retention/backup cycles, sustained contention, capability rotation, and deploy under load
  - Growth assertions compare memory/file/backlog slopes with modeled retained live/history data and
    reject leaks, unbounded queues, repeated O(history) lifecycle work, starvation, or hidden retries
  - Idle-host/environment/process inventory and typed non-evidentiary outcomes prevent interference,
    early exit, missing generations, or incomplete observations from passing
  acceptance_commands:
  - ./scripts/alpha-endurance --self-test
  - ./scripts/endurance-conformance --self-test
  - cargo +1.97.0 test -p riffdb-testkit --test endurance_harness --all-features
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: The harness reproducibly exercises every duration/size lifecycle named above, detects
    injected leaks/quadratic shutdown/recycle/queue faults, and cannot emit a passing receipt for a
    short, interfered, incomplete, or structurally invalid run.
  standing_design_tests:
    interface_safety: The harness uses release public surfaces and fault controls outside production
      interfaces; it cannot add a benchmark-only bypass or waive correctness to sustain load.
    scale: Load, clients, retention, artifacts, diagnostics, and fault cadence are explicitly bounded
      while duration and history are large enough to expose growth/recycling defects.
  closure:
    status: complete
    evidence: docs/performance/alpha-endurance.md
    decision: The bounded long-duration controller, lifecycle/fault action manifest, continuous
      conformance, progress/starvation detection, retention lane, journal-generation observation,
      resource-growth accounting, and tamper-evident receipts shipped through commits c96f2e92,
      d275bd33, b81f79bd, and 6d976fed. Both self-tests and the focused harness suite pass; WP-578
      remains the separate owner of the actual 72-hour evidence run.
  human_review_triggers: *id001
- id: WP-578
  name: Portability, disaster-recovery, and 72-hour endurance evidence
  depends_on: [WP-573, WP-575, WP-576, WP-577, WP-609, WP-610, WP-623, WP-705, WP-724, WP-760]
  objective: Bank release-candidate evidence that each ADR-0201 general-alpha subject preserves
    policy and portable data, restores after total local disk loss, and remains correct/resource-
    bounded for 72 continuous hours through repeated journal, checkpoint, retention, backup,
    rotation, and restart cycles without promoting its evidence classification.
  requirements: [AAA-003, AAA-010, AAA-012, AAA-013, AAA-014, AAA-018,
    SEC-001, SEC-002, SEC-003, SAFE-001, SAFE-002, SAFE-006, SAFE-007,
    SAFE-008, PERF-001, PERF-003, PERF-004, PERF-007, PERF-008, PERF-013,
    PERF-014, REC-001, REC-002, STO-001, STO-002, END-009, END-010, END-011]
  required_adrs: [ADR-0050, ADR-0080, ADR-0082, ADR-0085, ADR-0101,
    ADR-0103, ADR-0104, ADR-0110, ADR-0111, ADR-0112, ADR-0115, ADR-0116,
    ADR-0118, ADR-0119, ADR-0201]
  allowed_paths: [SPEC.md, work_packages.yaml, fixtures/adapters/**,
    fixtures/endurance/**, examples/**, benchmarks/**, tests/**, scripts/**,
    docs/**, release/evidence/**, .github/**,
    crates/riffdb-service/src/live_query.rs,
    crates/riffdb-server/src/daemon.rs,
    crates/riffdb-storage-redb/src/journal.rs,
    crates/riffdb-storage-redb/src/layout.rs,
    crates/riffdb-storage-redb/src/store.rs,
    crates/riffdb-storage-redb/src/startup.rs,
    crates/riffdb-storage-redb/src/validated_prefix.rs,
    crates/riffdb-storage-redb/tests/architecture.rs,
    crates/riffdb-sim/tests/seeded_campaign/**,
    tests/migrations/contract_migration_gate_a.rs]
  deliverables:
  - One retained 72-hour remote mixed-load receipt with all four general-alpha subjects/languages,
    periodic conformance and policy probes, exact ADR-0201 claim scopes, zero silent loss/leak, and
    no unsupported storage-unavailable path
  - Periodic semantic probes are built once from a clean release revision, sealed with their fixtures
    and release identity, and remain unaffected by later live-checkout edits throughout the run
  - Forced journal recycling across many generations, repeated checkpoint/recovery, supported
    retention and backup cycles, capability/certificate rotation, deploy-under-load, and restarts
  - Bounded RSS/file/backlog/queue slopes after accounting for declared retained data; final full
    startup validation and every applicable general-alpha conformance manifest pass
  - Each general-alpha subject completes symbolic export/reimport and remote destroy/restore drills,
    with receipts linked to its exact classification, format manifest, and observation digests
  - A 24-hour rehearsal may qualify harness changes, but cannot satisfy alpha; interruption,
    interference, missing lifecycle coverage, or an invalid observation makes the 72-hour run fail
  acceptance_commands:
  - ./scripts/endurance-conformance --self-test
  - ./scripts/alpha-endurance --duration-hours 72 --all-domains --all-languages --remote --require-idle-host --require-all-lifecycles
  - ./scripts/adapter-conformance --all-domains --all-languages
  - ./scripts/adapter-export-reimport-acceptance --all-domains --all-languages
  - ./scripts/adapter-disaster-recovery-acceptance --all-domains --remote
  - ./scripts/alpha-endurance --bind-release-receipt release/evidence/alpha-endurance-v1.json --release-evidence-inventory release/evidence/alpha-endurance-inventory-v1.json --output release/evidence/alpha-endurance-v1.json
  - ./scripts/alpha-endurance --verify-release-receipt release/evidence/alpha-endurance-v1.json
  - ./scripts/check-application-bindings
  - ./scripts/check-durable-format-manifest
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Retained 72-hour evidence covers every required lifecycle with bounded growth and exact
    correctness, every general-alpha subject exports/reimports and restores after disk loss, no claim
    is promoted beyond ADR-0201, and no short/interfered/partial run or policy/format/backup omission
    can be classified as an alpha pass.
  standing_design_tests:
    interface_safety: Evidence uses the same installed public and generated surfaces alpha ships;
      every policy/export/restore negative is required and no test-only authority enters the daemon.
    scale: The run proves bounded growth over sustained history/recycling and multiple tenants while
      receipts/artifacts remain capped; it does not claim infinite retention or global co-location.
  human_review_triggers: *id001
- id: WP-579
  name: Deployable Application Alpha release gate
  depends_on: [WP-421, WP-490, WP-493, WP-494, WP-496, WP-551, WP-552, WP-558,
    WP-562, WP-565, WP-567, WP-569, WP-573, WP-574, WP-575, WP-576, WP-578,
    WP-597, WP-598, WP-600, WP-623, WP-648,
    WP-722, WP-723, WP-724, WP-725, WP-726, WP-727, WP-728]
  objective: Close the first alpha release with installed, remote, multi-language, adapter-shaped,
    upgrade, rotation, correctness, recovery, security, performance, and documentation evidence
    over only generated and public symbolic application surfaces.
  requirements: [API-001, SYS-002, SYS-004, SEC-001, SEC-002, SEC-003, SEC-004,
    AAA-001, AAA-002, AAA-003, AAA-004, AAA-005, AAA-009, AAA-010, AAA-012,
    AAA-018, SAFE-001, SAFE-002, SAFE-006, SAFE-007, SAFE-008, PERF-001,
    PERF-002, PERF-003, PERF-004, PERF-007, PERF-008, PERF-009, PERF-013,
    PERF-014, PERF-018,
    NET-001, NET-002, NET-003, NET-004, NET-005, NET-006, NET-007, NET-008,
    NET-009, NET-010, NET-011, NET-012,
    DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006, DRV-007, DRV-008,
    DRV-009, DRV-010, DRV-011, DRV-012, DRV-013, DRV-014,
    BLK-001, BLK-002, BLK-003, BLK-004, BLK-005, BLK-006, BLK-007, BLK-008,
    BLK-009, BLK-010, BLK-011, BLK-012, BLK-013, BLK-014,
    OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-007, OQ-008,
    OQ-009, OQ-010, OQ-011, OQ-012, OQ-013, OQ-014, OQ-015, OQ-016,
    OQ-017, OQ-018, OQ-019, OQ-020, OQ-021, OQ-022, OQ-023, OQ-024,
    OQ-025, OQ-026, OQ-027, OQ-028, OQ-029, OQ-030,
    WF-001, WF-002, WF-003, WF-004, WF-005, WF-006, WF-007, WF-008,
    WF-009, WF-010, WF-011, WF-012, WF-013, WF-014,
    APE-001, APE-002, APE-003, APE-004, APE-005, APE-006, APE-007, APE-008,
    APE-009, APE-010, APE-011, APE-012, APE-013, APE-014,
    RAP-001, RAP-002, RAP-003, RAP-004, RAP-005, RAP-006, RAP-007, RAP-008,
    RAP-009, RAP-010, RAP-011, RAP-012, RAP-013, RAP-014, RAP-015, RAP-016,
    AFC-001, AFC-002, AFC-003, AFC-004, AFC-005, AFC-006, AFC-007, AFC-008,
    AFC-009, AFC-010, AFC-011, AFC-012,
    EXP-001, EXP-002, EXP-003, EXP-004, EXP-005, EXP-006, EXP-007, EXP-008,
    EXP-009, EXP-010, EXP-011, EXP-012, EXP-013, EXP-014,
    SECF-001, SECF-002, SECF-003, SECF-004, SECF-005, SECF-006,
    END-001, END-002, END-003, END-004, END-005, END-006, END-007, END-008,
    END-009, END-010, END-011]
  required_adrs: [ADR-0055, ADR-0056, ADR-0065, ADR-0074, ADR-0076, ADR-0079,
    ADR-0080, ADR-0101, ADR-0102, ADR-0103, ADR-0104, ADR-0105, ADR-0106,
    ADR-0107, ADR-0108, ADR-0109, ADR-0110, ADR-0111, ADR-0112, ADR-0114,
    ADR-0115, ADR-0116, ADR-0117, ADR-0118, ADR-0119, ADR-0130, ADR-0131,
    ADR-0201]
  allowed_paths: [SPEC.md, PLAN.md, work_packages.yaml, Cargo.lock, crates/**, proto/**,
    clients/**, fixtures/**, fuzz/**, tests/**, examples/**, benchmarks/**, scripts/**,
    templates/**, docs/**, release/**, .github/**]
  deliverables:
  - Release-derived Compose/Kubernetes deployment with TLS, proxy path, liveness/readiness,
    certificate and application-credential rotation, backup/restore, and no host-loopback shortcut
  - Installed Rust, Go, TypeScript, and Python driver conformance over pooling, cancellation,
    typed errors, retries/uncertainty, read-after-commit, reactive streams, and overload
  - OpenFGA, MLflow, Better Auth, and Woodpecker general-alpha empty-install/populated-upgrade
    workloads exercising collection commands, operational queries, row policies, workflows, events,
    migrations, portability/disaster recovery, and manifests within ADR-0201's exact claim scopes
  - Better Auth per-user record and MLflow experiment/run policy probes cover isolation,
    revocation, mutation races, projections/aggregates/live queries, and inference negatives;
    Payload remains a generated post-alpha regression fixture
  - Published durable-format compatibility manifest plus same-epoch upgrade/refusal and breaking-
    epoch export/reimport evidence; no unsupported binary opens or resets existing data
  - Crash/network/revocation/expiry/deploy-under-load/history-growth/security-negative matrix with
    exact final-state reconciliation and no unavailable/storage errors under supported load
  - Retained WP-490 same-run performance gate, public compatibility matrix, release artifacts,
    handbook, operator runbooks, incident/rotation procedures, and honest known limitations
  - WP-551 application-binding closure is green before any release run, and WP-552 contributes
    retained idle-host 90-second interactive/write-only comparator evidence; short runs are not
    accepted as substitutes
  - WP-576 remote destroy/restore drills pass every adapter, and WP-578 contributes the complete
    retained 72-hour lifecycle/resource-growth receipt; a 24-hour rehearsal cannot satisfy release
  - Six fresh independent public-only agent evaluations covering Blog and Orders across Rust, Go,
    TypeScript, and Python, proving zero kernel imports, raw IDs, generic writes, handwritten
    transport/query/transaction glue, or RiffDB implementation-source access
  - WP-722 authoring evidence, a fresh agent authoring a compiling contract and named query for an
    unseen domain from public sources alone. Evaluating generated bindings is not evidence that the
    contract and query languages are authorable
  - WP-723 continuous adapter protection green, so no language change lands while breaking a pinned
    out-of-tree adapter, and WP-724 has settled every general, partial-profile, and post-alpha claim
    record without erasing narrower external evidence or promoting a fixture to an upstream suite
  - WP-725 seeded campaign green; no release runs with a red first-party suite
  - WP-726 has decomposed the unary outside-service residual and either reduced it or accepted it on
    the record, with published service levels that constrain the artifact rather than admit it
  - WP-727 chart artifacts install, upgrade, rotate, back up, restore, and drain a cluster
    deployment with no hand-edited manifest
  - WP-728 publishes signed verifiable artifacts, a container image, and driver packages from a tag
    with no manual step, refusing an inconsistent version transition before publication
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-features
  - ./scripts/deployable-alpha-acceptance --all-domains --all-languages --remote
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/adapter-row-policy-acceptance --all-languages --domains better-auth,mlflow
  - ./scripts/adapter-export-reimport-acceptance --all-domains --all-languages
  - ./scripts/adapter-disaster-recovery-acceptance --all-domains --remote
  - ./scripts/alpha-endurance --bind-release-receipt release/evidence/alpha-endurance-v1.json --release-evidence-inventory release/evidence/alpha-endurance-inventory-v1.json --output release/evidence/alpha-endurance-v1.json
  - ./scripts/alpha-endurance --verify-release-receipt release/evidence/alpha-endurance-v1.json
  - ./scripts/check-durable-format-manifest
  - ./benchmarks/run-app-baseline --full --load interactive --load-concurrency-sweep --postgres-comparator safe-app --reps 3 --require-stable
  - ./benchmarks/run-app-baseline --full --load write_only --load-concurrency-sweep --postgres-comparator safe-app --reps 3 --require-stable
  - ./scripts/release-source-bootstrap-smoke
  - ./scripts/agent-application-alpha-acceptance --campaign campaign-03 --runs 6 --sealed --assert-gate
  - ./scripts/check-application-bindings
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - cargo +1.97.0 doc --workspace --no-deps
  - cargo deny check
  exit_gate: A design partner can obtain signed released artifacts, deploy and upgrade them from
    published packaging, operate, rotate, recover, author their own contract from public sources,
    and develop a non-critical adapter workload through generated public symbolic surfaces; every
    provisional alpha requirement and prior correctness/performance gate passes with a green
    first-party suite; and no waiver introduces an insecure listener, kernel/storage bypass, raw
    transaction, unbounded query, unfenced workflow, missing row policy, silent format reset,
    nonportable data, untested restore, shortened soak, silent authority widening, language-owned
    transport trust, an unpublished or unverifiable release artifact, a performance level that
    constrains nothing, or a gate sentence that overstates the evidence behind it.
  standing_design_tests:
    interface_safety: The final installed matrix must prove all unsafe alternatives absent across
      languages, transports, adapters, deployment, and evolution; no waiver or known-limitation
      label can convert an expressible bypass into an alpha pass.
    scale: Evidence covers bounded concurrent remote workloads, history growth, deployment under
      load, recovery, and adapter data shapes without requiring co-location, whole-state memory,
      unbounded scans, or benchmark-only semantics.
  human_review_triggers: *id001
- id: WP-571
  name: Repository ownership and open-source notices
  depends_on: [WP-200, WP-495]
  objective: Identify Kevin O'Shea and O'Shea & Sons, LLC as the copyright owners of
    original RiffDB project materials while retaining the existing dual open-source license,
    preserving separate notices, and carrying attribution into public and release artifacts.
  requirements: [OWN-001, OWN-002, OWN-003, OWN-004]
  required_adrs: []
  allowed_paths: [SPEC.md, work_packages.yaml, COPYRIGHT, LICENSE-MIT, README.md,
    scripts/check-copyright-notice, scripts/release-poc, scripts/release-install-smoke,
    docs/installation.md, docs/release.md, release/README.md, release/RELEASE_NOTES.md,
    website/src/components/SiteFooter.astro, website/tests/e2e/site.test.ts]
  deliverables:
  - Canonical root ownership notice naming Kevin O'Shea and O'Shea & Sons, LLC without
    altering the MIT or Apache-2.0 license choice
  - Public README, handbook, and marketing-site attribution with the company website link
  - Release assembly and installation checks that carry the ownership notice beside both licenses
  - Repository check preventing the canonical notice and release propagation from drifting
  acceptance_commands:
  - ./scripts/check-copyright-notice
  - bash -n scripts/check-copyright-notice scripts/release-poc scripts/release-install-smoke
  - npm --prefix website run check
  - npm --prefix website run test:e2e
  - ./scripts/handbook check
  - ./scripts/check-requirement-coverage
  exit_gate: Source, public documentation, the marketing website, and verified release installation
    all carry the same ownership notice, the company link is correct, both open-source license
    choices remain intact, and separately noticed third-party or contributor material is excluded.
  standing_design_tests:
    interface_safety: No application-facing database interface, authority, operation, or guarantee
      changes. The work changes ownership and distribution notices only.
    reversibility: Notice placement and release propagation are independent of database protocols,
      durable formats, and application compatibility. A future ownership change requires explicit
      maintainer and legal review rather than an implementation shortcut.
  closure:
    status: complete
    evidence: COPYRIGHT
    decision: Commit d3d702d0 established the canonical Kevin O'Shea and O'Shea & Sons, LLC
      ownership notice while retaining both MIT and Apache-2.0 license choices, propagated it
      through the README, handbook, website, release assembly, and install verification, and added
      the drift guard. The notice check, website static and browser suites, handbook check, shell
      validation, and requirement coverage pass at current HEAD.
  human_review_triggers: *id001
- id: WP-580
  name: DST foundation — simulator crate, simulated disk, redb backend adapter, determinism pin
  depends_on: [WP-478]
  objective: Begin ADR-0113 Phase 1 with a dev-only riffdb-sim crate whose seeded SimDisk
    (durable/volatile images, crash-torn unsynced writes, transient errors, capacity exhaustion)
    drives the pinned redb engine through redb's public StorageBackend seam, prove determinism
    with a versioned trace digest, and land the two commit-path hygiene items that freeze ambient
    nondeterminism out of conflict grouping and evaluation-pool sizing.
  requirements: [SIM-001, SIM-002, SIM-003, SIM-004, SIM-005, SIM-006, SIM-007]
  required_adrs: [ADR-0012, ADR-0113]
  allowed_paths: [Cargo.toml, Cargo.lock, SPEC.md, work_packages.yaml,
    adr/0113-deterministic-simulation-testing.md,
    crates/riffdb-sim/**, crates/riffdb-commit/src/command_execution.rs,
    crates/riffdb-commit/src/audit_executor.rs, crates/riffdb-commit/tests/architecture.rs,
    scripts/check-workspace-policy]
  deliverables:
  - Dev-only riffdb-sim crate (redb pinned at 4.1.0 as its only dependency, hand-rolled
    splitmix64, no clocks, no entropy crates, no hash-randomized collections) with SimDisk
    durable/volatile images, first-class crash and recovery, seeded keep/drop/prefix-truncate
    torn-write decisions at configurable granularity, transient errors, capacity exhaustion,
    and a versioned FNV trace chain over every operation and fault decision
  - SimBackend implementing redb's StorageBackend contract (zero-initialized growth,
    out-of-range refusal, fail-closed after close and across crash epochs) with a conformance
    suite
  - Phase-0 campaign; seeded single-table-per-transaction workloads under Durability::Immediate,
    crash-recover-continue loops with crashes placed inside the reopen window, a shadow BTreeMap
    oracle snapshotted at acknowledged durable commits with an explicit two-state acceptance
    while a commit is in flight, and the observed one-sync-per-Immediate-commit correspondence
    asserted from backend counters (a one-phase-commit precondition; two-phase commit doubles
    the sync count and the identity does not survive it)
  - Determinism pin (same seed twice, byte-identical trace and summary digests; different seed
    diverges) plus digest-sensitivity pins proving every fault family's decisions feed the
    versioned trace chain, plus a 32-seed sweep as the standing regression battery with
    live-fault-arm aggregate assertions
  - Architecture tests preventing riffdb-sim from entering any production dependency graph
    (driven from cargo metadata over the complete resolved member set, normal and build
    dependency kinds across all targets) and banning ambient time, entropy, and hash-randomized
    iteration from the simulator itself
  - Conflict-path HashSet-to-BTreeSet hygiene in riffdb-commit with an architecture pin, and the
    evaluation-pool worker count as an explicit constructor input with the
    available_parallelism-derived value as the production default at the existing call site
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-sim --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-runtime --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  deferred:
  - Full SIM-003 evidence (AuthoritativeCommandModel equality at the recovered durable frontier,
    with model extension and the startup-validation plus structural-inspection passes on every
    simulated recovery) belongs to WP-582, the ADR-0113 oracle-wiring package; this package
    delivers the engine-level shadow-map form only
  - SIM-004 evidence (simulator-found failures retained as seed-replayable regression fixtures
    replayed per merge, nightly exploration budget) delivered by WP-583 (found-seed corpus with
    per-merge replay and the env-gated deep-exploration budget)
  - SIM-006 evidence (every storage-layer storage_recovery_matrix / RECOVERY_SCENARIOS arm
    reproduced as a pinned simulation schedule) delivered by WP-583 (campaign-covered pinned
    schedules plus the closed typed-exclusion classification)
  exit_gate: The same seed replays a byte-identical versioned trace digest through the full
    crash-recover-continue campaign, every fault arm is provably reachable, torn unsynced state
    never bricks or diverges the reopened engine from the shadow oracle for any swept seed, no
    production crate can link the simulator, and the commit conflict path carries no
    hash-randomized collection.
  standing_design_tests:
    interface_safety: No application-facing surface changes; the simulator is a dev-only crate
      unreachable from client, CLI, MCP, or SDK paths, and its absence from production dependency
      graphs is pinned by an architecture test.
    scale: Simulated disk state is bounded by the configured workload and capacity, campaigns run
      in seconds on one core, and the sweep budget is fixed by seed count rather than database
      size.
  closure:
    status: complete
    evidence: adr/0113-deterministic-simulation-testing.md
    decision: Commit 15ae45ab delivered the dev-only seeded SimDisk/redb adapter, deterministic
      versioned trace and fault-sensitivity pins, crash-recover shadow oracle, 32-seed regression
      sweep, production dependency boundary, deterministic conflict grouping, and explicit
      evaluation-pool sizing. The simulator, commit, runtime, workspace-policy, and requirement
      batteries pass at current HEAD; the SIM-003, SIM-004, and SIM-006 items explicitly deferred
      here are closed by WP-582 and WP-583.
  human_review_triggers: *id001
- id: WP-581
  name: DST storage seams — journal media port, backend-parameterized open, first simulated store open
  depends_on: [WP-580]
  objective: Continue ADR-0113 Phase 1 by cutting the two production seams the simulator needs —
    an object-safe journal media port over the journal/checkpoint/spare/marker side-file family
    with a byte-identical real-filesystem default, and a hidden backend-parameterized RedbStore
    open that routes the format preflight, marker publication, and every journal side-file
    operation through one simulated media instance — then prove the seams with a shared media
    conformance suite and the first fully simulated store open.
  requirements: [SIM-005]
  required_adrs: [ADR-0012, ADR-0113]
  allowed_paths: [Cargo.lock, work_packages.yaml, crates/riffdb-sim/**,
    crates/riffdb-storage-redb/**]
  deliverables:
  - JournalMedia port in riffdb-storage-redb (doc-hidden, object-safe, with a MediaFile handle
    enum whose production arm is a plain std::fs::File so the journal worker's write/sync hot
    loop carries no dyn dispatch) threaded through the journal lane, extent lifecycle, scan and
    recovery paths, format-preflight probes, create-only marker publication, and every
    journal-adjacent store site, with RealJournalMedia preserving the exact prior syscalls and
    ordering — the existing journal, store, and format-preflight unit tests pass unmodified as
    the byte-identity proof
  - Hidden RedbStore::open_with_storage_media constructor (open_with_test_controller
    convention) accepting a redb StorageBackend engine backend plus a JournalMedia instance,
    selecting Builder::create_with_backend with the identical builder configuration, so a
    simulated open is fully simulated from the first filesystem touch; all five production open
    funnels keep the real-filesystem default
  - SimJournalMedia over SimDisk (namespace operations, sequential and positional data access,
    trace format version 3 with self-sufficient media refusal events and media fault-arm
    digest-sensitivity pins) plus a shared SIM-005 conformance suite run against BOTH the
    production filesystem implementation and the simulated one
  - First simulated RedbStore open smoke; a matrix-style fixed workload committed through the
    storage API over SimBackend plus SimJournalMedia, with clean-shutdown reopen and crash
    recovery under seeded torn-write resolution landing on the exact acknowledged state
    (semantic assertions only — store-level byte-exact replay is blocked on redb's hash-ordered
    pending_table_updates drain, documented in the smoke test; the seeded campaign and the
    reference-model oracle wiring are the following package)
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-targets
  - cargo +1.97.0 test -p riffdb-sim --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  exit_gate: Every pre-seam journal, store, format-preflight, and crash-matrix test passes
    unmodified against the threaded port; the shared media conformance suite passes against
    both implementations; the fully simulated store open commits, cleanly reopens, and
    crash-recovers onto the exact acknowledged state; and riffdb-sim stays absent from every
    production dependency graph.
  standing_design_tests:
    interface_safety: The port, handle types, and constructor are doc-hidden test surfaces
      unreachable from client, CLI, MCP, or SDK paths; production opens construct only the
      real-filesystem default, and the SIM-007 metadata-driven boundary test keeps the
      simulator out of every production dependency graph.
    scale: The media port adds one enum-dispatch branch on the journal hot loop's production
      arm and no allocation; simulated side-file state is bounded by the configured extent
      sizes and the smoke workload.
  closure:
    status: complete
    evidence: adr/0113-deterministic-simulation-testing.md
    decision: Commit d22c71a6 delivered the hidden JournalMedia and backend-parameterized open
      seams, byte-compatible real-filesystem default, SimJournalMedia, shared two-backend media
      conformance suite, and fully simulated store smoke with clean and crash recovery. Current
      storage, simulator, workspace-policy, and requirement batteries pass, and architecture tests
      continue to exclude simulator authority from every production dependency graph.
  human_review_triggers: *id001
- id: WP-582
  name: DST recovery oracle — AuthoritativeCommandModel equality at the recovered durable frontier
  depends_on: [WP-581]
  objective: Continue ADR-0113 Phase 1 by wiring the testkit reference model to the simulated
    redb store as the recovery oracle discharging SIM-003 — after every simulated recovery the
    reopened engine passes the full startup-validation and structural-inspection pass and its
    state equals AuthoritativeCommandModel at the recovered durable frontier, compared through
    the store's own validated readers with every representational normalization documented.
  requirements: [SIM-003]
  required_adrs: [ADR-0012, ADR-0113]
  allowed_paths: [Cargo.lock, work_packages.yaml,
    adr/0113-deterministic-simulation-testing.md,
    crates/riffdb-sim/**, crates/riffdb-testkit/**]
  deliverables:
  - DurableInspection accessor extension (additive, request-scoped, bounded) covering the four
    families the model is compared through but inspection could not read — secondary-index
    ranges with their atomically observed epochs, admissions by identity key through
    lookup_admission's locator-resolving decode, terminal outcomes assembled by sequence from
    the identity-keyed read surface, and the stored outbox intent record distinct from the
    status-read view — plus the recovered application frontier from the commit scan's frozen
    fence, structural page reach, and an inspect_opened_redb entry point for stores opened
    over simulated media
  - Model comparison support in riffdb-testkit; map iteration accessors, a first-divergence
    model diff naming the diverging family and key, and verify_model_against_inspection — a
    both-directions comparison with coverage checking (an under-declared harness fails loudly)
    and per-family agreement counts so a passing oracle proves a non-trivial surface
  - Recovery-oracle harness in riffdb-sim over the SIM-B seams; a mixed-admission-shape
    workload (two-phase admission, fused vacant-terminal, ADR-0083 supersession chain) driven
    in lockstep with the model and snapshotted at every acknowledged durable commit, recovered
    through clean shutdown, all-synced dirty-shutdown repair, and a full crash-window sweep
    with fused and two-phase interrupted commands under the two-state crash-boundary
    acceptance (recovered frontier within [last acknowledged, last attempted]; interrupted
    effects present exactly in full or absent exactly in full, both states provably exercised)
  - The smoke torn test's first-tearing-window break removed so its sweep also recovers and
    checks every crash window
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-testkit --lib
  - cargo +1.97.0 test -p riffdb-sim --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-targets
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  deferred:
  - The seeded workload generator and the open-ended exploration campaign with its regression
    corpus (SIM-004) delivered by WP-583; this oracle runs fixed fixtures over the existing
    crash-window sweep
  - Administration audit records and projection state remain outside the model's coverage;
    the comparison names the nine authoritative families it proves and the audit stream stays
    matrix-covered evidence
  - The index EPOCH record's schema binding is compared only as an epoch position; no public
    reader returns the stored epoch record, and its binding is independently validated at
    startup (HistoricalPersistedKeyEvidenceV1::from_index_epoch). Closed by a reader exposing
    the full stored epoch record or by comparing through that startup evidence surface.
    (Index ENTRY bindings are fully compared via scan_index_filtered; no residual there.)
  - The outbox intent comparison population is intents whose effective delivery state is
    still pending (scan_pending_outbox is the only public read exposing the stored intent
    record); the pending-population premise is enforced by the comparison, which refuses with
    NonPendingOutboxObserved rather than comparing a population it cannot see completely.
    Closed by a full-population intent read surface when a workload performs delivery
    transitions
  exit_gate: Every simulated recovery in the harness — clean, dirty-repair, and every crashing
    window of every swept seed — passes the complete startup-validation pass with zero
    structural findings over at least one inspected page and lands model-equal at the recovered
    frontier with the exact expected per-family agreement counts; the crash-boundary two-state
    acceptance is exercised from both sides; and no production crate changes.
  standing_design_tests:
    interface_safety: No application-facing surface changes; every new accessor lives in the
      dev-only testkit or simulator crates, reads through existing public store traits, and
      adds no mutation, repair, or migration authority to the inspection surface.
    scale: Inspection requests stay bounded by the existing 256-target and 10k-record limits,
      the oracle workload is four commands, and the sweep budget is fixed by the window range
      and seed count rather than database size.
  closure:
    status: complete
    evidence: adr/0113-deterministic-simulation-testing.md
    decision: Commit b766f43b delivered bounded durable-inspection accessors, bidirectional
      AuthoritativeCommandModel comparison with population accounting, and the fixed-workload
      recovery oracle across clean, dirty-repair, fused, and two-phase crash windows. Current
      simulator, testkit, and storage batteries prove startup validation, structural inspection,
      both crash-boundary outcomes, and model equality; the enumerated index-epoch, delivered
      outbox, and campaign-generation limits remain explicit rather than being treated as covered.
  human_review_triggers: *id001
- id: WP-583
  name: DST seeded campaign — workload generator, crash-schedule exploration, found-seed corpus, matrix subsumption
  depends_on: [WP-582]
  objective: Complete ADR-0113 Phase 1 (items 5-7) with a seeded workload generator over the
    compiled fixture contract family, an exploration campaign that drives the simulated store
    through generated plans with AuthoritativeCommandModel in lockstep under a crash schedule
    armed across bring-up, steady state, and recovery windows, a seed-replayable found-bug
    regression corpus replayed per merge, and the closed classification of every
    RECOVERY_SCENARIOS crash-matrix row as campaign-covered, typed-excluded, or out of the
    storage layer's scope.
  requirements: [SIM-004, SIM-006]
  required_adrs: [ADR-0012, ADR-0113]
  allowed_paths: [work_packages.yaml, adr/0113-deterministic-simulation-testing.md,
    crates/riffdb-sim/**]
  deliverables:
  - WorkloadPlan::generate(seed, config) in the seeded_campaign test target; a pure-value typed
    command sequence drawing command mix (creates, expected-version replaces, ADR-0083
    supersession chains of arbitrary depth), admission-shape mix (fused vacant-terminal and
    two-phase existing-pending), contention profile (capped fresh-versus-reuse targets),
    value-size distribution (a bounded note payload over a widened fixture contract), and
    target-disjoint atomic batch grouping, with identities derived via
    from_unix_milliseconds_and_random on seed material; determinism pinned by same-seed
    equality, cross-seed inequality, a golden identity digest under WORKLOAD_GENERATOR_VERSION
    (the draw-order falsifier), version and config sensitivity, structural invariants over 64
    seeds, and per-dimension non-vacuity
  - run_campaign_outcome; the plan driven against the simulated store (SIM-B seams) with the
    model in lockstep, the crash schedule armed from the end of first store creation through
    every recovery reopen, a full startup-validation and structural-inspection pass plus the
    SIM-C1 oracle comparison at the recovered frontier after every recovery with per-family
    agreement counts asserted against the model's own populations, two-state crash-boundary
    acceptance for interrupted batches (present or absent IN FULL; a partial batch is a named
    engine-defect panic), phase-one admissions, database initialization, and catalog
    activation, absent work re-admitted from the recovered frontier, deterministic termination
    via a crash budget, and failure output carrying seed, generator version, config, step
    index, and the named divergence
  - The campaign's first real engine catch, retained and dispositioned; redb 4.1.0 leaves file
    growth non-durable until the one-phase commit's single fsync, so a seeded torn crash can
    durably keep the in-commit god header while dropping the covering extension and every
    subsequent open panics (page_manager.rs:231) before redb's own repair path runs — fixed
    upstream in commit fd82ced (unreleased, the =4.1.0 pin predates it); carried as a typed,
    pin-guarded campaign placement exclusion (CAMPAIGN_PLACEMENT_EXCLUSIONS, keyed on
    REDB_PIN_CONTAINS_FD82CED with a manifest guard that reds when the pin moves), an
    ignored-by-default reproducer, and the corpus's inaugural entry whose expectation flips to
    clean recovery when the pin advances
  - SIM-004 corpus; typed entries (seed, generator version, config, what it caught, pin date,
    outcome expectations mapped to report counters and pinned non-vacuously) replayed per
    merge, seeded with the wedge entry plus three development-found interesting-territory
    seeds (heaviest torn recovery, the commit-present two-state resolution, deepest
    crash-during-recovery chain); an intentional durable-layout change that moves a physical
    crash window uses a receipted rotation that retains and replays the historical coordinate,
    names the exact causal commit and review date, appends a stable forward-only successor
    chain ending in an active witness, and forbids a green pre-fix corpus without at least one
    reproducing witness; env-gated deep
    exploration (RIFFDB_SIM_EXPLORE_SEEDS) as the open-ended budget, invocation documented in
    the module doc (no CI wiring exists on this machine)
  - SIM-006 subsumption; every RECOVERY_SCENARIOS row classified exactly once
    (campaign-covered pinned schedules for the command-commit and startup-initialization
    engine-commit arms with scouted, stability-rerun seeds; typed storage exclusions with
    verifiable reasons for the migration-batch arms and the no-crash-point owner typestate
    rows; out-of-scope classes for daemon-level, other-crate owner-package, and Phase-1
    real-filesystem-adapter rows), closed in both directions with kind-consistency rules so a
    new or reclassified row reds the guard until honestly classified; the fork+exec matrix
    itself untouched
  - The standing per-merge sweep; 24 scouted seeds under the commit-arms schedule with
    aggregate non-vacuity asserts (crashes, recoveries, torn decisions, recovery-window
    crashes, both commit two-state sides, admission resolutions) plus exact
    oracle-comparison accounting, wedged placements counted openly and bounded so exclusions
    cannot hollow the sweep
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-sim --all-features
  - cargo +1.97.0 test -p riffdb-testkit --lib
  - cargo +1.97.0 test -p riffdb-storage-redb --all-targets
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  deferred:
  - The migration.batch.* crash-matrix arms remain typed storage exclusions until a generator
    arm drives the migration control state
  - The redb file-growth wedge placement exclusion is removed by advancing the engine pin past
    upstream fd82ced and flipping REDB_PIN_CONTAINS_FD82CED, which also flips the corpus's
    inaugural entry to asserting clean recovery; the manifest guard makes the flip mandatory
    at the pin move
  - Store-level trace-digest determinism remains out of scope (redb's hash-ordered
    pending_table_updates drain and journal-worker draw interleaving, documented since SIM-B);
    the generator's command sequence is the pinned deterministic surface
  exit_gate: Every non-excluded campaign in the standing sweep, the corpus, and the covered
    pinned schedules completes its plan with the full startup-validation pass and the oracle
    holding at every recovered frontier; the sweep's aggregate counters prove crashes, torn
    recoveries, recovery-window crashes, and both commit two-state sides were exercised; the
    corpus replays per merge including the wedge reproduction under the current pin (with any
    physical-layout witness rotation retaining the old coordinate, exact causal receipt, and
    forward-only successor chain ending in a stable active witness); a new RECOVERY_SCENARIOS
    row or a moved redb pin reds the guards until classified; and no production crate changes.
  standing_design_tests:
    interface_safety: No application-facing surface changes; the generator, campaign, corpus,
      and classification live entirely in the dev-only simulator crate's test target and drive
      the store through existing hidden test seams.
    scale: Campaign state is bounded by the configured plan (commands, targets, note bytes)
      and the crash budget; inspection requests stay inside the 256-target and 10k-record
      limits; the sweep budget is fixed by seed count and finishes in under a minute.
  closure:
    status: complete
    evidence: adr/0113-deterministic-simulation-testing.md
    decision: Commit 4ac5ac40 completed ADR-0113 Phase 1 with the deterministic workload generator,
      crash-and-recovery campaign, model oracle at every recovered frontier, per-merge found-seed
      corpus, and closed RECOVERY_SCENARIOS classification. The current-head campaign and corpus
      pass with non-vacuous crash, torn-write, recovery-window, and both commit-outcome coverage;
      deep exploration remains intentionally environment-gated, typed exclusions remain visible,
      and the redb 4.2.0 pin guard has flipped the former fd82ced defect witness to clean recovery.
  human_review_triggers: *id001
- id: WP-590
  name: Vectors pillar — recon, VEC requirement registration, and design confirmation
  depends_on: []
  objective: Open the ADR-0091 implementation arc. Register the VEC-* requirement family in
    SPEC.md from the accepted and amended decision text (client-supplied embeddings through
    typed commands, typed staleness SLOs, the approximate-result contract with declared recall
    targets, filtered nearest-neighbor grammar with mandatory K and organization scope,
    policy-before-ranking, per-organization index statistics). Recon the current projection
    plane against every ADR-0086 obligation ADR-0091 inherits, produce the interface-first
    type plan for the ladder, and surface the two human-review decisions early with a written
    recommendation for each; neither may be resolved by implementation choice.
  requirements: []
  required_adrs: [ADR-0086, ADR-0087, ADR-0091]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/0091-native-vector-search-projections.md,
    docs/architecture/**]
  deliverables:
  - VEC-001..VEC-012 registered in SPEC.md with MUSTs traceable line-by-line to ADR-0091's
    amended decision and acceptance-criteria sections, each assigned across WP-591..WP-595
    under check-requirement-coverage
  - ADR-0091 Amendment 1 recorded (client-supplied embeddings; no database-owned loop)
  - A recon map (file:line) of the projection plane surfaces the ladder builds on, including
    the 0086 freshness/frontier/authorization transfer points
  - Human-review item one, the ANN structure decision — ruled exact-only-first with plan for
    first-party HNSW at WP-594
  - Human-review item two, the embedding endpoint boundary — ruled client-supplied; no
    endpoint config, no credential residence, no exposure-bound check
  - Confirmation of the WP-591..WP-595 decomposition with WP-592 rescoped
  acceptance_commands:
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: The VEC family is registered in SPEC.md and assigned across WP-591..WP-595 under
    check-requirement-coverage (this docs-only recon package discharges none itself); ADR-0091
    Amendment 1 and both human-review rulings are recorded; the recon map names real files.
  standing_design_tests:
    interface_safety: Documentation-only; no public surface changes.
    scale: Not applicable; no runtime code.
  closure:
    status: complete
    evidence: docs/architecture/vectors-recon-map.md
    decision: Commit ebe957e2 registered VEC-001 through VEC-012, recorded ADR-0091's
      client-supplied-embedding amendment and the exact-first/first-party-ANN rulings, mapped the
      inherited projection-plane authority and freshness seams, and fixed the WP-591 through WP-595
      decomposition. Requirement coverage and the complete public handbook pass at current HEAD.
  human_review_triggers: *id001
- id: WP-591
  name: Vectors contract surface — declared vector fields through grammar, IR, and compiler
  depends_on: [WP-590]
  objective: A contract declares a vector field on an entity (dimension, distance metric,
    source-field list, staleness SLO), and the compiler enforces that source fields exist on
    the entity and that dimension/metric/SLO are valid, with span-carrying diagnostics.
    Bindings and fixtures regenerate; no runtime behavior yet.
  requirements: [VEC-001]
  required_adrs: [ADR-0002, ADR-0086, ADR-0091]
  allowed_paths: [work_packages.yaml, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**, crates/riffdb-types/**,
    docs/contracts/**, fixtures/**]
  deliverables:
  - Grammar and AST for vector-field declaration with the ADR's exact vocabulary; language
    reference regenerated
  - Typed IR carrying dimension, metric, source fields, and staleness SLO, with versioned
    encoding and compatibility fixtures
  - Compiler rejection of invalid dimension, unknown metric, empty/nonexistent source fields,
    and non-positive staleness SLO with source-span snapshot plus semantic assertion per
    compiler-diagnostic house rule; negative examples added
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler
  - ./scripts/handbook check
  exit_gate: A contract declaring a vector_field compiles through the public
    compile_contract_source into a bundle that encodes, decodes, and content-addresses the
    dimension, metric, source fields, and staleness SLO (vector_contract.rs end-to-end tests);
    canonical tag 0x0e is pinned by golden fixtures with an exhaustiveness guard; every
    declared rejection carries a source-span snapshot; metric and staleness_slo names remain
    valid ordinary identifiers.
  standing_design_tests:
    interface_safety: Vector components are enforced into one canonical form (finite, -0.0
      canonicalized) at every construction and decode path, so equality, ordering, hashing,
      and durable digests agree; no surface accepts a NaN or infinite component.
    scale: Dimension is bounded by MAX_VECTOR_DIMENSION (4096, pinned in limits.rs); bundle
      spec sections are count-checked before allocation.
  deferred:
  - The vector JSON-schema node does not yet pin the exact byte length for its declared
    dimension; the schema is dimension-generic (validated at the value boundary instead)
  closure:
    status: complete
    evidence: docs/getting-started/VECTOR-SEARCH.md
    decision: Commits 2ce3d2b7 and 9f6a403c delivered contextual vector-field grammar, bounded
      canonical vector values, VEC metadata through versioned IR and bundle identity, and spanned
      compiler rejection for every invalid declaration. Current syntax, IR, compiler, compatibility,
      generation, and handbook checks pass; the intentionally dimension-generic JSON schema remains
      guarded by canonical value-boundary validation as declared above.
  human_review_triggers: *id001
- id: WP-592
  name: Vectors embedding write — typed command, staleness tracking, model-version observability
  depends_on: [WP-591]
  objective: An application writes a pre-computed embedding to a declared vector field through
    a typed command carrying the vector value, model identity, and model version. The command
    validates dimension against the contract, stores the vector as authoritative state with
    full provenance and idempotency, and the staleness surface tracks entities whose source
    fields outrun their embedding write. Model-version observability surfaces entities whose
    stored model version differs from the declared current. Health degrades when the staleness
    SLO is breached.
  requirements: [VEC-002, VEC-003, VEC-004, VEC-012]
  required_adrs: [ADR-0086, ADR-0091]
  allowed_paths: [work_packages.yaml, crates/riffdb-service/**, crates/riffdb-commit/**,
    crates/riffdb-runtime/**, crates/riffdb-contract-ir/**, crates/riffdb-types/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-storage-memory/**, docs/**, fixtures/**]
  deliverables:
  - Typed embedding-write command that carries vector value (f32 array matching declared
    dimension), model identity string, and model version string; dimension mismatch rejected
    with a typed diagnostic
  - Authoritative storage of the vector, model identity, model version, and the embedding
    write's commit sequence on the entity; provenance, idempotency, and commit atomicity
    identical to any other mutation
  - Typed staleness surface that counts and enumerates stale entities per vector field
    (source-field commit sequence newer than embedding-write commit sequence), queryable
    from the service layer
  - Model-version observability that counts and enumerates entities whose stored embedding
    model version differs from the contract's declared current version
  - SLO-breach health signal when stale count exceeds threshold
  - Provenance and idempotency assertions on embedding writes, dimension rejection evidence,
    and staleness correctness after interleaved source-field and embedding writes
  acceptance_commands:
  - cargo test -p riffdb-service -p riffdb-commit -p riffdb-runtime --all-features
  - cargo test -p riffdb-storage-redb --all-targets
  exit_gate: An embedding write lands through a typed command with provenance, idempotency,
    and dimension validation proven; staleness and model-version surfaces count and enumerate
    from authoritative state; the SLO-breach health signal is emitted and observed on both
    gRPC and MCP with ADR-0040 parity.
  standing_design_tests:
    interface_safety: No public surface may accept an embedding without model identity and
      version, and no wire path may pun a vector into another value kind.
    scale: Embedding writes are bounded by the declared dimension and the canonical document
      limits; staleness enumeration must be paginated under the standard query bounds.
  deferred:
  - Completion of the typed vector wire path, authoritative embedding consumer, count-based
    staleness surface, model-version enumeration, and VectorStaleness health parity is assigned
    to WP-596 under the maintainer rulings recorded 2026-08-11; no ruling remains open here.
  - Duration-based staleness (an entity stale longer than a declared duration) is a named future
    amendment; v1 enforces the declared stale-entity count threshold without clock machinery.
  closure:
    status: complete
    evidence: docs/architecture/vectors-projection-obligations.md
    decision: The WP-592 type foundation and the WP-596 production completion through commits
      90c9cd7c and 036d18be provide canonical dimension-checked embedding commands, atomic durable
      evidence with provenance and idempotency, bounded symbolic staleness/model-version inspection,
      and fail-closed VectorStaleness health across native, gRPC, MCP, CLI, and generated clients.
      Current production crate, real-server, generated-artifact, and recovery batteries pass;
      duration-based staleness remains the explicitly deferred future amendment.
  human_review_triggers: *id001
- id: WP-593
  name: Vectors exact tier — vector projection, exact KNN, nearest() with policy before ranking
  depends_on: [WP-592]
  objective: The vector projection consumes authoritative embeddings under every unamended
    ADR-0086 obligation (derived, org-partitioned, frontier-reporting, freshness-served,
    snapshot-rebuildable, replay-budgeted), and the nearest(field, $vector, k) operator lands
    with mandatory K and organization scope, composable with existing predicates where the
    filter and row-level policy both apply BEFORE distance ranking, served by exact KNN under
    ADR-0087 budgets as the reference path and small-partition default. No approximate
    structure in this package.
  requirements: [VEC-005, VEC-006, VEC-007, VEC-009, VEC-011]
  required_adrs: [ADR-0086, ADR-0087, ADR-0091]
  allowed_paths: [work_packages.yaml, crates/riffdb-columnar/**, crates/riffdb-projection/**,
    crates/riffdb-query-*/**, crates/riffdb-service/**, crates/riffdb-server/**, docs/**,
    fixtures/**]
  deliverables:
  - Vector projection registered through the 0086 plane; frontier semantics unchanged and
    proven by the existing frontier/crash invariants extended to the new projection kind
  - nearest() through grammar, plan, budgets, and wire, with mandatory K and org scope
    unrepresentable to omit; filtered-ANN semantics (filter before ranking) exact in the
    reference path
  - Adversarial policy-before-ranking evidence; unauthorized rows influence nothing
    (presence, scores, or timing beyond stated policy)
  - Exact-scan reference evaluator retained as the recall harness's ground truth for WP-594
  acceptance_commands:
  - cargo test -p riffdb-projection -p riffdb-columnar --all-features
  - cargo test --test projection_semantics
  exit_gate: nearest() compiles from RiffQL through plan to an org-scoped, K-bounded Nearest
    step (scopeless queries uncompilable), the columnar engine executes exact KNN with
    predicates applied before ranking (order-swap adversarial test), scan work is honestly
    charged at both plan and fuel layers, and exact KNN determinism anchors the WP-594
    recall harness.
  standing_design_tests:
    interface_safety: Organization scope and K are unrepresentable to omit on every layer
      that exists (program partition parameter, engine request field, resolver page bound);
      row stores refuse nearest steps as typed errors rather than serving unscoped scans.
    scale: K inherits the 499 page-take ceiling; the static plan cost charges the full
      partition-scan ceiling; the engine enforces its scan budget before allocation.
  deferred:
  - Completion of vector projection lifecycle/frontier/freshness/rebuild/replay integration,
    the production QueryReadView nearest adapter with the shared 500-row scan ceiling and
    policy before ranking, parameterized-K binding, and typed vector result transport is
    assigned to WP-596 under the maintainer rulings recorded 2026-08-11.
  closure:
    status: complete
    evidence: docs/architecture/vectors-projection-obligations.md
    decision: Commits 8bcb4271 and 9f6a403c established the exact bounded reference evaluator and
      filter/policy-before-ranking semantics; WP-596's production series ending at 036d18be wired
      the same compiler-owned projection through authoritative frontiers, freshness, rebuild,
      replay, parameterized K, and public transports. Current projection semantics, real-server
      vector acceptance, production crate, recovery, and generation tests pass with organization
      scope, K, and the 500-row scan bound unrepresentable to omit.
  human_review_triggers: *id001
- id: WP-594
  name: Vectors approximate tier — first-party ANN structure under the declared-recall contract
  depends_on: [WP-593, WP-596]
  objective: A first-party approximate structure engages above a declared row threshold, per
    organization, meeting each projection's declared recall target measured against exact scan
    at the same frontier; maintenance and compaction are bounded by recall regression at a
    frozen frontier, not byte equality; all result-shaping statistics (entry points,
    centroids, codebooks) are computed per organization so no tenant's data influences
    another's rankings or timings.
  requirements: [VEC-008, VEC-009, VEC-010]
  required_adrs: [ADR-0002, ADR-0086, ADR-0091]
  allowed_paths: [work_packages.yaml, crates/riffdb-columnar/**, crates/riffdb-projection/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-catalog/**, crates/riffdb-commit/**,
    crates/riffdb-cli/assets/editor/**, docs/**, fixtures/**, Cargo.toml, Cargo.lock, deny.toml]
  scope_expansion: Contract syntax, IR, and compiler are included solely because VEC-009 and
    VEC-010 require the ANN row threshold and recall target to be declared on the projection;
    the original ladder did not anticipate ownership of those declarations. Maintainer approval
    on 2026-08-15 additionally admits the optional `ann_threshold UInt, recall_target_bps UInt`
    vector-field clause through additive grammar/IR/bundle V12, and expands catalog and commit
    ownership only far enough to validate and audit that exact version. Existing bundles remain
    byte-identical; ANN metadata does not change authoritative index derivation. Maintainer
    approval on 2026-08-15 also admits `crates/riffdb-cli/assets/editor/**` only for the generated
    highlight snapshot made stale by the new contextual vector-field words.
  deliverables:
  - First-party HNSW implementation with per-organization graph isolation, no external
    dependency (forbid(unsafe_code) preserved)
  - Contract declarations for the per-organization ANN row threshold and recall target, with
    bounded values, versioned encoding, span-carrying diagnostics, and compatibility fixtures
  - Declared recall target enforcement in the acceptance harness (recall@K against exact scan
    at matched frontiers across randomized histories)
  - Maintenance bounded by recall regression at a frozen frontier, harness-proven
  - Per-organization statistics isolation evidence
  - Small-partition default routing (exact below the declared threshold) with the threshold a
    declared, observable property
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler
  - cargo test -p riffdb-catalog -p riffdb-commit --all-features
  - cargo test -p riffdb-columnar -p riffdb-projection --all-features
  - cargo deny check
  exit_gate: The approximate tier meets each projection's declared recall target against
    exact scan at matched frontiers, engages only above the declared per-organization row
    threshold, and computes every result-shaping statistic per organization.
  standing_design_tests:
    interface_safety: No public surface may opt out of the recall declaration or reach
      another tenant's statistics.
    scale: Graph memory and maintenance work bounded per organization; recall harness
      budgeted.
  deferred:
  - Durable vector graphs are intentionally absent in the POC. The first-party graph is
    deterministically rebuilt from the org-scoped visible snapshot, so compaction correctness
    is measured as recall at a frozen frontier rather than byte equality.
  closure:
    status: complete
    evidence: docs/performance/wp-594-vector-ann.md
    decision: The first-party deterministic HNSW tier, compiler-owned threshold and recall
      declarations, per-organization and policy isolation, exact small-partition routing, and
      frozen-frontier recall evidence are merged in commit 5dcc6e8f. WP-596 subsequently wired
      the same registered definition into the production projected-nearest path without adding
      a caller-selectable tier.
  human_review_triggers: *id001
- id: WP-595
  name: Vectors pillar acceptance — end-to-end evidence and handbook graduation
  depends_on: [WP-594]
  objective: Discharge ADR-0091's amended acceptance-criteria list end to end and graduate the
    pillar into the handbook as a user-visible surface. Every criterion lands as a named,
    replayable evidence artifact; the pillar's public surfaces satisfy the interface-safety
    standing test (no expressible unsafety — no unscoped nearest, no silent staleness, no
    policy bypass, no opt-out of the recall declaration).
  requirements: [VEC-005, VEC-007, VEC-008, VEC-010, VEC-011]
  required_adrs: [ADR-0086, ADR-0091, ADR-0113, ADR-0136]
  allowed_paths: [work_packages.yaml, crates/riffdb-sim/**, crates/riffdb-testkit/**,
    docs/**, examples/**, fixtures/**]
  deliverables:
  - Recall-at-matched-frontier evidence across randomized histories; embedding-write
    dimension validation and staleness correctness under interleaved writes; model-version
    observability evidence; adversarial policy and statistics-isolation reruns;
    replay-budget detach proof
  - Handbook pages (concepts + reference + quickstart addition) through ./scripts/handbook
    check, examples using current public interfaces only
  - An agent-legibility pass on every new public error and diagnostic (error text works as a
    prompt)
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/handbook check
  - ./scripts/check-requirement-coverage
  exit_gate: Every ADR-0091 amended acceptance criterion is discharged by a named,
    replayable evidence artifact, and the pillar's public surfaces pass the
    interface-safety standing test end to end.
  standing_design_tests:
    interface_safety: No expressible unsafety on any pillar surface — no unscoped nearest,
      no silent staleness, no policy bypass, no recall opt-out.
    scale: Evidence harnesses bounded and replayable per seed.
  deferred:
  - Durable vector graphs remain outside the POC; the accepted approximate graph is rebuilt
    ephemerally from one policy-admitted organization snapshot.
  closure:
    status: complete
    evidence: docs/architecture/wp-595-vector-acceptance.md
    decision: The acceptance ledger binds every VEC-005/VEC-007/VEC-008/VEC-010/VEC-011
      obligation to semantic tests from WP-594 and WP-596, and the public handbook now teaches
      the actual automatic exact/ANN routing, staleness inspection, freshness, and typed
      correction paths without exposing tier selection or raw storage identities.
  human_review_triggers: *id001
- id: WP-596
  name: Vectors prerequisites completion — typed ingress, staleness, exact-tier reachability
  depends_on: [WP-593]
  objective: Close the deferred load-bearing WP-592/WP-593 surfaces before the approximate
    tier — authoritative typed embedding ingress over native, gRPC, MCP, and CLI paths;
    count-threshold staleness and model-version observability with a parity-preserving health
    signal; vector projection lifecycle and exact nearest reachability with honest bounded
    scan charging and policy before ranking; and parameterized K bound at execution.
  requirements: [VEC-001, VEC-002, VEC-003, VEC-004, VEC-005, VEC-006, VEC-007, VEC-011,
    VEC-012]
  required_adrs: [ADR-0011, ADR-0040, ADR-0041, ADR-0086, ADR-0087, ADR-0091,
    ADR-0136]
  allowed_paths: [work_packages.yaml, SPEC.md,
    adr/README.md, adr/0136-authoritative-vector-evidence-and-projected-nearest.md,
    adr/0011-canonical-values-keys-and-hashing.md,
    adr/0040-public-grpc-mcp-parity-bridge.md,
    adr/0041-cli-public-flow-and-output-contract.md, proto/**, fixtures/**,
    crates/riffdb-types/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    # Coordinator-authorized 2026-08-21: production V15 bundles must cross the
    # catalog's exact version trust boundary before any public command can run.
    crates/riffdb-catalog/**,
    # Coordinator-authorized 2026-08-21: ADR-0136 requires the shared policy
    # operation and decision path to authorize the exact symbolic vector
    # projection before inspection and again before result release.
    crates/riffdb-policy/**,
    # Maintainer-authorized path expansion: the additive public application
    # operation needs the shared typed error-envelope operation identity.
    crates/riffdb-errors/**,
    # Coordinator-authorized 2026-08-21: the new closed application-service
    # operation must retain the existing fixed-cardinality metric label rather
    # than disappear from production observability.
    crates/riffdb-observability/**,
    crates/riffdb-service/**, crates/riffdb-commit/**, crates/riffdb-runtime/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**, crates/riffdb-cli/**,
    crates/riffdb-client-rust/**,
    # Coordinator-authorized 2026-08-21: ADR-0136 requires generated
    # Rust/Go/TypeScript/Python embedding inputs and the shared closed driver
    # registry must carry the canonical vector value those facades emit.
    crates/riffdb-query-module/**, crates/riffdb-driver-host/**, clients/**,
    # Maintainer-authorized path expansion: the generated Python binding uses
    # the first-party native bridge, which must map the same closed inspection
    # operation instead of reimplementing protocol or authorization in Python.
    crates/riffdb-client-python-native/**,
    crates/riffdb-server/**, crates/riffdb-columnar/**, crates/riffdb-projection/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-executor/**, docs/**, tests/**,
    # Maintainer-authorized path expansion 2026-08-21: generated vector
    # inspection facades rotate exact example locks and checked-in clients.
    examples/**, release/**]
  deliverables:
  - VEC-001 amended to define the v1 staleness SLO as a declared stale-entity count threshold;
    duration-based staleness remains a named future amendment
  - Typed protobuf vector value with max-known-field pins, compatibility fixtures, and the same
    canonical finiteness and declared-dimension validation as native ingress
  - Authoritative embedding storage with provenance/idempotency, paginated staleness and
    model-version enumeration, and a new VectorStaleness health component mapped in gRPC and
    MCP lockstep under the additive ADR-0040 amendment
  - Canonically round-tripping CLI vector JSON shape ratified by additive ADR-0041 amendment;
    ADR-0011 durable canonical tag 0x0e amendment ratified as written
  - Production vector projection consumer and QueryReadView adapter preserving ADR-0086
    frontier/freshness/rebuild/replay semantics, shared 500-row scan ceiling, and row-policy
    filtering before distance computation and ranking
  - nearest(field, $vector, $k) compiled and bound end to end; K=499 succeeds while K=0 and
    K=500 return typed span/bind errors; literal and parameter bounds share one definition
  - DotProduct non-finite comments accurately document the deliberate defensive ranking
    tradeoff without changing the independently reviewed behavior
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler
    -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-executor --all-features
  - cargo test -p riffdb-service -p riffdb-commit -p riffdb-runtime -p riffdb-server
    -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-cli --all-features
  - cargo test -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-columnar
    -p riffdb-projection --all-features
  - ./scripts/check-generated
  - ./scripts/handbook check
  - ./scripts/check-requirement-coverage
  exit_gate: A remote application can write a canonical dimension-checked embedding and
    observe typed staleness/model-version state and VectorStaleness health with gRPC/MCP
    parity; exact nearest is production-reachable under policy and shared scan bounds with
    literal or parameter K; all projection frontier/crash/replay guarantees remain intact.
  standing_design_tests:
    interface_safety: Organization scope, embedding metadata, dimension/finiteness checks,
      staleness visibility, policy-before-ranking, and K bounds are structural or typed on
      every public path; no transport can opt out of them.
    scale: Vector dimension, stale/model enumeration pages, exact scans, K, projection replay,
      diagnostics, and all wire collections are bounded before allocation or work.
  deferred:
  - Duration-based staleness is a future ADR amendment; v1 intentionally requires no clock.
  - ANN threshold and recall-target declarations plus the first-party approximate structure
    belong to WP-594, whose contract-owner path expansion is recorded above.
  closure:
    status: complete
    evidence: docs/architecture/wp-595-vector-acceptance.md
    decision: The production vector prerequisite series from commit 90c9cd7c through 036d18be
      completed canonical typed ingress, durable evidence and inspection, health parity, generated
      clients, projection registration/lifecycle, and exact projected-nearest execution under
      authorization and bounded scan work. The full current-head package union, real gRPC server
      vector path, projection semantics, recovery matrix, generated artifacts, handbook, and
      requirement coverage pass; WP-594 owns the separately closed approximate tier.
  human_review_triggers: *id001
- id: WP-597
  name: Secret field classification with structural display-surface redaction
  depends_on: [WP-573]
  objective: Implement ADR-0118. A field may be declared secret; the classification rides
    AST, versioned IR, bundle hash, catalogs, and generated bindings; every display surface
    (tracing, typed public errors and diagnostics, MCP text, CLI rendering, provenance and
    audit summaries, telemetry) consumes a redacting value wrapper so unredacted rendering is
    unrepresentable; capability field visibility excludes secret fields from wildcard and
    default grants; durable storage, backups, exports, and changelog frames remain
    full-fidelity under existing authority rules. Register the SEC-F requirement family.
  requirements: [SECF-001, SECF-002, SECF-003, SECF-004, SECF-005]
  required_adrs: [ADR-0111, ADR-0117, ADR-0118]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/0118-secret-field-classification.md,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-types/**, crates/riffdb-policy/**,
    crates/riffdb-errors/**, crates/riffdb-service/**, crates/riffdb-cli/**,
    crates/riffdb-api-mcp/**, crates/riffdb-observability/**, docs/**, fixtures/**,
    # Fix-round expansion, coordinator-authorized (2026-08-11): persisting the
    # grant's dedicated secret-field naming and giving it a production minting
    # surface requires the durable capability record V6 and the public wire
    # field, which live in the proto/codec/ingress crates below.
    proto/**, crates/riffdb-proto/**, crates/riffdb-storage-api/**,
    crates/riffdb-api-grpc/**, tests/**, release/durable-format-manifest-v1.json]
  deliverables:
  - secret keyword through grammar, AST, versioned IR with compatibility fixtures, bundle
    hash, generated catalogs and bindings, with source-span diagnostics
  - The redacting wrapper consumed by every display surface, with a per-surface redaction
    sweep test asserting value absence and marker presence
  - Field-visibility default-deny for secret fields with explicit-grant reveal, predicates
    and unique enforcement functioning without read visibility
  - Handbook page stating the classification's promise and its explicit non-promises (no
    cryptography), plus a negative example
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler
  - cargo test -p riffdb-service -p riffdb-cli --all-features
  - ./scripts/handbook check
  standing_design_tests:
    interface_safety: Secret classification is compiler-carried and default-denied; no display
      surface, wildcard grant, diagnostic, or generated example can reveal a secret value unless
      the exact public read surface requires explicit field authority, while durable carriage stays
      full-fidelity and cannot silently redact stored state.
    scale: Classification and redaction are bounded static field metadata plus existing visibility
      checks; no row scan, retained secret set, or whole-application runtime registry is introduced.
  closure:
    status: complete
    evidence: docs/security/SECRET-FIELDS.md
    decision: Commit 03199e45 completed secret classification through syntax, V8 IR, bundle and
      capability identity, durable V6 secret naming, structural default-deny visibility, explicit
      reveal authority, and redaction across public errors, diagnostics, CLI, MCP, telemetry,
      provenance, and audit summaries. Current compiler, service, CLI, transport, architecture,
      generated-artifact, recovery, and handbook tests pass; full-fidelity authorized persistence,
      backup, export, and changelog carriage remains distinct from display rendering.
  human_review_triggers: *id001
- id: WP-598
  name: Framework-profile prerequisite verification for the rescoped alpha gate
  depends_on: [WP-597]
  objective: Verify or deliver the generic capabilities ADR-0117 names so the rescoped gate
    (Better Auth shape) is honestly reachable, without any framework-specific code entering
    first-party crates. Each item concludes as VERIFIED-with-named-test, DELIVERED-small, or
    ESCALATED-as-own-package with evidence.
  requirements: []
  required_adrs: [ADR-0107, ADR-0109, ADR-0117]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/0109-compiled-workflow-concurrency.md,
    adr/README.md, docs/**, fixtures/**, scripts/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-runtime/**, crates/riffdb-invariant/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-testkit/**, tests/**]
  deliverables:
  - Unique-constraint enforcement verified transactional and conflict-integrated under
    concurrent admission (named tests; the gate workload's unique-identity fixture)
  - Single-use expiring token pattern proven expressible as compiled commands
    (consume-once mutation plus invariant comparing a stored timestamp against transaction
    logical time), with a replay-refusal and expiry-refusal fixture; smallest compiler or
    invariant addition delivered here if expressibility falls short
  - Atomic user/account/session workflow shape proven over ADR-0107/0109 machinery with a
    concurrent refresh/revocation race test
  - Adapter-minted idempotency documented as the keyless-retry pattern over existing client
    identity machinery, with a driver-level test
  - Gate workload-shape fixtures for the alpha corpus (framework-agnostic) covering the
    above, wired into the acceptance path that replaced the Payload shape
  acceptance_commands:
  - cargo test --workspace --all-features
  - ./scripts/check-requirement-coverage
  standing_design_tests:
    interface_safety: A framework profile can dispatch only to generated named operations and must
      reject unsupported plugins, dynamic schemas, generic stores, and transaction callbacks at
      generation time; sequential commands can never be reported as one atomic framework action.
    scale: Profile generation and verification operate over bounded declared plugin schemas and
      finite operation catalogs; runtime dispatch is a bounded symbolic lookup and adds no generic
      query, callback, or whole-database mechanism.
  closure:
    status: complete
    evidence: docs/architecture/WP-598-FRAMEWORK-PROFILE-VERIFICATION.md
    decision: The framework-neutral profile verifies transactional uniqueness, exact logical-time
      expiry, atomic workflow graphs, adapter-minted idempotency, and the one-time secret handout.
      WP-606 and WP-607 discharged the two deliberately escalated coordinator and unique-delete
      boundaries with the original race and ownership assertions intact.
  human_review_triggers: *id001
- id: WP-599
  name: Compiler-owned workflow reconstitution and all-domain portability closure
  depends_on: [WP-568, WP-570, WP-572, WP-574, WP-597]
  objective: Implement accepted ADR-0119 so an exact, not-ready application-reimport campaign can
    recreate complete workflow entity state from a canonical export through a distinct create-only
    compiled command class, while ordinary application roles remain unable to set workflow state or
    restore leases, then close WP-575's four-domain and all-language acceptance gate.
  requirements: [API-001, SEC-001, SEC-002, SEC-003, SAFE-001, SAFE-002, SAFE-003,
    SAFE-006, SAFE-007, REC-001, REC-002, WF-001, WF-002, WF-003, WF-004,
    WF-005, WF-008, WF-009, WF-010, APE-001, APE-002, APE-003, APE-004,
    APE-005, APE-009, APE-010, APE-011, APE-012, EXP-011, EXP-012, EXP-013,
    EXP-014]
  required_adrs: [ADR-0003, ADR-0005, ADR-0055, ADR-0072, ADR-0080, ADR-0106,
    ADR-0107, ADR-0109, ADR-0110, ADR-0111, ADR-0112, ADR-0115, ADR-0118,
    ADR-0119]
  allowed_paths: [Cargo.lock, SPEC.md, work_packages.yaml,
    adr/README.md, adr/0119-compiler-owned-workflow-reconstitution.md, proto/**, fixtures/proto/**,
    fixtures/export/**, fixtures/adapters/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-catalog/**, crates/riffdb-query-module/**,
    crates/riffdb-idempotency/**, crates/riffdb-commit/**,
    crates/riffdb-types/**, crates/riffdb-auth/**, crates/riffdb-policy/**,
    crates/riffdb-runtime/**, crates/riffdb-service/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-application/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-testkit/**, clients/**, examples/**, tests/**, scripts/**, docs/**,
    release/**]
  deliverables:
  - Versioned reimport-command grammar/IR/lock identity and compiler diagnostics for exact complete
    create-only records, compiler-derived dependencies, and inaccessible ordinary command surfaces
  - Workflow reconstitution preserves state/fence/attempts only from a quiescent null-owner/expiry
    snapshot, resets destination revision explicitly, requires the first destination claim to mint a
    fence strictly greater than the preserved nonzero fence, and rejects active/partial leases before
    mutation
  - Portability-intent export binds the exact portability manifest, records canonical per-workflow
    lease-quiescence evidence while the source remains available, publishes no completed portability
    receipt for active or partially cleared leases, and prevents a general export from being upgraded
    into reimport authority
  - Distinct bounded Capability V7 successor (verified as the next unused slot at the merge base) and
    start/page/status/cancel service, TLS gRPC, Rust operator, CLI, driver-host, and generated
    Go/TypeScript/Python operator bindings with no app-role discovery
  - Durable not-ready campaign checkpoints, deterministic record-derived idempotency, exact page hashes,
    crash resumption, typed partial failure/cancel, final validation/reconciliation, and credentials only
    after readiness publication
  - MLflow and Woodpecker portability fixtures with noninitial workflow state and nonzero quiescent fences,
    plus OpenFGA, MLflow, Better Auth, and Woodpecker completing export/reimport and post-import
    language observations; retained Payload remains a post-alpha regression fixture
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-runtime -p riffdb-service --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test application_export --all-features
  - ./scripts/adapter-export-reimport-acceptance --all-domains --all-languages
  - ./scripts/generate-proto --check
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: An empty not-ready database is reconstructed through only exact compiled reimport
    commands, including quiescent MLflow/Woodpecker workflow rows, and becomes ready only after
    receipted four-domain reconciliation while every ordinary workflow-state/import bypass remains
    unrepresentable.
  standing_design_tests:
    interface_safety: Reconstitution cannot be invoked or emulated by an application role, cannot
      target an existing row, select a state transformation, restore a live lease, choose ordering or
      idempotency, or publish a partial destination; raw entity/storage import remains absent.
    scale: Canonical pages, compiler-derived dependency schedules, command groups, checkpoints,
      diagnostics, observations, and retained replay state are bounded and stream without whole-state
      memory; unsupported dependency cycles fail compilation.
  closure:
    status: complete
    evidence: release/evidence/alpha
    decision: The compiler-owned create-only reimport class, distinct V7 authority, resumable
      not-ready campaign, quiescent workflow restoration, exact reconciliation, and generated
      Rust/Go/TypeScript/Python operator paths are merged. OpenFGA, MLflow, Better Auth, and
      Woodpecker carry receipted export/reimport observations; Payload remains the named regression
      fixture.
  human_review_triggers: *id001
- id: WP-600
  name: Sticky secret classification with declared reveals annotations
  depends_on: [WP-597]
  objective: Implement ADR-0118 Amendment 1. A secret-classified value flowing into a
    non-secret contract destination is a compile error unless the flow site carries an
    explicit reveals annotation naming the source field; diagnostics carry spans for both
    ends; the annotation rides AST, versioned IR, bundle hash, and generated catalogs; the
    handbook's laundering section is rewritten around the annotation; the gate workload's
    token-handout fixture becomes the canonical annotated example.
  requirements: [SECF-006]
  required_adrs: [ADR-0117, ADR-0118]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/0118-secret-field-classification.md,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-catalog/src/bundle.rs,
    crates/riffdb-commit/src/command_index.rs, docs/**, fixtures/**,
    scripts/generate-query-clients]
  deliverables:
  - Flow analysis from secret-classified fields to contract-declared destinations in the
    compiler, fail-closed with two-ended span diagnostics
  - The reveals annotation through grammar, AST, versioned IR with compatibility fixtures,
    and bundle hash under the exact-identity ceremony
  - SECF requirement additions registered; handbook and negative examples updated; the
    annotated token-handout fixture
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler
  - cargo test -p riffdb-catalog --lib
  - cargo test -p riffdb-commit --lib command_index
  - ./scripts/check-generated
  - ./scripts/handbook check
  - ./scripts/check-requirement-coverage
  exit_gate: Every compiler-visible secret flow into a non-secret entity field, durable event
    field, or outcome field either carries the exact bounded source-naming reveals declarations
    in syntax and V11 plan identity or fails compilation with both source and destination spans;
    no runtime caller can add a reveal or use one to widen read visibility.
  standing_design_tests:
    interface_safety: A secret-to-non-secret contract flow is unrepresentable without an exact
      source-naming reveals annotation checked by the compiler; the annotation is reviewable static
      authority and cannot be supplied dynamically or used to bypass read visibility.
    scale: Secret-flow analysis is bounded by the finite compiled contract graph and emits bounded
      two-ended diagnostics; it adds no per-row runtime traversal or retained value tracking.
  closure:
    status: complete
    evidence: docs/security/SECRET-FIELDS.md
    decision: Commit ff4ff910 carries source-naming reveals through syntax, AST, V11 executable
      identity, catalogs, diagnostics, compatibility fixtures, and the Better Auth token handout.
      Unannotated, incomplete, indirect, duplicate, and runtime-supplied disclosure authority remain
      unrepresentable.
  human_review_triggers: *id001
- id: WP-601
  name: Driver distribution — platform binaries and published runtimes across four registries
  depends_on: []
  objective: Publish the four-ecosystem toolchain per ADR-0120 section 1. CLI packages
    carrying prebuilt platform binaries (npm, PyPI, Go, crates.io; esbuild-pattern platform
    resolution), published runtime packages replacing generated-code vendoring, release
    machinery with checksums and signatures, and the durable-format compatibility statement
    surfaced at the package boundary. Rust remains the sole semantics implementation
    (ADR-0106); packages are distribution vessels.
  requirements: [DX-026, DX-027, DX-028, DX-029, DX-030]
  required_adrs: [ADR-0106, ADR-0112, ADR-0120]
  allowed_paths: [.github/workflows/driver-distribution.yml, work_packages.yaml, SPEC.md,
    docs/**, clients/**, fixtures/driver/**, scripts/**, crates/riffdb-api-grpc/**,
    crates/riffdb-cli/**, crates/riffdb-client-*/**, crates/riffdb-proto/**,
    crates/riffdb-storage-api/**, release/**]
  deliverables:
  - Platform-binary CLI packages for npm and PyPI plus install paths for Go and Rust, with a
    release build matrix, checksums, and signing
  - Published runtime packages; generate emits registry imports instead of vendor copies,
    with the vendored path retained for offline/sealed installs
  - Package READMEs carrying the four-step story; package-arrival smoke scripts per ecosystem
  acceptance_commands:
  - ./scripts/check-driver-distribution
  - ./scripts/check-generated
  - cargo test -p riffdb-cli --all-features
  - cargo test -p riffdb-proto -p riffdb-api-grpc -p riffdb-storage-api --all-features
  - ./scripts/driver-distribution-acceptance
  - ./scripts/handbook check
  - ./scripts/check-requirement-coverage
  standing_design_tests:
    interface_safety: Packages carry only the existing generated application boundary and
      compiler-owned CLI verbs; no target runtime or package manager receives a transaction,
      numeric-ID, raw transport, authorization, or durability escape hatch.
    scale: The platform, package, file, archive, output, and dependency inventories are closed
      and bounded; package arrival uses one finite topologically ordered Rust closure and one
      finite smoke per named ecosystem.
  closure:
    status: complete
    evidence: docs/getting-started/PACKAGE-INSTALLATION.md
    decision: The signed x86_64 package-arrival gate passes the native CLI, TypeScript, Python,
      Go, and Rust cells from the emitted local registries without source-tree fallback. The
      distribution manifest now binds the current durable-format manifest, and the arrival
      fixture names driver protocol V3 after the accepted runtime rotation. The two-platform
      release workflow remains the publication owner; this closure does not publish packages.
  human_review_triggers: *id001
- id: WP-602
  name: Database-shaped verbs — init, push, status, diff, migrate over riffdb.toml
  depends_on: []
  objective: Implement ADR-0120 section 2. riffdb init writes minimal config and schema into
    new or existing projects without application scaffolding; push folds check/lock/deploy
    with the acceptance ceremony surfaced only on compiler-owned identity change; status and
    diff report installed-versus-local state; migrate surfaces the existing staged-migration
    machinery for incompatible successors. No existing verb changes; no gate weakens.
  requirements: [DX-013, DX-014, DX-015, DX-016, DX-017, DX-018, DX-019, DX-020]
  required_adrs: [ADR-0057, ADR-0110, ADR-0120, ADR-0121]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, fixtures/**, crates/riffdb-cli/**,
    crates/riffdb-client-rust/**, crates/riffdb-query-module/**,
    adr/0121-empty-project-schema-and-selected-binding-materialization.md]
  deliverables:
  - riffdb.toml (database alias or URL, schema path, generator targets) with typed
    diagnostics; init idempotent in existing projects
  - push with ceremony-on-change-only (no-op push prompts nothing; changed lock requires
    acceptance; incompatible change refuses toward migrate with a typed pointer)
  - status/diff against the configured database; migrate wrapping the staged path with its
    existing gates
  - Ceremony boundary tests for all three push outcomes
  - Domain-empty symbolic application support plus project-selected language artifact
    materialization, without changing persistent lock or IR encodings
  acceptance_commands:
  - cargo test -p riffdb-cli --all-features
  - cargo test -p riffdb-query-module --all-features
  - ./scripts/handbook check
  closure:
    status: complete
    evidence: docs/getting-started/DATABASE-WORKFLOW.md
    decision: The database-shaped init, push, status, diff, and migrate workflow is merged in
      commit 39c9e947. Exact CLI and query-module tests prove domain-empty initialization,
      project-selected materialization, read-only status/check behavior, ceremony only on changed
      compiler-owned identity, and typed migration routing without changing existing verbs.
  human_review_triggers: *id001
- id: WP-603
  name: Self-describing database — agent init, MCP guidance resources, generated teaching artifacts
  depends_on: [WP-602]
  objective: Implement ADR-0120 section 3. riffdb agent init installs the skill file, an
    AGENTS.md section describing the schema-push-generate-query loop, and MCP configuration
    into the invoking repository; the daemon serves version-exact application-specific
    guidance as authorization-filtered MCP resources beside the existing catalog; the generic
    surface distillation (llms.txt convention) and quickstarts are generated from one in-repo
    source with a check-generated referee and shipped in packages and the docs site.
  requirements: [DX-021, DX-022, DX-023, DX-024, DX-025]
  required_adrs: [ADR-0008, ADR-0046, ADR-0117, ADR-0120, ADR-0122]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, fixtures/**, crates/riffdb-cli/**,
    crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**, crates/riffdb-service/**, scripts/**,
    adr/0122-agent-rails-and-application-guidance-resource.md]
  deliverables:
  - riffdb agent init writing skill, AGENTS.md section, and MCP config, idempotent and
    scoped to the invoking repository
  - Guidance MCP resources generated from the deployed application's catalog, authorization-
    filtered, bounded, with registry/schema fixtures per house MCP rules
  - The single-source distillation generator with drift refereed by check-generated
  acceptance_commands:
  - cargo test -p riffdb-cli --all-features
  - cargo test -p riffdb-api-mcp --all-features
  - cargo test -p riffdb-mcp-stdio --all-features
  - ./scripts/check-generated
  - ./scripts/handbook check
  closure:
    status: complete
    evidence: docs/getting-started/AGENT-DATABASE-QUICKSTART.md
    decision: The repository-scoped agent-init rails, authorization-filtered application guidance
      MCP resources, and single-source generated teaching artifacts are merged in commit
      3b3f3038. CLI, hosted/stdio MCP, generated-artifact, and handbook gates pass with no storage
      or policy authority in the guidance surface.
  human_review_triggers: *id001
- id: WP-604
  name: One-story conformance — cross-driver matrix, pinned step counts, campaign-03
  depends_on: [WP-601, WP-602, WP-603]
  objective: Implement ADR-0120 section 4. A cross-driver conformance matrix proves the same
    schema pushed and generated in all four languages yields agreeing operation identities,
    outcomes, idempotency behavior, and public errors; quickstart step counts are pinned so
    an added onboarding step reds conformance; the ADR-0056 sealed agent campaign re-runs
    from package installation in an empty directory with time-to-first-committed-row,
    ceremony count, and rescue count recorded as standing evidence.
  requirements: [DX-031, DX-032]
  required_adrs: [ADR-0056, ADR-0106, ADR-0120]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, fixtures/**, tests/**, scripts/**,
    clients/**, crates/riffdb-cli/**, crates/riffdb-query-module/**, templates/**,
    evaluations/agent-application-alpha/**,
    release/agent-application-alpha/**, release/distribution/**, release/evidence/**,
    release/python/**, .github/**]
  deliverables:
  - The four-language matrix over one schema with identity/outcome/error agreement assertions
  - Pinned per-driver quickstart step counts wired as a conformance assertion
  - Campaign-03 sealed runs from package install, cliff metrics recorded, friction ledger
    filed as tracked items
  acceptance_commands:
  - ./scripts/check-driver-package-matrix
  - ./scripts/driver-package-conformance-acceptance
  - ./scripts/agent-application-alpha-acceptance --campaign campaign-03 --runs 6 --sealed
    --assert-gate
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo test --workspace --all-features
  standing_design_tests:
    interface_safety: Conformance exercises only generated named operations and stable public
      errors from package-arrival projects; any handwritten wire adapter, compiler ID, kernel
      import, or source-tree dependency is a failing cell.
    scale: The language matrix, application corpus, operation set, campaign contexts, event
      count, and recorded metrics are finite and versioned.
  closure:
    status: complete
    evidence: release/evidence/agent-application-alpha-package-campaign-v1.json
    decision: The package-only conformance gate proves one four-step generated story across Go,
      Python, Rust, and TypeScript with identical typed outcomes, replay, read-after-commit, and
      stable reuse errors. The immutable six-run Blog/Orders campaign-03 receipt is current, has
      zero rescue, kernel use, or handwritten transport, and records a minimum independent rating
      of 8.7 with every selected evidence digest still exact.
  human_review_triggers: *id001
- id: WP-605
  name: Contract-language editor tooling — tree-sitter grammar and compiler-backed LSP
  depends_on: [WP-601]
  objective: Implement ADR-0120 section 5. A tree-sitter grammar for .riff and .riffq
    (editor and forge highlighting) and an LSP server shipped inside the CLI packages,
    serving diagnostics by invoking the same compiler check path so editor feedback is
    byte-identical to push diagnostics and can never drift from the authority; hover types,
    go-to-definition over entities/fields/commands/queries, and schema-fed completions.
    Serves agents equally — agent harnesses consume LSP diagnostics natively.
  requirements: [DX-033, DX-034, DX-035, DX-036]
  required_adrs: [ADR-0002, ADR-0120]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, fixtures/**, crates/riffdb-cli/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-compiler/**, clients/**,
    scripts/**, release/distribution/**]
  deliverables:
  - Tree-sitter grammar with a snapshot corpus over the language reference's examples,
    regenerated with the grammar per house generated-artifact rules
  - riffdb lsp serving diagnostics through the compiler's own check machinery with a
    diagnostics-parity test (same broken contract, byte-identical primary diagnostic via
    push and LSP), plus hover, definitions, and schema-fed completions with bounded responses
  - Editor packaging — VS Code extension scaffold plus editor-agnostic binary documentation;
    grammar published for forge highlighting
  acceptance_commands:
  - cargo test -p riffdb-contract-syntax -p riffdb-cli --all-features
  - ./scripts/editor-grammar-acceptance
  - ./scripts/handbook check
  - ./scripts/check-requirement-coverage
  standing_design_tests:
    interface_safety: The editor surface invokes the compiler-owned local check and exposes only
      bounded schema navigation; it owns no database authority, mutation, alternative grammar,
      or application transport.
    scale: Grammar fixtures and every LSP document, request, traversal, diagnostic, completion,
      and response are independently count- and byte-bounded.
  closure:
    status: complete
    evidence: docs/getting-started/EDITOR-TOOLING.md
    decision: The merged compiler-backed LSP and generated tree-sitter grammars pass the exact
      syntax/CLI, grammar-corpus, handbook, and requirement-coverage gates. Diagnostics retain
      compiler parity, navigation and completion remain schema-fed and bounded, the VS Code
      scaffold ships through the CLI distribution, and no editor path acquires database,
      mutation, policy, or application-transport authority.
  human_review_triggers: *id001
- id: WP-606
  name: Commit-coordinator index-derivation audit — admit IR bundle versions V7 through V10
  depends_on: [WP-598]
  objective: Discharge WP-598's coordinator escalation. The checked-command index-derivation
    whitelist ends at grammar/IR pair (V6, V6) while bundles pair through V10, so every
    mutating command on V7+ bundles (vectors, secret-classified, framework-profile, reimport
    era) fails closed before commit. Admit each version through a per-version audit with
    evidence — what the version changed, why index derivation is correct for it, the test
    proving it — never a blanket whitelist extension. WP-598's ignore-marked coordinator
    race schedules flip to passing UNCHANGED as the acceptance contract; the beyond-audited
    boundary stays fail-closed.
  requirements: []
  required_adrs: [ADR-0003, ADR-0107, ADR-0118, ADR-0119]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, fixtures/**, crates/riffdb-commit/**,
    tests/command_semantics/**]
  deliverables:
  - Git-archaeology finding on the whitelist's intent (deliberate parking versus silent gap)
    recorded before any change
  - Per-version audit notes at the whitelist and in the report; V7 vector-field, V8
    secret-field, V9 framework-profile derivation evidence tests; V10 reimport-class routing
    pin (ordinary derivation refuses reimport commands)
  - WP-598's two race schedules un-ignored and passing without assertion changes; the live
    zero-mutation V9 pin retained; unknown-version refusal pinned
  acceptance_commands:
  - cargo test -p riffdb-commit --all-features
  - cargo test --workspace --all-features
  closure:
    status: complete
    evidence: docs/architecture/WP-598-FRAMEWORK-PROFILE-VERIFICATION.md
    decision: The coordinator audit admits grammar/IR pairs V7 through V10 one version at a time,
      with anchored-event, secret/delete, framework-profile, and reimport-class evidence at the
      whitelist site. The unchanged WP-598 race schedules pass, the next version remains refused,
      and commit plus full-workspace acceptance is green without granting workers or callers new
      derivation authority.
  human_review_triggers: *id001
- id: WP-607
  name: Unseal entity delete for unique-carrying entities in the first delete format
  depends_on: [WP-606]
  objective: Discharge WP-598's second escalation. RDB-C044 unconditionally refuses delete on
    unique-carrying entities in ADR-0107's sealed first delete format; the Better Auth gate
    shape's delete-user and delete-session flows need it. Lift the seal through the
    diagnostic-retirement ceremony with a changelog/tombstone audit for unique index entries
    and both-order delete/create schedule evidence; commit-side index-entry delete derivation
    already exists and is audited under WP-606.
  requirements: []
  required_adrs: [ADR-0100, ADR-0107]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, fixtures/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-contract-ir/**,
    crates/riffdb-commit/**, tests/command_semantics/**, tests/storage_recovery/**]
  deliverables:
  - Diagnostic retirement for RDB-C044 on the audited path with span-asserted replacement
    semantics; the WP-598 seal pin updated to the unsealed expectation deliberately
  - Changelog/tombstone audit evidence for unique index entries under delete (delete-aware
    changelog v2 reciprocity)
  - Both-order delete/create and create/delete schedules with unique-value release semantics
    pinned; release-then-reinsert extended through the deleted path
  acceptance_commands:
  - cargo test -p riffdb-contract-compiler -p riffdb-commit --all-features
  - cargo test --test storage_recovery_matrix
  closure:
    status: complete
    evidence: docs/architecture/WP-598-FRAMEWORK-PROFILE-VERIFICATION.md
    decision: Unique-carrying entity deletion is unsealed only on the audited delete-aware path.
      Compiler diagnostics, both FIFO delete/create schedules, value release and reinsertion,
      reciprocal V2 tombstones, chain heads, checkpoint substitution, and recovery all pass while
      missing or mismatched durable transition evidence remains fail-closed.
  human_review_triggers: *id001
- id: WP-608
  name: Close mixed capsule and legacy command-history persistence
  depends_on: [WP-606]
  objective: Remove the storage-side trap where the legacy uncapsulated conformance path advances
    ENTITY_CHAIN_HEADS without retaining walkable CommittedEntityTransitionV1 evidence, so a later
    audited command on the same entity is accepted and then fails startup with MissingCrossLink.
    Audit every caller and choose one explicit invariant-preserving disposition by either persisting
    exact transition capsules on the legacy path or refusing a mixed target before mutation. Production
    application commands remain capsule-authoritative and no startup check may be weakened to
    tolerate an unprovable chain.
  requirements: [REC-001, REC-002, STO-002]
  required_adrs: [ADR-0102, ADR-0104]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**,
    crates/riffdb-storage-redb/src/application.rs,
    crates/riffdb-storage-redb/src/command_authority.rs,
    crates/riffdb-storage-redb/src/startup.rs,
    crates/riffdb-storage-redb/tests/architecture.rs,
    crates/riffdb-commit/src/audit_executor.rs,
    crates/riffdb-commit/src/command_execution.rs,
    crates/riffdb-sim/tests/**, tests/command_semantics/**,
    tests/storage_recovery/**]
  deliverables:
  - Caller and durable-history audit distinguishing the production capsule path from every retained
    legacy/test/conformance path, with the selected compatibility disposition reviewed before code
  - Deterministic both-order schedules for uncapsulated-then-audited and audited-then-uncapsulated
    commands on one target; each must either retain one walkable exact chain or refuse before any
    authoritative mutation
  - Startup and crash/reopen evidence proving no accepted mixed history can later surface
    MissingCrossLink, while genuinely missing transition evidence remains fail-closed
  acceptance_commands:
  - cargo test -p riffdb-storage-redb --all-features
  - cargo test -p riffdb-commit --test command_concurrency --all-features
  - cargo test -p riffdb-sim --test seeded_campaign --all-features
  closure:
    status: complete
    evidence: docs/architecture/WP-608-MIXED-COMMAND-HISTORY-AUDIT.md
    decision: Entity-bearing legacy uncapsulated commits now refuse before mutation, while the
      production capsule-authoritative path remains unchanged. Both mixed-history orders reopen
      cleanly, architecture tests prohibit unwalkable entity history, real missing evidence still
      reports corruption, and the storage, command-schedule, and seeded-simulation gates pass.
  human_review_triggers: *id001
- id: WP-609
  name: Rebase the durability journal before offline history retention
  depends_on: [WP-423]
  objective: Close the ADR-0085/ADR-0103 boundary exposed by the installed WP-578 endurance
    rehearsal. Offline retention currently commits tombstones and deletes retained command/event
    rows directly in redb while a clean-shutdown durability journal still names the pre-prune
    suffix; the next RedbStore open therefore fails CorruptData. Define and implement one
    fail-closed pre-prune recovery/rebase ceremony so no retention mutation can begin until the
    redb checkpoint is reconciled and an empty strictly newer journal generation is durable.
  requirements: [PERF-013, PERF-014, REC-001, REC-002, STO-001, STO-002]
  required_adrs: [ADR-0085, ADR-0103, ADR-0104]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/0085-history-retention-and-startup-scaling.md,
    docs/**, crates/riffdb-storage-redb/src/journal.rs,
    crates/riffdb-storage-redb/src/retention.rs,
    crates/riffdb-storage-redb/src/store.rs,
    crates/riffdb-storage-redb/tests/architecture.rs,
    crates/riffdb-cli/src/app.rs, tests/storage_recovery/**,
    tests/endurance_harness.rs, scripts/endurance-lifecycle]
  deliverables:
  - Maintainer-accepted ADR-0085 amendment defining the exact pre-prune journal recovery and
    rebase order; retention never journals each system delete and never starts from an
    unreconciled or unpublished command suffix
  - An exclusive typed retention preparation witness proving redb contains the complete published
    frontier, followed by an empty journal extent whose generation is strictly greater than every
    predecessor; checkpoint deletion and the first prune transaction are impossible before it
  - Deterministic failures before, during, and after next-extent creation, sync, rename, checkpoint
    deletion, and each prune sub-range; every accepted state reopens as either wholly unpruned or
    a tombstone-covered prefix, and stale residue or mixed generations remain corruption
  - Installed CLI evidence that a command-bearing database can shut down, prune, reopen, advance
    with another public command, prune again, and reopen without a stale scratch extent or a
    CorruptData startup failure
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - cargo +1.97.0 test -p riffdb-testkit --test endurance_harness
  - cargo +1.97.0 test -p riffdb-cli --all-features
  - ./scripts/check-requirement-coverage
  exit_gate: Retention cannot mutate a database whose journal has not been recovered and durably
    rebased; every rebase/prune crash arm reopens with exact frontier and tombstone semantics; two
    installed prune/restart cycles pass through the same public maintenance surface used by WP-578.
  standing_design_tests:
    interface_safety: The public retention verbs and typed outcomes do not change. The preparation
      witness is storage-internal and cannot be fabricated by CLI, service, or application code.
    scale: Journal recovery is bounded by the existing extent limits and happens once per offline
      prune invocation; rebase is O(one preallocated extent), while prune remains bounded by the
      accepted 1,024-sequence sub-range transactions.
  closure:
    status: complete
    evidence: docs/performance/alpha-endurance.md
    decision: Offline retention now recovers and validates the complete checkpoint-plus-journal
      authority, fences an empty strictly newer extent, and issues a storage-private preparation
      witness before deleting a checkpoint or pruning. Recovery, storage architecture, CLI,
      endurance, and requirement gates prove two-cycle reuse without stale suffix, mixed
      generation, frontier regression, or application-fabricated authority.
  human_review_triggers: *id001
- id: WP-610
  name: Receipted immutable-backup retirement
  depends_on: [WP-155, WP-577]
  objective: Replace the endurance harness's direct deletion of published immutable backups with
    one authorized, public, crash-recoverable retirement operation. Preserve V1 create/restore
    receipts byte-for-byte, add a retire-only V2 receipt that binds the exact succeeded create
    receipt and manifest, consume names permanently, and make a terminal retirement receipt the
    only proof under which startup accepts an absent published backup artifact.
  requirements: [API-001, SEC-001, SEC-002, SEC-003, REC-001, REC-002,
    STO-001, STO-002, END-006, END-008, END-009, END-010, END-011]
  required_adrs: [ADR-0050, ADR-0112]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0050-public-offline-backup-and-restore-maintenance.md,
    proto/**, crates/riffdb-types/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-policy/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-cli/**,
    crates/riffdb-server/**, crates/riffdb-proto/**, fixtures/**,
    tests/**, scripts/**, docs/**, release/**]
  deliverables:
  - Additive RetireOfflineBackup unary gRPC operation, API-neutral service request/result, Rust
    operator client, and `riffdb backup retire <name>` CLI using checked BackupNameV1 and a
    caller-stable maintenance operation ID; no MCP surface, path, URI, glob, retention expression,
    new capability permission, or direct storage authority
  - Byte-frozen readable V1 create/restore receipts and retire-only canonical V2 receipts binding
    the exact succeeded create operation and immutable manifest; old/unknown readers refuse before
    database mutation and create/restore continue least-version V1 writes
  - Crash-safe same-filesystem rename into the reserved `.maintenance/retired/<operation-id>`
    subtree, synced parent directories, bounded exact-file deletion, permanent name consumption,
    and fail-closed reconciliation of every named/staged/terminal state
  - Policy, audit, approval, cancellation, restart, operation polling, multi-database, symlink/path,
    mismatch, duplicate, corruption, and process-crash tests proving retirement follows the same
    global AdministerCapabilities and external maintenance-owner boundary as backup/restore
  - Endurance lifecycle integration that retires excess backups only through the installed public
    client/CLI, records operation and receipt evidence, and pins direct filesystem deletion or
    receipt deletion as an architecture failure; handbook and generated public references updated
  acceptance_commands:
  - ./scripts/generate-proto --check
  - cargo +1.97.0 test -p riffdb-types -p riffdb-storage-api -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-policy -p riffdb-service -p riffdb-api-grpc --all-features
  - cargo +1.97.0 test -p riffdb-client-rust -p riffdb-cli -p riffdb-server --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - cargo +1.97.0 test -p riffdb-testkit --test endurance_harness --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: A third installed backup lifecycle retires the oldest artifact through the authorized
    public surface, survives every rename/delete crash arm, restarts with exact create/retire
    receipt reconciliation and a permanently consumed name, and never treats unexplained absence
    as success. Existing V1 fixtures remain byte-identical and no MCP or raw-path operation exists.
  standing_design_tests:
    interface_safety: The caller names only one checked immutable backup and stable operation ID;
      service policy derives global maintenance authority, storage resolves configured roots, and
      only an exact terminal receipt pair can explain absence. No application, MCP, arbitrary path,
      predicate, or capability-format surface is added.
    scale: One incomplete retirement is bounded by the existing maintenance controller; validation
      and deletion visit only the fixed backup inventory, reconciliation is bounded by receipt and
      retired-stage ceilings, and retained backup count remains fixed without unbounded tombstones
      beyond the already bounded durable receipt registry.
  closure:
    status: complete
    evidence: docs/backup-restore.md
    decision: The additive backup-retirement RPC, Rust operator client, and CLI are fully wired
      through shared service and policy. Byte-frozen V1 create/restore receipts remain readable,
      retire-only V2 receipts permanently consume names, and every bounded rename/delete crash arm
      reconciles without inventing success. Proto, storage, policy, service, client, recovery,
      endurance, generated-artifact, requirement, and handbook gates pass with no MCP or raw-path
      escape hatch.
  human_review_triggers: *id001
- id: WP-611
  name: Four-ecosystem development package registries
  depends_on: [WP-601]
  objective: Materialize one immutable development build of the four supported driver ecosystems
    into package-manager-native loopback registries so adapter and application packages can be
    developed in separate repositories without source paths. Preserve the Rust-owned driver and
    generated-facade boundary, keep all application-specific integrations outside RiffDB, and keep
    development publication mechanically disjoint from signed production release qualification.
  requirements: [DX-037, DX-038, DX-039, DX-040]
  required_adrs: [ADR-0106, ADR-0112, ADR-0120]
  allowed_paths: [.github/workflows/driver-development-registry.yml,
    work_packages.yaml, SPEC.md, docs/**, clients/**, fixtures/driver/**,
    scripts/**, release/distribution/**]
  deliverables:
  - Versioned development-publication schema and checked manifest mapping one caller-supplied build
    identity to npm and Rust semver prereleases, a PEP 440 Python development version, and a Go
    module prerelease; artifact digests and the complete package inventory are immutable
  - Reproducible registry views for Verdaccio-compatible npm publication, a PEP 503/691 Python
    Simple index, a Go module proxy, and a Cargo sparse registry, plus loopback-only lifecycle and
    client configuration that never changes user-global package-manager state
  - Fresh-directory acceptance consumers for TypeScript, Python, Go, and Rust proving exact native
    registry resolution, lock/install metadata, compile or import success, and absence of source
    paths, workspace inheritance, and fallback registries
  - Production-boundary checks that reject development versions and manifests from the signed
    release workflow and prevent the development publisher from invoking signing or release upload
    machinery; handbook setup and teardown guidance for external adapter repositories
  acceptance_commands:
  - ./scripts/check-driver-development-registry
  - ./scripts/driver-development-registry-acceptance --all-ecosystems
  - ./scripts/check-driver-distribution
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: One fresh build identity is published to all four loopback registries and consumed by
    four source-independent external projects at the exact mapped versions; repeated identical
    publication is harmless, different bytes at an existing identity fail closed, and no
    development artifact or endpoint can enter signed release qualification.
  standing_design_tests:
    interface_safety: The development registries distribute only the existing safe generated-driver
      surfaces. They add no SQL, transaction callback, raw transport, Better Auth, identity,
      session, OAuth, or application-specific runtime API to RiffDB.
    scale: Publication visits one closed bounded artifact inventory, registry indices are generated
      deterministically, loopback services have explicit request and artifact-size limits, and
      acceptance uses one bounded consumer per ecosystem.
  closure:
    status: complete
    evidence: docs/getting-started/PACKAGE-INSTALLATION.md
    decision: One immutable development identity publishes to loopback npm, Python Simple, Go
      proxy, and Cargo sparse registries and is consumed by four clean external projects with exact
      package-manager metadata and no source fallback. Identical publication is idempotent,
      production/development inputs remain disjoint, and distribution, requirement, and handbook
      gates pass. The Python cell used the exact prebuilt abi3 wheel because this host cannot claim
      a manylinux_2_28 build.
  human_review_triggers: *id001
- id: WP-612
  name: Version topology and retirement governance
  depends_on: [WP-574, WP-601]
  objective: Replace scattered, implicitly coordinated version constants with one machine-readable
    topology that records the distinct ownership, reader window, writer policy, compatibility
    class, and retirement gate for every release-significant durable, executable-IR, wire,
    protocol, application-artifact, generated-surface, and evidence format. Preserve domain-specific
    versions rather than inventing a misleading global version, and make any unregistered version
    change fail repository checks before the first alpha release.
  requirements: [AFC-008, AFC-010, VER-001, VER-002, VER-003, VER-004, VER-005,
    VER-006, VER-007, VER-008]
  required_adrs: [ADR-0002, ADR-0006, ADR-0013, ADR-0057, ADR-0112, ADR-0120,
    ADR-0124]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/README.md,
    adr/0124-version-topology-and-retirement-governance.md, docs/**,
    release/version-topology-v1.json, scripts/check-version-topology,
    scripts/check-generated]
  deliverables:
  - Accepted ADR-0124 defining why version domains remain separate, the required registry fields,
    reader/writer compatibility rules, breaking-change classification, and decoder retirement
    ceremony without weakening ADR-0112's durable-format guarantees
  - Canonical `release/version-topology-v1.json` inventory covering every release-significant
    version family, with an owning source locator, explicit readable and writable identities,
    writer-selection policy, supported-release posture, fixtures/checks, and current lifecycle state
  - Repository checker that validates the registry schema and ordering, pins source constants and
    external baselines, cross-checks durable entries against the generated durable-format manifest,
    and rejects an unregistered or lifecycle-incomplete version change
  - Public handbook guidance for additive changes, breaking changes, deprecation, decoder
    retirement, reserved identifiers, release-note obligations, and the distinction between
    application migration, physical format upgrade, and symbolic export/reimport
  acceptance_commands:
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Every release-significant version domain is owned by the checked topology; a changed
    version constant, reader/writer window, external baseline, or durable-manifest claim fails CI
    until its compatibility classification and retirement evidence are updated; no decoder is
    retired merely because a newer version exists.
  standing_design_tests:
    interface_safety: The registry grants no decoder, migration, downgrade, reset, raw import, or
      compatibility bypass. Public and durable changes remain governed by their owning accepted ADRs,
      and unsupported inputs continue to fail closed at the original boundary.
    scale: The topology is a bounded release artifact over a fixed domain inventory; checks read only
      declared source files and manifests and never scan database contents or retain runtime state.
  closure:
    status: complete
    evidence: docs/versioning-and-retirement.md
    decision: The canonical topology governs 57 independently versioned domains and cross-checks
      all 11 durable reader/writer windows without introducing a runtime super-version. Source
      pins, explicit reader/writer sets, writer policies, lifecycle states, retirement evidence,
      durable-manifest agreement, generated artifacts, requirement coverage, and handbook checks
      all pass; no existing decoder is retired by this package.
  human_review_triggers: *id001
- id: WP-620
  name: Portable writer and checkpoint mechanics evidence
  depends_on: [WP-486, WP-490]
  objective: Make the isolated cloud performance failure mechanically attributable before changing
    durable bytes. Split canonical mutation, logical frame, padded extent, positional write, fence,
    publication, asynchronous checkpoint apply/commit/reclamation, and checkpoint-overlap costs;
    extend the existing journal probes through maximum-frame sizes on both the workstation/NVMe and
    general-purpose-cloud/persistent-disk profiles.
  requirements: [PERF-007, PERF-008, PERF-015, PERF-017, PERF-018]
  required_adrs: [ADR-0101, ADR-0103, ADR-0104]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, benchmarks/**, scripts/**,
    crates/riffdb-storage-redb/src/journal.rs,
    crates/riffdb-storage-redb/src/store.rs,
    crates/riffdb-storage-redb/src/benchmark_support.rs,
    crates/riffdb-storage-redb/tests/**,
    crates/riffdb-commit/src/telemetry.rs,
    crates/riffdb-observability/src/**,
    examples/app-baseline/**]
  deliverables:
  - Fixed-cardinality redaction-safe counters for logical mutation bytes, encoded and padded frame
    bytes, positional write versus fence, checkpoint apply/commit/reclamation, and overlap state
  - Journal mechanics sweeps at 4 KiB, 64 KiB, 256 KiB, 1 MiB, 4 MiB, and the accepted maximum,
    alone and during representative checkpoint materialization, with environment receipts
  - Per-table mutation count/key/value/expected-hash byte census for the checked TicketDesk write
    corpus so compact encoding targets measured redundancy rather than guessed structure
  - A short repeatable cloud diagnostic that finishes in under ten minutes and detects regression
    in the three dominant writer/read stages without replacing PERF-018 evidence
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-observability --all-features
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 run --release --manifest-path benchmarks/journal-mechanics/Cargo.toml
  - ./scripts/check-requirement-coverage
  exit_gate: Both inventoried profiles produce complete comparable stage/byte evidence; the short
    regression cell identifies frame construction, physical fence, and checkpoint overlap
    separately and contains no value-bearing or unbounded label.
  standing_design_tests:
    interface_safety: Adds internal observations and benchmark controls only. No application can
      select durability, grouping, checkpoint, or measurement behavior, and no data-bearing label
      crosses telemetry.
    scale: Every counter is fixed-cardinality and every probe uses the existing frame/suffix bounds;
      no per-command series, key, value, or unbounded histogram is retained.
  closure:
    status: complete
    evidence: docs/performance/wp-620-portable-writer-mechanics.md
    decision: Both inventoried profiles produced the fixed-cardinality stage and byte census, the
      mechanics sweep separates frame construction, positional write, fence, and checkpoint
      overlap, and the current-HEAD mechanics and crate acceptance gates pass without adding a
      public durability or measurement control.
  human_review_triggers: *id001
- id: WP-621
  name: Compact exact command-segment successor
  depends_on: [WP-620]
  objective: Implement ADR-0123's bounded raw-or-LZ4 command-segment successor and encode-once
    immutable byte ownership without changing command, authorization, ordering, publication,
    checkpoint, backup, replication, or recovery semantics. Activate the successor only after its
    real-corpus cloud mechanics gates pass.
  requirements: [PERF-008, PERF-009, PERF-015, PERF-017, REC-001, REC-002, STO-001, STO-002]
  required_adrs: [ADR-0101, ADR-0103, ADR-0104, ADR-0112, ADR-0123, ADR-0125]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/0123-portable-cloud-performance-and-compact-writer-frames.md,
    adr/0125-command-segment-raw-fallback-compatibility.md,
    Cargo.toml, Cargo.lock,
    docs/**, fixtures/**, benchmarks/**, scripts/**,
    proto/riffdb/storage/v1/**,
    crates/riffdb-proto/**,
    crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/src/lib.rs,
    crates/riffdb-storage-redb/src/application.rs,
    crates/riffdb-storage-redb/src/journal.rs,
    crates/riffdb-storage-redb/src/store.rs,
    crates/riffdb-storage-redb/src/composite_view.rs,
    crates/riffdb-storage-redb/src/benchmark_support.rs,
    crates/riffdb-storage-redb/src/backup.rs,
    crates/riffdb-storage-redb/tests/**,
    crates/riffdb-server/src/daemon.rs,
    examples/app-baseline/**,
    tests/storage_recovery/**,
    tests/endurance_harness.rs,
    release/**]
  deliverables:
  - Next-free registered LZ4-only command-segment record, retaining the current record as the exact
    raw representation, with exact uncompressed length and digest, bounded allocation/decompression,
    canonical 12.5-percent complete-envelope selection rule, complete existing semantic validation,
    and unknown-record refusal
  - One live owned stored-value allocation shared by reservation, overlay, positional write, and
    checkpoint; no acknowledgement-path decompression/decode of the just-sealed segment and no
    duplicate semantic graph
  - Exact-pinned safe-only checked-decode lz4_flex dependency confinement and deny/checksum review; durable
    manifest writer/minimum-reader rotation; byte-frozen old fixtures; successor golden/property
    fixtures; explicit old-reader typed refusal before mutation
  - Process crash matrix across old/successor boundary, torn compact record, extent recycle, checkpoint,
    reclamation, backup/restore, and clean/unclean startup
  - Captured redacted real TicketDesk corpus mechanics proving at least 35 percent fewer complete
    frame bytes, 25 percent fewer checkpointed segment bytes, 20 percent lower cloud c=32 combined
    writer-stage time, and no greater than 1.10 times unary p50 or smallest-frame CPU
  - Value-free failed-activation receipt and fixed-cardinality live frame census; when any hard
    activation threshold fails, retain the byte-frozen current writer and make no format rotation
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix
  - cargo +1.97.0 test -p riffdb-testkit --test endurance_harness --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Existing records remain readable and byte-frozen; the compact successor reconstructs
    the identical segment and ordered checked mutation program, passes every crash/backup path, and
    meets all activation thresholds on the inventoried cloud profile before becoming selected.
  standing_design_tests:
    interface_safety: Frame selection and compaction are storage-internal. Public commands retain
      typed idempotent atomic acknowledgement and cannot select a weaker durability or recovery path.
    scale: Uncompressed length is rejected against the existing segment/frame ceiling before
      allocation, frame/suffix/overlay ceilings remain exact, mixed-version recovery is linear in
      the already bounded suffix, and no full database rewrite is introduced.
  closure:
    status: complete
    evidence: docs/performance/wp-621-compact-command-segment-gate.md
    decision: The bounded compact successor passed its byte-reduction mechanics but failed the
      required cloud writer-stage activation threshold. The candidate was therefore removed, no
      V5 record or compression dependency entered production, the current writer remains exact,
      and the value-free rejection receipt closes the package honestly.
  human_review_triggers: *id001
- id: WP-622
  name: Immutable exact named-query resolution fast path
  depends_on: [WP-442]
  objective: Collapse a warm generated named-query's contract/module/operation resolution into one
    immutable exact-identity process-local lookup while preserving admission, every current
    authorization safe point, capability revision checks, row policy, freshness, and redaction.
  requirements: [PERF-007, PERF-008, PERF-018, AAA-006, AAA-007]
  required_adrs: [ADR-0052, ADR-0055, ADR-0070]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**,
    crates/riffdb-service/src/ports.rs,
    crates/riffdb-service/src/symbolic_query.rs,
    crates/riffdb-service/src/query_discovery_operations.rs,
    crates/riffdb-server/src/read_adapters.rs,
    crates/riffdb-server/src/storage.rs,
    crates/riffdb-server/tests/**,
    crates/riffdb-observability/src/**,
    examples/app-baseline/**]
  deliverables:
  - One bounded immutable resolved named-operation view keyed by complete contract and module
    identities plus operation name, published atomically on exact module activation
  - Warm lookup after ordinary port admission without storage I/O, blocking-port dispatch, deep
    clone, or multiple catalog/module cache locks; misses and races retain the authoritative path
  - Concurrent deployment, module replacement, cache contention/poisoning, cancellation, deadline,
    and capability-revocation tests proving exact fallback or fail-closed behavior
  - Cloud 128-client evidence reducing named-query plan-lookup mean by at least 4 times without a
    workstation p50 or authorization regression
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-service --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test -p riffdb-observability --all-features
  - ./scripts/check-requirement-coverage
  exit_gate: Warm exact generated reads perform one immutable lookup after admission and retain all
    per-request authentication, authorization, row-policy, freshness, and release checks; the cloud
    stage gate passes and cold/error semantics are unchanged.
  standing_design_tests:
    interface_safety: The cache contains no principal-specific allow decision. Every authority and
      redaction safe point remains per request, and a miss or identity race cannot select a nearby
      contract, module, query, or plan.
    scale: Entries are bounded by existing deployed contract/module/query cardinality limits and
      replacement publishes one immutable view; no request or row result is retained.
  closure:
    status: complete
    evidence: docs/performance/wp-622-exact-named-query-lookup.md
    decision: Warm generated reads resolve through one immutable complete-identity lookup after
      admission while every principal-specific authority and release check remains per request.
      The cloud lookup improved 6.90 times, exceeding the four-times gate, and current-HEAD service,
      server, observability, generated-artifact, requirement, and handbook gates pass.
  human_review_triggers: *id001
- id: WP-623
  name: Dual-profile alpha performance qualification
  depends_on: [WP-620, WP-621, WP-622, WP-662, WP-663, WP-664, WP-674]
  objective: Qualify the optimized release candidate on both the workstation/NVMe and
    general-purpose-cloud/persistent-disk profiles before restarting the 72-hour endurance gate.
    Preserve exact safe-application PostgreSQL obligations and report minimal PostgreSQL separately.
  requirements: [PERF-008, PERF-018, END-007, END-008, END-009, END-010]
  required_adrs: [ADR-0056, ADR-0104, ADR-0123, ADR-0137, ADR-0138, ADR-0142]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, benchmarks/**, scripts/**,
    examples/app-baseline/**, tests/endurance_harness.rs, release/**]
  deliverables:
  - Three same-run 90-second post-warmup repetitions of interactive and write-only sweeps at the
    frozen concurrency points on each inventoried idle hardware profile
  - Same-run minimal and safe-application PostgreSQL plus public gRPC RiffDB evidence with complete
    correctness reconciliation, stage/byte observations, and host-validity receipts
  - Automated refusal of a qualification set missing workstation, N1, or E2; containing
    interfered or unstable points; changing durability/client/workload shape; missing an absolute
    unary, frozen RiffDB regression, mixed, or seed gate; or suppressing a PostgreSQL unary ratio
  - Final optimized artifact digest handed unchanged to WP-579; prior partial soak evidence is
    retained as diagnostic and is never promoted
  acceptance_commands:
  - ./benchmarks/run-app-baseline --full
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: The exact optimized artifact meets PERF-008's mixed and seed comparison gates,
    absolute N1/E2 unary service levels, frozen N1/E2/workstation RiffDB regression ceilings,
    stability and correctness rules, and complete PERF-018 disclosure evidence; only that exact
    artifact may begin the final uninterrupted 72-hour run.
  standing_design_tests:
    interface_safety: Qualification changes no application surface and cannot waive correctness,
      durability, authorization, idempotency, freshness, or boundedness to obtain a passing ratio.
    scale: Evidence is a fixed matrix with bounded artifacts; the cloud profile exercises sustained
      checkpointing so a pass cannot borrow unbounded suffix, overlay, memory, or deferred work.
  human_review_triggers: *id001
- id: WP-624
  name: Portable contention-edge journal coalescing
  depends_on: [WP-620, WP-621]
  objective: Restore ADR-0098's bounded contention-only completion-edge collection for the
    standard journal-authoritative profile when the cloud writer is fence-frequency limited.
    Preserve immediate idle-unary dispatch, every FIFO/barrier boundary, independent command
    semantics, the hardened oracle, and all journal, suffix, recovery, and publication bounds.
  requirements: [PERF-004, PERF-005, PERF-008, PERF-015, PERF-017, PERF-018, REC-002,
    TXN-040, TXN-041, TXN-042, TXN-043, TXN-044]
  required_adrs: [ADR-0058, ADR-0060, ADR-0098, ADR-0103, ADR-0104, ADR-0123]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, benchmarks/**, scripts/**,
    crates/riffdb-commit/src/audit_executor.rs,
    crates/riffdb-commit/src/audit_executor/**,
    crates/riffdb-commit/src/telemetry.rs,
    crates/riffdb-observability/src/**,
    examples/app-baseline/**]
  deliverables:
  - Standard-profile selection of the accepted at-most-two-millisecond completion-edge window only
    after a prior writer unit leaves at least two FIFO commands queued; idle singletons, barriers,
    full prefixes, and fresh 200-microsecond formation retain their existing immediate paths
  - Fixed-cardinality observations proving selected command count, deferred count, dispatch reason,
    elapsed collection time, frame count, commands per frame, and writer-stage time without keys,
    values, identities, or per-command labels
  - Deterministic actor tests for idle unary non-delay, queued contention coalescing, FIFO/barrier
    preservation, bounded shutdown, cancellation, and hardened/standard selection
  - Paired cloud c=1 and c=32 activation evidence plus workstation c=1/c=32 non-regression; retain
    the candidate only if cloud c=32 throughput improves by at least 20 percent or combined
    validation/encoding/staging plus durable-writer time falls by at least 20 percent, while unary
    p50 remains no greater than 1.10 times baseline on either profile
  - Value-free failed-activation receipt; when public c32 throughput regresses despite larger
    frames or lower summed internal stage time, remove the standard-profile window and retain the
    immediate completion edge
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --all-features
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: The activation threshold passes on the inventoried cloud profile without unary,
    correctness, recovery, suffix, overlay, publication, or workstation regression; otherwise the
    standard profile remains immediate at completion edges and the failed candidate is removed with
    a value-free receipt.
  standing_design_tests:
    interface_safety: Collection is coordinator-internal and cannot be selected by applications.
      It changes neither logical grouping nor command atomicity, idempotency, authorization,
      acknowledgement durability, read visibility, typed freshness, or bounded failure outcomes.
    scale: Collection retains the accepted two-millisecond oldest-command ceiling, 256-transition
      and 16-MiB frame ceilings, bounded intake channel, FIFO barrier rules, and fixed-cardinality
      observations; it creates no unbounded queue, series, or retained work.
  closure:
    status: complete
    evidence: docs/performance/wp-624-contention-edge-coalescing-gate.md
    decision: The contention-edge candidate increased commands per frame but slowed the public
      closed-loop workload and missed both materiality alternatives. It was removed, immediate
      completion-edge dispatch remains authoritative, and the value-free rejection receipt plus
      current-HEAD coordinator and application-baseline tests close the package.
  human_review_triggers: *id001
- id: WP-625
  name: Streaming anchored command-authority encoding
  depends_on: [WP-621, WP-624]
  objective: Remove the complete generated-Prost graph construction still used by anchored-event
    command segment V4. Stream its byte-frozen canonical body from already-checked command values,
    using at most one bounded nested message at a time, without changing one durable byte, digest,
    manifest, command graph, acknowledgement, checkpoint, recovery, or public semantic.
  requirements: [PERF-007, PERF-008, PERF-009, PERF-015, PERF-017, PERF-018, REC-001,
    REC-002, STO-001, STO-002]
  required_adrs: [ADR-0101, ADR-0103, ADR-0104, ADR-0112, ADR-0123]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, benchmarks/**, scripts/**,
    crates/riffdb-storage-api/src/proto_codec/command_segment.rs,
    crates/riffdb-storage-api/src/proto_codec/tests/**,
    crates/riffdb-storage-api/tests/**,
    crates/riffdb-storage-redb/src/application.rs,
    crates/riffdb-storage-redb/src/benchmark_support.rs,
    crates/riffdb-storage-redb/tests/**,
    examples/app-baseline/**]
  deliverables:
  - Canonical V4 segment body writer that appends the byte-frozen field sequence directly from
    checked capsules and constructs at most one event, generation transition, entity transition,
    audit, or other bounded nested message at a time
  - One owned body buffer shared by digest calculation and final envelope assembly; the live seal
    path never builds `StoredCommandSegmentBodyV4` or its complete nested generated message graph
  - Golden, property, maximum-bound, anchored-event, mixed-event, and independent generated-Prost
    oracle tests proving byte identity, digest identity, decode identity, bounded allocation, and
    unchanged old-reader behavior
  - Cloud c=32 command-stage evidence reducing validation/encoding/staging time by at least 20
    percent or public throughput by at least 10 percent, with c=1 unary p50 no greater than 1.05
    times baseline; otherwise remove the candidate and retain a failed-activation receipt
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --all-features
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Every V4 writer byte and digest is identical to the frozen generated-Prost oracle and
    the cloud activation threshold passes without unary, CPU, memory, correctness, recovery,
    checkpoint, suffix, or workstation regression; no durable-format rotation is involved.
  standing_design_tests:
    interface_safety: This changes only construction of an already-fixed internal durable byte
      sequence. Applications cannot select it, and command authorization, idempotency, atomicity,
      publication, durability, recovery, freshness, and typed outcomes are unchanged.
    scale: The encoder retains the existing 256-command, 16-MiB frame, field-visit, event,
      transition, manifest, and envelope bounds; it owns one body buffer and at most one bounded
      nested generated message, never an additional complete semantic graph.
  closure:
    status: complete
    evidence: docs/performance/wp-625-streaming-authority-encoding-gate.md
    decision: The byte-identical streaming encoder candidate remained correctness-clean but
      improved public throughput only 2.8 percent and staging only 0.7 percent. The candidate and
      live-path selection were removed, the frozen V4 writer remains in use, and the failed-
      activation receipt closes the package without a durable-format change.
  human_review_triggers: *id001
- id: WP-626
  name: Retained compiler-fact command staging
  depends_on: [WP-625]
  objective: Carry the compiler-validated input-fact proof already owned by command preparation
    through admission, evaluation, transaction-current validation, and index derivation. Remove
    repeated normalized-input cloning and fact reconstruction from the sole writer without moving
    a safe point, trusting caller data, changing transaction-current reads, or changing one
    command, index, durable, recovery, acknowledgement, or public semantic.
  requirements: [PERF-007, PERF-008, PERF-009, PERF-015, PERF-017, PERF-018, TXN-002,
    TXN-012, TXN-013, TXN-030, TXN-031, TXN-040]
  required_adrs: [ADR-0058, ADR-0060, ADR-0101, ADR-0104, ADR-0111, ADR-0123]
  allowed_paths: [work_packages.yaml, SPEC.md, docs/**, benchmarks/**, scripts/**,
    crates/riffdb-commit/src/command_preparation.rs,
    crates/riffdb-commit/src/command_admission.rs,
    crates/riffdb-commit/src/command_attempt.rs,
    crates/riffdb-commit/src/command_validation.rs,
    crates/riffdb-commit/src/command_index.rs,
    crates/riffdb-commit/src/command_execution.rs,
    crates/riffdb-commit/src/telemetry.rs,
    crates/riffdb-commit/tests/**,
    examples/app-baseline/**]
  deliverables:
  - Move-only retention of the exact `InputDerivedCommandFacts` accepted by
    `CommandExecutionPreparation`; admission and attempt lowering verify its original plan/input
    join and never reconstruct it from normalized input
  - Transaction-current validation and secondary-index derivation consume the retained proof
    while preserving every current-state, policy, uniqueness, epoch, capacity, sequence, and
    storage recheck in its existing order
  - Semantic and architecture tests proving mismatched facts fail before mutation, retries retain
    the same proof, collection expansion remains exact, and no public or storage trait accepts
    caller-supplied facts
  - Cloud c=32 activation evidence reducing validation/encoding/staging time by at least 20
    percent or increasing public throughput by at least 10 percent, with c=1 unary p50 no greater
    than 1.05 times baseline; otherwise remove the candidate and retain a failed-activation receipt
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --all-features
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: The exact retained proof passes all semantic and architecture tests and materially
    improves the inventoried cloud c=32 cell without unary, correctness, recovery, checkpoint,
    suffix, publication, or workstation regression; otherwise the redundant derivation remains
    and the failed candidate is documented.
  standing_design_tests:
    interface_safety: The proof originates only in compiler- and policy-checked preparation and is
      move-only through private commit-owned states. Applications gain no input, escape hatch, or
      control; fresh authorization, transaction-current validation, row policy, indexes, capacity,
      durability, and typed outcomes remain mandatory.
    scale: Retention replaces repeated bounded reconstructions with one already-bounded proof. It
      adds no queue, task, cache, cardinality, recursion, scan, wait, or durable footprint and
      remains charged inside the existing bounded command-preparation admission unit.
  closure:
    status: complete
    evidence: docs/performance/wp-626-retained-compiler-fact-gate.md
    decision: The retained-proof candidate preserved correctness but reached neither its public-
      throughput nor staging-reduction activation threshold. Its private fields and alternate
      entrypoints were removed, the existing derivation remains authoritative, and the value-free
      rejection receipt records the bounded result.
  human_review_triggers: *id001
- id: WP-627
  name: Cloud journal flush and checkpoint attribution
  depends_on: [WP-620, WP-621, WP-624, WP-625, WP-626]
  objective: Attribute the remaining ordinary-persistent-disk write gap to actual journal
    durability fences, frames co-fenced per physical flush, checkpoint overlap, or serialized CPU
    before authorizing another production optimization. Preserve every current command,
    durability, publication, checkpoint, recovery, and public semantic.
  requirements: [PERF-007, PERF-008, PERF-009, PERF-015, PERF-017, PERF-018, REC-001,
    REC-002, TXN-040]
  required_adrs: [ADR-0058, ADR-0098, ADR-0101, ADR-0103, ADR-0104, ADR-0123]
  allowed_paths: [work_packages.yaml, docs/**, benchmarks/**, scripts/**,
    crates/riffdb-storage-redb/src/journal.rs,
    crates/riffdb-storage-redb/src/lib.rs,
    crates/riffdb-storage-redb/src/benchmark_support.rs,
    crates/riffdb-storage-redb/tests/**,
    crates/riffdb-server/src/daemon.rs,
    examples/app-baseline/**]
  deliverables:
  - One fixed-cardinality process-generation census of successful command-bearing physical journal
    flushes, command frames covered by them, logical commands, complete frame bytes, and maximum
    frames covered by one flush; no key, value, identity, symbol, or per-command label
  - Short workload-only cloud CPU profile separated by thread class and matched to one public c32
    result, with startup recovery excluded from command-path attribution
  - Cloud c=1 and c=32 diagnostic cells reporting logical commands per frame, command frames per
    physical flush, physical flush latency, checkpoint overlap, and coordinator/service CPU shares
  - A falsifiable recommendation naming the dominant remaining mechanism and rejecting candidates
    whose maximum possible end-to-end benefit is below ten percent
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --all-features
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: The cloud write gap is assigned to a measured physical-fence, checkpoint-interference,
    service-pipeline, or serialized-coordinator mechanism with enough headroom to satisfy the next
    activation gate; this diagnostic changes no public behavior or durable byte.
  standing_design_tests:
    interface_safety: This adds fixed-cardinality shutdown evidence only. Applications cannot read,
      select, influence, or use it to weaken command authorization, idempotency, atomicity,
      durability, publication, recovery, freshness, or bounded failure behavior.
    scale: Seven saturating process-generation counters replace no bounded state and add no queue,
      cache, task, scan, wait, retained record, metric label, or per-command observation.
  closure:
    status: complete
    evidence: docs/performance/wp-627-cloud-writer-attribution.md
    decision: Matched N1 and E2 cells, workload-only thread-class profiles, and the physical-flush
      census attribute the remaining ceiling to serialized coordinator CPU rather than freshness
      waits, checkpoint I/O, or fence frequency. The evidence rejects sub-ten-percent candidates
      and adds only fixed-cardinality value-free shutdown observations.
  human_review_triggers: *id001
- id: WP-628
  name: Compiler-bounded one-hop cascade language and executable IR
  depends_on: [WP-560]
  objective: Implement ADR-0126's closed one-hop cascade declaration, static aggregate/conflict/
    partition and capacity proofs, source-spanned diagnostics, and least-sufficient V13 executable
    IR without activating runtime cascade execution.
  requirements: [DEL-001, DEL-002, DEL-003, DEL-004, DEL-005, DEL-006]
  required_adrs: [ADR-0002, ADR-0003, ADR-0005, ADR-0012, ADR-0013, ADR-0055,
    ADR-0107, ADR-0111, ADR-0112, ADR-0124, ADR-0126]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0126-compiler-bounded-one-hop-cascade-deletion.md,
    release/version-topology-v1.json,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-diagnostics/**,
    crates/riffdb-catalog/**, crates/riffdb-query-module/**,
    crates/riffdb-cli/assets/editor/**,
    clients/**, fixtures/compiler/**, fixtures/contracts/**, fixtures/application-parity/**,
    fixtures/editor-grammar/**,
    fuzz/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Exact grammar, formatter, AST, spans, and diagnostics for exhaustive bounded cascade policies
    and the distinct delete-binding overflow outcome, with recursive/dynamic forms unrepresentable
  - Static proof of direct inbound coverage, source no-inbound policy, one aggregate/partition,
    complete common conflicts, one delete template, 32 relationships, 256 rows, and 16 MiB
  - V13 grammar/bundle/executable IR identities and canonical codec carrying stable relationship,
    reverse-index, maximum, ordering, conflict, partition, and outcome metadata
  - Compatibility fixtures proving V1 through V12 remain readable and byte-frozen and that the
    least-sufficient writer selects V13 only for a contract containing cascade deletion
  - Better-Auth-shaped positive compiler fixture and source-span negative corpus for missing,
    duplicate, recursive, cross-aggregate, cross-partition, excessive, or dynamically keyed policy
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-diagnostics --all-features
  - cargo +nightly-2026-07-12 fuzz run contract_parser -- -max_total_time=60
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every accepted cascade shape compiles to one bounded complete V13 plan, every unsafe
    or non-exhaustive shape fails at its source span, old contracts keep their prior writer
    identity, and no runtime path claims cascade support before WP-629.
  standing_design_tests:
    interface_safety: Applications can select only a named compiled command and typed values; the
      policy, index, bounds, conflicts, ordering, and overflow outcome remain compiler-owned, and
      unsupported runtimes fail before deployment.
    scale: Static maxima cap direct relationships at 32 and the whole possible graph at 256 rows
      and 16 MiB; no recursive traversal, scan, dynamic index, or cross-partition discovery exists.
  closure:
    status: complete
    evidence: docs/contracts/BOUNDED-COMMANDS.md
    decision: V13 grammar, formatter, compiler proofs, executable IR, diagnostics, topology, and
      compatibility fixtures encode only exhaustive one-hop indexed cascades with the accepted
      32-relationship, 256-row, and 16-MiB bounds. Compiler, syntax, generated-artifact, topology,
      requirement, handbook, formatting, Clippy, and 60-second contract-parser fuzz gates pass.
  human_review_triggers: *id001
- id: WP-629
  name: Atomic one-hop cascade runtime, storage, and recovery
  depends_on: [WP-561, WP-628]
  objective: Execute accepted V13 cascade plans through the ordinary authorization, conflict,
    command, commit, tombstone, replication, recovery, provenance, and idempotency paths with
    bounded exact-prefix discovery and zero partial deletion.
  requirements: [DEL-005, DEL-007, DEL-008, DEL-009, DEL-010, DEL-011]
  required_adrs: [ADR-0003, ADR-0004, ADR-0005, ADR-0012, ADR-0058, ADR-0059,
    ADR-0093, ADR-0100, ADR-0101, ADR-0102, ADR-0104, ADR-0107, ADR-0126]
  allowed_paths: [SPEC.md, work_packages.yaml, proto/**, fixtures/proto/**,
    crates/riffdb-types/**, crates/riffdb-contract-ir/**, crates/riffdb-invariant/**,
    crates/riffdb-catalog/**, crates/riffdb-runtime/**, crates/riffdb-auth/**,
    crates/riffdb-policy/**, crates/riffdb-idempotency/**, crates/riffdb-service/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-commit/**, crates/riffdb-proto/**,
    crates/riffdb-api-grpc/**, crates/riffdb-observability/**, crates/riffdb-testkit/**,
    tests/**, scripts/**, docs/**]
  deliverables:
  - Sealed exact-prefix maximum-plus-one enumeration after common canonical conflict acquisition,
    with stable relationship/key order and declared durable zero-mutation overflow
  - Root and discovered-row authorization, row-policy, relationship, revision, uniqueness,
    transaction-current, and capacity validation inside one ordinary command reevaluation loop
  - One children-before-parent atomic graph sharing command identity, outcome, sequence, audit,
    provenance, idempotency, tombstone, changelog, replication, checkpoint, and backup semantics
  - Deterministic create-versus-cascade schedules plus memory/redb overflow, cancellation,
    uncertainty, replay, crash, follower, bootstrap, recovery, and mixed-runtime refusal evidence
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-runtime -p riffdb-service -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit --all-features
  - cargo +1.97.0 test -p riffdb-testkit --all-features
  - cargo +1.97.0 test --test storage_recovery_matrix --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A cascade either persists its complete canonical graph and one declared outcome or no
    mutation, a racing inbound create cannot escape the common conflict domain, and every crash or
    retry resolves through the existing authoritative recovery and idempotency machinery.
  standing_design_tests:
    interface_safety: Runtime accepts only a sealed compiler plan and typed command values; no
      storage iterator, policy choice, raw key, transaction, partial-success, or history-erasure
      control crosses the public boundary.
    scale: Each exact-prefix read stops at its compiled maximum plus one, the total possible graph
      is precharged to 256 rows and 16 MiB, and reevaluation remains bounded with no recursive scan.
  closure:
    status: complete
    evidence: docs/contracts/BOUNDED-COMMANDS.md
    decision: The shared runtime executes the sealed V13 plan through bounded exact-prefix reads,
      row authorization, current-state validation, one canonical children-before-parent graph, and
      the ordinary atomic commit, tombstone, changelog, recovery, and idempotency paths. Maximum-
      plus-one and child-create races produce zero partial deletion, and current-HEAD runtime,
      storage, commit, crash, recovery, generated, requirement, handbook, format, and Clippy gates pass.
  human_review_triggers: *id001
- id: WP-630
  name: Generated cascade clients and Better Auth lifecycle acceptance
  depends_on: [WP-562, WP-611, WP-629, WP-634, WP-635]
  objective: Publish the closed cascade command and overflow outcome through every generated
    language and prove a real external Better Auth adapter can use named RiffQL for every read,
    including authorized secret output, while completing its bounded user lifecycle without a
    reveal-shaped read command, handwritten transaction, raw deletion, identity, session, or
    OAuth machinery in RiffDB.
  requirements: [DEL-012, QSO-012]
  required_adrs: [ADR-0055, ADR-0056, ADR-0074, ADR-0106, ADR-0107, ADR-0112,
    ADR-0117, ADR-0118, ADR-0120, ADR-0126, ADR-0128]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-application/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-api-mcp/**, crates/riffdb-query-module/**, clients/**, templates/**,
    fixtures/application-parity/**, fixtures/adapters/**, fixtures/driver/**, fixtures/export/**,
    examples/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - Generated Rust, Go, TypeScript, and Python methods, typed inputs, overflow outcomes, errors, and
    exact identities with no generic delete, transaction, raw transport, or kernel escape hatch
  - Immutable development-registry publication and fresh external Better Auth consumer proving
    signup, named exact and secret-bearing lookup, session/account cleanup, bounded full-user
    deletion, atomic verification consumption, and idempotent replay with no reveal-only command
  - Overflow, authorization, row-policy, revocation, racing account/session creation, uncertainty,
    malformed-host, and mixed-version observations shared by the all-language conformance corpus
  - Handbook adapter guidance that keeps end-user identity and session behavior application-owned
    and states the one-hop, declared-relationship, row, byte, and runtime-version limits
  acceptance_commands:
  - ./scripts/adapter-cascade-acceptance --all-languages
  - ./scripts/driver-development-registry-acceptance --all-ecosystems
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: Fresh external consumers in every supported language observe the same typed bounded
    lifecycle, Better Auth performs every read through named RiffQL including exact authorized
    secret output, and full user deletion remains atomic without a reveal-only command,
    application-owned transaction, or raw storage glue; RiffDB remains independent of userland
    auth semantics.
  standing_design_tests:
    interface_safety: Generated surfaces expose only the compiler-owned named command and declared
      outcomes. The adapter owns user/session meaning while RiffDB owns safe atomic data mutation.
    scale: Clients preflight fixed input bounds, runtime discovery remains server-side and bounded,
      and the adapter cannot request recursive traversal or split one claimed atomic deletion.
  closure:
    status: complete
    evidence: docs/contracts/BOUNDED-COMMANDS.md
    decision: The Better Auth fixture now exposes exact and explicitly secret-bearing named reads
      plus one generated bounded user-deletion command through Rust, Go, TypeScript, and Python.
      Fresh public TLS clients prove signup, token issue and single-use consumption, replay, exact
      lookup, session/account/token cleanup, recreate, authorized zero-mutation overflow,
      authorization denial, and revocation with identical typed observations and no handwritten
      transport or transaction glue. Immutable local-registry consumers compile the generated
      cascade facade in all four ecosystems. Secret output stays out of MCP and reactive surfaces,
      and identity, sessions, sign-in, and OAuth remain application-owned.
- id: WP-631
  name: TypeScript empty-frontier decoding and immutable development publication
  depends_on: [WP-611]
  objective: Correct the TypeScript driver boundary so a successful named query against a
    domain-empty database preserves the valid application frontier zero, while commit sequences,
    contract versions, and every other positive-only identity remain strictly positive. Publish
    the correction under a fresh immutable development build and prove it through an installed
    external npm consumer without changing the Rust-owned protocol or public command semantics.
  requirements: [DRV-008, DRV-014, DX-037, DX-038, DX-039, DX-040]
  required_adrs: [ADR-0106, ADR-0112, ADR-0120]
  allowed_paths: [work_packages.yaml, clients/typescript/runtime/**, fixtures/driver/**,
    crates/riffdb-cli/assets/scaffold/typescript-runtime/**,
    scripts/driver-development-registry-acceptance, scripts/check-driver-development-registry,
    scripts/build-driver-development-publication, release/distribution/**, docs/**]
  deliverables:
  - A dedicated checked non-negative u64 decoder used only for result application_head, preserving
    exact bigint zero and maximum-u64 values and rejecting negative, fractional, malformed, and
    out-of-range inputs before typed facade assembly
  - Regression tests proving zero is accepted for a query frontier while zero remains rejected for
    batch commit_sequence, handshake contract_version, and error contract_version fields
  - Fresh-directory npm-registry acceptance that installs the exact immutable @riffdb/client
    development build and exercises the zero-frontier transport path through the packaged runtime,
    with no repository source path or registry fallback
  - A fresh immutable development publication following WP-611; the bytes published as
    0.1.0-dev.3 remain untouched and the corrected package receives the next unused build identity
  acceptance_commands:
  - npm --prefix clients/typescript/runtime test
  - ./scripts/check-application-bindings
  - ./scripts/check-driver-development-registry
  - ./scripts/driver-development-registry-acceptance --all-ecosystems
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: An external npm consumer installed from the loopback registry receives a successful
    empty-database query with applicationHead equal to 0n; malformed frontiers still fail closed,
    every positive-only identity still rejects zero, and the corrected bytes exist only under a
    new immutable development version.
  standing_design_tests:
    interface_safety: The correction narrows one decoder to the protocol's existing non-negative
      query-frontier domain. It adds no operation, transaction, credential, raw transport,
      authorization bypass, or target-language runtime semantics.
    scale: Decoding remains one bounded u64 parse with no collection, allocation, retry, scan, or
      retained state; registry acceptance adds one finite installed-package transport exchange.
  closure:
    status: complete
    evidence: docs/getting-started/PACKAGE-INSTALLATION.md
    decision: The TypeScript runtime uses a dedicated non-negative-u64 decoder for the application
      frontier while every positive-only protocol identity retains its strict decoder. Unit and
      immutable development-registry acceptance prove an empty-domain query returns 0n through the
      installed package, malformed and out-of-range values fail closed, and the corrected bytes
      were published under the immutable development build recorded by the package matrix. The
      implementation merged in c617e0ba and its WP-631 registry entry merged through 1aabfac5.
  human_review_triggers: *id001
- id: WP-663
  name: Direct exclusive generated-operation lane candidate
  depends_on: [WP-649, WP-650, WP-660, WP-661, WP-662]
  objective: After exact acceptance of ADR-0138, determine whether a directly polled,
    one-in-flight first-party Rust application connection can remove the measured HTTP2 and
    client-actor floor while preserving every current application-service, authority, durability,
    freshness, uncertainty, lifecycle, and resource guarantee; activate nothing unless the exact
    cloud and release gates pass.
  requirements: [API-001, PERF-005, PERF-008, PERF-018,
    NET-001, NET-002, NET-003, NET-004, NET-005, NET-006, NET-007, NET-008,
    NET-009, NET-010, NET-011, NET-012,
    DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006, DRV-007,
    DRV-008, DRV-009, DRV-010, DRV-011, DRV-012, DRV-013, DRV-014]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0070, ADR-0105, ADR-0106,
    ADR-0120, ADR-0123, ADR-0127, ADR-0132, ADR-0133, ADR-0137, ADR-0138]
  allowed_paths: [work_packages.yaml, SPEC.md, Cargo.toml, Cargo.lock, adr/README.md,
    adr/0138-direct-exclusive-application-lane.md, proto/**,
    crates/riffdb-types/**, crates/riffdb-proto/**,
    crates/riffdb-api-application/**, crates/riffdb-api-exclusive/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, clients/go/**, clients/typescript/**,
    clients/python/**, examples/app-baseline/**, fixtures/**, fuzz/**,
    tests/**, benchmarks/**, scripts/**, release/**, docs/SUMMARY.md,
    docs/reference/**, docs/getting-started/**, docs/operations/**,
    docs/performance/**]
  deliverables:
  - Diagnostic-only direct-stream probe, outside every production listener and client selector,
    closing the exact per-operation stage ledger within five percent and passing ADR-0138's three-
    generation N1/E2 mechanics thresholds before any production frame implementation
  - One fixed-header, strict-Protobuf, one-in-flight generated-operation lane over existing
    loopback, direct-TLS, and local-socket trust profiles, with finite connection, frame, operation,
    wait, setup, idle, lifetime, pool, shutdown, and telemetry bounds
  - One API-neutral generated-operation adapter shared by unary gRPC and the exclusive lane, with
    no storage, catalog, policy, plan, command, projection, audit, clock, cached-allow, or target-
    language protocol authority in the transport
  - One session-authentication proof that amortizes only the exact credential presentation while
    reloading and rechecking every mutable capability, expiry, identity, role, contract, module,
    operation, tenant, row, field, secret, freshness, and response-release fact per operation
  - Native Rust exclusive-session and bounded-pool carriage plus Rust-driver-host use for Go,
    TypeScript, and Python; no target-language TLS, framing, retry, uncertainty, freshness, or error
    implementation
  - Hostile-frame, fragmentation, cancellation-by-close, stream-loss, reconnect, rotation,
    revocation, expiry, role/contract/module drift, read-after-commit, command-uncertainty, overload,
    slow-peer, pool saturation, graceful-shutdown, and unary/exclusive byte-semantic equivalence
  - Three-generation workstation/N1/E2 performance receipts and full release matrix only after the
    mechanics and semantic gates pass; failed thresholds remove all production selection and retain
    only a value-free failed-candidate receipt
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-targets --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/recovery_full
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The diagnostic first proves the direct-ownership mechanism on N1 and E2; the resulting
    production candidate is semantically indistinguishable from unary at every safe point and
    passes every workstation/N1/E2 c1, c32, seed, tail, correctness, resource, and stability gate;
    only then may it produce an exact PERF-018/default amendment for separate human acceptance.
    Any missed mechanics or release threshold removes production implementation and leaves WP-623
    open with a value-free failed-candidate receipt.
  standing_design_tests:
    interface_safety: The lane accepts only existing generated typed commands and exact named
      queries; one-in-flight ownership, pooling, and pay-once credential presentation cannot create
      a transaction, cache authority, imply ordering/freshness, relax durability or uncertainty,
      select storage, expose raw IDs, or permit fallback after possible command submission.
    scale: Connections, pools, frames, bytes, operations, buffers, waits, setup, idle, lifetime,
      cancellation, shutdown, telemetry, and evidence are hard-bounded independently of database
      size and duration; no completed result, authorization decision, request, or value is retained.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-663-direct-exclusive-closure.md
    decision: Direct ownership passed every loopback mechanics threshold in all three N1 and E2
      generations and reduced verified-TLS steady-state complete point-read mean by 48.5 to 52.4
      percent, but verified-TLS connection setup plus first-operation reduction was only 12.17
      percent on N1 and 22.23 percent on E2 against the required 25 percent. ADR-0138's
      reject-first rule therefore stopped the package after the first TLS generation; every
      listener, protocol, ALPN, client, and benchmark-selection implementation was removed, unary
      gRPC remains the default, PERF-018 is unchanged, and WP-623 remains open.
  human_review_triggers: *id001
- id: WP-664
  name: Established-connection generated-operation lane candidate
  depends_on: [WP-663]
  objective: After exact acceptance of ADR-0139, retest direct one-in-flight generated operations
    with trust establishment, application-session establishment, and operation latency accounted as
    distinct customer-paid intervals; activate nothing unless the corrected mechanics gate and the
    complete workstation/N1/E2 release matrix pass.
  requirements: [API-001, PERF-005, PERF-008, PERF-018,
    NET-001, NET-002, NET-003, NET-004, NET-005, NET-006, NET-007, NET-008,
    NET-009, NET-010, NET-011, NET-012,
    DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006, DRV-007,
    DRV-008, DRV-009, DRV-010, DRV-011, DRV-012, DRV-013, DRV-014]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0070, ADR-0105, ADR-0106,
    ADR-0120, ADR-0123, ADR-0127, ADR-0132, ADR-0133, ADR-0137, ADR-0138, ADR-0139]
  allowed_paths: [work_packages.yaml, SPEC.md, Cargo.toml, Cargo.lock, adr/README.md,
    adr/0139-established-connection-application-lane.md, proto/**,
    crates/riffdb-types/**, crates/riffdb-proto/**,
    crates/riffdb-api-application/**, crates/riffdb-api-exclusive/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, clients/go/**, clients/typescript/**,
    clients/python/**, examples/app-baseline/**, fixtures/**, fuzz/**,
    tests/**, benchmarks/**, scripts/**, release/**, docs/SUMMARY.md,
    docs/reference/**, docs/getting-started/**, docs/operations/**,
    docs/performance/**]
  deliverables:
  - Diagnostic-only three-interval ledger separating cold verified trust, application-session
    establishment, and generated-operation latency, closing their sum to caller time within five
    percent and structurally preventing cold setup from being relabeled as warm or resumed
  - One bounded immutable Rust transport generation sharing only exact trust/peer/ALPN configuration
    and process-local rustls resumption state across identical connections, with cold full-handshake
    evidence retained and zero-RTT or half-RTT application data forbidden
  - ADR-0138's strict one-in-flight generated-operation lane, finite exclusive pool, shared
    API-neutral application adapter, per-operation mutable-authority checks, and no target-language
    protocol implementation, restored only after ADR-0139's revised mechanics gate passes
  - Pool readiness, partial-fill, reconnect, uncertainty, rotation, revocation, drift, saturation,
    cancellation, and shutdown semantics that never report readiness before one exact authenticated
    lane exists and never return an uncertain command connection to the pool
  - Three-generation N1/E2 loopback and verified-TLS mechanics receipts plus full workstation/N1/E2
    c1, c32, seed, tail, correctness, lifecycle, resource, and stability evidence only after every
    reject-first threshold passes
  acceptance_commands:
  - cargo +1.97.0 test --workspace --all-targets --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/recovery_full
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The revised diagnostic proves cold setup non-regression and established-session first
    and steady operations on N1/E2; the production candidate is semantically indistinguishable from
    unary at every safe point and passes every workstation/N1/E2 release gate. Only then may it
    produce an exact PERF-018/default amendment for separate human acceptance. Any missed threshold
    removes production selection and leaves WP-623 open with a value-free failed-candidate receipt.
  standing_design_tests:
    interface_safety: The client exposes only ready generated operations and typed bounded failure;
      it cannot expose trust/resumption/pool knobs, cache authority, create a transaction, imply
      freshness, relax durability or uncertainty, select storage, or fall back after possible
      command submission.
    scale: Transport generations, resumption entries, connections, pools, frames, bytes, operations,
      waits, setup, idle, lifetime, cancellation, shutdown, and telemetry are hard-bounded
      independently of database size and duration; no result, authorization decision, or full-state
      cache survives delivery.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-664-established-connection-closure.md
    decision: The corrected lifecycle ledger passed its setup and first-operation gates in the
      initial N1/E2 loopback cells, but E2's third loopback generation reduced complete operation
      latency by only 34.66 percent and outside-service latency by only 53.25 percent against 40
      and 55 percent minima. E2's first verified-TLS generation also reduced the accepted-session
      first operation by only 24.40 percent against 25 percent. The reject-first matrix stopped,
      all candidate implementation was removed, unary gRPC remains the only generated-operation
      transport, PERF-018 is unchanged, and WP-623 remains open.
  human_review_triggers: *id001
- id: WP-665
  name: Serial one-in-flight gRPC application-session candidate
  depends_on: [WP-650, WP-663, WP-664]
  objective: After exact acceptance of ADR-0140, specialize the existing
    ApplicationSession protocol V1 maximum-in-flight-one case into one directly owned client and
    server operation state, eliminating the general correlation routers without adding a listener,
    protocol, TLS path, cached authority, or unsafe cancellation outcome. Activate nothing unless
    the mechanism and complete public alpha ratios pass.
  requirements: [API-001, PERF-005, PERF-008, PERF-018, SEC-001, SEC-002,
    NET-001, NET-002, NET-003, NET-004, NET-005, NET-006, NET-007, NET-008,
    NET-009, NET-010, NET-011, NET-012,
    DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006, DRV-007,
    DRV-008, DRV-009, DRV-010, DRV-011, DRV-012, DRV-013, DRV-014]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0105, ADR-0106,
    ADR-0120, ADR-0123, ADR-0127, ADR-0132, ADR-0133, ADR-0137, ADR-0138,
    ADR-0139, ADR-0140]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0140-serial-one-in-flight-grpc-session.md,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-query-module/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, clients/go/**, clients/typescript/**,
    clients/python/**, examples/app-baseline/**, examples/agent-alpha/**,
    examples/ticketdesk/**, fixtures/**, fuzz/**, tests/**, benchmarks/**,
    scripts/**, release/**, docs/SUMMARY.md, docs/reference/**,
    docs/getting-started/**, docs/operations/**, docs/performance/**]
  deliverables:
  - A failing architecture test that proves maximum-in-flight one currently enters the generic
    client pending-map/oneshot/response-router and server BTreeMap/FuturesUnordered/Abortable path,
    followed by exact pins that forbid those structures in the serial strategy without weakening
    the retained multiplexed strategy
  - One try-only, queue-free first-party Rust serial client lane that directly submits and receives
    one exact session operation, destroys its generation on cancellation or uncertainty, and
    retains no completed response, allow decision, operation value, or unbounded waiter
  - One server serial state machine that concurrently polls its sole operation, cancellation,
    stream loss, deadlines, lifetime, shutdown, and bounded response delivery without a per-operation
    spawn, general correlation collection, or authority outside the shared application adapter
  - A fixed serial-session pool with bounded cardinality, ready minimum, partial fill, saturation,
    rotation, reconnect, replacement, idle, lifetime, and byte states; applications select no pool
    or trust knob and target languages retain the first-party Rust driver host
  - Unary, generic-session, and serial-session semantic equivalence plus deterministic cancellation,
    uncertainty, revocation, expiry, drift, row/secret policy, read-after-commit, overload, slow-peer,
    shutdown, hostile-wire, and resource-bound tests on byte-identical session protocol V1
  - Three counterbalanced N1/E2 loopback and verified-TLS generations with a closed lifecycle and
    operation ledger; stop and remove the specialization if setup regresses above five percent,
    exact GetTicket outside-service mean falls less than 50 percent from unary, any server/resource
    metric regresses above five percent, or any semantic/lifecycle gate fails
  - Only after mechanics pass, a same-run short N1/E2 public matrix requiring every representative
    scenario at most 1.10 times safe-application PostgreSQL, c32 throughput at least 0.90 times and
    p95 at most 1.25 times PostgreSQL, seed at most 5.0 times PostgreSQL, clean correctness, and no
    otherwise-uncovered regression above five percent; only a complete pass may start WP-623
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/recovery_full
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The existing session V1 maximum-in-flight-one case is semantically identical to unary
    and the generic session at every safe point, owns only one bounded operation state, and passes
    every mechanics and public N1/E2 gate. Only a subsequent complete workstation/N1/E2 WP-623
    matrix and separately accepted PERF-018/default amendment can activate it; any earlier miss
    removes the specialization and leaves unary default and WP-623 open.
  standing_design_tests:
    interface_safety: Applications still invoke only generated commands and exact named queries and
      cannot select a transaction, ordering, pool size, trust mode, cached authority, durability,
      retry, uncertainty, freshness, raw method, or storage behavior. Every operation retains fresh
      authentication, authorization, row/field/secret policy, response release, audit, and typed
      cancellation semantics.
    scale: Each lane retains one bounded operation and response; pool, stream, permit, buffer, byte,
      task, wait, setup, idle, lifetime, shutdown, and replacement counts are hard-capped independent
      of database size and duration, with no queued caller population or completed-result history.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-665-serial-grpc-session-closure.md
    decision: The private maximum-in-flight-one client/server strategy preserved session protocol
      V1 and passed live generated command, fenced query, reconnect, and dead-peer semantics, but
      the first full-dataset N1 generation reduced exact GetTicket outside-service time by only
      31.13 percent against ADR-0140's 50 percent minimum. The reject-first matrix stopped; every
      serial implementation, selector, test pin, diagnostic surface, and prospective pool was
      removed before commit. Unary remains the generated-operation default, the optional generic
      session remains unchanged, PERF-018 is unchanged, and WP-623 stays open.
  human_review_triggers: *id001
- id: WP-666
  name: Post-readiness derived catch-up interference attribution
  depends_on: [WP-455, WP-623, WP-665]
  objective: Determine whether columnar and sibling derived catch-up running after public readiness
    materially inflates ordinary generated-operation latency on general-purpose cloud hardware.
    Separate background replay CPU, storage, and scheduling interference from the retained unary
    gRPC path before proposing another transport or derived-worker candidate.
  requirements: [PERF-002, PERF-008, PERF-018, PRJ-001, PRJ-002, PRJ-003, PRJ-004]
  required_adrs: [ADR-0055, ADR-0056, ADR-0086, ADR-0087, ADR-0123, ADR-0140]
  allowed_paths: [work_packages.yaml, crates/riffdb-columnar/**,
    crates/riffdb-server/src/columnar_worker.rs, crates/riffdb-server/src/exact_text_worker.rs,
    crates/riffdb-observability/**, examples/app-baseline/**, benchmarks/**, scripts/**,
    docs/performance/**]
  deliverables:
  - A non-evidentiary, hard-bounded diagnostic control that measures the unchanged generated unary
    operation either immediately after restart or after an explicit post-restart settle interval,
    records the interval in its report, and cannot alter the frozen PERF-018 comparator
  - Paired current-HEAD N1 and E2 full-dataset c1 receipts, with process and artifact identity,
    showing immediate versus settled caller, API-neutral service, outside-service, CPU, storage,
    readiness, and derived-frontier behavior
  - A closed causal ledger naming the CPU and off-CPU work that overlaps measured requests and
    predeclared Amdahl bounds for any production candidate; benchmark waiting is never credited as
    product improvement
  - A value-free closure that either sizes one bounded derived-worker scheduling/pay-once candidate
    for separate human review or rejects derived catch-up as the remaining unary mechanism
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-columnar -p riffdb-server -p riffdb-observability --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The paired immediate and settled cells close within five percent and either prove at
    least twenty percent of current N1 and E2 caller latency is caused by overlapping derived work,
    with one evidence-sized bounded production direction, or reject that mechanism without a
    production change. This diagnostic never qualifies WP-623 or changes PERF-018.
  standing_design_tests:
    interface_safety: The control exists only in the non-evidentiary diagnostic binary; no server,
      generated client, application, agent, or release comparator can request delayed readiness,
      relaxed freshness, reduced validation, worker priority, or omitted derived work.
    scale: The diagnostic interval is explicitly reported and capped at 120 seconds; receipts,
      stage families, frontiers, CPU classes, storage counters, and process observations remain
      fixed-cardinality and retain no application values, keys, credentials, or per-operation
      history.
  closure:
    status: complete_without_production_change
    evidence: docs/performance/wp-666-derived-catchup-attribution.md
    decision: A temporary diagnostic-only 0-to-120-second post-restart settle control and an N1
      CPU profile proved columnar replay overlaps immediate reads and can delay shutdown, but a
      40-second settle improved synchronous unary mean only 0.49 percent on N1 and 4.38 percent on
      E2; the asynchronous shadow improved 1.92 percent on N1 and regressed 9.64 percent on E2.
      This misses the twenty-percent causal threshold on both profiles. The control was removed,
      no benchmark wait or server behavior changed, PERF-018 is unchanged, and WP-623 remains
      open. Interruptible bounded derived catch-up is a separate lifecycle follow-up, not a unary
      performance candidate.
  human_review_triggers: *id001
- id: WP-667
  name: Interruptible bounded columnar catch-up turns
  depends_on: [WP-455, WP-666]
  objective: Bound the time before the columnar worker can observe shutdown without publishing an
    incomplete commit prefix, checkpointing unpublished state, slowing ordinary catch-up with a
    polling window, or changing any projected-query freshness and authorization guarantee.
  requirements: [PERF-008, PRJ-001, PRJ-002, PRJ-003, PRJ-004]
  required_adrs: [ADR-0086, ADR-0087]
  allowed_paths: [work_packages.yaml, crates/riffdb-columnar/**,
    crates/riffdb-server/src/columnar_worker.rs, docs/performance/**]
  deliverables:
  - A positive bounded page-turn type and engine entry point that processes at most the declared
    number of existing 64-record authoritative pages, reports caught-up versus more-work, and
    retains the current all-or-none commit and monotonic processed-frontier proofs
  - Worker scheduling that checks shutdown between bounded turns and immediately continues while
    more work exists, applying the existing 25-millisecond poll only after reaching ExactEnd
  - No publication, notification, checkpoint, durable-frontier advancement, or retention claim
    caused solely by turn-budget exhaustion; the existing maximal-safe-prefix publication during
    supersession holdback remains unchanged, and the final caught-up turn publishes the same
    byte-equivalent snapshot and frontier as one unbounded call
  - Deterministic multi-page equivalence, supersession holdback, checkpoint/reopen, and stop-between-
    turns tests plus a full-dataset process shutdown receipt proving bounded exit without catch-up,
    query, apply-throughput, CPU, or memory regression above five percent
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-columnar --all-features
  - cargo +1.97.0 test -p riffdb-server --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: A shutdown request is observed after at most one fixed catch-up turn; completed catch-
    up publishes and checkpoints exactly the pre-change state/frontier; incomplete turns expose no
    state; full catch-up and projected-query performance regress no more than five percent. Any
    semantic or throughput miss removes the bounded-turn candidate.
  standing_design_tests:
    interface_safety: Applications cannot select turn size, partial publication, checkpoint,
      freshness, fallback, scheduling, or shutdown behavior; projected reads retain the exact
      source, policy, authorization, frontier, and typed lifecycle contract.
    scale: One turn processes at most eight existing 64-record pages and the worker owns one turn,
      one engine lock, and existing bounded working/deferred state. Continued catch-up retains no
      per-turn history and never sleeps while a frozen prefix remains.
  closure:
    status: complete_without_production_change
    evidence: docs/performance/wp-667-interruptible-columnar-catchup-closure.md
    decision: The private engine proved frozen-fence continuation, no publication or checkpoint on
      budget exhaustion, exact final equivalence, and checkpoint/reopen safety. Eight-, four-, two-,
      and one-page worker schedules plus a setup-retaining one-page variant were paired against the
      exact old binary on N1 and E2. Every variant exceeded at least one five-percent lifecycle,
      request-latency, throughput, resource, or whole-process gate. All candidate code and
      tests were removed. A future package must attribute shutdown time across every worker and the
      final checkpoint before bounding the measured owner.
  human_review_triggers: *id001
- id: WP-668
  name: Fixed-cardinality graceful-shutdown stage attribution
  depends_on: [WP-642, WP-643, WP-667]
  objective: Attribute clean process shutdown across every ordered graph owner and the final
    validated-prefix checkpoint before proposing another bounded worker or lifecycle candidate,
    without changing drain order, deadlines, failure classification, or authoritative state.
  requirements: [MDB-007, PERF-007, PERF-018, REC-001, STO-001]
  required_adrs: [ADR-0004, ADR-0019, ADR-0086, ADR-0104, ADR-0123]
  allowed_paths: [work_packages.yaml, crates/riffdb-server/src/process_graph.rs,
    crates/riffdb-server/src/daemon.rs, examples/app-baseline/**, docs/performance/**]
  deliverables:
  - A closed eight-stage microsecond receipt for admitted service-job drain, exact-text worker,
    columnar worker, projection worker, notification closure, coordinator drain, blocking-port
    drain, and the final validated-prefix checkpoint, preserving their existing order exactly
  - One additive versioned shutdown-evidence line parsed optionally by old-compatible benchmark
    clients and emitted in diagnostic and app-baseline JSON without values, paths, identifiers,
    credentials, thread IDs, or unbounded labels
  - Deterministic formatter/parser exactness tests and process receipts from N1 and E2 that close
    measured graph shutdown against wall time and identify the dominant owner before any repair
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every clean current server emits exactly eight ordered bounded values; an old server
    remains parseable with absent stage evidence; N1 and E2 receipts assign at least ninety-five
    percent of graph-shutdown wall to the closed stages or explicitly name the residual boundary.
    No scheduling or shutdown behavior changes in this attribution package.
  standing_design_tests:
    interface_safety: The receipt is diagnostic observation only. No caller can select, skip,
      reorder, delay, retry, or weaken any shutdown stage, checkpoint, failure, or readiness rule.
    scale: One process retains eight saturating u64 totals and emits one fixed-size line. The
      receipt has no dynamic labels, per-operation history, application values, or tenant cardinality.
  closure:
    status: complete
    evidence: docs/performance/wp-668-shutdown-attribution.md
    decision: The eight stages close more than 99.995 percent of graph-local shutdown wall in all
      N1 and E2 full-dataset generations. The validated-prefix checkpoint is the stable owner at
      50.7 to 96.3 percent; pending columnar catch-up is the variable owner at 2.7 to 83.6 percent.
      The other six stages together remain below 1.94 percent. WP-667 already rejected bounded
      columnar scheduling, so the next package must decompose the checkpoint before proposing a
      repair. No scheduling, drain, failure, checkpoint, or public application behavior changed.
  human_review_triggers: *id001
- id: WP-669
  name: Exact-current validated-prefix checkpoint reuse
  depends_on: [WP-642, WP-668]
  objective: Decompose the stable graceful-shutdown checkpoint owner and avoid rebuilding or
    durably rewriting a proof that is already exact for the current authoritative head, without
    weakening startup validation, checkpoint binding, crash recovery, or the non-fatal shutdown
    rule.
  requirements: [PERF-007, PERF-008, PERF-018, REC-001, STO-001, STO-002]
  required_adrs: [ADR-0019, ADR-0085, ADR-0100, ADR-0104, ADR-0123]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-redb/**,
    crates/riffdb-server/src/process_graph.rs, crates/riffdb-server/src/daemon.rs,
    examples/app-baseline/**, tests/**, docs/performance/**]
  deliverables:
  - A closed checkpoint ledger separating journal-suffix barrier, retained-state read, checkpoint
    proof construction, encoding, entity-head snapshot replacement, metadata publication, and
    durable commit on workstation, N1, and E2 before selecting a candidate
  - An exact-current reuse decision derived only from process-generation-proven checkpoint identity,
    current commit and audit frontiers, retained metadata, retention watermark, and complete durable
    table counts; absence, mismatch, decode failure, or uncertainty executes the existing full path
  - Tests proving a read-only generation performs no checkpoint mutation, every authoritative or
    administrative advance performs the ordinary proof-carrying write, corrupt or predecessor
    checkpoints never qualify, and crash/startup validation fixtures remain byte-exact
  - Paired full-dataset N1 and E2 receipts with predeclared no-regression gates for seed, mixed/read
    latency and throughput, shutdown tail, startup readiness, writes, recovery, and inventory
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-redb -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The pre-change ledger attributes at least ninety-five percent of checkpoint wall or
    names the residual; an exact-current shutdown performs zero checkpoint write transaction and
    reduces the isolated validated-prefix-checkpoint p95 by at least ninety-five percent on both
    cloud profiles; changed heads retain byte-identical proof and recovery semantics; no public
    performance or correctness cell regresses more than five percent. Whole-graph shutdown remains
    reported but is not a candidate gate because its earlier columnar-drain stage is independently
    scheduled and varied by more than an order of magnitude in the paired receipts.
  standing_design_tests:
    interface_safety: Reuse is internal and fail-closed. Applications cannot select, forge, skip,
      invalidate, or observe checkpoint reuse, and cannot weaken startup validation or acknowledged
      durability. Any uncertain identity takes the existing proof-building path.
    scale: Exact-current admission uses fixed metadata and O(1) table lengths only. It retains no
      application values or per-command history and cannot replace the existing bounded startup
      proof or suffix validation.
  closure:
    status: complete_with_measured_gate_correction
    evidence: docs/performance/wp-669-exact-current-checkpoint-reuse.md
    decision: The pre-change ledger closes the stable checkpoint owner, and graceful exact-current
      reuse cuts isolated checkpoint p95 by 95.1 percent on N1 and 96.8 percent on E2 while opening
      no checkpoint write transaction. Startup still publishes one process-generation proof and
      any changed or uncertain identity retains the full proof path. The originally proposed
      whole-graph p95 gate was corrected before closure because its already-completed columnar stage
      varied from 0.4 to 900 milliseconds independently of this candidate; order-balanced receipts
      demonstrate that confounder. Paired E2 mixed-c32 receipts are clean and improve throughput and
      latency, so the candidate activates without a public regression claim.
  human_review_triggers: *id001
- id: WP-632
  name: Frozen-client transport attribution and asynchronous shadow diagnostic
  depends_on: [WP-627]
  objective: Attribute the low-concurrency cloud gap across the frozen PERF-018 synchronous
    benchmark bridge, the real asynchronous application client, Tonic/Protobuf transport, and
    server orchestration without changing the release comparator or counting measurement-artifact
    removal as a RiffDB product gain.
  requirements: [PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0055, ADR-0056, ADR-0106, ADR-0123]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0127-bounded-multiplexed-application-session.md, examples/app-baseline/**,
    crates/riffdb-client-rust/**, docs/performance/**, scripts/**]
  deliverables:
  - A distinct non-evidentiary attribution report that preserves the frozen synchronous
    comparator byte-for-byte, pairs each sampled call's outer synchronous duration with its
    runtime-entry and asynchronous-future duration, and labels bridge-only time as measurement
    artifact rather than product improvement
  - An asynchronous shadow probe over the same generated named operation and persistent channel,
    excluded structurally from PERF-018 qualification and incapable of emitting a release-pass
    receipt
  - Source-pinned and executable assertions that the exact Tonic client version defaults
    TCP_NODELAY on, plus bounded client/server timing comparison using matched operation samples
    rather than subtracting unlike aggregate statistics
  - Short N1 and E2 c=1/c=8/c=32 diagnostic receipts with predeclared Amdahl bounds, preserving the
    existing PostgreSQL durability and comparator-shape receipts and making no PERF-008 pass claim
  - Proposed ADR-0127 defining the optional bounded long-lived multiplexed application session;
    no protocol implementation, PERF-018 amendment, or release-evidence substitution is permitted
    until the ADR's exact text is separately accepted
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-client-rust --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The frozen and shadow paths report separately; paired evidence determines how much
    latency is benchmark bridging versus customer-paid transport/orchestration; TCP_NODELAY is
    proven; and any multiplexed-session implementation remains blocked on exact ADR acceptance.
  standing_design_tests:
    interface_safety: The shadow probe invokes the same generated named operations, values,
      credentials, authorization, bounds, and service path as the frozen driver. It adds no
      operation, transaction, retry, durability, policy, or transport choice to an application.
    scale: Diagnostic samples and histograms are fixed-cardinality and bounded; the shadow uses one
      existing persistent channel and retains no keys, values, credentials, or unbounded trace
      series. The proposed session ADR requires explicit finite in-flight, byte, and queue bounds.
  closure:
    status: complete
    evidence: docs/performance/wp-632-client-transport-attribution.md
    decision: Paired N1 and E2 samples separate the frozen synchronous bridge, asynchronous client,
      transport, and server orchestration without changing the PERF-018 comparator. TCP_NODELAY is
      source-pinned, both reports are structurally non-evidentiary, and ADR-0127 remained blocked
      until its later exact acceptance.
  human_review_triggers: *id001
- id: WP-634
  name: Compiler-declared RiffQL secret outputs and derived role authority
  depends_on: [WP-565, WP-572, WP-600, WP-612]
  objective: Implement ADR-0128's exact leaf-level RiffQL secret-output declaration, V4 query and
    role identities, least compiler-derived dedicated field visibility, shared fail-closed runtime
    release, and initial SDK-only surface boundary without activating installation reconciliation.
  requirements: [QSO-001, QSO-002, QSO-003, QSO-004, QSO-005, QSO-006, QSO-007]
  required_adrs: [ADR-0038, ADR-0055, ADR-0108, ADR-0111, ADR-0112, ADR-0118,
    ADR-0124, ADR-0128]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0128-compiler-declared-secret-outputs-for-named-riffql.md,
    release/version-topology-v1.json,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-types/**, crates/riffdb-catalog/**, crates/riffdb-auth/**,
    crates/riffdb-policy/**, crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-mcp-stdio/**,
    clients/editor/tree-sitter-riffql/**,
    fixtures/riffql/**, fixtures/query-modules/**, fixtures/application-locks/**,
    fixtures/application-manifests/**, fixtures/application-parity/**,
    fuzz/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Contextual exact `reveals binding.secret_field` leaf grammar, formatter, AST, source maps, safe
    explain/catalog/schema metadata, 1,024-leaf bound, and source-spanned negative corpus for every
    missing, duplicate, ordinary, unresolved, mismatched, aggregate, record, or excess declaration
  - Least-sufficient RiffQL language V3, query IR/module V4 envelope, and compiled-role codec V4
    carrying canonical per-output requirements, dedicated secret field IDs, and ordered symbolic
    query/entity/field atoms while freezing declaration-free V1 through V3 artifacts byte-for-byte
  - Role compiler architecture proving no application source, ad-hoc query, dependency field,
    wildcard, policy, index, primary key, or predicate can manufacture secret output authority
  - Shared-service success/denial, delegation, revocation, row-policy, partial-release, mixed-runtime,
    and identity-substitution tests retaining `RDB-AUTH-0214` whenever exact authority is absent
  - Generated/catalog architecture checks that keep typed SDK/gRPC execution available while
    omitting invocable MCP tools, redacting CLI/diagnostics, and rejecting watch/live/contextual or
    other reactive references at compile time
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module --all-features
  - cargo +1.97.0 test -p riffdb-types -p riffdb-auth -p riffdb-policy -p riffdb-service -p riffdb-api-mcp --all-features
  - cargo +nightly-2026-07-12 fuzz run riffql_parser -- -max_total_time=60
  - ./scripts/check-version-topology
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every newly compiled returned secret leaf has one exact declaration and V4 identity,
    selected named roles derive only its least dedicated visibility, absent authority denies before
    any release, old declaration-free artifacts remain exact, and no MCP/reactive path can expose it.
  standing_design_tests:
    interface_safety: Applications select only an immutable named query and typed values; they
      cannot name field authority, convert annotations into grants, bypass current authorization,
      or opt a secret-bearing query into an agent, stream, raw, kernel, or unredacted surface.
    scale: Static result and role sets are count/byte bounded, and runtime reuses the existing exact
      field-visibility release check without adding a scan, join, queue, retained row, or global lock.
  closure:
    status: complete
    evidence: docs/riffql/LANGUAGE.md
    decision: Exact leaf-level reveals declarations select least-sufficient V4 query and role
      identities, derive only dedicated returned-field authority, retain shared fail-closed release,
      and remain excluded from MCP and reactive surfaces. The acceptance rerun found and fixed a
      contextual-reveals formatter ambiguity; its regression test, reproducer, and subsequent
      60-second RiffQL fuzz cell pass with all compiler, policy, service, topology, and handbook gates.
  human_review_triggers: *id001
- id: WP-635
  name: Exact secret-output widening approval and generated-client conformance
  depends_on: [WP-568, WP-569, WP-611, WP-612, WP-634]
  objective: Extend application installation to diff and approve exact symbolic secret-output
    authority, publish least-sufficient V3 plans, and prove every generated language can perform an
    authorized secret-bearing named read without silent widening or a reveal-shaped command.
  requirements: [QSO-008, QSO-009, QSO-010, QSO-011]
  required_adrs: [ADR-0055, ADR-0057, ADR-0106, ADR-0110, ADR-0112, ADR-0117,
    ADR-0118, ADR-0120, ADR-0124, ADR-0128]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0128-compiler-declared-secret-outputs-for-named-riffql.md,
    release/version-topology-v1.json,
    crates/riffdb-application/**, crates/riffdb-query-module/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**,
    crates/riffdb-cli/**, crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    clients/**, templates/**, fixtures/installation/**,
    fixtures/application-locks/**, fixtures/application-manifests/**,
    fixtures/application-parity/**, fixtures/adapters/**, fixtures/driver/**,
    examples/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - Closed symbolic `Operation` and `QuerySecretOutput` authority atoms with exact canonical desired
    and observed sets capped at 1,024 of each and 2,048 total, containing no IDs, values, source
    text, capability bytes, hidden schema, or unbounded diagnostic content
  - Exact predecessor-bound widening approval for every added atom, including unchanged-operation
    and already-role-visible field cases, plus narrowing, stale/excess/missing/noncanonical refusal,
    identity-only rotation, successor proof, revocation, resume, and no-overwrite evidence
  - Least-sufficient installation-plan V3 with optional reimport, frozen V1/V2 bytes and readers,
    old-installer refusal, exact receipt binding, and complete installation-plan/query/role topology
    registration and compatibility fixtures
  - Generated Rust, Go, TypeScript, and Python result metadata, redaction, success, missing-authority,
    revocation, malformed-host, mixed-version, and identical semantic observation conformance over
    the shared named-query service path
  - Handbook security, installation, compatibility, SDK, and adapter guidance distinguishing source
    disclosure intent, derived runtime authority, reviewed widening, SDK release, and MCP/reactive
    exclusion without documenting the deferred Better Auth external campaign as complete
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-application -p riffdb-query-module -p riffdb-client-rust -p riffdb-driver-host --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-driver-development-registry
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every secret-output addition is visible and exactly approved before successor
    capability creation, plans without the feature retain V1/V2 bytes, old installers refuse V3,
    and all generated languages observe the same authorized named-query result and fail-closed denial.
  standing_design_tests:
    interface_safety: Installation exposes safe symbolic atoms only, cannot omit or broaden an
      approval, and never mutates a credential in place; generated applications receive only the
      named typed query method and no raw field, capability, transaction, transport, or MCP escape.
    scale: Authority sets, plans, diffs, approvals, receipts, client values, and diagnostics are
      count/byte bounded and add no database scan, retained stream, global state, or per-row metadata.
  closure:
    status: complete
    evidence: docs/operations/APPLICATION-INSTALLATION-CAMPAIGNS.md
    decision: Installation-plan V3 presents exact bounded Operation and QuerySecretOutput atoms,
      requires predecessor-bound approval for every widening, narrows without silent authority,
      and rotates credentials rather than mutating them. Rust, Go, TypeScript, and Python pass the
      shared remote secret-output corpus, while binding, development-registry, topology, generated,
      requirement, handbook, formatting, and workspace Clippy gates all pass.
  human_review_triggers: *id001
- id: WP-636
  name: Operation-count growth and exact named-query execute attribution
  depends_on: [WP-377, WP-487, WP-488, WP-627, WP-632]
  objective: Reproduce and attribute the super-linear cloud dilation and operation-count growth of
    the exact generated GetTicket read path before authorizing caching, validation, decoding,
    storage, or transport changes.
  requirements: [PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0055, ADR-0056, ADR-0104, ADR-0106, ADR-0123]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**,
    crates/riffdb-observability/**, crates/riffdb-service/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-proto/**, crates/riffdb-storage-redb/**, crates/riffdb-server/**,
    docs/performance/**, scripts/**]
  deliverables:
  - A distinct non-evidentiary fixed-GetTicket diagnostic that records bounded sequential windows
    at 500 and 10,000 c=1 samples, process smaps-rollup deltas at the same boundaries, exact server
    execute windows by operation ordinal, and no caller values, keys, credentials, or unbounded
    sample series
  - Closed execute-stage timing for snapshot acquisition/frontier capture, overlay or checkpoint
    point lookup, structural preflight and checksum, canonical decode/verification, query row and
    policy materialization, query-program assembly, and owned-result release, with every existing
    integrity and authorization check still executed
  - Workstation and one isolated cloud-host receipts for 500 and 10,000 c=1 samples plus the
    safe-application PostgreSQL GetTicket twin, reporting absolute dilation and per-window slope
    rather than hiding growth in one histogram
  - A workstation c=32 off-CPU and lock-wait capture with full symbols that distinguishes HTTP2 or
    runtime queueing, storage/read-view contention, allocator pressure, and executor CPU without
    changing the frozen PERF-018 comparator
  - Predeclared mixed-workload Amdahl bounds for the observed growing structure and for
    validate-once, decoded-entity cache, and lazy partial-decode candidates; no candidate may be
    implemented in this package or counted as a product gain
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-observability -p riffdb-query-executor -p riffdb-storage-redb -p riffdb-service -p riffdb-server --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The 500-to-10,000 operation growth is either reproduced with one bounded named
    retaining structure and sub-stage slope or falsified on both workstation and cloud; exact
    GetTicket costs have a safe-PostgreSQL target; c=32 waiting is classified; and optimization
    work remains blocked on predeclared mixed-workload arithmetic and a separate work package.
  standing_design_tests:
    interface_safety: Instrumentation observes only closed stage identities, operation ordinals,
      aggregate durations, and process resource totals. It neither changes nor bypasses the named
      query, authorization safe points, snapshot, integrity verification, row policy, result
      release, transport, freshness, or frozen comparator.
    scale: Execute windows, memory samples, stage identities, and lock-wait classes have fixed
      count and byte ceilings independent of database size and request count; excess samples merge
      into one terminal bounded window rather than retaining per-request state.
  closure:
    status: complete
    evidence: docs/performance/wp-636-query-execute-growth.md
    decision: Workstation, N1, and E2 fixed-operation windows reproduced and attributed the growth
      to application-frontier resolution over retained command authority, while the c32 capture
      classified the separate waiting component. The bounded diagnostic changed no comparator or
      product behavior and supplied the predeclared arithmetic used by WP-637.
  human_review_triggers: *id001
- id: WP-637
  name: Restore constant-time named-query application frontiers
  depends_on: [WP-481, WP-488, WP-636]
  objective: Restore the allocator-backed constant-time application frontier on every restarted,
    current, durable, and composite named-query path without weakening command-authority,
    retention, snapshot, recovery, or corruption guarantees.
  requirements: [PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0055, ADR-0056, ADR-0104, ADR-0106, ADR-0123]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-server/**,
    docs/performance/**, scripts/**]
  deliverables:
  - One same-snapshot allocator-to-application-head mapping for empty, next, exhausted, retained,
    and pruned states, with a constant-time command-authority presence check and fail-closed gross
    disagreement handling on current, durable, and composite reads
  - Actual `QueryExecutionPort` regression coverage proving a restarted named query does not decode,
    checksum, hash, re-encode, or allocate from retained command segments while startup, recovery,
    retention, explicit history reads, backup, and replication retain their complete validation
  - Pre/post workstation and isolated N1/E2 fixed-GetTicket plus public interactive c32 receipts,
    evaluated against WP-636's predeclared +100% c1, +35% c32, and no-regression gates without
    changing the PERF-018 client, transport, PostgreSQL, seed, or workload shapes
  - Updated performance note linking the production regression to commit `53d73087`, reporting
    absolute core-seconds and cloud dilation, and preserving the safe-application PostgreSQL target
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-query-executor -p riffdb-storage-redb -p riffdb-server --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: Restarted exact named queries recover WP-481's constant-time frontier without decoding
    retained command payloads, all corruption and recovery tests remain green, c1 and c32 meet the
    predeclared public gains on workstation and cloud, and no frozen comparator shape changes.
  standing_design_tests:
    interface_safety: The application continues to select only a named typed query and receives the
      exact same authorized snapshot and freshness frontier; no caller can select the fast path,
      suppress integrity validation, provide a frontier, or bypass retention and authority checks.
    scale: Frontier resolution performs a fixed number of same-transaction metadata and authority-
      presence lookups independent of retained history, segment width, entity count, or request count,
      and adds no cache, retained per-request state, scan, queue, or unbounded diagnostic surface.
  closure:
    status: complete
    evidence: docs/performance/wp-637-constant-time-query-frontier.md
    decision: Current and restarted named reads now derive the application frontier from same-
      snapshot allocator metadata plus constant-time command-authority presence, without decoding
      retained command payloads. Workstation, N1, and E2 receipts meet the predeclared gains, and
      current-HEAD query, recovery, storage, server, and comparator tests pass.
  human_review_triggers: *id001
- id: WP-638
  name: Closed public write-lane baseline and serialized-service ledger
  depends_on: [WP-627, WP-637]
  objective: Re-baseline the post-WP-637 public application workload against the frozen safe-
    application PostgreSQL comparator, then close the client-observed CreateComment write latency
    into bounded admission, coordinator, journal, fence, publication, acknowledgement, and queue
    components before authorizing any writer-pipeline redesign.
  requirements: [PERF-001, PERF-004, PERF-005, PERF-008, PERF-018]
  required_adrs: [ADR-0058, ADR-0059, ADR-0104, ADR-0123, ADR-0127]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**,
    crates/riffdb-observability/**, crates/riffdb-service/**,
    crates/riffdb-commit/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-server/**,
    docs/performance/**, scripts/**]
  deliverables:
  - Post-WP-637 N1 and E2 frozen-protocol ratio tables for interactive c1, c8, and c32 plus every
    representative unary scenario against same-run safe-application PostgreSQL, with durability,
    transport, seed, workload, correctness, and environment identities retained
  - One bounded redaction-safe CreateComment c1 ledger whose admission, coordinator validation and
    apply, journal submit, durable fence, publication, acknowledgement, and residual components sum
    to the client-observed mean within five percent on the workstation and one isolated cloud host
  - Public c32 queue-wait versus serialized-service decomposition using the existing bounded writer
    histograms, explicitly separating queueing from durability-fence and sequencing-essential work
  - A measured classification of movable versus admission-ordered sequencing-essential work and a
    report to the maintainer before any coordinator implementation, transport work, derived-consumer
    work, comparator expansion, compression, batching-window, cache, or lazy-decode candidate begins
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-observability -p riffdb-service -p riffdb-commit -p riffdb-storage-redb -p riffdb-server --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The two-host comparator gap is current and reproducible, c1 write latency closes to
    client mean within five percent, c32 queueing is separated from serialized service, and every
    measured writer component is classified as movable or sequencing-essential before a candidate
    design or implementation package is proposed.
  standing_design_tests:
    interface_safety: Instrumentation observes only closed stage identities, bounded aggregate
      durations, queue counts, and public outcome classes; it cannot alter admission order,
      authorization, conflict ownership, commit sequencing, durability, visibility, response
      release, uncertainty handling, or the frozen comparator.
    scale: Stage ledgers and queue evidence have fixed count and byte ceilings per process
      generation, retain no application values or per-operation series, and perform no history,
      entity, index, event, provenance, audit, or outbox scan.
  closure:
    status: complete
    evidence: docs/performance/wp-638-write-lane-ledger.md
    decision: Frozen-protocol N1 and E2 baselines are current, the workstation and cloud c1 ledgers
      close within five percent of client-observed mean, and c32 queue delay is separated from
      serialized service and physical fence work. The report classified movable and sequencing-
      essential work before the coordinator design began.
  human_review_triggers: *id001
- id: WP-639
  name: Batch writer ledger and admission-ordered coordinator proof split
  depends_on: [WP-620, WP-638]
  objective: Close the public seed path into disjoint per-command preparation, apply, journal,
    fence, and pipeline costs on the workstation and isolated N1 host, reconcile the frozen
    comparator history, and design a stronger coordinator candidate sized against every PERF-008
    gate without changing command ordering or commit behavior.
  requirements: [PERF-001, PERF-004, PERF-005, PERF-008, PERF-018]
  required_adrs: [ADR-0058, ADR-0059, ADR-0104, ADR-0123, ADR-0127]
  allowed_paths: [work_packages.yaml, examples/app-baseline/**,
    crates/riffdb-observability/**, crates/riffdb-commit/**,
    docs/performance/**, adr/**, scripts/**]
  deliverables:
  - Workstation and isolated N1 seed receipts whose writer wall closes to measured seed wall and
    whose disjoint per-command ledger reports evaluation, validation/encoding/staging, final apply,
    journal submission, fence latency and physical fence work, publication, idle, and residual cost
  - A receipted explanation of the ADR-0123 workstation comparator-ratio drift identifying safe-
    application code, PostgreSQL version/configuration, harness topology, and workload-shape changes
    or proving each unchanged, without rewriting or relaxing the frozen PERF-018 comparator
  - An explicit proof boundary separating deterministic preparation from current-state revalidation,
    sequence assignment, conflict ownership, and final apply, with bounded-pool preparation consumed
    in admission order and no global-order assumption beyond ADR-0059 conflict domains
  - Predeclared workstation and N1 queueing and Amdahl predictions for mixed c32, seed, unary writes,
    reads, correctness, durability, uncertainty, and tails; the coordinator candidate must be sized
    against all three release gate families rather than the mixed-throughput gate alone
  - A separate unary-latency ownership note retaining ADR-0127 and pre-fence work as deferred work,
    plus c32 evidence preserving the relationship between group size, fence bytes, and write p95
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-observability -p riffdb-commit --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The workstation and N1 seed ledgers close, comparator history is traceable, and a
    sequencing-safe coordinator proof plus all-gate predictions are ready for explicit maintainer
    review; this package makes no coordinator, ordering, conflict, durability, or public-interface
    behavior change.
  standing_design_tests:
    interface_safety: Evidence observes only fixed stage identities, bounded aggregate durations,
      counts, and byte totals; the proposed split retains fresh authority, admission-order
      revalidation, conflict ownership, commit sequencing, atomic publication, durable uncertainty,
      and the frozen public comparator, and no caller can select the preparation path.
    scale: Timers, stage identities, proof artifacts, and ratio receipts have fixed cardinality and
      byte ceilings independent of command count, history, entity count, or database size; bounded
      preparation capacity must apply backpressure without reordering or retaining unbounded work.
  closure:
    status: complete
    evidence: docs/performance/wp-639-batch-writer-ledger.md
    decision: Workstation and N1 seed ledgers close to measured wall time, the historical comparator
      drift is traced to an interfered unstable PostgreSQL run rather than shape drift, and the
      admission-ordered proof split plus all-gate predictions received maintainer review in
      ADR-0129 without changing coordinator or public behavior.
  human_review_triggers: *id001
- id: WP-640
  name: Bounded prepared command finalization and pay-once batch apply
  depends_on: [WP-639]
  objective: Implement ADR-0129's bounded pure-preparation, admission-order finalization, private
    logical frontier, and checked pay-once group apply only after the mechanics prove that the
    candidate can satisfy mixed c32 and seed gates without weakening unary, durability, or safety.
  requirements: [PERF-001, PERF-004, PERF-005, PERF-008, PERF-018]
  required_adrs: [ADR-0058, ADR-0059, ADR-0060, ADR-0094, ADR-0095, ADR-0096, ADR-0097,
    ADR-0098, ADR-0099, ADR-0101, ADR-0104, ADR-0123, ADR-0129]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/**, crates/riffdb-application/**,
    crates/riffdb-runtime/**, crates/riffdb-commit/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-storage-memory/**, crates/riffdb-observability/**, crates/riffdb-service/**,
    crates/riffdb-server/**, examples/app-baseline/**, tests/**, scripts/**,
    docs/performance/**]
  deliverables:
  - Reject-first mechanics for pure prepared bodies, ordered finalization proofs, prepared canonical
    fragments, and checked group apply demonstrating ADR-0129's workstation and N1 CPU thresholds
    before the production dispatch path changes
  - Fixed bounded preparation pool, FIFO admission-order consumption, one-group reorder ceiling,
    private logical frontier, exact rollback, cancellation, shutdown, and worker-panic semantics
  - Frontier-equivalence invariant proving after every epoch that the private logical frontier is
    identical to the applied entity, index-generation, allocator, command-identity, and application-
    frontier state, exercised under deterministic schedules and by a bounded debug assertion before
    publication
  - Checked storage apply that consumes proven canonical material once and architecture tests proving
    no repeated normalization, semantic validation, decode, graph reconstruction, or hashing
  - Fixed-cardinality production counters or histograms for preparation-pool depth, reorder-buffer
    occupancy, proof mismatch, and epoch rollback, retained in shutdown evidence without application
    values or command-, tenant-, entity-, or history-dependent labels
  - Deterministic schedules and process crash arms for every preparation, current-proof, sequence,
    ordered-apply, journal, fence, publication, uncertainty, and response boundary
  - Same-run workstation and N1 safe-application PostgreSQL receipts for seed, mixed c32, unary,
    reads, write p50/p95, group size, frame bytes, physical fence, and ordered fence latency
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-application -p riffdb-runtime -p riffdb-commit -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-observability -p riffdb-service -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-conflict --features shuttle --test shuttle_model
  - ./scripts/recovery_full
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Production activation occurs only if mechanics meet ADR-0129's preparation and ordered-
    lane thresholds, both hosts meet mixed c32 and seed ratios, unary/read/tail gates do not regress,
    the frontier-equivalence assertion holds after every explored epoch, new failure-mode telemetry
    is present in shutdown evidence, and every ordering, conflict, durability, crash, recovery, and
    uncertainty test remains exact;
    unary parity remains a separate release blocker when it is still above 1.10x.
  standing_design_tests:
    interface_safety: Applications continue to submit only compiled typed commands and cannot
      select workers, proofs, grouping, ordering, storage apply, transactions, durability, or
      fallback; every command retains current authorization, dependencies, row policy, conflict
      ownership, idempotency, atomic graph, and typed uncertainty.
    scale: Worker count, preparation capacity, reorder state, logical frontier, proof material, and
      prepared groups are capped by existing process, command, group, and byte ceilings and require
      neither co-located full state nor global preparation ordering across conflict domains.
  closure:
    status: closed_without_release_activation
    evidence: docs/performance/wp-640-closure.md
    decision: The bounded evaluation and pay-once proof work remains because it improved both
      workstation and cloud results without weakening semantics, but the ordered-lane mechanics,
      seed, mixed-c32, unary, and complete recovery gates did not all pass. The independent command-
      segment preparation pool is disabled on cloud-sized hosts, and WP-640 makes no release-pass
      claim.
    reliability_owners: [WP-641, WP-642, WP-643]
  human_review_triggers: *id001
- id: WP-641
  name: Cold-start service-audit availability reliability
  depends_on: [WP-640]
  objective: Reproduce and repair the fail-closed AuditUnavailable stop observed in both the exact
    WP-640 baseline and candidate during the first commands of a cold E2/N1 seed, before checkpoint
    activation, without weakening authoritative readiness or allowing unaudited application work.
  requirements: [PERF-008, REC-001, REC-002, STO-001]
  required_adrs: [ADR-0007, ADR-0049, ADR-0058, ADR-0104]
  allowed_paths: [work_packages.yaml, crates/riffdb-observability/**, crates/riffdb-service/**,
    crates/riffdb-server/**, crates/riffdb-commit/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, examples/app-baseline/**, tests/**, scripts/**,
    docs/performance/**]
  deliverables:
  - A deterministic or explicitly hooked reproduction that reaches AuditUnavailable before the
    checkpoint-start threshold on both the current implementation and the exact retained baseline
  - Root-cause evidence covering service-audit append, readiness failure, actor stop, and any storage
    dependency, plus a fail-closed repair with no sequence, graph, response, or audit omission
  - Cold and warm N1/E2 seed repetitions proving no recurrence, no hidden retry, and exact audit,
    command, outcome, and readiness counts
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-observability -p riffdb-service -p riffdb-server -p riffdb-commit -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The cold-start stop is reproducible before the fix and absent after it on both cloud
    profiles, while every service audit remains authoritative, readiness remains fail-closed, and
    no committed or uncertain command is reclassified.
  standing_design_tests:
    interface_safety: The repair preserves mandatory service auditing and authoritative readiness;
      no caller can bypass, defer, suppress, or retry around an audit failure.
    scale: The repair adds no history scan, unbounded retry, per-command retained label, or queue
      whose capacity depends on workload duration, tenant count, or audit history.
  closure:
    status: complete
    evidence: docs/performance/wp-641-cold-start-audit.md
    decision: Cold E2 evidence isolated the primary stop to redb detached staging treating a valid
      index-free writer-private group as a missing generation-base invariant. Staging now accepts
      absence only when every retained write plan has zero index-generation advances and preserves
      the exact captured generation map; an advance still requires the retained base. Three cold
      and one warm-host full-seed cells pass on each E2 and N1 with exact completion and inventory
      counts, zero frontier-equivalence failures, and unchanged fail-closed audit behavior.
  human_review_triggers: *id001
- id: WP-642
  name: Post-measurement authoritative inventory reliability
  depends_on: [WP-640]
  objective: Reproduce and repair the WP-640 case where every measured operation succeeded but the
    final process could not return the authoritative table inventory, so benchmark correctness and
    shutdown evidence remain a mandatory closed boundary rather than a best-effort epilogue.
  requirements: [PERF-018, REC-001, STO-001, STO-002]
  required_adrs: [ADR-0004, ADR-0104, ADR-0123]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-server/**, examples/app-baseline/**, tests/**,
    scripts/**, docs/performance/**]
  deliverables:
  - A process-level reproduction that separates operation success, shutdown, final frozen read view,
    table inventory, and database reopen so the failing boundary is unambiguous
  - A storage or harness repair that never reports a benchmark cell correct when its mandatory
    inventory cannot be proven, with typed bounded diagnostics and no value disclosure
  - Repeated workstation and cloud post-measurement inventory/reopen receipts with exact table,
    frontier, journal, and checkpoint closure
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The original final-inventory failure is deterministic before the fix, every repaired
    process closes inventory and reopen evidence, and an unprovable epilogue still fails the cell.
  closure:
    status: complete
    evidence: docs/performance/wp-642-authoritative-inventory.md
    decision: The old epilogue inventoried only the raw redb checkpoint and therefore neither
      proved the checkpoint-plus-journal durable unit nor identified its failing stage. The fixed
      path performs one ordinary structural reopen and recovery, closes it, then reads all 13
      fixed authoritative tables from one frozen transaction. It never retries or reports a
      partial inventory. Deterministic tests prove both exclusive-reopen refusal and the former
      false-green case where an invalid journal coexists with a readable raw redb inventory;
      repeated workstation, E2, and N1 process receipts close the production boundary.
  standing_design_tests:
    interface_safety: The package changes only internal conformance evidence and cannot expose a
      storage handle or let an application waive integrity, shutdown, inventory, or reopen checks.
    scale: Inventory remains a bounded gate operation over the fixed authoritative table catalog and
      retains neither application values nor an operation-count-dependent history.
  human_review_triggers: *id001
- id: WP-643
  name: Process recovery ready-shutdown ordering reliability
  depends_on: [WP-640]
  objective: Repair the recovery_full process launcher and server lifecycle contract where shutdown
    evidence can arrive before the harness's required ready line, while preserving every crash,
    maintenance, durability, and recovery assertion and without converting the package-level green
    matrix into a substitute for the process boundary.
  requirements: [REC-001, REC-002, STO-001, STO-002]
  required_adrs: [ADR-0004, ADR-0049, ADR-0050, ADR-0104]
  allowed_paths: [work_packages.yaml, crates/riffdb-server/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-testkit/**, tests/recovery/**,
    scripts/recovery_full, scripts/**, docs/performance/**]
  deliverables:
  - An exact-baseline and current-HEAD process reproduction naming the lifecycle state that emits
    shutdown evidence before readiness
  - One explicit ready, maintenance, stop, and evidence protocol with bounded diagnostics and no
    sleeps, ignored output, reordered crash arm, or relaxed expected result
  - A complete green process recovery matrix plus the unchanged package-level storage recovery cases
  acceptance_commands:
  - ./scripts/recovery_full
  - cargo +1.97.0 test -p riffdb-storage-redb -p riffdb-server --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The exact baseline demonstrates the old ordering failure, current HEAD completes every
    process crash and maintenance arm, and no readiness, shutdown, recovery, or durability assertion
    is removed or weakened.
  closure:
    status: complete
    evidence: docs/performance/wp-643-recovery-lifecycle.md
    decision: The failure was the successful single-database offline-maintenance rebound state,
      where the graph correctly emitted eight shutdown-evidence lines before the replacement graph
      published readiness but the process controller required every next stdout line to be ready.
      The recovery gate now consumes the exact ordered evidence block and following ready line as
      one bounded transition under one deadline. Missing, extra, reordered, disconnected, or timed
      out evidence fails with a closed diagnostic; no output, crash arm, or durability assertion is
      skipped. The nine-arm process matrix and unchanged storage recovery cases pass.
  standing_design_tests:
    interface_safety: The repair is confined to server lifecycle and recovery evidence; it cannot
      make an unhealthy database ready or let any caller skip recovery, maintenance authority, or
      durable validation.
    scale: The protocol uses fixed lifecycle states, bounded output, and fixed deadlines and adds no
      polling history, unbounded log retention, or sleep-based synchronization.
  human_review_triggers: *id001
- id: WP-644
  name: Bounded multiplexed generated-application session candidate
  depends_on: [WP-641, WP-642, WP-643]
  objective: Implement ADR-0127's optional bounded long-lived generated-operation session only
    after refreshing the current-HEAD N1/E2 gates, preserving one independently authenticated,
    authorized, idempotent, durable, and freshness-checked operation per envelope and retaining
    unary as the default unless every activation gate passes.
  requirements: [PERF-005, PERF-008, PERF-018, API-001, QRY-001, ID-001, SEC-001,
    SEC-002, NET-001, NET-002, NET-003]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0105, ADR-0106, ADR-0123, ADR-0127]
  allowed_paths: [work_packages.yaml, adr/0127-bounded-multiplexed-application-session.md,
    proto/riffdb/v1/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-query-module/**, crates/riffdb-server/**,
    examples/app-baseline/**, examples/agent-alpha/**, examples/ticketdesk/**, fixtures/**,
    tests/**, scripts/**, docs/**]
  deliverables:
  - Current-HEAD paired N1/E2 mixed c1/c8/c32/c128, dedicated unary, and seed receipts with exact
    safe-application PostgreSQL comparisons and candidate arithmetic frozen before protocol edits
  - Additive versioned bidirectional generated-operation session envelopes with one exact database,
    credential presentation, application identity, finite negotiated in-flight ceiling, correlation
    identities, cancellation, bounded response buffering, feature negotiation, and unary fallback
  - Server dispatch that re-enters the ordinary gRPC adaptation, authentication, authorization,
    API-neutral service, command/query runtime, freshness, durability, uncertainty, and error paths
    for every envelope, with no session transaction, authority cache, ordering, or storage access
  - Rust generated-client integration and an explicitly non-evidentiary app-baseline session shape,
    retaining unary side-by-side and reporting the selected transport; other language comparators
    remain deferred and continue through semantically equivalent unary fallback
  - Deterministic and process tests for out-of-order completion, duplicate/unknown correlation,
    bounded capacity, slow consumer, query and command cancellation, stream loss, reconnect,
    revocation, expiry, application drift, database routing, read-after-commit, and uncertainty
  - Same-run N1/E2 candidate receipts against ADR-0127's refreshed activation gates; a miss keeps
    the session disabled by default and cannot amend PERF-018 or claim a release pass
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/recovery_full
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The optional session is semantically indistinguishable from unary per operation,
    remains finite and fail-closed under every lifecycle edge, and activates as a release candidate
    only if both hosts meet the refreshed c1/c8 gains, c32 throughput and p95 ratios, every unary
    ratio, the 5.0x seed ceiling, correctness, recovery, and no-regression gates; otherwise unary
    remains the generated-client default and PERF-018 remains unchanged.
  standing_design_tests:
    interface_safety: The session accepts only generated named commands and exact named queries and
      cannot express a transaction, implicit ordering, relaxed durability, cached authorization,
      raw method, numeric symbol, storage behavior, freshness waiver, or unsafe retry; each envelope
      traverses the same application service and retains its own idempotency and uncertainty state.
    scale: Session lifetime work, in-flight operations, request/response bytes, correlation state,
      cancellation state, output buffering, and task count are fixed and finite; no session retains
      operation history, values, durable responses, or capacity proportional to data or runtime.
  closure:
    status: complete_without_release_activation
    evidence: docs/performance/wp-644-session-candidate.md
    decision: The optional bounded generated-operation session preserves ordinary per-envelope
      authentication, authorization, idempotency, durability, freshness, cancellation, uncertainty,
      and finite lifecycle semantics and materially improves mixed throughput. It remains explicit
      and disabled by default because both cloud profiles miss the c32 p95 gate and no dedicated
      representative scenario reaches the 1.10x ratio on both hosts. PERF-018 and unary defaults
      remain unchanged. A reproducible graceful-shutdown stall discovered during candidate evidence
      is repaired by an explicit generated-client session close and clean N1/E2 reruns.
  human_review_triggers: *id001
- id: WP-645
  name: Projection result-provider descriptor and real-engine conformance foundation
  depends_on: [WP-430, WP-432, WP-594, WP-641, WP-642, WP-643]
  objective: Freeze ADR-0130's least sufficient durable provider descriptor, result-set stage and
    epoch-proof vocabulary, then exercise it independently against the existing columnar and per-
    organization exact/ANN vector engines without adding a cross-provider executor, bridge, BM25
    runtime, or application-visible query behavior.
  requirements: [OQ-017, OQ-018, OQ-019, OQ-020, OQ-021, OQ-022, OQ-023, OQ-024]
  required_adrs: [ADR-0002, ADR-0051, ADR-0053, ADR-0086, ADR-0087, ADR-0091,
    ADR-0092, ADR-0111, ADR-0124, ADR-0129, ADR-0130]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0130-compiler-planned-projection-result-sets.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-catalog/**,
    crates/riffdb-projection/**, crates/riffdb-columnar/**,
    fixtures/query-modules/**, fixtures/version-topology/**, tests/**, scripts/**,
    docs/architecture/**]
  deliverables:
  - Canonical sealed V1 provider descriptor and digest carriage for exact/approximate posture,
    candidate/filter/rank/order/measure/facet/window capabilities, ranked policy mode, freshness,
    servable-epoch retention, lifecycle, static costs, and provider-state identities, registered in
    ADR-0124 topology before any consumer writes it
  - Minimal semantic result-set IR ordering candidates, policy/filtering, rank/order, whole-set
    measures, window, and typed output, with compiler-pinned provider identity and no runtime
    provider selection, fallback, scan, materialization, or public programmable dataflow
  - Independent columnar adapter/reference suite proving filtering, ordering, aggregate/facet shape,
    partition policy, and frontier behavior, plus an exact/ANN vector adapter/reference suite proving
    ranking, approximation/recall, per-organization statistics, pre-rank filtering, and matched epochs;
    each engine explicitly rejects the other's unsupported capabilities
  - Servable-interval negotiation reference with newest-common-epoch properties, incarnation and
    generation binding, cursor/lease retention rules, typed divergence/retirement/freshness outcomes,
    and deterministic lag/rebuild schedules that turn sustained admitted-load divergence into
    degraded/unavailable health rather than normal success-path refusal
  - Architecture evidence that descriptor validation is once per deployment/catalog generation and
    epoch negotiation is once per opened result set, never per row, candidate, measure, item, page,
    or provider call, while current authorization remains per request and safe point
  - Non-normative compile-only ADR-0092 descriptor sketch and negative dependency/artifact evidence
    proving this package contains no BM25 runtime, durable BM25 identity, bitmap/ID transfer,
    cross-provider bridge, or composite executor
  - Maintainer scheduling receipt that WP-641 through WP-643 plus the standing c=32, unary, and
    ADR-0127 priorities are cleared or explicitly reprioritized before implementation begins; the
    package stops for review if this foundation grows beyond the bounded descriptor/conformance slice
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog -p riffdb-projection -p riffdb-columnar --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Both real provider families pass independent descriptor, policy, bound, reference, and
    frontier conformance; newest-common-epoch availability holds under admitted lag schedules;
    descriptor and epoch proofs are pay-once at their declared scopes; every new durable/release
    identity is registered; and no composite runtime, bridge, BM25 implementation, or new public
    query behavior exists. Alpha-critical WP-646 may begin immediately from this frozen least-
    sufficient slice without waiting for optional foundation plumbing.
  closure:
    status: complete
    evidence: docs/architecture/OVERVIEW.md
    decision: Projection-provider descriptor V1, projection result-set plan V1, and named binding
      V1 are separately canonical, digest-bound, and topology-registered without rotating query IR
      or query-module V1 through V4. The real columnar and exact/ANN vector engines advertise only
      their independently exercised capabilities and reject the other engine's unsupported stages.
      Descriptor validation occurs once per deployment/catalog generation, newest-common-epoch
      negotiation occurs once per opened result set or exact continuation, and a nonserializable
      bounded proof binds plan, compiled policy shape, descriptor, state schema, incarnation,
      generation, and retained interval.
      Sustained admitted lag degrades then refuses normal success. The package contains no public
      activation, composite executor, cross-provider bridge, lexical runtime, durable lexical
      state, or caller-selectable provider behavior.
  standing_design_tests:
    interface_safety: Descriptors and plans are sealed compiler artifacts; applications cannot
      choose a provider, policy mode, epoch, precision, cost, bridge, scan, or fallback, and every
      unsupported combination fails closed before deployment or execution.
    scale: Descriptors are fixed size, proofs have a fixed participant ceiling, epoch negotiation
      is proportional only to the finite compiled participant count, engines remain partition scoped,
      and the package adds no full-
      database memory, authoritative-storage co-location, or cross-provider materialization.
  human_review_triggers: *id001
- id: WP-646
  name: Exact text provider profiles, durable state, and compiled access plans
  depends_on: [WP-563, WP-564, WP-645]
  objective: Implement ADR-0131's provider-shaped exact binary UTF-8 equals, prefix, suffix, and
    contains profiles with versioned bounded rebuildable index state, compiler-enumerated policy-
    scoped access plans, and independent reference equivalence, without public runtime activation
    or Better-Auth-specific branches.
  requirements: [OQ-017, OQ-019, OQ-021, OQ-024, OQ-025, OQ-026, OQ-029]
  required_adrs: [ADR-0002, ADR-0038, ADR-0051, ADR-0052, ADR-0053, ADR-0086,
    ADR-0108, ADR-0111, ADR-0124, ADR-0130, ADR-0131]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0130-compiler-planned-projection-result-sets.md,
    adr/0131-exact-indexed-text-cardinality-and-ordinal-windowing.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-contract-syntax/**, crates/riffdb-contract-ir/**,
    crates/riffdb-contract-compiler/**, crates/riffdb-riffql-syntax/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-catalog/**,
    crates/riffdb-projection/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-diagnostics/**, fixtures/**, fuzz/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Frozen `binary_utf8_v1` truth tables for equals/starts-with/ends-with/contains, typed empty-needle
    refusal, missing/null behavior, value/needle maxima, canonical parameter binding, and explicit
    exclusion of locale, wildcard, regex, tokenization, stemming, scoring, and corpus statistics
  - Compiler declarations and finite plan-family enumeration tying each field/operator/order shape
    to one exact provider descriptor, complete partition or policy-aligned scope, static candidate,
    write/storage/work amplification, and source-spanned rejection of unsupported or excessive forms
  - Versioned provider-state format, least-sufficient compiler/module identities, topology records,
    compatibility fixtures, explicit rebuild/migration and mixed-version refusal, with existing
    prefix text-key and query artifacts byte-identical
  - Partition-scoped exact index maintenance and independent oracle equivalence across randomized
    values, writes, deletes, compaction, rebuild, recovery, multibyte boundaries, and maximum bounds;
    denied rows never enter shared result-shaping statistics
  - Architecture negatives for entity scans, page folds, cursor walks, N+1 fetches, materialize-all,
    caller-supplied fields/operators/providers, Better-Auth route branching, full-text runtime, and
    cross-provider bridges
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog --all-features
  - cargo +1.97.0 test -p riffdb-projection -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - cargo +nightly-2026-07-12 fuzz run riffql_parser -- -max_total_time=60
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every declared exact match equals the independent oracle at the same partition and
    epoch under bounded indexed work, every format and compiler identity is migration-safe and
    topology-registered, unsupported shapes fail at source spans, and no public execution or
    framework-specific behavior is claimed.
  closure:
    status: complete
    evidence: docs/architecture/OVERVIEW.md
    decision: The frozen binary_utf8_v1 profile implements exact UTF-8 byte equality, prefix,
      suffix, and containment with typed empty-needle refusal and missing/null non-match semantics.
      The compiler enumerates only declared operator and total-order combinations, binds complete
      partition-aligned or bounded-row policy admission plus static amplification ceilings to one
      exact provider descriptor, and preserves source spans for closed RDB-QP009 failures. Exact
      provider checkpoints are partition-scoped, bounded, canonical, versioned, topology-registered,
      and rebuild postings from sorted source rows at an exact generation/frontier; unknown and
      mixed versions refuse before use. Randomized independent-oracle evidence covers replacement,
      deletion, Unicode, compaction, rebuild, and recovery. Existing query-module V1 through V4 and
      prefix text-key artifacts remain unchanged. No public execution surface, scan fallback,
      full-text dependency, cross-provider bridge, or framework-specific branch was added.
  standing_design_tests:
    interface_safety: Only a finite compiled named-query family can select an exact profile; callers
      cannot express regex, scans, arbitrary fields/operators, unbounded values, a policy bypass,
      or an index/fallback choice.
    scale: Field, needle, plan-family, partition, candidate, write amplification, provider state,
      rebuild, work, and diagnostics are bounded before deployment and execution; no query requires
      a full-state rewrite or database-wide materialization.
  human_review_triggers: *id001
- id: WP-647
  name: Exact whole-result count, ordinal windows, and generated query surfaces
  depends_on: [WP-565, WP-635, WP-646]
  objective: Activate exact profile queries through the shared snapshot, policy, service, wire,
    CLI, MCP, and four generated SDK paths with one-snapshot exact full-population count and indexed
    ordinal offset, preserving cursor defaults and forbidding page walks or client result shaping.
  requirements: [OQ-019, OQ-020, OQ-021, OQ-022, OQ-025, OQ-026, OQ-027,
    OQ-028, OQ-029, OQ-030]
  required_adrs: [ADR-0038, ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0074,
    ADR-0106, ADR-0108, ADR-0111, ADR-0117, ADR-0120, ADR-0124, ADR-0128,
    ADR-0130, ADR-0131]
  allowed_paths: [SPEC.md, work_packages.yaml, proto/**, fixtures/**,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-riffql-syntax/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-catalog/**,
    crates/riffdb-projection/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-policy/**, crates/riffdb-errors/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**,
    crates/riffdb-cli/**, crates/riffdb-mcp-stdio/**, crates/riffdb-server/**,
    clients/**, templates/**, examples/**, tests/**, scripts/**, docs/**,
    release/distribution/**, release/version-topology-v1.json]
  scope_expansion: Maintainer authority to add package paths as needed applies to the activation
    work. WP-646 froze a programmatic compile-only declaration and provider-state V1, but public
    activation requires the finite RiffQL spelling and durable derived-state ports that were
    accidentally omitted from this package. The added syntax and storage paths are limited to
    compiler-owned exact operators/window parameters and V2 provider-state persistence; they add
    no caller AST, scan fallback, authoritative mutation path, or Better-Auth-specific behavior.
  deliverables:
  - Exact whole-result count over the complete admitted population at one selected epoch, never an
    earlier take/page/candidate fold, with checked cardinality and typed overflow/lifecycle outcomes
  - Indexed ordinal selection for bounded numeric offset under every declared total order and unique
    tie-breaker, including zero, exact-end, beyond-end, maximum, overflow, and changed-current-
    snapshot behavior, with no walking or discarding earlier rows or cursor pages
  - One combined page/count execution and result contract binding predicates, policy shape, text
    profile, provider generation, history incarnation, module/plan, and epoch proof; continuations
    validate the bound proof while fresh authorization and revocation checks still run per request
  - Shared service, gRPC, CLI, MCP, Rust, Go, TypeScript, and Python generated methods/results/errors
    over the finite compiler family with no caller AST, field/order/provider name, client filtering,
    raw catalog, hidden secret authority, or consistency downgrade
  - Deterministic policy, concurrent-write, retention, rebuild, crash, cancellation, saturation,
    cost/fuel, cursor-reset, and cross-language corpus proving exact count/ordinal/reference equality
    and pay-once descriptor/epoch proof construction
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-executor -p riffdb-policy -p riffdb-service -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-client-rust -p riffdb-driver-host -p riffdb-cli -p riffdb-server --all-features
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every surface returns the same page and exact total from one authorized epoch, ordinal
    offsets seek rather than walk, current authorization remains fresh, lifecycle and retention
    failures are typed, and no adapter/runtime supplies structure or performs result shaping.
  closure:
    status: complete
    evidence: docs/architecture/OVERVIEW.md
    decision: Exact named RiffQL operations execute through one policy-bound derived provider epoch
      and return the ordinal page plus whole-population count without an authoritative request-time
      scan, page walk, client fold, or fallback. Bounded background activation applies the current
      candidate-bound row-policy proof before constructing counts, ranks, or ordinal state; each
      protected slot and checkpoint is isolated by capability identity and revision, while denied
      rows cannot influence released totals or order. Provider activation checkpoints also bind
      plan, partition, role, history incarnation, generation, frontier, payload, and checksum;
      corrupt, over-capacity, or stale state is never served and the background worker is the only
      scan owner. Fresh authorization runs before provider selection and again before release, with
      typed unavailable, rebuilding, freshness, retirement, divergence, and revocation outcomes.
      The shared remote TLS corpus proves identical generated Rust, Go, TypeScript, and Python
      behavior, and generated MCP, CLI, and service paths retain the same finite operation identity
      and schemas.
  standing_design_tests:
    interface_safety: Generated methods expose only typed finite parameters and results; no caller
      can request unindexed work, unsafe offset, arbitrary structure, weaker freshness, secret
      widening, client filtering, or a provider/fallback choice.
    scale: Cardinality and ordinal operations use declared indexed capabilities without row-sized
      intermediates; pages, offsets, epochs, waits, fuel, outputs, diagnostics, and driver buffers
      are bounded before work or allocation.
  human_review_triggers: *id001
- id: WP-648
  name: Better Auth indexed admin profile and exact acceptance
  depends_on: [WP-630, WP-647, WP-652]
  objective: Prove a fresh external Better Auth adapter can serve its real admin user-listing route
    through generated named RiffQL for contains, starts-with, ends-with, typed filters/sorts, exact
    total, and numeric offset with no adapter-side filtering, counting, sorting, page walking, raw
    query structure, or RiffDB-owned identity/session behavior.
  requirements: [OQ-027, OQ-028, OQ-029, OQ-030]
  required_adrs: [ADR-0055, ADR-0056, ADR-0074, ADR-0106, ADR-0108, ADR-0111,
    ADR-0117, ADR-0118, ADR-0120, ADR-0124, ADR-0128, ADR-0130, ADR-0131]
  allowed_paths: [.gitignore, SPEC.md, work_packages.yaml, crates/riffdb-application/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**,
    crates/riffdb-query-module/**, crates/riffdb-storage-redb/src/shared_ports.rs,
    clients/**, templates/**,
    fixtures/application-parity/**, fixtures/adapters/**, fixtures/driver/**,
    examples/**, tests/**, scripts/**, docs/**, release/distribution/**]
  deliverables:
  - Better-Auth-shaped compiled profile and generated TypeScript surface for exact contains,
    starts-with, and ends-with search, declared filters and total orders, bounded limit/offset, exact
    total, and snapshot metadata, with the provider containing no route-specific runtime branch
  - Fresh immutable development-package installation into the external adapter and real admin-route
    observations for empty, one-page, multi-page, offset zero/end/beyond-end, concurrent writes,
    Unicode-byte boundaries, null/missing, maximum values, and all supported operator/order shapes
  - Adversarial organization/partition, row/field policy, secret-output, revocation, stale snapshot,
    rebuild/divergence, saturation, malformed-host, and mixed-version evidence with identical safe
    typed outcomes and no unauthorized influence on total, ordinal, cursor, or work class
  - Static and runtime boundary evidence rejecting adapter filtering/count/sort/page-walk loops,
    caller query ASTs or field/operator names, raw storage/catalog access, reveal-shaped commands,
    and RiffDB-owned end-user identity, session, sign-in, or OAuth machinery
  - Handbook and adapter guidance documenting exact current-snapshot offset semantics, cursor reset,
    declared bounds, index/storage cost, lifecycle outcomes, and current POC limitations without
    claiming facets, BM25, or cross-provider composition
  acceptance_commands:
  - ./scripts/adapter-operational-query-acceptance --all-languages
  - ./scripts/driver-development-registry-acceptance --all-ecosystems
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: A fresh real Better Auth consumer observes exact server-side admin pages and totals for
    every accepted filter/search/order/offset shape at one authorized snapshot, every negative is
    typed and isolation-safe, and the adapter contains no result shaping, raw query/storage escape,
    or RiffDB-owned userland auth behavior.
  closure:
    status: complete
    evidence: docs/getting-started/OPERATIONAL-QUERIES.md
    ownership_correction: WP-655
    decision: WP-648 proved the external adapter route against the generic exact-text, exact-count,
      ordinal, snapshot, and policy surface. WP-655 corrects artifact ownership by removing the
      copied admin profile, generated methods, and route host from RiffDB; the owning external
      adapter repository retains responsibility for the real route matrix. RiffDB keeps neutral
      cross-language exact-result evidence and the generic redb transaction-current row-admission
      fix, which remains fail closed before any protected posting, count, or ordinal state is built.
  standing_design_tests:
    interface_safety: The adapter receives only generated named methods and typed bounded values;
      it cannot select fields, operators, indexes, providers, policy, freshness, count semantics,
      page-walk behavior, or hidden secret authority.
    scale: Search values, limit, offset, result bytes, waits, retries, diagnostics, and installed
      artifacts are bounded; exact total and ordinal positioning use server-side indexed capability
      and never scale adapter work with skipped or matching population.
  human_review_triggers: *id001
- id: WP-649
  name: Bounded journal fence pipeline and ordered durable-prefix publication
  depends_on: [WP-641, WP-642, WP-643, WP-644]
  objective: Close the journal completion tail, reject unsafe or losing physical-fence shapes,
    and implement ADR-0132's separate ordered-completion lane only after exact acceptance and the
    closed ledger demonstrates that it can satisfy unary, mixed-c32, tail, and seed gates together.
  requirements: [PERF-004, PERF-005, PERF-008, PERF-009, PERF-015, PERF-017, PERF-018,
    REC-001, REC-002, STO-001, STO-002]
  required_adrs: [ADR-0058, ADR-0098, ADR-0101, ADR-0103, ADR-0104, ADR-0123,
    ADR-0127, ADR-0129, ADR-0132]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0132-bounded-journal-fence-pipeline.md, crates/riffdb-bench-root/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-observability/**, crates/riffdb-commit/**, crates/riffdb-server/**,
    examples/app-baseline/**, fixtures/**, tests/**, scripts/**, docs/performance/**]
  deliverables:
  - A fixed-cardinality closed ledger from command enqueue through group residence, journal dequeue,
    extent encoding/write, durability syscall, receipt readiness, predecessor publication,
    composite/frontier publication, and application acknowledgement, closing representative unary,
    mixed c32, and seed latency/count/byte totals within five percent
  - A same-filesystem workstation/N1/E2 mechanics probe comparing current serial write/fence,
    append work overlapped with one in-progress fence, bounded same-file concurrent durability at
    depths two and four, and a separate-file control across the production frame-size range
  - A reject-first mechanics table applying ADR-0132's twenty-percent cloud p95 improvement and
    five-percent throughput non-regression thresholds before any production journal worker changes
  - After exact ADR acceptance only, a sole authoritative apply writer and separate bounded FIFO
    completion owner that waits checked command and audit fences, publishes one contiguous durable
    prefix, emits notifications, and releases responses, with no public hole, authority cache,
    application-selectable setting, or acknowledgement before immediate read-after-commit visibility
  - Fixed-cardinality production evidence for queue residence, in-flight depth, completion reorder,
    durable-prefix advancement, publication drain, and shutdown, with no tenant, command, path,
    identity, value, or history-dependent label
  - Deterministic schedules, simulation campaigns, and process crash arms covering every write,
    fence, completion, prefix, publication, checkpoint, recycle, cancellation, shutdown, error, and
    uncertainty boundary while durable bytes and recovery fixtures remain exact
  - Paired current-HEAD N1/E2 safe-application PostgreSQL receipts for every representative unary
    scenario, mixed c32 throughput and p95, full seed, reads, physical groups/bytes/fences, and the
    unchanged WP-644 session candidate
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-bench-root -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-observability -p riffdb-commit -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-sim --all-features
  - ./scripts/recovery_full
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Production activation occurs only after exact ADR-0132 acceptance, the completion lane
    removes the ledgered receipt-ready wait without changing physical journal scheduling, every
    representative unary scenario is at most 1.10x safe-application PostgreSQL, mixed c32 throughput
    is at least 0.90x and p95 at most 1.25x, seed remains at most 5.0x, reads do not regress more than
    five percent, and every ordering, durability, read-after-commit, crash, recovery, uncertainty,
    and shutdown proof is exact; otherwise current journal dispatch and unary defaults remain active.
  standing_design_tests:
    interface_safety: Applications continue to submit only compiled commands and cannot select or
      observe grouping, residence budgets, durability depth, completion order, publication order,
      transaction scope, durability, or uncertainty; acknowledgement still proves atomic recovery
      and immediate visibility through the returned commit frontier.
    scale: Append, fence, completion, publication, dirty-byte, overlay, queue, telemetry, and
      shutdown state are bounded by fixed process and existing journal ceilings independent of
      history, tenant count, command count, database size, or workload duration.
  closure:
    status: complete_without_release_activation
    evidence: docs/performance/wp-649-fence-mechanics.md
    decision: The separate bounded FIFO completion owner preserves sole-writer mutation and
      sequencing authority, exact ordered durability/publication, read-after-commit visibility,
      uncertainty, crash recovery, and fixed resource bounds. It closes the cloud mixed-tail
      mechanism and lets the unchanged bounded session pass c32 throughput and p95 on both hosts,
      while seed remains below 5.0x. The conjunctive dedicated unary gate still fails on both CPU
      families, so unary remains the generated-client default, PERF-018 is not amended, and no alpha
      release activation is claimed. Remaining small-read/service costs and BoardPage450 result
      assembly are assigned to follow-on packages; physical journal dispatch remains unchanged.
  human_review_triggers: *id001
- id: WP-650
  name: Exact named-read session ledger and safe orchestration decision
  depends_on: [WP-637, WP-644, WP-649]
  objective: Close the current-HEAD low-concurrency named-read ledger across unary and bounded-session
    transports, reject authentication or dispatch changes whose measured ceiling cannot satisfy the
    public gates, and draft or implement a session-proof amendment only after a material end-to-end
    gain and exact human acceptance.
  requirements: [SEC-001, SEC-003, PERF-007, PERF-008, PERF-018]
  required_adrs: [ADR-0007, ADR-0009, ADR-0055, ADR-0070, ADR-0105, ADR-0111,
    ADR-0123, ADR-0127, ADR-0129]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0127-bounded-multiplexed-application-session.md,
    adr/0133-session-authentication-proof-and-read-orchestration.md,
    crates/riffdb-auth/**, crates/riffdb-policy/**, crates/riffdb-service/**,
    crates/riffdb-api-grpc/**, crates/riffdb-server/**, crates/riffdb-observability/**,
    crates/riffdb-client-rust/**, examples/app-baseline/**, fixtures/**, tests/**,
    scripts/**, docs/performance/**]
  deliverables:
  - A current-HEAD N1/E2 exact GetTicket ledger for frozen unary and accepted bounded-session shapes,
    plus a receipted workstation attempt, separating benchmark bridge, client/session protocol,
    transport adaptation, credential authentication, lifecycle admission, service supervision
    dispatch, plan and parameter preparation, all three current-policy safe points, authoritative
    execution, result conversion, and the remaining customer-paid residual within five percent
  - A predeclared Amdahl table for session-scoped immutable credential proof, inline or supervised
    service orchestration, and their combined bound; any candidate whose perfect-removal ceiling
    cannot materially improve the failed public ratios is rejected before production editing
  - A reject-first decision on whether an ADR-0127 amendment is justified for paying immutable bearer
    proof once per bounded session while current capability state, revision, expiry, operation
    authority, row/field policy, response release, cancellation, and audit containment remain fresh
    at every existing safe point; no amendment is drafted when the measured ceiling misses the gate
  - If and only if the accepted candidate clears its mechanics threshold, a service-owned bounded
    named-read path that preserves the API-neutral application service, panic/audit containment,
    cancellation, response budgets, retry, freshness, and typed failure semantics without granting
    the transport policy, storage, plan, or result authority
  - If the mechanics threshold passes, deterministic revocation, expiry, policy revision, row-policy,
    cancellation, panic, slow-consumer, session-close, saturation, and shutdown schedules plus
    architecture tests proving no cached allow decision, credential exposure, storage bypass,
    unaudited accepted work, or raw query capability
  - If the mechanics threshold passes, paired safe-application PostgreSQL receipts for every
    representative unary scenario, mixed c32 throughput and p95, seed, and BoardPage50/200
    no-regression; BoardPage450 remains owned by the ADR-0130/ADR-0131 result-set campaign and cannot
    be hidden by this package
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-auth -p riffdb-policy -p riffdb-service -p riffdb-api-grpc -p riffdb-server -p riffdb-observability -p riffdb-client-rust --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Production activation requires an exact accepted ADR amendment, a closed ledger, at
    least a twenty-percent improvement in the exact bounded-session GetTicket mean on both cloud
    profiles, every representative unary scenario at most 1.10x safe-application PostgreSQL, mixed
    c32 throughput at least 0.90x and p95 at most 1.25x, seed at most 5.0x, no read/write/tail
    regression beyond five percent, and exact authorization, audit, cancellation, recovery, and
    shutdown semantics; otherwise the candidate closes honestly without production activation.
  standing_design_tests:
    interface_safety: Applications still invoke only generated named reads and commands and cannot
      select authentication scope, authorization reuse, dispatch mode, audit posture, retry,
      freshness, or transport internals; every released row is governed by current capability and
      row/field policy at the existing safe points.
    scale: Session proof state, operation supervision, in-flight work, buffers, telemetry, and
      diagnostic samples are fixed and bounded independently of history, tenant count, database
      size, session duration, or completed operation count; no full-state rewrite or co-located
      storage assumption is introduced.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-650-named-read-session-ledger.md
    decision: Exact back-to-back N1/E2 GetTicket cells reject session-scoped authentication proof
      and read-orchestration changes before production editing. Authentication plus service dispatch
      is only 24--26 microseconds per operation, while the accepted bounded session is 2.7 percent
      slower on N1 and 15.5 percent slower on E2 than the unchanged unary path. The candidate misses
      its twenty-percent mechanics threshold on both cloud families, so ADR-0127 is not amended,
      current per-operation authentication and authorization remain exact, and unary remains the
      generated-client default.
  human_review_triggers: *id001
- id: WP-651
  name: Authorization-before-cascade-overflow release
  depends_on: [WP-570, WP-629]
  objective: Close the relationship-cardinality inference channel found by WP-630 so a cascade
    overflow outcome can be selected or released only after the explicit root and every bounded
    maximum-plus-one candidate row have passed current capability and row-policy authorization.
  requirements: [DEL-007, DEL-008, DEL-009, DEL-012]
  required_adrs: [ADR-0055, ADR-0059, ADR-0107, ADR-0111, ADR-0126]
  allowed_paths: [work_packages.yaml, crates/riffdb-runtime/**, crates/riffdb-policy/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-commit/**, crates/riffdb-testkit/**,
    fixtures/adapters/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Bounded maximum-plus-one predecessor materialization for every cascade relationship before an
    overflow decision, preserving exact conflict ownership, stable ordering, and snapshot identity
  - A transaction-current policy proof for each present root and discovered predecessor whose
    existence can influence the declared cascade outcome, including capability revision recheck
  - Deterministic owner/outsider, hidden-child, revocation, create-versus-delete, replay,
    memory/redb, and malformed-snapshot tests proving denial precedes overflow and mutates nothing
  - The WP-630 Better Auth public generated-client regression demonstrating identical authorized
    overflow while an unauthorized principal receives only the ordinary typed authorization denial
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-runtime -p riffdb-policy -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit -p riffdb-testkit --all-features
  - ./scripts/adapter-cascade-acceptance --all-languages
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: No principal can distinguish an absent, within-limit, or over-limit protected cascade
    graph without authority over every bounded candidate that influences the outcome; authorized
    callers retain the exact zero-mutation overflow and all existing crash, replay, and ordering
    guarantees.
  standing_design_tests:
    interface_safety: The application still chooses only a compiled command and typed inputs; the
      runtime derives and authorizes the exact maximum-plus-one candidate set and never releases
      cardinality, identities, or outcomes from unauthorized rows.
    scale: Candidate materialization remains bounded by the compiler-fixed maximum plus one for
      each of at most 32 declared relationships, the transaction-current snapshot count and byte
      ceilings, and the existing 16-MiB successful-command graph ceiling. The sentinel row used to
      prove overflow is authorization evidence only and never becomes a mutation.
  closure:
    status: complete
    evidence: docs/contracts/BOUNDED-COMMANDS.md
    decision: Cascade discovery now retains the exact compiler-bounded maximum-plus-one candidate
      set through transaction-current validation. Before releasing a declared overflow, commit
      validation rechecks the current capability revision and delete row policy for each present
      root and predecessor that influenced the outcome. Deterministic runtime coverage proves the
      sentinel snapshot is accepted only as a zero-mutation declared outcome; the four-language
      Better Auth public-TLS regression proves an authorized owner receives that outcome while an
      outsider receives only the ordinary authorization denial, and revocation remains effective.
  human_review_triggers: *id001
- id: WP-652
  name: Exact result policy and finite-filter completion
  depends_on: [WP-570, WP-647]
  objective: Repair the gap between ADR-0131 and WP-647 by making protected exact-result plans
    compiler-reachable, rejecting every predicate the exact provider cannot execute, and completing
    the finite typed filter/order family before Better Auth acceptance can begin.
  requirements: [OQ-026, OQ-027, OQ-028, OQ-029, OQ-030]
  required_adrs: [ADR-0055, ADR-0108, ADR-0111, ADR-0117, ADR-0124, ADR-0130,
    ADR-0131]
  allowed_paths: [work_packages.yaml, release/version-topology-v1.json,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-projection/**,
    crates/riffdb-policy/**, crates/riffdb-service/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-server/**,
    fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - A compiler completeness gate that source-spans and rejects every extra, optional, disjunctive,
    or otherwise unimplemented exact predicate instead of retaining it only as non-executable
    metadata, plus architecture coverage proving no accepted predicate can be omitted at execution
  - Protected exact-query compilation selecting bounded row admission and end-to-end provider
    evidence that current candidate policy is applied before postings, exact count, total order,
    ordinal selection, cursor identity, and public release
  - One finite compiler-enumerated typed filter/order family whose request contains only typed
    values and closed generated choices, whose derived index performs exact intersection/count and
    direct ordinal selection, and whose plan/provider identities bind every admitted combination
  - Version-topology and compatibility receipts for any query, IR, descriptor, or rebuildable-state
    identity required by the finite family; existing unfiltered exact modules and provider V1/V2
    checkpoints remain readable and byte-exact
  - Deterministic reference, policy-inference, concurrent-write, rebuild, recovery, maximum-bound,
    and unsupported-shape tests proving filtered page and total share one authorized epoch without
    scan, page walk, materialize-all, remote hydration, or client result shaping
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-compiler -p riffdb-query-module -p riffdb-query-executor -p riffdb-projection -p riffdb-policy -p riffdb-service -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-server --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every exact-result predicate and total-order term accepted by the compiler is executed
    by the pinned provider before exact count and ordinal selection; protected plans admit only
    currently authorized rows; every unsupported combination fails at source; and WP-648 can use
    generated typed filters and sorts without adapter shaping or a hidden policy/count oracle.
  closure:
    status: complete
    evidence: docs/architecture/OVERVIEW.md
    decision: The exact-result compiler now rejects every predicate outside its complete executable
      family and admits one compiler-enumerated optional typed equality filter. Filtered plans use
      additive query-IR and query-module V6 identities plus provider-state V3, whose disjoint
      canonical-value postings perform intersection, exact count, and direct ordinal selection
      before public release. Unfiltered modules and V1/V2 provider checkpoints remain byte-exact
      and readable. Protected activation admits only currently authorized rows before constructing
      either whole-set or filtered postings, while fresh authorization still runs at every existing
      safe point. The shared remote TLS conformance corpus proves the same generated Rust, Go,
      TypeScript, and Python behavior without adapter filtering, page walking, or caller-supplied
      query structure.
  standing_design_tests:
    interface_safety: Applications receive only finite generated methods and typed bounded values;
      no request can supply a predicate AST, field/index/provider name, policy choice, fallback,
      post-filter, count strategy, order expression, or freshness downgrade, and compilation fails
      closed when the exact provider cannot execute the complete named predicate.
    scale: Candidate, filter-family, order-family, posting, count, offset, output, rebuild,
      checkpoint, wait, and diagnostic work is compiler bounded per partition and policy shape;
      direct ordinal selection never walks skipped rows or pages and no request materializes the
      matching population.
  human_review_triggers: *id001
- id: WP-653
  name: Large bounded named-result hydration and assembly
  depends_on: [WP-637, WP-645, WP-649, WP-650]
  objective: Close the BoardPage50/200/450 growth ledger across authoritative index traversal,
    entity hydration, durable-record validation, query-row materialization, result assembly, gRPC
    conversion, transport, and generated-client decoding, then activate only a byte-exact internal
    optimization whose measured ceiling materially reduces the 450-row public result without
    weakening authorization, freshness, durability validation, boundedness, or application safety.
  requirements: [PERF-001, PERF-002, PERF-008, PERF-018]
  required_adrs: [ADR-0055, ADR-0056, ADR-0074, ADR-0111, ADR-0123, ADR-0129,
    ADR-0130]
  allowed_paths: [work_packages.yaml, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-storage-memory/**,
    crates/riffdb-query-executor/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-observability/**,
    examples/app-baseline/**, fixtures/**, tests/**, scripts/**,
    docs/performance/**]
  deliverables:
  - A size-isolated BoardPage50/200/450 ledger on the workstation and one inventoried cloud host,
    separating index access, entity lookup, envelope identity/bounds, checksum, wire preflight,
    Protobuf decode, canonical verification, semantic reconstruction, row policy, row
    materialization, executor drive, service result assembly, gRPC conversion/encoding, transport,
    generated decoding, and benchmark-only bridge work; every measured request closes within five
    percent of client-observed mean
  - A per-row and fixed-cost model reconciled against the public 50/200/450 curve and the same-run
    safe-application PostgreSQL pages, with predeclared Amdahl bounds for every candidate and an
    explicit stop when no candidate can improve BoardPage450 by at least twenty percent on both
    tested CPU profiles
  - If the mechanics gate passes, one bounded internal optimization that preserves byte-identical
    named-query results, one authoritative snapshot, all three current authorization safe points,
    current row/field policy, exact fuel and output charges, continuation identity, and durable
    corruption detection; no application-selectable cache, validation downgrade, scan fallback,
    raw storage surface, provider-format change, or public query/module identity change is allowed
  - Production fixed-cardinality evidence and architecture tests proving any immutable validation
    or materialization proof is paid once only at its accepted datum or process-generation scope,
    never per returned row or operation, while corrupt, mixed-generation, stale-policy, and
    malformed-record cases still fail closed before release
  - Paired post-candidate BoardPage50/200/450 and ordinary point/list/detail receipts plus mixed c32,
    unary writes, and seed no-regression evidence; any durable format, compiler/provider identity,
    covering-index syntax, policy safe-point, or public protocol change stops for a separately
    accepted ADR rather than entering this package
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-query-executor -p riffdb-service -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-observability --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: BoardPage450 improves at least twenty percent on the workstation and one inventoried
    cloud host and reaches at most 1.10x same-run safe-application PostgreSQL, BoardPage50/200 and
    ordinary reads/writes/mixed/seed regress no more than five percent, every result and diagnostic
    remains bounded and redaction-safe, and authorization, snapshot, fuel, corruption, continuation,
    crash, and recovery semantics remain exact; otherwise the candidate closes honestly without
    production activation and the existing path remains selected.
  standing_design_tests:
    interface_safety: Applications continue to invoke only generated named queries with compiler-
      fixed fields, predicates, order, bounds, policy, and freshness; they cannot select hydration,
      validation, caching, provider, scan, fallback, transport, or response-assembly behavior, and
      every released row still passes current authorization and row/field policy at the existing
      safe points.
    scale: Index candidates, entity reads, decoded values, materialized rows, response bytes,
      continuations, proof state, telemetry, and any process-generation validation state remain
      compiler bounded per request and fixed-capacity per process independently of history, tenant
      count, database size, or completed-operation count; no full-population materialization or
      unbounded cache is introduced.
  closure:
    status: complete
    evidence: docs/performance/wp-653-large-result-ledger.md
    decision: The closed workstation ledger attributed 65 percent of BoardPage450 marginal time to
      storage execution and 35 percent to response, wire, and client assembly. The internal exact-
      projection candidate improved BoardPage450 only 4.9 percent and regressed BoardPage50 5.1
      percent, so it was removed before cloud activation. Existing production semantics remain
      selected; the separately proposed ADR-0133 owns the evidence-sized covering-index plus
      compact-carriage direction.
  human_review_triggers: *id001
- id: WP-654
  name: Compiler-planned covering result batches and compact carriage
  depends_on: [WP-645, WP-650, WP-653]
  objective: Implement the exact accepted ADR-0133 compiler, durable-index, internal positional-
    batch, negotiated public-carriage, and four-language generated-decoder path only after its
    mechanics ledger proves the combined candidate can close BoardPage450 without weakening any
    authorization, snapshot, durability, corruption, boundedness, or compatibility guarantee.
  requirements: [PERF-001, PERF-002, PERF-008, PERF-018, STO-002, API-001, QRY-001,
    QRY-002, QRY-003, QRY-004, QRY-006, QRY-007, QRY-008, QRY-009]
  required_adrs: [ADR-0022, ADR-0023, ADR-0027, ADR-0051, ADR-0052, ADR-0053,
    ADR-0055, ADR-0074, ADR-0111, ADR-0124, ADR-0128, ADR-0130, ADR-0131,
    ADR-0133]
  allowed_paths: [AGENTS.md, SPEC.md, work_packages.yaml, adr/**, proto/**,
    crates/riffdb-proto/**,
    crates/riffdb-types/**, crates/riffdb-contract-syntax/**,
    crates/riffdb-contract-ir/**, crates/riffdb-contract-compiler/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-module/**, crates/riffdb-catalog/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-query-executor/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-codegen-rust/**,
    crates/riffdb-codegen-typescript/**, crates/riffdb-codegen-go/**,
    crates/riffdb-codegen-python/**, crates/riffdb-driver-host/**,
    crates/riffdb-observability/**, crates/riffdb-cli/**, crates/riffdb-mcp-stdio/**,
    clients/editor/**, clients/go/**, clients/python/**, clients/typescript/**,
    examples/app-baseline/**,
    examples/agent-alpha/**, fixtures/**, tests/**, scripts/**,
    release/**,
    docs/SUMMARY.md, docs/contracts/**, docs/getting-started/**, docs/reference/**,
    docs/performance/**, docs/architecture/**]
  deliverables:
  - The accepted ADR-0133 SPEC amendments, version-topology entries, compiler grammar/IR/hash/lock
    fixtures, source diagnostics, and additive evolution rules for a new explicitly covering index;
    existing indexes, modules, and empty-cover bytes remain readable and byte-exact
  - Atomic coordinator derivation, update/delete behavior, epoch/changelog/recovery integration,
    rebuild/migration receipts, and process-generation structural proof for bounded covered values;
    missing, stale, malformed, duplicate, or non-derivable coverage refuses without entity fallback
  - A compiler-sealed one-step covered-result batch whose positional layout, predicates, order,
    policy, fuel, continuation, snapshot, and result identity are independently checked and cannot
    be selected or altered by an application
  - Additive negotiated compact Protobuf/session carriage plus strict codecs and parent goldens;
    old clients default to legacy named records, new clients accept both, and mixed or malformed
    arms fail closed
  - Rust, Go, TypeScript, and Python generated decoders that construct typed query results directly
    from the exact positional layout without handwritten glue, generic per-row maps, exposed
    ordinals, or caller-supplied schemas
  - A closed pre/post ledger and paired workstation, N1, and E2 receipts covering BoardPage50/200/
    450, ordinary reads, every unary write, mixed c32, seed, startup, deployment, recovery, and
    backup/restore, with every newly introduced write/read/startup cost reconciled
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-contract-syntax -p riffdb-contract-ir -p riffdb-contract-compiler -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-commit -p riffdb-query-executor -p riffdb-service -p riffdb-api-grpc -p riffdb-client-rust --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: BoardPage450 improves at least forty percent from WP-653 and reaches at most 1.10x
    paired safe-application PostgreSQL on N1 and E2, compact and legacy results are semantically
    identical in Rust, Go, TypeScript, and Python, every ordinary performance and reliability gate
    remains green, cover write/startup amplification is bounded and receipted, and no public caller
    can choose physical layout, fallback, validation, policy, or consistency behavior; otherwise
    partial candidates follow ADR-0133's independent activation rules and existing defaults remain.
  standing_design_tests:
    interface_safety: Applications continue to invoke only finite generated named queries with
      typed bounded parameters; compiler-owned cover/layout/encoding decisions expose no index,
      field ordinal, response schema, provider, fallback, validation, authorization, freshness, or
      durability choice, and both wire arms fail closed unless bound to the same exact plan.
    scale: Cover bytes, index/write amplification, migration/rebuild pages, startup proof, scan
      work, rows, columns, values, response bytes, continuation, SDK decode, and diagnostics are
      statically bounded; execution materializes only the requested page and creates no full-state
      cache or full-population result.
  closure:
    status: complete
    evidence: docs/performance/wp-654-covering-compact-closure.md
    decision: The retained V14 candidate improves BoardPage450 56.2 percent on the workstation,
      41.2 percent on N1, and 41.9 percent on E2, but remains 1.87x and 1.78x paired safe-
      application PostgreSQL on the cloud profiles; the combined 1.10x gate therefore fails
      honestly. ADR-0133 independent activation retains compact carriage after isolated legacy-arm
      controls prove 28.1, 29.7, and 31.5 percent customer-path wins, and retains compiler-owned
      covering production after it improves the declared read on every profile while seed,
      bounded write amplification, startup/recovery validation, compatibility, and four-language
      semantic gates remain intact. A later move-only decode experiment was removed after an E2
      control regressed beyond five percent. WP-623 owns release qualification of the unchanged
      retained artifact; WP-654 claims no alpha performance pass.
  human_review_triggers: *id001
- id: WP-655
  name: External adapter ownership and neutral exact-result conformance
  depends_on: [WP-647, WP-648, WP-652]
  objective: Correct the repository boundary after WP-648 by retaining only generic exact-text,
    exact-count, ordinal-pagination, policy, and transport behavior in RiffDB, moving real Better
    Auth admin profile and route acceptance ownership to its external adapter repository, and
    preserving the same semantic proof through a framework-neutral generated-client corpus.
  requirements: [OQ-027, OQ-028, OQ-029, OQ-030]
  required_adrs: [ADR-0055, ADR-0074, ADR-0111, ADR-0117, ADR-0120, ADR-0124,
    ADR-0128, ADR-0130, ADR-0131]
  allowed_paths: [SPEC.md, work_packages.yaml,
    fixtures/adapters/better-auth/**, fixtures/adapters/operational-conformance/**,
    fixtures/adapters/conformance/**, fixtures/export/**,
    tests/adapter_operational_remote_client.rs,
    scripts/adapter-cascade-acceptance, scripts/adapter-operational-query-acceptance,
    scripts/generate-query-clients, docs/SUMMARY.md, docs/getting-started/**,
    docs/integrations/**]
  deliverables:
  - Remove the WP-648 Better-Auth-specific admin query sources, role, principal fact, exact-text
    index, generated methods, route host, and route-level acceptance from the retained RiffDB
    fixture without removing its previously accepted generic cascade and lifecycle evidence
  - A framework-neutral Document exact-result family covering binary UTF-8 contains, starts-with,
    and ends-with predicates, one typed optional equality filter, deterministic ascending and
    descending orders, bounded numeric offset, and exact total from one provider epoch
  - Generated Rust, Go, TypeScript, Python, CLI, and MCP evidence that the neutral family exposes
    only compiler-declared parameters and results and performs no client-side filter, count, sort,
    page walk, provider selection, raw query construction, or storage access
  - Handbook and historical WP-648 closure corrections that distinguish generic RiffDB capability
    ownership from externally pinned framework-profile evidence and do not claim an adapter route
    or generated framework SDK is shipped by this repository
  acceptance_commands:
  - ./scripts/adapter-operational-query-acceptance --all-languages
  - ./scripts/adapter-cascade-acceptance --all-languages
  - ./scripts/check-application-bindings
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The repository contains no WP-648 Better Auth admin profile, generated admin method,
    or admin route host; the neutral exact-result corpus proves all three exact binary-text
    operators, typed optional filtering, both deterministic order directions, direct bounded
    offset, and complete exact total through every generated language plus CLI and MCP; the
    external adapter remains the sole owner of framework-specific route acceptance.
  closure:
    status: complete
    evidence: docs/getting-started/OPERATIONAL-QUERIES.md
    decision: The retained Better Auth lifecycle fixture is restored to its pre-WP-648 contract,
      role, two named queries, generated clients, cascade runner, conformance manifests, and
      portability receipts. The generic operational fixture now owns three Document exact-result
      methods and proves contains, starts-with, ends-with, optional typed equality, ascending and
      descending total order, one-row offset windows, and complete two-row totals through Rust,
      Go, TypeScript, Python, CLI, and MCP after an explicit post-commit provider-readiness gate.
      Generated closure, application bindings, requirement coverage, handbook, formatting,
      targeted Clippy, the neutral live corpus, and the independent cascade lifecycle gate pass.
      Route-level Better Auth acceptance remains required by OQ-030 but belongs only to the
      external adapter repository and is not shipped or generated here.
  standing_design_tests:
    interface_safety: Applications receive only generated finite named methods with typed search,
      filter, limit, and offset values; neither the neutral fixture nor an external adapter can
      select fields, operators, indexes, providers, policy, freshness, count semantics, raw query
      structure, or a client-side fallback.
    scale: Search values, filters, pages, offsets, exact totals, waits, retries, outputs, and
      diagnostics remain compiler bounded; exact count and ordinal selection never materialize the
      matching population or walk skipped pages, and no framework-specific runtime branch enters
      RiffDB.
  human_review_triggers: *id001
- id: WP-656
  name: Exact-result predicate and independent-order semantic closure
  depends_on: [WP-645, WP-652, WP-655]
  objective: After exact acceptance of ADR-0134, freeze one generic compiler-declared exact-result
    predicate and independent total-order model covering the bounded operational operator algebra,
    without provider activation, runtime query structure, framework-specific artifacts, or a scan
    fallback.
  requirements: [OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-009,
    OQ-010, OQ-015, OQ-017, OQ-019, OQ-021, OQ-022, OQ-024, OQ-026, OQ-027,
    OQ-028, OQ-029]
  required_adrs: [ADR-0002, ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124, ADR-0130, ADR-0131, ADR-0134]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0134-compiler-declared-exact-predicate-and-order-families.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-catalog/**, crates/riffdb-cli/assets/editor/**,
    clients/editor/**, fixtures/**, fuzz/**, tests/**, scripts/**,
    docs/SUMMARY.md, docs/riffql/**, docs/architecture/**,
    docs/getting-started/OPERATIONAL-QUERIES.md]
  deliverables:
  - Accepted ADR-0134 SPEC clarification and public RiffQL documentation for independent search,
    filter, and order dimensions; equality/inequality, scalar ranges, bounded in/not_in,
    null/existence, exact text, conjunction, and compiler-capped disjunction retain one closed
    typed truth model and add no public query AST
  - Canonical bounded predicate/order semantic IR with stable field, operator, value-slot,
    comparison/text-profile, Boolean-edge, direction, missing/null-placement, complete ascending
    entity-key tie-breaker, measure, and ordinal identities
  - Finite family compilation across every optional-presence, disjunction, and declared
    operator/order member, with source-spanned rejection when any member lacks a complete provider,
    policy, type, index, exact-count, ordinal, state-amplification, or work proof
  - Least-sufficient source-language, query-IR, plan/module/hash/lock, and generated-schema identity
    successors plus ADR-0124 topology entries and byte-exact retained fixtures for every existing
    narrow exact-result artifact
  - An independent bounded reference evaluator and randomized semantic corpus freezing null/missing,
    empty and duplicate set, canonical set order, range edge, text-profile, Boolean, mixed-direction,
    and unique-tie-breaker behavior before production provider code
  - Architecture negatives proving callers cannot submit fields, raw operators, predicates, order
    tuples, providers, scans, costs, policy choices, or fallbacks and that this package adds no
    Better Auth artifact, provider runtime, durable provider state, or cross-provider bridge
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog --all-features
  - cargo +nightly-2026-07-12 fuzz run riffql_parser -- -max_total_time=60
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every source-admitted predicate and order family member has one canonical independently
    evaluated semantic identity and a complete statically bounded provider requirement; all old
    artifacts remain byte-exact and readable; and no runtime provider, framework branch, public
    query structure, scan, or fallback is introduced.
  closure:
    status: complete
    evidence: docs/architecture/WP-656-EXACT-PREDICATE-SEMANTICS.md
    decision: RiffQL V6, query IR V9, and query-module V9 freeze one canonical bounded predicate,
      optional-presence, exact-count, ordinal-window, and independent total-order model while the
      compiler continues to emit older identities for existing narrow queries. Independent and
      randomized reference evaluation, strict codecs, source-spanned whole-family rejection,
      four-language generated-surface negatives, version topology, generated artifacts,
      requirement coverage, handbook, formatting, Clippy, and the required 60-second parser fuzz
      run pass. No provider runtime, durable provider state, framework artifact, public query AST,
      scan, client shaping, or fallback is activated; WP-657 and WP-658 retain that ownership.
  standing_design_tests:
    interface_safety: Application callers receive only finite generated operations and typed
      bounded values; compilation owns every field, operator, Boolean edge, order, tie-breaker,
      policy mode, provider requirement, cost, and freshness shape, and one unsupported member
      rejects the complete family.
    scale: Predicate leaves, Boolean depth and branches, presence combinations, set elements and
      bytes, order terms, family members, provider requirements, reference work, diagnostics, and
      artifacts are compiler bounded; the semantic model assumes neither a full result nor
      co-located authoritative storage.
  human_review_triggers: *id001
- id: WP-657
  name: Indexed exact-result set algebra and ordinal provider
  depends_on: [WP-647, WP-656]
  objective: Implement ADR-0134's provider-owned indexed predicate combination, independent total
    orders, exact cardinality, and direct ordinal selection under one authorized epoch, using an
    additive rebuildable provider-state identity and no authoritative request-time scan,
    materialize-all, page walk, or client shaping.
  requirements: [OQ-003, OQ-004, OQ-005, OQ-006, OQ-009, OQ-010, OQ-017,
    OQ-019, OQ-020, OQ-021, OQ-022, OQ-024, OQ-026, OQ-027, OQ-028, OQ-029]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0086, ADR-0108,
    ADR-0111, ADR-0124, ADR-0129, ADR-0130, ADR-0131, ADR-0134]
  allowed_paths: [SPEC.md, work_packages.yaml, release/version-topology-v1.json,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-catalog/**, crates/riffdb-projection/**,
    crates/riffdb-query-executor/**, crates/riffdb-policy/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-service/**,
    crates/riffdb-server/**, fixtures/**, fuzz/**, tests/**, scripts/**,
    docs/SUMMARY.md, docs/riffql/**, docs/architecture/**,
    docs/getting-started/OPERATIONAL-QUERIES.md]
  deliverables:
  - A new exact-provider state identity binding the complete normalized predicate program,
    comparison/text profiles, independent total-order layouts, authorized universe, exact-count and
    ordinal capabilities, static work/state amplification, partition, policy shape, generation,
    frontier, incarnation, plan, and checksum without reinterpreting V1 through V3 checkpoints
  - Provider-owned bounded indexed-set execution for equality/inequality, ranges, bounded in/not_in,
    null/existence, exact text, conjunction, and capped disjunction, including authorized complement
    and independent mixed-direction order tuples with the complete ascending entity-key tie-breaker
  - Exact count and direct rank/select-style ordinal seek over the fully combined predicate at one
    provider epoch, returning only the bounded page and never scanning authoritative entities,
    walking skipped rows/pages, sorting request results, materializing all matches, or fetching
    non-output rows outside separately declared bounded policy evidence
  - Partition-scoped and policy-aligned provider activation plus bounded-row-admission negatives
    proving unauthorized rows cannot affect candidate sets, complement universes, counts, order,
    ordinals, continuations, work class, diagnostics, or lifecycle selection
  - Canonical rebuild, catch-up, persistence, restart, corruption, compaction, cancellation,
    retention, mixed-version refusal, and migration receipts with descriptor/layout validation once
    per provider-plan generation and epoch proof once per opened result set
  - Independent randomized equivalence and architecture evidence for search-by-one-field/order-by-
    another, every activated operator/type/state profile, optional and disjunctive families,
    zero/end/beyond-end offsets, concurrent writes, maximum bounds, and forbidden scan/fallback/
    cross-provider transfer paths
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog -p riffdb-projection -p riffdb-query-executor -p riffdb-policy -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-service -p riffdb-server --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every compiler-admitted predicate/order member equals the independent evaluator at one
    authorized epoch, exact count and ordinal selection use only declared indexed work, existing
    checkpoints remain readable and byte-exact, lifecycle and corruption fail closed, and no
    request-time scan, population materialization, page walk, client shaping, or provider bridge
    exists.
  closure:
    status: complete
    evidence: docs/architecture/WP-657-INDEXED-EXACT-PROVIDER.md
    decision: Additive provider-state V4 implements bounded indexed set algebra, independent total
      orders, exact count, and direct ordinal selection over one policy-aligned partition and epoch.
      Background activation applies bounded row admission before index construction; the request
      port performs no authoritative scan, hydration, sort, page walk, matching-population
      materialization, fallback, or provider bridge. Randomized independent equivalence, every
      activated operator and scalar profile, lifecycle mutations, compaction, byte-exact restart,
      corruption and mixed-version refusal, epoch proofs, architecture boundaries, the complete
      package test set, generated/topology/coverage/policy checks, handbook, formatting, and
      workspace Clippy pass. WP-658 retains all public transport, SDK, and external publication
      activation.
  standing_design_tests:
    interface_safety: The runtime accepts only compiler-sealed plan/member identities and typed
      values; it cannot accept caller structure, switch providers, omit a predicate or order term,
      widen the complement universe, weaken policy/freshness, or substitute scans and fallbacks.
    scale: Provider state, write amplification, indexed-set operands, probes, words/segments,
      rank/select work, policy candidates, offsets, pages, outputs, rebuild work, waits, and
      diagnostics are statically bounded per declared partition/profile and no matching-population
      allocation is permitted.
  human_review_triggers: *id001
- id: WP-658
  name: Generic robust exact-query surfaces and external handoff
  depends_on: [WP-635, WP-655, WP-657]
  objective: Activate ADR-0134's robust exact-result families through the shared service, transports,
    generated Rust, Go, TypeScript, and Python SDKs, CLI, and MCP using only framework-neutral
    fixtures, then publish an immutable generic capability receipt for external adapter acceptance
    without importing any framework schema, profile, route, or runtime into RiffDB.
  requirements: [OQ-001, OQ-003, OQ-004, OQ-005, OQ-006, OQ-015, OQ-016,
    OQ-019, OQ-020, OQ-021, OQ-022, OQ-026, OQ-027, OQ-028, OQ-029, OQ-030,
    API-001, MCP-001, MCP-020, MCP-021, MCP-023, MCP-025, MCP-026, MCP-040,
    MCP-043, MCP-045, MCP-049]
  required_adrs: [ADR-0040, ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0074,
    ADR-0106, ADR-0108, ADR-0111, ADR-0117, ADR-0120, ADR-0124, ADR-0128,
    ADR-0130, ADR-0131, ADR-0133, ADR-0134]
  allowed_paths: [SPEC.md, work_packages.yaml, proto/**,
    fixtures/adapters/operational-conformance/**, fixtures/adapters/conformance/**,
    fixtures/application-parity/**, fixtures/query-modules/**, fixtures/export/**,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-catalog/**,
    crates/riffdb-projection/**, crates/riffdb-policy/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-client-rust/**,
    crates/riffdb-codegen-rust/**, crates/riffdb-codegen-typescript/**,
    crates/riffdb-codegen-go/**, crates/riffdb-codegen-python/**,
    crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-mcp-stdio/**, crates/riffdb-server/**, clients/**,
    templates/**, examples/**, tests/**, scripts/**, release/**,
    docs/SUMMARY.md, docs/riffql/**, docs/getting-started/**,
    docs/integrations/**, docs/reference/**]
  deliverables:
  - Shared application-service, gRPC, CLI, MCP, Rust, Go, TypeScript, and Python activation for
    finite generated exact-result operations whose search, filters, independent order, exact total,
    ordinal page, policy, provider epoch, plan identity, bounds, and typed failures remain identical
    on every surface
  - A framework-neutral entity/query corpus covering search-by-one-field/order-by-another;
    equality/inequality, range, bounded in/not_in, null/existence, prefix/suffix/substring,
    multi-filter conjunction, capped disjunction, ascending/descending mixed order, and deterministic
    key tie-breaking, with no caller-supplied field/operator/order or raw query structure
  - Adversarial generated-client evidence for optional combinations, empty/duplicate/maximum sets,
    null/missing, Unicode, offset zero/end/beyond-end/maximum, exact total, concurrent writes,
    authorization, tenant isolation, revocation, readiness, divergence, retired snapshots,
    saturation, malformed responses, and legacy/new mixed-version behavior
  - Static and runtime boundary checks forbidding generated or handwritten client filtering,
    sorting, counting, cursor/page walks, provider selection, storage access, query ASTs, framework
    names, and per-row descriptor/epoch/layout proof construction
  - Handbook guidance teaching application authors to create bounded named `.riffql` documents and
    declared provider indexes, regenerate typed SDK methods, understand finite family/storage costs,
    and use cursor pagination when cross-request snapshot stability is required
  - One immutable development publication and generic capability manifest suitable for external
    adapter pinning, explicitly recording that real framework route evidence remains owned and
    release-gating in the external repository and is not proved by RiffDB's neutral corpus
  acceptance_commands:
  - ./scripts/adapter-operational-query-acceptance --all-languages
  - ./scripts/driver-development-registry-acceptance --all-ecosystems
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public RiffDB surface returns the same exact authorized page and total for the
    complete generic operator/order matrix without client shaping or caller structure, the immutable
    publication is reproducible, and the repository contains no framework-specific profile, route,
    SDK, runtime, or false claim that outstanding external route acceptance has completed.
  closure:
    status: complete
    evidence: release/evidence/operational-query-capability-v1.json
    decision: The framework-neutral DirectoryUser and Document families execute the complete
      compiler-declared exact predicate, independent order, exact count, and ordinal-window surface
      through the shared service and generated Rust, Go, TypeScript, Python, CLI, and MCP paths.
      Installed TLS acceptance, the shared remote driver corpus, and immutable development-registry
      installation across npm, PyPI, Go, and Cargo pass from the exact generated artifacts. Static
      boundary checks retain no caller-supplied query structure, client filtering, sorting, counting,
      page walking, provider selection, storage access, or framework-owned route. The reproducible
      capability receipt explicitly leaves real framework route evidence owned by external adapter
      repositories and claims no such route acceptance inside RiffDB.
  standing_design_tests:
    interface_safety: Applications and adapters receive only generated finite methods or closed
      generated member choices with typed bounded values; they cannot select fields, raw operators,
      Boolean/order structure, providers, policy, cost, fallback, or freshness, and no transport
      exposes a generic unsafe query object.
    scale: Inputs, family choices, request/response bytes, provider work, page/offset, generated
      decode, buffers, waits, retries, diagnostics, and publication artifacts remain compiler or
      protocol bounded; no client or server surface retains or materializes the matching population.
  human_review_triggers: *id001
- id: WP-659
  name: Packed compiler-bound named-result carriage
  depends_on: [WP-654]
  objective: After exact acceptance of ADR-0135, reuse the existing bounded canonical-column
    codec for compiler-covered named-query results, decode it directly in all generated clients,
    and activate it only if the complete cloud large-result and no-regression gates pass.
  requirements: [API-001, QRY-001, QRY-002, QRY-003, QRY-004, QRY-006,
    QRY-007, QRY-008, QRY-009, PERF-002, PERF-008, PERF-018]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0074, ADR-0123,
    ADR-0124, ADR-0127, ADR-0130, ADR-0131, ADR-0133, ADR-0135]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0135-packed-compiled-result-carriage.md, proto/**,
    crates/riffdb-types/**, crates/riffdb-proto/**,
    crates/riffdb-query-module/**, crates/riffdb-service/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-client-python-native/**, crates/riffdb-driver-host/**,
    crates/riffdb-cli/**, crates/riffdb-api-mcp/**,
    crates/riffdb-mcp-stdio/**, clients/**, examples/**, fixtures/**,
    tests/**, scripts/**, benchmarks/**, release/**, docs/SUMMARY.md,
    docs/reference/**, docs/getting-started/**, docs/performance/**]
  deliverables:
  - Additive negotiated PACKED_V1 named-result response reusing the exact ProjectedReadyPacked
    canonical-cell and offset rules, with strict parent preflight, response charging, exclusive-arm
    validation, golden descriptors, and old/new client/server compatibility
  - Compiler-layout-bound Rust, Go, TypeScript, and Python generated decoders that construct typed
    rows directly from bounded column slices without generic Value, ApplicationValue, symbolic map,
    exposed ordinal, caller schema, or partial-result release
  - Unary and bounded-session parity for outcomes, identities, heads, cursors, authorization,
    revocation, cancellation, malformed messages, and every canonical value/type edge
  - Paired workstation, N1, and E2 BoardPage50/200/450 evidence plus the complete ordinary read,
    unary write, c32 mixed, seed, startup, recovery, backup/restore, memory, and generated-size
    no-regression matrix required by ADR-0135
  - Value-free failed-activation receipt and removal of production selection if either cloud
    BoardPage450 misses 1.10x safe PostgreSQL, any profile misses the 30-percent compact-relative
    improvement, or an existing semantic/performance gate regresses
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-proto -p riffdb-query-module -p riffdb-service -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-client-python-native -p riffdb-driver-host --all-features
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/driver-conformance --all-languages
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The packed arm is byte/semantically identical to legacy and compact results across
    every generated language, malformed or unauthorized responses fail closed, BoardPage450 meets
    both cloud 1.10x gates and every 30-percent candidate threshold, and no ordinary operation,
    durability, compatibility, resource, or tail gate regresses; otherwise packed selection is
    removed and WP-623 receives only the failed-candidate receipt.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-659-packed-result-closure.md
    decision: The additive PACKED_V1 protocol, strict canonical-column validation, and V1/V2/V3
      driver compatibility are retained, but direct generated decoder prototypes and production
      selection are removed; generated clients continue to negotiate COMPACT_V1. The paired workstation
      BoardPage450 candidate improved only 13.9 percent from compact and remained 1.65x same-run
      safe PostgreSQL, failing both conjunctive ADR-0135 thresholds before cloud or release-matrix
      execution. Representative generated clients also grew 5.5 to 8.7 percent, failing the
      no-regression gate. Architecture tests pin packed decoder bulk and production selection absent;
      this package claims no PERF-018 activation.
  standing_design_tests:
    interface_safety: Applications still invoke only compiler-fixed generated named queries and
      cannot select packing, layout, ordinals, types, provider, index, validation, policy,
      freshness, consistency, or fallback; every response arm proves the same exact operation.
    scale: Rows, columns, offsets, cells, bytes, buffers, allocations, decode work, output objects,
      waits, diagnostics, and fixtures are compiler/protocol bounded per request and no process-
      lifetime or data-population cache is introduced.
  human_review_triggers: *id001
- id: WP-660
  name: Single-owner bounded application-session mechanics
  depends_on: [WP-644, WP-649, WP-650]
  objective: Reimplement ADR-0127's already accepted optional application session so the common
    one-owner, one-in-flight generated-operation path does not pay multiplexing maps, oneshots, or
    response-router task hops, while retaining the existing bounded multiplexed path whenever
    concurrent in-flight work actually requires it and changing no operation semantics.
  requirements: [API-001, PERF-005, PERF-008, PERF-018, NET-001, NET-002,
    NET-003, NET-004, DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0105, ADR-0106,
    ADR-0123, ADR-0127, ADR-0132]
  allowed_paths: [work_packages.yaml, crates/riffdb-client-rust/**,
    crates/riffdb-api-grpc/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, examples/app-baseline/**,
    benchmarks/**, tests/**, scripts/**, docs/performance/**,
    docs/SUMMARY.md, docs/reference/**, docs/getting-started/**]
  deliverables:
  - One exclusive single-owner session state machine that owns its bounded request sender and
    response stream, permits exactly one live operation, validates monotonically increasing
    correlation and exact response kind inline, and uses no pending map, per-call oneshot, response
    router task, shared mutable authority, or implicit ordering/freshness promise
  - Automatic use only by an exclusive generated-client handle; cloning, concurrent calls, or
    configured in-flight capacity above one retains ADR-0127's existing bounded multiplexed state
    machine rather than weakening cancellation, out-of-order, capacity, or uncertainty behavior
  - Deterministic cancellation, stream-loss, revocation, expiry, application-drift, malformed-
    response, reconnect, read-after-commit, command-uncertainty, and graceful-shutdown equivalence
    between unary, exclusive session, and multiplexed session paths
  - Back-to-back c1/c8 mechanics receipts on N1 and E2 and full candidate c32/unary/seed controls;
    the exclusive path must improve point-read mean at least 20 percent on both hosts, then the
    complete ADR-0127 activation gates decide whether any default or PERF-018 amendment is proposed
  - Value-free failed-candidate closure if the mechanics threshold misses; no authentication,
    authorization, task-containment, protocol, or comparator change may be credited to a benchmark
    artifact or retained after a semantic/no-regression failure
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-client-rust -p riffdb-api-grpc -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/recovery_full
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The exclusive state machine is semantically indistinguishable from ordinary unary and
    ADR-0127 multiplexed operations, remains bounded and fail closed at every lifecycle edge, and
    improves c1 exact point-read mean at least twenty percent on both cloud profiles without a
    greater-than-five-percent regression; only a subsequent complete ADR-0127 gate pass and human
    PERF-018 amendment may change the generated-client default.
  standing_design_tests:
    interface_safety: The optimization accepts only one existing generated named operation at a
      time and cannot cache authority, create a transaction, infer ordering/freshness, relax
      durability, change retry/uncertainty, select storage, or expose raw protocol structure.
    scale: Exactly one request, response, correlation, cancellation record, and bounded pair of
      transport buffers exist per exclusive session; all concurrent or cloned use routes to the
      existing finite multiplexed implementation rather than growing hidden state.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-660-exclusive-session-closure.md
    decision: The one-owner candidate improved generated GetTicket mean 26.32 percent on N1 but
      regressed 8.24 percent on E2, failing the required 20-percent-on-both and five-percent-no-
      regression mechanics gates. The candidate was removed. The package also corrected the
      diagnostic so bounded-session workers retain the selected transport; the existing ADR-0127
      multiplexer then improved c1 mean 25.38 percent on N1 and 33.27 percent on E2. Those short
      cells are an existence proof only and authorize no release activation or PERF-018 change.
  human_review_triggers: *id001
- id: WP-661
  name: Corrected bounded-session activation and comparator decision
  depends_on: [WP-644, WP-649, WP-650, WP-660]
  objective: Re-evaluate ADR-0127's existing bounded application session after correcting the
    diagnostic topology that silently replaced selected sessions with unary worker connections,
    then either remove release selection again or produce the complete semantic and paired
    performance evidence required before a human PERF-018 comparator amendment.
  requirements: [API-001, PERF-005, PERF-008, PERF-018, NET-001, NET-002,
    NET-003, NET-004, DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0105, ADR-0106,
    ADR-0123, ADR-0127, ADR-0132]
  allowed_paths: [work_packages.yaml, SPEC.md,
    crates/riffdb-client-rust/**, crates/riffdb-api-grpc/**,
    crates/riffdb-server/**, crates/riffdb-observability/**,
    examples/app-baseline/**, benchmarks/**, tests/**, scripts/**,
    release/**, docs/performance/**, docs/SUMMARY.md, docs/reference/**,
    docs/getting-started/**]
  deliverables:
  - A regression test and diagnostic topology assertion proving every bounded-session worker owns
    and measures its selected stream instead of opening a fresh unary connection, with unary,
    asynchronous, and session cells remaining distinct and non-evidentiary until activation
  - Back-to-back c1/c8/c32 unary and bounded-session mechanics receipts on N1 and E2 using one exact
    release artifact, including client latency/throughput, server stages, process memory, session
    establishment exclusion, and fixed transport/in-flight identities
  - Complete ADR-0127 semantic equivalence for queries and commands under cancellation, stream
    loss, malformed/duplicate correlation, overload, revocation, expiry, application drift,
    read-after-commit, command uncertainty, reconnect, and graceful shutdown
  - Same-run safe-application PostgreSQL candidate sweeps proving at least 15-percent c1 and
    10-percent c8 RiffDB-over-unary improvement on both hosts; at c32, at least 0.90x PostgreSQL
    throughput, at most 1.25x PostgreSQL p95, and no greater-than-five-percent absolute regression
  - Every representative session-shaped unary scenario at most 1.10x same-run PostgreSQL p50,
    unchanged seed at most 5.0x, and no read/write/seed/tail/resource regression beyond five percent
    where a stricter gate does not apply
  - If and only if every gate passes, an exact proposed PERF-018 amendment freezing protocol,
    client, connections, maximum in-flight, retry, dataset, weights, and comparator shapes; no
    default or release evidence changes without separate exact human acceptance
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-client-rust -p riffdb-api-grpc -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/recovery_full
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The corrected bounded session passes every ADR-0127 semantic and two-profile
    performance gate and is presented for an exact human PERF-018 amendment; otherwise unary
    remains the release default, the failed activation is receipted, and no comparator or public
    default changes.
  standing_design_tests:
    interface_safety: The session accepts only existing generated named operations and cannot cache
      authority, create a transaction, infer order or freshness, change durability or uncertainty,
      select storage, or expose protocol structure; every operation retains all current safe points.
    scale: Correlations, live operations, request and response bytes, cancellation records, queues,
      tasks, waits, retries, telemetry, and evidence are capped by the accepted session and harness
      ceilings independently of history, database size, completed operations, or client runtime.
  closure:
    status: complete_without_release_activation
    evidence: docs/performance/wp-661-bounded-session-activation.md
    decision: Correct transport selection made the existing bounded session materially faster on
      N1, but counterbalanced E2 observations ranged from a 13.1-percent c1 regression to a
      0.8-percent gain and from a 9.6-percent c8 regression to a 10.2-percent gain. The required
      stable 15-percent c1 and 10-percent c8 improvements on both hosts therefore fail before the
      PostgreSQL release sweep. Unary remains the default, PERF-018 is unchanged, and only the
      non-evidentiary diagnostic topology correction is retained.
  human_review_triggers: *id001
- id: WP-662
  name: Bounded framed application-transport candidate
  depends_on: [WP-649, WP-650, WP-660, WP-661]
  objective: After exact acceptance of ADR-0137, replace the measured Tonic/HTTP2 scheduling floor
    for high-frequency generated named operations with one bounded first-party Rust frame protocol
    over the existing loopback/TLS ingress, while preserving gRPC compatibility and every
    per-operation semantic, authority, durability, freshness, uncertainty, and resource guarantee.
  requirements: [API-001, PERF-005, PERF-008, PERF-018,
    NET-001, NET-002, NET-003, NET-004, NET-005, NET-006, NET-007, NET-008,
    NET-009, NET-010, NET-011, NET-012,
    DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006, DRV-007,
    DRV-008, DRV-009, DRV-010, DRV-011, DRV-012, DRV-013, DRV-014]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0105, ADR-0106,
    ADR-0123, ADR-0127, ADR-0132, ADR-0133, ADR-0137]
  allowed_paths: [work_packages.yaml, SPEC.md, Cargo.toml, Cargo.lock, adr/README.md,
    adr/0137-bounded-framed-application-transport.md, proto/**,
    crates/riffdb-types/**, crates/riffdb-proto/**,
    crates/riffdb-api-application/**, crates/riffdb-api-frame/**,
    crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-driver-host/**,
    crates/riffdb-server/**, crates/riffdb-observability/**,
    clients/go/**, clients/typescript/**, clients/python/**,
    examples/app-baseline/**, fixtures/**, fuzz/**, tests/**, benchmarks/**,
    scripts/**, release/**, docs/SUMMARY.md, docs/reference/**,
    docs/getting-started/**, docs/operations/**, docs/performance/**]
  deliverables:
  - One versioned fixed-header frame protocol carrying only exact application-session open,
    generated command, named-query, cancellation, response, typed-failure, and controlled-close
    envelopes with strict Protobuf payloads and finite bytes, correlations, tasks, waits, and work
  - One loopback-preface and TLS-ALPN demultiplexer over the existing listener/trust profiles, with
    no insecure remote port, trust downgrade, cryptographic change, proxy principal, compression,
    application knob, or ingress dependency outside the accepted transport crates
  - One API-neutral Rust generated-operation adapter used by gRPC and framing, with architecture
    tests proving the frame engine owns no storage, catalog, policy, plan, commit, projection, audit,
    or cached-allow authority and every current safe point remains per operation
  - Native Rust transport plus Rust-driver-host carriage for Go, TypeScript, and Python, with no
    target-language implementation of TLS, framing, authentication, retry, uncertainty, freshness,
    or public-error semantics
  - Hostile-frame, fragmentation, cancellation, stream-loss, reconnect, rotation, revocation,
    expiry, role/contract/module drift, read-after-commit, command-uncertainty, overload, slow-peer,
    graceful-shutdown, and unary/session/framed byte-semantic equivalence evidence
  - Closed workstation, N1, and E2 mechanics ledgers and reject-first performance receipts; failed
    thresholds remove production listener/client selection, while a complete pass produces only a
    proposed exact PERF-018/default amendment for separate human acceptance
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-driver-host -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/recovery_full
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The framed candidate is semantically indistinguishable from unary gRPC at every safe
    point, passes the cross-profile mechanics and complete release performance gates in ADR-0137,
    and is submitted for an exact human comparator/default amendment; otherwise production framing
    is removed, WP-662 closes with only its failed-candidate receipt, and WP-623 remains open.
  standing_design_tests:
    interface_safety: Framing carries only existing generated named operations and cannot cache
      authority, create a transaction, imply ordering/freshness, relax durability or uncertainty,
      select storage, expose protocol structure, or bypass the shared application service.
    scale: Frames, payloads, correlations, tasks, buffers, queues, cancellations, waits, lifetime
      work, telemetry, and evidence are hard-bounded independently of database size and duration;
      no completed response or application value is retained after delivery.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-662-framed-transport-closure.md
    decision: The framed candidate reduced exact point-read mean materially, but missed the
      reject-first c1 threshold on all three workstation generations, one N1 generation, and all
      three E2 generations. E2 also missed the outside-service reduction in two generations. The
      production protocol, listener, clients, selectors, fixtures, and public documentation were
      removed; unary remains the default, PERF-018 is unchanged, and WP-623 remains open.
  human_review_triggers: *id001
- id: WP-670
  name: Diagnostic-gate-first exclusive application lane verdict
  depends_on: [WP-649, WP-650, WP-654, WP-659, WP-663, WP-664, WP-665, WP-666,
    WP-667, WP-669]
  objective: After exact acceptance of ADR-0141, implement only the minimum safe private
    new-identity direct exclusive candidate, immediately obtain a complete unary public-ratio
    verdict, and stop transport work on any service-owned small-operation miss; production
    hardening is permitted only after every small operation passes without hiding the separately
    owned large-result miss.
  requirements: [API-001, PERF-005, PERF-008, PERF-018, SEC-001, SEC-002,
    NET-001, NET-002, NET-003, NET-004, NET-005, NET-006, NET-007, NET-008,
    NET-009, NET-010, NET-011, NET-012,
    DRV-001, DRV-002, DRV-003, DRV-004, DRV-005, DRV-006, DRV-007,
    DRV-008, DRV-009, DRV-010, DRV-011, DRV-012, DRV-013, DRV-014]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0070, ADR-0105, ADR-0106,
    ADR-0120, ADR-0123, ADR-0127, ADR-0132, ADR-0133, ADR-0137, ADR-0138,
    ADR-0139, ADR-0140, ADR-0141]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0141-public-gate-first-exclusive-application-lane.md,
    Cargo.toml, Cargo.lock, proto/**, crates/riffdb-types/**, crates/riffdb-proto/**,
    crates/riffdb-api-application/**, crates/riffdb-api-frame/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, clients/go/**, clients/typescript/**,
    clients/python/**, examples/app-baseline/**, examples/agent-alpha/**,
    fixtures/**, fuzz/**, tests/**, benchmarks/**, scripts/**, release/**,
    docs/SUMMARY.md, docs/reference/**, docs/getting-started/**,
    docs/operations/**, docs/performance/**]
  deliverables:
  - The current-head N1/E2 short unary, mixed-c32, seed, and exact GetTicket ledger in
    docs/performance/wp-670-current-head-preflight.md, clearly marked non-evidentiary and preserving
    every unfavorable result
  - Before protocol editing, per-host arithmetic for every unary scenario separating unchanged
    service floor, removable outside-service cost, encoded request/response bytes, same-run
    PostgreSQL budget, and the candidate latency required to meet it
  - After exact ADR acceptance only, one private new magic/version/ALPN diagnostic lane with one
    caller-owned operation, strict existing Protobuf payloads, shared API-neutral semantics,
    current per-operation authority, minimum malformed-input/resource tests, and no public selector,
    target-language exposure, compatibility promise, or reuse of rejected experiment identities
  - Three counterbalanced loopback and verified-TLS generations on N1/E2 followed immediately by
    the complete existing unary scenario matrix and a closed per-scenario service/residual/byte
    ledger
  - >-
    A binary decision receipt: remove the candidate and stop transport work upon any service-owned
    small-operation miss, or preserve it as private diagnostic machinery and authorize WP-671 when
    every small read and command is at most 1.10x same-run safe-application PostgreSQL in every
    generation
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-driver-host -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The private candidate first produces a decisive complete-unary N1/E2 verdict. Any
    small read or command above 1.10x, or any service floor that cannot fit that budget, stops
    transport work and removes the candidate before production hardening. A complete small-
    operation pass closes WP-670 with a private candidate and authorizes WP-671, but permits no
    hardening, activation, public selector, generated exposure, or compatibility claim.
  standing_design_tests:
    interface_safety: Applications still invoke only generated typed commands and exact named
      queries and cannot select framing, pool size, trust, cached authority, transaction scope,
      durability, freshness, retry after uncertainty, fallback after submission, or storage;
      every operation traverses the shared application service and all current safe points.
    scale: Lanes, pool, frames, bytes, tasks, operations, waits, cancellation, replacement,
      telemetry, and evidence are fixed-bounded independently of history, database size, tenant
      count, completed operations, or process duration; no full-state or result cache is introduced.
  closure:
    status: complete_without_production_activation
    evidence: docs/performance/wp-670-exclusive-verdict.md
    decision: The private candidate improved generated GetTicket mean 35.48 percent on N1 and
      36.77 percent on E2 and closed its stage ledger within 0.167 percent, but the first complete
      unary generation still missed 1.10x for every N1 small operation and six E2 small operations.
      Multiple misses were already service-floor-owned. ADR-0141 therefore required immediate
      removal before further generations or hardening; unary gRPC and PERF-018 are unchanged, and
      WP-671/WP-672 are not authorized by this package.
  human_review_triggers: *id001
- id: WP-671
  name: BoardPage450 result-path attribution and decision
  depends_on: [WP-654, WP-659, WP-670]
  objective: While ADR-0141's private direct candidate exists, close a customer-paid stage ledger
    for BoardPage450 on N1/E2, determine whether result assembly, windowing, hydration, encoding,
    response release, or transport owns the remaining miss, and propose at most one separately
    reviewed result-path package whose measured Amdahl bound can reach the public gate.
  requirements: [PERF-005, PERF-008, PERF-018, QRY-001, QRY-002, QRY-003,
    QRY-004, QRY-005, QRY-006, QRY-007, QRY-008, QRY-009]
  required_adrs: [ADR-0040, ADR-0055, ADR-0070, ADR-0120, ADR-0123, ADR-0132,
    ADR-0137, ADR-0141]
  allowed_paths: [work_packages.yaml, adr/README.md, adr/**,
    crates/riffdb-query/**, crates/riffdb-api-application/**,
    crates/riffdb-api-frame/**, crates/riffdb-api-grpc/**,
    crates/riffdb-client-rust/**, crates/riffdb-observability/**,
    crates/riffdb-server/**, examples/app-baseline/**, benchmarks/**, scripts/**,
    tests/**, docs/performance/**]
  deliverables:
  - Counterbalanced N1/E2 BoardPage50/200/450 runs under unary gRPC and the same private WP-670
    candidate, with identical data, query identity, authorization, freshness, and result bytes
  - A closed caller-time ledger for BoardPage450 covering plan execution, row/window assembly,
    dependent hydration, value conversion, Protobuf encoding, response release, transport, and all
    remaining residual, closing within five percent
  - Absolute CPU, allocation/byte, and off-CPU evidence for the dominant stage, with response-size
    scaling across BoardPage50/200/450 and no value-bearing profile artifact in repository history
  - Predeclared Amdahl arithmetic for at most one candidate showing its predicted N1/E2 public
    ratios; no implementation proposal when the measured movable share cannot fit 1.10x
  - A closure receipt that either proposes a separate ADR/work package for exact human review or
    records that the universal BoardPage450 gate requires a maintainer product-rule decision
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-query -p riffdb-api-application -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-query -p riffdb-api-application -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-server --all-targets --all-features -- -D warnings
  exit_gate: BoardPage450 caller time closes within five percent on N1/E2, its dominant movable
    stage and response-size scaling are measured, and either one candidate is proven capable of
    reaching 1.10x before implementation or the result-path campaign stops and returns the product
    rule to the maintainer. WP-671 cannot activate transport or weaken query bounds, authorization,
    freshness, result identity, or response budgets.
  standing_design_tests:
    interface_safety: Attribution and any proposal preserve named compiler-bounded reads, current
      row/field policy, explicit freshness, exact result identity, and response budgets; no raw
      predicate, caller-selected projection, authority cache, hidden-row inference, or unbounded
      result surface is introduced.
    scale: Every diagnostic and proposed mechanism is bounded by the compiled query/result budget;
      retained bytes, rows, tasks, buffers, telemetry, and evidence do not grow with history,
      tenants, completed requests, or process lifetime.
  closure:
    status: complete_without_candidate
    evidence: docs/performance/wp-670-exclusive-verdict.md
    decision: WP-670 removed the private lane and did not authorize this conditional package.
      ADR-0142 subsequently replaced the universal unary ratio with absolute and frozen-regression
      qualification without reviving that lane. No BoardPage transport/result candidate is open.
  human_review_triggers: *id001
- id: WP-672
  name: Conditional exclusive application lane hardening and activation
  depends_on: [WP-670, WP-671]
  objective: Only after WP-670 proves every small unary operation and WP-671 supplies a viable
    large-result closure, harden the private exclusive lane into a bounded production candidate and
    retain it only when the complete public performance, semantic, lifecycle, resource, and
    cross-profile release matrix passes.
  requirements: [API-001, PERF-005, PERF-008, PERF-018, SEC-001, SEC-002,
    NET-001, NET-002, NET-003, NET-004, NET-005, NET-006, NET-007, NET-008,
    NET-009, NET-010, NET-011, NET-012, DRV-001, DRV-002, DRV-003, DRV-004,
    DRV-005, DRV-006, DRV-007, DRV-008, DRV-009, DRV-010, DRV-011, DRV-012,
    DRV-013, DRV-014]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0070, ADR-0105, ADR-0106,
    ADR-0120, ADR-0123, ADR-0127, ADR-0132, ADR-0133, ADR-0137, ADR-0138,
    ADR-0139, ADR-0140, ADR-0141]
  allowed_paths: [work_packages.yaml, adr/README.md,
    adr/0141-public-gate-first-exclusive-application-lane.md,
    Cargo.toml, Cargo.lock, proto/**, crates/riffdb-types/**, crates/riffdb-proto/**,
    crates/riffdb-api-application/**, crates/riffdb-api-frame/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-driver-host/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, clients/go/**, clients/typescript/**,
    clients/python/**, examples/app-baseline/**, examples/agent-alpha/**,
    fixtures/**, fuzz/**, tests/**, benchmarks/**, scripts/**, release/**,
    docs/SUMMARY.md, docs/reference/**, docs/getting-started/**,
    docs/operations/**, docs/performance/**]
  deliverables:
  - Finite independent-lane pool, bounded setup/replacement/lifetime behavior, production selector,
    shared Rust driver-host path, and no target-language protocol implementation
  - Exhaustive hostile-wire, cancellation, uncertainty, revocation, expiry, rotation, drift,
    saturation, reconnect, restart, shutdown, and resource evidence plus architecture pins for
    direct ownership and absence of semantic/storage authority in transport
  - A short complete product matrix containing every ordinary, command, and BoardPage scenario,
    mixed c32, seed, correctness, setup, resource, and no-regression gate on N1/E2
  - Only after every short gate passes, the complete workstation/N1/E2 90-second release matrix and
    an exact proposed PERF-018/default amendment for separate human acceptance
  - Public handbook/generated-client changes only in the same commit as the separately accepted
    activation/default decision; no diagnostic identity is documented as available before then
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-proto -p riffdb-api-grpc -p riffdb-client-rust -p riffdb-driver-host -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/recovery_full
  - ./scripts/generate-proto --check
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every representative unary scenario including BoardPage450 is at most 1.10x same-run
    safe-application PostgreSQL on N1/E2; mixed c32 throughput is at least 0.90x and p95 at most
    1.25x; seed remains at most 5.0x; correctness and semantic equivalence are exact; resource and
    lifecycle gates pass; and only a complete workstation/N1/E2 release pass may produce a
    separately accepted PERF-018/default amendment. Any miss leaves unary gRPC selected and no
    candidate wire identity active.
  standing_design_tests:
    interface_safety: Applications still invoke only generated typed commands and exact named
      queries and cannot select framing, pool size, trust, cached authority, transaction scope,
      durability, freshness, retry after uncertainty, fallback after submission, or storage; every
      operation traverses the shared application service and all current safe points.
    scale: Lanes, pool, frames, bytes, tasks, operations, waits, cancellation, replacement,
      telemetry, and evidence are fixed-bounded independently of history, database size, tenant
      count, completed operations, or process duration; no full-state or result cache is introduced.
  closure:
    status: complete_without_candidate
    evidence: docs/performance/wp-670-exclusive-verdict.md
    decision: WP-670 failed its prerequisite and removed the private protocol before hardening.
      ADR-0142 changes only qualification governance, keeps unary gRPC selected, and explicitly
      does not authorize or revive this conditional production lane.
  human_review_triggers: *id001
- id: WP-673
  name: Small-operation API-neutral service-floor attribution
  depends_on: [WP-622, WP-638, WP-650, WP-670]
  objective: After WP-670 removes transport as the next universal explanation, isolate every
    representative small read and command in its own measured process generation, close the
    API-neutral service, storage/query, coordinator, conversion, and remaining caller ledgers on
    N1/E2, and authorize at most one measured service-path candidate that can reach the unchanged
    public gate without weakening any safe point.
  requirements: [PERF-005, PERF-008, PERF-018, QRY-001, QRY-002, QRY-003,
    QRY-004, QRY-005, QRY-006, QRY-007, QRY-008, QRY-009, POC-005,
    LOG-001, TXN-001, TXN-002]
  required_adrs: [ADR-0040, ADR-0055, ADR-0056, ADR-0070, ADR-0120,
    ADR-0123, ADR-0129, ADR-0132, ADR-0141]
  allowed_paths: [work_packages.yaml, crates/riffdb-service/**,
    crates/riffdb-observability/**, crates/riffdb-storage-redb/**,
    crates/riffdb-server/**, examples/app-baseline/**, benchmarks/**,
    scripts/**, tests/**, docs/performance/**]
  deliverables:
  - A private exact-scenario diagnostic selection that restarts `riffdbd` after setup, executes only
    one existing generated scenario, and emits no application value, caller-chosen server operation,
    production selector, or change to PERF-018's normal ordering/topology
  - Paired N1/E2 64-sample ledgers for all seven ordinary read and four command scenarios, with
    each process generation containing only that operation after warmup and every unfavorable cell
    preserved
  - Read ledgers covering public adapter, authentication/admission, dispatch, plan/parameter work,
    three authorization/audit safe points, storage snapshot and fourteen existing query-execute
    substages, result assembly, conversion, response release, and remaining caller time
  - Command ledgers covering public adapter, API-neutral preparation, coordinator queue/evaluation/
    validation/apply/journal/fence/publication, service finish, conversion, response release, and
    remaining caller time
  - Per-host Amdahl arithmetic for at most one next candidate; no implementation package when no
    single cross-host movable family can fit every affected scenario's 1.10x budget
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - cargo +1.97.0 test -p riffdb-service -p riffdb-observability -p riffdb-storage-redb -p riffdb-server --all-features
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-service -p riffdb-observability -p riffdb-storage-redb -p riffdb-server --all-targets --all-features -- -D warnings
  exit_gate: Every small operation has a closed N1/E2 service-floor ledger and exact same-run
    PostgreSQL budget. The package either proposes one separately reviewed candidate whose measured
    movable share can reach 1.10x on both hosts, or stops the campaign and returns the universal
    unary threshold to the maintainer. It cannot revive the WP-670 transport or change semantics.
  standing_design_tests:
    interface_safety: Diagnostics select only one already compiled/generated scenario in the local
      harness; application and production surfaces gain no selector, cache, transaction, predicate,
      authorization, freshness, durability, uncertainty, or storage control, and all current safe
      points execute unchanged.
    scale: Scenario identity is one member of the frozen finite benchmark registry; stage counters,
      windows, bytes, samples, tasks, and retained evidence have fixed ceilings independent of data
      history, database size, tenants, completed operations, or process lifetime.
  closure:
    status: complete_without_candidate
    evidence: docs/performance/wp-673-service-floor-attribution.md
    decision: Exact 64-operation N1/E2 process generations closed all seven read and four command
      ledgers. Every failing read required 1.74 to 8.06 times the complete query-execute stage to
      disappear, while the two remaining E2 command failures required eliminating 99.57 and 99.85
      percent of the largest validation/encoding/staging family. No single cross-host API-neutral
      candidate can fit the unchanged 1.10x gate without removing required guarantees. WP-673
      authorizes no implementation and returns the universal threshold to the maintainer.
  human_review_triggers: *id001
- id: WP-674
  closure:
    status: unblocked_pending_qualification_run
    evidence: docs/performance/wp-674-comparator-stability.md
    decision: A fresh receipt-mode run on N1 at 35fe502d under ADR-0146's protocol -- five
      generations, 20 warmups, 1000 measured operations, PostgreSQL fsync=on -- shows RiffDB stable
      on all fourteen scenarios (central-three spread 1.002 to 1.028) and PostgreSQL over the 1.20
      limit on five. Raising generations from 100 to 1000 operations did not fix the comparator; it
      moved which scenarios fail. Because the stability rule is written "for each backend" and
      PostgreSQL must run in every generation, its spread invalidates the receipt even though
      ADR-0142 made its ratios disclosure rather than gates. The package cannot close without an
      accepted amendment to ADR-0142/ADR-0146 choosing how the comparator's stability is treated.
      ADR-0171 is that amendment and was accepted on 2026-08-30 -- the spread rule binds the gated
      backend, and the comparator's spread becomes mandatory published disclosure. SPEC PERF-008 and
      PERF-018 carry the amended text, and scripts/check-wp674-unary-baseline implements the split
      rule with self-test arms in both directions, each drift-checked to be separately load-bearing.
      The receipt records method.stability_rule_binds, so a candidate measured under the old rule
      fails method-drift rather than comparing against a baseline that meant something different.
      The profile runner now rejects a stale nested app-baseline lock before retaining its first
      result, and the app-baseline self-test uses Cargo's locked resolver, preventing a runner build
      from dirtying the exact source tree between scenarios. What remains is the qualification run
      itself -- five generations on N1, E2, and workstation with host-validity preflight and
      postflight -- which has no known in-repository blocker.
  name: Portable unary SLO governance and frozen alpha baseline
  depends_on: [WP-654, WP-670, WP-673]
  objective: After exact acceptance of ADR-0142 and its ADR-0143/ADR-0146 measurement amendments, replace the
    unreachable universal unary PostgreSQL ratio with fixed N1/E2 service levels and a
    non-rotatable RiffDB regression bank, retain same-run PostgreSQL unary disclosure and every
    mixed/seed comparator obligation, and hand WP-623 one internally consistent release gate.
  requirements: [PERF-005, PERF-008, PERF-018, END-007, END-008]
  required_adrs: [ADR-0056, ADR-0059, ADR-0123, ADR-0132, ADR-0141, ADR-0142, ADR-0143, ADR-0146,
    ADR-0171]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/README.md,
    adr/0142-portable-unary-service-levels-and-comparator-evidence.md,
    adr/0143-fixed-generation-unary-stability.md,
    adr/0146-fixed-generation-unary-sampling-and-cloud-host-validity.md,
    adr/0171-gated-backend-measurement-stability.md,
    examples/app-baseline/**, benchmarks/**, scripts/**, release/**,
    tests/**, docs/SUMMARY.md, docs/performance/**, docs/operations/**]
  deliverables:
  - Exact accepted SPEC and ADR reconciliation making PERF-008 the sole alpha performance-gate
    owner, preserving PERF-005's safety semantics while removing its superseded parity sentence,
    and explicitly amending the affected ADR-0123/0132/0141 future criteria
  - A frozen finite unary scenario-class registry and report schema carrying absolute N1/E2 p50
    and p95 ceilings, PostgreSQL ratios, exact RiffDB baseline identity, and per-scenario regression
    results without caller-selectable classification
  - Negative conformance for missing generations, hosts, backends or scenarios; greater-than-20-
    percent central-three repetition spread; performance-selected retry; host, workload, semantic
    or correctness drift; baseline rotation; and attempted post-result reclassification
  - Five fixed counterbalanced 20-warmup/1,000-measurement process generations per unary scenario
    on N1, E2, and workstation, retaining both extremes, binding V2 whole-cell host validity under
    the fixed 1.00-percent CPU-steal ceiling, and binding the exact artifact, immutable predecessor,
    and value-free report digests into the frozen baseline receipt while preserving every
    unfavorable PostgreSQL comparison
  - Paired unchanged c32 mixed and full-seed controls proving the governance implementation did
    not change the product, workload, comparator, durability, topology, or current public result
  - WP-623 dependency, deliverable, and exit-gate reconciliation plus closure of WP-671/WP-672 as
    unauthorized historical branches rather than open production work
  acceptance_commands:
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-workspace-policy
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --manifest-path examples/app-baseline/Cargo.toml --workspace --all-targets --all-features -- -D warnings
  exit_gate: The exact accepted rule is encoded once in SPEC and executable report validation; a
    fresh exact-artifact unary bank passes every absolute N1/E2 ceiling and binds N1/E2/workstation
    regression ceilings; all PostgreSQL ratios remain published; mixed and seed controls remain
    unchanged; and WP-623 can consume the receipt without contradictory PERF-005/PERF-008 rules.
  standing_design_tests:
    interface_safety: Qualification cannot disable or weaken authorization, row/field policy,
      boundedness, freshness, atomicity, durability, uncertainty, recovery, or response release;
      applications cannot select a latency class, baseline, comparator, host, or evidence result.
    scale: The scenario, profile, repetition, sample, artifact, and report matrices are fixed and
      bounded; no product cache, full-state materialization, or history-sized evidence is added.
  human_review_triggers: *id001
- id: WP-675
  name: Nullable exact-order semantic and compiler profiles
  depends_on: [WP-656, WP-658]
  objective: After exact acceptance of ADR-0145, add generic compiler-declared nulls-first and
    nulls-last total-order semantics for optional or historically missing scalar fields, freezing
    source, semantic IR, reference evaluation, and compatibility without activating provider
    runtime or importing any external framework artifact.
  requirements: [OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006, OQ-015,
    OQ-017, OQ-019, OQ-021, OQ-022, OQ-024, OQ-026, OQ-027, OQ-028, OQ-029]
  required_adrs: [ADR-0002, ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124, ADR-0130, ADR-0131, ADR-0134, ADR-0145]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/README.md,
    adr/0145-compiler-declared-nullable-total-order-placement.md,
    release/version-topology-v1.json, crates/riffdb-types/**,
    crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-catalog/**, crates/riffdb-cli/assets/editor/**,
    clients/editor/**, fixtures/**, fuzz/**, tests/**, scripts/**,
    docs/SUMMARY.md, docs/riffql/**, docs/architecture/**,
    docs/getting-started/OPERATIONAL-QUERIES.md]
  deliverables:
  - Accepted ADR-0145 SPEC reconciliation and public RiffQL documentation for per-term `nulls
    first` and `nulls last`, required whenever the complete family lacks a present/non-null proof,
    with no request-time placement or implicit backend default
  - One canonical NoValue order class in which missing and explicit null compare equal for the
    current term, later terms and the complete ascending entity key resolve ties, placement is not
    reversed by descending direction, and state predicates retain their existing truth model
  - RiffQL V7, query IR V10, and query-module V10 semantic successors carrying closed
    PresentOnlyV1/NullsFirstV1/NullsLastV1 identities, plus a preimplementation topology audit that
    stops for administrative renumbering rather than occupying an intervening identity
  - Least-sufficient compilation retaining byte-exact V6/V9/V9 artifacts when present/non-null is
    proven, preserving explicit placement even when currently vacuous, and rejecting missing
    proof, invalid placement, optional entity-key, unsupported comparison, excessive family, or
    incomplete provider requirements with source-spanned diagnostics
  - Independent bounded reference evaluation and randomized truth-table corpus for missing, null,
    optional text/timestamp, value boundaries, later terms, duplicate values, mixed directions,
    both placements, complete keys, predicates that prove non-null, and every finite family member
  - Canonical successor fixtures, locks, hashes, generated-schema metadata, editor support,
    version-topology entries, fuzz coverage, and architecture negatives proving this package adds
    no provider runtime/state, scan, caller query structure, framework branch, or compatibility
    reinterpretation
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-riffql-syntax -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog --all-features
  - cargo +nightly-2026-07-12 fuzz run riffql_parser -- -max_total_time=60
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every nullable exact-order family member has one canonical compiler-owned placement,
    total-order identity, independent semantic result, and complete statically bounded provider
    requirement; every old artifact remains byte-exact and readable; and no provider runtime,
    framework artifact, request-time sort structure, scan, or fallback is introduced.
  standing_design_tests:
    interface_safety: Application callers still receive only finite generated named operations;
      the compiler owns field, direction, state placement, comparison, tie-breaker, predicate,
      provider requirement, policy, cost, and freshness, and an incomplete family fails closed.
    scale: Order terms, placement profiles, family members, IR bytes, reference work, provider
      requirements, generated artifacts, inputs, outputs, and diagnostics are compiler bounded and
      the semantic model assumes neither a full result nor co-located authoritative storage.
  closure:
    status: complete
    completed_at: "2026-08-24"
    requirements_satisfied: [OQ-001, OQ-002, OQ-003, OQ-004, OQ-005, OQ-006,
      OQ-015, OQ-017, OQ-019, OQ-021, OQ-022, OQ-024, OQ-026, OQ-027, OQ-028,
      OQ-029]
    evidence: RiffQL V7, query IR V10, and query-module V10 add a canonical NoValue order class
      with explicit nulls-first/nulls-last placement, randomized reference semantics, strict
      successor decoding, contiguous-lineage proof checks, generated-surface negatives, editor
      fixtures, version-topology registration, parser fuzzing, and complete acceptance commands.
    known_limitations: Provider-state and runtime execution remain intentionally unavailable until
      WP-676; public transport and generated application activation remain owned by WP-677.
    follow_ups: [WP-676, WP-677]
  human_review_triggers: *id001
- id: WP-676
  name: Nullable exact-order provider state and ordinal execution
  depends_on: [WP-657, WP-675]
  objective: Implement ADR-0145's state-aware exact total orders as additive rebuildable provider-
    state V5 with exact count and direct ordinal selection over one authorized epoch, without
    authoritative scans, request-time sorting, materialization, skipped-row/page walks, sentinel
    values, client shaping, or cross-provider transfer.
  requirements: [OQ-003, OQ-004, OQ-005, OQ-006, OQ-017, OQ-019, OQ-020,
    OQ-021, OQ-022, OQ-024, OQ-026, OQ-027, OQ-028, OQ-029]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0086, ADR-0108,
    ADR-0111, ADR-0124, ADR-0129, ADR-0130, ADR-0131, ADR-0134, ADR-0145]
  allowed_paths: [SPEC.md, work_packages.yaml, release/version-topology-v1.json,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-catalog/**, crates/riffdb-projection/**,
    crates/riffdb-query-executor/**, crates/riffdb-policy/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-service/**,
    crates/riffdb-server/**, fixtures/**, fuzz/**, tests/**, scripts/**,
    docs/SUMMARY.md, docs/riffql/**, docs/architecture/**,
    docs/getting-started/OPERATIONAL-QUERIES.md]
  deliverables:
  - Additive exact-provider state V5 binding each state-aware order layout, NoValue rank, scalar
    comparison, later terms, complete ascending entity key, authorized universe, exact-count and
    ordinal capabilities, static work/state amplification, generation, epoch, plan, and checksum
  - Provider-owned order statistics where missing and explicit null share one term class, value
    keys use existing canonical scalar comparisons, direction reverses only value comparison, and
    both placements equal the independent evaluator for optional text and timestamp fields
  - Exact cardinality and direct ordinal seek over the complete authorized predicate and V5 order
    at one provider epoch, returning only the bounded page and never scanning authoritative rows,
    sorting request results, materializing matches, walking skipped rows/pages, or fetching non-page
    values outside separately declared bounded policy evidence
  - Partition-scoped and policy-aligned activation plus bounded-row-admission negatives proving
    denied rows cannot affect NoValue distribution, count, rank, offset, cursor, work class,
    readiness, diagnostics, or lifecycle choice and sortable fields do not become projectable
  - Canonical rebuild, catch-up, persistence, restart, corruption, compaction, cancellation,
    retention, concurrent state-transition, and mixed-version refusal evidence; V4 remains readable
    and byte-exact, least-sufficient V4 writers remain active, and nullable plans never downgrade
  - Descriptor/layout validation once per provider-plan generation and epoch proof once per opened
    result set, with architecture guards forbidding per-row/per-probe proof work, sentinel encoding,
    scan fallback, framework branches, and cross-provider bitmap or row-ID transfer
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-types -p riffdb-query-ir -p riffdb-query-compiler -p riffdb-query-module -p riffdb-catalog -p riffdb-projection -p riffdb-query-executor -p riffdb-policy -p riffdb-storage-api -p riffdb-storage-memory -p riffdb-storage-redb -p riffdb-service -p riffdb-server --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every compiler-admitted nullable order equals the independent evaluator at one
    authorized epoch, exact count and ordinal selection use only declared indexed work, V4 remains
    readable and byte-exact, lifecycle/corruption fail closed, and no scan, request sort,
    materialization, page walk, sentinel, client shaping, or provider bridge exists.
  standing_design_tests:
    interface_safety: Runtime accepts only compiler-sealed plan/member and placement identities with
      typed values; it cannot accept caller order structure, switch providers, invent state order,
      omit policy/freshness, or substitute scans, fallbacks, sentinels, or client processing.
    scale: Provider state, state buckets, write amplification, index probes, rank/select work,
      offsets, pages, outputs, rebuild/catch-up work, waits, and diagnostics are statically bounded;
      no request allocates the matching population or walks work proportional to the offset.
  closure:
    status: complete
    completed_at: "2026-08-24"
    requirements_satisfied: [OQ-003, OQ-004, OQ-005, OQ-006, OQ-017, OQ-019,
      OQ-020, OQ-021, OQ-022, OQ-024, OQ-026, OQ-027, OQ-028, OQ-029]
    evidence: Additive provider-state V5 binds the compiled nullable program, descriptor, policy
      shape, partition, history incarnation, generation, frontier, and checksum. Its policy-aligned
      background lifecycle builds missing/null state classes and canonical value indexes before
      exposing exact count plus direct ordinal pages at one epoch. Optional text, timestamp, and
      numeric fixtures cover both placements and directions, later-key ties, zero/end/beyond-end
      offsets, state transitions, deletion, compaction, byte-exact restart, corruption, V4/V5
      refusal, and independent-evaluator equivalence. Architecture checks prohibit per-request
      descriptor proof, scans, request sorting, materialization, page walks, sentinels, and provider
      bridges. All package tests, process recovery tests, topology/generated/coverage/policy checks,
      handbook, formatting, and workspace Clippy pass.
    known_limitations: WP-676 activates only the internal provider port and server lifecycle.
      Application-service dispatch, transport carriage, generated Rust/Go/TypeScript/Python
      methods, CLI, MCP, and the immutable external handoff remain intentionally fail-closed until
      WP-677.
    follow_ups: [WP-677]
  human_review_triggers: *id001
- id: WP-677
  name: Nullable exact-order generated surfaces and external handoff
  depends_on: [WP-658, WP-676]
  objective: Activate ADR-0145's generic nullable total orders through the shared service,
    transports, generated Rust, Go, TypeScript, and Python SDKs, CLI, and MCP using a framework-
    neutral optional-field corpus, then publish an immutable capability receipt for external
    acceptance without importing any framework schema, route, profile, adapter, or runtime.
  requirements: [OQ-001, OQ-003, OQ-004, OQ-005, OQ-006, OQ-015, OQ-016,
    OQ-019, OQ-020, OQ-021, OQ-022, OQ-026, OQ-027, OQ-028, OQ-029, OQ-030,
    API-001, MCP-001, MCP-020, MCP-021, MCP-023, MCP-025, MCP-026, MCP-040,
    MCP-043, MCP-045, MCP-049]
  required_adrs: [ADR-0040, ADR-0051, ADR-0052, ADR-0053, ADR-0055, ADR-0074,
    ADR-0106, ADR-0108, ADR-0111, ADR-0117, ADR-0120, ADR-0124, ADR-0128,
    ADR-0130, ADR-0131, ADR-0133, ADR-0134, ADR-0145]
  allowed_paths: [SPEC.md, work_packages.yaml, proto/**,
    fixtures/adapters/operational-conformance/**, fixtures/adapters/conformance/**,
    fixtures/application-parity/**, fixtures/query-modules/**, fixtures/export/**,
    crates/riffdb-types/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**,
    crates/riffdb-query-executor/**, crates/riffdb-catalog/**,
    crates/riffdb-projection/**, crates/riffdb-policy/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-service/**,
    crates/riffdb-proto/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-client-rust/**,
    crates/riffdb-codegen-rust/**, crates/riffdb-codegen-typescript/**,
    crates/riffdb-codegen-go/**, crates/riffdb-codegen-python/**,
    crates/riffdb-driver-host/**, crates/riffdb-cli/**,
    crates/riffdb-mcp-stdio/**, crates/riffdb-server/**, clients/**,
    templates/**, examples/**, tests/**, scripts/**, release/**,
    docs/SUMMARY.md, docs/riffql/**, docs/getting-started/**,
    docs/integrations/**, docs/reference/**]
  deliverables:
  - Shared application-service, gRPC, CLI, MCP, Rust, Go, TypeScript, and Python activation for
    finite generated nullable-order operations whose field, direction, placement, predicate, exact
    total, ordinal page, policy, provider epoch, plan identity, bounds, and typed failures remain
    identical on every surface
  - A framework-neutral InventoryRecord corpus with optional text and timestamp fields covering
    nulls first/last, ascending/descending, later-term ordering within NoValue, deterministic keys,
    exact totals, offset zero/end/beyond-end/maximum, and concurrent missing/null/value transitions
  - Generated-client and transport adversaries for tenant isolation, secret-field independence,
    revocation, readiness, divergence, retired epochs, saturation, malformed responses,
    mixed-version peers, output budgets, and every closed family member without caller-supplied
    field, direction, placement, order tuple, or raw query structure
  - Static and runtime boundary checks forbidding client filtering/sorting/counting, cursor/page
    walks, provider selection, storage access, sentinel values, per-row descriptor/epoch/layout
    proof construction, framework names, and any translation of typed refusal into false empty data
  - Handbook guidance for declaring nullable order placement in bounded named `.riffql` documents,
    regenerating typed SDK methods, understanding state/index amplification and readiness, and using
    cursor pagination when cross-request snapshot stability is required
  - One reproducible immutable development publication and successor generic capability receipt,
    explicitly recording that real framework route evidence remains owned and release-gating in the
    external repository and is not proved by RiffDB's neutral corpus
  acceptance_commands:
  - ./scripts/adapter-operational-query-acceptance --all-languages
  - ./scripts/driver-development-registry-acceptance --all-ecosystems
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/check-application-bindings
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public RiffDB surface returns the same exact authorized nullable-ordered page and
    total without caller structure or client shaping, the immutable publication is reproducible,
    all old artifacts remain compatible, and the repository contains no external framework schema,
    profile, route, adapter, runtime, or false external-acceptance claim.
  standing_design_tests:
    interface_safety: Applications and adapters receive only finite generated methods or closed
      member choices with compiler-fixed nullable order; they cannot select field, direction,
      placement, provider, policy, cost, fallback, or freshness, and no transport exposes a generic
      unsafe sort object.
    scale: Family choices, inputs, outputs, request/response bytes, provider work, page/offset,
      generated decode, buffers, waits, retries, diagnostics, and publication artifacts are bounded;
      no client or server surface retains or materializes the matching population.
  closure:
    status: complete
    completed_at: "2026-08-24"
    requirements_satisfied: [OQ-001, OQ-003, OQ-004, OQ-005, OQ-006, OQ-015,
      OQ-016, OQ-019, OQ-020, OQ-021, OQ-022, OQ-026, OQ-027, OQ-028, OQ-029,
      OQ-030, API-001, MCP-001, MCP-020, MCP-021, MCP-023, MCP-025, MCP-026,
      MCP-040, MCP-043, MCP-045, MCP-049]
    evidence: The shared named-query service now dispatches V4 present-only and V5 nullable exact
      families through one authorization, provider, epoch-proof, hydration, reauthorization, and
      response path. A neutral InventoryRecord contract creates historical missing fields,
      explicit nulls, and optional text/timestamp values; generated concurrent transitions and
      four compiler-fixed order members prove both placements and directions, NoValue later-term
      ties, exact totals, and zero/end/beyond/maximum direct offsets. One installed TLS deployment
      passed Rust, Go, TypeScript, Python, CLI, and MCP schema conformance. Static guards reject
      client filtering/sorting, storage access, and caller-supplied provider or order structure.
      The immutable V1 capability receipt remains byte-exact and successor V2 pins every nullable
      plan and generated artifact while explicitly leaving real framework routes to the external
      adapter repository. All acceptance commands pass; the registry command used the retained
      immutable Linux distribution because this host cannot itself emit a manylinux_2_28 wheel.
    known_limitations: Numeric offsets intentionally provide bounded same-epoch ordinal selection,
      not cross-request snapshot continuity; callers needing that property use generated cursors.
      Provider rebuilds remain bounded full-partition derived work. No external framework route or
      adapter acceptance is claimed by the neutral corpus.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-682
  name: One driver protocol core for every binding
  depends_on: [WP-604, WP-611]
  objective: Remove the duplicated request-validation, value-marshalling, and error-mapping surface
    between riffdb-driver-host and riffdb-client-python-native by giving the host one library
    protocol core that every binding consumes, proving both transports agree before anything moves,
    and preserving the self-contained Python wheel. No public surface, wire format, durable byte,
    deployment artifact, or supported platform changes.
  requirements: [DRV-001, DRV-002, DRV-014, DRV-015]
  required_adrs: [ADR-0074, ADR-0106, ADR-0148]
  allowed_paths: [work_packages.yaml, SPEC.md, adr/**, docs/**,
    crates/riffdb-driver-host/**,
    crates/riffdb-client-python-native/**,
    clients/python/runtime/**,
    tests/drivers/**,
    fixtures/adapters/**]
  deliverables:
  - One conformance corpus, landed and green BEFORE any consolidation, driving the existing
    socket path and the existing in-process path from the same entries and asserting
    byte-identical request material, identical ApplicationValue graphs, and identical public
    error classes; every disagreement found is recorded as a fixed finding naming the
    driver-host rule as authoritative rather than silently adopted
  - A library protocol surface on riffdb-driver-host exposing its request and response types,
    validation, and dispatch with no transport of its own, consumed unchanged by the socket
    transport
  - riffdb-client-python-native rebuilt on that surface, retaining its PyO3 boundary and adding
    no unsafe code, with its parallel parse_value, parse_command, and parse_query paths deleted
    rather than left dormant
  - Architecture tests proving the Python crate defines no value parser, bound, or error mapping
    of its own, and that no binding crate reaches riffdb-client-rust except through the shared
    core
  - A wheel installation test proving the Python artifact still runs with no daemon present, and
    an unchanged-daemon test proving an existing TypeScript application runs against it
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-driver-host --all-features
  - cargo +1.97.0 test -p riffdb-client-python-native --all-features
  - cargo +1.97.0 clippy --release -p riffdb-driver-host -p riffdb-client-python-native
    --all-features --all-targets
  - ./scripts/adapter-conformance
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: One protocol core owns every value rule, bound, and error class; both transports
    reproduce the complete conformance corpus identically; the Python wheel installs and runs with
    no daemon; and no public surface, wire format, durable byte, or protocol version changed.
  standing_design_tests:
    interface_safety: An application still invokes only a compiled operation with typed input and
      an idempotency identity. It cannot select a binding, transport, validation mode, protocol
      version, or fallback, and every operation retains fresh authorization, transaction-current
      dependency checks, atomic mutation and outcome, and typed uncertainty.
    scale: The shared core keeps the existing bounded pool, in-flight, reorder, frame, value, and
      retained-byte ceilings. No per-application cache, per-operation label, or unbounded retained
      state is added, and the in-process transport reuses the socket transport's admission bounds.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [DRV-001, DRV-002, DRV-014, DRV-015]
    evidence: riffdb-driver-host now owns one transport-neutral protocol core for canonical value
      admission, request construction, response conversion, query and command dispatch, and closed
      public error classification. The socket host and self-contained PyO3 wheel consume that core;
      architecture tests prohibit duplicate Python parsers, bounds, error maps, and direct
      riffdb-client-rust dependencies. A shared corpus proves byte-identical framed requests,
      identical ApplicationValue graphs, and identical public error classes for accepted and
      rejected values. The Python wheel runs its complete runtime suite without a daemon, and the
      unchanged Rust, Go, TypeScript, Python, CLI, and MCP adapter corpus remains green.
    known_limitations: This consolidation intentionally preserves every public binding surface and
      transport shape; it makes no performance claim and adds no new protocol version. Existing
      generated Python operation identity remains carried by the in-process typed request adapter
      while canonical value admission and execution dispatch are owned by the shared core.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-685
  name: Ordinary binary text-key total ordering
  depends_on: [WP-564, WP-565]
  objective: Correct ordinary bounded operational planning so one declared binary_utf8_v1 text-key
    index component may provide its canonical bytewise ascending or descending total order and
    opaque cursor traversal without requiring a prefix predicate on the same field, while retaining
    complete partition routing, index order, tie-breaker, cost, policy, and compatibility proofs.
  requirements: [OQ-001, OQ-002, OQ-004, OQ-005, OQ-006, OQ-007, OQ-008,
    OQ-009, OQ-015]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-contract-ir/**,
    crates/riffdb-query-ir/**, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**,
    crates/riffdb-testkit/**, fixtures/riffql/**, tests/operational_queries.rs,
    scripts/**, docs/SUMMARY.md, docs/riffql/**,
    docs/getting-started/OPERATIONAL-QUERIES.md]
  deliverables:
  - Planner eligibility recognizes a TextKey(binary_utf8_v1) index component as fully consumed by
    the matching compiler-declared order term when it is not consumed by a prefix predicate; every
    other encoded component still requires its existing predicate or nullable-order proof
  - Ascending and descending compiler regressions prove a partition-routed index with complete
    entity-key tie-breaker is selected without a text predicate, while an ordinary canonical string
    component retains its distinct length-first order and incomplete order, mixed direction,
    residual predicate, and unbounded shapes remain rejected
  - An independent operational execution corpus freezes bytewise order where `doc-3` precedes
    `doc6` although canonical length-first string order differs, and proves forward and reverse
    two-page opaque-cursor traversal has no duplicate, omission, scan fallback, or order drift
  - Public RiffQL planning and operational-query guidance documents that ordinary canonical string
    components retain canonical length-first order and bytewise text order requires the declared
    binary_utf8_v1 text-key profile; no external framework artifact or terminology enters RiffDB
  - Existing source, query IR, module, cursor, storage, durable, wire, provider-state, generated
    client, and compatibility identities remain byte-exact; newly accepted sources use the existing
    identities and ordinary deployment rotation
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-compiler -p riffdb-query-executor --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test operational_queries --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every admitted ordinary bytewise text order is a complete partition-routed declared
    index traversal with deterministic tie-breaker and exact forward/reverse cursor behavior;
    length-first canonical indexes remain distinct, all unsupported shapes fail compilation, old
    artifacts remain byte-exact, and no scan, client shaping, framework branch, or new identity lands.
  standing_design_tests:
    interface_safety: Applications still invoke only finite named operations with typed values and
      opaque cursors; they cannot select encoding, collation, index, field, direction, predicate,
      policy, cost, storage path, or fallback at request time.
    scale: The compiler performs bounded index-shape comparison, runtime traverses only the declared
      bounded page plus its continuation probe, and cursor, output, scan, memory, and diagnostic
      ceilings remain unchanged with no population-sized materialization or page walk.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-001, OQ-002, OQ-004, OQ-005, OQ-006, OQ-007,
      OQ-008, OQ-009, OQ-015]
    evidence: The ordinary planner now accepts a declared binary_utf8_v1 text-key component when
      the matching field is consumed by the complete compiler-declared order rather than a prefix
      predicate. Compiler tests retain partition, full-suffix, direction, tie-breaker, and cursor
      checks. The independent execution corpus traverses six encoded rows in one-row forward and
      reverse pages and freezes doc-3 before doc6 without duplicate or omission. Compiler,
      executor, memory, redb, recovery, topology, generated, coverage, policy, handbook, formatting,
      and workspace Clippy gates pass; old compatibility identities and artifacts are unchanged.
    known_limitations: Only the already available binary_utf8_v1 profile is executable;
      unicode_fold_v1 remains unavailable. Ordinary cursor pages retain snapshot-bound continuation
      semantics and do not provide numeric-offset stability across fresh requests.
    follow_ups: []
  human_review_triggers: *id001
- id: WP-686
  name: Exact binary text-key equality prefixes
  depends_on: [WP-564, WP-565, WP-685]
  objective: Correct ordinary bounded operational planning and execution so one declared
    binary_utf8_v1 text-key component may be consumed either by exact equality or by the complete
    compiler-declared order suffix, allowing progressively narrower named queries to share one
    maintained index without weakening partition, order, cursor, cost, or policy proofs.
  requirements: [OQ-001, OQ-002, OQ-004, OQ-005, OQ-006, OQ-007, OQ-008,
    OQ-009, OQ-015]
  required_adrs: [ADR-0038, ADR-0051, ADR-0053, ADR-0055, ADR-0108,
    ADR-0111, ADR-0124]
  allowed_paths: [SPEC.md, work_packages.yaml, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-testkit/**,
    fixtures/riffql/**, tests/operational_queries.rs, scripts/**, docs/SUMMARY.md, docs/riffql/**,
    docs/getting-started/OPERATIONAL-QUERIES.md]
  deliverables:
  - Planner eligibility advances an exact equality prefix across a
    TextKey(binary_utf8_v1) component and recognizes the same component as an ordered suffix in a
    wider query; prefix, order, partition, deterministic tie-breaker, and residual-predicate
    checks remain fail-closed
  - Execution lowers a typed logical string equality to the selected text-key component's exact
    physical binary_utf8_v1 bytes once while forming the bounded index prefix; row predicate
    evaluation continues to compare the original typed field value
  - Compiler and independent execution regressions prove wider and narrower cursor-paged query
    shapes select the same index and preserve bytewise order for length-divergent values without
    duplicate, omission, scan fallback, or duplicate index maintenance
  - Public planning and operational-query guidance explains that one declared text-key component
    supports exact equality, leading-byte prefix, and bytewise order in compatible compiled query
    shapes; no external adapter or framework surface enters RiffDB
  - Existing grammar, source, query IR, module, cursor, storage, durable, wire, provider-state,
    generated-client, and compatibility identities remain byte-exact; newly accepted sources use
    the existing identities and ordinary deployment rotation
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-query-compiler -p riffdb-query-executor --all-features
  - cargo +1.97.0 test -p riffdb-testkit --test operational_queries --all-features
  - cargo +1.97.0 test -p riffdb-storage-memory -p riffdb-storage-redb --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-workspace-policy
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One binary_utf8_v1 index proves both wider ordered and narrower exact-equality-prefix
    query shapes with exact cursor traversal and no duplicate index, scan, client shaping, new
    identity, framework branch, or weakened bound.
  standing_design_tests:
    interface_safety: Applications still invoke only finite named operations with typed values and
      opaque cursors; they cannot select encoding, collation, index, field, direction, predicate,
      policy, cost, storage path, or fallback at request time.
    scale: Equality narrows the existing ordered index prefix and runtime traverses only the
      declared bounded page plus its continuation probe. Index maintenance remains one delta per
      declared index, with no page walk, population materialization, or extra provider work.
  closure:
    status: complete
    completed_at: "2026-08-25"
    requirements_satisfied: [OQ-001, OQ-002, OQ-004, OQ-005, OQ-006, OQ-007,
      OQ-008, OQ-009, OQ-015]
    evidence: The ordinary planner now advances exact equality across a binary_utf8_v1 text-key
      component while rejecting range and other canonical predicates that the physical profile
      cannot prove. Runtime lowers the typed string to the existing ordered-byte component only
      when constructing the bounded index prefix. Compiler and independent execution regressions
      prove wider relation/user ordering and narrower relation equality plus user ordering select
      the same index, traverse one-row cursor pages without duplicate or omission, and preserve
      bytewise rel-3-before-rel6 and doc-3-before-doc6 order. Compiler, executor, memory, redb,
      recovery, topology, generated, coverage, policy, handbook, formatting, and workspace Clippy
      gates pass; old compatibility identities and artifacts are unchanged.
    known_limitations: Exact equality-prefix consumption is limited to the executable
      binary_utf8_v1 profile. unicode_fold_v1 remains unavailable, and bounded in or canonical
      range predicates do not acquire binary text-key prefix semantics.
    follow_ups: []
  human_review_triggers: *id001

- id: WP-746
  name: Replication stream, capability, follower applier, and gap-free resume
  tier: guarantee
  depends_on: [WP-772]
  objective: Activate the complete ADR-0186 V3 substrate in production behind one
    capability-guarded RPC. Add riffdbd follower mode whose applier is the sole writer, bootstrap
    at one exact V3 fence, and prove a byte-faithful, gap-free, resumable prefix across every
    authoritative namespace without reopening the durable format design.
  requirements: [REP-001, REP-002, REP-003, PERF-007]
  required_adrs: [ADR-0019, ADR-0061, ADR-0072, ADR-0082, ADR-0083, ADR-0093, ADR-0100,
    ADR-0101, ADR-0104, ADR-0124, ADR-0178, ADR-0186]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0178-follower-activation-and-changelog-derived-backup.md,
    adr/0186-complete-authoritative-changelog-v3.md, adr/README.md,
    proto/**, crates/riffdb-proto/**, crates/riffdb-types/**, crates/riffdb-auth/**,
    crates/riffdb-policy/**, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**,
    crates/riffdb-server/**, crates/riffdb-config/**, crates/riffdb-cli/**,
    crates/riffdb-testkit/**, crates/riffdb-sim/**, release/version-topology-v1.json,
    fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - One `ReplicateChangelog` capability permission kind that no application role or bootstrap
    convenience capability can carry, with create/revoke audit and generated schema updates
  - One server-streaming `ReplicationService.StreamChangelog` RPC whose handshake binds database
    id, history incarnation, leadership epoch, V3 catalog identity, and resume position, refusing
    stale epoch, foreign incarnation, unsupported catalog or format, and pruned history with typed
    outcomes
  - Production changelog publication port replacing `NoChangelogPublicationPort`, with the emitter
    reading only the WP-772 receipt cursor from `PublishedDurableSnapshot`, coalescing bounded
    notifications, resuming from V3 history, and never holding the exclusive write gate
  - riffdbd --mode follower with startup validation unchanged, one applier as sole writer, one
    V3 frame per storage transaction in sequence order, lineage-shared and node-local retained
    metadata kept distinct, and the typed `RDB-REP` follower-mode refusal on every write surface
  - Paged staged bootstrap at one durable V3 fence with an unprunable hold through exact tail
    attachment, complete validation before publication, and crash-safe bounded resume
  - V3 follower apply over every WP-772 replicated-authoritative namespace in redb, with the
    `riffdb-testkit` authoritative model as the independent oracle and required rebuildable state
    rebuilt and validated before ready
  - Tests `follower_applies_exact_prefix_byte_faithfully` and
    `replication_stream_resumes_gap_free_after_repeated_kills` under
    `tests/replication_follower.rs`, obligation proof
    `bootstrap_to_tail_fence_is_gap_free_across_crash`, plus crash-matrix arms for bootstrap,
    stream kill, and applier crash
  acceptance_commands:
  - ./scripts/acceptance --wp WP-746
  - ./scripts/ci-all
  exit_gate: A follower driven by the app-baseline workload holds a byte-faithful V3 prefix of
    every replicated-authoritative namespace at the compared position, bootstraps and resumes from
    its acknowledged position after repeated mid-flight kills with no gap or duplicate apply,
    passes complete startup validation with required rebuildable state afterward, and refuses every
    write with the typed follower-mode outcome.
  standing_design_tests:
    interface_safety: No application surface changes; the replication permission is
      administrative only, follower writes are unexpressible, and the stream carries only bytes
      the primary already made durable.
    scale: Bootstrap is paged and tail work follows bounded changed bytes retained in V3 history;
      the emitter reads pinned published snapshots, so no writer-private memory or recurring
      full-state rewrite is assumed.
  human_review_triggers: &id0178
  - Acknowledgement semantics, the commit hot path, or the single-writer property on either node
    would change.
  - Automatic election, cascading followers, multi-primary, or a quorum tier would be introduced.
  - A new provider family, result-set algebra, or performance package would be admitted to the
    alpha gate before WP-750 closes.
  - A durable record encoding, the backup manifest V1, changelog frame bytes, or a retention
    tombstone byte would change.

- id: WP-747
  name: Follower reads, typed follower-mode refusal, and sequence-lag health
  tier: guarantee
  depends_on: [WP-746]
  objective: Serve compiled, projected, and discovery reads on a follower under the existing
    Causal, Bounded, and Available policies against the applied frontier, refuse foreign or
    pre-promotion tokens fail-closed, and export replication lag in sequences as typed health
    and statistics on both nodes.
  requirements: [REP-004, REC-002]
  required_adrs: [ADR-0070, ADR-0072, ADR-0086, ADR-0093, ADR-0164, ADR-0178, ADR-0186]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0178-follower-activation-and-changelog-derived-backup.md,
    crates/riffdb-types/**, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-service/**, crates/riffdb-observability/**, crates/riffdb-api-grpc/**,
    crates/riffdb-api-mcp/**, crates/riffdb-server/**, crates/riffdb-client-rust/**,
    crates/riffdb-cli/**, crates/riffdb-testkit/**, proto/**, crates/riffdb-proto/**,
    fixtures/**, tests/**, docs/**]
  deliverables:
  - Follower read path using the applied frontier in the local-frontier role, with Causal waits
    inside the existing bounded register-before-read discipline and AdmissionHead reads fenced at
    the applied head
  - Fail-closed refusal of tokens, cursors, and fences from another lineage or a pre-promotion
    incarnation, mapped to the existing `RDB-HISTORY-0101` class
  - One `replication` health component and additive statistics fields reporting applied and
    acknowledged frontiers and lag in sequences, present on primary and follower, never in
    wall-clock units
  - Handbook pages for follower deployment, read semantics, and health, plus CLI and generated
    client exposure of the typed follower-mode refusal
  - Test `follower_causal_read_waits_for_token_then_matches_primary` under
    `tests/replication_follower.rs`
  acceptance_commands:
  - ./scripts/acceptance --wp WP-747
  - ./scripts/ci-all
  exit_gate: A Causal read on the follower with a fresh primary token waits, then returns bytes
    equal to the primary at the same frontier; a foreign-lineage token is refused; every write
    surface returns the typed follower-mode outcome; and health on both nodes reports lag in
    sequences.
  standing_design_tests:
    interface_safety: Applications keep the existing freshness vocabulary and cannot weaken it,
      select a node, or observe state beyond the applied frontier.
    scale: The read path assumes shared contract vocabulary and incarnation-bound tokens, not
      co-located storage or a shared clock.
  human_review_triggers: *id0178

- id: WP-748
  name: Promotion drill, leadership epoch, and follower retention fence
  tier: guarantee
  depends_on: [WP-747]
  objective: Implement ADR-0093 sections 4 through 6 as contracted by ADR-0178. Add the audited
    promotion operation with old-primary fencing, incarnation mint, and leadership epoch, and
    join the registered follower's acknowledged frontier to the retention fencing set with a
    hold budget and ceremony-only release.
  requirements: [REP-005, REP-006]
  required_adrs: [ADR-0072, ADR-0085, ADR-0093, ADR-0178, ADR-0186]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0178-follower-activation-and-changelog-derived-backup.md,
    proto/**, crates/riffdb-proto/**, crates/riffdb-types/**, crates/riffdb-auth/**,
    crates/riffdb-policy/**, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-commit/**, crates/riffdb-service/**,
    crates/riffdb-api-grpc/**, crates/riffdb-server/**, crates/riffdb-cli/**,
    crates/riffdb-testkit/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Audited follower register, retire, and promote administrative operations under administrative
    capabilities, exposed through gRPC, the Rust client, and CLI only
  - Fencing of the old primary by stop or replication-lease revocation, with a typed admission
    refusal on the fenced primary and a promotion receipt recording applied sequence, new
    incarnation, new leadership epoch, and RPO as the exact sequence delta
  - Incarnation mint through the ADR-0072 stamp path so every old-lineage frontier, token, cursor,
    and stream fails closed under the existing contract
  - Additive `FollowerLowWater` fencing input to the ADR-0085 retention collector with a
    per-registration hold budget in sequences, health degradation on exhaustion, and release only
    by audited retire or configured expiry
  - Tests `promotion_mints_incarnation_and_refuses_old_lineage_tokens` and
    `retention_prune_refuses_to_pass_registered_follower_frontier` under
    `tests/replication_promotion.rs`
  acceptance_commands:
  - ./scripts/acceptance --wp WP-748
  - ./scripts/ci-all
  exit_gate: Under load, fencing the primary and promoting the follower yields a new incarnation
    and epoch, a pre-promotion token is refused fail-closed, a post-promotion commit and read
    round-trip succeeds, the receipt's RPO equals the measured sequence delta, and offline prune
    refuses to pass a lagging registered follower until it catches up or is retired.
  standing_design_tests:
    interface_safety: Promotion, registration, and retirement are administrative, audited, and
      capability-gated; no application can trigger or observe them except through fail-closed
      incarnation refusals it already handles.
    scale: The fence is one sequence number per registration and the hold budget bounds how long a
      dead follower pins history; no node-local memory of another node is assumed.
  human_review_triggers: *id0178

- id: WP-749
  name: Changelog-derived incremental and remote backup
  tier: guarantee
  depends_on: [WP-746]
  objective: Implement ADR-0178 section 7. Persist validated changelog frames to a configured
    archive sink beside periodic offline full backups, publish an additive archive manifest, and
    restore from a verified full backup plus the archived suffix through the follower applier
    path with sequence-granular stopping and fail-closed gap and identity checks.
  requirements: [REP-007, AFC-007]
  required_adrs: [ADR-0050, ADR-0072, ADR-0093, ADR-0100, ADR-0112, ADR-0124, ADR-0178,
    ADR-0186]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0178-follower-activation-and-changelog-derived-backup.md,
    crates/riffdb-types/**, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-service/**, crates/riffdb-api-grpc/**, crates/riffdb-server/**,
    crates/riffdb-config/**, crates/riffdb-cli/**, crates/riffdb-testkit/**,
    proto/**, crates/riffdb-proto/**, release/version-topology-v1.json,
    release/durable-format-manifest-v1.json, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - First-party archive consumer persisting every validated V3 frame with checksum and sequence range
    to a filesystem or object-store sink, with bounded buffering, typed resync on overflow, and no
    effect on acknowledgement or the write gate
  - Additive external `archive-manifest/v1` binding database id, history incarnation, covered
    sequence range, frame digests, encryption posture, and the full-backup manifest digest, with
    backup manifest V1 unchanged and the new format registered in the version topology
  - Offline `riffdb storage restore --archive` replaying the archived suffix through the follower
    applier after a verified full restore, stopping at the last archived or an operator-supplied
    earlier sequence and failing closed on any gap, checksum, or incarnation mismatch
  - Handbook backup/restore pages stating sequence granularity and the absence of a wall-clock
    point-in-time promise
  - Test `archived_suffix_restore_recovers_to_last_archived_sequence` under
    `tests/replication_archive_restore.rs` plus a crash-matrix arm for archive-sink failure
  acceptance_commands:
  - ./scripts/acceptance --wp WP-749
  - ./scripts/ci-all
  exit_gate: A database restored from a verified full backup plus its archived suffix equals the
    primary through the last archived sequence, a truncated or reordered archive is refused before
    any state changes, and the archive consumer's presence leaves acknowledgement latency and the
    write gate unchanged.
  standing_design_tests:
    interface_safety: Archive configuration is operator-only; applications cannot select, read,
      or influence archive sinks, and restore remains the offline destructive ceremony of ADR-0050.
    scale: Archives are append-only frame sequences; restore streams the suffix and never requires
      the whole history in memory.
  human_review_triggers: *id0178

- id: WP-750
  name: Replication acceptance campaign and gate widening
  tier: guarantee
  depends_on: [WP-748, WP-749]
  objective: Run ADR-0093's six acceptance criteria as one receipted campaign against the
    app-baseline harness with a repeated kill loop on primary, follower, stream, and archive
    sink; prove no hot-path regression; publish release evidence; and record the provider gate
    plus one prerequisite and its evidence for a later, separately accepted performance-freeze
    lifting ADR. WP-750 closure alone changes no freeze state.
  requirements: [REP-008, REP-009, PERF-008, END-003]
  required_adrs: [ADR-0093, ADR-0142, ADR-0171, ADR-0178, ADR-0186, ADR-0199]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0178-follower-activation-and-changelog-derived-backup.md, adr/README.md,
    benchmarks/**, scripts/**, tests/**, crates/riffdb-testkit/**, crates/riffdb-sim/**,
    release/evidence/replication/**, release/RELEASE_NOTES.md, docs/**]
  deliverables:
  - scripts/replication-acceptance driving prefix exactness, gap-free resume, promotion drill,
    retention fence, follower reads, and no-hot-path-regression cells with a configurable kill
    loop and a versioned `riffdb.replication-acceptance/v1` receipt
  - Emitter-enabled PERF-008 interactive and write-only comparison cells on both PERF-018 profiles
    within the ADR-0171 stability rule, published as disclosure beside the receipt
  - Deterministic simulator follower cell under a seeded fault schedule covering apply, resume,
    and archive faults, retained as a seeded campaign
  - Release evidence under `release/evidence/replication/` bound to the exact source, lock, and
    binary identity, and release-notes and known-limitations updates retiring the single-node
    availability statement
  - While the ADR-0183 freeze is in force, only the exact pre-freeze definition, its existing
    non-candidate replication objective and no-hot-path-regression evidence, and a new closure
    are authorized; no new PERF requirement, performance candidate, threshold, admitted package,
    or direct freeze lift is part of WP-750
  - Test `replication_acceptance_campaign_meets_every_adr_0093_criterion` under
    `tests/replication_acceptance.rs` verifying the receipt schema and every criterion result
  acceptance_commands:
  - ./scripts/acceptance --wp WP-750
  - ./scripts/ci-all
  exit_gate: Every ADR-0093 acceptance criterion passes on both profiles under a repeated kill
    loop with receipted evidence, emitter-enabled throughput and p95 are within the ADR-0171
    stability rule of emitter-disabled runs, and the known-limitations page no longer states that
    replication and failover are absent.
  standing_design_tests:
    interface_safety: The campaign adds no public surface; it exercises administrative operations
      and existing application reads and writes only.
    scale: Evidence is collected per database on one primary and N followers and makes no claim
      about sharding or quorum durability.
  human_review_triggers: *id0178

- id: WP-751
  name: Operation registry and generated public-message validation
  depends_on: [WP-682, WP-744]
  objective: Add the riffdb-operation-registry leaf crate holding exactly one declaration per
    public operation, the deterministic scripts/generate-operation-adapters generator, and the
    generated bounded validation that replaces the hand-written validate_*_exchange entry points in
    riffdb-proto, after freezing every current public wire vector, MCP golden, and CLI envelope as
    fixtures the generated output reproduces byte for byte.
  requirements: [GEN-001, GEN-002, GEN-003]
  required_adrs: [ADR-0006, ADR-0007, ADR-0027, ADR-0028, ADR-0040, ADR-0118, ADR-0124,
    ADR-0179]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0179-generated-public-surface-adapters-and-operation-registry.md, adr/README.md,
    Cargo.toml, Cargo.lock, deny.toml, crates/riffdb-operation-registry/**,
    crates/riffdb-types/**, crates/riffdb-errors/**, crates/riffdb-proto/**,
    crates/riffdb-service/**, proto/**, fixtures/proto/**, fixtures/compatibility/**,
    fixtures/driver/**, tests/**, scripts/**, docs/**]
  deliverables:
  - riffdb-operation-registry crate depending only on riffdb-types, with one declaration per
    ServiceOperationV1 variant naming Protobuf messages, DTO paths, every bound, ingress kinds,
    permission kind, idempotency class, ADR-0118 redaction classes, audiences, and a closed field
    map; every_public_operation_has_exactly_one_registry_declaration
  - scripts/generate-operation-adapters reading the registry plus fixtures/proto/descriptors,
    failing on an unmapped, doubly mapped, or kind-incompatible field, wired into check-generated
  - Frozen fixture set for every current operation (public_client_vectors, wire_vectors,
    fixed-tool-conversion-goldens, riffdb.cli.output/v1 envelopes, generated MCP catalogs) taken
    before any replacement lands
  - Generated crates/riffdb-proto/src/generated/public_validation.rs replacing the 35 hand-written
    validate_*_exchange entry points with identical bounds and error classes;
    generated_adapters_reproduce_frozen_public_fixtures_byte_for_byte
  - Version-topology and compatibility record stating the change is internal with no domain advance
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-operation-registry -p riffdb-proto -p riffdb-service
    --all-features
  - ./scripts/generate-operation-adapters --check
  - ./scripts/check-file-size-guard
  - ./scripts/check-generated
  - ./scripts/check-version-topology
  - ./scripts/check-requirement-coverage
  - ./scripts/check-adr-obligations
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: Every public operation has one registry declaration, generation rejects any field the
    registry and descriptor set disagree on, the generated validation reproduces every frozen
    public fixture byte for byte with the same public error classes, and no Protobuf tag, MCP
    name, CLI envelope field, public error code, or durable byte changed.
  standing_design_tests:
    interface_safety: The registry can declare only operations already in the closed
      ServiceOperationV1 inventory with explicit bounds and redaction classes; it cannot express an
      unbounded field, a wildcard operation, or an opt-out from authorization or redaction.
    scale: Generation is an offline build-time step over a registry the size of the operation
      inventory; nothing assumes co-located storage, single-node memory, or a full-state rewrite.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [GEN-001, GEN-002, GEN-003]
    evidence: The riffdb-operation-registry leaf depends only on riffdb-types and declares all 57
      ServiceOperationV1 variants in exact tag order with descriptor identities, checked DTO
      paths, exact request and response byte bounds, ingress, permission, idempotency, redaction,
      audience, and closed root mappings. The one offline generator rejects missing, duplicate, or
      kind-incompatible mappings and exact-bound drift, reads the compiled descriptor set, and
      emits riffdb-proto's checked-in generated validation. The named architecture proof also
      scans riffdb-proto and finds no hand-written exchange validator outside src/generated. A
      SHA-256 manifest freezes 151 existing wire vectors, MCP schemas and goldens, generated MCP
      catalogs, and riffdb.cli.output/v1 fixtures; the generated validator preserves the frozen
      source byte for byte, including every error class. Version topology remains at 59 domains
      with no advance. This is an internal ownership change, so the public handbook is unaffected;
      WP-752 retains ownership of replacing the other five adapter layers.
  human_review_triggers: &id0179
  - A generated adapter would change any public Protobuf tag, MCP tool name, CLI envelope field,
    public error class, or durable byte.
  - The registry or a template would express an operation, bound, permission, or redaction class
    absent from the accepted service inventory, or would grant authority.
  - Generation would run inside riffdbd, riffdb, riffdb-mcp, or a client, or a template would
    receive application-controlled input.
  - A hand-written validation, conversion, or dispatch adapter would be reintroduced outside a
    generated directory.

- id: WP-752
  name: Generated gRPC, MCP, CLI, and Rust-client adapters
  depends_on: [WP-751]
  objective: Replace the hand-written riffdb-api-grpc conversion layer, the riffdb-api-mcp
    fixed-tool schema and dispatch, the riffdb-cli dispatch and output-envelope encoders, and the
    riffdb-client-rust typed method surface with generated modules from the registry, holding the
    hosted MCP context constructor, credential authenticator, and service safe points exactly where
    ADR-0007 and SPEC 5.3 place them, and shrinking the five files over 10,000 lines as a by-product.
  requirements: [GEN-002, GEN-003, GEN-007]
  required_adrs: [ADR-0007, ADR-0008, ADR-0020, ADR-0037, ADR-0040, ADR-0041, ADR-0064,
    ADR-0124, ADR-0179]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0179-generated-public-surface-adapters-and-operation-registry.md,
    crates/riffdb-operation-registry/**, crates/riffdb-proto/**, crates/riffdb-service/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**, crates/riffdb-cli/**,
    crates/riffdb-client-rust/**, crates/riffdb-mcp-stdio/**, crates/riffdb-driver-host/**,
    examples/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Generated crates/riffdb-api-grpc/src/generated/conversion.rs replacing the 187 hand-written
    conversion functions, with the frozen wire vectors reproduced byte for byte
  - Generated fixed-tool schema and dispatch in riffdb-api-mcp reproducing
    fixed-tool-registry-v1.json and fixed-tool-conversion-goldens-v1.json, leaving
    RequestContext::from_authenticated_mcp_http and the observer budget untouched
  - Generated riffdb.cli.output/v1 envelope shapes and dispatch table in riffdb-cli, and the
    generated typed method surface in riffdb-client-rust, each reproducing its frozen fixtures
  - Architecture test five_adapter_crates_contain_no_hand_written_conversion_or_dispatch
    forbidding hand-written conversion or dispatch outside the generated directories of the other
    five adapter crates
  - app.rs, dto.rs, public_message.rs, conversion.rs, and service_backend.rs each below 4,000
    lines with no standalone refactor; adapter_path_files_stay_under_the_decomposition_budget
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-api-grpc -p riffdb-api-mcp -p riffdb-cli -p riffdb-client-rust
    -p riffdb-mcp-stdio --all-features
  - cargo +1.97.0 test -p riffdb-api-mcp --test mcp_conformance --all-features
  - cargo +1.97.0 test -p riffdb-api-grpc --test grpc_end_to_end
  - ./scripts/generate-operation-adapters --check
  - ./scripts/check-file-size-guard
  - ./scripts/check-generated
  - ./scripts/check-application-bindings
  - ./scripts/check-requirement-coverage
  - ./scripts/check-adr-obligations
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: gRPC, MCP, CLI, and Rust-client adapters are generated from the registry and
    reproduce every frozen fixture byte for byte; the MCP and gRPC conformance suites pass
    unchanged; no hand-written adapter remains outside a generated directory; and every file on
    the adapter path is under the 4,000-line budget.
  standing_design_tests:
    interface_safety: Every generated adapter calls the same API-neutral service entry point and
      cannot add an operation, widen a bound, print a secret-classified output, or bypass the
      per-request authorization safe points.
    scale: Not applicable to storage; the generated adapters are the same per-request code paths
      with no new memory retention or state.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [GEN-002, GEN-003, GEN-007]
    evidence: The operation-adapter generator now owns the frozen service DTO, gRPC conversion,
      MCP fixed-tool service backend, CLI dispatch and envelope, and Rust-client typed-method
      layers under generated directories; their former owner files are one-to-three-line include
      wrappers. The named architecture proof forbids hand-written conversion or dispatch in all
      five wrappers, and a second proof reproduces each pre-generation source fixture byte for
      byte. The six named adapter-path owner files are below 4,000 lines. All 158 frozen public
      adapter artifacts remain hash-exact, and the unchanged gRPC and MCP conformance suites pass.
      RequestContext::from_authenticated_mcp_http, hosted credential authentication, service safe
      points, and the observer budget remain in their existing modules. This is an internal source
      ownership change with no public behavior, compatibility-domain, or handbook impact.
  human_review_triggers: *id0179

- id: WP-753
  name: Template-based language generators, shared drift corpus, and the three-places check
  tier: surface
  depends_on: [WP-752]
  objective: Move the Rust, Go, TypeScript, and Python application generators onto checked-in
    minijinja templates rendering one canonical generation model, extend the WP-682 driver corpus
    to every registry operation across all bindings, and add the CI check that proves adding one
    public operation hand-touches only the Protobuf source, the registry with the service
    implementation, and the handbook.
  requirements: [GEN-004, GEN-005, GEN-006]
  required_adrs: [ADR-0074, ADR-0106, ADR-0120, ADR-0124, ADR-0148, ADR-0179]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0179-generated-public-surface-adapters-and-operation-registry.md, Cargo.toml,
    Cargo.lock, deny.toml, crates/riffdb-operation-registry/**, crates/riffdb-query-module/**,
    crates/riffdb-driver-host/**, crates/riffdb-client-rust/**,
    crates/riffdb-client-python-native/**, templates/**, clients/**, examples/**,
    fixtures/driver/**, fixtures/query-modules/**, fixtures/adapters/**, tests/**, scripts/**,
    docs/**, .github/workflows/**]
  deliverables:
  - minijinja templates under templates/generators/{rust,go,typescript,python}/ rendered with
    strict undefined variables, autoescape off, and ordered inputs, replacing the write!-assembled
    generators in generation.rs, go_generation.rs, and python_generation.rs
  - One canonical JSON generation model per module frozen as a golden fixture, with every language
    output reproduced from it; template_generators_reproduce_golden_outputs_across_languages
  - fixtures/driver/corpus-v2.json covering every registry operation and bound for the Rust
    client, driver-host socket, Python in-process, Go, and TypeScript bindings, run by
    scripts/driver-conformance; every_binding_returns_the_same_error_class_for_the_shared_corpus
  - scripts/check-three-places --base <rev> and its CI job, failing when a diff that changes a
    .proto file or the registry touches any adapter-crate file outside src/generated/
  - Handbook and compatibility pages describing the three hand-written places and the template
    layout; downstream adapter repositories recompiled with byte-identical generated packages
  acceptance_commands:
  - ./scripts/acceptance --wp WP-753
  - ./scripts/ci-all
  exit_gate: All four language generators render from one golden model through checked-in
    templates and reproduce their golden outputs; every binding returns the identical closed error
    class for every corpus-v2 entry; a rehearsal diff that adds one operation passes
    check-three-places while a diff that hand-edits an adapter fails it; and the generated
    application packages shipped to clients/ and examples/ are byte-identical to before.
  standing_design_tests:
    interface_safety: Templates are data rendered offline in CI from a model the compiler owns;
      no application input reaches a template, and generated facades still submit only typed
      values through the one driver protocol core.
    scale: Not applicable; generation cost is linear in module size and runs outside any daemon
      or client process.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [GEN-004, GEN-005, GEN-006]
    evidence: Rust, Go, TypeScript, and Python now render checked-in minijinja templates from one
      ordered canonical JSON model with strict undefined variables and autoescape disabled; the
      named architecture and golden proofs reproduce every language output and the shipped client
      packages byte for byte. corpus-v2 covers all 57 registry operations and both request bounds,
      and the named conformance proof executes Rust-client, driver-host socket, Python, Go, and
      TypeScript cells and observes one identical closed error class per row. The three-places
      guard and its self-test are wired into CI, downstream adapters compile unchanged, the
      handbook documents the ownership rule, package acceptance passes, and ci-all passes.
  human_review_triggers: *id0179

- id: WP-754
  name: Executor-over-storage inversion and startup migration-driver relocation
  tier: guarantee
  depends_on: [WP-743, WP-745]
  objective: Move composite-query execution and row-policy admission out of both concrete storage
    backends into riffdb-query-executor, running over the storage-api readers plus one owned-snapshot
    handle, and move the index-migration page/apply/finish loop out of storage-redb startup.rs behind
    the ADR-0042 catalog-owned driver, with byte-exact page, cursor, count, and refusal parity proved
    by recorded fixtures.
  requirements: [DEP-001, DEP-002]
  required_adrs: [ADR-0004, ADR-0035, ADR-0038, ADR-0042, ADR-0053, ADR-0111, ADR-0164,
    ADR-0175, ADR-0180]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0180-crate-dependency-direction-repair.md,
    adr/README.md, Cargo.lock, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-storage-memory/**, crates/riffdb-query-executor/**, crates/riffdb-query-ir/**,
    crates/riffdb-catalog/**, crates/riffdb-policy/**, crates/riffdb-projection/**,
    crates/riffdb-service/**, crates/riffdb-server/**, crates/riffdb-testkit/**, fixtures/**,
    tests/**, scripts/**, docs/**]
  deliverables:
  - Recorded pre-inversion fixtures for every named-query, operational, provider, partition-set,
    and admission-head shape (pages, cursors, counts, aggregates, typed refusals) checked in under
    fixtures/ before any execution code moves
  - One storage-api owned-snapshot handle pinning application head, index epochs, and provider
    generations for one page and its continuation, exposed through the existing point, scan,
    filtered-scan, snapshot, and fence readers with no policy, program, or executor type in its
    signature
  - QueryExecutionPort implemented once in riffdb-query-executor over that handle; the redb and
    in-memory QueryExecutionPort implementations, RedbQueryView, and the executor imports in
    query.rs, shared_ports.rs, consumer.rs, and application.rs removed; ADR-0111 admission applied
    by the executor before any row, count, measure, or cursor is released
  - Index-migration page, apply, and finish driving relocated behind the catalog-owned driver;
    storage-redb startup.rs reduced to structural and historical evidence with its branded page,
    point-read, and apply ports kept private
  - Manifest edges from riffdb-storage-redb and riffdb-storage-memory to riffdb-query-executor,
    riffdb-query-ir, riffdb-policy, and riffdb-projection removed; cargo machete clean
  - Proofs storage_backends_depend_only_on_storage_api_types_proto_and_catalog_driver,
    executor_owned_snapshot_pages_match_pre_inversion_fixtures, and
    redb_startup_produces_evidence_only_and_drives_no_migration
  acceptance_commands:
  - ./scripts/acceptance --wp WP-754
  - ./scripts/ci-all
  exit_gate: Every recorded query fixture replays byte-identically through the executor over the
    redb readers, neither backend manifest names an executor, query-IR, policy, or projection
    dependency, startup.rs issues no migration instruction, and the recovery matrix, simulation
    campaign, and migration gates pass unchanged.
  standing_design_tests:
    interface_safety: No public surface changes; the same compiler-sealed programs, bounds, cursors,
      and authorization safe points execute from a different crate, and no row leaves the executor
      before admission.
    scale: The owned-snapshot handle is single-node and is the named reversible constraint; an
      executor over narrow readers is the shape a remote reader needs, and no bound widens.
  closure:
    status: complete
    completed_at: "2026-09-04"
    requirements_satisfied: [DEP-001, DEP-002]
    evidence: Query execution and row-policy admission now live once in riffdb-query-executor over
      an API-neutral owned storage snapshot, while both storage backends retain only reader and
      final-safe-point implementations and no forbidden production dependency edges. Twenty-eight
      recorded pre-inversion cases replay byte-identically through redb and memory readers, event
      release revalidates exact capability, event, row, and relationship observations, and the
      retired storage-owned query census publishes no false executor samples. The catalog-owned
      migration driver now calls a private redb backend module, startup.rs produces only evidence,
      the three named architecture obligations pass, package acceptance passes 15 of 15 gates,
      and the full CI battery passes including recovery, simulation, migration, and multi-language
      operator acceptance.
  human_review_triggers: &id0180
  - A page, cursor, count, aggregate, or typed refusal would differ from its recorded fixture.
  - A policy context, executor type, or compiler plan would cross a storage-api trait.
  - A storage encoding, key, journal frame, or durable record would change.
  - A layer exception would be added to the crate-graph allow-list without an ADR number.

- id: WP-755
  name: Client, observability, and test-kit dependency splits
  tier: guarantee
  depends_on: [WP-754]
  objective: Make riffdb-client-rust depend only on riffdb-proto, the Tonic client, and the ADR-0037
    foundational crates, make riffdb-observability a leaf that defines the telemetry types its
    consumers implement, and split riffdb-testkit into a core kit without server, cli, or service
    edges plus riffdb-testkit-server that owns temporary servers and daemon-linked root tests.
  requirements: [DEP-003, DEP-004, DEP-005]
  required_adrs: [ADR-0037, ADR-0040, ADR-0041, ADR-0049, ADR-0180]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0180-crate-dependency-direction-repair.md,
    adr/README.md, Cargo.toml, Cargo.lock, crates/riffdb-proto/**, crates/riffdb-types/**,
    crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**, crates/riffdb-application/**,
    crates/riffdb-observability/**, crates/riffdb-service/**, crates/riffdb-commit/**,
    crates/riffdb-catalog/**, crates/riffdb-policy/**, crates/riffdb-conflict/**,
    crates/riffdb-auth/**, crates/riffdb-api-mcp/**, crates/riffdb-server/**, crates/riffdb-cli/**,
    crates/riffdb-mcp-stdio/**, crates/riffdb-driver-host/**, crates/riffdb-testkit/**,
    crates/riffdb-testkit-server/**, crates/riffdb-sim/**, crates/riffdb-storage-memory/**,
    crates/riffdb-storage-redb/**, crates/riffdb-query-module/**, tests/**, fixtures/**,
    scripts/**, docs/**, .config/nextest.toml, release/version-topology-v1.json,
    examples/agent-alpha/domains/rust/Cargo.lock]
  deliverables:
  - Generated Tonic service clients and DATABASE_METADATA_KEY moved into riffdb-proto behind a
    client-only feature; riffdb-api-grpc consumes the same generated messages for the server side;
    ApplicationPortabilityManifest moved to riffdb-types with its serialized form unchanged
  - riffdb-client-rust manifest reduced to riffdb-proto, tonic, tonic-prost, riffdb-types,
    riffdb-errors, riffdb-config, the exact ADR-0018 entropy edge, and the exact zeroize edge; the
    riffdb-api-grpc and riffdb-application edges removed; generated Rust, Go, TypeScript, and
    Python client packages unchanged at their public surface
  - riffdb-observability reduced to riffdb-types, riffdb-errors, and tracing; telemetry event
    enums, observer traits, health components, and metric names defined there and implemented or
    emitted by service, commit, catalog, policy, conflict, auth, and MCP; server composition
    unchanged in behavior
  - riffdb-testkit retaining model, histories, failpoints, scratch, and inspection with no service,
    server, or cli edge; new riffdb-testkit-server owning process harnesses, temporary servers, and
    the thirty root [[test]] targets that link the daemon; policy, commit, conflict, storage, and
    other leaf crates taking only the core kit as a dev-dependency
  - Existing client and observability architecture tests replaced by cargo-metadata-driven proofs
    client_depends_only_on_proto_tonic_types_errors_and_config and observability_is_a_leaf_crate
  acceptance_commands:
  - ./scripts/acceptance --wp WP-755
  - ./scripts/ci-all
  exit_gate: The client manifest names exactly the ADR-0180 section 4 allow-list, observability has
    no edge above riffdb-errors, riffdb-policy and riffdb-commit test builds no longer link
    riffdb-server, every generated client package and driver conformance corpus passes unchanged,
    and the gRPC wire, descriptor, and fixture inventories are byte-identical.
  standing_design_tests:
    interface_safety: The SDK gains no authority-bearing dependency and loses transitive access to
      compiler and server internals; generated packages keep their public surface.
    scale: Telemetry types at the bottom of the graph are the shape a multi-process deployment
      needs; nothing here assumes co-located storage or single-node memory.
  closure:
    status: complete
    completed_at: "2026-09-04"
    requirements_satisfied: [DEP-003, DEP-004, DEP-005]
    evidence: Generated Tonic clients and shared metadata now live behind riffdb-proto's client
      feature, portability and installation values live in riffdb-types without serialization
      drift, and riffdb-client-rust has no application or server-adapter edge. Observability owns
      the closed telemetry vocabulary with only types, errors, and tracing dependencies. The core
      test kit has no service, server, or CLI edge, while riffdb-testkit-server owns all thirty
      daemon-linked root targets. Cargo-metadata architecture proofs, all 2,295 scoped tests, all
      24 generators, the fifteen-gate package acceptance plan, and the full CI battery pass; the
      reviewed adapter fixture change only relocates its generated-client import owner and leaves
      public wire, descriptor, and generated client surfaces unchanged.
  human_review_triggers: *id0180

- id: WP-756
  name: Crate-graph enforcement, in-memory backend removal, storage-api trim, and build evidence
  tier: surface
  depends_on: [WP-755]
  objective: Add the cargo-metadata crate-graph check with the ADR-0180 layered allow-list and
    ADR-numbered exceptions, delete riffdb-storage-memory after moving its remaining consumers to a
    storage-api reader fake, the testkit model, and the redb migration stage, trim riffdb-storage-api
    to what redb, the reader fake, and the simulator consume, and record before/after rebuild and
    link-time evidence.
  requirements: [DEP-005, DEP-006]
  required_adrs: [ADR-0004, ADR-0113, ADR-0124, ADR-0180]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0180-crate-dependency-direction-repair.md,
    adr/README.md, Cargo.toml, Cargo.lock, crates/**, tests/**, fixtures/**, scripts/**, docs/**,
    release/version-topology-v1.json]
  deliverables:
  - scripts/check-crate-graph resolving the workspace with cargo metadata over every compile
    target, rejecting any production or build edge that points upward or sideways across the nine
    ADR-0180 layers, listing each exception with its ADR number, and failing on an exception
    without one; invoked from scripts/check-workspace-policy and ci-all with a self-test that
    injects one upward edge into a temporary manifest and expects rejection
  - riffdb-storage-memory removed from the workspace, SPEC 5.1/5.2, EXPECTED_PACKAGES, and every
    architecture-test ban list; tests/migrations gate B, gate C, model, and index-cover tests run
    against the redb migration stage; executor unit tests run against a small storage-api reader
    fake; the testkit authoritative model remains the independent oracle
  - riffdb-storage-api trimmed to the traits, records, bounds, and codecs consumed by redb, the
    reader fake, and riffdb-sim; removed traits and fixtures classified under ADR-0124 with no
    durable, wire, or IR identity change
  - Non-evidentiary receipt in docs/performance recording warm incremental rebuild after a
    one-line riffdb-storage-api change and cargo test --no-run link time for riffdb-policy,
    riffdb-commit, and riffdb-service before WP-754 and after this package on one host
  - Proof check-crate-graph
  acceptance_commands:
  - ./scripts/acceptance --wp WP-756
  - ./scripts/ci-all
  exit_gate: The crate-graph check passes on the full workspace and rejects its injected
    counterexample, the workspace has no riffdb-storage-memory member, every migration gate and
    simulation campaign passes against redb, storage-api carries no trait without a consumer, and
    the recorded rebuild and link-time receipt shows leaf-crate tests no longer linking the daemon.
  standing_design_tests:
    interface_safety: No application-facing surface changes; the check is a repository gate and
      grants no runtime capability.
    scale: Removing the second single-node backend removes an assumption rather than adding one;
      the layered allow-list places readers below executors, which a remote reader requires.
  closure:
    status: complete
    completed_at: "2026-09-04"
    requirements_satisfied: [DEP-005, DEP-006]
    evidence: The cargo-metadata graph gate passes the full workspace and rejects both an injected
      upward edge and an exception without an ADR. The in-memory backend is removed; migration
      gates use the redb stage, executor tests use a small storage-api reader fake, and the testkit
      model remains the independent oracle. The storage-api consumer inventory found no orphaned
      trait, so no format-bearing API was removed and ADR-0124 records no durable, wire, or IR
      identity change. The non-evidentiary receipt records a 2.4 percent warm-incremental change
      and 91.8, 46.7, and 23.3 percent reductions in policy, commit, and service leaf-test link
      times. Migration, simulator, generated-artifact, handbook, adapter, governance, and package
      acceptance checks pass.
  human_review_triggers: *id0180

- id: WP-757
  name: Alpha format epoch 2 reset and one-identity-per-domain topology
  tier: guarantee
  depends_on: [WP-612, WP-711]
  objective: Declare alpha format epoch 2 through the ADR-0112 ceremony, delete every superseded
    decoder, fixture, generated artifact, and topology row while the pre-external window is open
    except the exact ADR-0207 authoritative-changelog V1/V2 read-only compatibility evidence, and
    make scripts/check-version-topology refuse a second readable identity for every other domain
    until the first external database is registered.
  requirements: [GOV-001, GOV-002, GOV-003, AFC-005, AFC-006, PRJ-007, VER-005]
  required_adrs: [ADR-0006, ADR-0112, ADR-0124, ADR-0181, ADR-0190, ADR-0192, ADR-0204,
    ADR-0207, ADR-0215, ADR-0217, ADR-0218]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0181-pre-alpha-format-epoch-reset-and-governance-load.md,
    adr/0192-compiler-bound-columnar-publication-control.md,
    adr/0215-epoch-one-columnar-control-obligation-retirement.md,
    adr/0217-snapshot-bound-export-of-command-owned-evidence.md,
    adr/0218-retired-catalog-index-migration-scaffolding-authority.md, adr/README.md,
    release/version-topology-v1.json, release/durable-format-manifest-v1.json,
    release/RELEASE_NOTES.md, release/evidence/**, proto/**, crates/**, clients/**,
    templates/**, examples/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Epoch-2 writer declaration in the durable-format manifest, format marker, and release notes,
    with RDB-FORMAT-0101 refusal of every epoch-1 database and the ceremony named as the sole
    safe action
  - Removal of every non-current reader, fixture, generated artifact, and topology identity across
    durable, executable-IR, wire, protocol, application-artifact, generated-surface, and evidence
    domains except ADR-0207's exact read-only ChangelogFrameV1/V2 decoders and compatibility
    evidence for `durable.authoritative_changelog`, with deleted Protobuf field numbers and enum
    values retained as reserved
  - Mandatory removal of the ADR-0192 tag-65 migration bridge, including source, decoder, table,
    generated message, fixture, durable-manifest row, and topology exception, proved by
    epoch_two_removes_legacy_vector_control_bridge
  - external_databases array in the version topology and open-window checks requiring every domain
    except `durable.authoritative_changelog` to have one equal readable, writable, and current
    active identity; the hard-coded exception has ordered readable identities exactly
    `riffdb.changelog-frame/v1`, `riffdb.changelog-frame/v2`, and
    `riffdb.changelog-frame/v3`, writable and current identities each exactly V3,
    `single_current` writer policy, V1/V2 exactly read_only, V3 solely active, empty candidate and
    retired sets, and exact Rust-type, numeric-version, frame-magic, and footer-magic bindings
    ChangelogFrameV1/1/RDBCLF01/RDBCLE01, ChangelogFrameV2/2/RDBCLF02/RDBCLE02, and
    ADR-0186 V3/3/RDBCLF03/RDBCLE03; the exception cannot be configured or expanded and ordinary
    ADR-0124 behavior resumes once the array is nonempty, proved by
    topology_allows_only_exact_changelog_compatibility_exception and
    topology_refuses_second_readable_identity_before_external_database
  - One real ceremony over a seeded TicketDesk epoch-1 database with export receipt, reimport
    reconciliation, and full epoch-2 startup validation, proved by
    epoch_two_ceremony_refuses_in_place_open_and_reconciles_reimport
  - Inventory proof that no epoch-1 identity remains in fixtures/compatibility, the release
    manifest, or the topology, proved by
    epoch_two_fixture_inventory_contains_no_epoch_one_identity
  - Handbook compatibility, upgrade, and known-limitations updates naming the epoch-2 posture
  acceptance_commands:
  - ./scripts/acceptance --wp WP-757
  - ./scripts/ci-all
  exit_gate: While external_databases is empty, every topology domain except exact
    `durable.authoritative_changelog` lists one equal readable, writable, and current identity as
    its sole active identity, and that exception matches ADR-0207's exact ordered V1/V2/V3 readable,
    V3-only writable/current, single_current, V1/V2-read_only, V3-active, empty-candidate/retired,
    immutable identity-binding topology; the checker rejects every variation or expansion and
    ordinary ADR-0124 behavior resumes once the array is nonempty; the manifest reports one version
    per family; an epoch-1 database is refused in place and crosses only through the ceremony with
    reconciled counts and hashes; no deleted field number or enum value is reused.
  standing_design_tests:
    interface_safety: No application or agent surface gains an operation; the ceremony is
      operator-only, refuses before mutation, and applications cannot select an epoch, decoder, or
      fallback.
    scale: One identity per domain is the starting state a billion-row tier would also want; the
      ceremony is bounded by the existing export and reimport ceilings and assumes no co-located
      storage.
  human_review_triggers: &id0181
  - A superseded identity would be retained as read_only or retirement_candidate instead of being
    deleted outside ADR-0207's exact authoritative-changelog V1/V2 read-only evidence, or a deleted
    field number, enum value, tag, or hash would be reused.
  - An epoch-1 database would be opened, upgraded, or restored in place rather than crossing the
    ceremony, or the ceremony would skip validation, backup, export, reconciliation, or retention.
  - The external_databases array would be populated without an operator-provided digest, date,
    and release label, or the single-identity rule would be relaxed beyond ADR-0207's exact
    hard-coded authoritative-changelog exception while it is empty.
  - A ledger entry, ADR section, or public handbook page would be exempted from the new checks
    without a recorded reason.

- id: WP-758
  name: Obligations ledger closure and ADR form governance
  tier: internal
  depends_on: [WP-757]
  objective: Discharge or strike all 144 entries in adr/obligations-outstanding.yaml, remove the
    ledger, make scripts/check-adr-obligations reject any ledger, and add scripts/check-adr-form with
    the repository glossary so ADR length, sections, and SPEC document-control rows are bounded.
  requirements: [GOV-004, GOV-005]
  required_adrs: [ADR-0156, ADR-0181]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/**, scripts/check-adr-obligations,
    scripts/check-adr-form, scripts/ci-all, scripts/check-workspace-policy, .github/workflows/ci.yml,
    docs/CONTRIBUTING.md, docs/architecture/**]
  deliverables:
  - A discharge record for every ledger entry, either an OBL declaration in its ADR header whose
    proof exists in the tree or a one-line strike reason, with the ledger file removed at zero
  - scripts/check-adr-obligations amended to fail on any ledger file or entry and on any undeclared
    obligation, with a self-test proving both, proved by check-adr-obligations --self-test
  - adr/GLOSSARY.md defining compiler-sealed, compiler-bounded, bounded, typed refusal,
    least-sufficient, fail closed, admission-head fenced, rebuildable, epoch, generation, and
    frontier once, with the ADR template referencing it
  - scripts/check-adr-form, proved by check-adr-form, that warns above 260 lines, fails on a missing
    or unknown section, and fails on a SPEC document-control row longer than one paragraph, wired
    into scripts/ci-all and the CI workflow
  - Contributing guidance stating that a new deferred obligation has exactly one form and that an
    ADR whose proof cannot exist at acceptance registers its package first
  acceptance_commands:
  - ./scripts/acceptance --wp WP-758
  - ./scripts/ci-all
  exit_gate: adr/obligations-outstanding.yaml no longer exists, check-adr-obligations reports zero
    acknowledged and zero undeclared obligations and fails when a ledger or undeclared obligation is
    introduced, every ADR passes check-adr-form or carries only the length warning, and the
    glossary is referenced by the template.
  standing_design_tests:
    interface_safety: Not applicable beyond one sentence; this package changes repository
      governance scripts and records only and adds no application, operator, or agent surface.
    scale: Not applicable; the package touches no storage, memory, or execution path.
  human_review_triggers: *id0181

- id: WP-759
  name: Agent-audience handbook rewrite measured by campaign 04
  tier: internal
  depends_on: [WP-757, WP-758, WP-579]
  objective: Rewrite docs/known-limitations.md as one sentence per limit, remove work-package and
    ADR numbers from every public handbook page, revise the quickstarts in public vocabulary, and
    prove the result with a sealed campaign-04 run against the stated time-to-first-row and rescue
    targets.
  requirements: [GOV-006, HBK-002, HBK-005, HBK-011]
  required_adrs: [ADR-0056, ADR-0122, ADR-0181]
  allowed_paths: [SPEC.md, work_packages.yaml, docs/**, scripts/handbook,
    scripts/check-handbook-audience, scripts/check-handbook-source, scripts/ci-all,
    evaluations/agent-application-alpha/**, release/evidence/**, fixtures/agent-guidance/**,
    crates/riffdb-cli/src/scaffold.rs]
  deliverables:
  - docs/known-limitations.md rewritten as one sentence per limit grouped by task, using only
    vocabulary defined in the handbook concept pages, with internal provenance moved to source
    comments or excluded evidence pages
  - Every non-excluded handbook page free of WP and ADR numbers, and the quickstarts and agent
    guidance resource revised under the same rule with the same seeded examples
  - scripts/check-handbook-audience, proved by check-handbook-audience, that fails on a WP or ADR
    number in a public page or a multi-sentence limitation entry, wired into scripts/handbook check
  - Campaign 04 under the campaign-03 bundle protocol with the rewritten handbook, its immutable
    campaign file, and a receipt comparing median time to first committed row and total rescue
    count against campaign 03 per language and brief cell, plus the published 25 percent
    slowest-cell improvement goal
  - Handbook publication of the epoch-2 compatibility posture in the same public vocabulary
  acceptance_commands:
  - ./scripts/acceptance --wp WP-759
  - ./scripts/ci-all
  exit_gate: The limitations page has one sentence per entry, no public page carries a WP or ADR
    number, the handbook builds and links cleanly, and the campaign-04 receipt shows median time to
    first committed row and total rescue count not worse than campaign 03 on every retained cell;
    a regression on either metric blocks closure.
  standing_design_tests:
    interface_safety: Documentation changes alter no public operation; the page rewrite cannot
      describe proposed or deferred behavior as available and cannot expose values or credentials.
    scale: Not applicable; the package changes documentation and evaluation receipts only.
  human_review_triggers: *id0181

- id: WP-760
  name: Bounded dirty recovery, first-access row validation, background scrub, and public scrub
  tier: guarantee
  depends_on: [WP-704, WP-705]
  objective: Make a startup without a valid clean-close certificate reach readiness through
    journal-suffix recovery plus the bounded-root validation the clean path already performs,
    with no population walk and a compiled recovery heap ceiling. Move exhaustive corruption
    discovery to first access plus one background scrub with closed health states, and expose
    the complete exact-end path as one authorized public offline scrub with a receipt.
  requirements: [REC-005, REC-006, REC-007, REC-008, REC-001, REC-002, REC-004, PERF-014,
    PERF-019]
  required_adrs: [ADR-0019, ADR-0050, ADR-0073, ADR-0085, ADR-0156, ADR-0157, ADR-0163,
    ADR-0165, ADR-0182, ADR-0199, ADR-0203]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0182-bounded-dirty-recovery-and-online-retention.md, adr/README.md,
    proto/**, crates/riffdb-proto/**, crates/riffdb-api-grpc/**, crates/riffdb-client-rust/**,
    crates/riffdb-policy/**, crates/riffdb-types/**, crates/riffdb-operation-registry/**,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**, crates/riffdb-server/**,
    crates/riffdb-service/**, crates/riffdb-observability/**, crates/riffdb-cli/**,
    crates/riffdb-sim/**, crates/riffdb-testkit/**, tests/**, fixtures/**, scripts/**,
    benchmarks/**, release/**, docs/**]
  deliverables:
  - Dirty readiness path ordered as engine open, format identity, journal-frame recovery,
    ADR-0156 section 5 bounded-root validation, dirty-generation consumption, readiness; the
    complete exact-end evidence stream is not constructed on that path, and health keeps the
    closed startup modes clean_certificate and complete_validation
  - A source and test inventory proving every population row class already validates envelope,
    checksum, canonical encoding, key identity, schema ownership, bounds, and reciprocal
    evidence on first access, with fail-closed local checks added where a path relied on
    startup; proof planted_row_corruption_fails_closed_on_first_access_after_dirty_startup
  - A compiled 64 MiB recovery-owned state ceiling with a typed internal-defect refusal, no
    population locator index on the dirty path, and the memory-bounded historical evidence
    cursor retained for the scrub; proof dirty_startup_reaches_readiness_without_a_population_walk
  - One background scrub over a least-authority reader with the closed scrub health component
    (pending, running, complete, failed, cancelled), a rows-per-step rate bound, shutdown
    cancellation, and incident-only failure handling; proof
    background_scrub_health_transitions_are_closed_and_cancellable
  - One authorized offline maintenance scrub operation on the ADR-0050 path, surfaced as
    riffdb storage scrub, with a versioned bounded receipt beneath .maintenance and reuse by
    backup verification, restore, upgrade, and retention preflight; proof
    public_offline_scrub_drives_the_complete_validation_path
  - PERF-013, PERF-014, and PERF-019 evidence reporting dirty-restart stages, total wall time,
    and peak recovery heap separately at the 65,536-row and production-scale checkpoints plus
    scrub rows per second, validated by scripts/check-dirty-recovery-evidence; this package is the
    exclusive owner of the production-scale dirty-readiness, zero-population-walk, and 64 MiB
    recovery-owned-state evidence, while WP-705's production-scale LimitExceeded receipt is an input
    rather than a passing result; handbook startup-integrity, troubleshooting, and known-limitations
    updates
  - While the ADR-0183 freeze is in force, only the exact pre-freeze definition, its existing
    non-candidate bounded dirty-recovery objective and recovery-performance evidence, and a new
    closure are authorized; no new PERF requirement, performance candidate, threshold, admitted
    package, or direct freeze lift is part of WP-760
  acceptance_commands:
  - ./scripts/acceptance --wp WP-760
  - ./scripts/ci-all
  exit_gate: A real-daemon kill and reopen at the 65,536-row and production-scale checkpoints
    reaches readiness with a pinned zero population-table enumeration count and peak
    recovery-owned heap under 64 MiB; every planted population-row corruption reaches readiness
    and then fails closed on first touch through read, mutation, delivery, projection, and
    export; the background scrub and the public offline scrub both detect the same planted
    defects; and repeated dirty restart creates no command, event, outcome, provenance, audit,
    projection, outbox, or allocator advance.
  standing_design_tests:
    interface_safety: No public request, contract, flag, or transport option can skip
      validation, force readiness, suppress the scrub, or obtain a storage handle; the only
      public addition is an authorized operator operation that performs more validation and
      one closed health component that exposes no hash, frontier, count, path, or key.
    scale: The dirty path no longer walks or rewrites full state before serving and holds a
      named 64 MiB ceiling regardless of population size; the scrub streams over a bounded
      cursor and the ceiling is measured at production scale rather than assumed.
  human_review_triggers: &id0182
  - A validation currently performed before dirty readiness would be removed without an
    equivalent fail-closed first-access check, or corruption would be reported as absence,
    a business outcome, partial output, or automatic repair.
  - A public option to skip validation, force readiness, suppress the scrub, or advance
    retention past any fencing input would be introduced.
  - A second writer, an exclusive lease on the online lane, a wall-clock retention target, or
    a change to acknowledgement semantics, durable formats, or the hardened profile would be
    introduced.
  - Recovery-owned state would exceed the 64 MiB ceiling or a prune step would exceed 256
    sequences or block the writer for more than one step.

- id: WP-761
  name: Online retention lane with complete fencing, crash matrix, and interference evidence
  tier: guarantee
  depends_on: [WP-760]
  objective: Run watermark advancement, prune, and tombstone verification inside the running
    process on a background maintenance lane that submits each bounded sub-range as one
    administration-class transition to the sole ordered writer, under the complete fencing set
    including registered follower frontiers, and prove every crash window and the interference
    bound. The offline exclusive operation remains available and unchanged.
  requirements: [STO-030, STO-031, STO-032, REC-001, REC-002]
  required_adrs: [ADR-0061, ADR-0072, ADR-0085, ADR-0093, ADR-0101, ADR-0104, ADR-0178,
    ADR-0182]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0182-bounded-dirty-recovery-and-online-retention.md, adr/README.md,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**, crates/riffdb-commit/**,
    crates/riffdb-server/**, crates/riffdb-service/**, crates/riffdb-observability/**,
    crates/riffdb-cli/**, crates/riffdb-sim/**, crates/riffdb-testkit/**, tests/**,
    fixtures/**, scripts/**, benchmarks/**, release/**, docs/**]
  deliverables:
  - One background retention lane in the server that reads the complete fencing set
    (projection frontiers, consumer low water, undelivered-outbox low water, staged migration
    frontier, operator holds, and every registered follower's acknowledged frontier under its
    hold budget) and refuses advancement on any unreadable, unvalidated, or
    incarnation-mismatched source
  - One administration-class prune transition submitted to the sole ordered writer that
    atomically deletes at most 256 sequences, appends the tombstone, advances the watermark,
    and deletes any validated-prefix checkpoint in the first step; no exclusive lease, no
    second writer, and no journal rebase on the online path
  - A riffdbd retention configuration expressed as retained-sequence count or hold, a
    sequences-per-epoch progress budget, and closed lane health; application principals gain
    no retention operation and the audited hold, detach, and reattach verbs are unchanged
  - Process-kill and riffdb-sim seeded crash windows before, during, and after every step's
    delete, tombstone append, watermark advance, and checkpoint deletion, plus fencing refusal
    arms for every source including a lagging follower; proof
    online_retention_crash_windows_recover_to_a_valid_watermark
  - 32-client interactive interference evidence with the lane active against idle inside the
    existing five-percent p95 no-regression bound, validated by
    scripts/check-online-retention-interference and added to the release evidence inventory;
    handbook operations, retention, backup, and known-limitations updates removing the
    offline-only statements
  acceptance_commands:
  - ./scripts/acceptance --wp WP-761
  - ./scripts/ci-all
  exit_gate: A database under sustained 32-client load compacts history to its configured
    retained-sequence target with no downtime, no exclusive lease, and p95 inside the
    five-percent bound; every armed crash window reopens with a valid watermark, contiguous
    tombstone chain, and allocator agreement and creates no command, event, outcome,
    provenance, audit, projection, outbox, or allocator advance; pruning never crosses a
    projection, consumer, outbox, migration, hold, or follower fence; and the offline
    operation still passes its existing recovery matrix unchanged.
  standing_design_tests:
    interface_safety: Retention is driven only by configuration and the existing audited
      operator verbs; no application principal can start, stop, target, or observe it beyond
      closed health, and no fence can be bypassed from any surface.
    scale: Compaction proceeds in bounded 256-sequence steps through the existing ordered
      writer without whole-database exclusive access, and the fencing set includes replica
      frontiers so the same rule holds with followers attached.
  human_review_triggers: *id0182

- id: WP-762
  name: Dual-profile baseline bank under ADR-0171 and performance-package freeze
  tier: internal
  depends_on: [WP-674, WP-745, WP-780]
  objective: Bank one qualified interactive c32, write-only, and unary baseline receipt set on
    both N1 and E2 under ADR-0171's gated-backend stability rule, then record the ADR-0183
    performance-package freeze in the same change so the freeze starts from banked receipts rather
    than the retained failed attempts.
  requirements: [PERF-020, PERF-021, PERF-018]
  required_adrs: [ADR-0123, ADR-0142, ADR-0143, ADR-0146, ADR-0171, ADR-0183,
    ADR-0199]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0183-performance-package-freeze-and-durable-group-lever.md, adr/README.md,
    benchmarks/**, scripts/**, release/evidence/**, docs/performance/**, docs/**]
  deliverables:
  - Interactive c32 and write-only concurrency-sweep receipts with three same-run 90-second
    repetitions on N1 and E2, each carrying V2 host validity, correctness reconciliation, and
    method.stability_rule_binds recorded as gated_backend
  - Complete unary matrix on both profiles under ADR-0146 generations; the retained N1 column
    from commit f4f03bae is reused only when its source, lock, harness, runner, and daemon
    identities match the banked revision
  - assemble-wp674-unary-manifest --require-both-profiles, which refuses an N1-only manifest or a
    receipt whose stability rule is not bound to the gated backend
  - A freeze record in work_packages.yaml created in the same change as the banked receipts. It
    inventories every then-open PERF package with a 64-lowercase-hex SHA-256 digest of the RFC
    8785 JCS UTF-8 bytes of its entire parsed package mapping after removing only top-level
    closure, and rejects non-JSON scalars and non-finite numbers. The record names exactly
    WP-750/WP-760 closure-only prerequisite authority plus WP-762/WP-763 existing authority;
    WP-750 and WP-760 are lifting conditions, never direct lifts
  - scripts/check-performance-freeze with its self-test, wired into ci-all and the WP-762 and
    WP-763 acceptance commands; the self-test accepts exact WP-750 and WP-760 closure-only
    transitions and covers rejection of a new PERF package, an inert-package edit and closure,
    a widened prerequisite definition or PERF set, a fifth exception, and a premature lift
  - SPEC 19.6 human-review trigger entry and docs/performance/wp-762-baseline-bank.md recording
    receipt digests, the freeze start, and the exact c32, p95, seed, and unary values banked
  acceptance_commands:
  - ./scripts/acceptance --wp WP-762
  - ./scripts/ci-all
  exit_gate: Both profiles hold qualified c32, write-only, and unary receipts bound to the
    gated-backend rule with matching artifact identities; the freeze record seals every then-open
    PERF-package definition, its checker passes only exact inert entries, WP-750/WP-760 closure-only
    transitions, and WP-762/WP-763 existing authority, and rejects every listed unauthorized arm;
    no performance value moved upward relative to the WP-644 baseline without a receipted
    attribution.
  standing_design_tests:
    interface_safety: No application or operator surface changes; the package produces receipts
      and a repository check, and no caller can select a stability rule, profile, or retry.
    scale: Baselines are per inventoried single-node profile; the freeze check is a bounded read
      of the manifest and does not grow with database size or command count.
  human_review_triggers: &id0183
  - A fifth package would be permitted to advance while the freeze is in force, or an inert
    pre-freeze PERF package would change or close.
  - WP-750 or WP-760 would add a PERF requirement, activate a performance candidate, alter a
    threshold, admit another package, or lift the freeze.
  - The freeze would start without the WP-762 bank, or lift before WP-750 and WP-760 close or
    without a separately accepted ADR.
  - A banked baseline value would be moved upward, a failed generation retried or replaced, or a
    receipt banked without both inventoried profiles.
  - PERF-009 acknowledgement, fence, or durability semantics would change, or any fsync, fence,
    or acknowledgement would be elided or reordered.
  - Group formation would wait for work that is not already admitted and prepared, or any
    ceiling of DeferredCommandEpoch would widen.

- id: WP-763
  name: Durable-group formation from ready prepared work and its activation gate
  tier: guarantee
  depends_on: [WP-762, WP-640]
  objective: Make every standard-profile epoch seal carry the complete already-admitted,
    already-prepared command prefix up to the existing 256-transition and 16 MiB ceilings, fed
    by WP-640's conflict-domain-parallel preparation mechanics, without any wait an idle singleton
    or a contended dispatch can observe, then activate only under ADR-0183's fixed arithmetic on
    both profiles or remove the candidate.
  requirements: [PERF-022, PERF-008, PERF-007]
  required_adrs: [ADR-0059, ADR-0098, ADR-0101, ADR-0104, ADR-0129, ADR-0142, ADR-0171,
    ADR-0183, ADR-0199]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0183-performance-package-freeze-and-durable-group-lever.md, adr/README.md,
    crates/riffdb-commit/**, crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**,
    crates/riffdb-server/**, crates/riffdb-service/**, crates/riffdb-testkit/**,
    benchmarks/**, scripts/**, release/evidence/**, tests/**, docs/performance/**, docs/**]
  deliverables:
  - Seal-edge group formation in riffdb-commit that drains only the contiguous prepared prefix
    within DeferredCommandEpoch ceilings, keeps the immediate one-phase path for an idle
    singleton, and retains the 200 µs OLDEST_GROUPABLE_TRANSITION_MAX_AGE as the only age bound;
    proofs idle_singleton_seals_without_group_formation_wait and
    group_formation_drains_only_prepared_prefix_within_ceilings under deterministic schedules
    with the ADR-0129 frontier-equivalence assertion at every epoch boundary
  - Fixed-cardinality commands-per-durable-flush histogram and ceiling-reached counter in the
    graceful-shutdown census, proved by shutdown_evidence_reports_commands_per_durable_flush on
    both durability profiles
  - scripts/check-durable-group-activation with --self-test covering pass-both, miss-N1, miss-E2,
    unary-regression, and write-only-regression receipts; activation requires c32 throughput at
    least 0.90 times and p95 at most 1.25 times same-run safe PostgreSQL on both profiles, write-
    only p95 within five percent of the WP-762 bank, and every unary p50/p95 within 1.10 times
    its low-water baseline
  - Candidate receipts on N1 and E2 retaining group-size, frame-byte, physical-fence, and
    ordered-fence-latency evidence; unchanged crash, recovery, replay, uncertainty, and
    storage-recovery matrices run against the candidate before any activation receipt
  - During the ADR-0183 freeze, WP-763 retains only its existing authority to build and either
    activate or remove this one candidate; it cannot widen its PERF set, admit another package,
    or lift the freeze
  - docs/performance/wp-763-durable-group-lever.md with the activation or removal verdict; a
    missed gate on either profile removes the candidate code before merge
  acceptance_commands:
  - ./scripts/acceptance --wp WP-763
  - ./scripts/ci-all
  exit_gate: The candidate either activates with both-profile receipts satisfying every ADR-0183
    section 4 gate and no crash, recovery, replay, uncertainty, or write-p95 regression, or is
    removed with its verdict recorded; in both outcomes an idle singleton seals immediately,
    group formation never exceeds a ceiling or waits for unprepared work, and commands per
    durable flush are reported in shutdown evidence.
  standing_design_tests:
    interface_safety: Applications cannot select group size, wait, fence, durability, or
      activation; acknowledgement still follows the covering durable fence and no partial epoch
      is observable.
    scale: Formation is bounded by first-party ceilings and named per conflict domain, so it
      applies unchanged to a future per-domain leader and holds no state proportional to load.
  human_review_triggers: *id0183

- id: WP-764
  name: Panic-lint gates, allowance ledger, and the writer fail-fast proof
  depends_on: [WP-580, WP-640]
  objective: Deny unwrap, expect, panic, and unreachable on the library targets of the five durable
    crates, convert or reason every surviving site, give the generators one infallible writer, and
    prove that a writer-thread panic stops the coordinator and exits riffdbd nonzero.
  requirements: [TEST-002]
  required_adrs: [ADR-0012, ADR-0058, ADR-0101, ADR-0104, ADR-0184]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0184-panic-discipline-test-economics-and-coordinator-simulation.md, adr/README.md,
    Cargo.toml, crates/riffdb-commit/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-service/**, crates/riffdb-server/**,
    crates/riffdb-query-module/**, crates/riffdb-testkit/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Crate-level `[lints.clippy]` deny for unwrap_used, expect_used, panic, and unreachable on
    the library targets of riffdb-commit, riffdb-storage-api, riffdb-storage-redb,
    riffdb-service, and riffdb-server, with test, example, and bench targets exempt
  - Every surviving site converted to a typed error or kept behind `#[expect(..., reason)]`
    naming the invariant; no crate-level blanket allow; `scripts/check-panic-allowances`
    prints the ledger and fails on an allowance without a reason
  - One infallible string-write helper in riffdb-query-module used by the Rust, Go, Python, and
    TypeScript generators; architecture test
    `generators_use_the_infallible_writer_and_carry_no_fmt_expect`; generated fixtures byte-identical
  - Test-only writer failpoint and root integration test
    `writer_thread_panic_stops_the_coordinator_and_exits_the_daemon_nonzero` owned by
    riffdb-server, asserting no acknowledgement after the panic, a typed unavailable outcome
    for a later submission, and nonzero daemon exit within the drain deadline
  acceptance_commands:
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  - ./scripts/check-panic-allowances
  - cargo +1.97.0 test -p riffdb-commit -p riffdb-storage-api -p riffdb-storage-redb
    -p riffdb-service -p riffdb-server -p riffdb-query-module --all-features
  - cargo +1.97.0 test -p riffdb-server --test writer_fail_fast
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-adr-obligations
  - cargo +1.97.0 fmt --all -- --check
  exit_gate: The five durable crates compile with the four panic lints denied, every allowance
    carries a reason the ledger script accepts, generated artifacts are byte-identical, and a
    forced writer-thread panic yields no acknowledgement, a typed refusal, and nonzero exit.
  standing_design_tests:
    interface_safety: No public surface changes; the writer failpoint exists only under a test
      feature and cannot be selected by configuration, request, or environment in a release
      binary.
    scale: Lint levels and the fail-fast path are process-local and independent of database
      size; no population walk or in-memory state is introduced.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [TEST-002]
    evidence: The five durable library targets deny unwrap_used, expect_used, panic, and
      unreachable outside test builds, while scripts/check-panic-allowances enumerates 54 scoped
      and reasoned expectations and rejects blanket or unreasoned suppression. All four language
      generators consume formatting results through one infallible String helper with generated
      artifacts unchanged. The writer_fail_fast real-process test arms a test-feature-only
      pre-storage command-dispatch panic, proves two commands receive typed storage-unavailable
      results without acknowledgement, and observes riffdbd exit nonzero within the drain bound.
  human_review_triggers: &id0184
  - A panic-capable call would be kept in a durable crate without a reason naming the invariant,
    or a crate-level blanket allowance would be introduced.
  - The daemon would continue acknowledging commands or reporting readiness after the writer
    panic flag is set, or the fail-fast test would be weakened to a log assertion.
  - A production crate would gain a dependency on riffdb-sim, a test clock, or a seed source.
  - A rename, lint, or CI change would alter any durable byte, artifact hash, or public protocol.

- id: WP-765
  name: Partitioned nextest execution and recorded CI wall time
  tier: internal
  depends_on: [WP-764]
  objective: Replace the single workspace test job with four fixed cargo-nextest partitions, prove
    every test target is scheduled exactly once, and record the measured wall time before and
    after with a target in release evidence.
  requirements: [TEST-003]
  required_adrs: [ADR-0184]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0184-panic-discipline-test-economics-and-coordinator-simulation.md, adr/README.md,
    .github/workflows/**, .config/**, Cargo.toml, scripts/**, release/evidence/**, docs/**]
  deliverables:
  - Nextest configuration in .config/nextest.toml with the pinned nextest version in `scripts/tool-versions`, four
    hash-based partitions, and per-test timeouts matching the existing slowest root targets
  - CI workflow change running one job per partition on the pinned toolchain, each writing its
    measured wall time to the job summary; `cargo test --workspace --all-features` retained in
    `scripts/ci-all`
  - Coverage script scripts/check-test-partition-coverage enumerating every test target from `cargo metadata`
    and asserting each is scheduled exactly once across the partitions, including all root
    `[[test]]` targets owned by riffdb-testkit and riffdb-server
  - Evidence file release/evidence/ci-wall-time-v1.json recording the single-job baseline measured before
    this package, the per-partition times after, and the 20-minute longest-partition target
  acceptance_commands:
  - ./scripts/acceptance --wp WP-765
  - ./scripts/ci-all
  exit_gate: Four partition jobs pass on CI, the coverage script proves exactly-once scheduling
    of every test target, and the evidence file holds a measured before and after with the
    longest partition and the target stated.
  standing_design_tests:
    interface_safety: CI layout is repository policy with no application-facing surface.
    scale: Partition count is fixed and discovered targets are hashed, so adding crates or
      tests requires no manual shard maintenance.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [TEST-003]
    evidence: CI uses four exact nextest filtersets derived by SHA-256 whole-target assignment,
      with the pinned tool version and slow-test timeout policy in repository configuration; the
      workspace cargo-test path remains in ci-all. The coverage proof assigns all 300 current
      Cargo test targets exactly once, including the riffdb-testkit and riffdb-server root targets.
      The retained v1 receipt records the 334.92-second clean baseline, accepted partition
      measurements, and the 1,200-second longest-partition target. A local four-job rehearsal
      passed partitions 1 through 3 concurrently and partition 4 in isolation after shared-host
      timing contention; the combined package acceptance and ci-all merge battery both pass.
  human_review_triggers: *id0184

- id: WP-766
  name: Deterministic coordinator simulation
  depends_on: [WP-583, WP-764]
  objective: Deliver ADR-0113 Phase 2 in riffdb-sim by composing SimDisk, the riffdb-conflict
    deterministic scheduler, and a seeded step scheduler over admission, preparation, epoch seal,
    fence, publication, completion, and changelog emission with crash points, checked against
    the testkit model after every recovery.
  requirements: [SIM-008, SIM-009]
  required_adrs: [ADR-0058, ADR-0095, ADR-0098, ADR-0101, ADR-0104, ADR-0113, ADR-0184]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0184-panic-discipline-test-economics-and-coordinator-simulation.md, adr/README.md,
    crates/riffdb-sim/**, crates/riffdb-commit/**, crates/riffdb-conflict/**,
    crates/riffdb-storage-redb/**, crates/riffdb-testkit/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Monotonic-time port for the coalescing decisions in riffdb-commit audit_executor with an
    unchanged production implementation and a harness-driven implementation under test only
  - Seeded coordinator step scheduler in riffdb-sim choosing lane interleavings and crash
    placement from the seed alone, with every decision folded into the versioned trace digest
  - Digest proof coordinator_interleaving_decisions_feed_the_digest proving byte-identical digests per seed
    and distinct digests when any scheduling or crash decision changes
  - Recovery proof every_coordinator_crash_window_recovers_to_the_model proving startup validation passes and
    state equals AuthoritativeCommandModel at the recovered frontier for every crash point,
    including seal-to-fence and fence-to-publication windows; model extended where narrower
  - Retained Phase-2 corpus replayed per merge under the existing seeded-campaign harness; the
    existing `no_production_crate_depends_on_the_simulator` test remains green
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-sim -p riffdb-commit -p riffdb-conflict --all-features
  - cargo +1.97.0 test -p riffdb-sim --test seeded_campaign
  - cargo +1.97.0 test -p riffdb-sim --test architecture
  - ./scripts/check-requirement-coverage
  - ./scripts/check-adr-obligations
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: One seed reproduces one digest, any decision change produces a different digest,
    every seeded coordinator crash window recovers to the model, and riffdb-sim remains absent
    from every production dependency graph.
  standing_design_tests:
    interface_safety: The time port and step scheduler are test seams; a release binary carries
      only the wall-clock implementation and no seed, schedule, or failpoint selector.
    scale: The harness bounds concurrent commands and crash schedules and inherits Phase 1's
      bounded disk; it assumes single-node composition exactly as ADR-0113 already does.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [SIM-008, SIM-009]
    evidence: The production coordinator now reads formation time through a monotonic port whose
      release constructor still installs only the wall-clock implementation; an all-features test
      drives the real audit lane through a simulator clock. riffdb-sim's bounded seed-only
      scheduler selects all seven per-command phases, lane interleavings, and one crash placement,
      while its versioned digest changes for every tested lane or crash decision. The named
      composition proof reaches production conflict checkpoints and SimDisk seal, fence, and
      recovery operations. The named recovery proof scouts seed-selected crashes at every phase,
      including seal-to-fence and fence-to-publication, then requires full startup validation and
      AuthoritativeCommandModel equality at the recovered frontier. Three versioned Phase-2
      schedules are retained in the per-merge seeded campaign. This is a test-only internal seam,
      so no public handbook behavior changed.
  human_review_triggers: *id0184

- id: WP-767
  name: Reactive-syntax crate rename and file-size guard
  depends_on: [WP-764]
  objective: Rename riffdb-query-syntax to riffdb-reactive-syntax with byte-identical reactive
    artifacts, and add a file-size guard that reports growth above 6,000 lines and refuses growth
    of any file already above 10,000 lines, wired into the consolidation packages and ci-all.
  requirements: [TEST-004]
  required_adrs: [ADR-0184]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0184-panic-discipline-test-economics-and-coordinator-simulation.md, adr/README.md,
    Cargo.toml, Cargo.lock, crates/riffdb-query-syntax/**, crates/riffdb-reactive-syntax/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**, fixtures/**, scripts/**,
    release/version-topology-v1.json, docs/**]
  deliverables:
  - Package, directory, import, workspace-policy, and handbook crate-inventory rename to
    riffdb-reactive-syntax with REACTIVE_GRAMMAR_VERSION_V1 and every fixture unchanged
  - Byte-identity proof reactive_module_artifacts_are_byte_identical_after_rename regenerating reactive-module
    fixtures through the renamed crate and diffing against the checked-in bytes
  - Guard script scripts/check-file-size-guard reporting non-generated Rust files above 6,000 lines,
    failing when a file above 10,000 lines has grown relative to the merge base, with a
    self-test that fabricates a grown file and proves refusal
  - Guard added to `scripts/ci-all` and to the acceptance commands of the ADR-0179 and ADR-0180
    packages that touch the six largest files
  acceptance_commands:
  - cargo +1.97.0 test -p riffdb-reactive-syntax -p riffdb-query-compiler
    -p riffdb-query-module --all-features
  - ./scripts/check-file-size-guard
  - ./scripts/check-file-size-guard --self-test
  - ./scripts/check-workspace-policy
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/check-adr-obligations
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy --workspace --all-targets --all-features -- -D warnings
  exit_gate: The workspace builds with riffdb-reactive-syntax and without riffdb-query-syntax,
    reactive fixtures are byte-identical, and the size guard refuses a fabricated growth of a
    file above 10,000 lines while passing the current tree.
  standing_design_tests:
    interface_safety: A crate rename and a repository script expose nothing to applications.
    scale: Not applicable; the guard reads source files and no database state.
  closure:
    status: complete
    completed_at: "2026-09-01"
    requirements_satisfied: [TEST-004]
    evidence: The reactive grammar leaf is now explicitly named riffdb-reactive-syntax throughout
      the workspace and retains REACTIVE_GRAMMAR_VERSION_V1. The named reactive-module proof
      recompiles the checked-in Woodpecker source and compares every canonical artifact byte with
      the retained fixture. scripts/check-file-size-guard inventories all non-generated Rust above
      6,000 lines, rejects positive merge-base growth above 10,000 lines, and proves refusal in a
      temporary Git history; ci-all and every affected ADR-0179/ADR-0180 package invoke it. The
      public handbook has no internal crate inventory, so no user-facing documentation changed.
  human_review_triggers: *id0184

- id: WP-768
  name: Operator admission rule, expansion syntax and IR, and refusal classes
  tier: surface
  depends_on: [WP-738, WP-743]
  objective: Establish ADR-0185's operator-before-provider rule in the compiler, add the
    one-to-many expansion syntax and additive RiffQL V14 / query IR V18 / module V18 identities
    with least-sufficient selection, and record anonymized refusal classes for every refused
    operational read shape. Compiler and fixtures plus fail-closed downstream admission only;
    no expansion execution path changes.
  requirements: [RQL-008, OQ-113, OQ-116, OQ-118]
  required_adrs: [ADR-0051, ADR-0054, ADR-0108, ADR-0124, ADR-0174, ADR-0185]
  allowed_paths: [SPEC.md, work_packages.yaml, adr/0185-relational-operators-in-the-candidate-algebra.md,
    crates/riffdb-types/**, crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**, crates/riffdb-diagnostics/**,
    crates/riffdb-query-executor/**, crates/riffdb-service/**, crates/riffdb-cli/**,
    crates/riffdb-storage-memory/src/query.rs, crates/riffdb-storage-redb/src/query.rs,
    clients/editor/tree-sitter-riffql/keywords.json, release/version-topology-v1.json,
    fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Grammar for `for each <driver> in <binding>` and `take N per <driver>` on a many binding, with
    exact-span diagnostics for cross-partition, unbounded, non-prefix, and depth-two drivers
  - Additive IR tag for the expansion access, product-bound proof (driver maximum times per-driver
    maximum within the 65,535 scan ceiling and the result ceiling), nesting proof against the
    return block, and least-sufficient V14/V18/V18 selection;
    expansion_plan_is_least_sufficient_and_legacy_bytes_are_unchanged
  - Refusal-class recording (operator kind, cardinality class, partition class, no values or
    names) surfaced by `riffdb application check --refusals` and the alpha campaign event schema;
    refused_shapes_are_recorded_as_anonymized_refusal_classes
  - Version-topology registration of the three additive identities and byte-exact legacy fixtures
  acceptance_commands:
  - ./scripts/acceptance --wp WP-768
  - ./scripts/ci-all
  exit_gate: Existing queries keep exact bytes and hashes; an expansion query compiles to the
    successor identities with a proven product bound; every refusal class is value-free; the
    topology shows no new provider domain.
  standing_design_tests:
    interface_safety: Driver, index, per-driver bound, order, and nesting are compiler identity;
      no generated schema, MCP tool, or CLI input carries them.
    scale: The product bound and the existing scan ceiling cap the plan before execution exists.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [RQL-008, OQ-113, OQ-116, OQ-118]
    evidence: The RiffQL grammar and exact-span diagnostics admit one bounded same-partition
      one-to-many expansion and refuse cross-partition, unbounded, non-prefix, and depth-two
      drivers. The compiler emits the additive expansion access only after proving driver-times-
      per-driver and result bounds, selects RiffQL V14, query IR V18, and module V18 only when
      needed, and the named proof keeps every legacy fixture byte and hash exact. Refusal records
      contain only closed operator, cardinality, and partition classes; application check emits
      the bounded empty receipt in refusal mode. Downstream executor, service, memory, and redb
      admission remains fail-closed with no expansion execution path. Version topology remains 59
      domains with no provider domain, package acceptance passes, and ci-all passes.
  human_review_triggers: &id0185
  - A shape would be admitted as a new provider family without a written operator refusal.
  - An operator would read across partitions, consume a caller-supplied plan, or release a
    partial result.
  - RiffQL, query IR, or module bytes for existing queries would change rather than selecting a
    successor identity.

- id: WP-769
  name: Expansion execution, carriage, and generated bindings
  tier: surface
  depends_on: [WP-768]
  objective: Execute one-to-many expansion in memory and redb inside the existing one-snapshot
    read, with complete-before-release semantics, policy before observation, nested and flat
    result carriage, and generated Rust, Go, TypeScript, Python, MCP, and CLI surfaces.
  requirements: [OQ-114, OQ-115]
  required_adrs: [ADR-0035, ADR-0053, ADR-0111, ADR-0164, ADR-0174, ADR-0185]
  allowed_paths: [work_packages.yaml, crates/riffdb-query-compiler/**,
    crates/riffdb-query-executor/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-memory/**, crates/riffdb-storage-redb/**, crates/riffdb-policy/**,
    crates/riffdb-service/**,
    crates/riffdb-server/**, crates/riffdb-query-module/**, crates/riffdb-client-rust/**,
    crates/riffdb-api-grpc/**, crates/riffdb-api-mcp/**, crates/riffdb-cli/**, clients/**,
    templates/**, fixtures/**, tests/**, scripts/**, docs/**]
  scope_expansion: Human-approved 2026-09-02. The mandatory memory/redb parity proof requires the
    memory backend, and nested expansion execution requires the compiler to retain fields selected
    below the driver record. Both paths remain limited to the accepted expansion semantics.
  deliverables:
  - One executor step that hydrates every driver's bounded target set through the existing
    authoritative readers at the captured snapshot, ordered by driver order then index order with
    the primary-key tie-breaker; expansion_completes_every_driver_before_release_or_refuses_whole
  - Row and field policy applied to targets before membership, count, order, or nesting, with
    denied targets indistinguishable from absent ones
  - Nested result carriage under the driver record plus flat carriage with the driver key, through
    the named-result carriage rules and every generated binding
  - Independent oracle, memory/redb parity, empty-driver, maximum-fan-out, denied-target,
    cancellation, and concurrent-mutation schedules without sleep-based synchronization;
    expansion_memory_redb_parity_matches_independent_oracle
  acceptance_commands:
  - ./scripts/acceptance --wp WP-769
  - ./scripts/ci-all
  exit_gate: Every driver's targets are complete at one snapshot or the whole query refuses;
    memory and redb agree byte for byte with the oracle; generated bindings expose only typed
    parameters and nested typed results.
  standing_design_tests:
    interface_safety: Storage receives the compiler-sealed expansion step and owned typed keys;
      no iterator, transaction, per-driver request, or fallback crosses the port.
    scale: Execution materializes at most the product bound and never a population.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [OQ-114, OQ-115]
    evidence: Expansion executes every bounded driver and its complete ordered target set inside
      one authoritative snapshot, applies target policy before observation, and withholds the
      whole result on bound or epoch refusal. Memory and redb match the independent maximum-fanout,
      empty-driver, and denied-target oracle; cancellation and concurrent mutation schedules are
      sleep-free. Nested typed records cross service, gRPC, MCP, CLI, and generated Rust, Go,
      TypeScript, and Python result surfaces through the existing name-addressed carrier, while
      legacy generated adapter fixtures remain byte exact. The explicit 19-path scope audit,
      all remaining package acceptance gates, 1,796 scoped tests, and ci-all pass.
  human_review_triggers: *id0185

- id: WP-770
  name: Existence predicates as candidate-algebra lowering
  tier: surface
  depends_on: [WP-768]
  objective: Add `exists` and `not exists` predicates over a declared junction index to root
    bindings, lowering to the ADR-0174 candidates intersection and authorized root-universe
    difference with byte-identical plans, so the operators add syntax and diagnostics only.
  requirements: [OQ-117]
  required_adrs: [ADR-0111, ADR-0174, ADR-0185]
  allowed_paths: [work_packages.yaml, crates/riffdb-riffql-syntax/**, crates/riffdb-query-ir/**,
    crates/riffdb-query-compiler/**, crates/riffdb-query-module/**, crates/riffdb-diagnostics/**,
    crates/riffdb-cli/assets/editor/highlights-v1.json, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Grammar and diagnostics for `exists <Junction> using <index> where ...` and its negation on a
    root binding, restricted to declared same-partition junction indexes projecting the root key
  - Lowering to the explicit candidates form with plan-byte equality proven per fixture;
    exists_predicates_lower_to_identical_candidate_plan_bytes
  - Handbook section in docs/riffql/LANGUAGE.md showing both spellings and the refusal cases
  acceptance_commands:
  - ./scripts/acceptance --wp WP-770
  - ./scripts/ci-all
  exit_gate: Every existence predicate has a byte-identical explicit candidates twin; no new
    execution path, cursor family, provider, or authority surface exists.
  standing_design_tests:
    interface_safety: Existence sources are compiler identity; negation always names the
      authorized root universe so denied rows cannot be inferred through complement.
    scale: Lowering reuses the candidate ceilings unchanged.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [OQ-117]
    evidence: Root-local exists and not-exists syntax now lowers only through declared
      same-partition junction relationships and complete index prefixes to the existing bounded
      candidate intersection or authorized root-universe difference. The named proof compares
      both forms with their explicit twins byte for byte and pins RiffQL V14 and query IR V18;
      module round-trip and generated Rust, Go, TypeScript, Python, and MCP checks prove the
      compiler-owned candidate structure remains hidden. Source-spanned refusals, handbook and
      limitation updates, regenerated editor artifacts, package scope audit, focused suites, and
      ci-all pass without adding an executor step, provider, cursor family, or authority surface.
  human_review_triggers: *id0185

- id: WP-771
  name: Refused-shape corpus acceptance and operator backlog ranking
  tier: surface
  depends_on: [WP-769, WP-770]
  objective: Prove the previously refused application shapes compile and execute end to end
    through every generated binding, rewrite the relationship-composition limitations, and
    publish the first refusal-class ranking from the alpha campaigns.
  requirements: [OQ-113, OQ-118]
  required_adrs: [ADR-0110, ADR-0117, ADR-0185]
  allowed_paths: [work_packages.yaml, examples/**, evaluations/**, clients/**, fixtures/**,
    tests/**, scripts/**, release/evidence/**, docs/**,
    crates/riffdb-cli/src/generated/app.rs,
    crates/riffdb-service/src/query_discovery_operations.rs,
    crates/riffdb-api-mcp/src/generated/service_backend.rs,
    crates/riffdb-query-module/src/generation.rs]
  scope_expansion: Human-approved 2026-09-02. The end-to-end CLI and hosted MCP corpus exposed
    schema-safe nested-result carriage and generated Rust decoder defects, so the approved paths
    are limited to their frozen/generated adapters, MCP discovery filtering, and query generator.
  deliverables:
  - Retained corpus of TicketDesk ticket page with comments per ticket, MLflow runs with tags per
    run, OpenFGA objects with relations per object, each compiled and executed through Rust, Go,
    TypeScript, Python, MCP, and CLI; check-operator-acceptance
  - docs/known-limitations.md relationship paragraph rewritten to the remaining refusals only
  - Refusal-class ranking receipt from campaign runs under release/evidence/operators/
  acceptance_commands:
  - ./scripts/acceptance --wp WP-771
  - ./scripts/ci-all
  exit_gate: Every corpus shape passes on every binding; the topology shows no provider domain
    added by WP-768 through WP-771; the ranking receipt names the next operator candidates.
  standing_design_tests:
    interface_safety: Corpus applications use only generated typed operations.
    scale: Not applicable; this package adds evidence, not execution.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [OQ-113, OQ-118]
    evidence: The retained TicketDesk, MLflow, and OpenFGA expansion shapes compile and execute
      with bounded nested cardinalities through generated Rust, Go, TypeScript, Python, CLI, and
      hosted MCP operations. The corpus proves no provider domain was added, preserves MCP
      authorization and schema validation, and records the remaining anonymized refusal classes
      and ranked operator candidates from all retained alpha campaign transcripts. Known
      limitations now describe only the remaining refused relationship shapes.
  human_review_triggers: *id0185

- id: WP-772
  name: Complete authoritative changelog V3 durable substrate
  tier: guarantee
  depends_on: [WP-155, WP-417, WP-486, WP-705]
  objective: Implement ADR-0186 sections 1 and 2 and the storage-substrate portions of sections 3
    and 5 through 7 as a prerequisite to replication activation. Freeze the authority inventory
    and V3 receipt format, make every same-lineage authoritative transaction durably resumable
    through the existing checkpoint-plus-journal-suffix authority, and prove rotation,
    reclamation, and crash behavior before any production emitter, RPC, bootstrap, or follower is
    activated.
  requirements: [REP-003, REC-001, PERF-007, STO-012]
  required_adrs: [ADR-0019, ADR-0061, ADR-0072, ADR-0082, ADR-0083, ADR-0085, ADR-0093,
    ADR-0100, ADR-0101, ADR-0104, ADR-0112, ADR-0124, ADR-0156, ADR-0157, ADR-0178,
    ADR-0186, ADR-0207]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0186-complete-authoritative-changelog-v3.md, adr/README.md,
    proto/**, crates/riffdb-proto/**, crates/riffdb-types/**, crates/riffdb-storage-api/**,
    crates/riffdb-storage-redb/**, crates/riffdb-commit/**,
    crates/riffdb-catalog/**, crates/riffdb-testkit/**, crates/riffdb-sim/**,
    release/version-topology-v1.json, release/durable-format-manifest-v1.json,
    fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - Storage-api-owned `AuthoritativeStateCatalogV1` classifying every table and metadata-key
    namespace exactly once as replicated-authoritative, rebuildable-local, or replication-control
    with exact transfer behavior; one generated reviewed fixture cross-checks redb layout,
    startup, backup, receipt construction, future bootstrap/apply, and exact prefix comparison
  - Closed `riffdb.changelog-frame/v3` transaction-receipt and checked
    `ChangelogTransactionSequence` allocator formats with canonical expected-state puts/deletes,
    complete source attribution, hard bounds, redaction-safe diagnostics, frozen vectors, registry
    migration, and durable-format fixtures; exact `durable.authoritative_changelog` topology with
    ordered readable identities `riffdb.changelog-frame/v1`, `riffdb.changelog-frame/v2`, and
    `riffdb.changelog-frame/v3`, each bound respectively to
    ChangelogFrameV1/1/RDBCLF01/RDBCLE01, ChangelogFrameV2/2/RDBCLF02/RDBCLE02, and ADR-0186
    V3/3/RDBCLF03/RDBCLE03; writable and current identities each exactly V3, writer policy
    `single_current`, V1/V2 exactly read_only, V3 solely active, and candidate and retired sets
    empty; existing V1/V2 source decoders and unit/recovery tests remain and frozen standalone V1
    and V2 frame fixtures are added alongside V3 without changing old bytes or interpretation;
    production negotiation, emission, apply, bootstrap tail, and archive carry, accept, persist,
    and select only exact V3 with no V1/V2 fallback, translation, bootstrap, restore, archive,
    downgrade, or partial-authority mode
  - Standard-profile receipt source derived exactly from existing validated Journal V1 mutations
    and allocator state, exposed only as a bounded storage cursor over `PublishedDurableSnapshot`;
    Journal V1 identity and bytes remain unchanged
  - Byte-identical materialization of each journal-sourced receipt by the same known-durable redb
    checkpoint before suffix reclamation, with overlap equality and crash recovery proving at least
    one complete source always remains
  - Atomic materialized receipts in each existing direct, hardened, migration, maintenance, and
    control-plane `Immediate` transaction, with no second transaction, flush, acknowledgement
    dependency, allocator lane, or application-visible order
  - Clean-close integration making each `CLEAN` final close and `DIRTY` activation/consumption one
    exactly attributed, non-recursive Immediate transaction over lifecycle, V3 allocator, history
    tail, and materialized control receipt; no write follows clean close, and clean eligibility
    validates V3 catalog/allocator/tail bounded roots or selects complete validation
  - Receipted V3 activation/rotation and refusal fixtures proving old inactive or complete V3
    recovery, exact V1/V2 immutability, no synthesized pre-activation tail, lineage-reset bootstrap
    after destructive restore, and refusal of unknown, partial, mismatched, exhausted, or oversized
    state before mutation
  - Retention implementation proving no Journal V1 suffix receipt source is reclaimed before its
    byte-identical materialized form is in a known-durable subsuming checkpoint, and no materialized
    history is reclaimed before a later subsuming checkpoint plus every follower, archive, and
    bootstrap fence permits it
  - Obligation proofs `replication_inventory_classifies_every_storage_namespace`,
    `successor_changelog_receipts_survive_checkpoint_and_recovery`, and
    `changelog_history_reclamation_respects_checkpoint_and_fences`, with process crash arms at
    allocator commit, journal flush, checkpoint materialization, rotation, and reclamation
  acceptance_commands:
  - ./scripts/acceptance --wp WP-772
  - cargo +1.97.0 test -p riffdb-storage-api -p riffdb-storage-redb -p riffdb-commit --all-features
  - ./scripts/recovery_full
  - ./scripts/ci-all
  exit_gate: Every current storage namespace has one reviewed authority class; journaled and
    direct/control-plane mutations yield one exact contiguous V3 receipt order through repeated
    crash, checkpoint, overwrite, delete, rotation, and reclaim edges; Journal V1 and application
    acknowledgement bytes and semantics remain unchanged; and WP-746 can consume the bounded
    published receipt cursor without making a durability decision.
  standing_design_tests:
    interface_safety: This package adds only internal storage formats and sealed receipt cursors;
      applications cannot select a class or format, inject a transition, bypass expected-state
      checks, force reclamation, or opt out of receipt durability.
    scale: Receipts are bounded by existing transaction ceilings, suffix materialization is folded
      into the existing checkpoint, and reclamation follows retained changed bytes and consumer
      fences rather than database population or process memory.
  human_review_triggers: &id0186
  - Any authoritative namespace would be omitted, reclassified as rebuildable, or represented by
    a lossy transition.
  - Application or administration sequence assignment, command atomicity, acknowledgement, or the
    single-writer property would change.
  - Journal V1 or changelog V1/V2 bytes, identity, tags, checksums, receipts, or interpretation
    would change.
  - The ADR-0207 exception's domain, ordered identities, Rust-type, numeric-version, magic,
    readable/writable/current, writer-policy, lifecycle, fixture-custody, or V3-only production
    bindings would drift, become configurable, or expand to another domain.
  - A suffix receipt source or materialized history could be reclaimed before a known-durable
    subsuming checkpoint and every replication fence permits it.
  - A public error, diagnostic, log, metric, or receipt would expose replicated keys, values,
    payload-derived hashes, capability material, or population cardinalities.

- id: WP-773
  name: Bounded worker-only columnar shutdown abandonment
  tier: guarantee
  depends_on: [WP-667, WP-668, WP-704]
  objective: Bound graceful columnar-worker termination at one existing 64-record page by
    abandoning only unpublished derived work after an internal stop request, while leaving
    ordinary catch-up publication, notification, checkpoint, freshness, and retention semantics
    unchanged and replaying from the last durable frontier after restart.
  requirements: [PRJ-001, PRJ-002, PRJ-003, PRJ-004, PERF-007, PERF-008, PERF-019]
  required_adrs: [ADR-0007, ADR-0049, ADR-0086, ADR-0156, ADR-0187]
  allowed_paths: [work_packages.yaml,
    adr/0187-bounded-columnar-shutdown-abandonment.md, adr/README.md,
    crates/riffdb-columnar/**, crates/riffdb-server/src/columnar_worker.rs,
    crates/riffdb-server/src/process_graph.rs, examples/app-baseline/**,
    benchmarks/**, tests/**, scripts/**, release/evidence/**, docs/**]
  deliverables:
  - One workspace-internal worker-only shutdown-aware catch-up entry owned by riffdb-columnar and
    not re-exported through application, SDK, transport, or operator surfaces. The server worker is
    its only production caller, owns the monotonic stop token, and may pass an internal read-only
    closure that observes it only after a complete existing page of at most 64 authoritative
    records and before the next page request. Ordinary no-stop apply retains its exact frozen fence,
    publication, and returned progress semantics while the worker retains its notification and
    checkpoint cadence; proof
    ordinary_columnar_apply_publication_and_checkpoint_are_unchanged
  - Stop observation returning one internal abandoned result without final publication,
    notification, checkpoint, or durable-frontier advance; any safe-prefix publication already
    completed under supersession holdback remains valid and only later unpublished in-memory
    working state is dropped; proof
    shutdown_abandons_only_unpublished_columnar_work_at_a_page_boundary
  - Restart and crash evidence proving replay begins from the prior durable projection frontier,
    remains idempotent, exposes only existing typed building or lagging outcomes, and converges to
    the same rows and frontier as uninterrupted catch-up; proof
    abandoned_columnar_shutdown_replays_from_the_durable_frontier
  - One fixed redaction-safe internal shutdown observation with exactly between_passes,
    abandoned_unpublished, and failed, carrying no path, identity, frontier, distance, row count,
    key, value, hash, credential, or caller-supplied control
  - Production-scale backlog and paired ordinary no-stop evidence proving shutdown completes after
    at most one page plus fixed join/reporting work and that catch-up, application latency and
    throughput, CPU, and memory retain the existing five-percent no-regression gates
  - Handbook and operational guidance stating that a clean process restart may replay derived
    columnar work from its durable frontier and temporarily return the existing typed freshness
    outcomes, without implying loss of authoritative data or a caller-selectable behavior
  acceptance_commands:
  - ./scripts/acceptance --wp WP-773
  - cargo +1.97.0 test -p riffdb-columnar -p riffdb-server --all-features
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/ci-all
  exit_gate: An explicit stop requested during production-scale catch-up is observed after at most
    one 64-record page; it publishes, notifies, and checkpoints nothing because of the stop and
    advances no durable frontier; any earlier safe-prefix publication remains exact; restart
    replays to the uninterrupted oracle; the closed diagnostic exposes no population or control
    value; ordinary no-stop semantics remain exact; and every paired five-percent lifecycle,
    latency, throughput, CPU, and memory gate passes.
  closure:
    status: complete
    completed_at: '2026-09-04'
    evidence: docs/performance/wp-773-columnar-shutdown.md
    decision: The worker-only stop is observed at the next existing 64-record page boundary and
      abandons only unpublished derived work. Deterministic and process-loss tests prove exact
      safe-prefix visibility, no stop-caused publication or durable-frontier advance, and replay
      convergence from the prior durable frontier. A valid counterbalanced three-repetition
      19,220-command production-profile run passed every five-percent lifecycle, request,
      throughput, CPU, RSS/PSS, and wall-time gate; columnar shutdown was 0.239x control and total
      lifecycle shutdown was 0.825x control.
  standing_design_tests:
    interface_safety: The server worker owns the stop token; the riffdb-columnar shutdown entry and
      result are workspace-internal and not re-exported through an application, SDK, transport, or
      operator surface. No application, agent, option, or configuration can select interruption,
      page size, publication, checkpoint, replay, freshness, or retention behavior.
    scale: Shutdown observes one token per existing 64-record page and retains no per-page history;
      after a request it completes at the next page boundary without scanning backlog,
      checkpointing the projection population, or holding state proportional to either.
  human_review_triggers: &id0187
  - A stop request would be observed inside a commit, or any partial commit could become visible
    or durable.
  - Ordinary no-stop catch-up would gain a different publication, notification, checkpoint,
    scan-fence, freshness, or retention behavior.
  - Shutdown abandonment would discard authoritative state, advance a durable frontier, or weaken
    restart replay and idempotency.
  - An application, transport, operator option, or configuration value could select interruption,
    page size, publication, checkpoint, or restart behavior.

- id: WP-774
  name: Bounded graceful validated-prefix checkpoint retention
  tier: guarantee
  depends_on: [WP-669, WP-704]
  objective: Implement ADR-0188 by removing graceful shutdown as a validated-prefix checkpoint
    creation or refresh point. After the required journal-suffix barrier, retain exact-current
    checkpoint state byte-for-byte, leave missing/stale/ineligible state untouched, keep CLEAN as
    the last transaction, and preserve unchanged exact dirty fallback and crash recovery.
  requirements: [STO-023, REC-001, REC-002, REC-004, PERF-007, PERF-013, PERF-019]
  required_adrs: [ADR-0019, ADR-0061, ADR-0085, ADR-0101, ADR-0104, ADR-0124,
    ADR-0156, ADR-0157, ADR-0188]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0188-bounded-graceful-checkpoint-retention.md, adr/README.md,
    crates/riffdb-storage-api/**, crates/riffdb-storage-redb/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, crates/riffdb-testkit/**, fixtures/**, tests/**,
    examples/app-baseline/**, scripts/**, docs/**]
  deliverables:
  - Sealed storage classifier returning only `RetainedExactCurrent`, `LeftAbsent`, `LeftStale`,
    `LeftIneligible`, or `ClassificationFailed` from fixed metadata, constant-time table
    cardinalities, and the existing process-generation witness after the durable suffix barrier
  - Graceful shutdown graph ordering of admission/drain, suffix recovery and empty-extent barrier,
    immutable checkpoint classification, bounded-root reread, and final Immediate CLEAN commit,
    with no checkpoint create/refresh/delete/repair and no database write after CLEAN
  - Exact-current retention proving canonical checkpoint metadata and every companion
    `validated_prefix_entity_heads` byte remain unchanged and no checkpoint transaction opens
  - Missing, stale, malformed, partial, unsupported, contradictory, and exhausted checkpoint
    fixtures proving shutdown performs no population scan or checkpoint mutation, may still close
    CLEAN after successful bounded classification, and leaves independent dirty verification exact
  - Closed non-durable receipt containing only the six disposition tags, four lifecycle outcome
    tags, and bounded saturating stage durations, with constructor rejection of impossible pairs
    and redaction tests covering logs, metrics, MCP text, CLI output, and public errors
  - Req-tagged proofs `graceful_close_retains_exact_current_checkpoint_byte_for_byte`,
    `graceful_close_leaves_unusable_checkpoint_state_unchanged`,
    `graceful_checkpoint_close_crash_matrix_preserves_dirty_fallback`, and
    `graceful_checkpoint_receipt_is_closed_and_redacted`, plus process-level crash arms before,
    during, and after barrier, classification, and CLEAN commit
  - Architecture and falsifiability checks proving `write_shutdown_validated_prefix_checkpoint`
    is absent, no shutdown path calls proof construction or checkpoint mutation, and startup after
    dirty termination still reaches the existing structural and catalog-semantic exact ends
  - Handbook and performance guidance documenting that graceful close preserves but does not earn
    validated-prefix proof, so a later dirty open may inspect a longer suffix or take the complete
    path while clean readiness remains independently bounded
  acceptance_commands:
  - ./scripts/acceptance --wp WP-774
  - cargo +1.97.0 test -p riffdb-storage-redb -p riffdb-server --all-features
  - ./scripts/recovery_full
  - cargo +1.97.0 test --manifest-path examples/app-baseline/Cargo.toml --workspace
  - ./scripts/ci-all
  exit_gate: Graceful shutdown performs the suffix barrier and one final CLEAN transaction without
    opening a checkpoint write or scanning population data; exact-current checkpoint state remains
    byte-identical, every other boundedly classifiable state remains absent or unchanged, every
    injected crash recovers through exact dirty fallback or a complete CLEAN successor, and all
    receipts and diagnostics remain closed and redacted.
  standing_design_tests:
    interface_safety: Checkpoint disposition and lifecycle outcomes are sealed internal values;
      no application, agent, operator argument, configuration, or transport can request refresh,
      preserve unsafe evidence, force CLEAN, or suppress dirty exact-end fallback.
    scale: The suffix barrier is already required, classification reads fixed metadata and
      constant-time engine cardinalities, and close performs no population walk, proof build,
      companion-table replacement, or checkpoint commit.
  closure:
    status: complete
    completed_at: "2026-09-02"
    requirements_satisfied: [STO-023, REC-001, REC-002, REC-004, PERF-007, PERF-013, PERF-019]
    evidence: Graceful close now classifies retained checkpoint state from one immutable bounded
      view after the journal-suffix barrier, retains every checkpoint byte unchanged, and performs
      exactly one independent final CLEAN transaction. Req-tagged exact-current, unusable-state,
      failure/crash-matrix, allocator, graph-ordering, and redaction proofs establish dirty exact
      fallback, closed diagnostic custody, and no population scan or checkpoint mutation.
  human_review_triggers: &id0188
  - Shutdown would create, refresh, delete, repair, or reinterpret a validated-prefix checkpoint.
  - Checkpoint classification would scan population rows or trust unbounded process-local state.
  - CLEAN could commit before the journal suffix is durably subsumed or any database write could
    follow it.
  - Dirty fallback would skip an exact end, trust unusable checkpoint state, mutate application
    authority, or advance an application or administration allocator.
  - A receipt, error, log, metric, or public surface would expose checkpoint contents or
    population-derived diagnostics.

- id: WP-775
  name: External binary-text interval execution receipt
  tier: surface
  depends_on: [WP-701]
  objective: Obtain and validate one fresh, value-free receipt from the owning consumer repository
    proving that a consumer pinned to the accepted RiffDB/generated identities compiles and executes
    one strict lower/upper binary_utf8_v1 named query in bytewise ascending order, returns the
    original logical string and reuses that consumer-owned value as its next request token, with no
    client filter/sort/page walk, duplicate index, database-owned token conversion, framework claim,
    or imported external schema/route/source/artifact/value.
  requirements: [OQ-061]
  required_adrs: [ADR-0055, ADR-0108, ADR-0112, ADR-0124, ADR-0129,
    ADR-0148, ADR-0150, ADR-0154, ADR-0193]
  allowed_paths: [work_packages.yaml,
    fixtures/riffql/wp775-external-binary-text-interval-execution-v1.json,
    scripts/generate-operational-query-capability,
    docs/getting-started/OPERATIONAL-QUERIES.md,
    docs/integrations/COMPILED-FRAMEWORK-PROFILES.md,
    docs/riffql/EXACT-PREDICATES.md]
  deliverables:
  - Fresh owning-repository receipt checksum-bound to exact consumer and RiffDB revisions and
    the immutable historical-receipt and V5 hashes, containing only bounded counts, timings,
    identity hashes, and closed verification facts
  - Fail-closed receipt validation proving actual strict-lower/strict-upper execution, bytewise
    ascending logical-string results, returned consumer-token reuse, and the absence of client
    shaping, token conversion, imported framework vocabulary, values, or parity claims
  - Terminal execution-v1 receipt status and handbook wording that distinguish this narrow external
    execution proof from framework or full-application conformance while preserving the historical
    source-only receipt and V5 capability fixture byte-for-byte
  acceptance_commands:
  - ./scripts/acceptance --wp WP-775
  - ./scripts/generate-operational-query-capability --check
  - ./scripts/check-generated
  - ./scripts/check-requirement-coverage
  - ./scripts/handbook check
  exit_gate: A fresh owning-repository receipt is checksum-bound to the exact consumer and RiffDB
    revisions plus the immutable historical-receipt and V5 hashes; it proves actual strict-lower/
    strict-upper execution, bytewise ascending logical-string results, returned-token reuse, finite
    page and work bounds, and absence of client shaping or token conversion; the repository validator
    fails closed on any missing execution, token, or checksum-binding fact; the fresh execution-v1
    receipt is terminal `passed` evidence while the historical receipt and V5 remain byte-exact;
    handbook wording accurately distinguishes this narrow proof from framework or full-application
    parity; and no external vocabulary or value enters RiffDB.
  closure:
    status: complete
    completed_at: "2026-09-04"
    requirements_satisfied: [OQ-061]
    evidence: fixtures/riffql/wp775-external-binary-text-interval-execution-v1.json
    decision: A fresh run at the checksum-bound consumer and RiffDB revisions executed one generated
      strict-lower/strict-upper binary-text query twice over one partition, returned four original
      logical strings in bytewise ascending order, and reused the first invocation's returned
      logical string as the second invocation's consumer-owned lower token. The terminal value-free
      receipt retains only bounded counts and duration, exact identity hashes, and closed facts;
      the validator rejects every missing or inverted execution, token, bound, shaping, and checksum
      fact while preserving the historical WP-701 receipt and V5 capability byte-exact.
  standing_design_tests:
    interface_safety: The receipt observes one compiler-generated named query and consumer-owned
      logical token only; it cannot authorize a physical comparator, profile, index, cursor,
      fallback, raw query, framework branch, or storage access.
    scale: The external run uses one partition, finite endpoints, bounded pages and operation counts,
      and retains no population-sized, duration-proportional, schema, route, artifact, or value data.
  human_review_triggers: *id001

- id: WP-777
  name: Demand-activated columnar runtime materialization
  tier: guarantee
  depends_on: [WP-704, WP-773, WP-774]
  objective: Implement ADR-0195 by keeping admitted columnar artifacts cold through core
    readiness and no-demand close, coalescing first semantic demand onto one server-owned
    activation worker, and installing only the exactly selected validated immutable view.
  requirements: [PERF-019, PRJ-002, PRJ-004, PRJ-008, PRJ-009,
    OQ-019, OQ-020, OQ-022, PERF-007, PERF-008]
  required_adrs: [ADR-0086, ADR-0156, ADR-0187, ADR-0190, ADR-0192, ADR-0195, ADR-0196]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0195-demand-activated-columnar-runtime-materialization.md,
    adr/0196-cold-columnar-query-outcome-alignment.md, adr/README.md,
    crates/riffdb-columnar/**, crates/riffdb-service/**, crates/riffdb-server/**,
    crates/riffdb-observability/**, crates/riffdb-testkit-server/**,
    fixtures/**, tests/**, scripts/**, docs/SUMMARY.md, docs/architecture/**,
    docs/operations/**, docs/performance/**, docs/known-limitations.md]
  deliverables:
  - One fields-private per-source Cold/Activating/Active/Failed/Stopped state machine retaining
    only bounded checked registration before demand, with no artifact open, decoded snapshot,
    catch-up, publication, checkpoint, or projection-population scan before readiness
  - One serialized server-owned activation worker and bounded wake path; concurrent first demand
    coalesces without caller tasks or retained requests and immediately returns the existing typed
    Building/unavailable result with no rows
  - Exact current-selection reread and complete bounded artifact validation before atomic immutable
    view installation, preserving ordinary apply, publication, checkpoint, freshness, cursor,
    policy, retention, and notification semantics after activation
  - Corruption, mismatch, incompatible bytes, limit refusal, and unavailable-storage tests proving
    typed rowless failure with no generation, layout, source, partition, frontier, V1, or
    authoritative fallback and no request-owned reopen loop
  - Deterministic barrier tests proving request cancellation does not cancel shared activation and
    shutdown joins the sole owner at an existing page boundary without stop-caused publication,
    checkpoint, notification, or frontier advance; cold close performs no projection I/O
  - Architecture and redaction tests proving no public activation/prewarm/eager-mode surface, one
    worker, read-only status/health observation, bounded diagnostics, and unchanged durable/public
    formats and transaction ownership
  - Handbook and performance guidance documenting first-demand Building behavior, derived-health
    degradation, retry expectations, clean lifecycle scope, and the unchanged post-demand
    PERF-007/PERF-008 resource gates
  - WP-705 referee integration recording cold-source and activation counts and proving zero
    activations and zero projection-population counters at the exact 65,536-row and canonical
    production-scale clean lifecycle checkpoints
  acceptance_commands:
  - ./scripts/acceptance --wp WP-777
  - cargo +1.97.0 test -p riffdb-columnar -p riffdb-service -p riffdb-server
    -p riffdb-testkit-server --all-features
  - ./scripts/handbook check
  - ./scripts/ci-all
  exit_gate: The exact configured production graph reaches core readiness and completes a no-demand
    graceful close without opening, decoding, applying, publishing, or checkpointing a columnar
    artifact; concurrent first demand creates one activation and returns only typed Building with
    no rows; only a fully validated current selection becomes queryable; every failure remains
    rowless without fallback; shutdown retains ADR-0187 abandonment; and both WP-705 lifecycle
    checkpoints pass their unchanged heap/time and zero-population-work gates.
  standing_design_tests:
    interface_safety: Activation is automatic on first semantic demand and server-owned; no
      application, agent, SDK, transport, operator, MCP, CLI, or configuration surface can select
      activation timing, bytes, generation, layout, fallback, validation, worker count, scheduling,
      or bounds, and no cold or failed source can return rows.
    scale: Startup retains at most 256 bounded registrations and one worker independent of entity
      or projection population; activation is serialized, uses existing page/segment/query bounds,
      and installs at most one engine per source without a duplicate in-memory generation.
  human_review_triggers: &id0195
  - A public or operator input could choose eager versus demand activation, skip validation, force
    readiness, select an artifact, or alter an activation bound.
  - A cold, activating, failed, mismatched, corrupt, partial, stale, excessive, or unknown source
    could serve rows or fall back to another source, generation, layout, partition set, or frontier.
  - More than one activation owner or thread could exist, first demand could block until population
    work completes, or request cancellation could cancel shared activation.
  - Startup or no-demand graceful close could open or decode a generation artifact, apply
    authoritative records, publish a view, write a checkpoint, or walk projection population.
  - Shutdown could interrupt inside a commit/page, publish or checkpoint because of stop, or fail
    to join the activation owner before storage closes.
  - A durable control, generation-root, manifest, segment, retention fence, public protocol,
    lifecycle enum, freshness outcome, or canonical identity would change.

- id: WP-778
  name: Fresh-process contiguous idempotency locator coverage
  tier: guarantee
  depends_on: [WP-491, WP-704, WP-774]
  objective: Implement ADR-0197 Decisions 1 through 15 with one storage-private proof system that arms only
    at the first mutation-gated command-write entry from exact empty authority, advances only across
    exact contiguous locator-bearing successors, and proves bounded novel-key absence without a
    history scan while preserving every existing idempotency and publication boundary.
  requirements: [OUT-001, OUT-002, TXN-042, REC-004, PERF-019]
  required_adrs: [ADR-0004, ADR-0006, ADR-0058, ADR-0100, ADR-0101, ADR-0104, ADR-0116,
    ADR-0156, ADR-0157, ADR-0165, ADR-0197]
  allowed_paths: [work_packages.yaml,
    adr/0165-durable-command-derived-locator-tables.md,
    adr/0197-fresh-process-contiguous-idempotency-locator-coverage.md, adr/README.md,
    crates/riffdb-storage-api/src/records.rs,
    crates/riffdb-storage-api/src/proto_codec/application.rs,
    crates/riffdb-storage-api/src/proto_codec/bounds.rs,
    crates/riffdb-storage-redb/**, crates/riffdb-commit/**, crates/riffdb-server/**,
    crates/riffdb-testkit-server/**, tests/storage_recovery/**, tests/command_semantics/**]
  deliverables:
  - One fields-private `Uninitialized | Disabled | PublicPrefixThrough | Rebinding` state whose sole
    initialization opportunity is `RedbOperationalPorts::admit_or_resolve_group`, immediately after
    its existing `begin_write()` and before `stage_admission_group`. The returned `RedbWriteAccess`
    writer-private transaction must prove application frontier `None`, allocator `Next(first)`, zero
    command authority in `COMMITS`, `IDEMPOTENCY`, `IDEMPOTENCY_PENDING`, and
    `IDEMPOTENCY_LOCATORS`, dormant transient indexes, and an unfenced handle. No new drain or order
    edge is added; `begin_deferred_epoch` and `BatchCore::open_with_access` are not activation sites.
    `None` is empty, raw `Some(0)` is corruption, and every exhausted, contradictory, or nonempty
    state disables coverage
  - Distinct fields-private move-only `PrivateChainWitness`, `CommandPublicationWitness`,
    `PreservePublicationWitness`, and `DirectCommandWitness` roles with no `Clone`, `Copy`,
    `Default`, serialization, public constructor, or cross-role conversion. Queued sealing A/B/C
    consumes private P/A/B and produces private A/B/C plus separate FIFO P-to-A/A-to-B/B-to-C
    publication witnesses. Queued service audit between A and B consumes private A, produces private
    A-audit plus FIFO preserve A-to-A-audit without advancing the application frontier, and B consumes
    private A-audit; publication consumes only its exact witness
  - Queued command-witness construction checks exact ADR-0104 composite predecessor/successor,
    inclusive
    `first = successor(predecessor)` and `count = last - first + 1`, segment/stage counts, and one
    canonical idempotency locator per sequence. Each locator and capsule must match sequence,
    database, environment, tenant scope, principal, contract lineage, command ID, and keyed
    caller-key digest; every prior locator remains readable across public and unpublished prefixes
  - Transaction-adjacent integration preserves the current result algebra and lookup order exactly;
    transient/unpublished/active-epoch member, durable locator point read and complete capsule and
    identity validation, then `Ok(None)` on locator miss. There is no write-side history fallback.
    A matching private witness determines only whether fresh coverage may survive and advance;
    missing or mismatched proof disables before the unchanged admission result proceeds
  - Operational direct-inspection integration accepts a locator miss without its existing bounded
    history scan only under public-prefix proof bound to the exact captured published view/frontier.
    Without proof the current scan and result/failure remain; malformed, duplicate, wrong-key,
    wrong-sequence, wrong-capsule, or wrong-identity evidence remains `CorruptData`
  - Direct/hardened audited publication constructs `DirectCommandWitness` only from a direct
    `RedbWriteAccess` with no composite stage, empty FIFO, matching public/private predecessor root,
    frontier and allocator, and an exact canonical audited segment, transaction-local count, span,
    locators, and identities. It consumes both predecessor roles before the existing Immediate
    commit and produces both successor roles only after exact commit/root refresh and expected
    frontier/allocator; it neither uses ADR-0104 staging nor adds an ordering edge
  - Exhaustive lane integration in which queued commands consume per-publication witnesses at the
    existing composite-successor/ADR-0100 edge and queued service audits consume preserve witnesses.
    Closed `PreservingImmediateClass` variants cover admission, execution failure, service audit,
    catalog, query/reactive module, capability bootstrap/grant/revoke, projection, outbox, consumer,
    columnar control, installation, and export. Each typed request creates an exact private
    `(table, encoded key, insert/delete)` permit before staging; every mutation is registered, and
    architecture checks reject raw/unregistered mutations. Permits exclude `COMMITS`,
    `IDEMPOTENCY_LOCATORS`, `PROVENANCE_LOCATORS`, `AUDIT_BY_REQUEST_LOCATORS`, and
    `META_APPLICATION_SEQUENCE`; bounded pre/post reads prove exact unchanged application frontier
    and allocator
  - A preserving Immediate lane with empty FIFO consumes both matching public and private roles plus
    its exact permit into an unusable pair token before commit. Definite precommit abort restores the
    predecessor pair; exact commit/root refresh restores both on the exact successor without clone,
    cross-role mint, or one-shot disable. Storage-format migration, contract
    migration/import, uncapsulated or unwitnessed command writes, restore/root replacement,
    unknown lanes, gaps, duplicates, fences, uncertainty, or dropped witnesses disable before
    visibility. Every offline retention hold/prune starts without inherited proof and drops its
    exclusive store state before the next store opens
  - Checkpoint/root rebase with no pending publication moves both roles into one pair token containing
    coverage epoch, both predecessor identities and Option frontiers under a short lock, then performs
    every wait and I/O without that lock. Definite prepublication failure restores the old pair;
    exact success restores both roles on the exact rebased successor without cross-role mint.
    Uncertainty, mismatch, partial handoff, lock poison, or postpublication failure disables and
    retains existing fencing; no new drain or ordering edge is added
  - Crash/close drops tokens; only a newly proved empty first command write may reinitialize after
    restart. State and fixed-size pending tokens remain bounded by the active epoch/publication
    queue, 256-transition, and 16-MiB frame ceilings and retain no population collection
  - Req-tagged deterministic proofs
    `fresh_locator_coverage_arms_at_first_command_write_only_from_exact_empty_authority`,
    `fresh_locator_private_chain_and_fifo_publications_are_linear`,
    `fresh_locator_write_miss_retains_current_semantics_and_gates_only_coverage`,
    `fresh_locator_miss_preserves_prior_identity_and_rejects_malformed_locators`, and
    `fresh_locator_coverage_classifies_every_publication_rebase_failure_and_restart`, covering
    `None`/raw zero/exhaustion, inclusive arithmetic, exact identity fields, prior locators, A/B/C
    and A-to-audit-to-B pipelining before A publication, wrong successors, witness loss, multiple
    private groups, direct access with no composite stage, same-successor Direct postcommit public-
    prefix operational-miss use and private-chain continuation, wrong-role/root/frontier/allocator
    negatives, every exact permit and raw/extra mutation rejection, every named preserving and
    disabling lane, suspended two-role Immediate rebind, 256 commands, 16 MiB, rebase
    success/failure/uncertainty, and before/after-publication kills with hooks/channels and no sleeps
  - Req-tagged proof `cold_fresh_database_publications_complete_without_history_scans` running at
    least 288 bounded concurrent unique publications through direct inspection and
    transaction-adjacent revalidation, reaching the exact final head with zero fallback history
    scans and zero transient-index rebuilds
  - Architecture proof that operational activation, checkpoint/certificate evidence, locator-table
    cardinality, and prior-process state cannot arm coverage; public audit and `SubscribeCommits`
    cannot consult either role; preserving lanes cannot mutate raw/unpermitted keys; ADR-0100
    receives the exact published snapshot; and no public,
    operator, durable, journal, Protobuf, storage-key, registry, conflict, ordering, batching,
    acknowledgement, or outcome surface changes
  - A narrow handoff authorizing WP-705 to rerun its separately owned 65,536-row and canonical
    production lifecycle qualification only after this package closes; no WP-705 report, closure,
    or WP-578 uninterrupted 72-hour evidence is created or claimed here
  acceptance_commands:
  - ./scripts/acceptance --wp WP-778
  - cargo +1.97.0 test -p riffdb-storage-redb --all-features
  - cargo +1.97.0 test -p riffdb-commit -p riffdb-server -p riffdb-testkit-server --all-features
  - cargo +1.97.0 test -p riffdb-storage-redb --test storage_recovery_matrix --all-features
  - ./scripts/recovery_full
  - ./scripts/ci-all
  exit_gate: The exact first mutation-gated command write proves empty authority and every accepted
    contiguous successor has distinct private-chain, direct or per-FIFO-publication, and published-prefix
    evidence; transaction-adjacent locator misses retain their exact current `Ok(None)` semantics
    with no invented fallback; 288 or more
    concurrent cold publications complete at the exact head with no history fallback scan or
    transient rebuild; all locator/identity/span mismatches, wrong successors, lanes, rebases,
    unpermitted mutations, uncertainties, failures, kills, and nonempty reopens preserve prior
    locators and fail closed; every preserving Immediate/checkpoint rebase restores both roles;
    ADR-0100 publication, exact idempotent replay, atomicity, transaction-adjacent validation,
    fencing, bounds, and lock-free checkpoint I/O remain unchanged; WP-705 and WP-578 evidence stay
    separately pending.
  closure:
    status: complete
    completed_at: "2026-09-05"
    evidence: Storage-private affine public, private, queued, direct, preserving, and rebase
      witnesses cover every exact contiguous locator-bearing successor and every protected
      mutation lane. Canonical anchored-event accounting now binds exact policy-anchor derivation
      and V2 envelope reservation; missing, extra, wrong-anchor, adversarial-size, locator,
      capsule, mutation-permit, failure, restart, and postpublication cases fail closed. The 288
      publication proof reaches the exact head with zero fallback scans or transient rebuilds. Raw
      zero and exhausted allocators, the exact 256-command frame, and exact 16-MiB verification
      input are covered with one-successor refusal. WP-778 acceptance, focused and full package
      suites, the nine-arm recovery gate, requirement coverage, ADR obligations and index, allowed
      paths, formatting, clippy, version topology, file-size guard, and panic allowances pass.
    decision: The optimization remains process-local and storage-private; no public, durable,
      protocol, storage-key, transaction-ordering, acknowledgement, or outcome surface changes.
      WP-705 fixed-scale qualification and WP-578 uninterrupted evidence remain separately pending.
  standing_design_tests:
    interface_safety: Coverage is storage-private, automatic, monotonic, and usable only after an
      exact locator miss; no application, agent, SDK, transport, MCP, CLI, operator, or
      configuration can arm, preserve, reset, observe, or bypass it, select a fallback, or suppress
      transaction-adjacent idempotency validation.
    scale: State is fixed public/private identities, epoch, and closed tag; fixed-size tokens are bounded by the
      existing active epoch/publication queue and 256-transition/16-MiB frame limits and retain no
      per-command value after publication; novel-key reads perform fixed point work and no retained
      history, locator-table, or checkpoint population scan.
  human_review_triggers: &id0197
  - Coverage would initialize outside the first command-write mutation gate, from operational
    activation, a checkpoint/certificate, table cardinality, nonempty history, or prior-process state.
  - Operational locator absence would be accepted without exact public-prefix proof, a private or
    publication witness could be forged/duplicated/reused, prior locators could disappear, or
    malformed identity/span/successor evidence could avoid CorruptData.
  - Transaction ordering, conflict ownership, transaction-adjacent revalidation, batching,
    acknowledgement, outcome sequencing, write fencing, or public successor ordering would change.
  - Checkpoint I/O would hold a transient lock, a rebase comparison could be skipped, ADR-0100's
    publication snapshot/edge would change, or an unclassified application lane could preserve proof.
  - Public audit or `SubscribeCommits` reconstruction, any public or operator surface, any durable
    byte or identity, or any WP-705 or WP-578 evidence or closure state would change.

- id: WP-779
  name: Generated MCP catalog descriptor parity
  tier: surface
  depends_on: [WP-137, WP-140, WP-701, WP-702]
  objective: Implement ADR-0198 Decisions 1 through 10 by adding a strict V6 successor for nonempty
    command registries, composing exact compiler command schemas with the accepted service envelope,
    and streaming an independently authorized expected descriptor set against hosted discovery.
  requirements: [MCP-001, MCP-020, MCP-021, MCP-026, MCP-040, MCP-043, MCP-045,
    DX-044, DX-047, DX-049, VER-001, VER-002, VER-003, VER-004, VER-008]
  required_adrs: [ADR-0198, ADR-0008, ADR-0020, ADR-0040, ADR-0047, ADR-0064,
    ADR-0124, ADR-0155, ADR-0194]
  allowed_paths: [SPEC.md, work_packages.yaml, Cargo.toml, Cargo.lock, adr/README.md,
    adr/0198-generated-mcp-catalog-descriptor-parity.md,
    release/version-topology-v1.json,
    examples/agent-alpha/generated/mcp/tools.json,
    examples/agent-alpha/riffdb.application.lock.json,
    examples/ticketdesk/generated/mcp/tools.json,
    examples/ticketdesk/riffdb.application.lock.json,
    crates/riffdb-query-module/**,
    crates/riffdb-api-mcp/**, crates/riffdb-cli/**, crates/riffdb-testkit-server/**,
    templates/**, fixtures/**, tests/**, scripts/**, docs/**]
  deliverables:
  - One strict `riffdb-generated-application-operations/v6` successor selected exactly when the
    post-reimport-exclusion command registry is nonempty, recording the selected V2-V5 predecessor
    and retaining `application_manifest_hash`, tools, commands, reactive tools, applicable SDK/vector
    registries, entry bytes, merge rules, and order while adding only the closed V6 identity and
    descriptor members
  - V6 `mcp_identity` with exactly required `command_id`, `composed_output`, `input_schema`,
    `operation_envelope`, `outcome_schema`, `tool_name` in canonical order; closed compiler children
    require `command_id`, `key`, `schema_hash`, `schema_id`, other children require `schema_hash`,
    `schema_id`, and no child permits extras; nonzero u32 JSON command ID; exact ADR-0064 underscore
    `McpCommandToolNameV2`; UTF-8 JSON-string names/keys/IDs, exact `command_input` and
    `command_outcome_union` keys; exact `riffdb.generated-schema/command-input/{id}/v1` and
    `riffdb.generated-schema/command-outcome-union/{id}/v1` IDs; canonical bodies and 64-lowercase-hex
    hashes; exact envelope ID/hash/body; and composed
    `riffdb.command-operation-envelope/v1+compiler-outcome` ID/body/hash, all cross-bound to one
    command under ADR-0020 normalization/stable-ID order; dotted names, stale/rebound keys, outcome
    `$defs`, and every cross-command substitution reject
  - Exact serialized `mcp_descriptor` root keys `name`, `inputSchema`, `outputSchema`, `annotations`;
    exact four annotation keys/booleans; and structural absence of title, description,
    `annotations.title`, execution, icons, `_meta`, and every unknown optional for commands and
    named queries
  - One bounded pure MCP-common composition/projection owner reused by hosted descriptors and CLI
    artifact assembly; the explicitly reviewed default-feature-disabled `riffdb-cli` to
    `riffdb-api-mcp` Cargo edge enables no stdio/HTTP, service, or auth feature, adds no duplicate
    source/composer, and creates no compiler/query-module-to-MCP/service dependency
  - Req-tagged proof `generated_mcp_command_catalog_v6_composes_authoritative_schemas` covering
    zero/one/many command selection, every predecessor profile, exact retained root/registry/entry
    structure, representative/maximum nested schemas, all identity bindings, mismatched envelope
    ID/hash/body, duplicate/missing placeholder, noncanonical input, stale/rebound compiler schemas,
    wrong output hash, raw-outcome-as-output refusal, and every one-byte/depth/node/aggregate excess
  - Req-tagged process proof `generated_mcp_catalog_matches_complete_hosted_descriptor_pages`
    deploying the exact lock and keeping the generated application-role artifact as candidate input,
    never authority; a separate closed `CapabilityGrantV1` uses `TenantScope::Global`,
    `PartitionScopeV1::All`, empty field visibility, max rows 65535, no approval-required kinds,
    `None` row-policy/export/reimport/vector-inspection extensions, no wildcard/admin, exact
    lineage/command and lineage/module-hash/query permissions, and one valid parameterized or
    unparameterized witness for every fixed kind; fixed expected bytes come from `riffdb-api-mcp`'s
    registry and all visibility comes from policy's fixed/dynamic candidate-to-permission mappings,
    before streaming full descriptor/schema pages through the exact terminal cursor
  - Adversarial full-discovery cases for missing, extra, duplicate, reordered, malformed,
    schema/metadata-different, fixed/dynamic-colliding, and dynamic/dynamic-colliding tools; limit 500,
    fewer byte-fitted items, independent 4-MiB per-page ledgers, schema bounds, nonempty continuation,
    canonical/new/bound cursors, loops, excess pages/items, and continuation after the final expected
    item, with one current descriptor/page retained and no aggregate full-schema inventory
  - Separate compact-watcher regression preserving only its accepted three-call 500/500/24 sequence,
    1,024-fingerprint retention ceiling, and item-1,025/cursor-after-1,024 refusal; compact hashes or
    those bounds cannot substitute for the full-schema parity proof
  - Req-tagged proof `generated_mcp_catalog_v6_topology_and_lock_migration_are_exact` freezing V2-V6
    source locators, readers/writers/current identities, exact nonempty-command writer predicate,
    predecessor preservation, Cargo feature/lock graph, reviewed fixture hashes, explicit migration
    preview/acceptance, V8 application-lock hash rotation, old exact artifact validation, and old
    catalogs' lack of a parity claim
  - Updated MCP/generated-operation fixtures and handbook compatibility guidance, with exact human
    review of every changed catalog, composed schema, artifact hash, and application-lock identity;
    hosted runtime, authorization, dispatch, execution, notifications, protocol, Protobuf, IR,
    bundle, plan, durable, and storage semantics remain unchanged
  acceptance_commands:
  - ./scripts/acceptance --wp WP-779
  - cargo +1.97.0 test -p riffdb-query-module -p riffdb-api-mcp -p riffdb-cli
    -p riffdb-testkit-server --all-features
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/check-workspace-policy
  - ./scripts/driver-conformance --all-languages --remote
  - ./scripts/mcp-inspector-smoke
  - ./scripts/handbook check
  - cargo +1.97.0 fmt --all -- --check
  - cargo +1.97.0 clippy -p riffdb-query-module -p riffdb-api-mcp -p riffdb-cli
    -p riffdb-testkit-server --all-targets --all-features -- -D warnings
  - ./scripts/ci-all
  exit_gate: Every nonempty-command MCP artifact is a strict V6 successor with exact current command
    identities and closed descriptors; its independently authorized expected set streams equal to
    every bounded full-schema hosted page through the terminal cursor; all owner, key, ID, hash,
    metadata, authority, collision, order, page, cursor, dependency, artifact, and lock mismatches
    fail closed; compact watching stays separate; and no hosted runtime semantic changes.
  standing_design_tests:
    interface_safety: Generated catalogs are deterministic expectation evidence only; no application,
      agent, SDK, transport, role, or configuration can supply a schema or envelope, alter descriptor
      metadata, grant or hide authority, dispatch a tool, or request a raw-outcome fallback.
    scale: Each schema is composed once under existing checked schema/artifact ceilings; full parity
      streams one independently 4-MiB-bounded page and current descriptor at a time through terminal
      cursor, while only the separate compact watcher retains at most 1,024 fingerprints.
  human_review_triggers: *id001
- id: WP-780
  name: ADR-0183 freeze exception reconciliation
  tier: guarantee
  depends_on: [WP-745]
  objective: After exact acceptance of ADR-0199, reconcile ADR-0183, PERF-020, and the named
    work-package contracts so the freeze seals every pre-freeze PERF declaration, permits only
    WP-750/WP-760 prerequisite closure plus WP-762/WP-763, and retains a separately accepted lift.
  required_adrs: [ADR-0199, ADR-0178, ADR-0182, ADR-0183]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0183-performance-package-freeze-and-durable-group-lever.md,
    adr/0199-performance-freeze-exceptions-for-named-lift-prerequisites.md,
    adr/README.md]
  deliverables:
  - Exact human acceptance of ADR-0199 followed by matching replacement text in ADR-0183
    OBL-0183-1 and Decision 1, SPEC PERF-020, and the WP-750/WP-760/WP-762/WP-763 package notes;
    the proposed record alone changes no accepted authority
  - One closed freeze-record contract for WP-762 that seals normalized definition digests for
    every then-open PERF package, treats unchanged non-exempt declarations as inert, and names
    only WP-750, WP-760, WP-762, and WP-763 as packages permitted to advance
  - Exact WP-750 and WP-760 exception text allowing only their existing non-candidate objectives,
    existing PERF evidence, and a closure field, with no new PERF requirement, candidate,
    threshold, admitted package, or direct freeze lift
  - Reconciled WP-762 checker acceptance contract requiring check-performance-freeze --self-test
    arms for a new PERF package, inert-package edit and closure, exact prerequisite closures,
    widened prerequisite, fifth exception, and premature lift; checker implementation remains
    owned by WP-762 and is not part of this package
  - Governance diff review proving all ADR-0183 performance thresholds, evidence identities,
    activation arithmetic, lift timing, runtime paths, and public surfaces are unchanged
  acceptance_commands:
  - ./scripts/acceptance --wp WP-780
  - ./scripts/check-adr-obligations
  - ./scripts/check-requirement-coverage
  - ./scripts/adr-index --check
  exit_gate: ADR-0199 is exactly accepted and every authoritative freeze statement agrees on one
    sealed pre-freeze inventory, four exact advancing packages, closure-only non-candidate authority
    for WP-750/WP-760, and lift only after both close through a later accepted ADR; no checker,
    threshold, receipt, runtime, or public surface changes in this package.
  closure:
    status: complete
    completed_at: "2026-09-05"
    evidence: Accepted ADR-0199 is reconciled into ADR-0183 OBL-0183-1 and Decision 1, SPEC
      PERF-020, and the WP-750/WP-760/WP-762/WP-763 contracts. The common text seals every
      pre-freeze PERF-package definition with the exact RFC 8785 JCS and SHA-256 rule, permits
      exactly four named transitions, limits WP-750/WP-760 to their existing non-candidate work
      and a closure, and retains lifting for a later separately accepted ADR after both close.
    decision: Governance-only reconciliation; performance thresholds, evidence identities,
      activation arithmetic, lift timing, runtime paths, durable behavior, and public surfaces
      remain unchanged.
  standing_design_tests:
    interface_safety: Repository governance gains no application or operator surface, and no caller
      can select an exception, performance candidate, threshold, durability rule, or lift state.
    scale: The future check hashes one finite work-package manifest and stores bounded identity
      metadata; it reads no database state and assumes no co-located runtime or full-state rewrite.
  human_review_triggers: &id0199
  - A fifth package would be permitted to advance while the freeze is in force, or an inert
    pre-freeze PERF package would change or close.
  - WP-750 or WP-760 would add a PERF requirement, activate a performance candidate, alter a
    threshold, admit another package, or lift the freeze.
  - The freeze would start without the WP-762 bank, or lift before WP-750 and WP-760 close or
    without a separately accepted ADR.
- id: WP-781
  name: Columnar history-incarnation reset authority reconciliation
  tier: guarantee
  depends_on: [WP-776]
  objective: After exact acceptance of ADR-0200, reconcile ADR-0192 and WP-711 so restored stale
    tag-67 controls can reach one current-incarnation unprepared V1 candidate without relabelling,
    bypassing the control CAS, changing restore, or widening any public or durable surface.
  requirements: [REC-001, PRJ-004, PRJ-006, PRJ-008, PRJ-009, PRJ-010]
  required_adrs: [ADR-0072, ADR-0160, ADR-0190, ADR-0192, ADR-0200]
  allowed_paths: [work_packages.yaml,
    adr/0192-compiler-bound-columnar-publication-control.md,
    adr/0200-columnar-control-reset-for-history-incarnation.md, adr/README.md]
  deliverables:
  - Exact human acceptance of ADR-0200 followed by matching amendment text in ADR-0192 Decisions
    11 through 16; Decision 17 remains unchanged and the proposed record alone changes no accepted
    authority
  - One closed ResetForCurrentHistoryIncarnation contract that consumes exact expected control,
    reads transaction-current authoritative history metadata internally, accepts no caller-selected
    identity, and produces only the exact fresh Building/Unprepared-V1/BeforeFirst shape
  - WP-711 dependency, allowed-path, obligation, crash-matrix, and acceptance-plan reconciliation
    for all current, stale, future, mixed, malformed, exhausted, Applied, mismatch, failure,
    unknown-commit, repeated-restart, path-collision, stale-plus-spec-drift, no-serving,
    no-retention, rebuild, publication, and acknowledgement arms
  - Governance diff proof that durable encodings, registry and storage keys, restore mechanics,
    application/administration sequencing, acknowledgements, public surfaces, and ordinary current-
    incarnation control transitions remain unchanged; only bounded derived cleanup quarantine names
    and reset-scoped cross-incarnation generation identity are added
  acceptance_commands:
  - ./scripts/acceptance --wp WP-781
  - ./scripts/check-adr-obligations
  - ./scripts/check-requirement-coverage
  - ./scripts/adr-index --check
  exit_gate: Accepted ADR-0200, ADR-0192, and WP-711 agree on exactly one transaction-current stale-
    incarnation reset and bounded exact-path retirement; every other state refuses or uses its
    existing transition, no stale pointer serves or fences retention, and no authoritative format,
    restore, runtime gate ordering, or public contract changes in this governance package.
  closure:
    status: complete
    completed_at: "2026-09-05"
    evidence: Exact ADR-0200 acceptance is recorded without changing its decision body. ADR-0192
      Decisions 11 through 16 now carry the same closed reset, retention, startup ordering,
      repository operation, collision-retirement, witness, and all-result recovery rules while
      Decision 17 remains unchanged. WP-711 depends on this package, requires ADR-0200, carries its
      permitted record path, all five proof obligations, complete reset/path crash matrix, and
      package/obligation acceptance checks. WP-781 acceptance, requirement coverage, ADR
      obligations, index generation, and allowed-path checks pass.
    decision: Governance reconciliation only. Durable encodings, registry and storage keys,
      destructive restore, application/administration sequences, acknowledgement rules, runtime
      gate ordering, ordinary current-incarnation transitions, and public surfaces remain
      unchanged; runtime implementation remains owned by WP-711.
  standing_design_tests:
    interface_safety: Repository governance adds no application or operator surface and gives no
      caller authority to choose an incarnation, mutation, repair target, or fallback.
    scale: Startup reconciles at most 256 controls with one bounded metadata read and exact CAS each;
      the subsequent rebuild retains every existing streaming and resource ceiling.
  human_review_triggers: &id0200
  - A stale pointer could serve, contribute a retention frontier, or be relabelled current.
  - A caller could choose the replacement incarnation, reset a current or mixed-incarnation control,
    delete the control outside its repository, or change authoritative application state.
  - Collision handling would scan a parent directory, recurse without the frozen bounds, reuse an
    artifact or same-incarnation generation, delete without a reference proof, touch any path other
    than the exact final-candidate/quarantine pairs, or open a gate early.
  - A durable encoding, registry, storage key, restore operation, application/administration
    sequence, acknowledgement rule, runtime gate ordering, or public surface would change.
- id: WP-782
  name: Exact adapter evidence-class governance reconciliation
  tier: surface
  depends_on: [WP-569, WP-573]
  objective: After exact acceptance of ADR-0201, reconcile accepted authority so every alpha
    adapter claim states exactly what its evidence proves, without changing runtime, public,
    durable, protocol, storage, authorization, transaction, threshold, or evidence behavior.
  requirements: []
  required_adrs: [ADR-0201, ADR-0110, ADR-0117, ADR-0148]
  allowed_paths: [SPEC.md, work_packages.yaml,
    adr/0110-application-installation-and-adapter-conformance.md,
    adr/0117-compiled-external-framework-profiles.md,
    adr/0201-exact-adapter-evidence-classifications.md, adr/README.md]
  deliverables:
  - Exact human acceptance of ADR-0201 followed by matching evidence-class text in ADR-0110
    Amendment 1, ADR-0117's current-alpha versus future-support boundary, every named SPEC claim
    listed by OBL-0201-2, milestone text, and WP-724/WP-578/WP-579; the proposed record alone changes
    no accepted authority
  - WP-724 remains surface tier, depends on WP-782, requires ADR-0201, owns all seventeen requirements
    whose claim guard it proves, and retains only its fixture, documentation, script, and SPEC paths;
    WP-578 depends on WP-724, while WP-578 and WP-579 both require ADR-0201
  - Exact ten-record `riffdb.adapter-evidence-inventory/v1` contract at
    fixtures/adapters/adapter-evidence-v1.json, preserving four general-alpha classifications plus
    Better Auth lifecycle/admin, MLflow filtered/partition/large-Run, and post-alpha Payload claims
    at their separate exact scopes and source locators
  - OBL-0201-1 through OBL-0201-3 remain owned by WP-724, whose later implementation must add the
    canonical inventory, exact claim guard, requirement tags, normative-claim completeness test,
    adversarial self-test, handbook updates, and human fixture review; WP-782 does not implement or
    close those obligations
  - Governance diff proof that no runtime/public/durable/protocol/storage/authorization/transaction
    behavior, evidence bytes or result, acceptance threshold, adapter implementation, or WP-578
    72-hour receipt changes
  acceptance_commands:
  - ./scripts/acceptance --wp WP-782
  - ./scripts/check-allowed-paths --wp WP-782
  - ./scripts/check-adr-obligations
  - ./scripts/check-requirement-coverage
  - ./scripts/adr-index --check
  exit_gate: Accepted authority agrees on the ten scoped claim records, current alpha claims no
    nonexistent upstream suite, completed external partial-profile receipts remain exact, future
    Better Auth support still requires ADR-0117 evidence, WP-724 owns all three proof obligations,
    and the diff contains only governance reconciliation paths.
  closure:
    status: complete
    completed_at: "2026-09-05"
    evidence: Accepted ADR-0201 and reconciled ADR-0110 Amendment 1, ADR-0117's current-alpha versus
      future-support boundary, every OBL-0201-2 SPEC claim, the alpha milestone, and WP-724/WP-578/
      WP-579 ownership and dependency text. Package acceptance, allowed-path, ADR-obligation,
      requirement-coverage, and ADR-index checks pass without running or changing evidence.
    decision: Governance now classifies the fixed ten scoped adapter claims exactly; only OpenFGA's
      general record is a complete upstream suite, narrower Better Auth and MLflow receipts remain
      intact, Payload remains post-alpha, and WP-724 retains all inventory/checker/proof work. No
      runtime, public, durable, protocol, storage, authorization, transaction, threshold, evidence,
      or WP-578 72-hour behavior changed.
  standing_design_tests:
    interface_safety: Governance labels add no public or operator surface, caller choice, generic
      mutation, transaction callback, runtime fallback, or way to claim a stronger guarantee.
    scale: Reconciliation covers one fixed ten-record inventory and bounded text; it reads no
      database, runtime, adapter-repository, duration-sized, or population-sized state.
  human_review_triggers: &id0201
  - Any inventory path/schema, claim ID, subject, scope, class, custody, upstream-suite flag, source
    locator, named requirement, or release-gate wording would differ from ADR-0201 or change without
    human fixture review.
  - Better Auth would be described beyond its exact materialized profile, its profile-bound generic
    facade would be mistaken for generic RiffDB CRUD, or it would gain dynamic schema, uncompiled
    transactions, field omission, or runtime fallback.
  - Any completed MLflow or Better Auth external, RiffDB-live, or matrix evidence would be erased,
    widened, or presented as complete upstream-suite conformance.
  - Runtime/public/durable/protocol/storage/authorization/transaction behavior, evidence artifacts
    or results, acceptance thresholds, adapter code, or the WP-578 72-hour execution would change.

- id: WP-783
  name: Published Helm two-version upgrade evidence
  tier: surface
  depends_on: [WP-727, WP-728]
  objective: Implement ADR-0202 Decision 7 after publication exists by executing the accepted
    operator runbook against two distinct immutable published chart versions and retaining one
    bounded redacted receipt that cannot be satisfied by local or draft artifacts.
  requirements: [NET-010, NET-012, APE-002]
  required_adrs: [ADR-0202, ADR-0055, ADR-0056, ADR-0105, ADR-0110, ADR-0112,
    ADR-0124]
  allowed_paths: [work_packages.yaml, release/evidence/wp-783/**,
    scripts/check-published-helm-upgrade-evidence,
    scripts/run-published-helm-upgrade-evidence, docs/operations/**, docs/**,
    .github/workflows/**]
  deliverables:
  - Sole bounded `riffdb.published-helm-upgrade-evidence/v1` receipt at
    `release/evidence/wp-783/published-helm-upgrade-v1.json` with exact root keys `schema`, `source`,
    `target`, `execution`, `status`; source and target each bind exact provider/repository/release/asset
    IDs, publication state/time, release/chart/app versions, source revision, archive/image identities,
    and WP-728's existing signed-artifact provenance and documented consumer-verification outputs
  - WP-783-owned checker uses read-only exact-ID provider queries to compare each WP-728 artifact and
    consumer-verification output with authoritative immutable published non-draft provider records;
    it consumes only WP-728's existing promised outputs and makes no external mutation
  - Independently anchored cluster evidence binds the exact reviewed harness/workflow paths, revision,
    content hashes, external workflow-run identity, signature/provenance, and every terminal result
    digest; hand-authored receipt fields, URLs, local claims, or unanchored logs grant nothing
  - Actual source-chart install, closed payload-free unauthenticated liveness, authenticated readiness,
    and stable-application access from a separate pod through service DNS and the pass-through proxy;
    an allowed-partner/refused-untrusted probe verifies the controlled-design-partner-network boundary
    before drain, verified backup, stopped-database candidate preflight, only the manifest-authorized
    action, target-chart upgrade, authenticated readiness, and application traffic readmission
  - Actual certificate reload and successor-prove-switch-revoke credential rotation observations,
    bounded shutdown, backup/restore, and the runbook's exact safe rollback result whose
    deployment-object rollback is allowed only before candidate storage access, otherwise requiring
    forward recovery or verified destructive restore while unsupported binary/database downgrade
    remains refused
  - Strict validator and negative self-test reject each missing/mismatched provider, repository,
    release, or asset ID; draft/mutable/unpublished state or invalid timestamp; unsigned,
    unprovenanced, or consumer-unverified WP-728 artifact; equal release/chart versions, archives,
    source revisions, appVersions, or intended image digests; appVersion/image mismatch; and cluster
    evidence lacking an external run anchor, reviewed harness/workflow binding, terminal result
    digests, signature, or provenance; truthful runbook/durable-manifest digests may be identical
  - Negative cases also reject missing separate-pod stable-application access, non-payload-free
    liveness, absent allowed/refused network probes, hand-edited manifests, loopback/host networking,
    unbounded settings, missing preflight/backup/operation results, prose-only claims, and secret/value
    leakage
  acceptance_commands:
  - ./scripts/acceptance --wp WP-783
  - ./scripts/run-published-helm-upgrade-evidence --self-test
  - ./scripts/check-published-helm-upgrade-evidence --self-test
  - ./scripts/check-published-helm-upgrade-evidence
  - ./scripts/check-helm-operator-package
  - ./scripts/check-helm-render
  - ./scripts/check-version-topology
  - ./scripts/check-generated
  - ./scripts/handbook check
  - ./scripts/ci-all
  exit_gate: WP-783's checker independently matches WP-728 signed artifacts and consumer-verification
    outputs to authoritative exact-ID provider records proving two distinct immutable published
    non-draft chart assets and revisions/appVersion-bound images, then cross-validates independently
    anchored signed/provenanced execution from the reviewed harness/workflow; the controlled cluster
    proves payload-free liveness, authenticated readiness, allowed/refused network boundaries,
    separate-pod stable-application proxy access, drain, verified backup, stopped-database preflight,
    target upgrade, both rotations, bounded shutdown, restore, and safe rollback/refusal with terminal
    result digests; shared truthful runbook or durable-manifest digests may match, and no local,
    draft, URL-only, unanchored, or hand-authored claim substitutes.
  standing_design_tests:
    interface_safety: Evidence observes existing operator and application-service operations only;
      it grants no authority, carries no bearer or data value, enables no application/MCP install,
      and cannot make downgrade, force, reset, raw storage, or handwritten deployment safe.
    scale: The campaign uses two chart archives, one controlled cluster, finite operation attempts,
      existing runtime bounds, bounded timestamps/counts/digests, and one fixed-size receipt; it
      retains no request stream, database contents, cluster inventory, or duration-proportional log.
  human_review_triggers: &id0202
  - Either WP-728 signed artifact or consumer-verification output would not match an authoritative
    immutable published non-draft provider record queried by exact repository/release/asset ID, or
    WP-728 would be broadened beyond its existing artifact/provenance and consumer-verification promises.
  - Cluster execution is not independently anchored to the exact reviewed harness/workflow, external
    run, terminal result digests, signature, and provenance.
  - Separate-pod stable-application proxy access, closed payload-free liveness, or the verified
    allowed-partner/refused-untrusted controlled-network boundary would be absent or prose-only.
  - Release/chart version, archive, source revision, appVersion, or intended image distinctness would
    weaken, or truthful common runbook/durable-manifest digests would be forced to differ.
  - A local render, draft/mutable artifact, URL, hand-authored receipt, operator assertion, loopback,
    host network, secret/value-bearing evidence, or unsafe rollback/downgrade could close the package.
