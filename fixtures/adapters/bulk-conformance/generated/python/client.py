# @generated by riffdb-query-module; do not edit.
from __future__ import annotations

from collections.abc import Callable, Sequence
from dataclasses import dataclass, field
from decimal import Decimal
from enum import StrEnum
from typing import Annotated, Final, Literal, TypeAlias
from uuid import UUID

from riffdb_application import (
AsyncApplicationTransport, AttemptBudget, CommandBatchOptions,
CommandBatchProgress, CommandBatchResult, Money, QueryOptions, RiffDate,
SyncApplicationTransport, Timestamp, TypedCommandResult, TypedQueryResult, WorkflowSuccessorRevision,
)
from riffdb_application._binding import decode_variant, encode_record, canonical_value_encoded_length, encode_value

def _compact_tag(value: object, tag: str, keys: frozenset[str]) -> dict[str, object]:
    if not isinstance(value, dict) or value.get("$riffdb") != tag or frozenset(value) != keys:
        raise ValueError("invalid RiffDB compact value")
    return value

CONTRACT_LINEAGE: Final[str] = "AdapterBulkConformance"
CONTRACT_VERSION: Final[int] = 1
CONTRACT_BUNDLE_HASH: Final[str] = "f01d15f5388a91e74ca01d5a36a35aefd5d0faf4e525480c82b7eef142ab5315"
QUERY_MODULE_HASH: Final[str] = "037c90a0762bb68c521a2e9f9ff30056c43b0f86df7f19fe9ce8fbc83c05fc45"

@dataclass(frozen=True, slots=True, kw_only=True)
class Metric:
    name: str
    step: Annotated[int, "i64"]
    metric_id: UUID
    value_micros: Annotated[int, "i64"]
    experiment_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class Document:
    title: str
    site_id: UUID
    document_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class FgaTuple:
    object: str
    subject: str
    relation: str
    store_id: UUID
    tuple_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class Pipeline:
    name: str
    pipeline_id: UUID
    organization_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class PipelineStep:
    step_id: UUID
    run_text: str
    pipeline_id: UUID
    organization_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class RestrictChild:
    child_id: UUID
    parent_id: UUID
    tenant_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class PolicyMutation:
    context: bytes | None
    relation: str
    mutation_id: UUID
    organization_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class RestrictParent:
    parent_id: UUID
    tenant_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class DocumentRevision:
    body: str
    site_id: UUID
    document_id: UUID
    revision_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class DocumentGraphInput:
    body: str
    title: str
    site_id: UUID
    document_id: UUID
    revision_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class PipelineGraphInput:
    name: str
    step_id: UUID
    run_text: str
    pipeline_id: UUID
    organization_id: UUID

GET_FGA_TUPLE_QUERY_PLAN_HASH: Final[str] = "3ea9d730dc275ffbbf2b9fc1770baedd82b77778f1a5c13ccce047207bbc3a13"

@dataclass(frozen=True, slots=True, kw_only=True)
class GetFgaTupleParams:
    store_id: UUID
    tuple_id: UUID

@dataclass(frozen=True, slots=True, kw_only=True)
class GetFgaTupleTupleFoundTuple:
    store_id: UUID
    tuple_id: UUID
    object: str
    relation: str
    subject: str

@dataclass(frozen=True, slots=True, kw_only=True)
class GetFgaTupleTupleFound:
    tuple: GetFgaTupleTupleFoundTuple
    outcome: Literal["TupleFound"] = field(default="TupleFound", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class GetFgaTupleTupleNotFound:
    outcome: Literal["TupleNotFound"] = field(default="TupleNotFound", init=False)

GetFgaTupleResult: TypeAlias = GetFgaTupleTupleFound | GetFgaTupleTupleNotFound

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateDocumentGraphsInput:
    documents: tuple[DocumentGraphInput, ...]
    request_id: UUID

CREATE_DOCUMENT_GRAPHS_PLAN_HASH: Final[str] = "94d2e95cffe5b23eb5ee7eaede65de0fd6b6517b7121b92d740c41a633ae6fdd"
@dataclass(frozen=True, slots=True, kw_only=True)
class CreateDocumentGraphsDocumentAlreadyExists:
    outcome: Literal["DocumentAlreadyExists"] = field(default="DocumentAlreadyExists", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateDocumentGraphsDocumentGraphsCreated:
    outcome: Literal["DocumentGraphsCreated"] = field(default="DocumentGraphsCreated", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateDocumentGraphsRevisionAlreadyExists:
    outcome: Literal["RevisionAlreadyExists"] = field(default="RevisionAlreadyExists", init=False)

CreateDocumentGraphsOutcome: TypeAlias = CreateDocumentGraphsDocumentAlreadyExists | CreateDocumentGraphsDocumentGraphsCreated | CreateDocumentGraphsRevisionAlreadyExists

@dataclass(frozen=True, slots=True, kw_only=True)
class CreatePipelinesWithStepsInput:
    pipelines: tuple[PipelineGraphInput, ...]
    request_id: UUID

CREATE_PIPELINES_WITH_STEPS_PLAN_HASH: Final[str] = "a672e97dc400e84e91801d31f208d6e2e77b0f07bcad30c08063f03e71054ac6"
@dataclass(frozen=True, slots=True, kw_only=True)
class CreatePipelinesWithStepsPipelinesCreated:
    outcome: Literal["PipelinesCreated"] = field(default="PipelinesCreated", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreatePipelinesWithStepsStepAlreadyExists:
    outcome: Literal["StepAlreadyExists"] = field(default="StepAlreadyExists", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreatePipelinesWithStepsPipelineAlreadyExists:
    outcome: Literal["PipelineAlreadyExists"] = field(default="PipelineAlreadyExists", init=False)

CreatePipelinesWithStepsOutcome: TypeAlias = CreatePipelinesWithStepsPipelinesCreated | CreatePipelinesWithStepsStepAlreadyExists | CreatePipelinesWithStepsPipelineAlreadyExists

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateRestrictChildrenInput:
    children: tuple[RestrictChild, ...]
    request_id: UUID

CREATE_RESTRICT_CHILDREN_PLAN_HASH: Final[str] = "325122db5811aeefaa498bd3d8d0e05d2879ff4329a7c0fd2f1336cf018e66e1"
@dataclass(frozen=True, slots=True, kw_only=True)
class CreateRestrictChildrenChildAlreadyExists:
    outcome: Literal["ChildAlreadyExists"] = field(default="ChildAlreadyExists", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateRestrictChildrenRestrictParentRequired:
    outcome: Literal["RestrictParentRequired"] = field(default="RestrictParentRequired", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateRestrictChildrenRestrictChildrenCreated:
    outcome: Literal["RestrictChildrenCreated"] = field(default="RestrictChildrenCreated", init=False)

CreateRestrictChildrenOutcome: TypeAlias = CreateRestrictChildrenChildAlreadyExists | CreateRestrictChildrenRestrictParentRequired | CreateRestrictChildrenRestrictChildrenCreated

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateRestrictParentsInput:
    parents: tuple[RestrictParent, ...]
    request_id: UUID

CREATE_RESTRICT_PARENTS_PLAN_HASH: Final[str] = "477220cf0cb888cf4638de6e08dd3dc3ea2b8bb33230113430538833d78bee1c"
@dataclass(frozen=True, slots=True, kw_only=True)
class CreateRestrictParentsParentAlreadyExists:
    outcome: Literal["ParentAlreadyExists"] = field(default="ParentAlreadyExists", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class CreateRestrictParentsRestrictParentsCreated:
    outcome: Literal["RestrictParentsCreated"] = field(default="RestrictParentsCreated", init=False)

CreateRestrictParentsOutcome: TypeAlias = CreateRestrictParentsParentAlreadyExists | CreateRestrictParentsRestrictParentsCreated

@dataclass(frozen=True, slots=True, kw_only=True)
class DeleteRestrictParentsInput:
    tenant_id: UUID
    parent_ids: tuple[UUID, ...]
    request_id: UUID

DELETE_RESTRICT_PARENTS_PLAN_HASH: Final[str] = "708fdfb224ee8e0a76940ed05cca04efe4c0a7286d5334f24ed65941192b8cc7"
@dataclass(frozen=True, slots=True, kw_only=True)
class DeleteRestrictParentsParentMissing:
    outcome: Literal["ParentMissing"] = field(default="ParentMissing", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class DeleteRestrictParentsParentReferenced:
    outcome: Literal["ParentReferenced"] = field(default="ParentReferenced", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class DeleteRestrictParentsRestrictParentsDeleted:
    outcome: Literal["RestrictParentsDeleted"] = field(default="RestrictParentsDeleted", init=False)

DeleteRestrictParentsOutcome: TypeAlias = DeleteRestrictParentsParentMissing | DeleteRestrictParentsParentReferenced | DeleteRestrictParentsRestrictParentsDeleted

@dataclass(frozen=True, slots=True, kw_only=True)
class LogMetricsInput:
    metrics: tuple[Metric, ...]
    request_id: UUID

LOG_METRICS_PLAN_HASH: Final[str] = "3bb9a736befa62c07dc56c2c810b1c1fdd520052e4d8ca2f401b31caf2764633"
@dataclass(frozen=True, slots=True, kw_only=True)
class LogMetricsMetricsLogged:
    outcome: Literal["MetricsLogged"] = field(default="MetricsLogged", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class LogMetricsMetricAlreadyExists:
    outcome: Literal["MetricAlreadyExists"] = field(default="MetricAlreadyExists", init=False)

LogMetricsOutcome: TypeAlias = LogMetricsMetricsLogged | LogMetricsMetricAlreadyExists

@dataclass(frozen=True, slots=True, kw_only=True)
class WritePolicyMutationsInput:
    mutations: tuple[PolicyMutation, ...]
    request_id: UUID

WRITE_POLICY_MUTATIONS_PLAN_HASH: Final[str] = "23bc5e089a0f3f7dcb560f42517cc3eed3a337875c0c63384244b5139eb74083"
@dataclass(frozen=True, slots=True, kw_only=True)
class WritePolicyMutationsPolicyMutationExists:
    outcome: Literal["PolicyMutationExists"] = field(default="PolicyMutationExists", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class WritePolicyMutationsPolicyMutationsWritten:
    outcome: Literal["PolicyMutationsWritten"] = field(default="PolicyMutationsWritten", init=False)

WritePolicyMutationsOutcome: TypeAlias = WritePolicyMutationsPolicyMutationExists | WritePolicyMutationsPolicyMutationsWritten

@dataclass(frozen=True, slots=True, kw_only=True)
class WriteTuplesInput:
    tuples: tuple[FgaTuple, ...]
    request_id: UUID

WRITE_TUPLES_PLAN_HASH: Final[str] = "3b9b7645f44d2d3b10ebdd20b2635e0b771df9e2f72b13b715f28d7c47376a03"
@dataclass(frozen=True, slots=True, kw_only=True)
class WriteTuplesTuplesWritten:
    outcome: Literal["TuplesWritten"] = field(default="TuplesWritten", init=False)

@dataclass(frozen=True, slots=True, kw_only=True)
class WriteTuplesTupleAlreadyExists:
    outcome: Literal["TupleAlreadyExists"] = field(default="TupleAlreadyExists", init=False)

WriteTuplesOutcome: TypeAlias = WriteTuplesTuplesWritten | WriteTuplesTupleAlreadyExists

class AdapterBulkConformanceClient:
    def __init__(self, transport: SyncApplicationTransport, command_attempts: AttemptBudget) -> None:
        self._transport = transport
        self._command_attempts = command_attempts

    def get_fga_tuple(self, parameters: GetFgaTupleParams, options: QueryOptions = QueryOptions()) -> TypedQueryResult[GetFgaTupleResult]:
        raw = self._transport._execute_named_query(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            contract_bundle_hash=CONTRACT_BUNDLE_HASH, module_hash=QUERY_MODULE_HASH,
            query_name="GetFgaTuple", plan_hash=GET_FGA_TUPLE_QUERY_PLAN_HASH,
            parameters=encode_record(parameters), options=options,
        )
        outcomes = {
            "TupleFound": GetFgaTupleTupleFound,
            "TupleNotFound": GetFgaTupleTupleNotFound,
        }
        return raw._map_value(lambda value: decode_variant(outcomes, value))

    def create_document_graphs(self, input: CreateDocumentGraphsInput) -> TypedCommandResult[CreateDocumentGraphsOutcome]:
        if not 1 <= len(input.documents) <= 64:
            raise ValueError("invalid bounded collection length for CreateDocumentGraphs.documents")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreateDocumentGraphs", plan_hash=CREATE_DOCUMENT_GRAPHS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "DocumentAlreadyExists": CreateDocumentGraphsDocumentAlreadyExists,
            "DocumentGraphsCreated": CreateDocumentGraphsDocumentGraphsCreated,
            "RevisionAlreadyExists": CreateDocumentGraphsRevisionAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def create_document_graphs_batch(
        self, inputs: Sequence[CreateDocumentGraphsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreateDocumentGraphsOutcome]:
        for input in inputs:
            if not 1 <= len(input.documents) <= 64:
                raise ValueError("invalid bounded collection length for CreateDocumentGraphs.documents")
        return self._transport._command_batch(inputs, options, self.create_document_graphs, progress)

    def create_pipelines_with_steps(self, input: CreatePipelinesWithStepsInput) -> TypedCommandResult[CreatePipelinesWithStepsOutcome]:
        if not 1 <= len(input.pipelines) <= 64:
            raise ValueError("invalid bounded collection length for CreatePipelinesWithSteps.pipelines")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreatePipelinesWithSteps", plan_hash=CREATE_PIPELINES_WITH_STEPS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "PipelinesCreated": CreatePipelinesWithStepsPipelinesCreated,
            "StepAlreadyExists": CreatePipelinesWithStepsStepAlreadyExists,
            "PipelineAlreadyExists": CreatePipelinesWithStepsPipelineAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def create_pipelines_with_steps_batch(
        self, inputs: Sequence[CreatePipelinesWithStepsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreatePipelinesWithStepsOutcome]:
        for input in inputs:
            if not 1 <= len(input.pipelines) <= 64:
                raise ValueError("invalid bounded collection length for CreatePipelinesWithSteps.pipelines")
        return self._transport._command_batch(inputs, options, self.create_pipelines_with_steps, progress)

    def create_restrict_children(self, input: CreateRestrictChildrenInput) -> TypedCommandResult[CreateRestrictChildrenOutcome]:
        if not 1 <= len(input.children) <= 8:
            raise ValueError("invalid bounded collection length for CreateRestrictChildren.children")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreateRestrictChildren", plan_hash=CREATE_RESTRICT_CHILDREN_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "ChildAlreadyExists": CreateRestrictChildrenChildAlreadyExists,
            "RestrictParentRequired": CreateRestrictChildrenRestrictParentRequired,
            "RestrictChildrenCreated": CreateRestrictChildrenRestrictChildrenCreated,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def create_restrict_children_batch(
        self, inputs: Sequence[CreateRestrictChildrenInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreateRestrictChildrenOutcome]:
        for input in inputs:
            if not 1 <= len(input.children) <= 8:
                raise ValueError("invalid bounded collection length for CreateRestrictChildren.children")
        return self._transport._command_batch(inputs, options, self.create_restrict_children, progress)

    def create_restrict_parents(self, input: CreateRestrictParentsInput) -> TypedCommandResult[CreateRestrictParentsOutcome]:
        if not 1 <= len(input.parents) <= 8:
            raise ValueError("invalid bounded collection length for CreateRestrictParents.parents")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreateRestrictParents", plan_hash=CREATE_RESTRICT_PARENTS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "ParentAlreadyExists": CreateRestrictParentsParentAlreadyExists,
            "RestrictParentsCreated": CreateRestrictParentsRestrictParentsCreated,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def create_restrict_parents_batch(
        self, inputs: Sequence[CreateRestrictParentsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreateRestrictParentsOutcome]:
        for input in inputs:
            if not 1 <= len(input.parents) <= 8:
                raise ValueError("invalid bounded collection length for CreateRestrictParents.parents")
        return self._transport._command_batch(inputs, options, self.create_restrict_parents, progress)

    def delete_restrict_parents(self, input: DeleteRestrictParentsInput) -> TypedCommandResult[DeleteRestrictParentsOutcome]:
        if not 1 <= len(input.parent_ids) <= 8:
            raise ValueError("invalid bounded collection length for DeleteRestrictParents.parent_ids")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="DeleteRestrictParents", plan_hash=DELETE_RESTRICT_PARENTS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "ParentMissing": DeleteRestrictParentsParentMissing,
            "ParentReferenced": DeleteRestrictParentsParentReferenced,
            "RestrictParentsDeleted": DeleteRestrictParentsRestrictParentsDeleted,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def delete_restrict_parents_batch(
        self, inputs: Sequence[DeleteRestrictParentsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[DeleteRestrictParentsOutcome]:
        for input in inputs:
            if not 1 <= len(input.parent_ids) <= 8:
                raise ValueError("invalid bounded collection length for DeleteRestrictParents.parent_ids")
        return self._transport._command_batch(inputs, options, self.delete_restrict_parents, progress)

    def log_metrics(self, input: LogMetricsInput) -> TypedCommandResult[LogMetricsOutcome]:
        if not 1 <= len(input.metrics) <= 128:
            raise ValueError("invalid bounded collection length for LogMetrics.metrics")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="LogMetrics", plan_hash=LOG_METRICS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "MetricsLogged": LogMetricsMetricsLogged,
            "MetricAlreadyExists": LogMetricsMetricAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def log_metrics_batch(
        self, inputs: Sequence[LogMetricsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[LogMetricsOutcome]:
        for input in inputs:
            if not 1 <= len(input.metrics) <= 128:
                raise ValueError("invalid bounded collection length for LogMetrics.metrics")
        return self._transport._command_batch(inputs, options, self.log_metrics, progress)

    def write_policy_mutations(self, input: WritePolicyMutationsInput) -> TypedCommandResult[WritePolicyMutationsOutcome]:
        if not 1 <= len(input.mutations) <= 100:
            raise ValueError("invalid bounded collection length for WritePolicyMutations.mutations")
        aggregate_element_bytes = sum(canonical_value_encoded_length(encode_value(item, PolicyMutation)) for item in input.mutations)
        if aggregate_element_bytes > 900000:
            raise ValueError("invalid aggregate collection bytes for WritePolicyMutations.mutations")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="WritePolicyMutations", plan_hash=WRITE_POLICY_MUTATIONS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "PolicyMutationExists": WritePolicyMutationsPolicyMutationExists,
            "PolicyMutationsWritten": WritePolicyMutationsPolicyMutationsWritten,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def write_policy_mutations_batch(
        self, inputs: Sequence[WritePolicyMutationsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[WritePolicyMutationsOutcome]:
        for input in inputs:
            if not 1 <= len(input.mutations) <= 100:
                raise ValueError("invalid bounded collection length for WritePolicyMutations.mutations")
            aggregate_element_bytes = sum(canonical_value_encoded_length(encode_value(item, PolicyMutation)) for item in input.mutations)
            if aggregate_element_bytes > 900000:
                raise ValueError("invalid aggregate collection bytes for WritePolicyMutations.mutations")
        return self._transport._command_batch(inputs, options, self.write_policy_mutations, progress)

    def write_tuples(self, input: WriteTuplesInput) -> TypedCommandResult[WriteTuplesOutcome]:
        if not 1 <= len(input.tuples) <= 128:
            raise ValueError("invalid bounded collection length for WriteTuples.tuples")
        raw = self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="WriteTuples", plan_hash=WRITE_TUPLES_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "TuplesWritten": WriteTuplesTuplesWritten,
            "TupleAlreadyExists": WriteTuplesTupleAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    def write_tuples_batch(
        self, inputs: Sequence[WriteTuplesInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[WriteTuplesOutcome]:
        for input in inputs:
            if not 1 <= len(input.tuples) <= 128:
                raise ValueError("invalid bounded collection length for WriteTuples.tuples")
        return self._transport._command_batch(inputs, options, self.write_tuples, progress)

class AsyncAdapterBulkConformanceClient:
    def __init__(self, transport: AsyncApplicationTransport, command_attempts: AttemptBudget) -> None:
        self._transport = transport
        self._command_attempts = command_attempts

    async def get_fga_tuple(self, parameters: GetFgaTupleParams, options: QueryOptions = QueryOptions()) -> TypedQueryResult[GetFgaTupleResult]:
        raw = await self._transport._execute_named_query(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            contract_bundle_hash=CONTRACT_BUNDLE_HASH, module_hash=QUERY_MODULE_HASH,
            query_name="GetFgaTuple", plan_hash=GET_FGA_TUPLE_QUERY_PLAN_HASH,
            parameters=encode_record(parameters), options=options,
        )
        outcomes = {
            "TupleFound": GetFgaTupleTupleFound,
            "TupleNotFound": GetFgaTupleTupleNotFound,
        }
        return raw._map_value(lambda value: decode_variant(outcomes, value))

    async def create_document_graphs(self, input: CreateDocumentGraphsInput) -> TypedCommandResult[CreateDocumentGraphsOutcome]:
        if not 1 <= len(input.documents) <= 64:
            raise ValueError("invalid bounded collection length for CreateDocumentGraphs.documents")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreateDocumentGraphs", plan_hash=CREATE_DOCUMENT_GRAPHS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "DocumentAlreadyExists": CreateDocumentGraphsDocumentAlreadyExists,
            "DocumentGraphsCreated": CreateDocumentGraphsDocumentGraphsCreated,
            "RevisionAlreadyExists": CreateDocumentGraphsRevisionAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def create_document_graphs_batch(
        self, inputs: Sequence[CreateDocumentGraphsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreateDocumentGraphsOutcome]:
        for input in inputs:
            if not 1 <= len(input.documents) <= 64:
                raise ValueError("invalid bounded collection length for CreateDocumentGraphs.documents")
        return await self._transport._command_batch(inputs, options, self.create_document_graphs, progress)

    async def create_pipelines_with_steps(self, input: CreatePipelinesWithStepsInput) -> TypedCommandResult[CreatePipelinesWithStepsOutcome]:
        if not 1 <= len(input.pipelines) <= 64:
            raise ValueError("invalid bounded collection length for CreatePipelinesWithSteps.pipelines")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreatePipelinesWithSteps", plan_hash=CREATE_PIPELINES_WITH_STEPS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "PipelinesCreated": CreatePipelinesWithStepsPipelinesCreated,
            "StepAlreadyExists": CreatePipelinesWithStepsStepAlreadyExists,
            "PipelineAlreadyExists": CreatePipelinesWithStepsPipelineAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def create_pipelines_with_steps_batch(
        self, inputs: Sequence[CreatePipelinesWithStepsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreatePipelinesWithStepsOutcome]:
        for input in inputs:
            if not 1 <= len(input.pipelines) <= 64:
                raise ValueError("invalid bounded collection length for CreatePipelinesWithSteps.pipelines")
        return await self._transport._command_batch(inputs, options, self.create_pipelines_with_steps, progress)

    async def create_restrict_children(self, input: CreateRestrictChildrenInput) -> TypedCommandResult[CreateRestrictChildrenOutcome]:
        if not 1 <= len(input.children) <= 8:
            raise ValueError("invalid bounded collection length for CreateRestrictChildren.children")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreateRestrictChildren", plan_hash=CREATE_RESTRICT_CHILDREN_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "ChildAlreadyExists": CreateRestrictChildrenChildAlreadyExists,
            "RestrictParentRequired": CreateRestrictChildrenRestrictParentRequired,
            "RestrictChildrenCreated": CreateRestrictChildrenRestrictChildrenCreated,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def create_restrict_children_batch(
        self, inputs: Sequence[CreateRestrictChildrenInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreateRestrictChildrenOutcome]:
        for input in inputs:
            if not 1 <= len(input.children) <= 8:
                raise ValueError("invalid bounded collection length for CreateRestrictChildren.children")
        return await self._transport._command_batch(inputs, options, self.create_restrict_children, progress)

    async def create_restrict_parents(self, input: CreateRestrictParentsInput) -> TypedCommandResult[CreateRestrictParentsOutcome]:
        if not 1 <= len(input.parents) <= 8:
            raise ValueError("invalid bounded collection length for CreateRestrictParents.parents")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="CreateRestrictParents", plan_hash=CREATE_RESTRICT_PARENTS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "ParentAlreadyExists": CreateRestrictParentsParentAlreadyExists,
            "RestrictParentsCreated": CreateRestrictParentsRestrictParentsCreated,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def create_restrict_parents_batch(
        self, inputs: Sequence[CreateRestrictParentsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[CreateRestrictParentsOutcome]:
        for input in inputs:
            if not 1 <= len(input.parents) <= 8:
                raise ValueError("invalid bounded collection length for CreateRestrictParents.parents")
        return await self._transport._command_batch(inputs, options, self.create_restrict_parents, progress)

    async def delete_restrict_parents(self, input: DeleteRestrictParentsInput) -> TypedCommandResult[DeleteRestrictParentsOutcome]:
        if not 1 <= len(input.parent_ids) <= 8:
            raise ValueError("invalid bounded collection length for DeleteRestrictParents.parent_ids")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="DeleteRestrictParents", plan_hash=DELETE_RESTRICT_PARENTS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "ParentMissing": DeleteRestrictParentsParentMissing,
            "ParentReferenced": DeleteRestrictParentsParentReferenced,
            "RestrictParentsDeleted": DeleteRestrictParentsRestrictParentsDeleted,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def delete_restrict_parents_batch(
        self, inputs: Sequence[DeleteRestrictParentsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[DeleteRestrictParentsOutcome]:
        for input in inputs:
            if not 1 <= len(input.parent_ids) <= 8:
                raise ValueError("invalid bounded collection length for DeleteRestrictParents.parent_ids")
        return await self._transport._command_batch(inputs, options, self.delete_restrict_parents, progress)

    async def log_metrics(self, input: LogMetricsInput) -> TypedCommandResult[LogMetricsOutcome]:
        if not 1 <= len(input.metrics) <= 128:
            raise ValueError("invalid bounded collection length for LogMetrics.metrics")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="LogMetrics", plan_hash=LOG_METRICS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "MetricsLogged": LogMetricsMetricsLogged,
            "MetricAlreadyExists": LogMetricsMetricAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def log_metrics_batch(
        self, inputs: Sequence[LogMetricsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[LogMetricsOutcome]:
        for input in inputs:
            if not 1 <= len(input.metrics) <= 128:
                raise ValueError("invalid bounded collection length for LogMetrics.metrics")
        return await self._transport._command_batch(inputs, options, self.log_metrics, progress)

    async def write_policy_mutations(self, input: WritePolicyMutationsInput) -> TypedCommandResult[WritePolicyMutationsOutcome]:
        if not 1 <= len(input.mutations) <= 100:
            raise ValueError("invalid bounded collection length for WritePolicyMutations.mutations")
        aggregate_element_bytes = sum(canonical_value_encoded_length(encode_value(item, PolicyMutation)) for item in input.mutations)
        if aggregate_element_bytes > 900000:
            raise ValueError("invalid aggregate collection bytes for WritePolicyMutations.mutations")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="WritePolicyMutations", plan_hash=WRITE_POLICY_MUTATIONS_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "PolicyMutationExists": WritePolicyMutationsPolicyMutationExists,
            "PolicyMutationsWritten": WritePolicyMutationsPolicyMutationsWritten,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def write_policy_mutations_batch(
        self, inputs: Sequence[WritePolicyMutationsInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[WritePolicyMutationsOutcome]:
        for input in inputs:
            if not 1 <= len(input.mutations) <= 100:
                raise ValueError("invalid bounded collection length for WritePolicyMutations.mutations")
            aggregate_element_bytes = sum(canonical_value_encoded_length(encode_value(item, PolicyMutation)) for item in input.mutations)
            if aggregate_element_bytes > 900000:
                raise ValueError("invalid aggregate collection bytes for WritePolicyMutations.mutations")
        return await self._transport._command_batch(inputs, options, self.write_policy_mutations, progress)

    async def write_tuples(self, input: WriteTuplesInput) -> TypedCommandResult[WriteTuplesOutcome]:
        if not 1 <= len(input.tuples) <= 128:
            raise ValueError("invalid bounded collection length for WriteTuples.tuples")
        raw = await self._transport._execute_command(
            contract_lineage=CONTRACT_LINEAGE, contract_version=CONTRACT_VERSION,
            command_name="WriteTuples", plan_hash=WRITE_TUPLES_PLAN_HASH,
            input=encode_record(input), attempts=self._command_attempts,
        )
        outcomes = {
            "TuplesWritten": WriteTuplesTuplesWritten,
            "TupleAlreadyExists": WriteTuplesTupleAlreadyExists,
        }
        return raw._map_outcome(lambda value: decode_variant(outcomes, value))

    async def write_tuples_batch(
        self, inputs: Sequence[WriteTuplesInput], options: CommandBatchOptions,
        progress: Callable[[CommandBatchProgress], None] | None = None,
    ) -> CommandBatchResult[WriteTuplesOutcome]:
        for input in inputs:
            if not 1 <= len(input.tuples) <= 128:
                raise ValueError("invalid bounded collection length for WriteTuples.tuples")
        return await self._transport._command_batch(inputs, options, self.write_tuples, progress)
