query ListDraftDocuments($organization_id: Document.organization_id) {
    many documents from Document
        where organization_id == $organization_id
          && state == DocumentState.Draft
        order by document_id asc
        take 25

    return Found {
        documents: documents { document_id title visibility state }
    }

    outcomes Found
}
