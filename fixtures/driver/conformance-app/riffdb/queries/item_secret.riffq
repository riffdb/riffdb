query ItemSecret(
    $organization_id: Item.organization_id,
    $item_id: Item.item_id,
) {
    one item from Item
        where organization_id == $organization_id && item_id == $item_id
        else NotFound

    return Found {
        secret: item {
            token_digest reveals item.token_digest
        }
    }

    outcomes Found | NotFound
}
