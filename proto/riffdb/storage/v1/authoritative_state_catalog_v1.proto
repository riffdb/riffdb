syntax = "proto3";

package riffdb.storage.v1;

// ADR-0186's closed catalog identity, not an editable inventory or configuration.
message StoredAuthoritativeStateCatalogV1 {
  bytes catalog_digest = 1;
}
