#!/usr/bin/env bash
# Historical ADR-0185 proof name retained after the in-memory backend removal.
# req: DEP-005, OQ-114, OQ-115
set -euo pipefail

exec "$(dirname "$0")/expansion_executor_redb_parity_matches_independent_oracle"
