#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_root"

./scripts/generate-archive-receipt-fixtures --check
exec ./scripts/generate-durable-format-manifest --check
